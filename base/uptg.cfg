// Blob G A/B: X produce without skinned decode compiled in.
// Skinned hits fail closed. Leave with exec uptx (pins 0).
exec uptx
r_pathTracingUnifiedPtGeometryNoSkinned 1
echo UPT G: X produce noskinned. Confirm noSkinned=1 and produce_noskinned blob. Leave: exec uptx
