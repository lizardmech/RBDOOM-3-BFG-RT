Geometry modernization - ordered execution plan
================================================
Status: controlling phase plan, 2026-07-23.

OBJECTIVE
---------
Replace the three coexisting PT geometry routes with a measured, incremental
system built around shared immutable mesh records, per-render-world instances,
per-instance skinned outputs, and portal-area static buckets. Preserve all
current shading, motion, material, emissive, and transient consumers while each
producer migrates.

ORDER AND DEPENDENCIES
----------------------
Execute the tickets in tasks/ in this order:

  PHASE A - freeze evidence and remove known correctness uncertainty
    GEO-00 runtime baseline and reproducible artifacts
      -> GEO-01 comm1 PT-only static contract instrumentation
      -> GEO-02 narrow comm1 static fix
      -> GEO-03 GPU-skinning observability and parity

  PHASE B - establish the common identity/ownership/storage foundation
    GEO-04 canonical mesh/instance ABI and consumer map
      -> GEO-05 per-world lifecycle shadow registry
      -> GEO-06 pooled mesh ingestion and shared-rigid compare route

GEO-04 status 2026-07-23: contract/passive-helper slice complete. Fixed-width
full-tuple keys, collision-aware comparison, checked uint64 storage arithmetic,
uint32 page narrowing and primary/subview HistoryOwner separation are covered
by PathTraceCanonicalIdentityHarness. Authoritative WorldKey production moves
to GEO-05; source asset generation/topology production moves to GEO-06. No live
route consumes the new helpers yet.

  PHASE C - migrate true-deforming geometry first
    GEO-07 authoritative jointCache GPU skinning
      -> GEO-08 per-instance update/refit BLAS
      -> GEO-09 temporal/material/emissive integration and skinned promotion

GEO-07 status 2026-07-25: slices 1-3 are accepted behind the default-off
master gate. geometry_geo39 accepts live renderer jointCache range transport,
canonical instance/history ownership, and retained GEO-03 current/previous
numeric parity. geometry_geo40 accepts checked compact-copy planning with exact
8/8 range accounting and 17088 contiguous bytes. geometry_geo41 accepts slice
3's structural GPU copy (23 ranges and 50112 bytes exact) but rejects the old
late mutable CPU oracle. geometry_geo42 closes that ambiguity: all 15 upload
snapshots were comparable, 13 late CPU sources had changed, all 15 jointCache
ranges and 32640 bytes submitted exactly, and 24 current/previous/motion samples
passed at 0.0000152587891 maximum error. Slice 4 now replaces the enabled
global last-view previous bridge with bounded full-key PrimaryGameplay owner
states and makes subviews read/write no history. geometry_geo43_auto accepts
the primary -> PDA -> primary transition: the owner tuple remained 2/2/1/1,
12/12 records matched before and after, serials advanced from 28/29 to
651/652, and both parity summaries passed at 0.0000152587891 maximum error.
The PDA is a 2D overlay in this build, however, so it did not schedule an
isSubview PT build and is not subview evidence. The focused
PathTraceSkinnedHistoryPolicyHarness supplies isSubview=true while deliberately
making a matching primary state available; the live selection helper returns
primary/ownerValid/stateFound/read/write=0/0/0/0/0 and the
subview-or-invalid-no-history route. All seven primary, subview, invalid,
mismatch, and legacy cases pass. Slice 4 is accepted. A real mirror/portal is
still useful scheduling coverage but is no longer a policy blocker. CPU
pre-skinning and legacy BLAS traversal remain authoritative.

The runtime loop no longer needs manual press-start or console input. The
shell defaults through local-user registration to Doom 3, and
`+set com_autoLoadSave doom3_1` queues the prepared save after 120 frames.
geometry_geo53_auto confirms autonomous load into game/mars_city1 and a clean
first-observation GEO-07 dump: 12/12 dispatches, 12 exact jointCache ranges
covering 28320 bytes, owner 2/2/1/1, history 1/1/1/0/0/1 with serial 0/1, and
24 parity samples passing at 0.0000152587891 maximum current error.

Slice-5 entry audit corrected one proposal mismatch: the accepted GEO-06
source registry originally rejected SkinnedBindSource/Skinned and the GPU
scaffold still repacked animated tri->verts per frame. Slice 5A is accepted.
The source producer is the MD5 load-time deformInfo bind geometry, not the
animated snapshot; exact model-surface counts and topology must match the
canonical MeshKey before the registry value-copies it. geometry_geo55_auto
resolves all 12 active skinned InstanceKeys through 12 exact bindings to 11
unique immutable source records, with one shared-source instance, nonzero
checksums for all 12 resolutions, zero skinned negatives, and clean joint/
parity results.

Slice 5B is also accepted in shadow mode. The checked allocator owns ranges by
the full InstanceKey, appends replacements instead of aliasing retired bytes,
tracks monotonic storage generations, and requires explicit completion before
old storage generations can be released. Exact identity Remove deltas retire
exact keys. The CPU harness covers stable reuse, resize, render-def generation
reuse, world replacement, growth, explicit in-flight release, overflow and
atomic capacity rejection. geometry_geo57_auto plans 12/12 exact ranges for
9889 vertices, with zero rejection/overlap; the one shared immutable-source
pair is non-aliased. Current dispatch buffers and BLAS traversal are unchanged.
Shared MeshKey ownership never permits shared pose, history, output, or later
BLAS state.

Slice 5C is now accepted for the mode-1 comparison route. With both canonical
gates enabled, geometry_geo58_auto resolves 12/12 source records and output
ranges, packs 11 unique sources with one reuse, dispatches all 12 records, and
passes 24 current/previous/motion samples at 0.0000152587891 maximum error.
Source upload falls from the legacy 9889 per-instance vertices to 6839 unique
vertices. geometry_geo60_auto explicitly proves the two 3050-vertex
Fatzombies read the same source offset 3651 while writing distinct output
offsets 3651 and 6701. geometry_geo59_auto disables the source-registry gate,
selects the legacy 9889/9889 comparison layout, and retains exact parity.

The slice-6 entry audit rejects CPU pre-skin removal as an independent GEO-07
change. AppendSmokeSurfaceGeometry currently produces the only world-space
skinned vertex stream consumed by the merged dynamic BLAS. The accepted
mode-1 output is a separate ShaderResource buffer and is not referenced by any
BLAS. Skipping the CPU transform before changing traversal would therefore
feed bind-pose/invalid positions to the live BLAS. Feeding the new output to a
BLAS is exactly the GEO-08 ownership change that the GEO-07 guardrail defers.

The order is corrected as follows:

  1. GEO-07 is accepted through the validated GPU source/joint/output producer
     and exact failure funnel. Full CPU capture remains the rollback/reference
     path.
  2. GEO-08 first adds per-instance updateable BLAS state and comparison TLAS
     routing from the accepted output ranges, without removing the merged
     dynamic path.
  3. Only after GEO-08 proves compute -> barrier -> BLAS build/update -> TLAS
     traversal and hit/material/motion parity may eligible surfaces leave the
     merged dynamic capture and skip full CPU pre-skinning.
  4. Structural ineligibility must be known before capture and stays on the CPU
     route. A failure discovered after CPU capture has been skipped cannot
     truthfully claim same-frame CPU fallback; its explicit fail-safe must be a
     validated prior-pose hold or route suppression, never bind-pose output.

This is a dependency correction, not a relaxation of the Phase C gate. CPU
pre-skin removal remains required before Phase C completes, but it lands with
the GEO-08 traversal cutover rather than before it.

GEO-08 slice 1 is accepted as a behavior-neutral state contract. The pure
PtSkinnedBlasStateTable keys every record by full SkinnedSurface InstanceKey,
requires exact skinned MeshKey/source/index-pool/output-buffer inputs, and
emits build, update, rebuild, failed and retiring states without owning GPU
objects. Stable contracts request update; topology/source/index-pool/output
generation or range changes, failed submissions, explicit force, and the fixed
cadence request rebuild. Pending work is invalidated when its storage contract
changes. Exact removal and world replacement retire records behind explicit
completion tokens. PathTraceSkinnedBlasStateHarness passes all transition,
bounds, generation, failure recovery, non-aliasing and retirement cases.
No live scene route consumes this table yet. Slice 2 is physical output-buffer
generation ownership plus shadow observation from the accepted GEO-07 jobs.

GEO-08 slice 2 is accepted as a behavior-neutral live shadow integration.
Every nonzero output allocator generation now maps to a generation-tagged
physical UAV/acceleration-build-input buffer, and exact reuse is forbidden
across generation changes. The committed generation is retained with replaced
scene packages. Only finalized dispatchedGpu jobs with checked GEO-06 source
index ranges and checked GEO-07 output ranges enter the BLAS state table.
geometry_geo62_auto proves 12/12 exact observations, twelve initial
build-pending records, zero rejection, exact 1,835,008-byte physical storage,
and unchanged 24-sample skinning parity. It creates no BLAS and changes no
TLAS traversal.

GEO-08 slice 3 accepts the explicit completion seam. Retired scene packages
own NVRHI event queries armed immediately after the main graphics command list
submission. On Vulkan, the query captures the queue's timeline-semaphore
submission ID; release polls that exact ID. r_pathTracingSceneRetireFrames is
now only an optional minimum delay after GPU completion. With the delay set to
zero, geometry_geo64_auto arms token 1 with one pending package and releases it
only after token 1 completes, leaving zero pending. The same run retains the
12/12 exact shadow contract and parity pass.

GEO-08 slice 4 accepts initial per-instance comparison BLAS construction.
geometry_geo65_auto maps twelve exact BuildPending records to twelve distinct
retained resources, records twelve AllowUpdate + PreferFastBuild initial
builds with zero failure, and explicitly excludes them from every TLAS. The
same run retains the 24-sample skinning parity pass. Slice 5 owns PerformUpdate,
cadence/source/output rebuild, and completion-owned replacement retirement.
Physical output growth and generation release still require a targeted runtime
exercise before traversal cutover.

GEO-08 slice 5A accepts stable-contract update and cadence rebuild.
geometry_geo68_auto reuses twelve exact resources for twelve PerformUpdate
submissions on frame 3, then performs twelve full rebuilds on the same handles
at the 60-frame cadence on frame 63. Both actions report zero deferral and zero
failure; parity remains green and TLAS remains excluded. Changed-contract
replacement is deliberately deferred until slice 5B supplies event-query-
protected old-owner retirement and exact state-retirement-token release.

GEO-08 slice 5B accepts changed-contract replacement, exact removal, and
physical output-generation retirement. geometry_geo69_auto queues eleven
removed owners with state-retirement packages, arms event tokens 1..11, and
releases both owners and state tokens only after token 11 completes.
geometry_geo71_auto forces output growth from generation 1 / 1,835,008 bytes
to generation 2 / 3,670,016 bytes by spawning three Fatzombies. Twelve existing
BLAS resources are replaced, all twelve retained packages arm and complete,
and only afterward is allocator generation 1 actually released. The final
15/15 exact shadow observation and 24-sample parity pass remain green, with
zero deferral, build failure, fatal, device-removal, NVRHI-error, or validation
error. Step 5 is therefore complete. Step 6, the canonical per-surface shader
hit-routing contract, is next; no comparison BLAS may enter a TLAS yet.

GEO-08 step 6A accepts the pure source-local hit-route plan and behavior-neutral
live shadow. The audit found that immutable-source BLAS PrimitiveIndex and the
legacy merged-dynamic PrimitiveIndex are not safely interchangeable because
legacy capture can compact posed zero-area triangles. The new planner treats
source topology as authoritative, maps legacy indexes only as an ordered
subsequence, and retains explicit per-source-primitive material, class,
canonical primitive, and emissive identity metadata plus exact current and
previous vertex ranges. geometry_geo72_auto accepts 12/12 records in disjoint
shader slots 113..124, 15,962/15,962 source-to-legacy triangle mappings, 12/12
motion ranges, zero source-only primitives in this scene, and zero route or
identity rejection/collision. Seven focused harnesses pass, including the
synthetic source-only primitive case. Step 6B is next: define/upload the GPU
route ABI and replace the current shader-wide `InstanceID >= 2 means rigid`
assumption across every active consumer. TLAS insertion remains forbidden.

GEO-08 step 6B1 accepts the GPU route-table ABI and persistent binding seam.
The 80-byte route and 36-byte source-triangle records are always bound at
t18/t19, with a zero-count sentinel for an empty table and logical counts in
record zero. The buffers follow scene-package lifetime/transition ownership.
geometry_geo73_auto and geometry_geo74_auto read back 12 routes and 15,962
triangles (575,592 bytes) exactly, including the header, with first InstanceID
113 and signature de825e58306bf8f1. Every affected Vulkan pipeline initializes
and traversal remains unchanged. Their mode-1 frame-1 skinning parity dump is
not oracle-backed and is excluded from acceptance; geometry_geo72_auto remains
the accepted mode-2 numeric proof. Step 6B2 is next: bind the geometry domains
needed by skinned hit shading and convert every rigid-range assumption before
step 7 can add comparison TLAS descriptors.

GEO-08 step 6B2 has an offline partial checkpoint. Vulkan bindings now carry
the canonical source-index, current-output, and previous-position domains at
t28/t29/t32. The main primary/shadow library, clean-DI transmission and spatial
libraries, ReGIR/NEE debug libraries, and motion/material consumers compile
with disjoint rigid and skinned ranges. The skinning producer contract was
rechecked: current output positions/normals/tangents and retained previous
positions are already world-space, so skinned hit paths must not apply
ObjectToWorldKHR a second time. Rigid paths instead use their explicit route
transform rows.

The clean-DI sentinel and clean-GI libraries are compiler-budget exceptions,
not accepted traversal consumers. Their unchanged checkpoint SPIR-V modules
are already 5,929,604 and 9,385,432 bytes. Full skinned reconstruction pushes
DXC past the SPIR-V ID limit. They therefore use a lightweight route include
that preserves baseline code generation while keeping t28/t29/t32 layouts
ready. The full Vulkan preset build passes, but no runtime proof was attempted
after the prior broad-log run destabilized the Windows display stack. Step 6B2
is not complete and step 7 remains blocked until the two oversized libraries
are split or an equivalently narrow consumer boundary compiles, followed by a
bounded user-run validation.

GEO-08 step 6B3 removes the clean-DI half of that compiler blocker offline.
A compact 17,196-byte hit-only Vulkan module preserves the sentinel payload
and material-visibility contract, and NVRHI appends its primary/shadow groups
at SBT records 2/3 while legacy records remain 0/1. SPIR-V validation,
entry-point/binding disassembly, and the release build pass; no TLAS descriptor
selects the new records yet. Clean-GI now has an equivalent 172,820-byte
compact module with its full alpha, translucency, and liquid-pool candidate
behavior. Vulkan validation, entry-point/binding/constant-offset disassembly,
and the release build pass while both monolithic GI blobs remain byte-exact.
Step 7 remains blocked pending bounded mixed-pipeline runtime creation proof
and exact offline SBT-contribution assertions.

The contribution planner/harness now passes, but the first mixed-module runtime
gate failed: eager clean-GI pipeline construction starved Windows input even
though the process and operating system remained alive. D36 supersedes that
build with sixteen source-partitioned Vulkan raygen pipelines per
debug/production variant, a fifteen-frame inter-build gap, and one yielding
deferred-host-operation joiner. A default-0 cvar permits only explicitly
bounded incremental tests. The 32/32 exact SPIR-V validation gate and release
build pass. geometry_geo75/76 show responsive execution but no GI pipeline
entry, so the next build adds one-shot scheduler/input-gate evidence. Runtime
acceptance, step 7, and comparison TLAS insertion remain blocked until user
runs at limits 1 through 16 prove responsive warmup.

geometry_geo78 through geometry_geo83 complete the bounded production warmup
through 16/16 without reproducing Windows input starvation. This accepts the
source-partitioned construction shape for responsiveness, not performance:
individual construction gaps reach 47 seconds and first full dispatch reaches
92 seconds. geometry_geo84 then localizes black rigid materials to the existing
source3 rigid TLAS route, not GI. The rigid plan had discarded per-instance
material identity while sharing a material-independent canonical mesh/BLAS.
The offline replacement carries material through the plan and makes all rigid
hit consumers use per-instance route material fields. D38 then corrects the
shared rigid-range predicate to read each consumer's live count field.
geometry_geo89 plus the final canonical-route-1 visual confirmation accept
both repairs; pipeline warmup and rigid materials no longer block step 7.

GEO-08 step 7A is structurally accepted at runtime. All ray pipelines
sharing the TLAS own four ordered records: legacy primary/shadow 0/1 and
skinned primary/shadow 2/3. A pure all-or-nothing planner validates the prior
CPU route, rebased shader upload, exact live resource/state/BLAS contract, both
SBT selections, and the 512-entry TLAS capacity. The default-off live gate adds
identity-transform, mask-0x02, contribution-2 descriptors only when the whole
tuple is exact and retains the merged dynamic BLAS. Harness and Vulkan release
build pass without shader changes. geometry_geo91_auto admits 12/12 exact
comparison descriptors at IDs 113..124, with all failure buckets zero and TLAS
occupancy 125/512; twelve exact updates and the frame-63 cadence rebuild also
pass. The accepted clean-DI/GI-off routed-only A/B then keeps rigid props in
geometry_geo94_off while adding the seated NPC and both Fatzombies only in
geometry_geo93_on. The comparison adds 338,548 non-miss pixels with coherent
posed geometry, normals, and materials. Exact 12-record / 15,962-triangle
readback plus 12/12 motion-ready routes close source primitive, material,
emissive, and motion-input transport. Step 7 is accepted and step 8 may now
begin the fail-closed CPU-capture split.

GEO-08 steps 8 and 9 accept the default-off CPU-capture split and its explicit
failure timing. Admission is keyed by the exact sorted visible skinned-instance
set and consumes only a previously TLAS-accepted build for that same signature.
Known structural failures remain on the CPU route with named counters; omitted
surfaces carry source-only primitive metadata and no legacy dynamic ranges.
Retained previous joint matrices preserve the previous-pose GPU route. Any
late route/resource/BLAS/TLAS failure suppresses and revokes only the affected
set instead of pretending CPU vertices exist.

geometry_geo102_auto and geometry_geo105_auto repeat the accepted main-view
tuple: 12 omitted surfaces, 9,889 vertices and 47,886 indexes removed from CPU
capture, zero CPU skin append, 12/12 routes/descriptors, and 12/12 motion-ready.
The auxiliary exact set independently routes 1/1/1 and leaves the other eleven
surfaces on named CPU fallback. The earlier union-cache probe is explicitly
rejected because geometry_geo99_auto caught 12 omissions but only one route
and descriptor. Step 10 lifecycle invalidation and retirement is next.

GEO-08 step 10 is accepted with a corrected admission/upload contract.
Accepted source-only routes remain the capture-time proof for the exact live
source/output/BLAS set; frame-local source-only material metadata may refresh
without forcing alternating CPU bootstrap frames. The current upload must
either match every omitted InstanceKey one-for-one or cover the complete
current set as a source-only pre-admission build, and must pass normal TLAS
admission. CPU-retained extras in a complete pre-admission build enter the
TLAS at mask zero for that frame, so they cannot double-trace. CPU-omitted
surface materials supplement the shared table without extending the legacy
dynamic triangle-index ABI.

geometry_geo113_auto proves physical 12 -> 15 growth, completion-owned BLAS
replacement/output-generation release, despawn retirement, and cold map
reload. geometry_geo115_auto proves offscreen/return and forced one-entry
cache eviction. The rebuilt geometry_geo116_auto repeats growth, four forced
evictions, despawn/respawn, zero-candidate visibility, and map teardown/reload.
Its 15-surface set remains CPU-omitted for 115 logged frames with no repeated
signature upgrade, omitted-set mismatch, pre-capture invalidation, late
revocation, device failure, or crash. The BFG PDA is the already-proven 2D
overlay and does not create a PT route set.

GEO-08 step 11 clone scaling is accepted by geometry_geo122_auto and
geometry_geo123_auto. The 21-route set first stays live while two new surfaces
use CPU fallback, then a complete 23-route source-only build is pre-admitted
with 21 active descriptors and two mask-zero descriptors. The following frame
has 23 active descriptors and omits all 43,439 vertices / 216,054 indexes,
thereby crossing the old 196,608-index cap without a full-CPU bootstrap.
The stable run records 173 consecutive 23/23 omission frames and normal
shutdown. The one-shot ownership dump reports 11 unique immutable sources,
23 exact mutable ranges and dispatches, 78/78 shared-source pairs non-aliased,
zero output overlaps, 23 active BLASes, contiguous TLAS IDs 113..135, and zero
TLAS admission failures.

GEO-08 step 12 is accepted by geometry_geo124_auto and
geometry_geo125_auto. A bounded diagnostic uses disjoint Vulkan timer queries
for the per-instance skinned BLAS batch and merged dynamic BLAS, with delayed
readback and microsecond CPU-skin accounting. The same 12 surfaces /
47,886 indexes were sampled for 20 frames in each capture-split state:

  metric                         split off median   split on median
  CPU skin append                4,830 us           0 us
  merged/residual dynamic BLAS   1,076.2 us         137.0 us
  12 per-instance BLAS updates   53.5 us            68.1 us

The active update distribution has deliberate GPU scheduling outliers
(20-frame mean 212.2 us, p95 814.1 us), so medians and percentiles are
retained rather than presenting a single best sample. The 70-frame cadence
run records one 12-BLAS initial build at 2,655.9 us, 67 update/reuse frames,
and one bounded 12-BLAS rebuild at frame 63 / 2,516.9 us. Both runs reach
normal shutdown without device failure. GEO-08 is complete; GEO-09 temporal,
material, and emissive promotion is the next Phase C ticket.

GEO-09 step 1 is accepted by geometry_geo128_auto. A default-zero one-shot
audits the live source-only CPU/upload/TLAS route and an independent same-frame
legacy-backed shadow without changing traversal or lighting. Both views cover
12 routes, 15,962 triangles, and 12/12 motion-ready ranges. The shadow maps
all 15,962 source-local triangles to the retained merged-dynamic material ID,
material index, and class tuple. CPU/GPU upload, TLAS, material-table, current/
previous range, primitive identity, and emissive identity checks have zero in
all thirteen failure buckets. The entry audit confirms that live emissive
  harvesting still explicitly skips skinned triangles.

GEO-09 steps 2/3 are accepted by geometry_geo134_auto. Debug mode 58 compacts
57,600 same-ray legacy/canonical pairs from the 2560x1440 prepared animated
scene. The exact renderer joint-cache snapshot first passes 24/24 GPU-skinning
samples at 0.0000152587891 maximum error. The ray audit observes 5,264
canonical skinned hits, classifies 397 closer legacy static occluders, and
maps all remaining 4,867 hits to the exact legacy primitive. All numeric,
material, and triangle-class mismatch buckets are zero. The audit also fixed
the source-only loss of RT_SMOKE_TRIANGLE_EMISSIVE_STAGE_OFF. Motion/history
validation is accepted by geometry_geo136_auto and geometry_geo137_auto:
4,769 and 4,953 exact mapped pairs have matching valid flags, supplier status,
and previous world position. The live temporal classifier is also the
directly tested `PtSelectSkinnedSurfaceTemporalPolicy`; its harness covers
first observation, continuous animation/locomotion, spawn, teleport,
offscreen return, material/class change, topology/skeleton failure, and
missing previous storage. Canonical GPU-output emissive harvesting and remap
is the next GEO-09 step.

GEO-09 step 5 comparison is accepted by geometry_geo152_auto. The default-zero
audit asynchronously reads the authoritative current skinned output and
previous-position buffers, reconstructs records from immutable source-local
indices plus canonical instance/primitive identity, and uses the shared
production remap helper. The final duplicate-instance scene covers 31 routes,
35,814 triangles, 2,398 current/previous records, eight emissive instances,
and 22,579 referenced current/previous vertices. Vertex failures, identity,
metadata, aggregate-record, remap, zero-identity, and collision buckets are
all zero; stable, spawn, despawn, and identical-instance remap checks pass.

The scene has no production-eligible skinned emissive material. Authored
monster glow stages are discovered as emissive/light-candidate but their
images have no safe RT texture handle. A private validation material view
supplies positive geometry coverage without changing production.

GEO-09 step 6 is accepted by geometry_geo154_auto. Canonical surface material
IDs resolve 15 shared material-table rows for 16,564 triangles and eight live
dynamic records with zero ID/index/row/record mismatch. The one concrete gap
was fixed at the producer: GPU skinning now applies the evaluated per-surface
texture matrix to UV.xy, matching legacy merged geometry while preserving
normal UV.zw. One Lost Soul matrix-bearing dispatch covers 86 vertices; all 24
GPU samples pass position, motion, and four-channel UV comparison with zero UV
error. The selected live header matrix is identity in the accepted frame, so
this is parity evidence rather than an ordered-stage animation-quality claim.

GEO-09 step 7 is accepted by geometry_geo157_auto and geometry_geo159_auto.
The audit found and repaired a stale 144-byte dispatch declaration in the
primary smoke and primary-surface producer shaders after CPU/compute moved to
176 bytes. It also replaced resolved-RR camera-only motion for canonical
skinned hits with checked previous-position reconstruction from the shared
route. The settled same-ray run accepts 4,801/4,801 exact primitives with zero
hit or motion mismatch; clean-DI view 16 constructs the corrected
transmission/RR and RLU-current pipelines and exits normally. DI visibility
and GI secondary reconstruction consume the same accepted canonical
material/current-vertex tuple. Step 8 production traversal and live
skinned-emissive publication are next; Q17 remains open until that cutover.

GEO-09 step 8A promotes the already-accepted exact-set route rather than
introducing a broader production shortcut. geometry_geo160_auto uses fresh
defaults with no geometry gate overrides: 12 surfaces omit 9,889 vertices and
47,886 indexes, CPU skin append time is zero, and 12/12 TLAS descriptors plus
motion routes are active. The settled TLAS plan and all four capture fallback
buckets are zero-failure; the route-set lifecycle records no eviction,
invalidation, or late revocation and shutdown is normal. The canonical
registry, authoritative GPU producer, skinned TLAS route, and capture split
now default on but remain independently reversible. Step 8B live skinned
emissive publication is the remaining GEO-09 implementation slice.

GEO-09 step 8B is accepted by geometry_geo170_auto. Immutable mesh-local
primitive/material/identity planning creates bounded placeholder records
without CPU skinning. After authoritative GPU skinning and final exact-set TLAS
acceptance, a Vulkan compute pass publishes current/previous geometry into the
emissive, unified-light, and Remix manager payload buffers; rejection
fail-closes weights. The validation-only scene injection publishes 24 current
and 24 previous records with zero metadata/numeric mismatch and
0.000000954 maximum error. SPIR-V work-record offsets and 64-byte stride are
explicitly verified, destination reuse requires UAV capability, and shutdown
is normal. The prepared scene has no safe production skinned-emissive material,
so no material-safety rule was weakened. GEO-09 implementation through step 8
is complete; step 9 regression coverage remains before closing the ticket.

  PHASE D - migrate static world and scheduling
    GEO-10 portal-area static bucket BLAS
      -> GEO-11 build budget, TLAS lifetime, and BLAS compaction
      -> GEO-12 attribute layout measurement and compressed storage

  PHASE E - retire old mandatory routes
    GEO-13 cutover, cap removal, cleanup, and default promotion

GEO-04 may perform its read-only consumer inventory while GEO-03 runtime data is
being collected, but no implementation from Phase B may land before the Phase A
gate. All other tickets are serial because they alter ownership contracts used
by later tickets.

PHASE A GATE
------------
Required before registry implementation:

  - comm1 area 41 reproduces under a durable label/save/replay;
  - the first PT producer/consumer mismatch is identified and fixed narrowly;
  - raster remains unchanged;
  - working static and crowded-skinned scenes still pass;
  - GPU mode 1 exposes readiness/failure counters;
  - GPU/CPU current-position readback passes the declared tolerance for at
    least player, one zombie, Fatzombie, and one mixed/single-bone MD5 asset;
  - mode 2 still has no T-pose or corruption.

Status 2026-07-23: satisfied for entry into GEO-04. geometry_geo14 through
geometry_geo18 established exact funnel accounting, independent sentinel
overwrite, requested asset coverage, finite current/available-previous/motion
parity, and mode-2 visual stability. The Fatzombie's missing previous record is
not a numeric failure or pointer churn; it exposed a global last-3D-view history
owner. D16 makes same-logical-view history a mandatory GEO-04/GEO-07 contract
instead of weakening GEO-03 matching.

PHASE B GATE
------------
Required before new registry data drives traversal:

  - visible drawSurf, area-walk, and lifecycle observations produce identical
    canonical mesh/instance identities for the same object;
  - Add/Update/Free shadow records survive map, portal, and PDA transitions;
  - freeing the lifecycle medkit removes exactly its two records;
  - no durable record retains frame-temporary geometry/model pointers;
  - pool offsets are 64-bit byte-safe and BLAS builds accept non-zero index and
    vertex offsets;
  - canonical rigid BLAS/instance traversal matches the established rigid route
    for hit identity, material, motion, emissive identity, and lifecycle;
  - old and shadow material/triangle/instance counts compare exactly or every
    intentional difference is enumerated.

Status 2026-07-24: satisfied for entry into GEO-07. GEO-06 provides immutable
CPU source ownership, generation-safe split GPU pools, non-zero-offset Vulkan
BLAS consumption, shared canonical rigid BLASes, exact fail-closed descriptor
selection, and live primary-hit parity for geometry, material, motion and
emissive identity. geometry_geo37 proves exact two-surface lifecycle
add/remove/re-add with zero-build BLAS reuse. geometry_geo38 proves map
replacement retires all 61 predecessor BLASes before exact 77/77 canonical
descriptor selection in the new world. Checkpoint: 92e6e1e45.

PHASE C GATE
------------
Required before static migration:

  - eligible r_useGPUSkinning=1 surfaces no longer execute CPU pre-skinning;
  - GPU fallback/readiness counters account for every skinned surface;
  - current/previous positions pass numeric and motion-vector validation;
  - per-instance BLAS update count equals eligible live skinned instance count;
  - periodic rebuild is bounded and observable;
  - repeated character source bytes scale by unique mesh, while mutable output
    and BLAS state scale by instance;
  - the crowded scene has no cap drops, T-poses, missing materials, or temporal
    regression attributable to geometry routing;
  - legacy gate off restores the baseline.

PHASE D GATE
------------
Required before deleting the monolithic static path:

  - comm1 area 41 and all baseline areas render correctly with static buckets;
  - only active portal-area buckets enter the TLAS;
  - inactive resident buckets may retain memory but are not traversed;
  - total static TLAS entries stay inside the budget by bucket coalescing;
  - active-set, BLAS, route-table, and shader generations match every frame;
  - build admission respects count and scratch-byte budgets without silent loss;
  - TLAS result and instance buffers have a proved in-flight lifetime;
  - compacted BLAS handles are not exposed before copy completion;
  - compressed attributes match full-fidelity shading within declared error and
    wide-UV meshes take the explicit fallback.

GEO-09 step-9 status 2026-07-26: automated Phase-C regression coverage is
complete. geometry_geo180_auto proves a controlled 20-route / 56,730-triangle
crowd with exact live/shadow consumer parity and zero CPU skin append.
geometry_geo177_auto proves spawn/despawn/respawn retirement plus an in-process
Mars City -> Alpha Labs reset, while the multi-save matrix covers 2-, 7-, and
15-route settled sets. geometry_geo181_auto returns to comm1 area 41 and
reports exact static cache/BLAS/shader counts with zero tuple mismatches.
GEO-09 remains open only for the current-executable visual brush-movement and
PDA-open/close observations; those are not replaced by numeric logs.

PHASE E GATE / DEFINITION OF DONE
---------------------------------
The modernization is complete only when:

  - static world uses area/size bucket records;
  - immutable model geometry uses shared mesh records;
  - true skinned geometry uses per-instance GPU outputs and refit BLASes;
  - the per-frame merged path contains only the explicit D11 fallback classes;
  - no mandatory path depends on PT_SCENE_UNIVERSE_STATIC_MAX_* or
    RT_SMOKE_MAX_* compile-time geometry caps;
  - no old route is deleted until material, motion, emissive, and triangle-class
    consumers have moved and compare-mode evidence is archived;
  - warm identical-content performance is captured against the 2026-07-23
    baseline, including CPU scene/capture time, upload bytes, BLAS/TLAS time,
    memory, and frame time;
  - all new gates have an intentional default and rollback path;
  - diagnostics are reduced to bounded funnel totals, failure counters, and
    one-shot samples; duplicate bring-up scaffolding is removed;
  - Vulkan release build and SPIR-V-only deployment are verified.

ROLLBACK PHILOSOPHY
-------------------
Until GEO-13, each migration lane retains one default-off master gate that can
restore the last verified producer without changing unrelated rendering lanes.
Rollback is a normal validation tool, not a substitute for fixing a mismatch.

PERFORMANCE TARGETS
-------------------
Targets are acceptance directions, not invented guarantees:

  - eliminate the 9-10 ms CPU skinning sub-step measured in Capture B for
    eligible GPU-skinned surfaces;
  - make immutable source geometry upload scale with unique meshes, not frames
    or instances;
  - replace full merged skinned BLAS rebuild work with per-instance updates plus
    bounded periodic rebuilds;
  - make static BLAS/TLAS work scale with dirty/active buckets;
  - remove whole-payload byte hashing from steady immutable geometry;
  - admit modern assets by checked memory budget rather than compile-time caps.

If measurements contradict one of these targets, the owning ticket stops and
updates the decision register before later tickets proceed.
