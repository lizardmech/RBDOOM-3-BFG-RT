GEO-09 - Promote skinned traversal with temporal/material/emissive parity
==========================================================================

GOAL
----
Move eligible skinned geometry fully onto canonical per-instance traversal only
after every downstream consumer matches the legacy path.

PRECONDITIONS
-------------
- GEO-08 per-instance BLAS compare mode passes.
- This ticket owns consumer migration, not new lighting algorithms.

FILES TO AUDIT FIRST
--------------------
  neo/renderer/NVRHI/PathTraceSceneInputs.h
  neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
  neo/renderer/NVRHI/PathTraceSmokeDispatch.cpp
  neo/renderer/NVRHI/PathTracePrimarySurface.h
  neo/renderer/NVRHI/PathTraceMaterialUniverse.cpp/.h
  neo/renderer/NVRHI/PathTraceDynamicMaterialState.cpp/.h
  neo/renderer/NVRHI/PathTraceEmissiveCandidates.cpp/.h
  neo/renderer/NVRHI/PathTraceUnifiedLight.cpp/.h
  neo/shaders/builtin/pathtracing/PathTracePrimarySurface.hlsli
  neo/shaders/builtin/pathtracing/pathtrace_smoke_rab_motion_supplier.hlsli

STEPS
-----
1. Inventory all consumers of legacy merged skinned primitive/instance IDs,
   previous positions, material indices, triangle flags, and emissive records.
2. Make canonical hit routing recover mesh key, instance key, local primitive,
   current/previous position, material ID, and triangle class without depending
   on legacy merged offsets.
3. Compare primary hit tuple old/new for sampled pixels across all baseline
   models: instance, primitive, barycentrics, world position, normal/tangent/UV,
   material, and triangle flags.
4. Compare motion output for idle, animated, locomoting, newly spawned,
   teleported, offscreen-returning, and removed instances. Validate history reset
   rather than accepting plausible-looking denoiser output.
5. Build emissive records from canonical mesh + local primitive + instance
   identity. Compare current/previous inventories and remaps across animation,
   spawn/despawn, and identical model instances.
6. Prove material texture bindings and dynamic material state are identical.
   Keep Fatzombie's known NVRHI texture-descriptor rejection classified as a
   separate material defect; geometry/material identity still must match.
7. Compare DI/GI/RR geometry inputs numerically where available. Do not tune or
   replace ReSTIR/RR algorithms in this ticket.
8. Promote only surfaces whose full consumer funnel passes. Leave unsupported or
   failed surfaces on the explicit legacy fallback with counters.
9. Run crowded, static, comm1, lifecycle, PDA, and map-transition regressions.

VALIDATION
----------
- Same-frame old/new hit tuple and material/triangle identities agree.
- Current/previous emissive identity/remap has no collision or unexplained loss.
- Motion vectors/history validity agree within declared tolerance.
- No new T-pose, missing texture beyond known Fatzombie issue, light popping,
  ghosting, or unexplained reservoir reset attributable to routing.

DONE WHEN
---------
Eligible skinned geometry no longer needs the merged route for traversal,
materials, motion, or emissives; fallback remains explicit and reversible.

CHECKPOINT
----------
  pt geometry GEO-09: promote canonical skinned traversal

PROGRESS 2026-07-26
-------------------
Step 1 consumer inventory and its first behavior-neutral audit slice are
accepted. `r_pathTracingGeometrySkinnedConsumerAudit` defaults to zero and
compares both the live source-only CPU/upload/TLAS route and an independent
same-frame legacy-backed shadow. geometry_geo128_auto proves 12 routes and
15,962 triangles in both views, 15,962/15,962 legacy mappings in the shadow,
12/12 motion-ready routes, exact material ID/index/class transport, nonzero
primitive/emissive identities, and zero in all thirteen failure buckets for
both views. The focused injected-failure harness passes.

The inventory also confirms the open Q17 boundary: the live emissive inventory
still explicitly skips all skinned triangles. No emissive behavior changes in
this slice.

Steps 2/3 are accepted by geometry_geo134_auto. The default-zero mode-58 audit
samples 57,600 exact legacy/canonical ray pairs and maps 4,867/4,867
unobstructed canonical skinned hits to the exact legacy primitive. All numeric,
material, and triangle-class mismatch buckets are zero under declared 1e-3
tuple and 1e-2 normalized-basis tolerances. GPU vertex parity passes 24/24 at
0.0000152587891 maximum error when authoritative mode 2 retains the exact
renderer joint-cache snapshot.

The audit found and fixed one real metadata mismatch: source-only skinned
triangles lost RT_SMOKE_TRIANGLE_EMISSIVE_STAGE_OFF. The canonical skinned
capture record now transports the evaluated per-surface class/flag tuple
without legacy offsets.

Step 4 is accepted by geometry_geo136_auto and geometry_geo137_auto. The
mode-58 audit compares the legacy/canonical motion-valid flags, supplier
status, and previous world position on exact mapped primitives. Settled
frames 122 and 62 pass 4,769 and 4,953 comparable motion pairs with zero
motion mismatches and 0.00048828125 maximum previous-position delta.

The live per-surface temporal classifier now calls the pure
`PtSelectSkinnedSurfaceTemporalPolicy`. Its focused harness passes first
observation, continuous animation/locomotion, spawn, teleport, offscreen
return, material/class changes, topology/skeleton failure, and unavailable
previous storage. Removed surfaces have no current motion record; return uses
the explicit absent-previous reset. Q16 is resolved. Canonical GPU-output
emissive harvesting and temporal remap remain open for step 5.

Step 5 comparison is accepted by geometry_geo152_auto. The default-zero
emissive audit reads authoritative current/previous GPU skinning outputs and
reconstructs canonical records from immutable mesh-local indices, per-instance
identity, local primitive, material, and retained triangle flags. It compares
2,398 current and 2,398 previous records across eight live emissive instances,
with zero vertex, identity, metadata, aggregate-record, remap, or collision
failures. The shared remap helper passes stable, added, removed, and distinct
identical-model-instance cases.

The test scene has zero safe production candidates: monster glow materials are
correctly discovered as emissive/light-candidate, but their authored emissive
images have no RT texture handle. The audit uses a private validation-only
constant material view and reports `productionBehaviorChanged=0`; production
material flags and light inventories remain unchanged. The comparison portion
of Q17 is resolved. Live publication waits for step 8. Step 6 material texture
binding and dynamic material state parity is next.

Step 6 is accepted by geometry_geo154_auto. The audit resolves canonical
surface material IDs to the exact shared material table and dynamic-record
rows without requiring a legacy triangle offset. Across 15 dispatches and
16,564 triangles, all material ID/index, row completeness, and dynamic-record
ownership mismatch buckets are zero. Unsafe diffuse/normal/specular rows,
including the known Fatzombie class, remain separately classified material
defects.

The audit found and fixed one real UV-state mismatch. Legacy merged geometry
baked the evaluated texture matrix into vertex UV.xy, while canonical GPU
skinning copied immutable source UVs. The compute dispatch now carries the
canonical surface's evaluated 2x3 matrix and pathtrace_skinning.cs.hlsl applies
it once at output production, preserving UV.zw. A Lost Soul supplies one live
matrix-bearing dispatch covering 86 vertices. All 24 authoritative samples
pass position, previous-position, motion, and four-channel UV comparison with
zero UV error. Its selected legacy header matrix evaluates to identity in the
accepted frame, so ordered-stage animation correctness is not claimed.

Step 7 is accepted by geometry_geo157_auto and geometry_geo159_auto. The audit
fixed two geometry-input defects: primary smoke/producer shaders retained a
144-byte skinned dispatch declaration after CPU/compute moved to 176 bytes,
and resolved RR projected skinned secondary surfaces with camera-only motion.
All declarations now retain the texture-matrix rows, while a bounded canonical
route helper supplies exact current/previous triangle indices to primary and
resolved-RR motion. The settled audit passes 4,801/4,801 exact primitives with
zero hit or motion mismatch and 0.000305175781 maximum previous-position
delta. Clean-DI view 16 constructs the corrected transmission/RR and
RLU-current pipelines, validates 12 route records and 15,962 triangles, and
shuts down normally. DI visibility and GI secondary reconstruction are
structurally tied to the already-accepted canonical current geometry/material
tuple. Production traversal and skinned-emissive publication remain deferred
to step 8.

Step 8A production traversal is accepted by geometry_geo160_auto. The fresh
launch supplies no geometry migration gate overrides. The canonical registry,
authoritative GPU producer, per-instance TLAS route, and exact-set CPU-capture
split now default on while preserving independent rollback. Twelve eligible
surfaces omit 9,889 vertices and 47,886 indexes, CPU skin append time is zero,
and all 12 TLAS descriptors and motion routes are active. Upload, route,
resource, BLAS, SBT, capacity, and the four settled capture fallback buckets
are all zero-failure; no route set is evicted, invalidated, or late-revoked.
Step 8B same-frame GPU-output skinned-emissive publication remains.
