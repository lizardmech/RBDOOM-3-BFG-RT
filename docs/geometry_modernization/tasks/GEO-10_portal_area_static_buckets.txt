GEO-10 - Replace monolithic static traversal with portal-area buckets
====================================================================

GOAL
----
Build resident static BLAS buckets by portal area and bounded size, then place
only active buckets in the TLAS while preserving full static shading identity.

PRECONDITIONS
-------------
- Phase C gate passes.
- GEO-02 comm1 invariant is retained as a mandatory regression.
- New static route is default off and compare-capable.

ARCHITECTURE OVERRIDE - 2026-07-27
----------------------------------
Read `artifacts/GEO-10_architecture_pivot_2026-07-27.txt` before changing GEO-10
code, then read
`artifacts/GEO-10_reference_contract_review_2026-07-27.txt`. The reference
contract review is the newest authority. These artifacts supersede the
parallel t100-t105 route-record decoder, contribution 4/5, compact hit-library,
material hit-stage ladder, and triangle-base InstanceID direction documented
below. The remainder of this ticket is retained as historical evidence unless
the newer artifacts explicitly preserve a contract.

The active direction keeps deterministic bucket assignment, resident BLASes,
active TLAS selection, and rollback. Each independently active bucket remains
one BLAS, but that BLAS contains one GeometryDesc per packed surface.
InstanceID supplies the first resident surface-record index, GeometryIndex
selects the surface, and PrimitiveIndex selects its local triangle. The
resident pool and appended surface records use existing static t3/t4/t5/t9/t11
slots. Every TraceRay geometry-contribution multiplier must first be proven
zero. Resident-pack caching and Vulkan device-loss checkpoints are
preconditions for live traversal, not deferred cleanup.

The 2026-07-27 REF-4 route-off runtime checkpoint passed with no major issue
after every Vulkan TraceRay geometry-contribution multiplier was changed to
zero. REF-5 now builds each shadow bucket BLAS from one strictly validated,
ordered GeometryDesc per surface record. CPU tests reject invalid flags,
non-contiguous/reordered ranges, and cross-bucket record bleed. The full
release build passes without rebuilding shaders. Production TLAS insertion
remains impossible because the bucket consumer-support gate is hard-false;
the next gate is a shadow-only bucket build with route zero.

`bucket5.log` accepts that gate: 56/56 resident BLASes became ready, all 1,789
surface records mapped to exactly 1,789 GeometryDescs, 52 buckets contained
multiple geometries, and every invalid/publication failure counter was zero.
Portal movement changed the active set without rebuilding the resident set;
the user observed no issue.

REF-6 appends the same 1,789 four-word surface records after the unchanged
50,108 triangle-class words in the resident t5 upload. CPU validation rejects
any changed prefix or mismatched tail field. No shader consumer is enabled and
the live t5 binding remains monolithic until the later atomic route cutover.
`bucket6.log` accepts the shadow runtime gate: t5 contains 57,264 words with
the surface tail beginning at word 50,108 and occupying 28,624 bytes. All
56 bucket BLASes are ready/reused, all 1,789 records still map to 1,789
GeometryDescs, and every class-layout, geometry, publication, and device-loss
failure counter is zero.

REF-7 now defines the replacement address contract in one shared helper:
InstanceID supplies a resident surface-record base, GeometryIndex selects one
four-word t5 record, and PrimitiveIndex selects the local triangle. A CPU
mirror proves the exact record/triangle/index/vertex tuple and rejects invalid
namespace, geometry, and primitive inputs. The host publishes the total
resident surface-record count only after the route is accepted, and the old
triangle-base compatibility loader deliberately fails closed. Shader
consumers remain compile-gated off until they are migrated one pipeline family
at a time; this checkpoint is address-foundation proof, not live traversal
acceptance.

`bucket7.log` accepts REF-7's route-off reload baseline. REF-8 now compiles the
decoder only into the primary-surface producer and adds route mode 2 as a
view-2, GI-off, checkpoint-enabled opaque-primary probe. Each primary hit entry
resolves the hardware address once. Alpha/decal/glass/liquid records and static
bucket emissive replay remain excluded; route mode 1 is still fail-closed.

IMPLEMENTATION STATUS
---------------------
Steps 1 through 5 are complete in shadow mode. Static world records now carry the
authoritative `_areaN` owner assigned by AddWorldModelEntities. The CPU-only
assignment planner uses world/area/split/source/storage identity, deterministic
ordering, bounded splitting, explicit fallback ownership, and exact duplicate,
unassigned, invalid-area, invalid-range, oversized, split, and key-collision
counters.

PathTraceCpuPlanningHarness passes the deterministic and failure cases.
geometry_geo187_auto accepts 589/589 surfaces and 22,558/22,558 primitives
across 14 active area buckets with every failure counter zero and a nonzero
world/source/storage generation tuple. geometry_geo189_auto and
geometry_geo190_auto then produce identical fresh-process assignment and
packed-content signatures while copying all 26,926 vertices, 67,674 indexes,
and 22,558 material/class/source-identity triangles into contiguous
bucket-local ranges with all six packing failure counters zero. Production
remains the monolithic static BLAS.

The step-4 source is now a separate cap-free full-map resident universe, not
the selected/pruned production cache. geometry_geo193_auto packs 1,789/1,789
surfaces and 50,108/50,108 triangles into 56 area buckets, uploads 8,846,672
bytes of vertex/index/class/material/identity storage once, and incrementally
builds all 56 bucket BLASes at two per frame. The selected-area mask marks only
3 buckets active and retains 53 inactive. No bucket enters the production TLAS;
the shadow planner blocks its route because shader support is deliberately
still false.

Step 5 derives one publication generation from the source, storage, material,
packed-content, and active-set identities. The active TLAS descriptors and
checked route records are constructed together and carry that same generation.
Publication fails closed until every active bucket BLAS is ready, if counts
differ, or if any generation is zero or mixed. geometry_geo194_auto observes
the expected empty publication while the three active BLASes converge, followed
by an exact atomic 3/3 publication. The live production TLAS remains
monolithic; these descriptors and route records are still shadow-only.

The shader-route ABI foundation is also complete in shadow mode. The audit
found that material IDs alone were insufficient because the primary producer
also consumes a table index per triangle. The packed route now publishes and
binds all six checked SRV streams: route records, vertices, indexes, triangle
classes, material IDs, and resolved material indexes. geometry_geo195_auto
reports zero missing active material indexes, successful GPU route upload, and
zero Vulkan binding errors. Live bucket instances are still excluded.

The first live-cutover foundation is now present without changing traversal.
Static buckets reserve the high 24-bit InstanceID range beginning at
0x00800000, leaving the established static/dynamic/rigid/skinned IDs unchanged.
The primary producer and clean-DI shared libraries receive an explicit route
count plus both 32-bit halves of the publication generation. Their common
loader rejects a wrong namespace, count, 48-byte record stride, generation,
packed range, 112-byte vertex stride, index topology, or material/class range.
The acceleration submit plan now separates retaining/building the monolithic
static BLAS from including instance 0 in the TLAS, so rollback ownership can
survive the eventual bucket-only submission.

geometry_geo197_auto accepts creation of the enlarged clean-DI constant buffer,
all six clean-DI route bindings, and the affected Vulkan pipelines. It settles
to the same exact 3/3 publication, builds all 56 resident BLASes, reports zero
binding/device/validation errors, and shuts down normally. Traversal remains
monolithic because actual primary/any-hit/spatial/GI hit reconstruction and
static-emissive identity have not moved. geometry_geo196_auto is rejected as an
early-terminated first-load diagnostic, not a runtime failure.

The primary-surface producer is now the first complete checked hit consumer.
Its triangle validation, material ID/class/table-index lookup, alpha/decal
texture and vertex-color interpolation, semantic liquid-card evidence,
any-hit support predicate, and closest-hit vertex reconstruction all recognize
the reserved bucket namespace. Every bucket access goes through the common
generation/range/topology loader before reading packed storage. Monolithic
static, dynamic, rigid, and skinned fallback behavior is unchanged. This code
is deliberately unreachable until the later all-consumer cutover gate admits
bucket instances to the production TLAS.

Standalone clean-DI spatial now consumes the same checked route tuple and six
packed SRVs already present in its binding set. Its receiver triangle
reconstruction, emissive replay geometry, live material-index recovery, and
shadow any-hit material lookup all fail closed through the common loader.
Both debug and production view-16 Vulkan variants build and deploy, while the
live TLAS remains monolithic.

Remix GI now mirrors the complete 512-byte clean-DI constants prefix instead
of placing its private tail over the final 32 bytes. Its independent layout
binds the six bucket streams at t100 through t105. Central hit-range,
material-ID/index, class, full-surface, compact-geometry, ray-query, any-hit,
and emissive replay consumers use one non-inlined checked route loader. Vulkan
uses only the split GI libraries; the two unused monolithic SPIR-V build
outputs were removed after the enlarged source exceeded their compiler ID
limit. All 32 runtime split libraries and both compute wrappers build and
deploy. Bucket traversal remains off.

The first attempt to import the checked decoder directly into every clean-DI
consumer compiled but caused repeatable Vulkan device removal even with the
route disabled. That integration is quarantined. Vulkan pipelines now append
separate compact bucket hit-only modules at SBT contributions 4/5 instead:
one for the core clean-DI payload, one for glass/transmission's extended
payload, and one for the clean-GI payload. Primary and spatial use the same
six-record contract. Default launch/save-load tests passed independently for
core DI, primary/spatial, material features, and GI with no device removal or
visual regression. The checked decoder is therefore available in every live
Vulkan ray-pipeline family without expanding the large raygen libraries.

Primary history and motion now classify a checked bucket InstanceID as static
world geometry. Bucket vertices are immutable world-space data, so their
previous position is the current hit position and the existing previous-camera
projection produces motion pixels and depth. This bypasses monolithic static
snapshot indexing without entering rigid/skinned object-motion arithmetic.
The legacy smoke library compiles the old contract byte-for-byte; only the
primary producer enables the bucket-motion predicate.

Current-frame bucket preparation now runs once after material-table completion
and before emissive construction, acceleration submission, binding creation,
and scene-input commit. One frame result owns the pack, resolved material
indexes, BLAS state, active publication, route upload result, and generation.
The later GEO-10 diagnostics read that result instead of rebuilding or patching
the already committed `m_sceneInputs`. The fresh local scene-input package now
receives the route count, generation, and validity before resource commit.
This closes the stale publication-order blocker while traversal remains
shadow-only.

The emissive-identity audit then rejected dense active-subset InstanceIDs:
adding or removing an earlier portal-area bucket could renumber a still-visible
bucket. Publication now assigns InstanceID from the stable resident-bucket
ordinal. The TLAS still contains only active buckets, while the checked route
table contains one record for every resident bucket. CPU validation accepts an
active TLAS count independently from the complete resident route count and
proves the route count remains complete when the active subset changes.

A shadow static-emissive builder now reads the exact current-frame packed
bucket ranges and emits only active-bucket records using resident InstanceID
plus bucket-local primitive index. Static/dynamic summaries recognize both
monolithic instance 0 and the reserved bucket namespace as static. The
one-shot GEO-10 audit reports monolithic/bucket counts plus zero-identity and
collision counts. Production emissive publication remains monolithic until the
same default-off gate switches traversal and emissive identity atomically.

`r_pathTracingGeometryStaticBucketRoute 1` is now that default-off gate. A
request never allocates resources or submits BLAS builds implicitly. Explicitly
enable the bucket resource and build gates after cold pipeline warmup; the live
route continues rendering monolithic until every resident bucket is ready.
Cutover additionally requires an exact current-frame publication, complete
resident route table, successful route upload, debug mode 0, clean DI enabled,
and clean-DI view 2 through 25. View 1 explicitly fails the gate because the
all-views sentinel could not compile bucket hit support. Acceptance uses the
same boolean to omit monolithic TLAS instance 0, append active bucket instances,
and replace monolithic static-emissive records with bucket-local records.
Setting the cvar back to 0 restores both monolithic contracts.

The CPU harness accepts the exact fully resident route and rejects unsupported
consumer, partial residency, incomplete route table, and default-off cases.
The Vulkan release build, SPIR-V validation, CPU harness, and unselected
six-record pipeline runtime tests pass. Live bucket traversal proof is still
required before the route can be considered accepted.

The first live test exposed two separate facts. Contribution zero made primary
brushes visible but selected legacy records in the other pipelines, producing
missing behind-glass brushes and portal-dependent behavior. Correcting the TLAS
descriptor to contribution 4 then caused immediate Vulkan device removal.
Production route admission is fail-closed again.

The no-trace probe survived with no visual regression. Its console spam
contained only the expected validation warning that mask-zero instances never
intersect. Contribution-4 TLAS construction is accepted. The next rung is:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteNoTraceProbe 0
  r_pathTracingCleanRtxdiDiView 2
  r_pathTracingGeometryStaticBucketRouteView2Probe 1
  r_pathTracingGeometryStaticBucketRoute 1

This is a live traversal test and may display the view-2 diagnostic output. It
isolates primary plus compact core bucket hits; it does not admit route
traversal in any other clean view.

View-2 probe build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    86B6B66EFC42B9F0DF3FEBBE70B92D0A55AEB5A7D380BC415070F60CF9DAFA0C

The view-2 probe survived with an exact accepted publication and no device
removal. Primary, sentinel, and the compact core bucket hit pair are accepted.
The next rung isolates the clean-DI production pipelines:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteView2Probe 0
  r_pathTracingGeometryStaticBucketRouteView16CoreProbe 1
  r_pathTracingCleanRtxdiDiTransmissionProducer 0
  r_pathTracingCleanRtxdiDiTransmissionCompose 0
  r_pathTracingCleanRtxdiDiTransmissionDebugView 0
  r_pathTracingCleanRtxdiDiGlassShader 0
  r_pathTracingCleanRtxdiDiGlassDebugView 0
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketRoute 1

The route is accepted only while every dependency above is exact. This runs
initial, temporal, and spatial production with compact core bucket hits while
excluding the distinct material-feature and GI bucket-hit families.

View-16 production-core probe build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    81007DDC46F6655DCBAECA4E27EFDDB3BFA291005D15A4F9F5AEB2BE44538F05

The view-16 production-core probe survived portal movement while the active set
changed from three to five buckets. Initial, temporal, spatial production, and
the compact core hit pair are accepted. The next rung adds only normal
material-feature traversal:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteView16CoreProbe 0
  r_pathTracingGeometryStaticBucketRouteView16MaterialProbe 1
  r_pathTracingCleanRtxdiDiTransmissionProducer 0
  r_pathTracingCleanRtxdiDiTransmissionCompose 0
  r_pathTracingCleanRtxdiDiTransmissionDebugView 0
  r_pathTracingCleanRtxdiDiGlassShader 1
  r_pathTracingCleanRtxdiDiGlassDebugView 0
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketRoute 1

This restores transmission PSR and the transmission/glass passes while keeping
the distinct GI bucket-hit family excluded.

View-16 material-feature probe build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    761917DFF5A3DE59CA9C9106857EF182E6A33AE695B5D5C68B856C99ED384DD7

The first material-feature probe caused immediate Vulkan device removal. The
accepted production-core baseline and excluded GI isolate the material-feature
pipeline. Audit found a concrete payload contract mismatch: the compact
material bucket hit library uses a 176-byte trace-hit/liquid-candidate payload,
while the material-feature pipelines declared a 64-byte maximum. Both clean
material-feature pipeline descriptors now declare 176 bytes. Re-run the same
probe after rebuilding; do not widen to GI until it passes.

Material payload fix build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    EFCBDE0B174E99F7D6D7404B8C37FFDCAA741160404B91C399AC0E98D9AC0F80

The full material probe still device-removed after the payload correction.
Static SPIR-V validation, payload-type comparison, descriptor slots 100..105,
and host bindings pass. The material probe cvar is now a mode. Test mode 1
first:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteView16CoreProbe 0
  r_pathTracingGeometryStaticBucketRouteView16MaterialProbe 1
  r_pathTracingCleanRtxdiDiTransmissionProducer 1
  r_pathTracingCleanRtxdiDiTransmissionCompose 0
  r_pathTracingCleanRtxdiDiTransmissionDebugView 0
  r_pathTracingCleanRtxdiDiGlassShader 0
  r_pathTracingCleanRtxdiDiGlassDebugView 0
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketRoute 1

Mode 1 isolates the standalone transmission producer dispatch. Do not advance
to mode 2 or 3 until mode 1 survives.

Staged material probe build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  spirv-val material/transmission/glass             passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    17510A4B6126C9C7222096B35AE139B25B002360EC4F796FCD760228FF71BD86

Mode 1 survived with an exact accepted publication at 3/56/56. The standalone
transmission producer dispatch is accepted. Source inspection then corrected
the earlier interpretation: this late phase does not issue TraceRay, so it did
not exercise the compact material bucket hit records. Mode 2 was the first such
trace and device-removed with glass and GI excluded.

The rebuilt diagnostic divides mode 2 into cumulative hit stages. Stage 0
fails route admission closed. Start with stage 1:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteView16MaterialProbe 2
  r_pathTracingGeometryStaticBucketMaterialHitStage 1
  r_pathTracingCleanRtxdiDiTransmissionProducer 1
  r_pathTracingCleanRtxdiDiTransmissionCompose 1
  r_pathTracingCleanRtxdiDiTransmissionDebugView 0
  r_pathTracingCleanRtxdiDiGlassShader 0
  r_pathTracingCleanRtxdiDiGlassDebugView 0
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingGeometryStaticBucketBlas 1
  r_pathTracingGeometryStaticBucketBlasBuild 1
  wait for resident/ready to reach 56/56
  r_pathTracingGeometryStaticBucketRoute 1

Mode 2 adds the pre-DI transmission PSR dispatch while glass and GI remain
excluded. The hit-stage ladder is:

  1  minimal closest-hit payload, no bucket metadata
  2  checked bucket material ID/index/class metadata
  3  source-pane rejection
  4  transparent-card/glass rejection
  5  alpha-test rejection
  6  emissive-card passthrough
  7  full liquid-pool candidate collection

For each rung, disable the route, change only
`r_pathTracingGeometryStaticBucketMaterialHitStage`, then re-enable the route.
Stop on the first device removal. Do not enable glass or GI during this ladder.

Material hit-stage ladder build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  spirv-val sentinel/material/transmission/glass    passed
  rebuilt blob mtimes newer than final include      yes
  built/deployed executable SHA-256
    6D2298BA764B0D7CD35FF8709492BE39CA961626CAC67F1138D5963ECE7F5822
  compact material bucket blob SHA-256
    3CC6BD10DB248CA227B43BD03D478C4ACB5A13A89C53EE736DF78D852A5525BA

The current shadow resource path also repacks the full resident static universe
every frame and has high overhead. Correctness proof does not waive step 9:
steady state must cache the resident pack and scale recurring work with portal
active-set/TLAS changes.

`r_pathTracingGeometryStaticBucketAudit 1` is a one-shot shadow audit.
`r_pathTracingGeometryStaticBucketBlas 1` owns the full-map shadow resources,
and `r_pathTracingGeometryStaticBucketBlasBuild 1` submits bounded shadow
builds. All are default off and intentionally independent from the route
request. See artifacts/GEO-10_entry_audit.txt.

FILES TO AUDIT FIRST
--------------------
  neo/renderer/NVRHI/PathTraceSceneUniverse.cpp/.h
  neo/renderer/NVRHI/PathTraceGeometryUniverse.cpp/.h
  neo/renderer/NVRHI/PathTraceInstanceUniverse.cpp/.h
  neo/renderer/NVRHI/PathTraceAccelerationPlan.cpp/.h
  neo/renderer/NVRHI/PathTraceAcceleration.cpp/.h
  neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
  neo/renderer/NVRHI/PathTraceSceneInputs.h
  neo/shaders/builtin/pathtracing/PathTracePrimarySurface.hlsli

STEPS
-----
1. Define a static bucket key: world generation, portal area, size split, and
   source/storage generation. Define deterministic splitting and stable order.
2. Assign each static surface/primitive to exactly one bucket. Emit duplicate,
   unassigned, invalid-area, oversized, and split counters.
3. Build bucket geometry from GEO-06-style pooled full-fidelity ranges. Preserve
   local primitive -> material/triangle/source identity tables.
4. Build/reuse one BLAS per dirty bucket. Resident inactive buckets may keep
   storage and BLAS state, but must not be put in the active TLAS.
5. Derive one active-set generation from portal visibility. Use that same
   generation for TLAS instances and shader route tables; reject mixed epochs.
6. Coalesce tiny buckets deterministically if static TLAS entries exceed the
   declared budget. Never drop a bucket silently.
7. Compare old/new active static triangles, material IDs, sampled hit tuples,
   emissive inventory, and TLAS entries per area.
8. Run comm1 area 41 first. Record active/retained ranges plus the full GEO-01
   generation tuple. Then run STATIC_BUSY, portal transitions, PDA, and map
   changes.
9. Measure dirty bucket builds/reuse, active TLAS entries, upload bytes, scratch,
   retained memory, and timings with identical content.

GUARDRAILS
----------
- Do not use the old compile-time static cap as bucket admission policy.
- Do not include all resident inactive buckets just to make comm1 render.
- Do not delete the monolithic path until GEO-13.

DONE WHEN
---------
- Every static primitive has one bucket and correct shader identity.
- Only active buckets enter TLAS; generations match end-to-end.
- comm1 and all baselines render with zero silent loss.
- Old static route is a one-cvar rollback.

CHECKPOINT
----------
  pt geometry GEO-10: add portal-area static BLAS buckets

REF-8 ACTIVE-SET FINDING - bucket8.log - 2026-07-27
---------------------------------------------------
The first live hardware-address probe did not report an address, binding,
validation, or device-loss failure. Mode 2 repeatedly admitted exact
publications with all 56 resident bucket BLASes ready. The active subset changed
from three to five buckets during portal movement.

The visual result is rejected. Opaque world geometry more than roughly one
portal edge away disappeared in view 2. This matches the current selector:
`BuildPortalAreaActiveMask` is a breadth-first portal graph neighborhood with
`r_pathTracingGeometryStaticBucketPortalSteps=1`. Raster visibility uses the
actual clipped recursive portal flood and can expose areas farther away.
Increasing the fixed depth is not an accepted production repair; prior runtime
evidence found portal step 8 only temporarily recovered the old route.

View 16 was not a bucket-route success. Mode 2 rejected that view and restored
monolithic traversal as designed.

Mode 2 is now explicitly a full-resident decoder probe. After its existing
view-2, marker-enabled, clean-DI, GI-off predicate passes, every resident portal
bucket enters the diagnostic TLAS. Production mode 1 and route-off shadow
planning still use the ordinary portal mask. Logs distinguish the ordinary
`portalMaskValid` source from the final `maskValid` and the
`fullResidentProbe` override.

This diagnostic override does not close step 5 or approve all-resident
production traversal. The production route still requires a conservative
immutable frontend snapshot derived from actual portal visibility, with one
revision shared by TLAS membership and every dependent publication.

Corrected probe verification:

  PathTraceCpuPlanningHarness --stress20             passed
  full win64-pt-dev-release build                    passed
  shader source/blob changes                         none
  built/deployed executable SHA-256
    236C9A5EA4F8331E5BEB2BF1545F11647EE176E515A2E2BD510DB561E74871EF

The repeated REF-8 run must report 56/56/56 active/resident/ready, 56/56
TLAS/routes, `fullResidentProbe=1`, and bucket-resident-pool traversal. Reject
on device removal, missing opaque brushes, opaque material corruption, or any
nonzero address/publication failure.

REF-8 FULL-RESIDENT PUBLICATION REPAIR - bucket9.log - 2026-07-27
----------------------------------------------------------------
The user reported no visual issue, but the log rejects this as decoder evidence.
Every mode-2 view-2 frame remained on the monolithic fail-safe:

  requested / accepted                              1 / 0
  consumer supported / full-resident probe           1 / 1
  active / resident / ready                         56 / 56 / 56
  publication exact                                  0
  TLAS / route outputs                               0 / 0
  traversal                                          monolithic

The full-resident active mask exposed a second diagnostic-contract mismatch.
The frame material table was still hydrated from the ordinary portal-selected
monolithic triangle stream, while publication now required valid material
indexes for every resident bucket triangle. The active publication correctly
failed closed instead of admitting a partially mapped table.

For mode 2 only, material metadata and table membership now include the unique
material IDs from the resident static-bucket triangle stream. The hydration
cache key includes a separate signature for that set, so switching the probe on
cannot reuse the narrower route-off table. Production modes are unchanged.
The cutover diagnostic now prints `materialIndexMissingActive` directly.

Verification:

  PathTraceCpuPlanningHarness --stress20             passed
  full win64-pt-dev-release build                    passed
  shader source/blob changes                         none
  source/deployed executable hashes                  equal
  executable SHA-256
    6FDB534E2290DE9953A110961511BF44ACD1412C223DA11CA00E602C27122686

Repeat the same mode-2 test. Acceptance additionally requires
`materialIndexMissingActive=0`; a visually correct monolithic fallback is not a
pass.

REF-8 OPAQUE PRIMARY DECODER ACCEPTANCE - bucket10.log - 2026-07-27
-------------------------------------------------------------------
The corrected probe passed. View 2 repeatedly admitted live bucket traversal:

  requested / accepted                              1 / 1
  consumer supported / primary probe                 1 / 1
  full-resident probe                                1
  all resident ready / publication exact             1 / 1
  active / resident / ready                         56 / 56 / 56
  TLAS / route outputs                              56 / 56
  missing active material indexes                    0
  traversal                                          bucket-resident-pool

Switching to view 16 rejected the probe and restored monolithic traversal.
Switching back to view 2 re-admitted the same exact 56/56 bucket publication.
This repeated twice without device loss, Vulkan validation/binding errors, or
reported visual corruption.

REF-8 is accepted for opaque primary InstanceID + GeometryIndex +
PrimitiveIndex decoding and full-resident material lookup. This does not accept
the fixed-depth portal active-set policy, alpha/decal/glass/liquid consumers,
secondary/query rays, static emissive replay, or production route mode 1.

REF-9A PRIMARY ALPHA-TEST CHECKPOINT - 2026-07-27
-------------------------------------------------
The first REF-9 slice stays inside the accepted primary producer. Static-bucket
any-hit and shadow any-hit now admit ordinary opaque plus alpha-tested
materials. They reuse the one resolved hardware address and interpolate alpha
UVs directly from its three packed vertex indexes. No route-table re-decode,
descriptor, SBT record, payload field, secondary ray, glass, decal, particle,
liquid, DI/GI, motion, or emissive contract changed.

All other deferred primary classes remain ignored by mode 2. This is an
alpha-cutout rung, not general transparency.

Verification:

  PathTraceCpuPlanningHarness --stress20             passed
  full win64-pt-dev-release build                    passed
  rebuilt Vulkan blobs                                1
  DX12 shader rebuilds                                0
  primary blob newer than edited source             yes
  spirv-val --target-env vulkan1.2                   passed
  source/deployed executable hashes                  equal
  source/deployed primary blob hashes                equal
  executable SHA-256
    6FDB534E2290DE9953A110961511BF44ACD1412C223DA11CA00E602C27122686
  primary blob SHA-256
    7D078719E39B4BADDED618981815EA88CC0E86E272E0DD8C7A211A30FE5E7909

Runtime test:

  1. Keep the REF-8 view-2/GI-off/marker/BLAS settings.
  2. Aim at a static alpha-tested cutout such as a grate or masked panel.
  3. Compare route 0 and route 2. The silhouette and holes must match; route 2
     must not turn the card into a solid polygon.
  4. Traverse the existing scene and confirm opaque geometry remains stable.
  5. Capture the log while route 2 reports exact 56/56 bucket traversal.

Reject on device loss, opaque regression, solid alpha cards, wrong cutout
coverage, or any loss of exact publication.

REF-9A RUNTIME ACCEPTANCE - bucket11.log - 2026-07-27
-----------------------------------------------------
The first capture was monolithic because diagnostic markers were off. After
enabling `r_pathTracingNsightGpuMarkers 1`, the updated log repeatedly admitted:

  requested / accepted                              1 / 1
  consumer supported / primary probe                 1 / 1
  full-resident probe                                1
  all resident ready / publication exact             1 / 1
  active / resident / ready                         56 / 56 / 56
  TLAS / routes                                     56 / 56
  missing active material indexes                    0
  traversal                                          bucket-resident-pool

The user then continued moving through the scene and reported no visual issue.
No device-loss, binding, or validation failure appears in the log. REF-9A is
accepted for primary and shadow alpha-test handling.

REF-9B DETAIL-DECAL OFFLINE CHECKPOINT - 2026-07-27
----------------------------------------------------
The second REF-9 slice admits receiver-owned detail decals in the same primary
producer only. It does not widen glass, particles, liquid cards, secondary
rays, GI, or spatial DI.

The pre-edit audit found that the bucket InstanceID and surface-local
PrimitiveIndex cannot reuse DECAL-03's old sort-key expression. The bucket ID
occupies the high half of the 24-bit instance namespace, and equal local
primitive indexes recur in different GeometryIndex values. Reusing the old
expression would both overflow the instance component and collide across
surfaces.

The four-word surface record now stores the source monolithic triangle offset
in bits 1..31 of its existing flags word; bit 0 remains the valid bit. The
resolved hardware address therefore carries both:

  triangleIndex        packed resident-pool metadata address
  sourceTriangleIndex  original monolithic primitive index

This is an exact preservation of the existing static DECAL-03 key after
portal/size repacking: the decal branch applies DECAL-03's existing
`0x3fffffff` primitive mask to `sourceTriangleIndex`. It is not a new ordering
heuristic. The signed-int source range contract bounds the encoded offset to
31 bits. No fifth record word or new descriptor is required.

Primary any-hit resolves the address once, loads the material flags once, and
for `RT_SMOKE_MATERIAL_DETAIL_DECAL`:

  - interpolates authored UVs from the resolved packed vertex triple;
  - stores the decal with `sourceTriangleIndex`;
  - ignores the decal hit so the receiver remains closest.

The existing decal bin, receiver-distance guard, blend operations, and
route-0 path are unchanged. Non-collect modes still fail closed on a bucket
detail decal.

Offline/deployment proof:

  PathTraceCpuPlanningHarness                      passed
  explicit reordered-source sort-key test          passed
  full win64-pt-dev-release build                  passed
  DX12 shader rebuilds                              0
  intended/deployed Vulkan blobs                    1
  primary blob newer than both edited shaders      yes
  spirv-val --target-env vulkan1.2                 passed
  descriptor-set/binding decorations vs REF-9A      identical (96 / 96)
  InstanceCustomIndex / GeometryIndex / Primitive  present
  TraceRay geometry multiplier                      0
  source/deployed executable hashes                 equal
  source/deployed primary blob hashes               equal
  executable SHA-256
    A7B63FE41627B6B503961EB708ABC6B93B15F087E3C85A940069ECA3F8A2FE39
  primary blob SHA-256
    92E2829CB31989538C78D5F53593518C0010921547B7F98F8C356C81A18C423E

The shared include timestamp caused the build system to recompile disabled
DI/GI compatibility families; none of those outputs were deployed. Only the
primary producer has the bucket consumer macro enabled.

Runtime test:

  1. Before loading/reloading the map, set
     `r_pathTracingDecalComposite 1`.
  2. Keep markers on, clean DI view 2, GI off, bucket BLAS/build on, and route
     mode 2, matching the accepted REF-9A setup.
  3. Compare route 0 and route 2 on one visible wall grime/detail decal. The
     decal color, coverage, receiver, and stability must match.
  4. If stacked detail decals are available, check that their top/bottom
     ordering does not swap between routes or flicker while moving.
  5. Continue ordinary traversal and capture an audit/condump that reports
     exact 56/56 bucket traversal.

Reject on a missing decal, a decal becoming closest geometry, changed layer
order, receiver holes, flicker, opaque/alpha regression, device loss, or an
inexact publication. REF-9B remains pending runtime acceptance.

REF-9B VISUAL-GATE CORRECTION - bucket12.log - 2026-07-27
----------------------------------------------------------
`bucket12.log` mechanically accepts the new hit branch but cannot visually
accept it. In view 2 the route repeatedly reached exact full-resident traversal:

  requested / accepted                              1 / 1
  consumer supported / primary probe                 1 / 1
  active / resident / ready                         56 / 56 / 56
  TLAS / routes                                     56 / 56
  missing active material indexes                    0
  traversal                                          bucket-resident-pool

There is no device-loss, binding, or validation failure. The final audit was
taken after switching to view 16, so it correctly reports monolithic fallback
and is not the accepted sample. The reported black hovering card appeared only
in view 16. It therefore cannot be caused by REF-9B bucket traversal: view 16
had `consumerSupported=0` and never submitted the bucket TLAS.

The original visual instruction was invalid because clean-DI view 2 is
`primary-surface-status`: valid opaque surfaces are intentionally solid green.
It proves hit validity, not decal color or layering.

The mode-2 gate now also permits clean-DI view 19. View 19 reads the RR diffuse
albedo guide written by the bucket-aware primary producer after
`ApplyDetailDecalComposite`. It runs no analytic/material-validation resolve
and uses the already accepted sentinel output pipeline. View 16 remains
rejected. This supplies a direct post-composite decal image without admitting
bucket instances to an unmigrated ray family.

Verification/deployment:

  PathTraceCpuPlanningHarness --stress 20           passed
  full win64-pt-dev-release build                    passed
  Vulkan shader rebuilds                             0
  DX12 shader rebuilds                               0
  source/deployed executable hashes                  equal
  executable SHA-256
    D4039161E807CC8257E1A130D21A7A11BFF238B82B8D01ECDAB7EDBD8A2578E8

Corrected visual test:

  1. Set `r_pathTracingDecalComposite 1` before loading/reloading.
  2. Keep markers on, GI off, and bucket BLAS/build on.
  3. Set `r_pathTracingCleanRtxdiDiView 19`.
  4. Compare route 0 and route 2 on the same visible detail decals.
  5. In route 2, issue the bucket audit and condump.

View 19 is an unlit albedo guide, not beauty lighting; glass/windows may look
unrepresentative. Acceptance concerns decal color/coverage/order and opaque
receiver continuity. REF-9B remains visually pending.

REF-9B OPPOSITE-FACE REJECTION - 2026-07-27
-------------------------------------------
The corrected view-19 test rejected the first detail-decal implementation.
Some decals matched, while others appeared as a second hovering/stacked layer;
one wrong layer was viewed backwards. This is not an ordering-key symptom:
the source-triangle key controls layer order but cannot flip authored UVs.

The concrete mismatch is in receiver ownership. Mode 2 deliberately places the
full resident static universe in the diagnostic TLAS. A primary ray can
therefore visit the back of a detail-decal card on the opposite side of nearby
thin geometry. The existing receiver guard uses absolute projected hit
separation. At grazing angles that card can remain inside the distance
envelope and composite onto the committed receiver, producing exactly a
hovering duplicate or backwards texture.

Static capture already defines the ownership direction. It computes the card
face normal from the triangle, orients it with vertex 0's authored normal, and
lifts the card along that outward direction. Bucket any-hit now repeats that
orientation test on the resolved packed vertex triple and stores the decal only
when its authored outward face points against the primary ray. Degenerate
triangles fail closed. The card is still ignored as geometry in either case.

This correction:

  - changes only the mode-2 static-bucket detail-decal consumer;
  - adds no payload field, descriptor, SBT record, route record, or buffer;
  - preserves the exact source-triangle sort key and UV interpolation;
  - leaves route 0 and every non-bucket ray family unchanged.

Offline/deployment proof:

  PathTraceCpuPlanningHarness --stress 20           passed
  full win64-pt-dev-release build                    passed
  rebuilt Vulkan blobs                                1
  DX12 shader rebuilds                                0
  primary blob newer than edited source             yes
  spirv-val --target-env vulkan1.2                   passed
  descriptor-set/binding decorations old/new       96 / 96
  source/deployed executable hashes                  equal
  source/deployed primary blob hashes                equal
  executable SHA-256
    D4039161E807CC8257E1A130D21A7A11BFF238B82B8D01ECDAB7EDBD8A2578E8
  primary blob SHA-256
    E3BE23D895837981A5DA77C117DCF7B8CE6DBA6C5E5206FEB887306261765BDF

Runtime re-test:

  1. Keep the same view-19, GI-off, markers-on, full-resident mode-2 setup.
  2. Revisit the decals that showed hovering duplicates or backwards layers.
  3. Compare route 0 and route 2 on the same camera positions.
  4. Confirm legitimate front-facing decals remain visible and stable.
  5. Capture an audit/condump proving exact 56/56 bucket traversal.

Reject if the wrong layer remains, a valid decal disappears, route-0/route-2
color or ordering differs, publication falls back, or the device is lost.

REF-9B RUNTIME DISPOSITION - 2026-07-27
---------------------------------------
The opposite-face correction removed the hovering/stacked artifacts in route
2. One reversed decal remains, but the same card is reversed in route 0. Route
0 also retains the duplicate artifact that route 2 now rejects. Therefore:

  - the reversed card is baseline authored/decal behavior, not bucket decode;
  - the duplicate is a baseline receiver-ownership defect, not a route-2
    regression;
  - the bucket detail-decal address, UV, source ordering, and new outward-face
    ownership behavior pass the visual comparison.

REF-9B is visually accepted. Exact publication was already proven by the same
view-19-capable full-resident mode-2 gate; retain an audit in the next combined
runtime capture rather than requiring a separate launch for this disposition.

REF-9C LIQUID-CARD IDENTITY CHECKPOINT - 2026-07-27
---------------------------------------------------
The next primary-only material slice is semantic liquid-card collection. The
audit found a concrete identity mismatch before enabling it: the existing
liquid payload stores InstanceID and PrimitiveIndex but no GeometryIndex, while
a bucket hit's PrimitiveIndex is only surface-local. Reusing the hardware tuple
after any-hit would either collide across geometries or enter the deliberately
fail-closed legacy decoder.

No payload widening is required. A collected bucket liquid card is
canonicalized as:

  payload instanceId      bucket namespace | exact surfaceRecordIndex
  payload primitiveIndex  original monolithic sourceTriangleIndex

At raygen evidence reconstruction, the canonical surface record supplies its
source-triangle offset. Subtraction recovers the surface-local primitive, and
the accepted GeometryIndex-zero address decoder reconstructs the same packed
triangle and vertex triple. This keeps a stable source primitive in the
five-word liquid contributor key while retaining exact packed geometry access.

Bucket any-hit now mirrors the existing semantic-liquid path: it evaluates
stage color and coverage, stores a positive-coverage candidate, ignores the
card as geometry, and preserves fail-closed status on missing stage state.
The REF-9B outward-face guard also applies, preventing opposite-side liquid
cards from becoming receiver candidates. Ordinary detail decals continue
through the already accepted branch.

The CPU harness includes a multi-geometry case proving that canonical surface
InstanceID plus source triangle recovers the exact surface record, packed
triangle, index offset, and vertex triple. Glass, generic transmission,
particles, DI/GI secondary hits, and emissive replay remain closed.

Offline/deployment proof:

  PathTraceCpuPlanningHarness --stress 20           passed
  canonical liquid address test                     passed
  full win64-pt-dev-release build                    passed
  rebuilt Vulkan blobs                                1
  DX12 shader rebuilds                                0
  primary blob newer than edited source             yes
  spirv-val --target-env vulkan1.2                   passed
  descriptor-set/binding decorations old/new       96 / 96
  source/deployed executable hashes                  equal
  source/deployed primary blob hashes                equal
  executable SHA-256
    D4039161E807CC8257E1A130D21A7A11BFF238B82B8D01ECDAB7EDBD8A2578E8
  primary blob SHA-256
    F0D7B2326A15AC33461F7D7FC9F24CB30DA5B98E948F995FA514232843F4A1DF

Runtime test:

  1. Retain the accepted view-19, markers-on, GI-off, mode-2 bucket setup.
  2. Leave `r_pathTracingLiquidPoolMode 3`, its current production default.
  3. Compare route 0 and route 2 on a known wet/liquid decal card.
  4. The receiver texture must remain visible under one stable film; reject a
     missing card, black/floating duplicate, reversed contribution, or
     route-dependent coverage/tint.
  5. Traverse and audit once so the combined capture retains exact 56/56
     publication and no device/validation failure.

REF-9C RUNTIME ACCEPTANCE - 2026-07-27
--------------------------------------
The user reported no issue with the route-2 liquid-card comparison. REF-9C is
accepted for primary semantic-liquid collection and receiver resolve. No
missing, black, floating, reversed, portal-dependent, or stability regression
was observed.

REF-9D PRIMARY STATIC MOTION/HISTORY PROBE - 2026-07-27
-------------------------------------------------------
The next primary-only slice requires no new hit decoder. The producer already
classifies a checked bucket instance as immutable static world geometry in
`pathtrace_smoke_rab_motion_supplier.hlsli`. For such a hit, previous world
position equals current world position; the existing previous-camera
projection produces motion pixels, depth delta, and reset policy. It does not
index the monolithic previous-static snapshot with a surface-local primitive.

Clean-DI view 17 is the existing direct motion diagnostic. It is rendered by
the sentinel library and reads `PathTraceMotionVectors`,
`PathTraceRRMotionVectors`, and `PathTraceMotionVectorMask` after the
bucket-aware primary producer writes them. It does not enter initial, temporal,
spatial, material-feature, glass, or GI ray traversal.

Mode 2 now admits view 17 under the same markers-on and GI-off requirements as
views 2 and 19. This is a CPU cutover-predicate and help-text change only; no
shader, descriptor, SBT, payload, or pipeline binary changes.

Offline/deployment proof:

  full win64-pt-dev-release build                    passed
  explicit PathTraceCpuPlanningHarness target       passed
  PathTraceCpuPlanningHarness --stress 20           passed
  canonical liquid address test                     passed
  view-17 support and rejection matrix              passed
  Vulkan shader rebuilds                             0
  DX12 shader rebuilds                               0
  source/deployed executable hashes                  equal
  executable SHA-256
    F1ED59CEFF624B4CD4F3CA49883D6783459F1866A900BDEA0F9150CF9E26BF00
  retained primary blob SHA-256
    F0D7B2326A15AC33461F7D7FC9F24CB30DA5B98E948F995FA514232843F4A1DF

Runtime test:

  1. Retain markers on, GI off, bucket BLAS/build on, and exact resident-ready
     publication.
  2. Set `r_pathTracingCleanRtxdiDiView 17`.
  3. Compare route 0 and route 2 while standing still, rotating the camera,
     walking, and crossing portals.
  4. The static world must retain the same valid motion field and source tint;
     reject magenta/invalid bands, portal-dependent discontinuities, or
     persistent reset colors unique to route 2.
  5. Return to view 19 before later material checks.

REF-9D RUNTIME ACCEPTANCE - 2026-07-27
--------------------------------------
The route-0 and route-2 view-17 motion fields were reported visually
identical. REF-9D is accepted.

REF-9E PRIMARY GLASS CLASSIFICATION PROBE - 2026-07-27
------------------------------------------------------
The next bounded primary-only slice uses clean-DI view 24. Its material-
classifier proof disables temporal and spatial work, does not select production
view 16 in the material-feature registry, and renders through the sentinel
library. Transmission producer, glass compose, shadow-glass, GI, and other
secondary ray consumers remain outside this rung.

Primary bucket any-hit now admits only the established glass categories:
portal-window subtype, or object-glass subtype with its fallback material flag.
It uses the already resolved hardware address for metadata and UVs. The
existing stochastic glass coverage test is reused with monolithic static
InstanceID zero and sourceTriangleIndex so bucket repacking cannot change the
route-0 sample hash. Primary closest-hit already records GeometryIndex and
loads the exact packed surface.

Mode 2 now permits view 24 under the existing markers-on, GI-off gate. View 25
remains explicitly rejected because it executes the transmission/PSR path.
Bucket shadow glass also remains fail-closed.

Offline/deployment proof:

  full win64-pt-dev-release build                    passed
  explicit PathTraceCpuPlanningHarness target       passed
  PathTraceCpuPlanningHarness --stress 20           passed
  view-24 support/view-25 rejection matrix          passed
  rebuilt Vulkan blobs                                1
  DX12 shader rebuilds                                0
  primary blob newer than edited source             yes
  spirv-val --target-env vulkan1.2                   passed
  descriptor-set/binding decorations              48 / 48
  source/deployed executable hashes                  equal
  source/deployed primary blob hashes                equal
  executable SHA-256
    128D467C75A02F41665727DDA370B527CB0CBE08BAB25A0F2F230D308A922016
  primary producer SHA-256
    7B0FCD935A7C0521E29E4516D03E4C1EF13C79037AC6574F8BEB7D16B4281B4A

Runtime test:

  1. Retain markers on, GI off, bucket BLAS/build on, and exact 56/56
     resident-ready publication.
  2. Set `r_pathTracingCleanRtxdiDiView 24`.
  3. Compare route 0 and route 2 while looking through portal-window glass and
     at object glass, then rotate, move, and cross portals.
  4. Material-class colors and stochastic coverage must match. Reject
     route-2-only magenta, missing bands, coverage shimmer, or class changes.
  5. Do not use view 25 for this rung.

REF-9E RUNTIME ACCEPTANCE - 2026-07-28
--------------------------------------
The user reported no visual issue in the view-24 comparison. REF-9E is
accepted.

REF-11A RESIDENT MATERIAL-INDEX CACHE - 2026-07-28
--------------------------------------------------
The resident geometry pack already avoids full vertex/index/metadata repacking
when its assignment, geometry, and material generations are unchanged. The
remaining per-frame full-triangle path rebuilt every resident triangle's
material-table index, rescanned active triangle ranges for missing indexes, and
hashed the complete array before an unchanged upload could be skipped.

The material-index cache now owns:

  - the complete resident triangle-to-table index array;
  - one missing-index count per resident bucket;
  - the resident-pack content signature;
  - the resolved table index of every material ID referenced by the pack;
  - exact triangle and bucket counts.

Portal active membership is intentionally not a cache key. An unchanged or
portal-only frame reuses the array, sums cached missing counts for active
buckets, and skips the GPU upload without rehashing every triangle. Route 2
places the full resident material universe before portal-selected and dynamic
supplements, so unrelated table churn cannot shift resident indexes. Any pack,
referenced binding, count, exactness, or cache-validity change rebuilds. CPU
cache state clears with the geometry universe, not with a GPU-only BLAS
release.

Offline proof:

  full win64-pt-dev-release build                    passed
  explicit PathTraceCpuPlanningHarness target       passed
  PathTraceCpuPlanningHarness --stress 20           passed
  unchanged pack/binding cache reuse                passed
  binding/pack/count/inexact/invalid rejection      passed
  Vulkan shader rebuilds                             0
  DX12 shader rebuilds                               0
  retained primary producer SHA-256
    7B0FCD935A7C0521E29E4516D03E4C1EF13C79037AC6574F8BEB7D16B4281B4A
  executable SHA-256
    2B8D3E99C8BF930F92F57CF5646D850684BDC9667E7FF3DF1563A7382C1B7A08

Runtime test:

  1. Restart with the rebuilt executable; keep bucket BLAS/build enabled.
  2. Let the level settle with route 2 and exact 56/56 publication.
  3. Set `r_pathTracingGeometryStaticBucketAudit 1`.
  4. The active-set line must report
     `sourceBuilt/cacheHit=1/1`,
     `residentPack/materialIndexCacheHit=1/1`, and
     `materialIndexMissingActive=0`.
  5. Note the `materialBinding` value in
     `signatures(plan/active/resident/materialBinding/tlas)`.
  6. Cross portals and traverse near animated objects. The resident and
     materialBinding signatures must remain stable, and both cache hits must
     remain 1 unless resident content or a referenced binding truly changed.
  7. Reject any visual regression, device loss, or unexpected rebuild/upload.

BUCKET13 FIRST RUNTIME RESULT - 2026-07-28
------------------------------------------
Exact 56/56 publication and `materialIndexMissingActive=0` passed. The
resident-pack cache stayed hot, and the material-index cache hit for 25
consecutive build-ramp samples. Three late `materialIndexCacheHit=0` samples
with an unchanged resident signature exposed an over-broad whole-table key.
The binding-only key and stable resident-material prefix above are the
required correction; runtime acceptance remains pending one repeat.

REF-11A RUNTIME ACCEPTANCE - BUCKET14 - 2026-07-28
--------------------------------------------------
The repeat passed. After the expected cold build and first hydrated remap,
27 consecutive active-set samples reported:

  sourceBuilt/cacheHit=1/1
  residentPack/materialIndexCacheHit=1/1
  materialBinding=8100992515290164393
  materialIndexMissingActive=0

The hydrated binding signature never changed. Exact 56/56 TLAS/route
publication appeared in 22 settled samples, and the log contained no
device-removal or validation error. REF-11A is accepted.

REF-9F PORTAL-MATCHED STATIC EMISSIVE SHADOW AUDIT - 2026-07-28
---------------------------------------------------------------
The old audit compared the monolithic portal-selected inventory with route 2's
all-resident 56-bucket set, producing a meaningless 4/503 count. REF-9F keeps
the all-resident primary probe unchanged but builds a second CPU publication
from the original portal mask. The publication carries an explicit active
bucket mask consumed by emissive replay.

Bucket emissive hashes now reconstruct route-0 identity from each packed
surface record's source-triangle offset plus its surface-local primitive.
Bucket InstanceID and packed ordering remain traversal data only. The live
emissive source is still monolithic; this is exact shadow comparison evidence.

Offline proof:

  full win64-pt-dev-release build                    passed
  explicit PathTraceCpuPlanningHarness target       passed
  PathTraceCpuPlanningHarness --stress 20           passed
  source-offset + local-primitive identity case     passed
  Vulkan shader rebuilds                             0
  DX12 shader rebuilds                               0
  executable SHA-256
    6DD492281F38D9AC514E2871CE3824AD0949C4C784076F5F7D6DB6C76D79A25A

Runtime test:

  1. Keep the accepted route-2/BLAS settings and let 56/56 settle.
  2. Set `r_pathTracingGeometryStaticBucketAudit 1`.
  3. Find `GEO10 static bucket emissive identity`.
  4. Require `exact=1`, `portalPublicationValid=1`, equal monolithic/bucket
     identity counts, and all zero/collision/missing/extra/invalid values zero.
  5. The emissive route count may be smaller than 56 because it intentionally
     uses the current portal mask.
  6. Reject device loss or any visual regression, although no live route or
     shader changed in this rung.

REF-9F FIRST RUNTIME RESULT - BUCKET15 - 2026-07-28
---------------------------------------------------
The first audit was correctly rejected:

  portalPublicationValid                              1
  routes active/resident                              3/56
  triangles monolithic/bucket                         136/48
  identities missing/extra                            136/48
  invalid/zero/collision                              0/0/0

This was not a shader or GPU-route failure. The monolithic side represented
the whole draw-surface plus four-step static-preload cache, while the bucket
side represented the one-step portal mask. The first implementation also
decoded the packed surface record's triangle offset as if it addressed the
monolithic cache. It actually addresses the independent full-resident bucket
source, explaining the complete identity disjointness.

REF-9F CANONICAL-SURFACE REMAP CORRECTION - 2026-07-28
------------------------------------------------------
The repeat audit must join the independent storage universes through their
shared canonical static-surface key:

  1. Build a key-to-live-monolithic-triangle-range table.
  2. Enumerate only surfaces in portal-active buckets.
  3. Remap bucket surface-local primitive N to
     `matchedMonolithicRange.offset + N`.
  4. Filter monolithic emissives to those same matched active surfaces.
  5. Compare the resulting production identity sets exactly.

Duplicate keys, missing active surfaces, missing primitive mappings, invalid
triangle identities, or mismatched sets all fail closed. The new line reports:

  surfaces(active/matched/missing/duplicate)
  mapping(mapped/missing/invalid)
  triangles(monolithicPortal/bucket/captured/invalid)
  traversal=portal-mask-canonical-surface-shadow-only

Offline proof:

  full win64-pt-dev-release engine target             passed
  PathTraceCpuPlanningHarness --stress 20             passed
  reordered resident/monolithic surface remap         passed
  missing active canonical surface rejection          passed
  Vulkan shader rebuilds                              0
  DX12 shader rebuilds                                0
  executable SHA-256
    27ECD743FBFC917CC4D029208A98DF79C69A2EB95D0AFF17AF181E37BECD0659

Repeat acceptance requires `exact=1`, equal active/matched surface counts,
zero missing/duplicate surface bindings, zero missing/invalid mappings, equal
monolithicPortal/bucket identity counts, and all identity failure counts zero.

REF-9F AUDIT ARMING CORRECTION - BUCKET16..19 - 2026-07-28
-----------------------------------------------------------
Four repeat captures did not invalidate the canonical remap. Every cold-frame
attempt reported:

  surfaces(active/matched/missing/duplicate)          263/263/0/0
  mapping(mapped/missing/invalid)                     4777/0/0
  monolithic portal emissives                         48

The comparison could not publish bucket emissives because the one-shot audit
was entered before `loadgame` and reset on frame 1 while only 0 or 2 of 56
BLASes were ready. Later frames reached exact 56/56/56 publication, but the
audit was no longer armed.

`r_pathTracingGeometryStaticBucketAudit 1` now remains armed until its portal
publication is valid. When route 2 forces the full-resident diagnostic set, it
also waits for the full active publication to become exact. The identity
comparison, detailed dump, and reset occur only on that ready frame.

Offline proof:

  full win64-pt-dev-release build                     passed
  PathTraceCpuPlanningHarness --stress 20             passed
  cold full-resident audit readiness rejection        passed
  exact portal plus full-resident readiness acceptance passed
  shader rebuilds                                     0
  executable SHA-256
    46B74A88A673D9E35E5899ED0259AAF95B69C4BFF9DB3C842165310246904866

The original startup ordering is now supported: arm the audit and BLAS gates
before `loadgame`; the audit must wait and emit once after 56/56/56 becomes
exact.

REF-9F SETTLED STATE MISMATCH - BUCKET20 - 2026-07-28
-----------------------------------------------------
The first correctly timed audit isolated the remaining mismatch:

  portal publication                                  valid
  surfaces active/matched/missing/duplicate           116/116/0/0
  primitive mapping mapped/missing/invalid            3805/0/0
  triangles monolithicPortal/bucket/captured/invalid  4/48/48/0
  identities missing/extra                            4/48

Geometry selection and identity addressing passed. The mismatch is runtime
triangle state. The resident bucket source labels authored emissive-capable
surfaces as stage-on, while the draw-surface monolithic cache records current
stage enablement. Runtime material-table rows and other class flags may also
differ between these independently maintained stores.

The corrected CPU replay copies the matched monolithic triangle's current
class/stage and material-table index into an audit-only aligned overlay after
canonical surface/local-primitive remap. It reports raw mismatch counts:

  state(mapped/invalid/class/stage/nonStage/materialIndexMismatch)

and then evaluates bucket geometry using the exact live state that production
currently uses. Overlay coverage must equal active primitive coverage with
zero invalid rows. Resident geometry, GPU class metadata, shaders, descriptors,
and primary traversal remain unchanged.

Offline proof:

  full win64-pt-dev-release build                     passed
  PathTraceCpuPlanningHarness --stress 20             passed
  reordered live-state overlay                        passed
  stage/material mismatch reporting                   passed
  shader rebuilds                                     0
  executable SHA-256
    4EEC941B8F3997A65BF774A009FC146BD09FAFCE83AA724555BDA0E831CFAD42

Runtime acceptance now requires exact overlay coverage, equal
monolithicPortal/bucket/captured counts, equal identity sets, and zero invalid,
zero, collision, missing, or extra counters. Raw class/stage/material mismatch
counts are diagnostic and may be non-zero.

REF-9F EMPTY-SET OVERLAY ACCEPTANCE - BUCKET21 - 2026-07-28
-----------------------------------------------------------
The corrected executable survived the full-resident route and emitted one
settled audit:

  route 2 requested/accepted                         1/1
  buckets active/resident/ready                      56/56/56
  portal routes active/resident                      5/56
  surfaces active/matched/missing/duplicate          155/155/0/0
  mapping mapped/missing/invalid                     4932/0/0
  state mapped/invalid                               4932/0
  raw class/stage/nonStage mismatch                  0/0/0
  raw material-index mismatch                       3998
  triangles monolithicPortal/bucket/captured/invalid 0/0/0/0
  identities missing/extra                          0/0
  exact                                              1

No device-removal, buffer-binding, or validation error appears in the capture.
The large raw material-index mismatch is expected evidence that the overlay,
not resident authored state, supplied the comparison's live binding rows.

This accepts complete overlay coverage and the empty-set case only. REF-9F
still needs one otherwise identical settled audit with
`triangles(monolithicPortal/bucket/captured)` all equal and greater than zero.
Do not change code for that repeat. Use a portal position containing a live
static emissive surface, keep route 2 settled at 56/56/56, then arm
`r_pathTracingGeometryStaticBucketAudit 1`.

REF-9G CANONICAL EMISSIVE REPLAY ADDRESS - 2026-07-28
-----------------------------------------------------
The REF-10 entry audit found a hidden false-positive path in REF-9F. Identity
hashes were reconstructed from monolithic source identity, but each bucket
emissive record still carried:

  instanceId       bucket BLAS first-surface record
  primitiveIndex   packed primitive across the entire bucket

That is not a hardware hit address for a multi-geometry BLAS. `GeometryIndex`
selects a surface and `PrimitiveIndex` restarts at zero for that surface.
Several emissive consumers use the record fields for geometry replay and
self-intersection suppression, so hash equality alone could not admit them.

The CPU publication now gives each packed triangle a canonical replay tuple:

  instanceId       namespace | exact surface-record index
  primitiveIndex   that surface record's source triangle index

The surface record recovers local PrimitiveIndex by subtracting its source
offset. Identity hashing remains independent and continues to use route-0
monolithic instance/primitive identity. No GPU buffer layout, descriptor,
payload, SBT, shader, TLAS, or route predicate changed.

The audit reverses every emitted bucket emissive record to its packed triangle
and adds:

  replay(records/mapped/invalidInstance/invalidSurface/invalidPrimitive/
         duplicate/class/material)

Acceptance requires records equal mapped and all six failure categories zero,
in addition to the existing non-empty exact identity requirements.

Offline proof:

  canonical 5/5 packed-triangle replay plan           passed
  overlapping surface ownership rejection             passed
  PathTraceCpuPlanningHarness --stress 20              passed
  full win64-pt-dev-release build                      passed
  SPIR-V shader rebuilds                               0
  executable SHA-256
    48C8FE4703163E6CD61D9ADCD153BCB597E37F5D30B4066E26334EC12F93F780

The updated executable is deployed. Repeat the existing pre-load audit at the
save's initial position; the required line must now have a non-zero equal
triangle set and an exact replay tuple.

REF-9F/REF-9G NON-EMPTY ACCEPTANCE - BUCKET22 - 2026-07-28
-----------------------------------------------------------
The initial save position supplied the required live static emissive set:

  portal routes active/resident                      3/56
  surfaces active/matched/missing/duplicate          263/263/0/0
  mapping mapped/missing/invalid                     4777/0/0
  state mapped/invalid/class/stage/nonStage/material 4777/0/0/0/0/0
  replay records/mapped                              48/48
  replay invalidInstance/invalidSurface              0/0
  replay invalidPrimitive/duplicate/class/material   0/0/0/0
  triangles monolithicPortal/bucket/captured/invalid 48/48/48/0
  identities monolithic/bucket                      48/48
  identities zero/collision/missing/extra           0/0/0/0
  exact                                               1

No device-removal, binding, or validation error appears in the capture.

The launch did not set `r_pathTracingNsightGpuMarkers 1`; therefore route 2
remained monolithic with `consumerSupported=0` and `primaryOpaqueProbe=0`.
That is expected fail-closed behavior and does not invalidate the independently
constructed CPU shadow publication. Earlier captures already accepted the
primary route. REF-9F and REF-9G are accepted for non-empty canonical emissive
identity and replay addresses.

REF-10 may now begin the clean-DI secondary-address slice under compile-only
gating. Requirements:

  1. resolve bucket hardware hits from InstanceID, GeometryIndex, and
     surface-local PrimitiveIndex in the hit stage;
  2. return canonical surface InstanceID plus source triangle through the
     existing hitInstanceId/hitPrimitiveIndex payload fields;
  3. reconstruct post-hit geometry through the canonical surface record;
  4. canonicalize self-ignore comparisons;
  5. do not add payload fields, descriptors, SBT records, or hit groups;
  6. keep clean-DI view 16 and production route mode 1 unsupported until a
     standalone compile/decode checkpoint passes.

REF-10A CLEAN-DI SECONDARY ADDRESS COMPILE GATE - 2026-07-28
------------------------------------------------------------
Implemented the focused compile/decode slice:

  - common route helper resolves hardware
    InstanceID + GeometryIndex + PrimitiveIndex;
  - the hit stage canonicalizes to surface-record InstanceID plus source
    triangle without adding payload fields;
  - post-hit geometry, primary-record, and emissive replay reverse that same
    tuple through one checked surface record;
  - material ID/index/class reads use the already resolved packed address;
  - visibility and emissive self-ignore compare canonical tuples;
  - spatial static-emissive geometry replay now consumes the canonical record.

The 176-byte trace-hit payload is unchanged. No new resource, descriptor, SBT
record, hit group, or CPU cutover condition was introduced.

The first compile attempt enabled the route in the all-views sentinel and hit
DXC's SPIR-V ID ceiling. That library already sits at the compiler limit. The
accepted shape compiles the route only into focused initial, temporal, and
spatial entry libraries plus the dedicated transmission producer; the
standalone sentinel preprocesses it out. This is not a compact-hit-library
detour and adds no pipeline-family ABI.

Proof:

  win64-pt-dev-release                                passed
  initial/debug + production                          2/2
  temporal/debug + production                         2/2
  spatial/debug + production                          2/2
  transmission producer                               passed
  primary surface producer                            passed
  spirv-val                                           9/9
  RayGeometryIndexKHR in focused entry blobs          7/7
  sentinel hash vs deployed baseline                  exact

Changed Vulkan blob hashes:

  primary
    B7631134D3EDA196121D081FAC6118AAFA54A8283E8D9F9C3B5A1B96B0D2F737
  initial
    715CB03BD6488EFA620F2C7A5C380A7FC4DB7E4F33ECD82DD1CED0B95CDF61D5
  initial production
    C0ED432BC97D436CFD83123ADCC5E614F98DF8A1E59BD9B43E34728421DB305F
  spatial
    5A593747EE3290E3C240DC8EC75DF681FC06EF8CC041060EE0DDA06F1026AB27
  spatial production
    E4F37A6B694624C32C8714306C413F5AC38763D3EA93798EC3BA765495551C05
  temporal
    D912C0C83A830F918FC56F4FF1CE127707B4C2A30AE1772B45C1DA717C7254CE
  temporal production
    58CCD3E63CDB6F3C9CE0C5A8243D853BD00AEBAD59F11971537D862B3137D58F
  transmission producer
    30152E184DCCC0E6109E4CF43555F649B2E3B408F71BA2FB9BD4EEA70B764AF8

The eight changed SPIR-V blobs and executable were deployed with matching
source/destination hashes. Runtime clean-DI bucket traversal remains
unsupported by the CPU route predicate; no game test is required for this
compile-only gate.

REF-10B MARKER-GATED VIEW-16 SECONDARY PROBE - 2026-07-28
---------------------------------------------------------
The exact view-16 runtime consumer set is:

  - primary surface producer;
  - production initial, temporal, and spatial clean-DI libraries;
  - transmission/reflection producer with the existing 176-byte payload;
  - glass material-feature pass;
  - canonical static emissive geometry and self-ignore records.

The glass pass now compiles the canonical surface/source replay. The CPU route
has a separate clean-DI secondary predicate. It accepts only:

  route mode                                           2
  clean DI                                             enabled
  clean-DI view                                        16
  diagnostic GPU markers                              enabled
  clean GI                                             disabled

Route mode 1, other views, disabled DI, missing markers, and enabled GI all
remain fail-closed. Existing primary-only isolate probes are unchanged.

When the secondary predicate is accepted, live static emissives come from the
canonical bucket publication accepted by REF-9F/REF-9G. Primary-only probes
continue using monolithic emissives. This prevents a view-16 visibility ray
from comparing a canonical bucket hit with a monolithic emissive instance ID.

Offline proof:

  secondary predicate positive case                    passed
  secondary predicate rejection cases                  passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  glass spirv-val                                      passed
  glass RayGeometryIndexKHR                            present
  glass blob newer than source                         passed
  glass deployed hash
    631033E8241608FEA0D83E9D77D5C1F8BD86FB45541520D813110862AE5BD9D9
  executable deployed hash
    025A95995DCED7E7C7EC0D3AA5D2270C8944BA5DE7228123BAD2A3C1DEC33183

Focused runtime sequence, set before loading the save:

  r_pathTracingNsightGpuMarkers 1
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketBlas 1
  r_pathTracingGeometryStaticBucketBlasBuild 1
  r_pathTracingGeometryStaticBucketRoute 2
  loadgame doom3_1

Let the full-resident build settle, traverse several portals, inspect glass and
static emissive surfaces, then `condump bucket23.log`. Do not enable smoke log
and do not use route mode 1. Acceptance requires the cutover line to report
`requested/accepted=1/1`, `cleanDiSecondaryProbe=1`, exact resident readiness,
and `traversal=bucket-resident-pool`, with no device removal, validation error,
missing geometry/material, portal flicker, glass failure, or emissive
self-shadow regression.

REF-10B RUNTIME REJECTION AND SAFE ROLLBACK - 2026-07-28
--------------------------------------------------------
The focused sequence above failed twice during level load. Both runs caused an
NVIDIA display-driver timeout/recovery and then terminated the process. The
Windows evidence was:

  display event 4101                                  2 matching recoveries
  provider                                             Display / nvlddmkm
  process exception                                    0xc0000409
  process fault module                                 ucrtbase.dll
  new condump                                          none
  Aftermath/checkpoint artifact                        none found

This rejects the REF-10B admission. The secondary predicate is removed from
the CPU plan, scene material hydration/publication/cutover return to the
primary-isolate predicate, and view 16 again traverses monolithic static
geometry. The route-aware glass shader remains compile-only; the REF-10A
canonical tuple work remains intact.

Do not repeat the full view-16 route-2 test with the rejected executable. A
future admission must first narrow the failure to a dispatch boundary or
produce a useful GPU crash/checkpoint artifact. Existing route-2 primary
isolates remain available for bounded testing; route mode 1 remains closed.

Rollback verification:

  win64-pt-dev-release                                passed
  PathTraceCpuPlanningHarness --stress 20              passed
  shader rebuild                                      not required
  deployed executable SHA-256
    68E8DCCD42E599BDBC18D0622FE6E3387621F8AF0DAD41268B7C20CAD74C4B6C

REF-10C CHECKPOINTED VIEW-16 STAGE ISOLATION - 2026-07-28
---------------------------------------------------------
The next diagnostic does not restore the rejected REF-10B all-consumer gate.
`r_pathTracingGeometryStaticBucketSecondaryProbeStage` is default 0 and is the
additional CPU admission requirement for route-2/view-16 traversal.

Stage meanings:

  0  closed; monolithic view 16
  1  bucket acceleration structures + primary surface only
  2  + pre-DI transmission PSR
  3  + initial
  4  + temporal
  5  + spatial
  6  + post-DI transmission/glass material composition

Stages are cumulative. The dispatch planner is inactive for monolithic
publication, so this cvar cannot truncate normal view 16. An active bucket
publication with stage 0 fails closed after primary rather than running the
full secondary chain.

All active stages suppress the adjacent full-screen NEE-cache primary-surface
update. It is outside this cumulative clean-DI ladder; allowing it to run made
the first stage-1 attempt ambiguous rather than a primary-only result.

The corrected stage-1 executable SHA-256 is:

  7BA7622537920FE2002E82B204587850010C1CABD62BD4DF6E0D4A47FB7E710F

Corrected stage-1 runtime verdict: REJECTED. It reproduced device removal.
Every view-16 stage value is now fail-closed. Do not repeat this test or
advance the ladder; the next implementation slice must remove the
bucket-specific primary hit branch in favor of the existing static decode.

REF-10D implements that primary collapse. The primary shader resolves the
bucket hardware tuple once, normalizes resource lookup to instance 0 plus the
packed resident triangle, and reuses the monolithic static any-hit/closest-hit
logic. View 16 remains prohibited. Validate route 2 in view 2 before any new
production-view admission.

REF-10D view-2 runtime result: PASSED. Route-2 traversal loaded and moved
through the test scene without a visible issue. The next authorized test is
the corrected view-16 stage 1 only. Stages 2 through 6 remain prohibited.

Checkpoint coverage:

  static-bucket BLAS build                             area/split/key label
  monolithic or bucket TLAS build                      distinct label
  primary surface                                      stage-1 label
  transmission PSR                                     stage-2 outer label
  clean-DI initial/temporal/spatial                    stage-3/4/5 labels
  material-feature compose                             stage-6 outer label

`r_pathTracingNsightGpuMarkers 1` inserts checkpoints but does not persist
their device-lost warnings. Start the process with a flushed logfile:

  +set logFileName bucket_stage1.log +set logFile 2

Then set this exact stage-1 configuration before loading:

  r_pathTracingNsightGpuMarkers 1
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiExternalPdfNeeCurrent 0
  r_pathTracingRestirPdfNeeVerifierEnable 0
  r_pathTracingCleanRtxdiDiTransmissionProducer 1
  r_pathTracingCleanRtxdiDiTransmissionCompose 1
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketBlas 1
  r_pathTracingGeometryStaticBucketBlasBuild 1
  r_pathTracingGeometryStaticBucketRoute 2
  r_pathTracingGeometryStaticBucketSecondaryProbeStage 1
  loadgame doom3_1

Stage 1 intentionally omits DI lighting and later composition, so incomplete
or diagnostic-looking output is expected. Acceptance is no device removal and
a cutover line containing `secondaryIsolation=1 secondaryStage=1` plus
`traversal=bucket-resident-pool`. On failure, preserve `bucket_stage1.log`;
its final `Vulkan device lost checkpoint` line is the primary evidence. Do not
advance to stage 2 until the stage-1 result is recorded.

Offline proof:

  isolation admission/rejection tests                 passed
  exact cumulative dispatch-plan tests                 passed
  active stage-0 fail-closed test                      passed
  monolithic non-interference test                     passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  shaders rebuilt/deployed                             none
  deployed executable SHA-256
    571E6ABBEFCF8C47B66A7709C464EC8D456F314737AF90DACD81B5B33FEE9ADE

REF-10E TRUE PRIMARY-ONLY CORRECTION - 2026-07-28
-------------------------------------------------
The previous stage-1 wording was stronger than its implementation. It skipped
secondary dispatches, but view 16 still eagerly created the clean-DI
initial/temporal/spatial production pipelines and material-feature pipelines
before the primary producer ran. Therefore its device removal did not prove
that the primary trace was the fault.

The corrected stage-1 path:

  1. Requires transmission producer and compose to both be 0.
  2. Skips NEE-cache task clears.
  3. Skips clean-DI sentinel, initial, temporal, spatial, external PDF, and
     material-feature pipeline creation.
  4. Creates the primary producer pipeline if needed.
  5. Dispatches the primary producer over the accepted bucket TLAS.
  6. Returns immediately; no clean-DI binding setup, allocation, dispatch,
     compose, GI, RR, or readback path follows.

The ordinary route-2 primary probe now accepts view 2 only. Views 17, 19, and
24 are rejected because view 19 could enter the production resolve chain
without activating the view-16 stage limiter.

Do not change any shader for this diagnostic. The missing
`surfaceRecordBase < routeInfo.y` shader hardening is real, but the shared
include fans out across DI, GI, and feature pipelines and would invalidate the
warm pipeline-cache premise. Apply and rebuild it only after this C++-only
pipeline-creation test records a result.

Authorized runtime configuration:

  r_pathTracingNsightGpuMarkers 1
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiExternalPdfNeeCurrent 0
  r_pathTracingRestirPdfNeeVerifierEnable 0
  r_pathTracingCleanRtxdiDiTransmissionProducer 0
  r_pathTracingCleanRtxdiDiTransmissionCompose 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketBlas 1
  r_pathTracingGeometryStaticBucketBlasBuild 1
  r_pathTracingGeometryStaticBucketRoute 2
  r_pathTracingGeometryStaticBucketSecondaryProbeStage 1

Stages 2 through 6 remain prohibited. The expected one-time console line is:

  GEO-10 view-16 stage-1 primary-only dispatch completed

If this build survives, the failing surface is secondary pipeline creation or
later view-16 setup, not bucket traversal in the primary producer. If it still
removes the device, preserve the Vulkan checkpoint line if one is emitted;
that is the first result that isolates the primary-only command stream.

Offline proof:

  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  rebuilt SPIR-V blobs                                 40
  rebuilt SPIR-V content differences vs deployment    0
  SPIR-V deployed                                     none
  executable SHA-256
    2328767BED6F43E49AA85B5A2700507FEC0862783DFD8409E8D238BBF487FE53

The shader target reran because the shared header's filesystem timestamp
changed during review and was restored. All 40 regenerated Vulkan blobs were
byte-identical to the currently deployed copies, so copying them would only
change timestamps and was intentionally skipped.

REF-10F COLD PIPELINE-CREATION SPLIT - 2026-07-28
-------------------------------------------------
The REF-10E run removed the device at 16:02:45 during the first cold level
load. Console history shows route 2 and view 16 were selected before any
route-0/view-2 warm-up load. No engine checkpoint line survived, so REF-10E
does not distinguish primary pipeline creation from primary dispatch.

The replacement is one two-step test in the same process:

  stage 1  create the primary RT pipeline with deferred host operations;
           return before `DispatchRays`
  stage 2  reuse that pipeline and dispatch the primary producer only;
           return before every secondary consumer

Stages 3 through 6 are rejected and no longer represent the old cumulative
secondary ladder.

Cold stage-1 configuration:

  r_pathTracingNsightGpuMarkers 1
  r_pathTracingCleanRtxdiDiEnable 1
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiExternalPdfNeeCurrent 0
  r_pathTracingRestirPdfNeeVerifierEnable 0
  r_pathTracingCleanRtxdiDiTransmissionProducer 0
  r_pathTracingCleanRtxdiDiTransmissionCompose 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketBlas 1
  r_pathTracingGeometryStaticBucketBlasBuild 1
  r_pathTracingGeometryStaticBucketSecondaryProbeStage 1
  r_pathTracingGeometryStaticBucketRoute 2
  loadgame doom3_1

Wait for:

  GEO-10 primary pipeline creation completed; stage 1 returns before DispatchRays

Do not restart or reload. If stage 1 survives, set only:

  r_pathTracingGeometryStaticBucketSecondaryProbeStage 2

Stage 2 should print:

  GEO-10 view-16 stage-2 primary-only dispatch completed

Interpretation:

  stage 1 removes device
    Primary pipeline creation or the already-proven bucket AS setup is unsafe;
    no ray dispatch occurred.

  stage 1 survives, stage 2 removes device
    The bucket primary dispatch is unsafe.

  both survive
    The prior failure was cold synchronous pipeline pressure or a consumer
    removed by REF-10E, not the primary bucket traversal itself.

Offline proof:

  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  shaders rebuilt/deployed                             none
  executable SHA-256
    651EED97E60460AFEAAB04D68B7A4E03FAEC02545FEDA4C130CF3C36A729F93C

REF-10F RUNTIME RESULT AND SAFETY ROLLBACK - 2026-07-28
-------------------------------------------------------
Stage 1 removed the device before stage 2 was entered. Console history proves
the last command sequence selected stage 1 and never selected stage 2.
Windows recorded LiveKernelEvent 141 and display recovery event 4101 at
16:11:01. Therefore no primary `DispatchRays` occurred.

Runtime disposition:

  - stages 1 through 6 are all rejected;
  - route 2 remains available only through the established view-2 diagnostic;
  - the deferred-host primary-pipeline experiment is removed;
  - no further in-game test is authorized for the view-16 surface-record
    architecture.

Next implementation slice:

  1. Replace the surface-record/GeometryIndex validating route with bucket
     BLAS topology over the existing packed static triangle pool.
  2. Encode one triangle base in each bucket TLAS instance.
  3. Resolve `triangleBase + PrimitiveIndex` once and feed the established
     static lookup path.
  4. Use contribution 0 and the existing primary hit groups.
  5. Delete the bucket-specific shader decoder and any descriptors that become
     dead.
  6. Keep resident packing and unchanged bucket BLASes cached.
  7. Prove descriptor/SBT/payload parity and primary SPIR-V size before
     considering a new runtime gate.

Safety build:

  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  shaders rebuilt/deployed                             none
  executable SHA-256
    45723965047A5566BD7901EA81D825E7FCC1CB5E4A0504ABB487EE6B7A7762DD

REF-10G STORAGE/TOPOLOGY AUDIT AND CPU-FIRST PIVOT - 2026-07-28
---------------------------------------------------------------
The post-failure source audit narrows the redesign. GEO-10 does not need a
second vertex/index/material universe: the resident pack already contains one
contiguous full-map static pool, with each bucket represented by an exact
contiguous vertex/index/triangle range. Resident packing and material-index
translation are already signature-cached.

The unnecessary parallel universe begins after packing:

  - one BLAS geometry descriptor is emitted per source surface;
  - the TLAS InstanceID encodes the first surface-record index;
  - GeometryIndex selects another four-word surface record;
  - the hit path validates that record to reconstruct the packed triangle;
  - the class buffer upload includes a surface-record suffix solely for that
    reconstruction.

The replacement CPU/GPU topology is therefore:

  resident storage       unchanged contiguous static pool
  bucket BLAS            one geometry descriptor over the bucket index range
  TLAS InstanceID        namespace bit plus bucket triangle base
  hit triangle           triangle base plus PrimitiveIndex
  SBT contribution       0
  GeometryIndex          structurally always 0 and not part of decode

First implementation slice:

  1. Make `BuildSmokeStaticBucketBlasGeometryPlan` emit exactly one contiguous
     geometry range per valid bucket.
  2. Make the BLAS scaffold upload only the triangle-class prefix to its class
     buffer; surface records remain CPU-only legacy validation data.
  3. Remove surface-record fields from the BLAS input signature so metadata
     changes cannot retire unchanged geometry.
  4. Publish triangle-base InstanceIDs for bucket TLAS instances and CPU route
     observations.
  5. Keep all shader consumers and every live route, including the old view-2
     probe, rejected.

This slice intentionally does not edit or rebuild shaders. The next slice must
replace the shared HLSL resolver, define how liquid/decal/emissive replay keeps
stable identity without a surface-record decode, and prove that the resulting
primary library is smaller before runtime admission.

Offline proof:

  one geometry descriptor per valid bucket             passed
  BLAS independence from legacy surface metadata       passed
  triangle-base InstanceID boundary/overflow tests     passed
  former view-2 and all view-16 routes rejected        passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  deployed executable SHA-256
    6BB3595C94CFBE45A5458B25BEA6A72CE2779CF4767CE4802B75363003DDF565

REF-10H ARITHMETIC SHADER AND REPLAY CONTRACT - 2026-07-28
----------------------------------------------------------
The one-geometry bucket topology makes the shader identity contract:

  hardware tuple       triangle-base InstanceID + bucket-local PrimitiveIndex
  packed lookup        DecodeTriangleBase(InstanceID) + PrimitiveIndex
  static decoder       instance 0 + packed triangle index
  replay tuple         unchanged hardware tuple
  detail-decal sort    packed triangle index
  GeometryIndex        must equal 0

This tuple is stable because the full resident pack is signature-cached and
does not change when portal activity changes. CPU emissive parity retains the
existing surface/source mapping for comparison and record construction; that
mapping is no longer uploaded or decoded by hit shaders.

`StaticBucketRouteInfo.y` now carries the resident packed triangle count. The
shared HLSL resolver checks the namespace, base, GeometryIndex, global triangle
range, index range, and vertex range, then performs the single base-plus-local
addition. The four-word surface-record load and every surface namespace test
are deleted.

All live routes remain rejected during compilation and SPIR-V inspection.

Offline proof:

  affected Vulkan SPIR-V validation                    9/9 passed
  affected SPIR-V aggregate size                       -91,868 bytes
  primary producer size                                1,050,900 -> 1,041,124
  spatial debug size                                   388,676 -> 357,264
  spatial production size                              353,388 -> 329,884
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  changed Vulkan blobs deployed                        9
  DX12 blobs built/deployed                            0/0
  deployed executable SHA-256
    5EE62D5E7B7B53695583E96F5A5B1D1FF8E1D87782C97BF36CA6163087DDBA12

Every rebuilt blob has an mtime newer than
`PathTraceStaticBucketRoute.hlsli`. The long first build rebuilt 50 transitive
Vulkan outputs; 41 were byte-identical to the deployed copies and were not
copied. The CPU publication field is also renamed from the obsolete
`staticBucketSurfaceRecordCount` to `staticBucketTriangleCount`.

REF-10I DEAD ABI/UPLOAD RETIREMENT - 2026-07-28
-----------------------------------------------
The contribution-4/5 static-bucket SBT constants and their six-record assert
have no remaining producer or consumer: bucket TLAS instances use contribution
0 and the ordinary primary/shadow pair. They are deleted.

The packed triangle-identity array remains useful to CPU emissive parity, but
its GPU buffer had no binding or shader reader. Creating, uploading, tracking,
and retaining that buffer is deleted. A bucket resident-pack upload now
contains four buffers: vertices, indexes, triangle classes, and material IDs.
Material indexes remain a separately generation-cached upload.

Offline proof:

  obsolete static SBT references                        0
  unused identity GPU buffer references                 0
  resident-pack buffer uploads                          5 -> 4
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  deployed executable SHA-256
    F99804E347DBAD2CD66FAA91961050E65CDA81DDA24C04AC050140D785FABBF6

REF-10J BOUNDED ARITHMETIC-ROUTE READMISSION - 2026-07-28
---------------------------------------------------------
Re-admit only the instrumented view-2 primary opaque probe after REF-10G
through REF-10I. This is not the failed view-16 pipeline-creation route:

  accepted view                 2
  required route mode           2
  required clean DI             enabled
  required GPU markers          enabled
  required clean GI             disabled
  view 16                       rejected
  production route mode 1       rejected
  secondary stages              rejected

The probe now exercises one-geometry bucket BLASes, contribution 0, standard
static descriptors, and the arithmetic triangle-base decoder. It cannot enter
the view-16 clean-DI consumer chain that produced the prior cold TDR.

Offline proof:

  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  deployed executable SHA-256
    79F2069173D3D6FC49B07395F212EA3B8BCBCA5FBD962AB4A3929312277B6F35

REF-10J RUNTIME ACCEPTANCE - 2026-07-28
---------------------------------------
The user reported that the rebuilt route-2/view-2 probe seemed fine. The
one-geometry bucket TLAS, contribution-0 hit groups, standard static
descriptors, and one-add triangle decoder are accepted for the isolated
primary status view.

REF-10K VIEW-16 PRIMARY PIPELINE-ONLY READMISSION - 2026-07-28
--------------------------------------------------------------
The next rung re-admits only secondary-isolation stage 1 under the exact
route-2/view-16/markers-on/GI-off/external-PDF-off/transmission-off contract.
The dispatch plan creates the ordinary primary pipeline and returns before
DispatchRays. Its active-isolation guard also skips clean-DI sentinel,
initial, temporal, spatial, material-feature, external-PDF, transmission, and
NEE-cache pipeline initialization.

Stage 2 and production route mode 1 remain rejected. This is a CPU admission
change only; no shader, descriptor, SBT, or payload change is required.

Offline proof:

  stage-1 positive/rejection matrix                    passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  source/deployed executable hashes                    equal
  executable SHA-256
    9F6A95266BBA6FCB1C623E06DDC80BD99EB2CE4E4E96EE9B902736752DEA76C1

REF-10K RUNTIME VERDICT - REJECTED - 2026-07-28
------------------------------------------------
The exact stage-1 configuration removed the device as the level loaded. The
console history confirms route 2, view 16, markers enabled, GI disabled,
external-PDF paths disabled, transmission producer/compose disabled, and
secondary stage 1 immediately before `loadgame doom3_1`.

The intended stage 1 returns before DispatchRays, but post-crash control-flow
audit found that its isolation guard depended on accepted route publication.
During the bucket build ramp, the guard was inactive and dispatch fell through
to the full monolithic view-16 DI path. The crash therefore does not localize
to primary pipeline creation. All view-16 stages are again fail-closed. Do not
repeat or advance the ladder until requested isolation is latched before
publication readiness.

Rollback proof:

  exact console-history configuration                 confirmed
  Windows LiveKernelEvent                             141 at 17:13
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  source/deployed executable hashes                    equal
  fail-closed executable SHA-256
    768E6BB390AFA9AD87ECC63058E6FD4C521D770947EF7A4F6A3B9AE1561F3830

REF-10L PRE-PUBLICATION ISOLATION LATCH - 2026-07-28
-----------------------------------------------------
Post-crash audit found the stage ladder's `active` bit was derived from
`staticBucketRoutePublicationValid`. While bucket BLAS publication was still
ramping, requested stage 1 therefore looked inactive and inherited the normal
view-16 defaults: primary dispatch, NEE-cache update, transmission, initial,
temporal, spatial, and material-feature compose were all enabled.

The dispatch plan now separates four facts:

  requested
  supported
  routePublicationValid
  active

A nonzero mode-2/view-16 diagnostic request latches `active` immediately.
Every secondary flag becomes false at that point. Primary pipeline creation
requires both `supported` and `routePublicationValid`; primary dispatch also
requires stage 2 or later. Unsupported or not-yet-published requests return
before pipeline creation, so they cannot fall through to production work.

The support predicate remains revoked. This is an offline fail-closed
correction, not authorization to repeat REF-10K.

Offline proof:

  requested + unsupported pre-pipeline rejection       passed
  requested + publication-pending rejection            passed
  supported stage-1 create/no-dispatch plan             passed
  supported stage-2 create/dispatch plan                passed
  monolithic non-request defaults unchanged             passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  source/deployed executable hashes                    equal
  executable SHA-256
    AEB6578E2B5C4B2929F5981FA835BC9FD83B810ACBFEB67DA214559BAAE8B982

REF-10L RUNTIME ACCEPTANCE - SUCCESS.LOG - 2026-07-28
------------------------------------------------------
The fail-closed latch passed runtime validation. On frame 1, only 2 of 56
bucket BLASes were ready, cutover was rejected, and dispatch reported:

  GEO-10 view-16 isolation fail-closed before pipeline creation
  stage=1 supported=0 publication=0

The bucket build ramp then reached 56/56 ready with exact publication. Because
runtime support remained revoked, cutover stayed rejected and the same
pre-pipeline guard remained active. The log contains no device-loss, Vulkan
error, or diagnostic-checkpoint failure.

This accepts REF-10L only. It does not accept REF-10K stage 1 and does not
authorize another view-16 rung.

REF-10M POST-LATCH STAGE-1 READMISSION - 2026-07-28
----------------------------------------------------
After REF-10L runtime acceptance, re-admit only the exact stage-1 support
predicate. The corrected sequence is now:

  1. a nonzero mode-2/view-16 request latches isolation immediately;
  2. while bucket publication is incomplete, return before all pipelines;
  3. after exact publication, create only the ordinary primary pipeline;
  4. return before DispatchRays.

Stage 2, stages 3-6, and production route mode 1 remain rejected. This is a
CPU admission/help-text change only; no shader, descriptor, SBT, or payload
change is required.

Offline proof:

  stage-1 support predicate                            passed
  pre-publication pipeline block                       passed
  stage-2 and malformed-contract rejection             passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  source/deployed executable hashes                    equal
  executable SHA-256
    C14FB0D41B6E437E75C7D236EB3E384729C99FCCB6BF6634184967ECACD70723

REF-10M RUNTIME ACCEPTANCE - SUCCESS2.LOG - 2026-07-28
-------------------------------------------------------
The corrected stage-1 sequence passed. The build ramp remained pre-pipeline
until exact 56/56 publication, then logged:

  GEO-10 primary pipeline creation begin (stage=1 deferredHost=1)
  primary-surface producer RT smoke pipeline initialized
  GEO-10 primary pipeline creation completed; stage 1 returns before DispatchRays

Cutover was accepted with `consumerSupported=1`, `secondaryIsolation=1`,
`secondaryStage=1`, and `traversal=bucket-resident-pool`. The log contains no
device-loss, Vulkan, binding, or validation error. REF-10M is accepted.

REF-10N PRIMARY-DISPATCH-ONLY READMISSION - 2026-07-28
------------------------------------------------------
Re-admit stage 2 under the same latched exact contract. It waits for exact
publication, creates/reuses the accepted ordinary primary pipeline, dispatches
the primary surface producer once, and returns before NEE-cache update,
transmission, initial, temporal, spatial, or material-feature work.

Stages 3-6 and production route mode 1 remain rejected. No shader, descriptor,
SBT, or payload change is required.

Offline proof:

  stage-1/stage-2 support matrix                       passed
  pre-publication stage-2 pipeline/dispatch block      passed
  stages 3-6 and malformed contracts rejected          passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  source/deployed executable hashes                    equal
  executable SHA-256
    3751C1AE118CE0938572ED5842FAEEA3459B8C242C50C2ABB5E33ABD872EDC03

REF-10N RUNTIME ACCEPTANCE - SUCCESS3.LOG - 2026-07-28
-------------------------------------------------------
The latched stage-2 primary dispatch passed. After exact 56/56 publication,
the primary producer dispatched at 2560x1440 and logged that secondary pipeline
creation and dispatch were skipped. Cutover reported
`consumerSupported=1`, `secondaryIsolation=1`, `secondaryStage=2`, and
`traversal=bucket-resident-pool`. The log contains zero device-loss, Vulkan,
binding, or validation errors. REF-10N is accepted.

REF-10O CORE-DI PIPELINE-CREATION RUNG - 2026-07-28
----------------------------------------------------
The next bounded rung is deliberately not an initial-lighting dispatch.
Stage 3 retains the accepted primary dispatch, additionally creates the
clean-DI sentinel, initial, temporal, and view-16 production shader tables,
then returns before every secondary DispatchRays call.

This separates driver pipeline creation from secondary shader execution.
Transmission PSR, initial, temporal, spatial, material-feature compose,
external-PDF, NEE-cache update, and GI remain suppressed. Stages 4-6 and
production route mode 1 remain rejected.

Offline proof:

  stage-3 support and stage-4..6 rejection              passed
  stage-3 core-pipeline/no-secondary-dispatch plan      passed
  publication-pending/unsupported pre-pipeline block   passed
  monolithic pipeline behavior unchanged                passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  source/deployed executable hashes                    equal
  executable SHA-256
    05B44F1CCAC165F5C3CB1099C1C7B0DA00CDF8347CC1D34EEB62759A8291C90E

REF-10O RUNTIME ACCEPTANCE - SUCCESS4.LOG - 2026-07-28
-------------------------------------------------------
Stage 3 loaded and initialized the sentinel, initial, temporal, initial-
production, and temporal-production pipelines, completed the accepted primary
dispatch, and skipped every secondary DispatchRays call. There are no
device-loss, Vulkan, binding, or validation errors.

The sole new warning was:

  WARNING: idSessionLocal::Pump was not called for 3 seconds

It appears immediately after the five cold driver pipeline initializations and
is the expected one-time main-thread stall, not a GPU contract failure. The
stage-3 completion message was unintentionally emitted every frame; that
diagnostic spam is corrected before the next rung. REF-10O is accepted.

REF-10P INITIAL-DI-ONLY RUNG - 2026-07-28
------------------------------------------
Stage 4 retains the accepted primary and core-pipeline work, dispatches only
the view-16 initial DI producer, performs its required UAV barriers, and
returns before temporal, spatial, transmission, material-feature composition,
NEE-cache update, external-PDF, or GI work.

Stages 5-6 and production mode remain rejected. No shader, descriptor, SBT, or
payload change is required.

Offline proof:

  stage-4 support and stage-5..6 rejection              passed
  initial-only/no-temporal dispatch plan                passed
  stage-3 completion logging bounded to creation        passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  source/deployed executable hashes                    equal
  executable SHA-256
    A6A7959ECC06CEFDC6EA6F78DD1D0063C9DD5E414AA4DCE577D677014E1287AD

REF-10P RUNTIME VERDICT - REJECTED - 2026-07-28
------------------------------------------------
Stage 4 removed the device. The reported last GPU checkpoint was:

  GEO10.View16.Stage3 Initial DispatchRays

The marker retains the older cumulative-ladder number, but it labels the
initial-DI dispatch uniquely enabled by current stage 4. Core DI pipeline
creation and primary dispatch had already passed in REF-10O/REF-10N. The GUI
and crosshair appeared before device removal, further confirming that startup
and pipeline construction completed.

This localizes the failure to initial secondary-ray execution. Revoke stage 4
and keep stages 5-6 and production mode rejected. Do not advance the consumer
ladder; the next work must audit the initial-production shader's secondary hit
contract against the accepted primary route.

Rollback verification:

  stage-1..3 support and stage-4..6 rejection matrix   passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  SPIR-V rebuild/deployment                            none
  source/deployed executable hashes                    equal
  executable SHA-256
    C82977C400540475BEC350CAF732CC2BE218C452E7F112FAEFC9B061303F1F0A

REF-10Q ONE-ADD SECONDARY HIT REPAIR - 2026-07-28
--------------------------------------------------
The rejected initial-DI dispatch exposed a concrete violation of the resident
pool design. Clean-DI `AnyHit` and `ShadowAnyHit` called the full static-bucket
geometry decoder for per-triangle material lookup. That decoder read and
validated three indices and three vertex bounds even though hardware traversal
had already supplied the bucket-local primitive and the hit shaders needed only
the material row.

The repaired hit path now performs the intended bounded arithmetic:

  triangleBase  = InstanceID & 0x007fffff
  triangleIndex = triangleBase + PrimitiveIndex

`GeometryIndex` must be zero and both values are checked against the published
static triangle count before `t11[triangleIndex]` is read. Full index/vertex
decode remains available only to reconstruction consumers that need geometry.
The non-static fallback is a separate helper so DXC cannot retain the full
static decoder through an otherwise-dead fallback branch.

Offline proof:

  direct production-initial candidate compile          passed
  direct all-views sentinel compile / ID ceiling       passed
  final clean-DI SPIR-V blobs validated                9/9
  every rebuilt blob newer than newest edited include  passed
  initial-production ShadowAnyHit static index SRV     absent
  initial-production bytes before/after                996396/992684
  payload shape                                        unchanged, 5 uint
  descriptor/SBT layout                                unchanged

Re-admit stage 4 only. Stages 5-6 and production mode remain rejected. The GPU
checkpoint is renamed `GEO10.View16.Stage4 Initial DispatchRays`; the old
`Stage3` label no longer applies.

Final build/deployment proof:

  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  final build shader status                            all SPIR-V up to date
  hash-changed Vulkan blobs deployed                   6
  source/deployed executable hashes                    equal
  executable SHA-256
    6DA37D4F3E334E068350EC074DF75892FDEEC41AC85CB54151B15A7D5B79B211

REF-10Q RUNTIME ACCEPTANCE - SUCCESS5.LOG - 2026-07-28
-------------------------------------------------------
The repaired stage-4 initial-DI dispatch passed. Exact 56/56 route
publication completed before pipeline creation and dispatch. The log then
reported:

  GEO-10 view-16 stage-4 initial-only dispatch completed

Cutover remained accepted with `secondaryStage=4`,
`traversal=bucket-resident-pool`, and no missing BLAS, material-index, range,
or instance failures. There is no device-loss, Vulkan, binding, or validation
error. The 248-second `idSessionLocal::Pump` warning follows cold pipeline
creation and is an expected host stall on this machine.

REF-10R TEMPORAL-DI-ONLY RUNG - 2026-07-28
-------------------------------------------
Stage 5 retains the accepted primary and initial-DI dispatches and adds only
the view-16 temporal DI producer. It performs the ordinary reservoir and
output barriers, then returns before spatial, transmission, material-feature
composition, NEE-cache update, external-PDF, or GI work.

The compiled temporal-production hit contract already contains the REF-10Q
repair. `AnyHit` and `ShadowAnyHit` declare
`SmokeStaticTriangleMaterialIndexes` but neither `SmokeStaticIndices` nor
`SmokeStaticVertices`; no shader, descriptor, SBT, or payload change is
required for this rung.

Stage 6 and production route mode 1 remain rejected.

Offline proof:

  production-temporal hit-interface disassembly        passed
  stage-5 temporal-only/stage-6 rejection matrix       passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  final build shader status                            all SPIR-V up to date
  SPIR-V deployment                                    none
  source/deployed executable hashes                    equal
  executable SHA-256
    7A97713902CB672CA15BC8A1ACC13BCF13FFE78B77A035380FA85E92156C33C4

REF-10R RUNTIME ACCEPTANCE - SUCCESS6.LOG - 2026-07-28
-------------------------------------------------------
The cumulative stage-5 initial plus temporal dispatch passed. Exact 56/56
publication completed first, then the log reported:

  GEO-10 view-16 stage-5 initial-plus-temporal dispatch completed

Cutover remained accepted with `secondaryStage=5`,
`traversal=bucket-resident-pool`, and no device-loss, Vulkan, binding, or
validation error. The four-second `idSessionLocal::Pump` warning is a normal
pipeline warmup stall.

REF-10S SPATIAL HIT REPAIR AND SPATIAL-ONLY RUNG - 2026-07-28
-------------------------------------------------------------
The pre-admission spatial disassembly exposed one remaining full-decode hit
path. Spatial `AnyHit` resolved bucket identity through all three static
indices even though hardware traversal already supplied the bucket-local
primitive. `ShadowAnyHit` also retained that decoder through its general
material fallback.

The repaired hit stages use bounded:

  triangleBase  = InstanceID & 0x007fffff
  triangleIndex = triangleBase + PrimitiveIndex

Full static index and vertex decode remains in the spatial raygen interface
because spatial surface reconstruction needs triangle geometry. It is absent
from both rebuilt `AnyHit` and `ShadowAnyHit` interfaces.

Stage 6 retains accepted initial and temporal dispatches, adds only spatial
DI, performs its ordinary barriers, and returns before material-feature
composition, transmission, NEE-cache update, external-PDF, or GI work.
Production route mode 1 remains rejected.

Offline proof:

  rebuilt spatial Vulkan blobs                         2
  spatial debug/production bytes                       353760/326156
  rebuilt blobs newer than edited source               passed
  spirv-val                                             2/2
  AnyHit/ShadowAnyHit static index/vertex SRVs          absent
  full raygen reconstruction resources                 retained
  stage-6 spatial-only dispatch matrix                 passed
  PathTraceCpuPlanningHarness --stress 20              passed
  win64-pt-dev-release                                 passed
  final build shader status                            all SPIR-V up to date
  executable plus two spatial blobs deployed           hashes equal
  executable SHA-256
    EF43078C96959013B53B4814C430D5918078A71872A5AE702E3995C37DFC2242

REF-10T FORWARD CONSUMER HIT-DECODE AUDIT - 2026-07-28
-------------------------------------------------------
Before admitting anything beyond the stage-6 spatial rung, enumerate the
entire shader set that explicitly compiles with
`RB_PT_ENABLE_STATIC_BUCKET_SHADER_CONSUMERS=1`:

  primary surface producer
  clean-DI initial
  clean-DI temporal
  clean-DI spatial
  clean-DI glass
  clean-DI transmission producer

No GI, NEE-cache, or external-PDF shader enables the bucket contract. Those
subsystems remain prohibited during GEO-10 production admission.

Audit result:

  primary
    The accepted primary route still called the full index/vertex decoder
    unconditionally while normalizing bucket identity, then decoded geometry
    again in alpha or closest-hit work. Identity now uses the bounded one-add
    tuple. Index and vertex validation is deferred to detail-decal, alpha, and
    closest-hit branches that need geometry.

  initial / temporal
    AnyHit and ShadowAnyHit use one-add material metadata. Full geometry
    remains only in raygen/replay reconstruction.

  spatial
    REF-10S removes full geometry from both hit stages. Full geometry remains
    in spatial raygen reconstruction.

  glass
    AnyHit and ShadowAnyHit already use one-add metadata and declare neither
    static indices nor static vertices.

  transmission
    Hardware-hit identity already uses one-add metadata. Three material/class
    helpers still called the full decoder; those helpers now use one-add
    triangle rows. Transmission AnyHit legitimately retains index/vertex
    access for UV-dependent alpha and emissive evaluation. Its control flow
    performs at most one such UV reconstruction for the relevant material
    branch. ShadowAnyHit declares no static index or vertex buffers.

Local compile proof, not yet deployed over the stage-6 test build:

  rebuilt Vulkan libraries                            10
  rebuilt libraries newer than edited sources         10/10
  spirv-val                                            10/10
  initial/temporal/spatial/glass hit metadata          index/vertex-free
  transmission ShadowAnyHit                            index/vertex-free
  transmission AnyHit geometry access                 retained for UV
  primary hit geometry access                         retained lazily

Do not deploy or open a later gate until the outstanding stage-6 runtime
result is recorded against executable hash
`EF43078C96959013B53B4814C430D5918078A71872A5AE702E3995C37DFC2242`.

REF-10S RUNTIME RESULT CORRECTION - SUCCESS7.LOG - 2026-07-28
--------------------------------------------------------------
The cumulative stage-6 run did not execute spatial. Exact 56/56 publication
completed first and the log reported:

  GEO-10 view-16 stage-6 initial-plus-temporal-plus-spatial dispatch completed

That message was not gated by `cleanSpatialRoute`. While secondary isolation
was active, `cleanRtxdiDiSpatialShaderRequested` was forced false. A fresh
process therefore loaded no spatial shader library, `cleanSpatialRoute`
remained false, and the spatial block was bypassed. There is no spatial shader
load line in `success7.log`.

The run accepts exact bucket publication plus primary, initial, and temporal
dispatch. It does not accept spatial. The lack of device-loss, Vulkan,
binding, validation, or visual errors remains valid evidence for the paths
that actually executed. The four-second `idSessionLocal::Pump` warning is
pipeline warmup.

REF-10T AUDIT-FIX DEPLOYMENT - 2026-07-28
------------------------------------------
The compiled REF-10T fix was deployed without changing the executable. Seven
Vulkan blobs differed from the prior installation:

  primary surface producer
  clean-DI initial debug and production
  clean-DI temporal debug and production
  clean-DI glass
  clean-DI transmission producer

Sentinel and both spatial blobs are byte-identical and are not redeployed.
All seven source/deployed hashes match. The executable remains:

  EF43078C96959013B53B4814C430D5918078A71872A5AE702E3995C37DFC2242

The user repeated the same route and reported no crash or visual change. That
accepts the audit replacements for the primary, initial, and temporal paths
which actually ran. It does not admit spatial or any later consumer.

REF-10U SPATIAL PIPELINE-CREATION RUNG - 2026-07-28
----------------------------------------------------
Correct the control path before collecting another runtime result:

  - add `spatialPipelineCreation` separately from `spatial` to the isolation
    dispatch plan;
  - retain stage 6 as the highest admitted diagnostic stage;
  - at stage 6, request kind-20 spatial debug and production pipeline creation
    even though ordinary spatial shader routing is disabled by isolation;
  - print explicit begin/completed messages around that creation;
  - leave `spatial=false`, retain the accepted initial and temporal dispatches,
    and return before spatial `DispatchRays` and all later consumers;
  - keep stage 7 rejected until stage-6 pipeline creation survives runtime.

Required runtime evidence is now explicit. A valid stage-6 run must show:

  GEO-10 spatial pipeline creation begin
  GEO-10 spatial pipeline creation completed
  GEO-10 view-16 stage-6 spatial pipelines plus initial-and-temporal dispatch completed

The final line must also state that spatial `DispatchRays` and later consumers
were skipped. This isolates device loss during spatial pipeline construction
from device loss during spatial execution.

Local proof and deployment:

  CPU planning harness                              passed
  CPU planning stress iterations                    20
  full release build                                passed
  Vulkan shader build status                        all SPIR-V up to date
  source/deployed executable SHA-256                equal

  AA3C0CC8D35765D4ECDE0274B53C537BA13CCDFF5D5BD33DC225A94912A51B60

REF-10V STAGE-6 ACCEPTANCE AND SPATIAL-DISPATCH RUNG - 2026-07-28
-----------------------------------------------------------------
The corrected stage-6 runtime log contains the required evidence in order:

  - exact publication reaches 56/56 resident, active, ready, TLAS, and route
    records with zero missing BLAS, material-index, range, or instance errors;
  - the spatial pipeline-creation begin marker is emitted;
  - spatial debug loads and initializes;
  - spatial production loads and initializes;
  - the spatial pipeline-creation completed marker is emitted;
  - the final stage-6 line confirms initial and temporal dispatch completed
    while spatial `DispatchRays` and all later consumers were skipped.

There is no device-loss, Vulkan, binding, or validation failure. The
ten-second `idSessionLocal::Pump` warning is deferred pipeline warmup.

Stage 7 is therefore the next single-variable rung:

  - expand the exact isolation support range from 1-6 to 1-7;
  - preserve stage-6 `spatialPipelineCreation=true, spatial=false`;
  - set stage-7 `spatialPipelineCreation=true, spatial=true`;
  - use the already loaded production spatial shader table for view 16;
  - identify its GPU checkpoint as `GEO10.View16.Stage7 Spatial DispatchRays`;
  - return immediately after spatial output barriers;
  - keep material-feature composition, transmission, glass, NEE-cache,
    external-PDF, GI, and production routing disabled.

Required stage-7 completion evidence:

  GEO-10 view-16 stage-7 initial-plus-temporal-plus-spatial dispatch completed

The line must also state that post-DI consumers were skipped.

Local proof and deployment:

  stage-6 pipeline-only/stage-7 spatial matrix         passed
  CPU planning stress iterations                      20
  full release build                                  passed
  Vulkan shader build status                          all SPIR-V up to date
  source/deployed executable SHA-256                  equal

  FF5A8BFB8FAE1166BD8C99EB6663C4AB7A4E1DEA32E5D5571DEA1EAE30172D82

REF-10W STAGE-7 ACCEPTANCE AND MATERIAL-PIPELINE RUNG - 2026-07-28
-----------------------------------------------------------------
`success8.log` accepts spatial execution:

  publication resident/active/ready                  56/56/56
  publication TLAS/routes                            56/56
  missing BLAS/material/range/instance               0/0/0/0
  spatial debug/production initialization            passed/passed
  stage-7 initial/temporal/spatial completion         present
  post-DI consumers                                  skipped
  device-loss/Vulkan/binding/validation failures     none
  user visual result                                 no issues

Stage 8 isolates material-feature pipeline creation from execution:

  - expand the exact isolation support range from 1-7 to 1-8;
  - add `materialFeaturePipelineCreation` independently from
    `materialFeatureCompose`;
  - build the transmission and glass pipeline registrations from the forced
    layout registry, which marks both pipelines for creation without changing
    the runtime producer/compose cvars;
  - emit explicit material-feature pipeline creation begin/completed markers;
  - retain accepted primary, initial, temporal, and spatial dispatches;
  - return before transmission PSR, transmission/glass composition, and every
    later consumer.

Required stage-8 evidence:

  GEO-10 material-feature pipeline creation begin
  clean-room RTXDI DI transmission producer material-feature RT pipeline initialized
  clean-room RTXDI DI glass material-feature RT pipeline initialized
  GEO-10 material-feature pipeline creation completed
  GEO-10 view-16 stage-8 material-feature pipelines plus initial-temporal-spatial dispatch completed

The completion line must state that transmission/glass `DispatchRays` and
later consumers were skipped.

Local proof and deployment:

  stage-7 dispatch/stage-8 pipeline-only matrix        passed
  CPU planning stress iterations                      20
  full release build                                  passed
  Vulkan shader build status                          all SPIR-V up to date
  source/deployed executable SHA-256                  equal

  B288C5998741F7665FD80A5A1F7929E936B1A0ACD21AB912C17E97E1D0E4B9DA

REF-10X STAGE-8 ACCEPTANCE AND TRANSMISSION-PSR RUNG - 2026-07-28
-----------------------------------------------------------------
The updated `success8.log` supplies the complete stage-8 boundary:

  transmission producer shader/pipeline              loaded/initialized
  glass shader/pipeline                              loaded/initialized
  material pipeline begin/completed markers          present/present
  initial/temporal/spatial dispatch                   completed
  transmission/glass DispatchRays                    skipped
  device-loss/Vulkan/binding/validation failures     none
  user visual result                                 no issues

The 35-second `idSessionLocal::Pump` warning is expected deferred pipeline
construction and is not a device error.

Stage 9 adds only pre-DI transmission PSR:

  - expand support from stages 1-8 to 1-9;
  - require both `r_pathTracingCleanRtxdiDiTransmissionProducer 1` and
    `r_pathTracingCleanRtxdiDiTransmissionCompose 1` at stage 9;
  - continue requiring both controls off at stages 1-8;
  - build runtime transmission/glass registrations for the active isolation
    route, reusing the stage-8-created pipeline state;
  - dispatch only the transmission feature with the PSR phase flag before DI;
  - identify the checkpoint as `GEO10.View16.Stage9 TransmissionPSR`;
  - retain initial, temporal, and spatial dispatch;
  - return before post-DI feature composition and every later consumer.

Required completion evidence:

  GEO-10 view-16 stage-9 transmission-PSR plus initial-temporal-spatial dispatch completed

The line must state that post-DI transmission/glass composition and later
consumers were skipped.

Local proof and deployment:

  stages 1-8 transmission-off contract                passed
  stage-9 transmission-on contract                    passed
  stage-9 PSR-only dispatch plan                      passed
  CPU planning stress iterations                      20
  full release build                                  passed
  Vulkan shader build status                          all SPIR-V up to date
  source/deployed executable SHA-256                  equal

  D62E38E88C627D290EE54905D4F9A6C534E41A79B679623C504594C9FD85F66B

REF-10Y STAGE-9 REJECTION AND LIVE-BINDING RUNG - 2026-07-28
-------------------------------------------------------------
The combined stage-9 runtime attempt ended in device removal during level
load. It produced no condump; the console command history ends at
`loadgame doom3_1`. Therefore the last accepted GPU boundary remains stage 8,
where both material pipelines were initialized without live registrations or
material-feature dispatch.

The rejected stage bundled three new operations:

  1. live transmission/glass registry selection;
  2. material-feature output bindings, UAV state transitions, and clears;
  3. transmission PSR `DispatchRays`, followed by the optional sky-surface
     repair.

Replace it with two exact rungs:

Stage 9 - runtime registration and bindings only

  - require producer=1 and compose=1;
  - build the live runtime feature-pass list;
  - admit its output resources into availability checks and the clean-DI
    binding set;
  - perform the ordinary material-feature UAV states and output clears;
  - retain accepted primary, initial, temporal, and spatial dispatches;
  - keep `transmissionPsr=false` and `materialFeatureCompose=false`;
  - return before every transmission/glass `DispatchRays`.

Required stage-9 completion evidence:

  GEO-10 view-16 stage-9 material-feature runtime bindings plus initial-temporal-spatial dispatch completed

The same line must state that transmission/glass `DispatchRays` and later
consumers were skipped.

Stage 10 - transmission PSR execution

  - retain the stage-9 runtime registration and binding path;
  - set `transmissionPsr=true`;
  - identify its checkpoint as `GEO10.View16.Stage10 TransmissionPSR`;
  - dispatch only the pre-DI transmission producer;
  - return before post-DI feature composition and every later consumer.

Stage 10 is code-complete for a no-rebuild follow-up, but it is not admitted
for testing until stage 9 survives. If stage 10 fails after stage 9 passes,
split the transmission dispatch from the immediately following sky-surface
repair before considering any broader shader or ABI changes.

Local proof and deployment:

  stage-9 binding/stage-10 dispatch matrix             passed
  CPU planning stress iterations                      20
  full release build                                  passed
  Vulkan shader build status                          all SPIR-V up to date
  source/deployed executable SHA-256                  equal

  528918C78933942AD82D99C319F09432BD91637A187719401F8D8E51C2952A8F

REF-10Z STAGE-9 ACCEPTANCE AND TRANSMISSION-EXECUTION RUNG - 2026-07-28
----------------------------------------------------------------------
`success9.log` supplies the complete binding-only boundary:

  publication resident/active/ready                  56/56/56
  publication TLAS/routes                            56/56
  missing active material indexes                    0
  transmission/glass runtime registrations           initialized
  primary feature-parameter table                    bound
  initial/temporal/spatial dispatch                   completed
  transmission/glass DispatchRays                    skipped
  device-loss/Vulkan/binding/validation failures     none
  user result                                         no crash; windows black

The black windows are expected for this deliberately incomplete rung.
`ClearPathTraceCleanRtxdiDiMaterialFeatureOutputs` clears registered primary
outputs to `(0,0,0,1)`, while stage 9 explicitly prevents the transmission
producer from filling the cleared glass pixels. It does not invalidate the
geometry or binding acceptance.

Stage 9 therefore accepts:

  - live registry selection;
  - feature-output availability checks;
  - clean-DI binding-set construction;
  - UAV state transitions and output clears;
  - the feature-parameter table consumed by the primary producer.

Stage 10 is admitted as the next single-variable rung. Keep every stage-9
control unchanged and set only:

  r_pathTracingGeometryStaticBucketSecondaryProbeStage 10

The expected checkpoint is:

  GEO10.View16.Stage10 TransmissionPSR

Required completion evidence:

  GEO-10 view-16 stage-10 transmission-PSR plus initial-temporal-spatial dispatch completed

The completion line must still state that post-DI transmission/glass
composition and later consumers were skipped. If stage 10 device-removes,
split transmission `DispatchRays` from the optional sky-surface repair before
changing shader ABI, descriptors, or bucket decoding again.

REF-10AA STAGE-10 REJECTION AND NO-REBUILD TRACE-LANE SPLIT - 2026-07-28
-------------------------------------------------------------------------
The full stage-10 producer device-removed. The reported diagnostic identifies
the transmission producer. Its marker scope contains the material-feature
`DispatchRays` and output barrier; the optional sky-surface repair runs only
after that scope ends. Combined with accepted stage 9, this rejects live
producer execution while keeping the following contracts accepted:

  - transmission/glass pipeline and shader-table creation;
  - live registry selection;
  - feature output bindings, states, and clears;
  - primary, initial, temporal, and spatial bucket traversal.

The producer is not one indivisible trace. Default controls admit:

  - opaque exact-mirror tracing;
  - glass mirror tracing;
  - reflection-hit analytic shading and optional shadow tracing;
  - refracted behind-glass continuation tracing.

Use the existing runtime controls before compiling a new probe shader. Keep
the accepted stage-10 list, then override:

  r_pathTracingCleanRtxdiDiGlassReflectionPsr 0
  r_pathTracingReflectionOpaqueMirror 0
  r_pathTracingCleanRtxdiDiGlassRefractedPsr 0

This reduced stage 10 must retain:

  - producer raygen execution;
  - primary glass surface and material decode;
  - straight-through behind-glass `TraceRay`;
  - transmission any-hit and closest-hit groups.

It must remove mirror tracing, reflection secondary shade/shadow work, opaque
mirror tracing, and refracted direction calculation. A surviving run assigns
the crash to one of those removed reflection paths. A device removal assigns
it to the remaining core transmission trace/hit-group path and authorizes one
compiled split: raygen prologue without `TraceRay`, followed by exactly one
straight-through trace.

REF-10AB CORE-TRACE REJECTION AND FOUR-RUNG HIT PROBE - 2026-07-28
------------------------------------------------------------------
The reduced producer survived level entry and device-removed only when the
camera looked at a window. With both reflection systems and refraction off,
the first glass pixel still executes one straight-through transmission
`TraceRay`. That is now the exact failing boundary.

Compile one reusable isolation ladder rather than one shader per test.
Reserve clean-DI flags 28-29 for a transmission trace probe mode used only by
the GEO-10 producer isolation:

  mode 1 / stage 10
    Run producer raygen, primary glass-surface decode, material parameter
    decode, and direction setup. Return immediately before `TraceRay`.

  mode 2 / stage 11
    Call `TraceRay` with `RAY_FLAG_FORCE_OPAQUE |
    RAY_FLAG_SKIP_CLOSEST_HIT_SHADER`. This traverses the current bucket TLAS
    but invokes neither any-hit nor closest-hit.

  mode 3 / stage 12
    Call `TraceRay` with `RAY_FLAG_FORCE_NON_OPAQUE |
    RAY_FLAG_SKIP_CLOSEST_HIT_SHADER`. This invokes transmission any-hit but
    suppresses closest-hit.

  mode 0 / stage 13
    Use the production `RAY_FLAG_FORCE_NON_OPAQUE` path with transmission
    any-hit and closest-hit.

For every stage 10-13 producer dispatch, the CPU must clear:

  CLEAN_RTXDI_DI_FLAG_GLASS_REFLECTION_PSR
  CLEAN_RTXDI_DI_FLAG_OPAQUE_MIRROR_REFLECTION
  CLEAN_RTXDI_DI_FLAG_GLASS_REFRACTED_PSR

Required checkpoint names:

  GEO10.View16.Stage10 TransmissionRaygenNoTrace
  GEO10.View16.Stage11 TransmissionTraversalNoHitShaders
  GEO10.View16.Stage12 TransmissionAnyHitOnly
  GEO10.View16.Stage13 TransmissionFullHitPath

Test one stage at a time and stop on the first device removal. Black or
unresolved glass is expected through stage 12. Only crash survival and the
stage-specific completion line are acceptance evidence.

Build and deployment proof:

  CPU planning harness                                passed
  CPU planning stress iterations                      20
  full release build                                  passed
  transmission producer spirv-val                     passed
  transport source mtime                              2026-07-28 21:52:41
  transmission blob mtime                             2026-07-28 21:59:59
  source/deployed executable hash                     equal
  source/deployed transmission blob hash              equal

  executable
    3F06B4E741AFE7291B5315E8DE473EC6873F6AF2365C9AF52128C4720EB1508F

  transmission producer
    70D604E04CEFB43EBAD9B04BC93E1D259D6412114A735B791DBCDBF867EB2455

The build also recompiled sentinel, spatial debug/production, and temporal
debug/production because the shared include timestamp was touched during the
edit. All five hashes remained identical to the deployed versions, so they
were not copied. Only the changed transmission producer blob was deployed.

REF-10AC ROUTE-1 PRODUCTION ACCEPTANCE - 2026-07-29
----------------------------------------------------
`success20.log` proved the requested route-2/stage-0 run was still
fail-closed: it remained monolithic and device-removed at
`clean-rtxdi-di-transmission-producer`. It was not a bucket-resident crash.

The first route-1 production admission is deliberately limited to clean-DI
view 16, stage 0, diagnostic checkpoints enabled, GI/RR/external PDF-NEE
disabled, transmission producer and composition enabled, and reflection,
opaque mirror, and refracted PSR disabled. Route 1 selects the bounded
forced-opaque transmission resolver during the monolithic startup wait and
after bucket cutover. Only route 2 retains the diagnostic full-resident active
mask; route 1 uses the portal-active mask.

`success21.log` accepts that checkpoint:

  production route supported                         yes
  startup traversal                                  bounded monolithic wait
  resident BLAS readiness                            advances to 56/56
  accepted traversal                                 bucket-resident-pool
  active TLAS buckets during traversal               2, 3, and 5 observed
  resident route records                             56
  active TLAS outputs                                exact active count
  missing active material indexes                    0
  device-loss checkpoints                            none
  shutdown                                           normal

The likely old trigger was a runtime-tinted emissive door panel. Door-light
materials contain conditional additive stages and `colored` RGB registers,
which are represented by runtime material variants and ordered stage records.
The old light-candidate count did not change before failure, so the trigger
does not implicate emissive-light publication. It implicates the rejected
legacy transmission any-hit decode when that material first became a ray-hit
candidate. The accepted bounded resolver performs the same material and
emissive-card decisions outside any-hit after a forced-opaque closest hit.

Do not disable colored emissives. Do not restore the legacy transmission
any-hit producer for route 1.

REF-10AD FRONTEND-VISIBLE STATIC MASK - 2026-07-29
---------------------------------------------------
The `success21` route is stable but visually rejected. The user observed only
one or two portal areas of brush geometry, while rigid and skinned objects
loaded and retired appropriately. The log proves this is selection loss, not
BLAS or material failure: all 56 bucket BLASes were ready, active publications
were exact, missing material indexes stayed zero, but the static mask used
`portalSteps=1` and selected only two to five buckets.

Do not raise the fixed camera-area radius. REF-8 already rejected that shape:
raster visibility follows the clipped recursive portal flood and may include
areas farther away than a fixed graph depth.

The corrected route captures the frontend portal-flood result into each
`viewDef_t` after `FlowViewThroughPortals`. The backend static mask contains
every frontend-visible area, then expands one portal neighbor from that
complete seed set using the existing default-one control. The snapshot is
view-owned so subviews cannot overwrite it through the global portal
`viewCount` markers. Missing or empty snapshots fail closed to monolithic
traversal. Route 2 keeps its full-resident decoder override.

Required runtime evidence:

  - `areas(frontendVisible/selected)` is nonzero;
  - selected areas are at least the frontend-visible count;
  - distant brush geometry visible through chained portals is present;
  - active TLAS outputs still exactly match active buckets;
  - `materialIndexMissingActive=0`;
  - portal traversal and the tinted door-panel view remain device-loss free.

REF-10AE FRONTEND-VISIBLE MASK ACCEPTED - 2026-07-29
----------------------------------------------------
The post-fix run accepts REF-10AD's required evidence. The user reports that
the scene now looks correct in game, including the brush geometry that had
previously disappeared beyond the first one or two portal areas.

The log records:

  frontend-visible/selected areas at startup          2/4
  resident/ready bucket BLASes at cutover             56/56
  accepted active bucket counts while moving          4, 8, 5, 7, 3, 6
  active TLAS outputs                                 exact active count
  resident route records                              56
  missing active material indexes                     0
  device-loss checkpoints                             none
  shutdown                                            normal

This is direct proof that route 1 followed the frontend clipped portal flood
and changed its active set while retaining visually required brush geometry.
The frontend snapshot plus default-one neighbor expansion is now the accepted
production static-area policy.

Two later diagnostics after `noclip OFF` reported an empty bucket publication
and correctly fell back to monolithic traversal. No visual issue or device
loss accompanied those transitions. Their exact view/area cause is not proven
by this log; keep the fail-closed path and reopen only if the route drops
during ordinary in-area traversal.

REF-10AF BOUNDED GLASS-REFLECTION GATE - 2026-07-29
---------------------------------------------------
The accepted route-1 baseline disabled all reflection and refraction systems.
The next smallest feature-completeness slice restores only glass reflection:

  - the glass mirror ray uses the route-1 bounded forced-opaque iterative
    resolver instead of the rejected legacy transmission any-hit decoder;
  - each hardware hit is canonicalized by closest-hit;
  - pane/card continuation and material classification occur once in raygen;
  - secondary reflection visibility rays remain disabled;
  - opaque mirrors and refracted PSR remain unsupported;
  - GI, RR, and external PDF-NEE remain unsupported.

The production support predicate accepts reflection PSR only while
`r_pathTracingReflectionSecondaryShadows=0`. It still accepts the prior
reflection-off baseline regardless of the unused shadow control. CPU contract
tests cover both accepted states and reject reflection plus secondary shadows.

Offline proof:

  PathTraceCpuPlanningHarness                        passed
  PathTraceCpuPlanningHarness --stress 20            passed
  full win64-pt-dev-release build                    passed
  transmission producer spirv-val                    passed
  source mtime                                       2026-07-29 14:15:30
  transmission blob mtime                            2026-07-29 14:17:14
  blob newer than source                             yes

  executable SHA-256
    014373127864BF845D27849E734BB1F1A195318E6F7788972D682812967DD832

  transmission producer SHA-256
    6D2E29B4434EF1AAEF5C82FBA3C72622AF1B11071CBBA8608924C67208B70FA3

The glass material-feature library was rebuilt because the dependency changed,
but its hash remained equal to the deployed artifact and must not be copied.
Only the changed transmission producer SPIR-V and executable are deployed.

Runtime gate:

  1. Keep the accepted route-1/view-16 configuration.
  2. Set glass reflection PSR to 1 and reflection secondary shadows to 0.
  3. Keep opaque mirror reflection and glass refracted PSR at 0.
  4. Load the positioned save, look at and move around the windows, cross
     portals, and include the tinted emissive door panel in frame.
  5. Reject device removal, camera-movement-only failure, swapped materials,
     broken brush geometry behind glass, or a route-1 cutover failure.
  6. Record whether glass reflection is visibly present; black/unreflected
     windows are a visual rejection even if the run is crash-stable.

REF-10AG GLASS ACCEPTANCE AND OPAQUE-MIRROR GATE - 2026-07-29
--------------------------------------------------------------
The user reports that glass reflections work. Minor tinted-door-panel and
image-emissive artifacts were confirmed to predate reflection enablement, so
they do not reject REF-10AF and remain outside GEO-10's reflection gate.

The next isolated widening enables opaque exact mirrors. This is a caller-only
admission: opaque mirrors use the same bounded mirror trace service accepted
for glass. Secondary reflection shadows stay off, so no new visibility
`TraceRay` enters the route. Refracted PSR and later consumers stay disabled.

CPU support-predicate tests accept:

  - straight transmission with reflection disabled;
  - glass reflection with secondary shadows disabled;
  - glass plus opaque-mirror reflection with secondary shadows disabled.

They reject the reflection family when secondary shadows are enabled and keep
the stage, GI, RR, PDF-NEE, and refraction gates unchanged.

Offline proof:

  PathTraceCpuPlanningHarness                        passed
  PathTraceCpuPlanningHarness --stress 20            passed
  full win64-pt-dev-release build                    passed
  Vulkan shader content change                       none
  rebuilt glass/transmission hashes vs deployed      equal/equal

  executable SHA-256
    4705A83FD23AC8A09DB831F24EA506FD0104D64E29AC487991616F3BAA69D82B

Only the executable is deployed for this gate.

Runtime gate:

  1. Keep the accepted REF-10AF configuration.
  2. Change only `r_pathTracingReflectionOpaqueMirror` from 0 to 1.
  3. Keep `r_pathTracingReflectionSecondaryShadows=0` and
     `r_pathTracingCleanRtxdiDiGlassRefractedPsr=0`.
  4. Exercise windows, reflective opaque surfaces, camera motion, portals, and
     the tinted door-panel view.
  5. Reject device removal, swapped materials, incorrect reflected geometry,
     broken geometry behind glass, or loss of the route-1 cutover.

REF-10AH REFLECTION PORTAL HALO - 2026-07-29
---------------------------------------------
The opaque-mirror run exposed one geometry-coverage defect: reflected rays
lost brush geometry after approximately one or two portal crossings. The
existing `r_pathTracingGeometryStaticBucketPortalSteps 1` membership was
correct for the frontend primary view but insufficient for off-primary
reflection rays.

The live A/B is accepted:

  primary/frontend expansion                         1
  manual test expansion                              4
  reflected brush cutoff at 1                        present
  reflected brush cutoff at 4                        not observed

A dedicated reflection-only control now defaults to four:

  r_pathTracingGeometryStaticBucketReflectionPortalSteps 4

Effective portal steps are `max(primary, reflection)` only while the accepted
route-1 glass or opaque-mirror reflection family is active. Primary-only route
1 remains at one. The full-resident route-2 diagnostic is unchanged. Both
values clamp to 0..8, and diagnostics report:

  portalSteps=<effective value> reflectionHalo=<0 or 1>

Offline proof:

  PathTraceCpuPlanningHarness                        passed
  PathTraceCpuPlanningHarness --stress 20            passed
  primary 1, reflection inactive                     resolves to 1
  primary 1, reflection 4 active                     resolves to 4
  primary 6, reflection 4 active                     resolves to 6
  invalid/out-of-range controls                      clamp to 0..8
  full win64-pt-dev-release build                    passed
  SPIR-V rebuilds                                    0

  executable SHA-256
    DDB6BE1922637E109D6023B65773CEDAEDAB9C1D22DF2E64EB3FB363B58B8513

Only the executable is deployed. Runtime acceptance requires the normal
reflection-enabled route-1 configuration with the primary control returned to
one. The active-set diagnostic must show `portalSteps=4 reflectionHalo=1`,
reflected brush geometry must remain present, and portal movement must remain
device-loss free.
