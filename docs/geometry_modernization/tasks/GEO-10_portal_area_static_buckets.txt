GEO-10 - Replace monolithic static traversal with portal-area buckets
====================================================================

GOAL
----
Build resident static BLAS buckets by portal area and bounded size, then place
only active buckets in the TLAS while preserving full static shading identity.

PRECONDITIONS
-------------
- Phase C gate passes.
- GEO-02 comm1 invariant is retained as a mandatory regression.
- New static route is default off and compare-capable.

ARCHITECTURE OVERRIDE - 2026-07-27
----------------------------------
Read `artifacts/GEO-10_architecture_pivot_2026-07-27.txt` before changing GEO-10
code, then read
`artifacts/GEO-10_reference_contract_review_2026-07-27.txt`. The reference
contract review is the newest authority. These artifacts supersede the
parallel t100-t105 route-record decoder, contribution 4/5, compact hit-library,
material hit-stage ladder, and triangle-base InstanceID direction documented
below. The remainder of this ticket is retained as historical evidence unless
the newer artifacts explicitly preserve a contract.

The active direction keeps deterministic bucket assignment, resident BLASes,
active TLAS selection, and rollback. Each independently active bucket remains
one BLAS, but that BLAS contains one GeometryDesc per packed surface.
InstanceID supplies the first resident surface-record index, GeometryIndex
selects the surface, and PrimitiveIndex selects its local triangle. The
resident pool and appended surface records use existing static t3/t4/t5/t9/t11
slots. Every TraceRay geometry-contribution multiplier must first be proven
zero. Resident-pack caching and Vulkan device-loss checkpoints are
preconditions for live traversal, not deferred cleanup.

The 2026-07-27 REF-4 route-off runtime checkpoint passed with no major issue
after every Vulkan TraceRay geometry-contribution multiplier was changed to
zero. REF-5 now builds each shadow bucket BLAS from one strictly validated,
ordered GeometryDesc per surface record. CPU tests reject invalid flags,
non-contiguous/reordered ranges, and cross-bucket record bleed. The full
release build passes without rebuilding shaders. Production TLAS insertion
remains impossible because the bucket consumer-support gate is hard-false;
the next gate is a shadow-only bucket build with route zero.

`bucket5.log` accepts that gate: 56/56 resident BLASes became ready, all 1,789
surface records mapped to exactly 1,789 GeometryDescs, 52 buckets contained
multiple geometries, and every invalid/publication failure counter was zero.
Portal movement changed the active set without rebuilding the resident set;
the user observed no issue.

REF-6 appends the same 1,789 four-word surface records after the unchanged
50,108 triangle-class words in the resident t5 upload. CPU validation rejects
any changed prefix or mismatched tail field. No shader consumer is enabled and
the live t5 binding remains monolithic until the later atomic route cutover.
`bucket6.log` accepts the shadow runtime gate: t5 contains 57,264 words with
the surface tail beginning at word 50,108 and occupying 28,624 bytes. All
56 bucket BLASes are ready/reused, all 1,789 records still map to 1,789
GeometryDescs, and every class-layout, geometry, publication, and device-loss
failure counter is zero.

REF-7 now defines the replacement address contract in one shared helper:
InstanceID supplies a resident surface-record base, GeometryIndex selects one
four-word t5 record, and PrimitiveIndex selects the local triangle. A CPU
mirror proves the exact record/triangle/index/vertex tuple and rejects invalid
namespace, geometry, and primitive inputs. The host publishes the total
resident surface-record count only after the route is accepted, and the old
triangle-base compatibility loader deliberately fails closed. Shader
consumers remain compile-gated off until they are migrated one pipeline family
at a time; this checkpoint is address-foundation proof, not live traversal
acceptance.

`bucket7.log` accepts REF-7's route-off reload baseline. REF-8 now compiles the
decoder only into the primary-surface producer and adds route mode 2 as a
view-2, GI-off, checkpoint-enabled opaque-primary probe. Each primary hit entry
resolves the hardware address once. Alpha/decal/glass/liquid records and static
bucket emissive replay remain excluded; route mode 1 is still fail-closed.

IMPLEMENTATION STATUS
---------------------
Steps 1 through 5 are complete in shadow mode. Static world records now carry the
authoritative `_areaN` owner assigned by AddWorldModelEntities. The CPU-only
assignment planner uses world/area/split/source/storage identity, deterministic
ordering, bounded splitting, explicit fallback ownership, and exact duplicate,
unassigned, invalid-area, invalid-range, oversized, split, and key-collision
counters.

PathTraceCpuPlanningHarness passes the deterministic and failure cases.
geometry_geo187_auto accepts 589/589 surfaces and 22,558/22,558 primitives
across 14 active area buckets with every failure counter zero and a nonzero
world/source/storage generation tuple. geometry_geo189_auto and
geometry_geo190_auto then produce identical fresh-process assignment and
packed-content signatures while copying all 26,926 vertices, 67,674 indexes,
and 22,558 material/class/source-identity triangles into contiguous
bucket-local ranges with all six packing failure counters zero. Production
remains the monolithic static BLAS.

The step-4 source is now a separate cap-free full-map resident universe, not
the selected/pruned production cache. geometry_geo193_auto packs 1,789/1,789
surfaces and 50,108/50,108 triangles into 56 area buckets, uploads 8,846,672
bytes of vertex/index/class/material/identity storage once, and incrementally
builds all 56 bucket BLASes at two per frame. The selected-area mask marks only
3 buckets active and retains 53 inactive. No bucket enters the production TLAS;
the shadow planner blocks its route because shader support is deliberately
still false.

Step 5 derives one publication generation from the source, storage, material,
packed-content, and active-set identities. The active TLAS descriptors and
checked route records are constructed together and carry that same generation.
Publication fails closed until every active bucket BLAS is ready, if counts
differ, or if any generation is zero or mixed. geometry_geo194_auto observes
the expected empty publication while the three active BLASes converge, followed
by an exact atomic 3/3 publication. The live production TLAS remains
monolithic; these descriptors and route records are still shadow-only.

The shader-route ABI foundation is also complete in shadow mode. The audit
found that material IDs alone were insufficient because the primary producer
also consumes a table index per triangle. The packed route now publishes and
binds all six checked SRV streams: route records, vertices, indexes, triangle
classes, material IDs, and resolved material indexes. geometry_geo195_auto
reports zero missing active material indexes, successful GPU route upload, and
zero Vulkan binding errors. Live bucket instances are still excluded.

The first live-cutover foundation is now present without changing traversal.
Static buckets reserve the high 24-bit InstanceID range beginning at
0x00800000, leaving the established static/dynamic/rigid/skinned IDs unchanged.
The primary producer and clean-DI shared libraries receive an explicit route
count plus both 32-bit halves of the publication generation. Their common
loader rejects a wrong namespace, count, 48-byte record stride, generation,
packed range, 112-byte vertex stride, index topology, or material/class range.
The acceleration submit plan now separates retaining/building the monolithic
static BLAS from including instance 0 in the TLAS, so rollback ownership can
survive the eventual bucket-only submission.

geometry_geo197_auto accepts creation of the enlarged clean-DI constant buffer,
all six clean-DI route bindings, and the affected Vulkan pipelines. It settles
to the same exact 3/3 publication, builds all 56 resident BLASes, reports zero
binding/device/validation errors, and shuts down normally. Traversal remains
monolithic because actual primary/any-hit/spatial/GI hit reconstruction and
static-emissive identity have not moved. geometry_geo196_auto is rejected as an
early-terminated first-load diagnostic, not a runtime failure.

The primary-surface producer is now the first complete checked hit consumer.
Its triangle validation, material ID/class/table-index lookup, alpha/decal
texture and vertex-color interpolation, semantic liquid-card evidence,
any-hit support predicate, and closest-hit vertex reconstruction all recognize
the reserved bucket namespace. Every bucket access goes through the common
generation/range/topology loader before reading packed storage. Monolithic
static, dynamic, rigid, and skinned fallback behavior is unchanged. This code
is deliberately unreachable until the later all-consumer cutover gate admits
bucket instances to the production TLAS.

Standalone clean-DI spatial now consumes the same checked route tuple and six
packed SRVs already present in its binding set. Its receiver triangle
reconstruction, emissive replay geometry, live material-index recovery, and
shadow any-hit material lookup all fail closed through the common loader.
Both debug and production view-16 Vulkan variants build and deploy, while the
live TLAS remains monolithic.

Remix GI now mirrors the complete 512-byte clean-DI constants prefix instead
of placing its private tail over the final 32 bytes. Its independent layout
binds the six bucket streams at t100 through t105. Central hit-range,
material-ID/index, class, full-surface, compact-geometry, ray-query, any-hit,
and emissive replay consumers use one non-inlined checked route loader. Vulkan
uses only the split GI libraries; the two unused monolithic SPIR-V build
outputs were removed after the enlarged source exceeded their compiler ID
limit. All 32 runtime split libraries and both compute wrappers build and
deploy. Bucket traversal remains off.

The first attempt to import the checked decoder directly into every clean-DI
consumer compiled but caused repeatable Vulkan device removal even with the
route disabled. That integration is quarantined. Vulkan pipelines now append
separate compact bucket hit-only modules at SBT contributions 4/5 instead:
one for the core clean-DI payload, one for glass/transmission's extended
payload, and one for the clean-GI payload. Primary and spatial use the same
six-record contract. Default launch/save-load tests passed independently for
core DI, primary/spatial, material features, and GI with no device removal or
visual regression. The checked decoder is therefore available in every live
Vulkan ray-pipeline family without expanding the large raygen libraries.

Primary history and motion now classify a checked bucket InstanceID as static
world geometry. Bucket vertices are immutable world-space data, so their
previous position is the current hit position and the existing previous-camera
projection produces motion pixels and depth. This bypasses monolithic static
snapshot indexing without entering rigid/skinned object-motion arithmetic.
The legacy smoke library compiles the old contract byte-for-byte; only the
primary producer enables the bucket-motion predicate.

Current-frame bucket preparation now runs once after material-table completion
and before emissive construction, acceleration submission, binding creation,
and scene-input commit. One frame result owns the pack, resolved material
indexes, BLAS state, active publication, route upload result, and generation.
The later GEO-10 diagnostics read that result instead of rebuilding or patching
the already committed `m_sceneInputs`. The fresh local scene-input package now
receives the route count, generation, and validity before resource commit.
This closes the stale publication-order blocker while traversal remains
shadow-only.

The emissive-identity audit then rejected dense active-subset InstanceIDs:
adding or removing an earlier portal-area bucket could renumber a still-visible
bucket. Publication now assigns InstanceID from the stable resident-bucket
ordinal. The TLAS still contains only active buckets, while the checked route
table contains one record for every resident bucket. CPU validation accepts an
active TLAS count independently from the complete resident route count and
proves the route count remains complete when the active subset changes.

A shadow static-emissive builder now reads the exact current-frame packed
bucket ranges and emits only active-bucket records using resident InstanceID
plus bucket-local primitive index. Static/dynamic summaries recognize both
monolithic instance 0 and the reserved bucket namespace as static. The
one-shot GEO-10 audit reports monolithic/bucket counts plus zero-identity and
collision counts. Production emissive publication remains monolithic until the
same default-off gate switches traversal and emissive identity atomically.

`r_pathTracingGeometryStaticBucketRoute 1` is now that default-off gate. A
request never allocates resources or submits BLAS builds implicitly. Explicitly
enable the bucket resource and build gates after cold pipeline warmup; the live
route continues rendering monolithic until every resident bucket is ready.
Cutover additionally requires an exact current-frame publication, complete
resident route table, successful route upload, debug mode 0, clean DI enabled,
and clean-DI view 2 through 25. View 1 explicitly fails the gate because the
all-views sentinel could not compile bucket hit support. Acceptance uses the
same boolean to omit monolithic TLAS instance 0, append active bucket instances,
and replace monolithic static-emissive records with bucket-local records.
Setting the cvar back to 0 restores both monolithic contracts.

The CPU harness accepts the exact fully resident route and rejects unsupported
consumer, partial residency, incomplete route table, and default-off cases.
The Vulkan release build, SPIR-V validation, CPU harness, and unselected
six-record pipeline runtime tests pass. Live bucket traversal proof is still
required before the route can be considered accepted.

The first live test exposed two separate facts. Contribution zero made primary
brushes visible but selected legacy records in the other pipelines, producing
missing behind-glass brushes and portal-dependent behavior. Correcting the TLAS
descriptor to contribution 4 then caused immediate Vulkan device removal.
Production route admission is fail-closed again.

The no-trace probe survived with no visual regression. Its console spam
contained only the expected validation warning that mask-zero instances never
intersect. Contribution-4 TLAS construction is accepted. The next rung is:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteNoTraceProbe 0
  r_pathTracingCleanRtxdiDiView 2
  r_pathTracingGeometryStaticBucketRouteView2Probe 1
  r_pathTracingGeometryStaticBucketRoute 1

This is a live traversal test and may display the view-2 diagnostic output. It
isolates primary plus compact core bucket hits; it does not admit route
traversal in any other clean view.

View-2 probe build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    86B6B66EFC42B9F0DF3FEBBE70B92D0A55AEB5A7D380BC415070F60CF9DAFA0C

The view-2 probe survived with an exact accepted publication and no device
removal. Primary, sentinel, and the compact core bucket hit pair are accepted.
The next rung isolates the clean-DI production pipelines:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteView2Probe 0
  r_pathTracingGeometryStaticBucketRouteView16CoreProbe 1
  r_pathTracingCleanRtxdiDiTransmissionProducer 0
  r_pathTracingCleanRtxdiDiTransmissionCompose 0
  r_pathTracingCleanRtxdiDiTransmissionDebugView 0
  r_pathTracingCleanRtxdiDiGlassShader 0
  r_pathTracingCleanRtxdiDiGlassDebugView 0
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketRoute 1

The route is accepted only while every dependency above is exact. This runs
initial, temporal, and spatial production with compact core bucket hits while
excluding the distinct material-feature and GI bucket-hit families.

View-16 production-core probe build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    81007DDC46F6655DCBAECA4E27EFDDB3BFA291005D15A4F9F5AEB2BE44538F05

The view-16 production-core probe survived portal movement while the active set
changed from three to five buckets. Initial, temporal, spatial production, and
the compact core hit pair are accepted. The next rung adds only normal
material-feature traversal:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteView16CoreProbe 0
  r_pathTracingGeometryStaticBucketRouteView16MaterialProbe 1
  r_pathTracingCleanRtxdiDiTransmissionProducer 1
  r_pathTracingCleanRtxdiDiTransmissionCompose 1
  r_pathTracingCleanRtxdiDiTransmissionDebugView 0
  r_pathTracingCleanRtxdiDiGlassShader 1
  r_pathTracingCleanRtxdiDiGlassDebugView 0
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketRoute 1

This restores transmission PSR and the transmission/glass passes while keeping
the distinct GI bucket-hit family excluded.

View-16 material-feature probe build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    761917DFF5A3DE59CA9C9106857EF182E6A33AE695B5D5C68B856C99ED384DD7

The first material-feature probe caused immediate Vulkan device removal. The
accepted production-core baseline and excluded GI isolate the material-feature
pipeline. Audit found a concrete payload contract mismatch: the compact
material bucket hit library uses a 176-byte trace-hit/liquid-candidate payload,
while the material-feature pipelines declared a 64-byte maximum. Both clean
material-feature pipeline descriptors now declare 176 bytes. Re-run the same
probe after rebuilding; do not widen to GI until it passes.

Material payload fix build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    EFCBDE0B174E99F7D6D7404B8C37FFDCAA741160404B91C399AC0E98D9AC0F80

The full material probe still device-removed after the payload correction.
Static SPIR-V validation, payload-type comparison, descriptor slots 100..105,
and host bindings pass. The material probe cvar is now a mode. Test mode 1
first:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteView16CoreProbe 0
  r_pathTracingGeometryStaticBucketRouteView16MaterialProbe 1
  r_pathTracingCleanRtxdiDiTransmissionProducer 1
  r_pathTracingCleanRtxdiDiTransmissionCompose 0
  r_pathTracingCleanRtxdiDiTransmissionDebugView 0
  r_pathTracingCleanRtxdiDiGlassShader 0
  r_pathTracingCleanRtxdiDiGlassDebugView 0
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingCleanRtxdiDiView 16
  r_pathTracingGeometryStaticBucketRoute 1

Mode 1 isolates the standalone transmission producer dispatch. Do not advance
to mode 2 or 3 until mode 1 survives.

Staged material probe build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  spirv-val material/transmission/glass             passed
  shader source/blob changes                        none
  built/deployed executable SHA-256
    17510A4B6126C9C7222096B35AE139B25B002360EC4F796FCD760228FF71BD86

Mode 1 survived with an exact accepted publication at 3/56/56. The standalone
transmission producer dispatch is accepted. Source inspection then corrected
the earlier interpretation: this late phase does not issue TraceRay, so it did
not exercise the compact material bucket hit records. Mode 2 was the first such
trace and device-removed with glass and GI excluded.

The rebuilt diagnostic divides mode 2 into cumulative hit stages. Stage 0
fails route admission closed. Start with stage 1:

  r_pathTracingGeometryStaticBucketRoute 0
  r_pathTracingGeometryStaticBucketRouteView16MaterialProbe 2
  r_pathTracingGeometryStaticBucketMaterialHitStage 1
  r_pathTracingCleanRtxdiDiTransmissionProducer 1
  r_pathTracingCleanRtxdiDiTransmissionCompose 1
  r_pathTracingCleanRtxdiDiTransmissionDebugView 0
  r_pathTracingCleanRtxdiDiGlassShader 0
  r_pathTracingCleanRtxdiDiGlassDebugView 0
  r_pathTracingCleanRestirGiEnable 0
  r_pathTracingGeometryStaticBucketBlas 1
  r_pathTracingGeometryStaticBucketBlasBuild 1
  wait for resident/ready to reach 56/56
  r_pathTracingGeometryStaticBucketRoute 1

Mode 2 adds the pre-DI transmission PSR dispatch while glass and GI remain
excluded. The hit-stage ladder is:

  1  minimal closest-hit payload, no bucket metadata
  2  checked bucket material ID/index/class metadata
  3  source-pane rejection
  4  transparent-card/glass rejection
  5  alpha-test rejection
  6  emissive-card passthrough
  7  full liquid-pool candidate collection

For each rung, disable the route, change only
`r_pathTracingGeometryStaticBucketMaterialHitStage`, then re-enable the route.
Stop on the first device removal. Do not enable glass or GI during this ladder.

Material hit-stage ladder build verification:

  PathTraceCpuPlanningHarness                       passed
  Vulkan release build                              passed
  spirv-val sentinel/material/transmission/glass    passed
  rebuilt blob mtimes newer than final include      yes
  built/deployed executable SHA-256
    6D2298BA764B0D7CD35FF8709492BE39CA961626CAC67F1138D5963ECE7F5822
  compact material bucket blob SHA-256
    3CC6BD10DB248CA227B43BD03D478C4ACB5A13A89C53EE736DF78D852A5525BA

The current shadow resource path also repacks the full resident static universe
every frame and has high overhead. Correctness proof does not waive step 9:
steady state must cache the resident pack and scale recurring work with portal
active-set/TLAS changes.

`r_pathTracingGeometryStaticBucketAudit 1` is a one-shot shadow audit.
`r_pathTracingGeometryStaticBucketBlas 1` owns the full-map shadow resources,
and `r_pathTracingGeometryStaticBucketBlasBuild 1` submits bounded shadow
builds. All are default off and intentionally independent from the route
request. See artifacts/GEO-10_entry_audit.txt.

FILES TO AUDIT FIRST
--------------------
  neo/renderer/NVRHI/PathTraceSceneUniverse.cpp/.h
  neo/renderer/NVRHI/PathTraceGeometryUniverse.cpp/.h
  neo/renderer/NVRHI/PathTraceInstanceUniverse.cpp/.h
  neo/renderer/NVRHI/PathTraceAccelerationPlan.cpp/.h
  neo/renderer/NVRHI/PathTraceAcceleration.cpp/.h
  neo/renderer/NVRHI/PathTraceSmokeSceneBuild.cpp
  neo/renderer/NVRHI/PathTraceSceneInputs.h
  neo/shaders/builtin/pathtracing/PathTracePrimarySurface.hlsli

STEPS
-----
1. Define a static bucket key: world generation, portal area, size split, and
   source/storage generation. Define deterministic splitting and stable order.
2. Assign each static surface/primitive to exactly one bucket. Emit duplicate,
   unassigned, invalid-area, oversized, and split counters.
3. Build bucket geometry from GEO-06-style pooled full-fidelity ranges. Preserve
   local primitive -> material/triangle/source identity tables.
4. Build/reuse one BLAS per dirty bucket. Resident inactive buckets may keep
   storage and BLAS state, but must not be put in the active TLAS.
5. Derive one active-set generation from portal visibility. Use that same
   generation for TLAS instances and shader route tables; reject mixed epochs.
6. Coalesce tiny buckets deterministically if static TLAS entries exceed the
   declared budget. Never drop a bucket silently.
7. Compare old/new active static triangles, material IDs, sampled hit tuples,
   emissive inventory, and TLAS entries per area.
8. Run comm1 area 41 first. Record active/retained ranges plus the full GEO-01
   generation tuple. Then run STATIC_BUSY, portal transitions, PDA, and map
   changes.
9. Measure dirty bucket builds/reuse, active TLAS entries, upload bytes, scratch,
   retained memory, and timings with identical content.

GUARDRAILS
----------
- Do not use the old compile-time static cap as bucket admission policy.
- Do not include all resident inactive buckets just to make comm1 render.
- Do not delete the monolithic path until GEO-13.

DONE WHEN
---------
- Every static primitive has one bucket and correct shader identity.
- Only active buckets enter TLAS; generations match end-to-end.
- comm1 and all baselines render with zero silent loss.
- Old static route is a one-cvar rollback.

CHECKPOINT
----------
  pt geometry GEO-10: add portal-area static BLAS buckets

REF-8 ACTIVE-SET FINDING - bucket8.log - 2026-07-27
---------------------------------------------------
The first live hardware-address probe did not report an address, binding,
validation, or device-loss failure. Mode 2 repeatedly admitted exact
publications with all 56 resident bucket BLASes ready. The active subset changed
from three to five buckets during portal movement.

The visual result is rejected. Opaque world geometry more than roughly one
portal edge away disappeared in view 2. This matches the current selector:
`BuildPortalAreaActiveMask` is a breadth-first portal graph neighborhood with
`r_pathTracingGeometryStaticBucketPortalSteps=1`. Raster visibility uses the
actual clipped recursive portal flood and can expose areas farther away.
Increasing the fixed depth is not an accepted production repair; prior runtime
evidence found portal step 8 only temporarily recovered the old route.

View 16 was not a bucket-route success. Mode 2 rejected that view and restored
monolithic traversal as designed.

Mode 2 is now explicitly a full-resident decoder probe. After its existing
view-2, marker-enabled, clean-DI, GI-off predicate passes, every resident portal
bucket enters the diagnostic TLAS. Production mode 1 and route-off shadow
planning still use the ordinary portal mask. Logs distinguish the ordinary
`portalMaskValid` source from the final `maskValid` and the
`fullResidentProbe` override.

This diagnostic override does not close step 5 or approve all-resident
production traversal. The production route still requires a conservative
immutable frontend snapshot derived from actual portal visibility, with one
revision shared by TLAS membership and every dependent publication.

Corrected probe verification:

  PathTraceCpuPlanningHarness --stress20             passed
  full win64-pt-dev-release build                    passed
  shader source/blob changes                         none
  built/deployed executable SHA-256
    236C9A5EA4F8331E5BEB2BF1545F11647EE176E515A2E2BD510DB561E74871EF

The repeated REF-8 run must report 56/56/56 active/resident/ready, 56/56
TLAS/routes, `fullResidentProbe=1`, and bucket-resident-pool traversal. Reject
on device removal, missing opaque brushes, opaque material corruption, or any
nonzero address/publication failure.

REF-8 FULL-RESIDENT PUBLICATION REPAIR - bucket9.log - 2026-07-27
----------------------------------------------------------------
The user reported no visual issue, but the log rejects this as decoder evidence.
Every mode-2 view-2 frame remained on the monolithic fail-safe:

  requested / accepted                              1 / 0
  consumer supported / full-resident probe           1 / 1
  active / resident / ready                         56 / 56 / 56
  publication exact                                  0
  TLAS / route outputs                               0 / 0
  traversal                                          monolithic

The full-resident active mask exposed a second diagnostic-contract mismatch.
The frame material table was still hydrated from the ordinary portal-selected
monolithic triangle stream, while publication now required valid material
indexes for every resident bucket triangle. The active publication correctly
failed closed instead of admitting a partially mapped table.

For mode 2 only, material metadata and table membership now include the unique
material IDs from the resident static-bucket triangle stream. The hydration
cache key includes a separate signature for that set, so switching the probe on
cannot reuse the narrower route-off table. Production modes are unchanged.
The cutover diagnostic now prints `materialIndexMissingActive` directly.

Verification:

  PathTraceCpuPlanningHarness --stress20             passed
  full win64-pt-dev-release build                    passed
  shader source/blob changes                         none
  source/deployed executable hashes                  equal
  executable SHA-256
    6FDB534E2290DE9953A110961511BF44ACD1412C223DA11CA00E602C27122686

Repeat the same mode-2 test. Acceptance additionally requires
`materialIndexMissingActive=0`; a visually correct monolithic fallback is not a
pass.

REF-8 OPAQUE PRIMARY DECODER ACCEPTANCE - bucket10.log - 2026-07-27
-------------------------------------------------------------------
The corrected probe passed. View 2 repeatedly admitted live bucket traversal:

  requested / accepted                              1 / 1
  consumer supported / primary probe                 1 / 1
  full-resident probe                                1
  all resident ready / publication exact             1 / 1
  active / resident / ready                         56 / 56 / 56
  TLAS / route outputs                              56 / 56
  missing active material indexes                    0
  traversal                                          bucket-resident-pool

Switching to view 16 rejected the probe and restored monolithic traversal.
Switching back to view 2 re-admitted the same exact 56/56 bucket publication.
This repeated twice without device loss, Vulkan validation/binding errors, or
reported visual corruption.

REF-8 is accepted for opaque primary InstanceID + GeometryIndex +
PrimitiveIndex decoding and full-resident material lookup. This does not accept
the fixed-depth portal active-set policy, alpha/decal/glass/liquid consumers,
secondary/query rays, static emissive replay, or production route mode 1.

REF-9A PRIMARY ALPHA-TEST CHECKPOINT - 2026-07-27
-------------------------------------------------
The first REF-9 slice stays inside the accepted primary producer. Static-bucket
any-hit and shadow any-hit now admit ordinary opaque plus alpha-tested
materials. They reuse the one resolved hardware address and interpolate alpha
UVs directly from its three packed vertex indexes. No route-table re-decode,
descriptor, SBT record, payload field, secondary ray, glass, decal, particle,
liquid, DI/GI, motion, or emissive contract changed.

All other deferred primary classes remain ignored by mode 2. This is an
alpha-cutout rung, not general transparency.

Verification:

  PathTraceCpuPlanningHarness --stress20             passed
  full win64-pt-dev-release build                    passed
  rebuilt Vulkan blobs                                1
  DX12 shader rebuilds                                0
  primary blob newer than edited source             yes
  spirv-val --target-env vulkan1.2                   passed
  source/deployed executable hashes                  equal
  source/deployed primary blob hashes                equal
  executable SHA-256
    6FDB534E2290DE9953A110961511BF44ACD1412C223DA11CA00E602C27122686
  primary blob SHA-256
    7D078719E39B4BADDED618981815EA88CC0E86E272E0DD8C7A211A30FE5E7909

Runtime test:

  1. Keep the REF-8 view-2/GI-off/marker/BLAS settings.
  2. Aim at a static alpha-tested cutout such as a grate or masked panel.
  3. Compare route 0 and route 2. The silhouette and holes must match; route 2
     must not turn the card into a solid polygon.
  4. Traverse the existing scene and confirm opaque geometry remains stable.
  5. Capture the log while route 2 reports exact 56/56 bucket traversal.

Reject on device loss, opaque regression, solid alpha cards, wrong cutout
coverage, or any loss of exact publication.

REF-9A RUNTIME ACCEPTANCE - bucket11.log - 2026-07-27
-----------------------------------------------------
The first capture was monolithic because diagnostic markers were off. After
enabling `r_pathTracingNsightGpuMarkers 1`, the updated log repeatedly admitted:

  requested / accepted                              1 / 1
  consumer supported / primary probe                 1 / 1
  full-resident probe                                1
  all resident ready / publication exact             1 / 1
  active / resident / ready                         56 / 56 / 56
  TLAS / routes                                     56 / 56
  missing active material indexes                    0
  traversal                                          bucket-resident-pool

The user then continued moving through the scene and reported no visual issue.
No device-loss, binding, or validation failure appears in the log. REF-9A is
accepted for primary and shadow alpha-test handling.

REF-9B DETAIL-DECAL OFFLINE CHECKPOINT - 2026-07-27
----------------------------------------------------
The second REF-9 slice admits receiver-owned detail decals in the same primary
producer only. It does not widen glass, particles, liquid cards, secondary
rays, GI, or spatial DI.

The pre-edit audit found that the bucket InstanceID and surface-local
PrimitiveIndex cannot reuse DECAL-03's old sort-key expression. The bucket ID
occupies the high half of the 24-bit instance namespace, and equal local
primitive indexes recur in different GeometryIndex values. Reusing the old
expression would both overflow the instance component and collide across
surfaces.

The four-word surface record now stores the source monolithic triangle offset
in bits 1..31 of its existing flags word; bit 0 remains the valid bit. The
resolved hardware address therefore carries both:

  triangleIndex        packed resident-pool metadata address
  sourceTriangleIndex  original monolithic primitive index

This is an exact preservation of the existing static DECAL-03 key after
portal/size repacking: the decal branch applies DECAL-03's existing
`0x3fffffff` primitive mask to `sourceTriangleIndex`. It is not a new ordering
heuristic. The signed-int source range contract bounds the encoded offset to
31 bits. No fifth record word or new descriptor is required.

Primary any-hit resolves the address once, loads the material flags once, and
for `RT_SMOKE_MATERIAL_DETAIL_DECAL`:

  - interpolates authored UVs from the resolved packed vertex triple;
  - stores the decal with `sourceTriangleIndex`;
  - ignores the decal hit so the receiver remains closest.

The existing decal bin, receiver-distance guard, blend operations, and
route-0 path are unchanged. Non-collect modes still fail closed on a bucket
detail decal.

Offline/deployment proof:

  PathTraceCpuPlanningHarness                      passed
  explicit reordered-source sort-key test          passed
  full win64-pt-dev-release build                  passed
  DX12 shader rebuilds                              0
  intended/deployed Vulkan blobs                    1
  primary blob newer than both edited shaders      yes
  spirv-val --target-env vulkan1.2                 passed
  descriptor-set/binding decorations vs REF-9A      identical (96 / 96)
  InstanceCustomIndex / GeometryIndex / Primitive  present
  TraceRay geometry multiplier                      0
  source/deployed executable hashes                 equal
  source/deployed primary blob hashes               equal
  executable SHA-256
    A7B63FE41627B6B503961EB708ABC6B93B15F087E3C85A940069ECA3F8A2FE39
  primary blob SHA-256
    92E2829CB31989538C78D5F53593518C0010921547B7F98F8C356C81A18C423E

The shared include timestamp caused the build system to recompile disabled
DI/GI compatibility families; none of those outputs were deployed. Only the
primary producer has the bucket consumer macro enabled.

Runtime test:

  1. Before loading/reloading the map, set
     `r_pathTracingDecalComposite 1`.
  2. Keep markers on, clean DI view 2, GI off, bucket BLAS/build on, and route
     mode 2, matching the accepted REF-9A setup.
  3. Compare route 0 and route 2 on one visible wall grime/detail decal. The
     decal color, coverage, receiver, and stability must match.
  4. If stacked detail decals are available, check that their top/bottom
     ordering does not swap between routes or flicker while moving.
  5. Continue ordinary traversal and capture an audit/condump that reports
     exact 56/56 bucket traversal.

Reject on a missing decal, a decal becoming closest geometry, changed layer
order, receiver holes, flicker, opaque/alpha regression, device loss, or an
inexact publication. REF-9B remains pending runtime acceptance.

REF-9B VISUAL-GATE CORRECTION - bucket12.log - 2026-07-27
----------------------------------------------------------
`bucket12.log` mechanically accepts the new hit branch but cannot visually
accept it. In view 2 the route repeatedly reached exact full-resident traversal:

  requested / accepted                              1 / 1
  consumer supported / primary probe                 1 / 1
  active / resident / ready                         56 / 56 / 56
  TLAS / routes                                     56 / 56
  missing active material indexes                    0
  traversal                                          bucket-resident-pool

There is no device-loss, binding, or validation failure. The final audit was
taken after switching to view 16, so it correctly reports monolithic fallback
and is not the accepted sample. The reported black hovering card appeared only
in view 16. It therefore cannot be caused by REF-9B bucket traversal: view 16
had `consumerSupported=0` and never submitted the bucket TLAS.

The original visual instruction was invalid because clean-DI view 2 is
`primary-surface-status`: valid opaque surfaces are intentionally solid green.
It proves hit validity, not decal color or layering.

The mode-2 gate now also permits clean-DI view 19. View 19 reads the RR diffuse
albedo guide written by the bucket-aware primary producer after
`ApplyDetailDecalComposite`. It runs no analytic/material-validation resolve
and uses the already accepted sentinel output pipeline. View 16 remains
rejected. This supplies a direct post-composite decal image without admitting
bucket instances to an unmigrated ray family.

Verification/deployment:

  PathTraceCpuPlanningHarness --stress 20           passed
  full win64-pt-dev-release build                    passed
  Vulkan shader rebuilds                             0
  DX12 shader rebuilds                               0
  source/deployed executable hashes                  equal
  executable SHA-256
    D4039161E807CC8257E1A130D21A7A11BFF238B82B8D01ECDAB7EDBD8A2578E8

Corrected visual test:

  1. Set `r_pathTracingDecalComposite 1` before loading/reloading.
  2. Keep markers on, GI off, and bucket BLAS/build on.
  3. Set `r_pathTracingCleanRtxdiDiView 19`.
  4. Compare route 0 and route 2 on the same visible detail decals.
  5. In route 2, issue the bucket audit and condump.

View 19 is an unlit albedo guide, not beauty lighting; glass/windows may look
unrepresentative. Acceptance concerns decal color/coverage/order and opaque
receiver continuity. REF-9B remains visually pending.

REF-9B OPPOSITE-FACE REJECTION - 2026-07-27
-------------------------------------------
The corrected view-19 test rejected the first detail-decal implementation.
Some decals matched, while others appeared as a second hovering/stacked layer;
one wrong layer was viewed backwards. This is not an ordering-key symptom:
the source-triangle key controls layer order but cannot flip authored UVs.

The concrete mismatch is in receiver ownership. Mode 2 deliberately places the
full resident static universe in the diagnostic TLAS. A primary ray can
therefore visit the back of a detail-decal card on the opposite side of nearby
thin geometry. The existing receiver guard uses absolute projected hit
separation. At grazing angles that card can remain inside the distance
envelope and composite onto the committed receiver, producing exactly a
hovering duplicate or backwards texture.

Static capture already defines the ownership direction. It computes the card
face normal from the triangle, orients it with vertex 0's authored normal, and
lifts the card along that outward direction. Bucket any-hit now repeats that
orientation test on the resolved packed vertex triple and stores the decal only
when its authored outward face points against the primary ray. Degenerate
triangles fail closed. The card is still ignored as geometry in either case.

This correction:

  - changes only the mode-2 static-bucket detail-decal consumer;
  - adds no payload field, descriptor, SBT record, route record, or buffer;
  - preserves the exact source-triangle sort key and UV interpolation;
  - leaves route 0 and every non-bucket ray family unchanged.

Offline/deployment proof:

  PathTraceCpuPlanningHarness --stress 20           passed
  full win64-pt-dev-release build                    passed
  rebuilt Vulkan blobs                                1
  DX12 shader rebuilds                                0
  primary blob newer than edited source             yes
  spirv-val --target-env vulkan1.2                   passed
  descriptor-set/binding decorations old/new       96 / 96
  source/deployed executable hashes                  equal
  source/deployed primary blob hashes                equal
  executable SHA-256
    D4039161E807CC8257E1A130D21A7A11BFF238B82B8D01ECDAB7EDBD8A2578E8
  primary blob SHA-256
    E3BE23D895837981A5DA77C117DCF7B8CE6DBA6C5E5206FEB887306261765BDF

Runtime re-test:

  1. Keep the same view-19, GI-off, markers-on, full-resident mode-2 setup.
  2. Revisit the decals that showed hovering duplicates or backwards layers.
  3. Compare route 0 and route 2 on the same camera positions.
  4. Confirm legitimate front-facing decals remain visible and stable.
  5. Capture an audit/condump proving exact 56/56 bucket traversal.

Reject if the wrong layer remains, a valid decal disappears, route-0/route-2
color or ordering differs, publication falls back, or the device is lost.

REF-9B RUNTIME DISPOSITION - 2026-07-27
---------------------------------------
The opposite-face correction removed the hovering/stacked artifacts in route
2. One reversed decal remains, but the same card is reversed in route 0. Route
0 also retains the duplicate artifact that route 2 now rejects. Therefore:

  - the reversed card is baseline authored/decal behavior, not bucket decode;
  - the duplicate is a baseline receiver-ownership defect, not a route-2
    regression;
  - the bucket detail-decal address, UV, source ordering, and new outward-face
    ownership behavior pass the visual comparison.

REF-9B is visually accepted. Exact publication was already proven by the same
view-19-capable full-resident mode-2 gate; retain an audit in the next combined
runtime capture rather than requiring a separate launch for this disposition.

REF-9C LIQUID-CARD IDENTITY CHECKPOINT - 2026-07-27
---------------------------------------------------
The next primary-only material slice is semantic liquid-card collection. The
audit found a concrete identity mismatch before enabling it: the existing
liquid payload stores InstanceID and PrimitiveIndex but no GeometryIndex, while
a bucket hit's PrimitiveIndex is only surface-local. Reusing the hardware tuple
after any-hit would either collide across geometries or enter the deliberately
fail-closed legacy decoder.

No payload widening is required. A collected bucket liquid card is
canonicalized as:

  payload instanceId      bucket namespace | exact surfaceRecordIndex
  payload primitiveIndex  original monolithic sourceTriangleIndex

At raygen evidence reconstruction, the canonical surface record supplies its
source-triangle offset. Subtraction recovers the surface-local primitive, and
the accepted GeometryIndex-zero address decoder reconstructs the same packed
triangle and vertex triple. This keeps a stable source primitive in the
five-word liquid contributor key while retaining exact packed geometry access.

Bucket any-hit now mirrors the existing semantic-liquid path: it evaluates
stage color and coverage, stores a positive-coverage candidate, ignores the
card as geometry, and preserves fail-closed status on missing stage state.
The REF-9B outward-face guard also applies, preventing opposite-side liquid
cards from becoming receiver candidates. Ordinary detail decals continue
through the already accepted branch.

The CPU harness includes a multi-geometry case proving that canonical surface
InstanceID plus source triangle recovers the exact surface record, packed
triangle, index offset, and vertex triple. Glass, generic transmission,
particles, DI/GI secondary hits, and emissive replay remain closed.

Offline/deployment proof:

  PathTraceCpuPlanningHarness --stress 20           passed
  canonical liquid address test                     passed
  full win64-pt-dev-release build                    passed
  rebuilt Vulkan blobs                                1
  DX12 shader rebuilds                                0
  primary blob newer than edited source             yes
  spirv-val --target-env vulkan1.2                   passed
  descriptor-set/binding decorations old/new       96 / 96
  source/deployed executable hashes                  equal
  source/deployed primary blob hashes                equal
  executable SHA-256
    D4039161E807CC8257E1A130D21A7A11BFF238B82B8D01ECDAB7EDBD8A2578E8
  primary blob SHA-256
    F0D7B2326A15AC33461F7D7FC9F24CB30DA5B98E948F995FA514232843F4A1DF

Runtime test:

  1. Retain the accepted view-19, markers-on, GI-off, mode-2 bucket setup.
  2. Leave `r_pathTracingLiquidPoolMode 3`, its current production default.
  3. Compare route 0 and route 2 on a known wet/liquid decal card.
  4. The receiver texture must remain visible under one stable film; reject a
     missing card, black/floating duplicate, reversed contribution, or
     route-dependent coverage/tint.
  5. Traverse and audit once so the combined capture retains exact 56/56
     publication and no device/validation failure.

REF-9C RUNTIME ACCEPTANCE - 2026-07-27
--------------------------------------
The user reported no issue with the route-2 liquid-card comparison. REF-9C is
accepted for primary semantic-liquid collection and receiver resolve. No
missing, black, floating, reversed, portal-dependent, or stability regression
was observed.

REF-9D PRIMARY STATIC MOTION/HISTORY PROBE - 2026-07-27
-------------------------------------------------------
The next primary-only slice requires no new hit decoder. The producer already
classifies a checked bucket instance as immutable static world geometry in
`pathtrace_smoke_rab_motion_supplier.hlsli`. For such a hit, previous world
position equals current world position; the existing previous-camera
projection produces motion pixels, depth delta, and reset policy. It does not
index the monolithic previous-static snapshot with a surface-local primitive.

Clean-DI view 17 is the existing direct motion diagnostic. It is rendered by
the sentinel library and reads `PathTraceMotionVectors`,
`PathTraceRRMotionVectors`, and `PathTraceMotionVectorMask` after the
bucket-aware primary producer writes them. It does not enter initial, temporal,
spatial, material-feature, glass, or GI ray traversal.

Mode 2 now admits view 17 under the same markers-on and GI-off requirements as
views 2 and 19. This is a CPU cutover-predicate and help-text change only; no
shader, descriptor, SBT, payload, or pipeline binary changes.

Offline/deployment proof:

  full win64-pt-dev-release build                    passed
  explicit PathTraceCpuPlanningHarness target       passed
  PathTraceCpuPlanningHarness --stress 20           passed
  canonical liquid address test                     passed
  view-17 support and rejection matrix              passed
  Vulkan shader rebuilds                             0
  DX12 shader rebuilds                               0
  source/deployed executable hashes                  equal
  executable SHA-256
    F1ED59CEFF624B4CD4F3CA49883D6783459F1866A900BDEA0F9150CF9E26BF00
  retained primary blob SHA-256
    F0D7B2326A15AC33461F7D7FC9F24CB30DA5B98E948F995FA514232843F4A1DF

Runtime test:

  1. Retain markers on, GI off, bucket BLAS/build on, and exact resident-ready
     publication.
  2. Set `r_pathTracingCleanRtxdiDiView 17`.
  3. Compare route 0 and route 2 while standing still, rotating the camera,
     walking, and crossing portals.
  4. The static world must retain the same valid motion field and source tint;
     reject magenta/invalid bands, portal-dependent discontinuities, or
     persistent reset colors unique to route 2.
  5. Return to view 19 before later material checks.
