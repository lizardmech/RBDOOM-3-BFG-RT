GEO-11 ENTRY AUDIT: AS BUDGET, LIFETIME, AND COMPACTION CONTRACTS
=================================================================

DATE
----
2026-07-29

STATUS
------
Audit complete. Implementation is authorized only in the ordered slices below.
This document validates the current Vulkan/NVRHI contract; the GEO-11 proposal
is not treated as proof.

SCOPE
-----
GEO-11 owns acceleration-structure work admission, result lifetime, retirement,
and backend-supported compaction. It must not change geometry identity, bucket
assignment, shader decoding, material lookup, SBT routing, or shading.

GEO-10 ENTRY EVIDENCE
---------------------
`geometry_geo_step9c.log` closes the GEO-10 prerequisite:

  resident / ready bucket BLASes               56 / 56
  warm build / reuse / retire                   0 / 56 / 0
  active cutovers observed                      14, 13, 12, 11, 16
  missing active BLAS/material indices          0
  warm upload bytes                             0
  recurring publication                         297 microseconds
  audit-only validation                       1,070 microseconds
  device removal / binding failure              none

GEO-11 must preserve this accepted geometry and routing behavior.

VALIDATED SUBMISSION CONTRACT
-----------------------------
The path-tracing scene build, AS commands, and ray dispatch are recorded on the
main graphics command list. `GL_EndFrame` closes and submits that command list,
then calls `PathTracePrimaryPass::OnGraphicsCommandListSubmitted`.

The Vulkan backend retains resources explicitly referenced by a command buffer
until the graphics timeline reports completion. AS scratch allocations and
CPU-authored TLAS instance uploads are suballocated with the command-buffer
recording/version ID. Their backing chunks are submitted with the command list
and cannot be recycled as if they belonged to a later recording.

Therefore:

  - NVRHI already owns in-flight scratch-chunk lifetime.
  - NVRHI already owns in-flight CPU TLAS-instance-upload lifetime.
  - Graphics-queue ordering prevents a later TLAS rebuild command from
    executing before prior commands on the same queue.
  - The application still owns the lifetime of every BLAS result handle whose
    device address was copied into a TLAS.

The last point matters because Vulkan TLAS construction reads a BLAS device
address from each `InstanceDesc`; the TLAS command buffer tracks the TLAS
resource but does not add every nested BLAS handle to its retained-resource
list. A CPU handle release can therefore destroy storage still named by an
older in-flight TLAS unless the application retires it against GPU completion.

CURRENT LIFETIME COVERAGE
-------------------------
Owner / resource                         Current authority          Result
--------------------------------------   -------------------------  ----------
Main scene buffers + static/dynamic AS   exact graphics event query covered
Main scene TLAS/bindings/textures        exact graphics event query covered
Skinned comparison BLAS + source data    exact graphics event query covered
Vulkan AS scratch chunks                 NVRHI recording/timeline   covered
Vulkan TLAS instance upload chunks       NVRHI recording/timeline   covered
GEO-10 bucket BLAS results               immediate handle release  GAP
GEO-10 bucket input/metadata buffers     immediate handle release  GAP
Canonical rigid BLAS results             three CPU frames          provisional
Older rigid-residency BLAS results       three CPU frames          provisional

The main scene retirement path creates an event query when a package is
retired, arms it immediately after the exact graphics submission that follows
the retirement, and releases only after both the minimum frame and query
completion. A query allocation failure intentionally retains the package until
reset. That is the accepted model for GEO-11.

CONCRETE STATIC-BUCKET DEFECT
-----------------------------
The live GEO-10 bucket route has three premature-release paths:

  1. A changed range, input signature, or resident buffer sets
     `record->blas = nullptr` immediately.
  2. An unseen bucket record is erased immediately.
  3. Disabling/clearing the bucket scaffold nulls all bucket buffers and clears
     all BLAS records immediately.

Map/reset teardown waits for device idle before clearing and is safe. Live CVar
disable, route transition, buffer growth, source revision, and record
replacement do not have that proof. These resources must move into an
event-query-backed retired package owned by `PathTracePrimaryPass`.

The safe ordering is:

  old handle selected for retirement
    -> retain old BLAS and every replaced backing buffer in a CPU package
    -> submit the current graphics command list
    -> arm a graphics event query after that submission
    -> poll completion on later frames
    -> release retained handles only after completion

Arming after the following submission is deliberately conservative: it covers
all older queue work and the current transition command list.

RESULT AND SCRATCH BUDGET CAPABILITY
------------------------------------
`IDevice::getAccelStructMemoryRequirements` exposes allocated AS result-buffer
size and alignment. GEO-11 can use it for result-byte census and admission
without changing NVRHI.

Exact build/update scratch sizes are calculated inside the Vulkan
`buildBottomLevelAccelStruct` and `buildTopLevelAccelStruct` calls and are not
exposed through the current NVRHI interface. The application must not invent a
scratch estimate and label it authoritative. Initial admission can therefore
enforce:

  - operation count;
  - allocated result bytes;
  - deferred work count, reason, and age;
  - active-versus-background priority.

Exact scratch high-water/admission requires a later narrow NVRHI Vulkan
telemetry/query extension. Until that extension exists, scratch remains
backend-owned and completion-safe but not application-budgeted.

COMPACTION CAPABILITY DECISION
------------------------------
The public NVRHI API contains `AllowCompaction`,
`compactBottomLevelAccelStructs`, `isCompacted`, and result memory queries.
That API shape is not proof that this build can compact.

The active cache has:

  NVRHI_WITH_RTXMU = OFF

In the Vulkan backend the compacted-size handling, copy scheduling, and handle
replacement inside `compactBottomLevelAccelStructs` are compiled only under
`NVRHI_WITH_RTXMU`. With RTXMU disabled the method is a no-op. Adding
`AllowCompaction` alone would request a compactable build but would never
complete the required query/copy/publish sequence.

GEO-11 therefore explicitly rejects compaction in the current build. Enabling
RTXMU is a separate backend/build-system decision and is not part of the first
GEO-11 implementation. No fake frame-delayed handle swap is permitted.

ORDERED IMPLEMENTATION SLICES
-----------------------------
Slice 1 - measured census, no behavior change

  - Report bucket BLAS result bytes and alignment from NVRHI.
  - Report live/ready/deferred/retired-package counts and retained bytes.
  - Keep the existing count build limit and accepted route unchanged.
  - Prove a warm capture reports zero builds and stable result bytes.

Slice 2 - exact bucket retirement

  - Move replaced/removed bucket BLASes and buffers into a pending package.
  - Drain packages into `PathTracePrimaryPass`.
  - Reuse the exact graphics-event-query arm/poll policy.
  - On query creation failure retain until idle reset.
  - Add counters for pending, armed, released, and no-query packages.
  - Test live gate off/on, forced rebuild, map restart, and portal traversal.

Implemented checkpoint:

  - Buffer growth/replacement retains the old buffer handle.
  - Material-index buffer growth retains the old buffer handle.
  - Changed and unseen bucket records retain the old BLAS handle.
  - Live scaffold disable retains all five bucket buffers and every BLAS.
  - The geometry universe transfers retained handles to
    `PathTracePrimaryPass`.
  - The primary pass creates a graphics event query, arms it after the next
    graphics submission, and releases only after the query polls complete.
  - A missing query retains the package rather than guessing completion.
  - Package diagnostics report retained/released buffer bytes and BLAS result
    bytes.

The bounded live test is:

  1. Start from the accepted route-1 configuration and wait for 56/56 ready.
  2. `r_pathTracingGeometryStaticBucketAudit 1`
  3. `r_pathTracingGeometryStaticBucketBlas 0`
  4. Wait for the GEO11 armed and released lines while moving across portals.
  5. `r_pathTracingGeometryStaticBucketBlas 1; r_pathTracingGeometryStaticBucketBlasBuild 1`
  6. Wait for 56/56 ready, move across portals, then run
     `r_pathTracingGeometryStaticBucketAudit 1`.

Temporary monolithic fallback while bucket resources rebuild is expected.
Missing geometry, device removal, a retirement package released without an
armed/completed query, or failure to return to an exact route-1 cutover is not.

Slice-2 build and deployment:

  command       cmake --build --preset win64-pt-dev-release
  result        success; all SPIR-V shaders up to date
  executable    15,990,784 bytes
  sha256        4534B58ED185723298C78E8E0EE8CA585984CABE3D8074B53D16E0633116168A
  deployment    source and prebuilt executable byte-identical

Slice 3 - exact rigid retirement

  - Replace canonical and older rigid three-frame guesses with the same
    completion authority.
  - Preserve last-valid BLAS publication while replacement is deferred.

Slice 4 - admission

  - Separate new build, update, periodic rebuild, and future copy categories.
  - Enforce operation-count and result-byte budgets.
  - Prioritize live active work; background work carries explicit reason/age.
  - Never publish a TLAS reference to an unbuilt/replaced BLAS.
  - Never silently omit live geometry: retain the last valid route or use the
    existing monolithic fail-safe.

Slice 5 - optional backend telemetry

  - Add exact Vulkan scratch requirement/high-water exposure only if measured
    operation/result admission is insufficient.
  - Keep the NVRHI extension read-only before using it for admission.

Slice 6 - stress and acceptance

  - Forced-low count/result budgets must converge.
  - Exercise portal churn, spawn/despawn, forced rebuild, gate off/on, map
    restart, and buffer growth.
  - Re-run the comm1 generation tuple, crowded skinned scene, and lifecycle
    removal.
  - Capture median/p90/max build/TLAS CPU submission times, result bytes,
    retained retirement bytes, deferral age, and all stale/omission counters.

REJECTED SHORTCUTS
------------------
- CPU frame count alone as proof of GPU completion.
- Treating NVRHI resource tracking of a TLAS as retention of nested BLASes.
- Guessing scratch bytes from result bytes.
- Setting `AllowCompaction` without a compacted-size/copy/completion path.
- Rebuilding or changing shader/SBT geometry identity for an AS lifetime task.
- Omitting active geometry to satisfy a budget without an explicit fail-safe.

FIRST ACCEPTANCE GATE
---------------------
The first code checkpoint adds census only. On the one-shot GEO-10 audit frame,
each ready bucket BLAS now reports its NVRHI result-buffer requirement. The
summary fields are:

  result(queries/failures/bytes/max/alignment/compacted)

These calls are not made on ordinary frames. No CVar default, AS build flag,
route, geometry identity, or shader changed.

Build and deployment:

  command       cmake --build --preset win64-pt-dev-release
  result        success; all SPIR-V shaders up to date
  executable    15,985,664 bytes
  sha256        528E944B1175F1DDA2219E88898241BA1D404A718E8D0584C3E469CAFEEA5DCA
  deployment    source and prebuilt executable byte-identical

The runtime command remains:

  r_pathTracingGeometryStaticBucketAudit 1

The census checkpoint is accepted when a warm route-1 capture shows:

  bucket result bytes                    stable and nonzero
  bucket BLAS build/reuse                 0 / all resident
  result census failures                  0
  geometry/material/cutover failures      0
  route or shading changes                none

POST-SLICE-2 TRAVERSAL REGRESSION AND REPAIR
--------------------------------------------
`success21.log` exposed a pre-existing resident-source invalidation defect
before the requested BLAS off/on retirement test ran. The accepted route first
reached 56/56 resident/ready buckets. During ordinary traversal, the scene
source epoch then changed from 3 to 4 and bucket storage changed from 1790 to
3579. Publication remained empty afterward. No `GEO11 static bucket
retirement armed` line appeared, so this was not a premature event-query
release.

The exact cause was the scene-universe cache key:

  - `RtPathTraceSceneUniverse::EnsureBuilt` treated any change to the broad
    `renderWorld->entityDefs.Num()` as a static-world shape change;
  - spawned or removed runtime entities therefore invalidated the immutable
    authored world-brush census;
  - the full static bucket builder appended all 1,789 surfaces to the existing
    resident arrays instead of replacing or pruning the old records;
  - duplicate stable bucket-surface keys made the assignment/resident pack
    inexact, so material-index publication failed closed and route 1 stayed on
    the empty fallback.

The repair narrows immutable-source invalidation to render-world identity, map
name/timestamp, and portal-area topology. Runtime entity-count churn no longer
rebuilds authored brush storage. A genuine source replacement still receives a
defensive complete-enumeration prune before assignment, so stale records cannot
survive into the exact publication path.

Build and offline validation:

  command       cmake --build --preset win64-pt-dev-release
  result        success; all SPIR-V shaders up to date
  harness       PathTraceCpuPlanningHarness PASS
  executable    15,991,296 bytes
  sha256        C9C209AC0A5E72A5A03D4AFD24C962C4F3FEFC62D06209AB2671CD955BA852DF
  deployment    source and prebuilt executable byte-identical

Runtime acceptance is deliberately narrower than the pending retirement test:

  1. Use the accepted route-1 configuration.
  2. Wait for exact 56/56 resident/ready publication.
  3. Traverse far enough to spawn/despawn runtime entities and cross the
     previously failing portal sequence.
  4. Run `r_pathTracingGeometryStaticBucketAudit 1` after traversal.

Accept only if source/storage generations remain stable during ordinary entity
churn, resident-pack and material-index caches remain exact, route 1 remains
accepted, and no texture or brush corruption appears. A real map reload may
print one bounded `source replacement pruned stale resident records` line and
temporarily rebuild bucket BLASes; it must recover to exact publication.

Do not resume the BLAS off/on retirement test until this traversal repair is
accepted.

TRAVERSAL REPAIR ACCEPTED
-------------------------
`geometry_geo_step9d.txt` accepts the repair. After 1,405 frames of traversal,
the user reported no visual corruption and the one-shot audit showed:

  source / storage / material epochs          3 / 1790 / 1
  source / assignment / pack / material /
    source-fast-path cache hits               1 / 1 / 1 / 1 / 1
  assignment surfaces input / assigned        1789 / 1789
  assignment duplicate / invalid / missing    0 / 0 / 0
  resident triangles assigned / retained      50108 / 50108
  resident / active / ready buckets            56 / 17 / 56
  active TLAS / resident routes                17 / 56
  publication valid / exact / mixed rejected   1 / 1 / 0
  missing BLAS / material / range / ID          0 / 0 / 0 / 0
  route requested / accepted                    1 / 1
  BLAS build / reuse / retire                    0 / 56 / 0
  warm upload bytes                                      0

No source-replacement prune, GEO-11 retirement, device-loss, or fatal-error
line occurred. The broad entity-count invalidation defect is closed. The
focused BLAS off/on retirement test from Slice 2 is authorized again.

FIRST OFF/ON CAPTURE DID NOT EXERCISE WARM BLAS RETIREMENT
----------------------------------------------------------
`geometry_geo_step9e.txt` and the continuous `success21.log` prove clean
enable/build/recovery, but not the requested warm retirement case. The launch
configuration already set `r_pathTracingGeometryStaticBucketBlas 0`. The only
retirement package contained one 200,432-byte metadata buffer and zero BLASes:

  armed packages / tokens / pending          1 / 1..1 / 1
  released buffers / BLASes                  1 / 0
  released buffer / result bytes             200432 / 0
  result-query failures                      0

The path then built from zero to all 56 BLASes and recovered exact route-1
publication without visual corruption or device loss. That accepts cold
enable/recovery only.

The static-bucket armed/released logger was incorrectly one-shot per process,
so its startup metadata package suppressed later warm-retirement evidence.
Because retirement is an exceptional bounded event, every nonempty armed and
released batch is now logged. Query-creation failure remains one-shot. The next
test must start from 56/56 ready, disable once, and show a nonzero BLAS count in
the matching armed/released package before re-enabling.

Diagnostic rebuild and deployment:

  command       cmake --build --preset win64-pt-dev-release
  result        success; all SPIR-V shaders up to date
  executable    15,991,296 bytes
  sha256        FDF659A90640687235F6373837F6E54D5995BD52D9B5AA76146E1B40A888C75A
  deployment    source and prebuilt executable byte-identical

SLICE-2 WARM RETIREMENT ACCEPTED
--------------------------------
The updated `success21.log` starts with bucket BLAS enabled, reaches exact
56/56 readiness, disables the live scaffold once, and records the complete
retirement package:

  armed packages / tokens / pending          1 / 1..1 / 1
  released packages / pending                1 / 0
  released buffers / BLASes                  5 / 56
  released buffer bytes                      8245376
  released BLAS result bytes                 3635712
  result-query failures                      0
  authority                                  gpu-event-query

The off interval used monolithic fallback. Re-enable recreated four backing
buffers, rebuilt two BLASes per frame from 2/56 through 56/56, and returned to
exact route-1 publication:

  source / storage / material epochs          3 / 1790 / 1
  resident / active / ready buckets            56 / 14 / 56
  active TLAS / resident routes                14 / 56
  publication valid / exact / mixed rejected   1 / 1 / 0
  missing BLAS / material / range / ID          0 / 0 / 0 / 0
  steady BLAS build / reuse / retire             0 / 56 / 0
  warm upload bytes                                      0

No device loss or fatal error occurred. Slice 2 is accepted. Slice 3 may replace
canonical and older rigid frame-count retirement guesses with the same exact
graphics completion authority.

SLICE-3 EXACT RIGID RETIREMENT IMPLEMENTED
------------------------------------------
The rigid lifetime audit found three independent CPU-frame guesses, plus one
unretained resource class:

  - older rigid mesh BLASes used a three-frame queue only when async BVH was
    enabled and otherwise dropped immediately;
  - older rigid vertex/index buffers dropped immediately on growth, eviction,
    scaffold disable, or cache clear;
  - canonical rigid BLASes used a three-frame queue;
  - canonical pooled source buffers and the offset-BLAS probe each used their
    own three-frame queue.

All replaced or removed resources now drain into one rigid retirement package.
The package retains older rigid vertex/index buffers, older rigid BLASes,
canonical rigid BLASes, canonical pool buffer generations, and retired
offset-probe BLASes. `PathTracePrimaryPass` creates one event query per
nonempty package, arms it on the graphics queue only after command-list
submission, and releases the package only after the query polls complete. A
query-creation failure retains the package until idle teardown.

Replacement publication remains fail-retained. Canonical build deferral or
allocation failure continues to publish the prior built BLAS. Older rigid BLAS
allocation failure likewise retains the prior built handle. A successful
replacement records its build before the prior BLAS enters the exact
retirement package.

The bounded diagnostics are:

  GEO11 rigid retirement armed
  GEO11 rigid retirement released

The released line reports package, total buffer/BLAS, source-class, result
query failure, retained byte, completion-token, and pending-package counts.

Slice-3 build and deployment:

  command       cmake --build --preset win64-pt-dev-release
  result        success; all SPIR-V shaders up to date
  harness       PathTraceGeometryPoolHarness PASS
  executable    15,995,904 bytes
  sha256        CB9D4A04560EEC740E6FA211838FB2F507BE0A2B576C6E65F41AEBAA5479807F
  deployment    source and prebuilt executable byte-identical

Runtime acceptance remains pending. Start from the accepted full configuration
with the rigid routes warm, then run:

  r_pathTracingRigidBlasGpuScaffold 0; r_pathTracingGeometryCanonicalRigidBlas 0

Move across portals until the matching armed and released lines appear. Then:

  r_pathTracingRigidBlasGpuScaffold 1; r_pathTracingRigidBlasGpuBuild 1; r_pathTracingGeometryCanonicalRigidBlas 1

Continue ordinary traversal and request the existing geometry-source/rigid
diagnostics. Accept only if both retirement lines use
`authority=gpu-event-query`, released pending reaches zero, result-query
failures are zero, the source counts are nonzero for every warm route that was
active, rigid/canonical TLAS parity recovers, and there is no missing geometry,
texture corruption, or device removal.

POST-SLICE-3 VIEW-16 TRIAGE
---------------------------
The runtime launch failed before the requested retirement toggle. Disabling
canonical rigid BLAS, setting clean DI light mode 0, and forcing GPU idle on
portal/scene transitions did not prevent device removal. View 12 survived the
same content and camera direction. The fault is therefore downstream of shared
scene/TLAS/material construction and upstream or inside production view-16
temporal/spatial/resolve work.

Audit of the host dispatch route found that view 16 ignored the temporal and
spatial diagnostic CVars for stage admission. The CPU route now provides three
real boundaries without shader edits:

  temporal=0              initial only
  temporal=1 spatial=0    initial plus temporal
  temporal=1 spatial=1    full production stage chain

Initial-only and temporal-only return before transmission PSR,
material-feature composition, GI, RR, and all later consumers. The correction
built successfully with all SPIR-V up to date. The deployed 15,996,416-byte
executable is byte-identical to the build output, SHA-256
D0BAD4076A6D4E96368742C0A23D9060442826C5EB24D68066883E53901A4AA1.
