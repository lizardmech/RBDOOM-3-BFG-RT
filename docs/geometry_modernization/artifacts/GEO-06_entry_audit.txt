GEO-06 - pooled mesh ingestion entry audit
============================================
Status: entry audit complete; callback-MD5 registry proof passed; checked
allocation, immutable CPU source registry, and lifecycle shadow wiring pass.
Date: 2026-07-24.

SCOPE
-----
This is the required current-code audit before GEO-06 allocation or routing
changes. It treats the ticket and decision register as contracts, not the older
proposal documents. No pooled buffer or live-route code is implemented here.

PREDECESSOR CHECK
-----------------
GEO-05 now passes:

  per-world owner and monotonic world generation;
  entity slot generation and exact Free invalidation;
  canonical mesh/instance shadow records;
  immutable frontend SMP entity snapshot;
  populated feed before and after same-map restart;
  zero admission drops and no repeated portal-list crash in geometry_geo20.

The audit found one missing predecessor proof: callback MD5 entities initially
never produced SkinnedBindSource/SkinnedSurface shadow records. GEO-05 now
contains the validated frontend-only discovery correction. geometry_geo23
proves bounded discovery, thousands of steady refreshes, exact skinned
removal/re-add, source-mesh reuse and zero collision/stale counts.

CURRENT RIGID ROUTE
-------------------
RtSmokeGeometryUniverse::RigidMeshCandidateRecord currently combines four
different ownership domains:

  source observation: const srfTriangles_t* tri and sourceRange;
  cached CPU payload: cachedLocalVertices/cachedLocalIndexes;
  GPU storage: one rigidVertexBuffer and rigidIndexBuffer per mesh candidate;
  acceleration: one rigidBlas plus its desc/build state per mesh candidate.

The route already shares a candidate BLAS among its eligible rigid instances.
Its scalability problem is storage ownership: every unique candidate owns
separate vertex/index buffers, while frame route buffers merge copies and carry
uint32 vertexOffset/indexOffset/triangleOffset per instance.

PathTraceSmokeVertex is the legacy interleaved rendered layout:

  position, normal, texCoord, color, color2, tangent, bitangent.

It is full fidelity, but it is not the new registry ABI. D13 requires raw
float3 positions to be split from full-fidelity attributes immediately.
Therefore GEO-06 must not name an interleaved PathTraceSmokeVertex allocation
the canonical pool and defer the split.

OFFSET AND BLAS AUDIT
---------------------
NVRHI GeometryTriangles already exposes uint64 vertexOffset and indexOffset.
The Vulkan backend adds each offset to the corresponding buffer device address
and uses vertexCount - 1 as maxVertex.

The local wrapper is not ready:

  InitSmokeTriangleGeometry accepts only an int indexOffset;
  it hard-codes vertexOffset=0;
  CreateSmokeBlas accepts counts but no byte ranges;
  the wrapper assumes sizeof(PathTraceSmokeVertex) vertex stride.

The canonical wrapper must accept checked byte offsets and explicit position
stride/count. It must reject misalignment, overflow, out-of-buffer ranges and
uint32 count narrowing before creating an acceleration structure.

ACCEPTED FIRST STORAGE ABI
--------------------------
The first pool slice must keep these allocations logically separate:

  position bytes
    raw full-precision float3 positions used by BLAS;

  attribute bytes
    full-fidelity normal, UV, color/color2, tangent and bitangent data;

  index bytes
    uint32 local vertex indices;

  triangle bytes
    material ID, material-table binding/revision, triangle class/flags and
    any emissive-local metadata required by current consumers.

Each immutable mesh source record must contain:

  canonical MeshKey;
  source content generation/revision;
  source checksum plus full-key equality;
  buffer storage generation;
  uint64 byte offset, byte size and alignment for every allocation;
  vertex/index/triangle counts;
  explicit residency/upload/BLAS states;
  last-use/retirement information sufficient to keep in-flight generations
  alive during pool growth.

Canonical identity must not contain storage offsets, material state, AS handles
or frame route indices. Shader uint32 values may be produced only by checked
page-relative narrowing.

IMPLEMENTATION ORDER AFTER THE MD5 GATE
---------------------------------------
1. Add pure checked uint64 allocation/range helpers and deterministic CPU tests
   for alignment, addition/multiplication overflow, near-4-GiB offsets, page
   narrowing and growth-generation preservation.
2. Add observation-only CPU source records keyed by canonical MeshKey. Copy
   immutable source payload on first discovery/source revision; do not retain a
   frame-temporary srfTriangles_t pointer as durable ownership.
3. Add separate position/attribute/index/triangle growable GPU pools and
   storage-generation retirement. Keep the live rigid route unchanged.
4. Count first upload bytes, revision uploads, steady-state upload bytes,
   source reuse, retained bytes, rejected ranges and retired generations.
5. Extend the BLAS descriptor wrapper with uint64 byte offsets and validate a
   Vulkan non-zero-position/non-zero-index-offset micro-test, including first
   and last primitive material/attribute lookup.
6. Add same-content shadow comparison against BuildRigidLocalMeshData for
   counts, checksums, material ranges and representative decoded endpoints.
7. Adapt the established rigid route behind a new default-off compare gate.
   One eligible immutable mesh owns one pooled BLAS; TLAS instances retain
   independent transforms and InstanceKeys.
8. Compare old/new rigid geometry, material, transform, motion, emissive and
   lifecycle tuples. Enumerate every mismatch; do not silently fall back.
9. Validate repeated rigid props as positive sharing controls and repeated
   zombies/Fatzombie as negative routing controls. Skinned source records may
   share immutable data, but no live skinned output or BLAS may enter the rigid
   route.
10. Validate removal/re-add, map restart and pool growth while old buffer
    generations are in flight, then collect identical-content upload/memory/
    BLAS/TLAS measurements.

NON-GOALS
---------
No compression, meshlets, static-area cutover, skinned-output promotion, broad
scene-origin rewrite, cap increase, whole-payload steady-frame hashing, or
default-on route is part of this entry slice.

NEXT GATE
---------
Implement step 1: pure checked uint64 allocation/range helpers and deterministic
CPU boundary tests. Do not add GPU pools or alter the live rigid route in that
slice.

STEP 1 CHECKED POOL PLANNER (2026-07-24)
----------------------------------------
Implemented:

  neo/renderer/NVRHI/PathTraceGeometryPoolPlan.h/.cpp
  neo/tests/pathtrace_geometry_pool_harness.cpp

The planner is pure CPU code with no renderer object, NVRHI resource, pointer,
upload or live-route input. It defines:

  checked elementCount * elementStride byte sizing;
  aligned append allocation using uint64 byte offsets/sizes;
  failure-atomic capacity/overflow rejection;
  bounded geometric growth with a monotonic storage generation;
  explicit old/new capacity, copy bytes and generation in the growth plan;
  range rebasing only when the entire source range belongs to the previous
  generation and lies inside the copied payload;
  stable named results for every rejection.

The deterministic PathTraceGeometryPoolHarness covers:

  aligned append and padding;
  capacity failure without state mutation;
  empty size, zero alignment and zero element stride;
  element counts above UINT32_MAX;
  multiplication/alignment/end overflow;
  absolute allocation at the 4 GiB boundary;
  checked page-base subtraction before shader uint32 narrowing;
  no-growth generation preservation;
  1 KiB -> 8 KiB bounded growth and generation 7 -> 8;
  preservation of copied range offset/size across growth;
  wrong-generation and not-fully-copied range rejection;
  maximum-capacity and generation-overflow rejection.

Verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  cmake --build --preset win64-pt-dev-release
    --target PathTraceGeometryPoolHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

The broader PathTraceCpuPlanningHarness currently reports three assertions in
the existing rigid-transform/BVH dirty-token planner lane. This slice changes
none of that target's source files or inputs; those failures are recorded as
non-gating rather than silently attributed to the pool planner.

Deployed executable:

  SHA-256:
    18EEE765DD9AC699E11A4EA0FF2F32AFC9CAA93A1BA95F477C03ED65673C407C
  mtime:
    2026-07-24 16:24:07 Australia/Sydney

Live behavior:

  unchanged. The planner has no live call site and allocates no GPU resource.

STEP 1 RESULT
-------------
  PASS - checked uint64 byte sizing/allocation
  PASS - explicit fail-closed range and capacity errors
  PASS - generation-safe growth/rebase planning
  PASS - near-4-GiB and shader-page boundary coverage
  PASS - no live route or shader change

Next implementation slice:

  observation-only immutable CPU source records keyed by canonical MeshKey,
  with first-discovery/source-revision copies and no retained frame-temporary
  srfTriangles_t pointer.

STEP 2 IMMUTABLE CPU SOURCE REGISTRY (2026-07-24)
-------------------------------------------------
Implemented:

  neo/renderer/NVRHI/PathTraceGeometrySourceRegistry.h/.cpp
  neo/tests/pathtrace_geometry_source_registry_harness.cpp

The source registry is still pure CPU code with no live lifecycle call site,
renderer pointer, NVRHI resource, upload, BLAS or route mutation. It owns four
separate value streams:

  raw float3 positions;
  full-fidelity decoded normal, UV, color/color2, tangent, bitangent and
    bitangent-sign attributes;
  uint32 local indexes;
  source material slot, triangle class/flags and geometry-local emissive index.

The registry accepts only valid RegisteredRenderModel + Rigid canonical mesh
keys in this slice. Static world data is deferred to the static-bucket ticket.
SkinnedBindSource and Skinned observations are named negative controls so an
animated MD5 snapshot cannot be mistaken for immutable rigid source data.

The initial proposal put resolved material ID and material-table binding/
revision in the immutable triangle stream. That is incompatible with D17:
skins and per-instance material overrides are binding state, not MeshKey-owned
source content. The corrected source ABI retains a stable source material slot.
The later instance/route binding record resolves that slot to a material-table
ID and revision. This prevents one instance override from revising or replacing
a source record shared by other instances.

Observe behavior is explicit:

  first non-zero source revision plus a complete payload
    validates counts/topology/local index ranges, computes one source checksum,
    deep-copies all four streams and returns Added;

  the same canonical key and source revision
    returns Reused without accepting, hashing or copying a payload;

  a larger source revision plus a complete valid payload
    builds a replacement, updates checksum/retained bytes and returns Revised;

  a stale/zero revision, missing payload, count mismatch, non-triangle
    topology, out-of-range index or ineligible source class
    returns a stable named rejection without publishing a partial record.

The deterministic PathTraceGeometrySourceRegistryHarness covers:

  separate position and attribute ownership;
  mutation of caller arrays after ingestion;
  exact retained/copied-byte accounting;
  steady same-revision reuse with no second payload copy;
  content-revision replacement and checksum change;
  stale-revision failure atomicity;
  static/skinned negative controls;
  zero revision and missing first-discovery payload;
  stream-count, topology and local-index validation;
  checksum sensitivity to indexes, source material slots and bitangent sign.

Verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Deployed executable:

  SHA-256:
    B578B10E8F52F50A12E6EF712A7C60F81FD4D969FA413B686D1709D7E82582BC
  mtime:
    2026-07-24 16:40:00 Australia/Sydney

Live behavior:

  unchanged. The registry has no lifecycle or renderer call site yet.

STEP 2 RESULT
-------------
  PASS - canonical-keyed, value-owned source payload
  PASS - split position/attribute/index/triangle CPU ABI
  PASS - first/revision copy and zero-copy steady reuse contract
  PASS - rigid eligibility and skinned negative controls
  PASS - resolved material binding kept outside immutable mesh content
  PASS - no retained srfTriangles_t/idDrawVert/index pointer

Next implementation slice:

  wire observation-only RegisteredRenderModel + Rigid first-discovery ingestion
  into the lifecycle shadow registry. Convert idDrawVert values only when a
  source record is absent or explicitly revised, report source reuse/copy/
  reject bytes in the bounded dump, and keep the live rigid route unchanged.

STEP 2 LIFECYCLE SHADOW WIRING CANDIDATE (2026-07-24)
-----------------------------------------------------
Implemented but not yet runtime-accepted:

  PtGeometryLifecycleWorldRegistry owns one CPU source registry;
  map begin and shadow disable clear all owned source values;
  RegisteredRenderModel + Rigid discovery decodes/copies idDrawVert values only
    when the canonical MeshKey has no source record;
  repeated observations call the same-revision no-payload reuse path;
  resolved MD5 snapshots increment a skinned negative-control counter and are
    never copied into the immutable rigid registry;
  lookup is hash-bucketed and still requires full MeshKey equality;
  the GEO06 bounded dump reports records, retained bytes, add/reuse/revise/
    reject/skinned-negative counts, payload-copy count and copied bytes;
  up to eight source samples report revision/checksum/counts/retained bytes and
    first/last local index plus source material slot;
  the established live rigid route is unchanged.

Classifier correction:

  D17 also separates classifier state from MeshKey content. The source
  triangle field is therefore geometryLocalFlags, not the material-derived
  live triangleClassAndFlags value. Source material slot remains immutable;
  resolved material ID, classifier flags and emissive binding belong to the
  later instance/route binding record.

Current revision signal:

  registered render models are treated as immutable within their non-zero
  sourceAssetGeneration. The lifecycle candidate uses that generation as the
  source content revision. A future explicit model-reload generation is still
  required before same-name in-place source hot reload can be claimed; steady
  observation deliberately does not hash every vertex to infer one.

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    78B15C80221C5533114C671589B713C755BB742D3819CF3D08F9473B29CE8350
  mtime:
    2026-07-24 16:45:45 Australia/Sydney

Focused runtime acceptance:

  r_pathTracingGeometryShadowRegistry 1
  mapRestart
  traverse a prop-heavy area and view/spawn several animated zombies
  r_pathTracingGeometryShadowRegistryDump 1
  move/turn for several seconds
  r_pathTracingGeometryShadowRegistryDump 1

First dump requirements:

  GEO06 records/add/copies/copiedBytes > 0;
  reused > 0 when repeated rigid mesh surfaces are present;
  reject = 0;
  skinnedNegative > 0 when MD5 entities were discovered;
  retainedBytes equals stable owned source storage, not per-instance geometry;
  GEO05 collision/stale remain zero;
  no visual or route behavior changes.

Second dump requirements:

  records and retainedBytes stay unchanged;
  add/revise/copies/copiedBytes = 0 during steady observation;
  reject remains zero;
  a quiet interval may legitimately report reuse = 0 because unchanged rigid
    defs do not need to re-enter the lifecycle funnel.

STEP 2 LIFECYCLE SHADOW WIRING RUNTIME PASS (2026-07-24)
--------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo24.log
  bytes:
    28151
  created:
    2026-07-24 16:48:53 Australia/Sydney
  last write at inspection:
    2026-07-24 16:49:50 Australia/Sydney
  executable banner:
    RBDOOM 3 BFG 1.6.0.1403 win-x64 Jul 24 2026 14:28:14

First-discovery dump at frame 1567:

  GEO06 records/retainedBytes = 946/5464756;
  add/reuse/revise/reject = 946/10425/0/0;
  skinnedNegative = 62;
  payload copies/copiedBytes = 946/5464756;
  source hash collisions = 0;
  GEO05 collision/stale = 0/0;
  live instance classes static/rigid/skinned = 1791/1681/62.

This proves one owned payload copy for every admitted rigid source record,
substantial reuse across repeated observations/instances, and explicit
rejection of all 62 currently discovered skinned snapshots from the rigid
source pool. The 946 source records are fewer than 1681 rigid instances as
required by mesh sharing.

Steady dump at frame 1853:

  GEO06 records/retainedBytes = 946/5464756, unchanged;
  add/reuse/revise/reject = 0/4584/0/0;
  skinnedNegative = 0;
  payload copies/copiedBytes = 0/0;
  source hash collisions = 0;
  GEO05 collision/stale = 0/0;
  live instance classes static/rigid/skinned = 1791/1681/62, unchanged.

All eight bounded samples retain identical mesh hash, asset generation,
surface ordinal, source revision, checksum, counts, retained bytes, endpoint
indexes and endpoint source material slots across both dumps. The thousands of
steady rigid observations therefore use the same-revision no-payload path:
they neither re-decode nor hash/copy the source arrays.

The log contains no fatal error, crash, access violation or Vulkan device
removal. Its image warnings are ordinary mars_city1 level-load warnings and
precede the accepted source-registry dumps.

STEP 2 FINAL RESULT
-------------------
  PASS - one value-owned CPU source record per canonical rigid mesh
  PASS - repeated rigid observations reuse existing source values
  PASS - steady source copy and copied bytes are exactly zero
  PASS - skinned frontend snapshots remain explicit negative controls
  PASS - record count, retained bytes and sampled checksums are stable
  PASS - zero source reject/collision and zero GEO05 collision/stale
  PASS - live geometry route remains observation-only

Next implementation slice:

  plan and instantiate separate shadow-only position, attribute, index and
  source-triangle GPU pools using the checked planner. Keep the established
  rigid route unchanged, preserve old storage generations across growth, and
  add upload/retirement diagnostics before attempting non-zero-offset BLAS
  consumption.

STEP 3 SMP OWNERSHIP AUDIT (2026-07-24)
---------------------------------------
The lifecycle source registry is frontend-owned. The NVRHI device, command
list, RtSmokeGeometryUniverse and map-reset waitForIdle boundary are backend-
owned. With com_smp enabled, the backend must not read the live registry,
renderWorld entityDefs, mutable registry vectors or source srfTriangles_t
arrays through a new cross-thread pointer.

The existing entity-feed snapshot proves the usable command-boundary pattern:
frontend values are copied into R_FrameAlloc storage, attached to viewDef_t and
remain valid until that view command finishes on the backend. MAX_FRAME_MEMORY
is 64 MiB, so copying the accepted 5.46 MiB source universe every frame would
fit once but would be an invalid steady-state cost and could multiply across
subviews.

Accepted source transport:

  only the primary, non-subview view carries source deltas;
  the frontend appends never-published Added/Revised records to an immutable
    frame snapshot;
  snapshot metadata contains WorldKey, source revision/checksum, canonical
    MeshKey and checked offsets/counts into four contiguous value arrays;
  position, attribute, index and source-triangle values are copied only for
    the delta, never for same-revision reuse;
  a checked configurable byte budget paginates a large first population over
    multiple frames instead of risking the 64 MiB frame arena;
  the backend imports each canonical key/revision into its own persistent CPU
    source registry and deduplicates repeated delta delivery by full key plus
    revision/checksum;
  backend CPU ownership survives GPU pool growth/reset and is the replay source
    after ResetRayTracingSmokeSceneResources;
  map/world generation mismatch rejects the packet before any allocation or
    upload;
  the frontend may advance its published cursor only after attaching the
    packet to the primary draw command; draw commands are not optional work;
  source revisions require an append-only publication event rather than
    mutating a packet already visible to the backend.

Rejected alternatives:

  direct backend access to PtGeometryLifecycleWorldRegistry
    violates the established SMP snapshot rule and recreates the stale-owner
    crash class;

  retaining lifecycle/source vector pointers in RtSmokeGeometryUniverse
    is invalid because vector growth, map reset and world teardown are frontend
    lifetime events;

  copying all 5.46 MiB into every viewDef every frame
    violates the zero steady-state copy requirement;

  rebuilding identity from the current pointer-heavy RtPathTraceMeshKey
    preserves material/cache/pointer state in identity and does not satisfy
    D17 or the validated canonical MeshKey contract;

  uploading directly from the frame snapshot without a backend CPU owner
    cannot replay after device/resource reset and makes paginated partial
    residency difficult to validate.

Revised step-3 order:

  1. Add pure checked source-delta layout/import planning and boundary tests.
  2. Add primary-view-only immutable frame delta capture with a bounded budget.
  3. Add backend CPU import/dedup diagnostics; do not create GPU buffers yet.
  4. Runtime-prove first-population pagination, zero steady transport bytes,
     exact record/checksum parity and map-restart generation rejection.
  5. Instantiate the four backend shadow GPU pools from the imported records.
  6. Add generation-safe buffer replacement/retirement and reset replay.

This transport prerequisite is required before the task's GPU-pool step; it is
not a new live route or a relaxation of the GEO-06 scope.

STEP 3A CHECKED SOURCE-DELTA TRANSPORT PLANNER (2026-07-24)
-----------------------------------------------------------
Implemented:

  neo/renderer/NVRHI/PathTraceGeometrySourceTransport.h/.cpp
  neo/tests/pathtrace_geometry_source_transport_harness.cpp

This remains pure CPU code. It does not allocate frame memory, attach data to a
view, access the backend, create a GPU resource or alter a route.

The planner consumes an append cursor, immutable source registry and uint64
byte budget. It produces:

  an exact first/next record cursor and complete/deferred state;
  one metadata record per admitted canonical source;
  source revision, checksum and retained-byte contract;
  element-relative offsets into separate position, attribute, index and
    source-triangle arrays;
  exact uint64 element totals and packed bytes;
  accounting for the actual pointer-bearing frame snapshot structure plus
    the frontend R_FrameAlloc 128-byte metadata/value allocation rounding.

If the next complete source record does not fit after at least one admission,
the page closes cleanly and exposes its continuation cursor. If the first
record cannot fit, RecordExceedsBudget returns with no cursor progress. Invalid
cursor, zero budget, invalid source record and arithmetic overflow are distinct
fail-closed results.

The import validator checks:

  full canonical MeshKey validity;
  non-zero source revision/checksum;
  presence and checked bounds of all four streams;
  counts against the canonical key;
  local topology/index validity through the source payload validator;
  exact retained bytes;
  one-time delta checksum equality.

Harness coverage:

  complete multi-record plan;
  bounded partial page and exact continuation;
  no replay of earlier record offsets;
  invalid cursor and zero budget;
  single-record-over-budget failure atomicity;
  empty complete delta;
  intact import;
  checksum, retained-byte, range and missing-stream rejection.

Verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometrySourceTransportHarness
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Live behavior:

  unchanged; there is no transport call site yet.

Next implementation slice:

  add the primary-view-only R_FrameAlloc producer and backend CPU importer
  behind the existing geometry shadow gate. First population must paginate
  under a configurable budget; steady frames must publish zero records/bytes.

STEP 3B LIVE SMP TRANSPORT CANDIDATE (2026-07-24)
-------------------------------------------------
Implemented but not yet runtime-accepted:

  viewDef_t carries one immutable PtGeometrySourceTransportSnapshot pointer;
  PtGeometryLifecycle publishes only from the primary non-subview frontend;
  r_pathTracingGeometrySourceDeltaBudgetMB defaults to 8 and clamps to 1..32;
  checked plans paginate complete source records without splitting a mesh;
  metadata and four value arrays use R_FrameAlloc lifetime and 128-byte
    allocation rounding;
  attaching the primary draw command advances the frontend publication cursor;
  world generation plus publication generation/sequence distinguish map reset,
    disable/re-enable replay and ordered pages;
  RtSmokeGeometryUniverse validates the entire packet before importing any
    record, then deep-copies it into a backend-owned source registry;
  backend full-key/revision/checksum equality remains authoritative;
  backend CPU values survive later GPU pool replacement and are cleared with
    geometry-universe/map resource reset;
  the bounded GEO06 backend dump reports world, publication tuple, cursor,
    records, retained bytes and interval import/reuse/revise/reject/transport
    bytes;
  no NVRHI buffer, BLAS or live route is changed.

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometrySourceTransportHarness
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    D3B8BF522439239B797CA4463762343DCB37C3F3E3EAC2FA858B80A79225F904
  mtime:
    2026-07-24 17:25:01 Australia/Sydney

Focused runtime acceptance:

  r_pathTracingGeometrySourceDeltaBudgetMB 1
  r_pathTracingGeometryShadowRegistry 1
  mapRestart
  wait/traverse until the initial pages have crossed the backend
  r_pathTracingGeometryShadowRegistryDump 1
  wait ten seconds
  r_pathTracingGeometryShadowRegistryDump 1

First dump requirements:

  frontend and backend source record counts/retained bytes match;
  backend cursor equals the frontend source record count;
  publication sequence > 1 proves forced 1 MiB pagination;
  import count equals backend record count;
  reject/revise = 0;
  transport bytes are bounded page totals, not steady-frame traffic;
  GEO05 collision/stale and source reject/collision remain zero.

Second dump requirements:

  frontend/backend records and retained bytes remain unchanged;
  backend interval import/reuse/revise/reject/transportBytes = 0/0/0/0/0;
  no visual change, crash, frame-arena exhaustion or Vulkan device removal.

STEP 3B LIVE SMP TRANSPORT RUNTIME PASS (2026-07-24)
----------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo25.log
  bytes:
    129813
  created/last write:
    2026-07-24 17:30:26 Australia/Sydney

The test forced r_pathTracingGeometrySourceDeltaBudgetMB=1.

Populated dump at frame 1247:

  frontend records/retainedBytes = 946/5464756;
  frontend add/revise/reject/copies = 946/0/0/946;
  frontend source collisions = 0;
  backend world = 3;
  backend publication generation:sequence = 3:6;
  backend cursor/records/retainedBytes = 946/946/5464756;
  backend import/reuse/revise/reject = 946/0/0/0;
  backend transportBytes = 5573504;
  GEO05 collision/stale = 0/0.

Six publication pages prove that the 1 MiB budget paginated the first source
population. Backend count and retained bytes exactly match the frontend owner.
Transport overhead above retained source values is 108748 bytes across the six
frame packets, including snapshot structures, record metadata and 128-byte
allocation rounding.

Steady dump at frame 1468:

  frontend records/retainedBytes = 946/5464756, unchanged;
  frontend add/revise/reject/copies/copiedBytes = 0/0/0/0/0;
  backend publication remains 3:6 and cursor remains 946;
  backend records/retainedBytes = 946/5464756, unchanged;
  backend import/reuse/revise/reject/transportBytes = 0/0/0/0/0;
  GEO05 collision/stale = 0/0.

The log contains no producer rejection, frame-arena exhaustion, fatal error,
crash or Vulkan device removal.

STEP 3B FINAL RESULT
--------------------
  PASS - immutable primary-view frontend/backend packet lifetime
  PASS - forced multi-frame pagination and exact continuation cursor
  PASS - complete backend persistent CPU ownership
  PASS - frontend/backend record and retained-byte parity
  PASS - zero rejected records/pages
  PASS - exactly zero steady transport records and bytes
  PASS - no subview duplication or live route change observed

Next implementation slice:

  instantiate four backend shadow GPU pools from the imported CPU registry.
  Use checked uint64 suballocations, upload only newly resident/revised records,
  retain old storage generations across growth, and report pool capacity/used/
  upload/retirement state without changing BLAS or traversal.
