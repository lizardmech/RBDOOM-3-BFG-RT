GEO-06 - pooled mesh ingestion entry audit
============================================
Status: entry audit complete; callback-MD5 registry proof passed; checked
allocation, immutable CPU source registry, and lifecycle shadow wiring pass.
Date: 2026-07-24.

SCOPE
-----
This is the required current-code audit before GEO-06 allocation or routing
changes. It treats the ticket and decision register as contracts, not the older
proposal documents. No pooled buffer or live-route code is implemented here.

PREDECESSOR CHECK
-----------------
GEO-05 now passes:

  per-world owner and monotonic world generation;
  entity slot generation and exact Free invalidation;
  canonical mesh/instance shadow records;
  immutable frontend SMP entity snapshot;
  populated feed before and after same-map restart;
  zero admission drops and no repeated portal-list crash in geometry_geo20.

The audit found one missing predecessor proof: callback MD5 entities initially
never produced SkinnedBindSource/SkinnedSurface shadow records. GEO-05 now
contains the validated frontend-only discovery correction. geometry_geo23
proves bounded discovery, thousands of steady refreshes, exact skinned
removal/re-add, source-mesh reuse and zero collision/stale counts.

CURRENT RIGID ROUTE
-------------------
RtSmokeGeometryUniverse::RigidMeshCandidateRecord currently combines four
different ownership domains:

  source observation: const srfTriangles_t* tri and sourceRange;
  cached CPU payload: cachedLocalVertices/cachedLocalIndexes;
  GPU storage: one rigidVertexBuffer and rigidIndexBuffer per mesh candidate;
  acceleration: one rigidBlas plus its desc/build state per mesh candidate.

The route already shares a candidate BLAS among its eligible rigid instances.
Its scalability problem is storage ownership: every unique candidate owns
separate vertex/index buffers, while frame route buffers merge copies and carry
uint32 vertexOffset/indexOffset/triangleOffset per instance.

PathTraceSmokeVertex is the legacy interleaved rendered layout:

  position, normal, texCoord, color, color2, tangent, bitangent.

It is full fidelity, but it is not the new registry ABI. D13 requires raw
float3 positions to be split from full-fidelity attributes immediately.
Therefore GEO-06 must not name an interleaved PathTraceSmokeVertex allocation
the canonical pool and defer the split.

OFFSET AND BLAS AUDIT
---------------------
NVRHI GeometryTriangles already exposes uint64 vertexOffset and indexOffset.
The Vulkan backend adds each offset to the corresponding buffer device address
and uses vertexCount - 1 as maxVertex.

The local wrapper is not ready:

  InitSmokeTriangleGeometry accepts only an int indexOffset;
  it hard-codes vertexOffset=0;
  CreateSmokeBlas accepts counts but no byte ranges;
  the wrapper assumes sizeof(PathTraceSmokeVertex) vertex stride.

The canonical wrapper must accept checked byte offsets and explicit position
stride/count. It must reject misalignment, overflow, out-of-buffer ranges and
uint32 count narrowing before creating an acceleration structure.

ACCEPTED FIRST STORAGE ABI
--------------------------
The first pool slice must keep these allocations logically separate:

  position bytes
    raw full-precision float3 positions used by BLAS;

  attribute bytes
    full-fidelity normal, UV, color/color2, tangent and bitangent data;

  index bytes
    uint32 local vertex indices;

  triangle bytes
    material ID, material-table binding/revision, triangle class/flags and
    any emissive-local metadata required by current consumers.

Each immutable mesh source record must contain:

  canonical MeshKey;
  source content generation/revision;
  source checksum plus full-key equality;
  buffer storage generation;
  uint64 byte offset, byte size and alignment for every allocation;
  vertex/index/triangle counts;
  explicit residency/upload/BLAS states;
  last-use/retirement information sufficient to keep in-flight generations
  alive during pool growth.

Canonical identity must not contain storage offsets, material state, AS handles
or frame route indices. Shader uint32 values may be produced only by checked
page-relative narrowing.

IMPLEMENTATION ORDER AFTER THE MD5 GATE
---------------------------------------
1. Add pure checked uint64 allocation/range helpers and deterministic CPU tests
   for alignment, addition/multiplication overflow, near-4-GiB offsets, page
   narrowing and growth-generation preservation.
2. Add observation-only CPU source records keyed by canonical MeshKey. Copy
   immutable source payload on first discovery/source revision; do not retain a
   frame-temporary srfTriangles_t pointer as durable ownership.
3. Add separate position/attribute/index/triangle growable GPU pools and
   storage-generation retirement. Keep the live rigid route unchanged.
4. Count first upload bytes, revision uploads, steady-state upload bytes,
   source reuse, retained bytes, rejected ranges and retired generations.
5. Extend the BLAS descriptor wrapper with uint64 byte offsets and validate a
   Vulkan non-zero-position/non-zero-index-offset micro-test, including first
   and last primitive material/attribute lookup.
6. Add same-content shadow comparison against BuildRigidLocalMeshData for
   counts, checksums, material ranges and representative decoded endpoints.
7. Adapt the established rigid route behind a new default-off compare gate.
   One eligible immutable mesh owns one pooled BLAS; TLAS instances retain
   independent transforms and InstanceKeys.
8. Compare old/new rigid geometry, material, transform, motion, emissive and
   lifecycle tuples. Enumerate every mismatch; do not silently fall back.
9. Validate repeated rigid props as positive sharing controls and repeated
   zombies/Fatzombie as negative routing controls. Skinned source records may
   share immutable data, but no live skinned output or BLAS may enter the rigid
   route.
10. Validate removal/re-add, map restart and pool growth while old buffer
    generations are in flight, then collect identical-content upload/memory/
    BLAS/TLAS measurements.

NON-GOALS
---------
No compression, meshlets, static-area cutover, skinned-output promotion, broad
scene-origin rewrite, cap increase, whole-payload steady-frame hashing, or
default-on route is part of this entry slice.

NEXT GATE
---------
Implement step 1: pure checked uint64 allocation/range helpers and deterministic
CPU boundary tests. Do not add GPU pools or alter the live rigid route in that
slice.

STEP 1 CHECKED POOL PLANNER (2026-07-24)
----------------------------------------
Implemented:

  neo/renderer/NVRHI/PathTraceGeometryPoolPlan.h/.cpp
  neo/tests/pathtrace_geometry_pool_harness.cpp

The planner is pure CPU code with no renderer object, NVRHI resource, pointer,
upload or live-route input. It defines:

  checked elementCount * elementStride byte sizing;
  aligned append allocation using uint64 byte offsets/sizes;
  failure-atomic capacity/overflow rejection;
  bounded geometric growth with a monotonic storage generation;
  explicit old/new capacity, copy bytes and generation in the growth plan;
  range rebasing only when the entire source range belongs to the previous
  generation and lies inside the copied payload;
  stable named results for every rejection.

The deterministic PathTraceGeometryPoolHarness covers:

  aligned append and padding;
  capacity failure without state mutation;
  empty size, zero alignment and zero element stride;
  element counts above UINT32_MAX;
  multiplication/alignment/end overflow;
  absolute allocation at the 4 GiB boundary;
  checked page-base subtraction before shader uint32 narrowing;
  no-growth generation preservation;
  1 KiB -> 8 KiB bounded growth and generation 7 -> 8;
  preservation of copied range offset/size across growth;
  wrong-generation and not-fully-copied range rejection;
  maximum-capacity and generation-overflow rejection.

Verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  cmake --build --preset win64-pt-dev-release
    --target PathTraceGeometryPoolHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

The broader PathTraceCpuPlanningHarness currently reports three assertions in
the existing rigid-transform/BVH dirty-token planner lane. This slice changes
none of that target's source files or inputs; those failures are recorded as
non-gating rather than silently attributed to the pool planner.

Deployed executable:

  SHA-256:
    18EEE765DD9AC699E11A4EA0FF2F32AFC9CAA93A1BA95F477C03ED65673C407C
  mtime:
    2026-07-24 16:24:07 Australia/Sydney

Live behavior:

  unchanged. The planner has no live call site and allocates no GPU resource.

STEP 1 RESULT
-------------
  PASS - checked uint64 byte sizing/allocation
  PASS - explicit fail-closed range and capacity errors
  PASS - generation-safe growth/rebase planning
  PASS - near-4-GiB and shader-page boundary coverage
  PASS - no live route or shader change

Next implementation slice:

  observation-only immutable CPU source records keyed by canonical MeshKey,
  with first-discovery/source-revision copies and no retained frame-temporary
  srfTriangles_t pointer.

STEP 2 IMMUTABLE CPU SOURCE REGISTRY (2026-07-24)
-------------------------------------------------
Implemented:

  neo/renderer/NVRHI/PathTraceGeometrySourceRegistry.h/.cpp
  neo/tests/pathtrace_geometry_source_registry_harness.cpp

The source registry is still pure CPU code with no live lifecycle call site,
renderer pointer, NVRHI resource, upload, BLAS or route mutation. It owns four
separate value streams:

  raw float3 positions;
  full-fidelity decoded normal, UV, color/color2, tangent, bitangent and
    bitangent-sign attributes;
  uint32 local indexes;
  source material slot, triangle class/flags and geometry-local emissive index.

The registry accepts only valid RegisteredRenderModel + Rigid canonical mesh
keys in this slice. Static world data is deferred to the static-bucket ticket.
SkinnedBindSource and Skinned observations are named negative controls so an
animated MD5 snapshot cannot be mistaken for immutable rigid source data.

The initial proposal put resolved material ID and material-table binding/
revision in the immutable triangle stream. That is incompatible with D17:
skins and per-instance material overrides are binding state, not MeshKey-owned
source content. The corrected source ABI retains a stable source material slot.
The later instance/route binding record resolves that slot to a material-table
ID and revision. This prevents one instance override from revising or replacing
a source record shared by other instances.

Observe behavior is explicit:

  first non-zero source revision plus a complete payload
    validates counts/topology/local index ranges, computes one source checksum,
    deep-copies all four streams and returns Added;

  the same canonical key and source revision
    returns Reused without accepting, hashing or copying a payload;

  a larger source revision plus a complete valid payload
    builds a replacement, updates checksum/retained bytes and returns Revised;

  a stale/zero revision, missing payload, count mismatch, non-triangle
    topology, out-of-range index or ineligible source class
    returns a stable named rejection without publishing a partial record.

The deterministic PathTraceGeometrySourceRegistryHarness covers:

  separate position and attribute ownership;
  mutation of caller arrays after ingestion;
  exact retained/copied-byte accounting;
  steady same-revision reuse with no second payload copy;
  content-revision replacement and checksum change;
  stale-revision failure atomicity;
  static/skinned negative controls;
  zero revision and missing first-discovery payload;
  stream-count, topology and local-index validation;
  checksum sensitivity to indexes, source material slots and bitangent sign.

Verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Deployed executable:

  SHA-256:
    B578B10E8F52F50A12E6EF712A7C60F81FD4D969FA413B686D1709D7E82582BC
  mtime:
    2026-07-24 16:40:00 Australia/Sydney

Live behavior:

  unchanged. The registry has no lifecycle or renderer call site yet.

STEP 2 RESULT
-------------
  PASS - canonical-keyed, value-owned source payload
  PASS - split position/attribute/index/triangle CPU ABI
  PASS - first/revision copy and zero-copy steady reuse contract
  PASS - rigid eligibility and skinned negative controls
  PASS - resolved material binding kept outside immutable mesh content
  PASS - no retained srfTriangles_t/idDrawVert/index pointer

Next implementation slice:

  wire observation-only RegisteredRenderModel + Rigid first-discovery ingestion
  into the lifecycle shadow registry. Convert idDrawVert values only when a
  source record is absent or explicitly revised, report source reuse/copy/
  reject bytes in the bounded dump, and keep the live rigid route unchanged.

STEP 2 LIFECYCLE SHADOW WIRING CANDIDATE (2026-07-24)
-----------------------------------------------------
Implemented but not yet runtime-accepted:

  PtGeometryLifecycleWorldRegistry owns one CPU source registry;
  map begin and shadow disable clear all owned source values;
  RegisteredRenderModel + Rigid discovery decodes/copies idDrawVert values only
    when the canonical MeshKey has no source record;
  repeated observations call the same-revision no-payload reuse path;
  resolved MD5 snapshots increment a skinned negative-control counter and are
    never copied into the immutable rigid registry;
  lookup is hash-bucketed and still requires full MeshKey equality;
  the GEO06 bounded dump reports records, retained bytes, add/reuse/revise/
    reject/skinned-negative counts, payload-copy count and copied bytes;
  up to eight source samples report revision/checksum/counts/retained bytes and
    first/last local index plus source material slot;
  the established live rigid route is unchanged.

Classifier correction:

  D17 also separates classifier state from MeshKey content. The source
  triangle field is therefore geometryLocalFlags, not the material-derived
  live triangleClassAndFlags value. Source material slot remains immutable;
  resolved material ID, classifier flags and emissive binding belong to the
  later instance/route binding record.

Current revision signal:

  registered render models are treated as immutable within their non-zero
  sourceAssetGeneration. The lifecycle candidate uses that generation as the
  source content revision. A future explicit model-reload generation is still
  required before same-name in-place source hot reload can be claimed; steady
  observation deliberately does not hash every vertex to infer one.

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    78B15C80221C5533114C671589B713C755BB742D3819CF3D08F9473B29CE8350
  mtime:
    2026-07-24 16:45:45 Australia/Sydney

Focused runtime acceptance:

  r_pathTracingGeometryShadowRegistry 1
  mapRestart
  traverse a prop-heavy area and view/spawn several animated zombies
  r_pathTracingGeometryShadowRegistryDump 1
  move/turn for several seconds
  r_pathTracingGeometryShadowRegistryDump 1

First dump requirements:

  GEO06 records/add/copies/copiedBytes > 0;
  reused > 0 when repeated rigid mesh surfaces are present;
  reject = 0;
  skinnedNegative > 0 when MD5 entities were discovered;
  retainedBytes equals stable owned source storage, not per-instance geometry;
  GEO05 collision/stale remain zero;
  no visual or route behavior changes.

Second dump requirements:

  records and retainedBytes stay unchanged;
  add/revise/copies/copiedBytes = 0 during steady observation;
  reject remains zero;
  a quiet interval may legitimately report reuse = 0 because unchanged rigid
    defs do not need to re-enter the lifecycle funnel.

STEP 2 LIFECYCLE SHADOW WIRING RUNTIME PASS (2026-07-24)
--------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo24.log
  bytes:
    28151
  created:
    2026-07-24 16:48:53 Australia/Sydney
  last write at inspection:
    2026-07-24 16:49:50 Australia/Sydney
  executable banner:
    RBDOOM 3 BFG 1.6.0.1403 win-x64 Jul 24 2026 14:28:14

First-discovery dump at frame 1567:

  GEO06 records/retainedBytes = 946/5464756;
  add/reuse/revise/reject = 946/10425/0/0;
  skinnedNegative = 62;
  payload copies/copiedBytes = 946/5464756;
  source hash collisions = 0;
  GEO05 collision/stale = 0/0;
  live instance classes static/rigid/skinned = 1791/1681/62.

This proves one owned payload copy for every admitted rigid source record,
substantial reuse across repeated observations/instances, and explicit
rejection of all 62 currently discovered skinned snapshots from the rigid
source pool. The 946 source records are fewer than 1681 rigid instances as
required by mesh sharing.

Steady dump at frame 1853:

  GEO06 records/retainedBytes = 946/5464756, unchanged;
  add/reuse/revise/reject = 0/4584/0/0;
  skinnedNegative = 0;
  payload copies/copiedBytes = 0/0;
  source hash collisions = 0;
  GEO05 collision/stale = 0/0;
  live instance classes static/rigid/skinned = 1791/1681/62, unchanged.

All eight bounded samples retain identical mesh hash, asset generation,
surface ordinal, source revision, checksum, counts, retained bytes, endpoint
indexes and endpoint source material slots across both dumps. The thousands of
steady rigid observations therefore use the same-revision no-payload path:
they neither re-decode nor hash/copy the source arrays.

The log contains no fatal error, crash, access violation or Vulkan device
removal. Its image warnings are ordinary mars_city1 level-load warnings and
precede the accepted source-registry dumps.

STEP 2 FINAL RESULT
-------------------
  PASS - one value-owned CPU source record per canonical rigid mesh
  PASS - repeated rigid observations reuse existing source values
  PASS - steady source copy and copied bytes are exactly zero
  PASS - skinned frontend snapshots remain explicit negative controls
  PASS - record count, retained bytes and sampled checksums are stable
  PASS - zero source reject/collision and zero GEO05 collision/stale
  PASS - live geometry route remains observation-only

Next implementation slice:

  plan and instantiate separate shadow-only position, attribute, index and
  source-triangle GPU pools using the checked planner. Keep the established
  rigid route unchanged, preserve old storage generations across growth, and
  add upload/retirement diagnostics before attempting non-zero-offset BLAS
  consumption.

STEP 3 SMP OWNERSHIP AUDIT (2026-07-24)
---------------------------------------
The lifecycle source registry is frontend-owned. The NVRHI device, command
list, RtSmokeGeometryUniverse and map-reset waitForIdle boundary are backend-
owned. With com_smp enabled, the backend must not read the live registry,
renderWorld entityDefs, mutable registry vectors or source srfTriangles_t
arrays through a new cross-thread pointer.

The existing entity-feed snapshot proves the usable command-boundary pattern:
frontend values are copied into R_FrameAlloc storage, attached to viewDef_t and
remain valid until that view command finishes on the backend. MAX_FRAME_MEMORY
is 64 MiB, so copying the accepted 5.46 MiB source universe every frame would
fit once but would be an invalid steady-state cost and could multiply across
subviews.

Accepted source transport:

  only the primary, non-subview view carries source deltas;
  the frontend appends never-published Added/Revised records to an immutable
    frame snapshot;
  snapshot metadata contains WorldKey, source revision/checksum, canonical
    MeshKey and checked offsets/counts into four contiguous value arrays;
  position, attribute, index and source-triangle values are copied only for
    the delta, never for same-revision reuse;
  a checked configurable byte budget paginates a large first population over
    multiple frames instead of risking the 64 MiB frame arena;
  the backend imports each canonical key/revision into its own persistent CPU
    source registry and deduplicates repeated delta delivery by full key plus
    revision/checksum;
  backend CPU ownership survives GPU pool growth/reset and is the replay source
    after ResetRayTracingSmokeSceneResources;
  map/world generation mismatch rejects the packet before any allocation or
    upload;
  the frontend may advance its published cursor only after attaching the
    packet to the primary draw command; draw commands are not optional work;
  source revisions require an append-only publication event rather than
    mutating a packet already visible to the backend.

Rejected alternatives:

  direct backend access to PtGeometryLifecycleWorldRegistry
    violates the established SMP snapshot rule and recreates the stale-owner
    crash class;

  retaining lifecycle/source vector pointers in RtSmokeGeometryUniverse
    is invalid because vector growth, map reset and world teardown are frontend
    lifetime events;

  copying all 5.46 MiB into every viewDef every frame
    violates the zero steady-state copy requirement;

  rebuilding identity from the current pointer-heavy RtPathTraceMeshKey
    preserves material/cache/pointer state in identity and does not satisfy
    D17 or the validated canonical MeshKey contract;

  uploading directly from the frame snapshot without a backend CPU owner
    cannot replay after device/resource reset and makes paginated partial
    residency difficult to validate.

Revised step-3 order:

  1. Add pure checked source-delta layout/import planning and boundary tests.
  2. Add primary-view-only immutable frame delta capture with a bounded budget.
  3. Add backend CPU import/dedup diagnostics; do not create GPU buffers yet.
  4. Runtime-prove first-population pagination, zero steady transport bytes,
     exact record/checksum parity and map-restart generation rejection.
  5. Instantiate the four backend shadow GPU pools from the imported records.
  6. Add generation-safe buffer replacement/retirement and reset replay.

This transport prerequisite is required before the task's GPU-pool step; it is
not a new live route or a relaxation of the GEO-06 scope.

STEP 3A CHECKED SOURCE-DELTA TRANSPORT PLANNER (2026-07-24)
-----------------------------------------------------------
Implemented:

  neo/renderer/NVRHI/PathTraceGeometrySourceTransport.h/.cpp
  neo/tests/pathtrace_geometry_source_transport_harness.cpp

This remains pure CPU code. It does not allocate frame memory, attach data to a
view, access the backend, create a GPU resource or alter a route.

The planner consumes an append cursor, immutable source registry and uint64
byte budget. It produces:

  an exact first/next record cursor and complete/deferred state;
  one metadata record per admitted canonical source;
  source revision, checksum and retained-byte contract;
  element-relative offsets into separate position, attribute, index and
    source-triangle arrays;
  exact uint64 element totals and packed bytes;
  accounting for the actual pointer-bearing frame snapshot structure plus
    the frontend R_FrameAlloc 128-byte metadata/value allocation rounding.

If the next complete source record does not fit after at least one admission,
the page closes cleanly and exposes its continuation cursor. If the first
record cannot fit, RecordExceedsBudget returns with no cursor progress. Invalid
cursor, zero budget, invalid source record and arithmetic overflow are distinct
fail-closed results.

The import validator checks:

  full canonical MeshKey validity;
  non-zero source revision/checksum;
  presence and checked bounds of all four streams;
  counts against the canonical key;
  local topology/index validity through the source payload validator;
  exact retained bytes;
  one-time delta checksum equality.

Harness coverage:

  complete multi-record plan;
  bounded partial page and exact continuation;
  no replay of earlier record offsets;
  invalid cursor and zero budget;
  single-record-over-budget failure atomicity;
  empty complete delta;
  intact import;
  checksum, retained-byte, range and missing-stream rejection.

Verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometrySourceTransportHarness
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Live behavior:

  unchanged; there is no transport call site yet.

Next implementation slice:

  add the primary-view-only R_FrameAlloc producer and backend CPU importer
  behind the existing geometry shadow gate. First population must paginate
  under a configurable budget; steady frames must publish zero records/bytes.

STEP 3B LIVE SMP TRANSPORT CANDIDATE (2026-07-24)
-------------------------------------------------
Implemented but not yet runtime-accepted:

  viewDef_t carries one immutable PtGeometrySourceTransportSnapshot pointer;
  PtGeometryLifecycle publishes only from the primary non-subview frontend;
  r_pathTracingGeometrySourceDeltaBudgetMB defaults to 8 and clamps to 1..32;
  checked plans paginate complete source records without splitting a mesh;
  metadata and four value arrays use R_FrameAlloc lifetime and 128-byte
    allocation rounding;
  attaching the primary draw command advances the frontend publication cursor;
  world generation plus publication generation/sequence distinguish map reset,
    disable/re-enable replay and ordered pages;
  RtSmokeGeometryUniverse validates the entire packet before importing any
    record, then deep-copies it into a backend-owned source registry;
  backend full-key/revision/checksum equality remains authoritative;
  backend CPU values survive later GPU pool replacement and are cleared with
    geometry-universe/map resource reset;
  the bounded GEO06 backend dump reports world, publication tuple, cursor,
    records, retained bytes and interval import/reuse/revise/reject/transport
    bytes;
  no NVRHI buffer, BLAS or live route is changed.

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometrySourceTransportHarness
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    D3B8BF522439239B797CA4463762343DCB37C3F3E3EAC2FA858B80A79225F904
  mtime:
    2026-07-24 17:25:01 Australia/Sydney

Focused runtime acceptance:

  r_pathTracingGeometrySourceDeltaBudgetMB 1
  r_pathTracingGeometryShadowRegistry 1
  mapRestart
  wait/traverse until the initial pages have crossed the backend
  r_pathTracingGeometryShadowRegistryDump 1
  wait ten seconds
  r_pathTracingGeometryShadowRegistryDump 1

First dump requirements:

  frontend and backend source record counts/retained bytes match;
  backend cursor equals the frontend source record count;
  publication sequence > 1 proves forced 1 MiB pagination;
  import count equals backend record count;
  reject/revise = 0;
  transport bytes are bounded page totals, not steady-frame traffic;
  GEO05 collision/stale and source reject/collision remain zero.

Second dump requirements:

  frontend/backend records and retained bytes remain unchanged;
  backend interval import/reuse/revise/reject/transportBytes = 0/0/0/0/0;
  no visual change, crash, frame-arena exhaustion or Vulkan device removal.

STEP 3B LIVE SMP TRANSPORT RUNTIME PASS (2026-07-24)
----------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo25.log
  bytes:
    129813
  created/last write:
    2026-07-24 17:30:26 Australia/Sydney

The test forced r_pathTracingGeometrySourceDeltaBudgetMB=1.

Populated dump at frame 1247:

  frontend records/retainedBytes = 946/5464756;
  frontend add/revise/reject/copies = 946/0/0/946;
  frontend source collisions = 0;
  backend world = 3;
  backend publication generation:sequence = 3:6;
  backend cursor/records/retainedBytes = 946/946/5464756;
  backend import/reuse/revise/reject = 946/0/0/0;
  backend transportBytes = 5573504;
  GEO05 collision/stale = 0/0.

Six publication pages prove that the 1 MiB budget paginated the first source
population. Backend count and retained bytes exactly match the frontend owner.
Transport overhead above retained source values is 108748 bytes across the six
frame packets, including snapshot structures, record metadata and 128-byte
allocation rounding.

Steady dump at frame 1468:

  frontend records/retainedBytes = 946/5464756, unchanged;
  frontend add/revise/reject/copies/copiedBytes = 0/0/0/0/0;
  backend publication remains 3:6 and cursor remains 946;
  backend records/retainedBytes = 946/5464756, unchanged;
  backend import/reuse/revise/reject/transportBytes = 0/0/0/0/0;
  GEO05 collision/stale = 0/0.

The log contains no producer rejection, frame-arena exhaustion, fatal error,
crash or Vulkan device removal.

STEP 3B FINAL RESULT
--------------------
  PASS - immutable primary-view frontend/backend packet lifetime
  PASS - forced multi-frame pagination and exact continuation cursor
  PASS - complete backend persistent CPU ownership
  PASS - frontend/backend record and retained-byte parity
  PASS - zero rejected records/pages
  PASS - exactly zero steady transport records and bytes
  PASS - no subview duplication or live route change observed

Next implementation slice:

  instantiate four backend shadow GPU pools from the imported CPU registry.
  Use checked uint64 suballocations, upload only newly resident/revised records,
  retain old storage generations across growth, and report pool capacity/used/
  upload/retirement state without changing BLAS or traversal.

STEP 4 SHADOW GPU POOL CANDIDATE (2026-07-24)
---------------------------------------------
Implemented but not yet runtime-accepted:

  four independent backend NVRHI buffers now own canonical position,
  full-attribute, index and source-triangle streams;
  all byte counts, aligned offsets, growth limits and storage generations use
  the checked GEO-06 pool planner;
  the backend persistent CPU registry remains the replay authority;
  only newly imported or source-revised records allocate and upload ranges;
  a growth allocates one replacement buffer, copies exactly the previous used
  prefix, rebases resident record generations, and retains the old handle for
  three backend frames;
  a transport world/publication change retires all four active buffers before
  starting new generation-1 pool states;
  malformed packets are fully validated before they may replace the current
  publication and pools;
  legacy RtSmokeGeometryUniverse::Clear calls no longer discard canonical
  transport state. Those calls can be legacy cache-policy events rather than
  frontend source-publication resets, so clearing there would strand the
  backend after the append-only frontend cursor had already advanced;
  the existing bounded source dump also emits record, upload, revision,
  rejection, growth-copy, create/grow/retired and cap/used/generation values
  for every pool;
  the buffers are shadow-only: no BLAS geometry, descriptor binding, shader,
  TLAS instance or traversal route consumes them.

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometrySourceTransportHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    1F5226BCF7E4794591C9CA0361042640C1972211926C4D324206C4355A5BEB04
  mtime:
    2026-07-24 17:39:26 Australia/Sydney

Focused runtime acceptance:

  r_pathTracingGeometrySourceDeltaBudgetMB 1
  r_pathTracingGeometryShadowRegistry 1
  mapRestart
  traverse until the initial pages have crossed the backend
  r_pathTracingGeometryShadowRegistryDump 1
  wait ten seconds
  r_pathTracingGeometryShadowRegistryDump 1

First dump requirements:

  backend source records and GPU pool records match;
  interval GPU upload count equals the backend source record count;
  uploadBytes equals backend retainedBytes;
  revise/reject = 0;
  all four cap values are >= used and all used values are non-zero;
  all four generations are non-zero;
  create totals four and forced pagination may produce non-zero grow/copy
  totals;
  no old pool is retained more than three later backend frames;
  raster and path-traced output remain visually unchanged.

Second dump requirements:

  record, capacity, used and generation values remain unchanged;
  interval upload/revise/reject/uploadBytes/copyBytes/create/grow is all zero;
  backend source transport interval remains all zero;
  no crash, severe hitch after initial population, Vulkan validation error or
  device removal.

The first dump should be taken after initial pagination finishes. A dump during
the six known 1 MiB pages is still useful, but its upload count and used bytes
will describe only the prefix imported so far.

STEP 4 SHADOW GPU POOL RUNTIME PASS (2026-07-24)
-------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo26.log
  bytes:
    130373
  created/last write:
    2026-07-24 17:51:25 Australia/Sydney

Populated dump at frame 2817:

  frontend records/retainedBytes = 946/5464756;
  backend cursor/records/retainedBytes = 946/946/5464756;
  GPU records/upload/revise/reject = 946/946/0/0;
  GPU uploadBytes = 5464756, exactly equal to retained source bytes;
  GPU growth copyBytes/create/grow = 4834580/4/10;
  retired buffers = 0 after the three-frame retirement window;
  position cap/used/gen = 1048576/544860/5;
  attribute cap/used/gen = 4194304/3632400/4;
  index cap/used/gen = 1048576/551784/5;
  triangle cap/used/gen = 1048576/735712/4.

The backend import interval is 950 rather than 946 because four source records
crossed before the requested mapRestart. The new publication discarded that
prefix and replayed the complete 946-record registry. This is not duplicate
GPU residency: the GPU interval reset at publication replacement and reports
exactly 946 uploads and 946 resident records.

Steady dump at frame 3112:

  frontend source interval add/revise/reject/copies/bytes = 0/0/0/0/0;
  backend transport interval import/reuse/revise/reject/bytes = 0/0/0/0/0;
  GPU interval upload/revise/reject/uploadBytes/copyBytes/create/grow =
    0/0/0/0/0/0/0;
  all records, capacities, used byte counts and generations are unchanged;
  retired buffers remain 0.

The log contains no fatal error, Vulkan error, device removal, allocation
failure, frame-arena exhaustion or source-producer rejection.

STEP 4 FINAL RESULT
-------------------
  PASS - exact CPU retained-byte to GPU upload-byte parity
  PASS - four independently sized, non-empty pools
  PASS - generation-preserving growth and completed retirement
  PASS - exactly zero immutable steady upload/growth work
  PASS - zero source or GPU record rejection
  PASS - no live render-route or visual-output change reported

STEP 5 NON-ZERO-OFFSET BLAS PROBE CANDIDATE (2026-07-24)
--------------------------------------------------------
Implemented but not yet runtime-accepted:

  after three frames with an unchanged pooled candidate signature, one
  shadow-only Vulkan BLAS is built directly from the shared position and index
  buffers;
  the selected record must have non-zero position, attribute, index and
  triangle byte offsets, at least two primitives, exact stream byte sizes,
  exact element alignment and matching source revision/checksum;
  the BLAS descriptor uses RGB32_FLOAT positions at the record's non-zero
  position byte offset and R32_UINT indexes at its non-zero index byte offset;
  after the BLAS build on the same command stream, a delayed readback copies
  primitive 0 and the last primitive's three indexes, six full source
  attributes and both source-triangle records from their non-zero pooled
  offsets;
  byte-for-byte readback equality is required, including first/last source
  material slots;
  pool buffer/generation or source revision changes retire the probe BLAS for
  three frames and require a new stable candidate/build;
  the probe BLAS is not inserted into a TLAS and cannot affect traversal.

This probe establishes non-zero offset build acceptance plus exact endpoint
storage addressing. It does not yet claim ray-hit parity or settle Q3's BLAS
performance question; those require the later compare/measurement route.

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometrySourceTransportHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    8D358CFAC34AA8695A1FD5BD2ECF3C747E6D55EED1F7864D449CDC068B01EBCB
  mtime:
    2026-07-24 17:57:53 Australia/Sydney

Focused runtime acceptance:

  keep r_pathTracingGeometryShadowRegistry 1;
  mapRestart;
  wait/traverse for initial population plus at least ten seconds;
  r_pathTracingGeometryShadowRegistryDump 1;
  wait ten seconds;
  r_pathTracingGeometryShadowRegistryDump 1.

Both probe dumps must report:

  sources = GPU pool records;
  eligible > 0 and selected != UINT64_MAX;
  every reported stream offset > 0;
  stableFrames >= 3;
  BLAS valid/create/build = 1/1/1;
  readback pending/pass/fail/endpointsValid = 0/1/0/1;
  the second dump retains the same signature, offsets and counts without
  another create/build/readback;
  no Vulkan error, device removal, crash or visible change.

STEP 5 NON-ZERO-OFFSET BLAS PROBE RUNTIME PASS (2026-07-24)
-----------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo27.log
  bytes:
    131084
  created/last write:
    2026-07-24 18:03:34 Australia/Sydney

Populated dump at frame 1139:

  sources/eligible/selected = 946/945/1;
  stableFrames = 449;
  position/attribute/index/triangle offsets = 48/320/24/32;
  selected counts vertices/indexes/primitives = 80/144/48;
  first/last source material slot = 0/0;
  BLAS valid/create/build/retire = 1/2/2/1;
  readback pending/pass/fail/endpointsValid = 0/2/0/1;
  total CPU build-call submission time = 116 microseconds.

The two builds and one retirement are expected lifecycle evidence, not a
steady-state rebuild. As with geometry_geo26, four sources crossed before the
requested mapRestart. The first stable probe built on that publication; the
mapRestart then replaced the pool buffers, retired the old probe BLAS, and
built/read back the final 946-record publication.

Steady dump at frame 1469:

  candidate signature remains 9072394738787902106;
  offsets, counts and material endpoints are unchanged;
  stableFrames advances from 449 to 779;
  BLAS create/build/retire remains 2/2/1;
  readback pass/fail remains 2/0;
  GPU pool upload/growth interval remains entirely zero.

The log contains no fatal error, Vulkan error, device removal, allocation
failure, frame-arena exhaustion, readback map failure or producer rejection.

STEP 5 FINAL RESULT
-------------------
  PASS - Vulkan BLAS consumes non-zero shared position/index offsets
  PASS - primitive 0 and primitive 47 pooled indexes decode exactly
  PASS - six referenced full attributes read back byte-for-byte
  PASS - first/last source-triangle material metadata reads exactly
  PASS - publication replacement retires the old BLAS generation
  PASS - stable publication performs no repeated build or readback

STEP 6 CANONICAL-VERSUS-LEGACY RIGID SOURCE COMPARE CANDIDATE (2026-07-24)
-------------------------------------------------------------------------
Implemented but not yet runtime-accepted:

  every persistent eligible legacy rigid mesh record now retains its resolved
  model-surface ordinal;
  on the bounded GEO06 dump, legacy cached local geometry is converted back to
  the frozen canonical position/full-attribute/index/source-triangle layout;
  matching first requires rigid source domain, surface ordinal, vertex/index
  counts and topology signature, then requires the complete source payload
  checksum;
  representative first/last positions, full attributes, indexes and triangles
  are compared explicitly even after a full checksum match;
  canonical source material slots must equal the source model-surface ordinal;
  resolved material-table IDs and resolved triangle class/flags are counted as
  separate live bindings, not incorrectly compared to immutable source slots;
  exact content present under more than one canonical asset is reported as
  equivalent content rather than merged identity;
  unmatched canonical sources are reported separately because lifecycle
  discovery intentionally covers more content than the currently retained
  legacy rigid route;
  this remains content-only shadow comparison. It does not claim that a legacy
  pointer/cache/material-derived mesh hash has become a canonical MeshKey.

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometrySourceTransportHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    E22A2F9BC4C2AFE81F6EAD2D0BF1C2E765666CF7BF3260AC825FD9DC263C0BB2
  mtime:
    2026-07-24 18:09:25 Australia/Sydney

Focused runtime acceptance:

  keep r_pathTracingGeometryShadowRegistry 1;
  mapRestart;
  traverse the representative rigid-prop area for at least ten seconds;
  r_pathTracingGeometryShadowRegistryDump 1;
  wait ten seconds without changing areas;
  r_pathTracingGeometryShadowRegistryDump 1.

Required comparison invariants:

  valid legacy payloads = legacy rigid records;
  exact payload matches = valid legacy payloads;
  unique + equivalent exact matches = exact payload matches;
  missingShape/payload/endpoint/sourceMaterial failures = 0/0/0/0;
  resolved material and triangle-class bindings each equal legacy records;
  sample flags may be 0 or equivalent-content bit 0x8 only;
  every sample sourceMaterial first/last equals its surface ordinal;
  unmatchedCanonical may be non-zero and is coverage information, not a
  content mismatch;
  the second dump is unchanged except for frame/stability counters.

STEP 6 CANONICAL-VERSUS-LEGACY RIGID SOURCE RUNTIME PASS (2026-07-24)
---------------------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo28.log
  bytes:
    138164
  last write:
    2026-07-24 18:14:36 Australia/Sydney

Populated dump at frame 1073:

  canonical/legacy/valid = 946/89/89;
  exact unique/equivalent = 82/7, total 89;
  missing shape/payload/endpoint/source-material = 0/0/0/0;
  resolved material/class bindings = 89/89;
  unmatched canonical sources = 862.

Steady dump at frame 1298 repeats every content, failure and binding count.
The sampled records have exact full-payload checksums and matching endpoint
data. Every sampled immutable source-material endpoint equals its canonical
model-surface ordinal. No producer rejection, fatal error, Vulkan error,
device removal, allocation failure or readback failure appears in the log.

The seven equivalent-content matches are the important boundary result:
identical bytes can exist under more than one durable source asset. Therefore
the live route must not infer a canonical MeshKey from counts/checksums. It
must receive the exact lifecycle-owned InstanceKey-to-MeshKey association.

STEP 6 FINAL RESULT
-------------------
  PASS - every comparable legacy rigid payload has exact canonical content
  PASS - source-material ordinal and resolved live bindings remain distinct
  PASS - duplicate content is reported without collapsing canonical identity
  PASS - stable dumps produce no repeated uploads, BLAS work or readbacks

STEP 7 EXACT CANONICAL IDENTITY TRANSPORT CANDIDATE (2026-07-24)
----------------------------------------------------------------
Implemented but not yet runtime-accepted:

  the frontend lifecycle registry now journals pointer-free canonical
  InstanceKey-to-MeshKey upserts and removals;
  initial discovery publishes an upsert, a mesh change on the same live
  InstanceKey publishes a revision, and removal publishes a tombstone;
  event sequence and full-key hashes are validated before backend mutation;
  bounded primary-view packets use the frame arena and exact continuation
  cursors, so the backend does not read frontend registry memory under SMP;
  a new world/publication must start at cursor zero; stale world/publication,
  out-of-order packet and corrupt-hash input fail closed;
  backend lookup uses the full canonical key after hash bucket selection, so
  hash collisions do not merge identities;
  the backend registry reports active static/rigid/skinned binding counts;
  a published journal compacts only after 16384 events, replacing itself with
  one upsert per live instance under a new publication generation;
  this remains shadow-only. Legacy routing and liveness ownership are
  unchanged.

CPU verification:

  PathTraceGeometryIdentityTransportHarness
    PASS - complete and paged publication
    PASS - exact continuation cursor
    PASS - live mesh-binding revision
    PASS - removal tombstone
    PASS - corrupt hash rejection without partial publication
    PASS - new publication cursor-zero requirement

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  PathTraceGeometryIdentityTransportHarness
    PASS
  PathTraceGeometrySourceTransportHarness
    PASS
  PathTraceGeometrySourceRegistryHarness
    PASS
  PathTraceGeometryPoolHarness
    PASS
  PathTraceCanonicalIdentityHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    E61498DEC1581F7875E1D9CD3CB63E07B6A138DA60473B48DC691DE7C02F3CF8
  mtime:
    2026-07-24 18:25:52 Australia/Sydney

Focused runtime acceptance:

  keep r_pathTracingGeometryShadowRegistry 1;
  mapRestart;
  traverse the representative rigid-prop area for at least ten seconds;
  r_pathTracingGeometryShadowRegistryDump 1;
  wait ten seconds without changing areas;
  r_pathTracingGeometryShadowRegistryDump 1.

Both identity transport dumps must report:

  non-zero world/publication/sequence/cursor;
  active bindings > 0 and active rigid bindings > 0;
  active = static + rigid + skinned;
  binding slots >= active bindings;
  first populated interval has upserts > 0 and reject = 0;
  second steady interval has upsert/reuse/revise/remove/reject/bytes all zero;
  world/publication/cursor and all binding counts remain unchanged;
  no producer rejection, fatal error, Vulkan error, device removal, crash or
  visible change.

STEP 8 LIVE RIGID-ROUTE IDENTITY COVERAGE RUNTIME PASS (2026-07-24)
-------------------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo30.log
  bytes:
    145270
  last write:
    2026-07-24 20:41:00 Australia/Sydney

Populated frame 1503 and steady frame 1760 both report:

  route/validKey/identity/source/legacy/exact =
    111/111/111/111/111/111;
  invalidKey/missingIdentity/invalidBinding/missingSource/missingLegacy/
    payload = 0/0/0/0/0/0.

All eight samples have flags 0x0, non-zero distinct canonical instance and
mesh hashes, and exact legacy/canonical payload checksums. Source transport,
identity transport, GPU pools and the non-zero-offset BLAS probe are steady
with zero rejection or repeated upload/build work. The log contains no fatal
error, Vulkan error, device removal, allocation failure or readback failure.

STEP 8 FINAL RESULT
-------------------
  PASS - every current rigid route instance constructs a valid canonical key
  PASS - every key selects its exact lifecycle-owned canonical MeshKey
  PASS - every selected MeshKey has immutable CPU and pooled GPU source data
  PASS - exact selected source bytes match the corresponding legacy route

STEP 9 DEFAULT-OFF CANONICAL POOLED RIGID BLAS CANDIDATE (2026-07-24)
---------------------------------------------------------------------
Implemented but not yet runtime-accepted:

  r_pathTracingGeometryCanonicalRigidBlas defaults to 0 and never changes
  traversal selection;
  when enabled, the current visible/resident rigid route resolves exact
  canonical MeshKeys through the accepted identity registry;
  one BLAS is shared by each full canonical MeshKey, independent of resolved
  material binding;
  BLAS geometry points directly at the shared position/index pool with the
  source record's non-zero byte offsets and float3/R32 formats;
  builds are bounded by
    r_pathTracingGeometryCanonicalRigidBlasBuildsPerFrame
  which defaults to 16;
  unchanged built records are reused without uploads or rebuilds;
  source publication replacement and input-signature change retire the old
  handle for three frames before rebuilding;
  disable releases the scaffold through the same deferred retirement path;
  legacy BLAS, TLAS, route buffers and traversal remain authoritative.

Build verification:

  cmake --build --preset win64-pt-dev-release --target RBDoom3BFG
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    F44CF8D0747EC966259349CC85EB9ABE3C2EDAA0D40DAF72746C36F0AA879BB5
  mtime:
    2026-07-24 20:46:00 Australia/Sydney

Focused runtime acceptance:

  r_pathTracingGeometryShadowRegistry 1;
  r_pathTracingGeometryCanonicalRigidBlas 1;
  mapRestart;
  traverse the representative rigid-prop area for at least ten seconds;
  r_pathTracingGeometryShadowRegistryDump 1;
  wait ten seconds without changing areas;
  r_pathTracingGeometryShadowRegistryDump 1.

The populated BLAS dump must report:

  enabled = 1;
  requested instances > 0 and requested unique meshes > 0;
  resolved identity = requested instances;
  resolved source = resolved pool = requested unique meshes;
  ready = requested unique meshes;
  active >= ready;
  deferred = 0 after the build budget drains;
  missing identity/source/pool = 0/0/0;
  create = build > 0 and retire = 0 after map population settles;
  traversal=legacy and route=shadow-only.

The steady dump must retain the same requested, ready and active counts with:

  create/build/retire = 0/0/0;
  reuse > 0 is expected and proves the handles were found unchanged;
  no producer rejection, fatal error, Vulkan error, device removal, crash or
  visible change.

STEP 7 EXACT CANONICAL IDENTITY TRANSPORT RUNTIME PASS (2026-07-24)
-------------------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo29.log
  bytes:
    139254
  last write:
    2026-07-24 18:37:56 Australia/Sydney

Populated dump at frame 2339:

  world/publication/sequence/cursor = 3/3/15/3638;
  active static/rigid/skinned/total = 1791/1679/140/3610;
  binding slots = 3624;
  interval upsert/reuse/revise/remove/reject/bytes =
    3624/0/0/14/0/439424.

The accounting closes exactly: 3624 upserts minus 14 removals equals 3610
active bindings, and static + rigid + skinned also equals 3610.

Steady dump at frame 2588:

  world/publication/sequence/cursor remains 3/3/15/3638;
  every binding count remains unchanged;
  upsert/reuse/revise/remove/reject/bytes = 0/0/0/0/0/0.

The wider rigid source comparison also remained exact at 139/139 payloads,
with 126 unique and 13 equivalent-content matches and no content, endpoint or
source-material failures. The log contains no producer rejection, fatal
error, Vulkan error, device removal, allocation failure or readback failure.

STEP 7 FINAL RESULT
-------------------
  PASS - lifecycle identity crosses the SMP boundary without pointer reads
  PASS - add/remove accounting and per-class active counts close exactly
  PASS - steady state publishes no packets and performs no backend work
  PASS - duplicate source content remains separate canonical identity

STEP 8 LIVE RIGID-ROUTE IDENTITY COVERAGE CANDIDATE (2026-07-24)
----------------------------------------------------------------
Implemented but not yet runtime-accepted:

  after the current frame's visible/offscreen rigid instance list and
  residency selection are complete, every live route instance constructs its
  canonical InstanceKey from world generation, renderDef slot/generation and
  model-surface ordinal;
  the key must resolve the exact lifecycle-transported MeshKey;
  that MeshKey must resolve an imported immutable source record;
  the same route instance's legacy mesh must still exist;
  the exact selected canonical source, rather than any equivalent-content
  candidate, must match the legacy topology, counts, complete payload
  checksum and source-material endpoints;
  failures are separated into invalid instance key, missing identity,
  invalid binding, missing source, missing legacy mesh and payload mismatch;
  this remains shadow-only and does not alter BLAS/TLAS selection.

Build verification:

  cmake --build --preset win64-pt-dev-release --target RBDoom3BFG
    PASS
  PathTraceGeometryIdentityTransportHarness
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    E2A082897810C41927E3627A04EF84B604ECA08C1F99E33E1B38BDF6AFDF7425
  mtime:
    2026-07-24 18:43:51 Australia/Sydney

Focused runtime acceptance:

  keep r_pathTracingGeometryShadowRegistry 1;
  mapRestart;
  traverse the representative rigid-prop area for at least ten seconds;
  r_pathTracingGeometryShadowRegistryDump 1;
  wait ten seconds without changing areas;
  r_pathTracingGeometryShadowRegistryDump 1.

Both coverage dumps must report:

  route > 0;
  route = validKey = identity = source = legacy = exact;
  invalidKey/missingIdentity/invalidBinding/missingSource/missingLegacy/
    payload = 0/0/0/0/0/0;
  every sample flags = 0;
  every sample legacy and canonical checksum is equal and non-zero;
  the steady dump retains identical coverage counts;
  no producer rejection, fatal error, Vulkan error, device removal, crash or
  visible change.

STEP 9 CANONICAL POOLED RIGID BLAS RUNTIME PASS (2026-07-24)
------------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo31.log

Populated frame 1529 reports:

  requested instances/unique meshes = 188/125;
  resolved identity/source/pool = 188/125/125;
  ready/active/deferred = 125/180/0;
  missing identity/source/pool = 0/0/0;
  interval create/build/reuse/retire/buildUs =
    180/180/56154/0/4541.

The 180 active records include canonical meshes encountered earlier during
traversal and retained by the cache. The current request contains 125 unique
meshes, and all 125 are ready; active is therefore expected to be greater than
the current request rather than equal to it.

Steady frame 1758 retains the same 188/125 request and 125 ready records:

  interval create/build/reuse/retire/buildUs =
    0/0/28573/0/0.

The corresponding exact route coverage is unchanged in both dumps:

  route/validKey/identity/source/legacy/exact =
    188/188/188/188/188/188;
  invalidKey/missingIdentity/invalidBinding/missingSource/missingLegacy/
    payload = 0/0/0/0/0/0.

The log contains no fatal error, Vulkan error, device removal, allocation
failure or readback failure.

STEP 9 FINAL RESULT
-------------------
  PASS - every requested canonical mesh resolves pooled GPU source storage
  PASS - every currently requested unique mesh has one ready canonical BLAS
  PASS - steady state performs no create, build, retirement or source upload
  PASS - retained records do not make current request accounting ambiguous
  PASS - traversal remains legacy and the canonical route remains shadow-only

STEP 10 CANONICAL RIGID TLAS DESCRIPTOR SHADOW COMPARE (2026-07-24)
-------------------------------------------------------------------
Implemented but not yet runtime-accepted:

  each accepted rigid TLAS plan entry transports a backend-local dense
  canonical BLAS record index plus the canonical mesh hash;
  the record index is produced only after a full canonical MeshKey lookup, so
  mesh-hash collisions cannot select the record;
  input tokens and emitted-plan signatures include this route metadata;
  accepted current and valid previous transforms are now included in the
  rigid-plan input token, closing the pre-existing async cache-key omission;
  valid previous BVH dirty-token state is now included in the frame-planning
  input token, closing the corresponding pre-existing async cache-key omission;
  a parallel descriptor list uses the canonical BLAS while preserving the
  legacy instance ID, trace mask, hit-group contribution, transform, cached
  source filtering and cull flags;
  descriptor construction rejects a missing record index, mesh-hash mismatch,
  missing BLAS or unsubmitted build with separate counters;
  the canonical descriptors are discarded after comparison. They are not
  submitted to the TLAS, and traversal remains legacy.

CPU verification:

  PathTraceCpuPlanningHarness
    PASS
    including transform, canonical record/hash, previous-dirty-token,
    immutable snapshot and deterministic signature coverage.

Build verification:

  cmake --build --preset win64-pt-dev-release
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    F92FD16E4A76253AE8DB56E2BF4E9D4422A619376ABB083340851FA7A0011FB7
  mtime:
    2026-07-24 20:58:28 Australia/Sydney

Focused runtime acceptance:

  keep r_pathTracingGeometryShadowRegistry 1;
  keep r_pathTracingGeometryCanonicalRigidBlas 1;
  mapRestart;
  traverse the representative rigid-prop area for at least ten seconds;
  echo === GEO06_CANONICAL_RIGID_TLAS_POPULATED ===;
  r_pathTracingGeometryShadowRegistryDump 1;
  wait ten seconds without changing areas;
  echo === GEO06_CANONICAL_RIGID_TLAS_STEADY ===;
  r_pathTracingGeometryShadowRegistryDump 1;
  condump geometry_geo32.log.

Both canonical TLAS descriptor dumps must report:

  enabled = 1 and planned > 0;
  descriptors legacy = canonical;
  exactRecordMappings = legacy descriptors = canonical descriptors;
  missingIndex/hashMismatch/missingBlas = 0/0/0;
  submit=shadow-only and traversal=legacy.

The canonical rigid BLAS dump must still report:

  requested instances > 0 and requested unique meshes > 0;
  resolved identity = requested instances;
  resolved source = resolved pool = ready = requested unique meshes;
  deferred = 0 and missing identity/source/pool = 0/0/0;
  the steady interval has create/build/retire = 0/0/0.

There must be no producer rejection, fatal error, Vulkan error, device
removal, crash or visible change.

STEP 10 CANONICAL RIGID TLAS DESCRIPTOR RUNTIME PASS (2026-07-24)
-----------------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo32.log

Populated frame 1112 reports:

  planned instances = 111;
  legacy/canonical descriptors = 111/111;
  exact record mappings = 111;
  missing index/hash mismatch/missing BLAS = 0/0/0.

The corresponding canonical BLAS state reports:

  requested instances/unique meshes = 111/83;
  resolved identity/source/pool = 111/83/83;
  ready/active/deferred = 83/142/0;
  missing identity/source/pool = 0/0/0.

Steady frame 1315 preserves the same counts and reports:

  legacy/canonical descriptors = 111/111;
  exact record mappings = 111;
  missing index/hash mismatch/missing BLAS = 0/0/0;
  canonical BLAS create/build/retire = 0/0/0.

Both frames preserve exact canonical source coverage at
111/111/111/111/111/111 with all six failure counters zero. The log contains
no fatal error, Vulkan error, device removal, allocation failure or readback
failure.

STEP 10 FINAL RESULT
--------------------
  PASS - every submitted legacy rigid descriptor has one canonical counterpart
  PASS - every counterpart selects the full-key-derived canonical BLAS record
  PASS - descriptor counts remain exact after the BLAS cache reaches steady
  PASS - no incomplete canonical descriptor list reaches TLAS submission
  PASS - the accepted capture remains shadow-only with legacy traversal

STEP 11 DEFAULT-OFF CANONICAL RIGID TRAVERSAL CANDIDATE (2026-07-24)
--------------------------------------------------------------------
Implemented but not yet runtime-accepted:

  r_pathTracingGeometryCanonicalRigidTraversal defaults to 0;
  r_pathTracingGeometryCanonicalRigidBlas must also be enabled and provide a
  complete descriptor set;
  canonical selection requires legacy descriptor count = canonical descriptor
  count = exact full-key record mappings, with every failure counter zero;
  the selection decision is covered by the CPU planning harness;
  any disabled provider, deferred/missing BLAS, bad record index, hash
  mismatch or descriptor-count mismatch fails closed to the complete legacy
  descriptor list;
  when selected, only the rigid descriptors supplied to the existing TLAS
  change. Static/dynamic instances, TLAS ownership, instance IDs, masks,
  transforms, route records, material records and shaders remain unchanged;
  disabling the traversal gate restores legacy rigid BLAS descriptors without
  rebuilding shader or material state.

CPU verification:

  PathTraceCpuPlanningHarness
    PASS
    including default shadow-only, exact-parity selection, missing-BLAS
    fail-closed and descriptor-mismatch fail-closed cases.

Build verification:

  cmake --build --preset win64-pt-dev-release --target
    PathTraceCpuPlanningHarness RBDoom3BFG
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    B22D43E2264491CD0FE826EA8D86A752747E2AEE7634E0D4A1F5612ED5846BDC
  mtime:
    2026-07-24 21:05:26 Australia/Sydney

Focused runtime acceptance:

  r_pathTracingGeometryShadowRegistry 1;
  r_pathTracingGeometryCanonicalRigidBlas 1;
  r_pathTracingGeometryCanonicalRigidTraversal 0;
  mapRestart;
  traverse the representative rigid-prop area for at least ten seconds;
  echo === GEO06_RIGID_TRAVERSAL_LEGACY ===;
  r_pathTracingGeometryShadowRegistryDump 1;
  r_pathTracingGeometryCanonicalRigidTraversal 1;
  remain in the same area, move the camera and inspect repeated props for at
    least ten seconds;
  echo === GEO06_RIGID_TRAVERSAL_CANONICAL ===;
  r_pathTracingGeometryShadowRegistryDump 1;
  r_pathTracingGeometryCanonicalRigidTraversal 0;
  wait at least five seconds;
  echo === GEO06_RIGID_TRAVERSAL_ROLLBACK ===;
  r_pathTracingGeometryShadowRegistryDump 1;
  condump geometry_geo33.log.

Expected legacy and rollback dumps:

  descriptors legacy = canonical;
  exact record mappings = both descriptor counts;
  missingIndex/hashMismatch/missingBlas = 0/0/0;
  submit(requested/eligible/selected) = 0/1/0;
  traversal = legacy.

Expected canonical dump:

  descriptors legacy = canonical;
  exact record mappings = both descriptor counts;
  missingIndex/hashMismatch/missingBlas = 0/0/0;
  submit(requested/eligible/selected) = 1/1/1;
  traversal = canonical.

There must be no visible geometry, material, lighting, reflection, shadow or
motion change at either toggle; no fail-closed message while the canonical
dump is taken; and no producer rejection, fatal error, Vulkan error, device
removal or crash.

STEP 11 CANONICAL RIGID TRAVERSAL NUMERIC RUNTIME PASS (2026-07-24)
-------------------------------------------------------------------
Capture:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo33.log

Legacy frame 1065:

  planned = 111;
  legacy/canonical descriptors = 111/111;
  exact mappings = 111;
  failures = 0/0/0;
  submit requested/eligible/selected = 0/1/0;
  traversal = legacy.

Canonical frame 2539:

  planned = 121;
  legacy/canonical descriptors = 121/121;
  exact mappings = 121;
  failures = 0/0/0;
  submit requested/eligible/selected = 1/1/1;
  traversal = canonical.

Rollback frame 2860:

  planned = 121;
  legacy/canonical descriptors = 121/121;
  exact mappings = 121;
  failures = 0/0/0;
  submit requested/eligible/selected = 0/1/0;
  traversal = legacy.

The canonical selection therefore reached the live TLAS and the rollback
restored legacy selection. Canonical source coverage remained exact in every
capture, and the steady rollback interval performed no BLAS create, build or
retirement. No fail-closed message, fatal error, Vulkan error, device removal,
allocation failure or readback failure appears in the log. No visible
regression was reported with the returned capture.

STEP 11 FINAL RESULT
--------------------
  PASS - the default-off gate selects canonical descriptors only at exact parity
  PASS - the selected descriptors reach the live TLAS
  PASS - disabling the gate restores the legacy descriptors
  PASS - canonical and rollback captures retain exact identity/source coverage
  PASS - no partial canonical route or fail-closed transition was observed

STEP 12 CENTER-PIXEL PRIMARY-HIT TUPLE READBACK (2026-07-24)
-------------------------------------------------------------
Implemented but not yet runtime-accepted:

  r_pathTracingGeometryCanonicalRigidHitDump is a one-shot command;
  it reuses the existing bounded primary-surface readback path and copies one
  176-byte center-pixel record after the live ray dispatch;
  the record is labeled from the actual descriptor selection decision, not
  merely from the requested traversal cvar;
  it reports whether the hit instance is inside the current rigid TLAS range;
  it reports instance, primitive, triangle flags, material ID/index/flags,
  surface class, emissive texture/radiance, current position, previous
  position/motion selector, valid flags and debug status;
  no shader ABI or SPIR-V changed;
  the command refuses to overwrite an in-flight readback and asks for a retry.

Build verification:

  cmake --build --preset win64-pt-dev-release --target RBDoom3BFG
    PASS
  all SPIR-V shaders
    up to date; no shader source or blob changed

Runtime-candidate executable:

  SHA-256:
    4BD79EF889D48588481C28DC98DC9DDEA7AD6E9F95D2A75C9B5742CDFA851F50
  mtime:
    2026-07-24 21:14:43 Australia/Sydney

Focused runtime acceptance:

  enable the shadow registry and canonical BLAS provider;
  select a broad, opaque rigid .lwo prop and hold the crosshair well inside
    one surface, away from an edge;
  set canonical traversal to 0 and issue the hit dump;
  wait at least three rendered frames for the legacy readback line;
  without moving the camera, set canonical traversal to 1;
  issue the hit dump again and wait for the canonical readback line;
  restore canonical traversal to 0 and condump geometry_geo34.log.

Both lines must report:

  validFlags has RT_PRIMARY_SURFACE_VALID;
  status = 0;
  rigid = 1;
  identical dimensions and center pixel;
  identical instance, primitive, triangle flags, material ID/index/flags,
    surface class and emissive texture/radiance;
  identical object-motion validity and previous-position selector.

Current and previous XYZ should be identical for a stationary object, except
for a possible sub-pixel primary-ray difference when projection jitter is
active. Any position difference must remain on the same primitive and must not
alter the discrete tuple. A non-rigid center hit is an invalid sample and must
be repeated while aiming at a rigid prop.
