GEO-05 - per-world lifecycle owner audit and first runtime gate
================================================================
Status: complete; lifecycle owner, SMP snapshot restoration and callback-MD5
source discovery/removal validated.
Date: 2026-07-23, updated 2026-07-24.

HOOK AUDIT
----------
The existing Add/Update/Free hooks were real but incomplete:

1. AddEntityDef called UpdateEntityDef and notified Add afterward.
   Direct UpdateEntityDef creation of an unused handle had no Add notification.
   GEO-05 makes UpdateEntityDef notify Add for a newly allocated def and removes
   the wrapper's duplicate responsibility.

2. The exact-memcmp early return performed no lifecycle observation.
   GEO-05 reports it as unchanged.

3. The callback fast-update early return changed render parms but performed no
   lifecycle observation.
   GEO-05 reports it as Update after copying the new parms.

4. AddWorldModelEntities allocates authored static area entities directly and
   deliberately bypasses AddEntityDef.
   GEO-05 adds an explicit Add notification after derivation and area linking.

5. FreeWorld calls FreeDefs, which calls FreeEntityDef for every owned entity
   before inline models and world storage are deleted.
   This is the safe exact-removal point. A new world generation is allocated
   only after those Free notifications complete.

6. InitFromMap has three reset shapes:

     empty world        FreeWorld -> new generation -> ClearWorld
     retained same map FreeDefs  -> new generation -> re-add area entities
     full reload/change FreeWorld -> new generation -> load/re-add entities

   mapLoadSerial increments before all three. The shadow owner allocates a new
   monotonic WorldKey and primary HistoryOwnerKey at the marked boundary.

PER-WORLD OWNER
---------------
idRenderWorldLocal now owns one opaque
PtGeometryLifecycleWorldRegistry pointer. Construction allocates it; destruction
first frees world defs, then destroys it. The old process-global
unordered_map<renderWorld pointer, state> is gone.

The only process-global identity state is three monotonic atomic allocators:

  world generation;
  logical history-owner generation;
  shadow asset ID.

They contain no live-object pointers or records.

PtRenderDefKey remains transitional because live legacy consumers still carry
the raw world pointer. It now also carries worldGeneration, and liveness rejects
a key whose generation does not match the world's current owner. Canonical
InstanceKey uses only the numeric world generation, slot generation and surface
ordinal.

SHADOW RECORDS
--------------
Gate:

  r_pathTracingGeometryShadowRegistry 0

Default is off. When enabled, the owner bootstraps current entityDefs once and
then follows Add/Update/Unchanged/Free. It never drives traversal.

Each registry-eligible model surface produces:

  canonical InstanceKey;
  canonical MeshKey;
  copied transform;
  material ID/name;
  model name;
  vertex/index/triangle counts;
  lifecycle class and last-modified frame.

No shadow mesh or instance record stores:

  idRenderEntityLocal pointer;
  idRenderModel pointer;
  srfTriangles_t pointer;
  drawSurf pointer;
  frontend/cache address;
  NVRHI resource or AS handle.

The shadow asset namespace is world-owner-local for GEO-05. Full model-name +
source-domain equality allocates a monotonic numeric asset ID; the name hash is
not identity. Repeated instances of the same model in one world share MeshKeys.
GEO-06 must move registered-model asset generation to the pooled mesh owner
before cross-world sharing or live storage.

Topology signature is derived from immutable vertex/index counts and the full
local index sequence. Material, transform and output/cache addresses are
excluded.

Mesh records remain after liveReferences reaches zero. The transition is
counted as deferredMeshEviction; GEO-05 has no implicit frame-age eviction.

DUMP CONTRACT
-------------
Trigger:

  r_pathTracingGeometryShadowRegistryDump 1

The next PT frame prints one summary and up to 16 bounded samples, prioritizing
non-static instances. Summary fields:

  worldGeneration / previousWorldGeneration / mapLoadSerial;
  primaryHistoryOwner;
  reset count and records discarded at the last reset;
  interval add/update/remove/unchanged;
  interval collision/stale/missing/transient/deferredMesh;
  live instance/referenced-mesh/zero-ref-mesh/asset counts;
  shared mesh count and maximum references to one mesh;
  static/rigid/skinned instance counts;
  live vertex/index/triangle totals;
  interval removed vertex/index/triangle totals.

Interval counters reset after each dump. Live record totals do not reset. This
makes the medkit removal delta machine-checkable without contaminating it with
earlier map-load events.

FIRST RUNTIME CAPTURE
---------------------
Use a new log, geometry_geo19.log. Enable the shadow registry before restarting
or loading the test map:

  r_pathTracingGeometryShadowRegistry 1
  mapRestart

In game/mc_underground area 25, settle, then:

  spawn func_static model models/items/medkit/medkit_large.lwo name geom_lifecycle_probe
  echo === GEO05_VISIBLE ===
  r_pathTracingGeometryShadowRegistryDump 1
  r_pathTracingGeometryLifecycleDump 1

Move to another portal area and return:

  echo === GEO05_RETURNED ===
  r_pathTracingGeometryShadowRegistryDump 1

Open and close the PDA:

  echo === GEO05_PDA_RETURN ===
  r_pathTracingGeometryShadowRegistryDump 1

Immediately remove the probe:

  remove geom_lifecycle_probe
  echo === GEO05_REMOVED ===
  r_pathTracingGeometryShadowRegistryDump 1
  r_pathTracingGeometryLifecycleDump 1

Required:

  visible/returned/PDA live record and geometry totals are identical;
  worldGeneration and primaryHistoryOwner are identical across those labels;
  collision=0 and stale=0;
  removal interval:
      remove=2
      removedGeometryInterval=120/552/184
  live totals after removal:
      exactly 2 fewer instances
      exactly 120/552/184 fewer vertices/indexes/triangles
  two mesh records may remain with zero references; that is the required
  deferred-eviction behavior, not a live-instance leak.

Then run:

  echo === GEO05_MAP_RESTART_BEFORE ===
  r_pathTracingGeometryShadowRegistryDump 1
  mapRestart
  echo === GEO05_MAP_RESTART_AFTER ===
  r_pathTracingGeometryShadowRegistryDump 1

Required after restart:

  worldGeneration changed;
  previousWorldGeneration equals the pre-restart generation;
  primaryHistoryOwner changed;
  resetDiscarded liveInstances=0;
  collision=0 and stale=0.

The crowded MD5 comparison and visible/area/lifecycle tuple comparison are the
next GEO-05 slice after this owner/lifetime gate passes.

RUNTIME CRASH CORRECTION (2026-07-24)
-------------------------------------
The first deployed GEO-05 observer produced two repeatable CPU access
violations after roughly one minute of play:

  0xc0000005 at executable RVA 0x5f3cbb
  0xc0000005 at executable RVA 0x5fcc23

Windows Application Error/WER identified RBDoom3BFG.exe itself as the faulting
module; there was no Vulkan device-removed event. Disassembly of the exact
deployed executable localized the first RVA to a virtual model-surface-count
call in the new shadow observation path. The second RVA dereferenced the raw
idRenderWorldLocal pointer while resolving a PtRenderDefKey to its per-world
registry.

Both accesses violated existing renderer lifetime rules:

  * unchanged and callback-fast entity updates deliberately return without
    dereferencing hModel, so a passive observer cannot add model traversal to
    those paths;
  * retained PtRenderDefKey world pointers are opaque identity values and may
    outlive the idRenderWorldLocal object, so lookup cannot dereference them.

Correction:

  * the shadow registry no longer bootstraps by traversing all existing
    entityDefs when enabled;
  * unchanged updates only refresh lifecycle liveness and do not classify or
    traverse model surfaces;
  * callback-fast updates explicitly mark their source as unstable and do not
    classify or traverse model surfaces;
  * callback/generated sources are rejected before any model virtual call;
  * free diagnostics use the class stored during the last stable add/update;
  * a mutex-protected non-owning live-world directory validates opaque world
    pointer values before returning a registry. All slot, mesh, instance,
    generation and history state remains owned by the per-world registry.

Operational consequence: enable the shadow registry before mapRestart or a map
load. Mid-map enable starts observing only subsequent stable add/update events;
it intentionally does not reconstruct a complete snapshot from potentially
unsafe retained pointers.

MAP-RELOAD AREA-REFERENCE CRASH (2026-07-24)
--------------------------------------------
geometry_geo19.log reached the first PT frame after same-map reload, printed the
map serial transition 1 -> 2 and then terminated before any requested GEO-05
dump. Windows recorded a new 0xc0000005 at executable RVA 0x5f3c9a for deployed
executable SHA-256:

  2AAF930B03C7093FC3FADF2D405A154389466B3CBF85AE23A71E490766BD66AE

Initial disassembly-only comparison placed the fault in
RefreshRigidResidencyAreaWalk. That attribution is superseded by the exact
Release map evidence recorded below; both the legacy area walk and entity feed
have structurally similar portal-reference/model loops.

This is separate from the two lifecycle-observer faults above. The immediate
correction builds a pointer-only live membership set from renderWorld->entityDefs
before walking selected portal areas. An area reference must be present in that
set and must round-trip through world/index/entityDefs before any entity field
is read. Stale references are rejected and counted as rejected area-walk
entities.

This is a crash guard, not the final ownership architecture. The late portal
area walk still consumes mutable engine structures and must be replaced by the
engine-owned snapshot boundary in the later modernization steps.

V2 DIAGNOSTIC AREA-WALK REMOVAL (2026-07-24)
--------------------------------------------
The guarded build completed the requested V2 residency dump, survived same-map
reload, spawned the medkit probe and emitted both GEO-05 shadow and lifecycle
dumps. During later ordinary traversal it crashed with 0xc0000005 at executable
RVA 0x5f3dcb.

The membership guard had rejected stale entity pointers, but disassembly still
showed the new fault at model->NumSurfaces(). The initial attribution to
RefreshRigidResidencyAreaWalk is superseded below by an exact Release map.

For residency V2, r_pathTracingRigidResidencyDump no longer activates the
legacy portal-area walk. The dump now reports the V2 entity-feed/residency
snapshot already used by the live route. The legacy walk remains reachable
only when r_pathTracingGeometryResidencyV2 is explicitly disabled; replacing
that legacy ownership path is later modernization work.

EXACT SYMBOL CORRECTION AND ENTITY-FEED GUARD (2026-07-24)
----------------------------------------------------------
A current Release map was generated from the exact source/object set after the
same RVA 0x5f3dcb recurred with V2 diagnostics confined to snapshots. The map
places the fault inside:

  CaptureEntityFeedRigidSurfaces
  function start RVA 0x5f31f0
  fault delta +0xbdb

The faulting instruction is the hModel virtual call for NumSurfaces. This
explains the reported spatial correlation with animated objects: the V2 feed
walked reachable area references, read entity->parms.hModel, and enumerated
surfaces before rejecting callback/skinned/generated entities which cannot be
admitted by this rigid-only feed.

Correction:

  * area-reference entity pointers must belong to the current entityDefs set
    and round-trip through world/index/entityDefs before any entity field read;
  * callback, force-update, jointed, depth-hack and generated/dynamic-model
    entities are rejected before any hModel virtual call;
  * the resident store now keys same-object map reloads by mapLoadSerial;
  * resident entity slots now compare worldGeneration as well as raw world,
    index and render-def generation.

The mapLoadSerial/worldGeneration omissions were independent correctness bugs:
a same-map reload could otherwise retain surface/material/model facts from the
previous world generation even when the world pointer and slot generation were
reused.

SECOND EXACT SYMBOL CORRECTION AND SMP CONTAINMENT (2026-07-24)
---------------------------------------------------------------
The first guarded build did not remove the crash. The replacement executable
was confirmed live: its PE timestamp changed from 0x6a62dd49 to 0x6a62e01a and
the fault RVA moved from 0x005f3dcb to 0x005f4be2.

The matching Release map places CaptureEntityFeedRigidSurfaces at RVA
0x005f31f0. Exact disassembly of the replacement executable resolves
0x005f4be2 to:

  mov rcx, qword ptr [rcx]

This is the increment of the portal-area linked-list walk:

  ref = ref->areaNext

The crashing value can therefore be a node that the frontend unlinked and
returned to areaReferenceAllocator while the backend was processing the
previous frame. The animated-object correlation is causal evidence: ordinary
animated entity updates rebuild their area references. RenderCommon.h states
the governing engine contract directly:

  * viewEntity_t is a frame-temporary backend copy because the frontend may
    modify idRenderEntityLocal simultaneously in SMP mode;
  * the backend must not reference viewEntity_t::entityDef.

The entity feed violated the stronger form of that contract by walking
renderWorld->portalAreas[].entityRefs and dereferencing entityDefs from the
backend. Pointer-membership checks cannot repair a concurrently mutating list;
the node can be freed after any check.

Containment:

  * with the default com_smp=1, ProduceEntityFeedRigidEntities now fails closed
    before reading renderWorld, portal entityRefs, or entityDefs;
  * the existing frame-owned draw-surface mirror remains the visible-geometry
    producer;
  * com_smp=0 retains the old feed for single-thread diagnosis only.

Required architectural follow-up:

  Build an immutable, frontend-owned entity-feed frame snapshot and attach it
  to the view command. It must contain value-copied identity, transform,
  classification, material, area-membership, and stable rigid mesh references.
  The backend must consume that snapshot without dereferencing
  idRenderEntityLocal, areaReference_t, or mutable renderWorld entity arrays.
  The off-screen rigid feed must not be re-enabled under SMP until this
  producer/consumer handoff is validated.

SMP CONTAINMENT RUNTIME VALIDATION (2026-07-24)
-----------------------------------------------
The containment executable completed the full geometry_geo19 test without a
crash. The log contains the requested visible, leave/return, PDA return,
spawn/remove and same-map-restart markers:

  GEO05_VISIBLE
  GEO05_RETURNED
  GEO05_PDA_RETURN
  GEO05_REMOVED
  GEO05_MAP_RESTART_BEFORE
  GEO05_MAP_RESTART_AFTER

The same-map restart advanced mapLoadSerial 3 -> 4, worldGeneration 4 -> 5,
primaryHistoryOwner 4 -> 5 and reset count 3 -> 4. The post-restart registry
repopulated with collision=0 and stale=0. The log ends in the normal render,
game-map and event-system shutdown sequence. No later Windows application
error was recorded after the earlier crashing executable.

This validates the root-cause isolation: withholding the backend portal/entity
walk under com_smp removes the recurring access violation while the existing
draw-surface path and lifecycle registry continue through all requested
transitions.

FRONTEND FRAME SNAPSHOT RESTORATION (2026-07-24)
------------------------------------------------
The next implementation replaces the containment-only shutdown with a frame
snapshot captured in R_RenderView immediately before R_AddDrawViewCmd.

Frontend producer contract:

  * compute the reachable portal-area set while the frontend owns the live
    render world;
  * walk portal entity references only on the frontend;
  * admit only non-world DM_STATIC models with drawing surfaces and reject
    callback, force-update, jointed, depth-hack and generated-model entities
    before the command boundary;
  * clone the required renderEntity values, transform matrices, reference
    bounds and stable renderer-asset pointers into frame memory;
  * clear callbackData, callback, joints, GUI, remote-view, sound, decal,
    overlay, interaction and live area-reference pointers;
  * capture the lifecycle render-def key, model epoch, map-load serial,
    area/depth and current-frame visibility as values;
  * build frame-owned area lists whose nodes refer only to frame-owned entity
    clones.

Backend consumer contract:

  * com_smp requires the snapshot attached to the exact view command; a
    missing snapshot still fails closed;
  * reachable areas, entity membership, area links, identity, epoch,
    transform and visibility come from the snapshot;
  * the render-world pointer is opaque identity and the residency store uses
    the captured map-load serial instead of reading mutable world fields;
  * visible draw-surface matching uses the captured source pointer only as an
    opaque map key and never dereferences it;
  * only stable DM_STATIC model, triangle, material, skin and shader assets are
    retained for same-frame classification and mesh ingestion;
  * the old animated/single-bone/joint/reachable diagnostic walks are disabled
    under com_smp because they independently dereference live entity arrays.

Offline result:

  win64-pt-dev-release: PASS
  All SPIR-V shaders are up to date.
  PathTraceCanonicalIdentityHarness: PASS
  executable bytes: 15571456
  executable SHA-256:
    3086BD2E6C074099396E137EE376ECE82017FE9D67014F32A6A4F096CF0C73A8
  build and E:/prog/rbdoom-3-BFG-prebuilt deployed hashes match.

This slice changes no HLSL source or shader blob. Runtime restoration remains gated on a
capture showing a non-zero "PT entityFeed snapshot" entity count, normal
entity-feed admission/residency output, restored offscreen rigid behavior and
no recurrence during traversal plus same-map restart.

SNAPSHOT RESTORATION RUNTIME VALIDATION (2026-07-24)
----------------------------------------------------
geometry_geo20.log validates the frontend snapshot and SMP backend consumer:

  path:
    C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo20.log
  bytes: 50161
  created: 2026-07-24 15:19:39 Australia/Sydney
  modified: 2026-07-24 15:24:07 Australia/Sydney

Initial capture:

  marker: GEO05_SNAPSHOT_START
  mapSerial=1
  snapshot areas/entities=58/100
  reachable areas=5
  candidates/admitted=121/121
  droppedBudget=0

Immediately before same-map restart:

  marker: GEO05_SNAPSHOT_BEFORE_RESTART
  mapSerial=1
  snapshot areas/entities=58/86
  reachable areas=1
  candidates/admitted=114/114
  droppedBudget=0

After same-map restart:

  marker: GEO05_SNAPSHOT_AFTER_RESTART
  mapSerial=2
  snapshot areas/entities=58/85
  reachable areas=1
  candidates/admitted=113/113
  droppedBudget=0

The log contains 225 entity-feed frame reports spanning frames 4093 through
9513. Every populated report admitted all candidates and every report had
droppedBudget=0. Three zero-candidate reports share the same frame number as a
preceding populated report and occur immediately after the dump cvar is
disabled; they are secondary/console view commands, not a loss of the primary
snapshot.

There is no "frontend frame snapshot is missing" or SMP-disable warning, no
access violation/device-removed/fatal record, and no matching RBDoom3BFG
Application Error or Windows Error Reporting event during the capture window.
The log ends with normal render-system, game-map and event-system shutdown.

Result:

  PASS - immutable frontend snapshot present under SMP
  PASS - non-zero entity feed restored before and after map restart
  PASS - map-load serial invalidation 1 -> 2
  PASS - candidate admission stable with zero budget loss
  PASS - prior portal-list access violation did not recur

The structural and stability portion of the GEO-05 snapshot restoration gate is
closed. Visual offscreen-object appearance remains a user-observed criterion
because it is not encoded in this log.

CALLBACK MD5 SOURCE-DISCOVERY PREREQUISITE (2026-07-24)
-------------------------------------------------------
The GEO-06 entry audit found a contradiction in the accepted GEO-05 evidence:
crowded MD5 entities were alive in the renderer, but every shadow-registry dump
reported:

  instanceClass(static/rigid/skinned)=.../.../0

The Add notification for a deferred entity occurs before its render callback
has necessarily supplied hModel. ShadowSourceDomain also deliberately rejected
all callback entities because lifecycle fast paths do not own a safe model
surface traversal. Later callback fast updates use sourceStable=false, so those
updates correctly avoid dereferencing the model but also cannot discover the
bind source. The result was a permanent canonical-record gap for callback MD5
entities.

The bounded correction preserves the lifetime rule:

  Add/Free lifecycle hooks remain instance-liveness authority;
  R_RenderView calls ObserveFrontendDeformingEntities after R_AddModels;
  this frontend boundary has resolved callback models and owns viewEntitys;
  first discovery traverses the immutable DM_CACHED bind source once;
  later observations refresh only entity values/transforms;
  duplicate viewEntity entries are suppressed;
  no live entity/model/surface pointer crosses to the SMP backend.

New interval diagnostics:

  frontendDeforming(discovered/refreshed)=<first-records>/<value-refreshes>

This remains observation-only under the existing default-off
r_pathTracingGeometryShadowRegistry gate. It does not route skinned output,
change BLAS ownership, or modify shader inputs.

Build and offline proof:

  cmake --build --preset win64-pt-dev-release
    PASS, 2026-07-24
  PathTraceCanonicalIdentityHarness
    PASS
  source/deployed executable SHA-256:
    DB87BB860D8AC30FD2E14C505AD0D61E321A70CD1B25C3698E35CF2B5B5CC5E1
  executable mtime:
    2026-07-24 15:32:26 Australia/Sydney
  SPIR-V:
    unchanged; build reported all SPIR-V shaders up to date

Focused runtime acceptance still required:

  r_pathTracingGeometryShadowRegistry 1
  mapRestart
  visit or spawn several animated zombies, including Fatzombie if convenient
  r_pathTracingGeometryShadowRegistryDump 1

The dump must show skinned > 0 and frontendDeforming discovered/refreshed > 0.
Sample records must identify SkinnedSurface/SkinnedBindSource. Collision and
stale-generation counts must remain zero. Removal/re-add, offscreen return and
same-map restart must not crash or retain the removed instance generation.

FIRST CALLBACK MD5 CAPTURE AND CORRECTION (2026-07-24)
------------------------------------------------------
geometry_geo21.log did not pass the callback-MD5 discovery gate:

  path:
    C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo21.log
  bytes: 59353
  created: 2026-07-24 15:44:22 Australia/Sydney
  modified: 2026-07-24 15:49:48 Australia/Sydney

All five LIVE/RETURN/REMOVED/READDED/RESTART_AFTER dumps reported:

  frontendDeforming(discovered/refreshed)=0/0
  instanceClass(.../.../skinned)=.../.../0
  collision=0
  stale=0

The map-restart contract remained correct:

  worldGeneration 3 -> 4
  previousWorldGeneration after restart = 3
  mapLoadSerial 2 -> 3
  primaryHistoryOwner 3 -> 4

The capture ended in a normal renderer/game/event-system shutdown with no
access violation, fatal error or Vulkan device removal.

Root cause:

  idRenderModelMD5 is the durable source asset and reports DM_CACHED, but it
  intentionally owns no instantiated modelSurface_t array. R_AddModels calls
  R_EntityDefDynamicModel and places current renderable surfaces in
  entity->dynamicModel. The first frontend correction correctly reached the
  safe post-callback boundary but still traversed parms.hModel, so it observed
  zero source surfaces and could not add a record.

Second correction:

  canonical asset identity and model name still come from parms.hModel;
  surface ordinal/topology/material/counts are copied from the current
  post-R_AddModels dynamic snapshot;
  MD5 snapshot surface->id supplies the stable source-mesh ordinal;
  the snapshot is accepted only when dynamicModelFrameCount == tr.frameCount;
  no snapshot/model/surface pointer is retained in a shadow record;
  lifecycle Add/Update with no resolved snapshot now preserves existing
  SkinnedBindSource records instead of interpreting zero source-model surfaces
  as removal;
  subsequent frontend frames refresh values without rehashing topology.

This correction requires a replacement focused capture before GEO-06 begins.

SECOND CALLBACK MD5 CAPTURE AND CHURN CORRECTION (2026-07-24)
-------------------------------------------------------------
geometry_geo22.log proved that the resolved-snapshot boundary reaches MD5
geometry, but it exposed invalid steady-state lifecycle churn:

  path:
    C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo22.log
  bytes: 16727
  created: 2026-07-24 16:04:07 Australia/Sydney
  modified: 2026-07-24 16:05:35 Australia/Sydney

LIVE dump:

  frontendDeforming(discovered/refreshed)=4994/110
  instanceClass(static/rigid/skinned)=1791/1681/21
  add/remove=27668/24175
  deferredMesh=21303
  removedGeometryInterval(v/i/t)=10714246/52538856/17512952
  collision/stale=0/0

The 21 live skinned surfaces prove successful MD5 source/snapshot association,
but thousands of discoveries and tens of millions of removed elements violate
the first-discovery/steady-refresh contract.

Root cause:

  a moving callback entity can take the full UpdateEntityDef path when bounds,
  origin or axis no longer match. The lifecycle observer rejected callback
  surface traversal, as required, but then interpreted that safety rejection as
  an empty source and removed the frontend-owned records. The next frontend
  view rediscovered them, repeating the cycle.

Correction:

  lifecycle ObserveEntity now returns immediately for callback entities unless
  it was explicitly invoked by the safe frontend boundary. It neither traverses
  nor removes callback-owned surfaces. The post-R_AddModels frontend observation
  is the sole surface discovery/reconciliation path, while explicit Free remains
  authoritative removal.

The capture stopped after the first LIVE dump, so it contains no shutdown or
removal/restart evidence. A replacement capture must show bounded discovery,
growing refresh counts, stable live skinned totals, and removal only at the
explicit removal/restart markers.

The replacement dump prioritizes SkinnedSurface samples ahead of rigid/static
records and adds removedClassInterval(static/rigid/skinned). This makes callback
churn and explicit skinned removal directly machine-checkable.

THIRD CALLBACK MD5 CAPTURE - STEADY STATE PASS (2026-07-24)
-----------------------------------------------------------
The appended enabled portion of geometry_geo23.log passes the discovery and
steady-refresh contract:

  path:
    C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo23.log
  bytes at inspection: 18008
  created: 2026-07-24 16:11:12 Australia/Sydney
  modified: 2026-07-24 16:14:10 Australia/Sydney

Enabled dump at frame 4097:

  worldGeneration=3
  mapLoadSerial=2
  primaryHistoryOwner=3
  add/update/remove=3540/25505/0
  collision/stale=0/0
  frontendDeforming(discovered/refreshed)=17/6429
  records(instances/meshesReferenced/meshesZeroRef)=3540/2794/0
  records(shared/maxRefs)=191/19
  instanceClass(static/rigid/skinned)=1791/1681/68
  removedGeometryInterval(v/i/t)=0/0/0
  removedClassInterval(static/rigid/skinned)=0/0/0

All 16 prioritized samples report kind=3/source=3
(SkinnedSurface/SkinnedBindSource). They include distinct entity slots sharing
the same canonical mesh, for example two soft_desk_chair2 entities referencing
one mesh hash. The interval contains thousands of value refreshes with no
surface rediscovery churn and no removal.

Result:

  PASS - safe resolved MD5 surface discovery
  PASS - bounded first discovery versus steady refresh
  PASS - stable live skinned records with zero steady-state removal
  PASS - SkinnedSurface/SkinnedBindSource tuple visibility
  PASS - canonical immutable mesh sharing across distinct instances
  PASS - collision/stale remain zero

Still required before final closure:

  explicit removal of one named spawned MD5 entity;
  re-add of that entity;
  proof that only its skinned surfaces leave/re-enter the live registry.

World/restart invalidation already passed in geometry_geo20/21, but the final
capture should include the same-map restart if convenient.

THIRD CALLBACK MD5 CAPTURE - REMOVAL/RE-ADD PASS (2026-07-24)
-------------------------------------------------------------
The final append to geometry_geo23.log closes the explicit MD5 lifecycle gate.

Immediately after removing geo_md5_fat1:

  frame=6051
  add/update/remove=13/65924/2
  frontendDeforming(discovered/refreshed)=4/42056
  instanceClass(static/rigid/skinned)=1791/1681/79
  removedGeometryInterval(v/i/t)=6100/30576/10192
  removedClassInterval(static/rigid/skinned)=0/0/2
  records(meshesReferenced/assets)=2804/634
  collision/stale=0/0

Other newly visible deforming entities account for the 13 interval additions;
the class-split counter proves that the named removal invalidated exactly two
skinned surface instances.

After re-adding geo_md5_fat1:

  frame=6573
  add/update/remove=2/9287/0
  frontendDeforming(discovered/refreshed)=1/2531
  instanceClass(static/rigid/skinned)=1791/1681/81
  removedClassInterval(static/rigid/skinned)=0/0/0
  records(meshesReferenced/assets)=2804/634
  collision/stale=0/0

The live skinned count rises by exactly two. Referenced mesh and asset counts do
not rise, proving reuse of the existing immutable source records rather than
duplicate asset creation. The replacement instance then receives thousands of
value refreshes without rediscovery churn.

FINAL GEO-05 RESULT
-------------------
  PASS - per-world owner and monotonic world/history generations
  PASS - exact entity slot generation and Free invalidation
  PASS - immutable SMP frontend snapshot and backend entity feed
  PASS - callback MD5 source identity separated from resolved surface snapshot
  PASS - bounded first discovery and steady value-only refresh
  PASS - explicit skinned removal/re-add with source-mesh reuse
  PASS - same-map restart invalidation
  PASS - collision/stale counters remain zero
  PASS - no repeated CPU crash in the accepted captures

GEO-06 pooled source ingestion is now unblocked.

Accepted executable:

  SHA-256:
    1E0C758A9E0953C6221D5B3F98E8AE7BEFF98B75D223AB3B0CCFDDFF85D090B9
  mtime:
    2026-07-24 16:08:41 Australia/Sydney
  build:
    win64-pt-dev-release PASS
  PathTraceCanonicalIdentityHarness:
    PASS
  SPIR-V:
    unchanged; all SPIR-V shaders reported up to date
