GEO-09 entry audit - skinned temporal/material/emissive promotion
================================================================
Status: step 1 consumer inventory and CPU contract audit accepted, 2026-07-26.

SCOPE
-----
This entry slice is behavior-neutral. It inventories the live skinned
consumer funnel and adds a one-shot validator. It does not change traversal,
materials, motion, emissive inventories, DI/GI/RR behavior, or shaders.

LIVE PRODUCER AND HIT ROUTE
---------------------------
The accepted GEO-08 route already provides:

  CPU route:
    PtSkinnedHitRouteRecord
      full canonical InstanceKey and MeshKey
      collision-checked 64-bit instance hash
      source index, current output, and previous-position ranges
      source/output generations and source-local triangle range

    PtSkinnedHitRouteTriangle
      source-local primitive index
      optional legacy merged-dynamic primitive index
      material ID and material-table index
      triangle class/flags
      canonical primitive identity
      canonical emissive identity

  GPU route:
    PathTraceSkinnedHitRouteGpuRecord at t18
    PathTraceSkinnedHitRouteGpuTriangle at t19
    immutable source indexes at t28
    current world-space skinned output at t29
    previous world-space positions at t32

  AS route:
    one updateable BLAS per eligible skinned surface
    identity-transform TLAS descriptors
    mask 0x02 for active descriptors
    SBT contributions 2/3 for skinned primary/shadow hits

CONSUMER INVENTORY
------------------
Primary/general PT:

  PathTraceSkinnedHitRoute.hlsli
  pathtrace_primary_surface_producer.rt.hlsl
  pathtrace_smoke.rt.hlsl

These recover source-local current vertex attributes, material ID/index, and
triangle class. The primary payload carries instance, primitive, material,
class, current world position, barycentrics, UV, normals, and tangent data.

Temporal/motion:

  pathtrace_smoke_rab_motion_supplier.hlsli
  PathTracePrimarySurface.hlsli

The motion supplier uses the route's previousPositionOffset and the same
source-local indexes. Current and previous positions are already world-space.
Primary-surface history packs the resulting instance/primitive/material/class
and previous object position for ReSTIR/RR reprojection.

DI/GI/debug:

  cleanroom_rtxdi/pathtrace_clean_rtxdi_di_hit_surface_adapter.hlsli
  cleanroom_rtxdi/pathtrace_clean_rtxdi_di_surface_adapter.hlsli
  cleanroom_rtxdi/pathtrace_clean_rtxdi_di_skinned_hits.rt.hlsl
  remix_restir_gi/pathtrace_clean_restir_gi_skinned_hits.rt.hlsl
  pathtrace_regir_debug.rt.hlsl
  pathtrace_nee_cache_debug.rt.hlsl

These contain explicit skinned route branches. Their algorithm behavior stays
outside GEO-09; later steps validate only geometry/material inputs.

Materials:

  PathTraceMaterialUniverse and PathTraceDynamicMaterialState own the material
  table and runtime texture/state records. Canonical skinned triangles carry
  both material ID and the current frame's material-table index.

Emissives and the concrete remaining mismatch:

  BuildSmokeEmissiveTriangleInventory walks captured static/dynamic payloads.
  AppendSmokeEmissiveInventoryForGeometry explicitly skips
  RT_SMOKE_SURFACE_CLASS_SKINNED_DEFORMED.

Canonical skinned triangles therefore contribute no
PathTraceSmokeEmissiveTriangle records, emissive distribution entries, or
unified-light records. This matches the legacy skip but does not satisfy
GEO-09/Q17. The replacement must consume mesh-local primitives, per-instance
identity, and current GPU-skinned positions without restoring CPU skinning.

FIRST ACCEPTED VALIDATOR
------------------------
New default-zero cvar:

  r_pathTracingGeometrySkinnedConsumerAudit 1

The one-shot validates two independent views:

  live:
    exact source-only CPU route, GPU upload, and accepted TLAS plan.

  shadow:
    same frame and surface set rebuilt diagnostically with retained legacy
    merged-dynamic offsets. It never enters a TLAS or GPU buffer.

Both views validate route/triangle counts, CPU/GPU field equality, current and
previous bounds, material-table identity, source-local primitive identity,
canonical emissive identity, and identity collisions. The shadow additionally
validates every mapped triangle's material ID/index and class/flags against
the legacy arrays. Injected harness failures cover material-table,
legacy-class, motion-range, GPU-upload, and TLAS-route mismatches.

AUTOMATED EVIDENCE
------------------
Focused harness:

  PathTraceSkinnedConsumerAuditHarness: PASS

Accepted runtime:

  log:
    C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
      geometry_geo128_auto.log

  scene:
    prepared doom3_1 save, maps/game/mars_city1

  gates:
    r_pathTracingGpuSkinning 1
    r_pathTracingGeometryAuthoritativeGpuSkinning 1
    r_pathTracingGeometryShadowRegistry 1
    r_pathTracingGeometrySkinnedTlasCompare 1
    r_pathTracingGeometrySkinnedCaptureSplit 0
    r_pathTracingGeometrySkinnedConsumerAudit 1 -> one-shot reset to 0
    clean DI/GI disabled

  frame 3:
    live accepted/routes/triangles:       1 / 12 / 15,962
    live legacyMapped/sourceOnly:         0 / 15,962
    live motionReady/missing:             12 / 0
    shadow accepted/routes/triangles:     1 / 12 / 15,962
    shadow legacyMapped/sourceOnly:       15,962 / 0
    shadow motionReady/missing:           12 / 0
    all 13 live failure buckets:          0
    all 13 shadow failure buckets:        0

The game completed normal renderer/game/event shutdown. The log contains no
fatal, device-removed, NVRHI-error, or validation-error marker.

BUILD AND DEPLOY
----------------
  preset:
    cmake --build --preset win64-pt-dev-release

  result:
    PASS

  shader:
    no shader source changed
    all SPIR-V reported up to date
    no SPIR-V copied
    no DX12 shader compile or deploy performed

  executable SHA-256, build and deployed:
    69896D79AEE1D12DF0229C0806EA87D29C4D00DF97B96A807D6A16DE20BF02C0

ACCEPTED BOUNDARY AND NEXT STEP
-------------------------------
GEO-09 step 1 is accepted. This proves the metadata/identity/range seam. It is
not shader hit-tuple parity and it does not add skinned emissive lights.

Next:

  - add a bounded same-ray legacy/canonical primary-hit tuple comparator for
    instance, primitive mapping, barycentrics, world position, normal, tangent,
    UV, material, and triangle flags;
  - validate motion/history reset scenarios;
  - then implement a shadow canonical skinned emissive inventory sourced from
    GPU output and compare its temporal remap.

SAME-RAY PRIMARY-HIT AUDIT
--------------------------
Step 2/3 adds default-zero:

  r_pathTracingGeometrySkinnedHitAudit 1
  r_pathTracingDebugMode 58

Mode 58 samples one adjacent legacy/canonical pixel pair per 8x8 block. Both
pixels reconstruct the exact same ray at the pair midpoint. The legacy pixel
traces mask 0x01; the canonical pixel traces mask 0x02. Numeric payloads are
compacted into the existing primary-surface history buffer, so no shader ABI,
descriptor, or pipeline binding changes were required. At 2560x1440 the audit
copies 57,600 pairs / 20,275,200 bytes rather than the full 648,806,400-byte
history.

Each canonical skinned hit is mapped through InstanceID and source-local
PrimitiveIndex to the independent legacy-backed shadow. Static geometry that
occludes the canonical-only ray is counted separately. Only unobstructed
exact-primitive pairs enter tuple comparison. The compared fields are:

  hit T and world position;
  geometric and shading normal;
  tangent and bitangent;
  UV and normal-map UV;
  barycentrics;
  material ID, table index, subtype, and surface class;
  triangle class/flags.

Declared tolerances:

  tuple/position/geometric/UV/barycentric: 1e-3 max component
  normalized shading/tangent/bitangent:    1e-2 max component

The wider basis tolerance covers independent CPU/GPU weighted normalization.
The accepted run's worst basis delta is 0.00529411435; its position and
barycentric maxima are 0.000305175781 and 0.000289946795.

FIRST MISMATCH FOUND AND FIXED
------------------------------
geometry_geo130_auto proved 5,264 canonical skinned samples but every exact
pair disagreed in triangle flags. The source-only producer kept only surface
class 2 and dropped RT_SMOKE_TRIANGLE_EMISSIVE_STAGE_OFF (0x00040000), while
legacy triangles carried 0x00040002. `RtSmokeSkinnedSurfaceRecord` now captures
the already-evaluated per-surface class/flag tuple, and source-only route
metadata consumes it without consulting legacy merged offsets.

JOINT-SOURCE ORACLE REQUIREMENT
-------------------------------
geometry_geo132_auto intentionally cross-checked mode 1 without an exact CPU
snapshot. GPU skinning parity failed 14/24 current samples at 0.116073608 max,
and the hit audit showed corresponding animated-surface deltas. This is a
same-frame oracle skew: the retained CPU bridge and renderer joint-cache copy
did not consume byte-identical joint matrices.

For acceptance, authoritative mode 2 is mandatory. It retains the exact
frame-owned CPU snapshot corresponding to the renderer joint-cache upload.
geometry_geo134_auto then proves:

  joint-cache copies:                         12 / 28,320 bytes
  CPU-vs-GPU skinning parity:                 PASS, 24/24 samples
  maximum GPU vertex error:                  0.0000152587891
  sampled ray pairs:                         57,600
  canonical skinned hits:                    5,264
  legacy-closer static occluders:             397
  comparable / exact mapped primitive:       4,867 / 4,867
  all 13 hit-tuple mismatch buckets:         0
  accepted:                                  1

The accepted gates were:

  r_pathTracingGpuSkinning 1
  r_pathTracingGpuSkinningParityDump 1
  r_pathTracingGeometryAuthoritativeGpuSkinning 2
  r_pathTracingGeometryShadowRegistry 1
  r_pathTracingGeometrySkinnedTlasCompare 1
  r_pathTracingGeometrySkinnedCaptureSplit 0
  r_pathTracingGeometrySkinnedHitAudit 1
  r_pathTracingDebugMode 58
  clean DI/GI disabled

The game completed normal renderer/game/event shutdown with no fatal,
device-removed, Vulkan, NVRHI, validation, or crash marker.

STEP 2/3 BUILD AND DEPLOY
-------------------------
  focused harnesses:
    PathTraceSkinnedConsumerAuditHarness: PASS
    PathTraceSkinnedHitRouteHarness: PASS

  preset:
    cmake --build --preset win64-pt-dev-release

  result:
    PASS

  Vulkan shader:
    pathtrace_smoke.rt.bin
    source edit: 2026-07-26T04:51:15.3110781Z
    rebuilt blob: 2026-07-26T04:52:19.1596948Z
    rebuilt newer than source: yes

  The shared source also rebuilt:
    pathtrace_restir_pdf_nee_rlu_current.rt.bin

  No DX12 shader was compiled or deployed.

  final executable SHA-256 after logging-only cleanup, build and deployed:
    C851B3460486D2B77AFF37DD0B2FCB72BA5A823B24E3F87DB246EE6D13865796

  Vulkan SPIR-V SHA-256:
    pathtrace_smoke.rt.bin
      037C1D6309EC3E0B12B1C61A68AF4C7F974F05743858504FA3AF422A0F1BF43E
    pathtrace_restir_pdf_nee_rlu_current.rt.bin
      D7704B958217BE893731067BF61D5514653DCB160F8253957E6E79FBCACECB69

STEP 2/3 DISPOSITION
--------------------
Primary-hit geometry/material/class parity is accepted for the prepared
animated scene under an exact joint-source oracle. Q16 remains open for the
motion/history scenarios in step 4. Q17 remains open for canonical
GPU-output emissive inventory and remap in step 5.

STEP 4 - MOTION AND HISTORY RESET CONTRACT
------------------------------------------
The mode-58 compact records now also carry the result of the live
`TryPathTracePrimarySurfaceObjectMotion` supplier. For each exact mapped
legacy/canonical primitive the CPU readback compares:

  object-motion and previous-position valid flags
  motion failure/debug status
  previous world position at 1e-3 max-component tolerance

This reuses the existing 176-byte primary-surface record and the accepted
57,600-pair compact readback. It adds no shader ABI, binding, or production
history behavior.

`r_pathTracingGeometrySkinnedHitAudit` retains value 1 as next-eligible-frame
arming. Values greater than 1 now count down eligible geometry frames before
arming, allowing deterministic settled-animation sampling without console
input.

First probe, geometry_geo135_auto:

  frame:                                      3
  exact mapped primitives:                    4,865
  motion-valid legacy/canonical/comparable:   4,865 / 4,865 / 4,865
  motion validity/status/previous mismatch:   0 / 0 / 0
  previous-position maximum:                  0.000869750977
  motion accepted by the separated verdict:  yes

The old combined `accepted` field was zero because one unrelated hit-distance
sample measured 0.00115966797 against the already-declared 0.001 hit
tolerance. This run is retained as a rejected combined-hit probe; it did not
motivate widening either tolerance.

Delayed settled probe, geometry_geo136_auto:

  requested countdown / armed frame:          120 / 122
  canonical skinned hits:                     5,168
  exact mapped primitives:                    4,769 / 4,769
  motion-valid legacy/canonical/comparable:   4,769 / 4,769 / 4,769
  all 16 hit + motion mismatch buckets:       0
  previous-position maximum:                  0.00048828125
  combined accepted / motion accepted:        1 / 1

The per-surface reset classification was then extracted, without changing its
inputs or outputs, into `PtSelectSkinnedSurfaceTemporalPolicy`, which the live
scene builder now calls. `PathTraceSkinnedHistoryPolicyHarness` directly
proves:

  first observation rejects with NO_PREVIOUS_FRAME
  stable animation and continuous locomotion accept complete prior state
  newly spawned and offscreen-returning surfaces reject absent prior identity
  transform discontinuity rejects and clears transform continuity
  material or surface-class identity changes reject
  topology, skeleton, or previous-buffer failures reject

A removed instance produces no current surface record and therefore no motion
record. If it returns later, the absent exact prior identity takes the same
explicit NO_PREVIOUS_SURFACE reset path covered above.

Post-extraction runtime, geometry_geo137_auto:

  armed frame:                                 62
  canonical skinned hits:                     5,346
  exact mapped primitives:                    4,953 / 4,953
  all 16 hit + motion mismatch buckets:       0
  motion-valid legacy/canonical/comparable:   4,953 / 4,953 / 4,953
  previous-position maximum:                  0.00048828125
  combined accepted / motion accepted:        1 / 1
  GPU skinning parity:                        PASS, 24/24

Both accepted delayed runs completed normal renderer, Streamline, game, and
event shutdown with no fatal, device-removed, Vulkan, NVRHI, or validation
error marker.

STEP 4 BUILD AND DEPLOY
-----------------------
  focused harness:
    PathTraceSkinnedHistoryPolicyHarness: PASS

  preset:
    cmake --build --preset win64-pt-dev-release

  result:
    PASS

  Vulkan shader:
    pathtrace_smoke.rt.bin
    source edit: 2026-07-26T05:10:08Z
    rebuilt blob: 2026-07-26T05:11:14Z
    rebuilt newer than source: yes
    SHA-256:
      6577CA88A019C094FC15A923274F098E9804F58E64AADC9F85029B59A6E6CE97

  final executable SHA-256 after temporal-policy extraction:
    351C678899A75E5C4D422CB678E34C3ACC4C0CE29976EC6035625AF329312A19

  Only the affected Vulkan smoke blob and executable were deployed. No DX12
  shader was compiled or deployed.

STEP 4 DISPOSITION
------------------
Stable animated previous-position parity and every owned reset decision are
accepted. Q16 is resolved for the canonical skinned motion/history contract.
Step 5 canonical GPU-output emissive inventory and temporal remap is next;
Q17 remains open.

STEP 5 MATERIAL BOUNDARY AUDIT
------------------------------
The first source-local census separated three materially different cases:

  no emissive stage
  emissive but not a light candidate
  production-eligible emissive light candidate

geometry_geo147_auto spawned Archvile, Revenant, Bruiser, and Cacodemon
actors. Five routed monster materials were discovered as emissive and as
light candidates:

  models/monsters/bruiser/bruiser3
  models/monsters/revenant/reye
  models/monsters/archvile/archvilehand2
  models/monsters/cacodemon/cacoeye
  models/monsters/cacodemon/cacobrain

All five had an authored emissive image but no RT texture handle:

  image / handle / safe = 1 / 0 / 0

The material universe therefore correctly withheld
RT_SMOKE_MATERIAL_EMISSIVE and
RT_SMOKE_MATERIAL_EMISSIVE_LIGHT_CANDIDATE. Promoting those surfaces with a
constant white fallback would light the entire mesh and is not a valid
geometry fix. Step 5 does not weaken texture safety or alter production
material flags. This is a separate material-texture binding limitation, wider
than the already-known Fatzombie descriptor defect.

STEP 5 GPU-OUTPUT SHADOW
------------------------
`r_pathTracingGeometrySkinnedEmissiveAudit` remains default zero. Value 1
arms the next eligible frame; larger values count down eligible geometry
frames. The one-shot:

  reads the authoritative current skinned output and previous-position GPU
  buffers asynchronously;
  resolves immutable source-local indices through the canonical mesh record;
  carries canonical instance, local primitive, material, triangle flags, and
  emissive identity into triangle-light records;
  builds current and previous inventories with the same shared remap helper
  used by production emissive lights;
  compares them with a same-frame CPU oracle without changing production
  geometry, materials, lights, or shaders.

For positive geometry coverage only, the audit makes a private material-table
copy and force-enables materials that discovery already classified as
emissive/light-candidate but production rejected for an unsafe texture. It
uses the discovered constant stage color and never publishes that view to
production. Every log states `productionBehaviorChanged=0`.

Authoritative referenced vertex positions retain the existing 1e-3 absolute
and 1e-5 relative tolerance. Derived center/area/normal/weight/PDF values use
a separate 1e-2 absolute aggregate tolerance because small vertex errors can
amplify through cross products and normalization. Identity, material,
primitive, flags, and remap remain exact.

The shared remap helper also runs four focused transition cases over accepted
real records:

  stable inventory maps every record to itself;
  one added record reports previous-missing;
  one removed record reports current-missing;
  a second identical-model-style record with a distinct instance identity
  maps without duplicate flags.

STEP 5 ACCEPTED RUNTIME
-----------------------
Final log:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo152_auto.log

Scene:

  prepared doom3_1 save, maps/game/mars_city1
  Archvile, Revenant, Bruiser, and three Cacodemons spawned

Accepted frame 241:

  canonical routes / source triangles:              31 / 35,814
  production eligible / validation-forced materials: 0 / 9
  validation current / previous records:         2,398 / 2,398
  unique emissive instances / materials:              8 / 8
  runtime-inactive triangles:                         1,840
  referenced current / previous vertices:         22,579 / 22,579
  current / previous vertex failures:                  0 / 0
  max current / previous vertex error:       0.000122070312
  identity / metadata / numeric / remap mismatch:       0 / 0 / 0 / 0
  max aggregate-record error:                0.000736236572
  zero identities / collisions:                       0 / 0
  valid settled remaps:                         2,398 / 2,398
  stable / spawn / despawn / identical transition:     1 / 1 / 1 / 1
  accepted:                                              1

The game completed normal renderer, Streamline, game, and event shutdown.
The log contains no fatal, device-removed, Vulkan, NVRHI, or validation-error
marker.

STEP 5 BUILD AND DEPLOY
-----------------------
  preset:
    cmake --build --preset win64-pt-dev-release

  result:
    PASS

  final executable SHA-256:
    AC0E0C131B4937B4946DC1659B538C8CE04C3A09CAFA3594D875CE5D646BE1E1

No shader source changed in step 5. The build reported all SPIR-V shaders up
to date; no DX12 or SPIR-V blob was copied. Only the executable was deployed.

STEP 5 DISPOSITION
------------------
The canonical GPU-output inventory construction, identity, current/previous
geometry, and remap comparison are accepted. This resolves the comparison
portion of Q17. Production still skips skinned triangles in the live
CPU-built inventory, and the runtime material universe currently exposes no
safe skinned emissive-light candidate in this scene. Production cutover stays
open for GEO-09 step 8; step 6 material binding/state parity is next.

STEP 6 MATERIAL/UV CONTRACT
---------------------------
The trace found one concrete mismatch before promotion. Legacy merged dynamic
geometry applies the evaluated `PathTraceDynamicMaterialRecord` texture matrix
to vertex UV.xy on the CPU. Canonical GPU skinning previously copied the
immutable source UV unchanged. Material ID/index parity therefore did not by
itself guarantee texture lookup parity for a skinned surface with
`RT_SMOKE_DYNAMIC_MATERIAL_RECORD_HAS_TEX_MATRIX`.

The fix belongs at the canonical skinned-output producer, not in each ray
consumer:

  the per-surface compute dispatch carries the evaluated 2x3 texture matrix;
  matrix selection resolves the material-table row from the canonical
  `RtSmokeSkinnedSurfaceRecord.materialId`;
  retained merged triangle offsets are used only to compare the old material
  ID/index tuple while CPU capture exists;
  CPU-capture-omitted surfaces can resolve the same row without a legacy
  triangle offset;
  pathtrace_skinning.cs.hlsl writes transformed UV.xy and preserves the
  independent normal-map UV.zw channels.

This keeps primary, shadow-any-hit, DI, GI, RR, ReGIR, NEE, alpha-test, and
decal consumers on one canonical output instead of duplicating matrix logic.
They already load the exact shared `PathTraceSmokeMaterial` and
`PathTraceDynamicMaterialRecord` row selected by the route material index.

The existing default-zero `r_pathTracingGpuSkinningParityDump` now:

  audits every routed surface material ID against the shared table row;
  validates material/facts/feature/parameter row completeness;
  validates live dynamic-record material ID/index ownership;
  reports routed unsafe descriptor classes separately;
  reserves samples for matrix-bearing skinned surfaces first;
  compares all four GPU/CPU UV channels in addition to position and motion.

STEP 6 ACCEPTED RUNTIME
-----------------------
Final log:

  C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/
    geometry_geo154_auto.log

Scene:

  prepared doom3_1 save, maps/game/mars_city1
  one monster_flying_lostsoul spawned for a scrolling-stage material

Accepted frame 179:

  dispatches / source triangles:                       15 / 16,564
  distinct shared material-table rows:                       15
  valid live dynamic records:                                 8
  texture-matrix dispatches / vertices:                    1 / 86
  range / mixed-material / index / ID mismatch:          0 / 0 / 0 / 0
  incomplete table row / dynamic-record mismatch:           0 / 0
  routed unsafe diffuse/alpha/normal/specular/emissive: 2 / 0 / 1 / 1 / 0
  GPU parity samples / texture-matrix samples:             24 / 1
  position / previous / motion / UV failures:          0 / 0 / 0 / 0
  max current / previous / motion error:  0.0000610351562 /
                                           0.0000152587891 /
                                           0.0000610351562
  max UV error:                                               0
  non-finite outputs:                                         0
  accepted:                                                    1

The live matrix-bearing Lost Soul record selected the same identity matrix as
the legacy header at the sampled frame, so `texMatrixChangedSamples=0`. This
proves exact old/new state consumption and executes the matrix branch, but it
does not claim a non-identity live transform. Ordered-stage animation quality
is an existing material-system question outside geometry promotion.

The unsafe descriptor counts remain classified material data. In particular,
the known Fatzombie texture rejection is not reclassified as a geometry
failure and no descriptor safety rule is weakened.

The game completed normal renderer, Streamline, game, and event shutdown.
The log contains no fatal, device-removed, Vulkan, NVRHI, or validation-error
marker.

STEP 6 BUILD AND DEPLOY
-----------------------
  preset:
    cmake --build --preset win64-pt-dev-release

  result:
    PASS

  final executable SHA-256:
    B948378FF6EE0F899F2EAF7B56623442F0154227B9F333FC7B109F64448F3AF3

  rebuilt Vulkan shader:
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_skinning.cs.bin

  SPIR-V SHA-256:
    97132DC3F4446136E31F6CA7CE7CAE357956FE86802FD2B82A608E114A0E32DB

The SPIR-V timestamp is newer than the shader edit. Only that SPIR-V blob and
the executable were deployed; no DX12 shader was compiled or copied.

STEP 6 DISPOSITION
------------------
Material-table identity, bindless row selection, live dynamic-record ownership,
and authoritative GPU-output UV parity are accepted. The canonical producer no
longer depends on a merged triangle offset to select its matrix. Step 7 DI/GI/
RR geometry-input comparison is next. Production traversal/emissive
publication remains owned by step 8.
