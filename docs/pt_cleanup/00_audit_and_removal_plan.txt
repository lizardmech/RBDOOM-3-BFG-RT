PT GPU-SIDE CLEANUP: AUDIT OF DEFUNCT SHADER/C++ SURFACES AND PHASED REMOVAL PLAN
==================================================================================
Date: 2026-07-19
Reviewed against branch restir-development: 2026-07-21
Scope: neo/shaders/builtin/pathtracing/** and the NVRHI C++ files that drive them.
Goal: retire defunct render lanes (debug mode 56 ReSTIR PT lane and friends), keep
CleanRtxdiDiView 16 as the primary renderer, and separate retained basic debug
views from the production ReSTIR PT pathway.

----------------------------------------------------------------------------------
1. HOW THE FRAME IS ROUTED TODAY (ground truth)
----------------------------------------------------------------------------------
Entry: PathTracePrimaryPass (PathTracePrimaryPass.cpp:360-400)
  InitRayTracingSmokeTest -> BuildRayTracingSmokeTestScene (PathTraceSmokeSceneBuild.cpp,
  8,973 lines) -> ExecuteRayTracingSmokeTest (PathTraceSmokeDispatch.cpp:964, the
  function is ~6,400 lines) -> ReadBackRayTracingSmokeTest (PathTraceSmokeReadback.cpp).

Two routes live inside ExecuteRayTracingSmokeTest:

  A) CLEAN ROUTE (production). Gate: r_pathTracingCleanRtxdiDiEnable != 0 and
     r_pathTracingCleanRtxdiDiView in 1..25 (cleanRtxdiDiRouteRequested,
     SmokeDispatch:1203). Pass chain:
       primary-surface producer raygen (dispatch at :2878)
       -> NEE-cache primary-surface update CS
       -> optional clean ReGIR build + external PDFNEE prepass (:3493/:3513)
       -> PSR/glass/transmission producers
       -> CleanDI.0 Initial / CleanDI.1 Temporal / CleanDI.2 Spatial raygen (:4350-4402)
       -> material-feature compose passes (glass, transmission, liquid, decals)
       -> clean ReSTIR GI (PathTraceCleanRestirGi.cpp, dispatched from :4443 lambda)
       -> DI boiling filter, GI sky resolve, particle composite, DLSS-RR
     The clean route RETURNS at SmokeDispatch:4814. Nothing after that line runs in
     production.

  B) LEGACY ROUTE (everything after :4814). Only reachable when the clean route is
     off/invalid. Contains: pdfNee RLU producer, PDFNEE verifier, ReGIR debug raygen,
     the 57-mode monolith dispatch (r_pathTracingDebugMode 0..57, tile + full
     dispatches at :6872/:6885), and the mode-56 standalone producer chain
     (:7123/:7152/:7178: indirect-initial, direct-temporal, direct-spatial,
     reflection, combined-resolve).

Key entanglement: both routes share the same file, the same m_smoke* resource
namespace, the same PathTraceSmokeConstants buffer, the same binding sets, and
scene build has requestedDebugMode == NN gates sprinkled through all 8,973 lines.
PathTraceCVars.cpp declares 500 idCVars; r_pathTracingRestirPTDiDebugView alone
documents 78 values, all of them mode-56-only.

----------------------------------------------------------------------------------
2. SHADER INVENTORY (195 files under builtin/pathtracing)
----------------------------------------------------------------------------------
Compile mechanisms (three, all must be touched when deleting):
  (1) bespoke DXC custom commands in neo/shaders/CMakeLists.txt (raygen + GI CS)
  (2) engine RenderProgs builtins (RenderProgs.cpp:948-952: skinning, DI boiling
      filter, sky cube probe, NEE-cache primary-surface update CS)
  (3) generated material-feature variants via
      cleanroom_rtxdi/pathtrace_clean_rtxdi_di_material_features.cmake
      (glass + transmission producers compile through this)

A. LIVE - production view-16 lane (keep):
   cleanroom_rtxdi/**                       (~13,600 LOC; DI initial/temporal/spatial,
                                             sentinel, glass, transmission, NEE cache,
                                             material adapters, RR guide export)
   cleanroom_common/** EXCEPT restir_pt_*   (clean DI/GI resampling cores, shared math)
   remix_restir_gi/pathtrace_clean_restir_gi*.{rt,cs}.hlsl  (5 files, GI lane)
   remix_restir_gi/restir_gi_temporal_reuse.rt.hlsl (directly included by the
                                             live clean GI raygen; KEEP)
   pathtrace_primary_surface_producer.rt.hlsl (2,238 LOC production prepass)
   pathtrace_material_classifier.hlsli, PathTracePrimarySurface.hlsli,
   PathTraceMaterialFeature*.hlsli, PathTraceMaterialLightTarget.hlsli,
   PathTraceOpaqueDirectMaterial.hlsli, PathTraceOpenPbrEonGgxVndf.hlsli
   pathtrace_sky_environment.hlsli, pathtrace_sky_cube_probe.cs.hlsl,
   pathtrace_sky_surface_resolve.cs.hlsl, pathtrace_skinning.cs.hlsl,
   pathtrace_particle_*.hlsl
   Rtxdi/ shim tree DI/GI/Utils (all now clean shims into cleanroom_common - the
   license remediation is COMPLETE for these; Rtxdi/PT/* shims are legacy-only)
   RtxdiBridge SUBSET actually included by clean lanes:
     RAB_UnifiedLightRecord.hlsli, RAB_LightTarget.hlsli, RAB_NeeCache.hlsli
   remix_bridge SUBSET included by clean GI:
     RAB_GIInitialSampleBridge, RAB_GIReservoirBridge,
     RAB_GITemporalValidationBridge, RAB_SurfaceBridge
     plus RAB_GradientBridge and RAB_TemporalOutputBridge, which are reached
     transitively through RAB_GITemporalValidationBridge (KEEP until that live
     bridge is deliberately refactored).

B. PROVEN DEAD ON DISK - no current includers or compile entries (delete first):
   remix_rtxdi/rtxdi_initial_sampling.rt.hlsl
   remix_rtxdi/rtxdi_gradients.rt.hlsl
   pathtrace_smoke_restir.rt.hlsl
   These three files are the corrected Phase 1 set. Verify with a final grep
   immediately before deletion.

C. LEGACY - compiled but only reachable through the legacy route:
   pathtrace_smoke.rt.hlsl                  5,892 LOC monolith, 57 debug modes +
                                            RB_PT_ENABLE_RESTIR macro forest
   pathtrace_pdf_nee_verifier.rt.hlsl       5,914 LOC near-copy of the monolith
   pathtrace_regir_debug.rt.hlsl            1,611 LOC
   pathtrace_nee_cache_debug.rt.hlsl        1,841 LOC (NEE cache itself is live;
                                            only this debug raygen is legacy)
   pathtrace_restir_combined_resolve.rt.hlsl  952 LOC (mode 56 final resolve)
   10 thin #define wrappers re-including the monolith:
     pathtrace_smoke_mode18_restir_hybrid, pathtrace_smoke_restir_{initial,temporal,
     temporal_shading,attribution,spatial_reservoir,spatial,spatial_attribution,
     combined}, pathtrace_restir_{indirect_initial,direct_temporal,
     direct_spatial_reservoir,reflection}_producer, pathtrace_restir_pdf_nee_rlu_current
   Mode-56 debug hlsli set (~9,460 LOC):
     pathtrace_smoke_rab_*.hlsli, pathtrace_restir_reference_*.hlsli,
     pathtrace_restir_local_*.hlsli, pathtrace_restir_di_initial_debug.hlsli,
     pathtrace_smoke_native_nee_reservoir_preview.hlsli,
     pathtrace_restir_rr_guide_debug.hlsli,
     pathtrace_restir_combined_resolve_reservoir_pages.hlsli
   cleanroom_common/restir_pt_*.hlsli (1,784 LOC) + Rtxdi/PT/* shims (mode-56 PT lane)
   remix_rtxdi/rtxdi_temporal_reuse.rt.hlsl and rtxdi_spatial_reuse.rt.hlsl
     (included by pathtrace_restir_local_debug_reservoirs, which is included by
     pathtrace_restir_combined_resolve; remove with the mode-56 shader slice,
     not as dead-on-disk files)
   RtxdiBridge legacy majority (~everything not in the live subset above:
     RAB_PathTracer*, RAB_Surface, RAB_Brdf*, RAB_ReGIR, RAB_DuplicationMap,
     RAB_LightInfo*, RAB_LightSampling*, Debug/*, PathTraceRtxdiBridge*, etc.)
     NOTE: RAB_LightTarget includes PathTraceMaterialLightTarget ->
     PathTraceOpaqueDirectMaterial -> PathTraceOpenPbrEonGgxVndf; that chain is live.
   pathtrace_nee.hlsli + pathtrace_emissive_sampling.hlsli are included ONLY by the
     monolith + verifier -> legacy once those go (confirm at deletion time).

D. RETAINED DEBUG CANDIDATES (currently inside the monolith):
   Basic geometry/material/motion views worth keeping per user intent:
     modes 0-17 (hit/miss, depth, normals, surface class, UV, material ID/table,
     textures, albedo, translucency, normal/specular map)
     modes 21-25 (bounds boxes, routed-rigid TLAS validation)
     modes 38-49 (motion-vector / reprojection diagnostics)
     mode 52 (routed-rigid transform parity), mode 57 (material classifier GPU route)
   ReSTIR-PT-specific modes 18-20, 26-37, 50-51, 53-56 are defunct with the lane.

----------------------------------------------------------------------------------
3. C++ INVENTORY
----------------------------------------------------------------------------------
SHARED LEGACY-DEBUG modules (not removable with mode 56 alone):
   PathTraceRestirPT.cpp/.h, PathTraceRestirPTReservoirs.cpp/.h,
   PathTraceRestirPasses.h, PathTraceRestirPTParameters.h
     The 2026-07-21 execution audit disproved the original legacy-only claim.
     Modes 26-33, 50-51, 53-55 and the optional mode-18 ReSTIR hybrid still use
     the pass plan, context parameters, buffer-page selection, and prepass flags.
     Keep these modules until those consumers have their own removal gates.
   PathTraceReGIR.cpp/.h is NOT legacy-only. The clean route owns and allocates
     its state and can dispatch its build shader. Keep the module and separate
     its production/build path from the optional debug presentation later.
   Mode-56 sections of PathTraceSmokeReadback.cpp (view 68/69 dumps, :473-495)
   Large parts of PathTraceDebugDumps.cpp / PathTraceSurfaceDebugDumps.cpp keyed to
     removed modes (audit per-function at execution time)
   Legacy cases inside InitRayTracingSmokeRestirPipeline and their matching
   m_smokeRestir* shader tables/pipelines in PathTraceSmokeResources.cpp.
   The initializer itself is shared: cases 9, 15, 17, 18, and 20 service the
   primary-surface producer, Clean DI, ReGIR, external-PDFNEE, and Clean DI
   spatial paths and must remain until they are mechanically renamed/split.

LIVE but ENTANGLED (keep, do not delete; needs untangling only):
   PathTraceSmokeDispatch.cpp - production dispatch lives here (lines ~1188-4814)
   PathTraceSmokeSceneBuild.cpp - one scene builder for both routes; debugMode gates
     (e.g. :3907 enableTextureProbe chain, :5332 rigid-route enable) must collapse to
     clean-route policy once modes die
   PathTraceSmokeResources.cpp - shared m_smoke* buffers/textures
   PathTraceRestirLightManager.cpp - RLU emissives feed the clean route
     (cleanRtxdiDiSceneBuildRluEmissives, SceneBuild:3891) and NEE cache; live
   PathTraceNeeCache.cpp - live (clean NEE cache build prepass)
   PathTraceCleanRestirGi.cpp - already well isolated; model to imitate
   PathTraceDLSSRRBridge.cpp - live for the clean lane despite "mode 56" wording in
     its cvar help strings
   PathTraceCVars.cpp - 500 cvars; large minority are mode-56/verifier-only

----------------------------------------------------------------------------------
4. PHASED REMOVAL PLAN (checkpoint after each meaningful slice)
----------------------------------------------------------------------------------
Verification per phase: Vulkan only. Force affected SPIR-V recompilation by
deleting the corresponding .rt.bin before building whenever an edited include or
raygen may not retrigger DXC. Build with cmake --build --preset
win64-pt-dev-release, confirm each rebuilt SPIR-V blob is newer than its source
edit, deploy only RBDoom3BFG.exe plus rebuilt files under base/renderprogs2/spirv,
then launch a map, confirm view 16 renders identically, confirm one retained debug
view still works, and skim the console for new warnings. DX12/DXIL compilation is
not an acceptance requirement for this Vulkan-only branch.

PHASE 0 - decisions (no code):
  D1: final keep-list of basic debug views (proposal in section 2D).
  D2: KEEP PathTraceReGIR. It participates in clean-route resource/build plumbing.
      Keep pathtrace_nee_cache_debug as runway unless a later isolated audit proves
      its presentation shader has no value. Move optional debug presentation
      behind its own cvars, out of the monolith.
  D3: which CleanRtxdiDiView bring-up proofs (views 1-11, 13-15) to prune from the
      clean shaders themselves; 12/16/17-25 stay.

PHASE 1 - delete only the three proven dead-on-disk shaders in section 2B. No
  CMake edits are expected. This is low risk, not "zero risk": grep immediately
  before deletion and run the normal Vulkan build afterward.

PHASE 2 - remove the mode-56 ReSTIR PT lane, C++ first:
  T2.1: delete the standalone producer chain dispatch block (SmokeDispatch
        ~:7100-7205) and mode56 accumulation plumbing (:5578-5600, FrameResources
        fields, r_pathTracingRestirPTMode56Accumulation*).
  T2.2: retire mode 56 at the C++ route boundary; remove its pass-plan case,
        combined-mode dispatch/resource selection, pass-plan logging fields that
        are mode-56-only, and view68/69 readback. Preserve the shared pass-plan
        and UpdateRestirPTContextState path for retained legacy-debug consumers.
  T2.3: delete the four shared ReSTIR PT modules only after modes 18-20, 26-37,
        50-51, and 53-55 have separately been removed or proven independent.
        Until then, limit this phase to mode-56-only wrappers/resources.
  T2.3b (LEGACY RESOURCE CLEANUP): delete PathTraceRemixRtxdiResources.cpp/.h only
        after proving every clean-route reference safely falls back to the owned
        frame resources. The <Rtxdi/...> host headers now resolve to rbdoom-owned
        files under neo/Rtxdi; external RTXDI runtime severance was already
        completed on 2026-06-14. Do not describe this as a new license win and do
        not make the containment allow-list empty while active local host-ABI
        includes remain.
  T2.4: prune only legacy cases 0-8 and 10-14 plus their matching
        m_smokeRestir* pipelines/shader tables/buffers. Keep shared initializer
        cases 9, 15, 17, 18, and 20. Rename or split the survivor later, after
        call sites are stable.
  T2.5: delete r_pathTracingRestirPT* cvars (approx 80+) incl. the 78-value
        r_pathTracingRestirPTDiDebugView, RrxDebug* bypass family, spatial
        diagnostic cvars.
  T2.6: delete shader wrappers + mode-56 debug hlsli set + combined_resolve +
        cleanroom_common/restir_pt_* + Rtxdi/PT shims; remove their CMake custom
        commands and include-list entries. This is where the legacy
        remix_rtxdi temporal/spatial reuse files and their now-unreferenced bridge
        dependencies are reconsidered. Keep the live clean-GI temporal file and
        its Gradient/TemporalOutput bridge chain.

PHASE 3 - strip the monolith to the retained debug views:
  T3.1: remove all RB_PT_ENABLE_RESTIR* regions and modes 18-20/26-37/50-51/53-56
        from pathtrace_smoke.rt.hlsl; drop now-unused includes (pathtrace_nee,
        pathtrace_emissive_sampling, remaining RtxdiBridge legacy headers).
  T3.2: the standalone pathtrace_pdf_nee_verifier.rt.hlsl library was already
        deleted in commit 0fe6147aa. The surviving verifier-named CVars now drive
        the live RLU-current producer used by the clean route; preserve that
        producer and its controls until a deliberate rename or retirement.
        Remove legacy bridge headers only after their last includer disappears.
  T3.3: rename survivor to pathtrace_debug_views.rt.hlsl (optional but clarifying);
        clamp r_pathTracingDebugMode range to the keep-list and renumber only if
        cheap (renumbering breaks muscle memory - consider keeping numbers and
        rejecting removed ones with a console message).

PHASE 4 - structural separation (production vs debug):
  T4.1: split ExecuteRayTracingSmokeTest: extract the clean route (lines ~1188-4814
        today) into ExecutePathTraceCleanFrame in its own file
        (PathTraceCleanFrame.cpp); ExecuteDebugViewFrame keeps the monolith
        dispatch. Entry picks one route; no shared 6,000-line body.
  T4.2: collapse requestedDebugMode gates in SceneBuild to two policies
        (production policy = current cleanRtxdiDiSceneBuildRluEmissives behavior;
        debug policy = what the keep-list needs).
  T4.3: prune cvars again (bring-up proofs from D3, dead diagnostics); goal is to
        cut PathTraceCVars.cpp roughly in half.
  T4.4 (RESTIR HYGIENE - key for Enhanced ReSTIR PT upgrades): split the clean-lane
        debug views out of the production shader binaries via compile-time
        permutations. Today the DI initial/temporal wrappers #include the 2,561-line
        sentinel and the view is a RUNTIME uint switch (sentinel:2489-2553 is a
        25-way else-if chain); the production view-16 binary statically carries all
        25 views, temporal_output.hlsli (1,362 LOC viewer), diagnostics.hlsli (616),
        rr_debug.hlsli (318) and the audit/GUI counter plumbing. Because raygen
        occupancy is the measured GI bound, dead branches cost real registers.
        Mechanism (matches existing #define-entry + material-features .cmake
        pattern):
          - compile each clean DI/GI/spatial raygen twice: production permutation
            with CLEAN_DI_VIEW_STATIC=16 (view is a literal; DXC dead-strips other
            branches; debug hlsli includes #if'd out) and a debug permutation that
            keeps the runtime switch unchanged;
          - C++ selects the shader table by view: 16 -> production, else -> debug;
          - temporal-audit readback + GUI snapshot stay debug-permutation-only;
          - resampling math stays in cleanroom_common (view-free) - that is the
            surface where paper upgrades (shift mappings, MIS changes) land.
        Do this AFTER Phase 2/3 so the shader CMake is already minimal.
  T4.5 (optional, last): mechanical rename of the SmokeTest/m_smoke* naming to
        PathTraceFrame naming - huge diff, do only when the tree is otherwise quiet.

Ordering rationale: files that nothing compiles go first (free); mode-56 C++ next
because every branch is explicitly keyed on debugMode==56/restirPTCombinedMode and
is after the production early-return (mechanical, low risk); the monolith strip
only after its wrappers are gone; the structural split last, when the code that
would have to move is already minimal.

Estimated removal: ~35-40k LOC of HLSL (monolith regions, verifier, debug hlsli,
PT lane, dead bridges) plus several thousand lines of C++ and ~150+ cvars.

----------------------------------------------------------------------------------
5. 2026-07-21 VIABILITY REVIEW AND RECENT-FEATURE GUARDRAILS
----------------------------------------------------------------------------------
Verdict: the overall cleanup direction is viable after the corrections above.
The original Phase 1 and wholesale pipeline-removal wording was not viable.

Live-contract corrections proven by the include/call graph:
  - remix_restir_gi/pathtrace_clean_restir_gi.rt.hlsl directly includes
    restir_gi_temporal_reuse.rt.hlsl.
  - that temporal file reaches RAB_GITemporalValidationBridge, which reaches
    RAB_GradientBridge and RAB_TemporalOutputBridge.
  - pathtrace_restir_combined_resolve includes
    pathtrace_restir_local_debug_reservoirs, which reaches the remix_rtxdi
    temporal/spatial reuse files. Those files are legacy, but not dead-on-disk.
  - the clean route calls InitRayTracingSmokeRestirPipeline for live shader kinds;
    remove legacy cases, not the shared function.
  - PathTraceReGIR state is created and consumed before the clean-route early
    return. Keep the module; separate optional debug presentation later.

Last-24-hour feature audit (nine commits, all liquid-pool work):
  - production ownership lives in the retained primary-surface producer,
    cleanroom_rtxdi DI adapters/producers/spatial shader, clean ReSTIR GI shader,
    material-feature ABI/parameters, draw-surface capture, and liquid modifier.
  - PathTraceSmokeReadback contains new clean liquid-pool probe selection and
    reporting. When deleting view-68/69 mode-56 readback, preserve the liquid
    globals, status-buffer readback, probe scan, and printout.
  - PathTraceCVars contains the new default r_pathTracingLiquidPoolMode=3 plus
    liquid debug/page controls. CVar pruning must preserve all three.
  - pathtrace_restir_combined_resolve received routed-rigid liquid support in
    commit b7c30aae. Deleting the defunct mode-56 lane intentionally deletes
    that legacy copy; it must not be mistaken for the production implementation.
  - no cleanup slice may delete or rename shared liquid material ABI fields,
    LiquidPoolInfo constant transport, primary-surface liquid status buffers, or
    the clean DI/GI modifier include chain without a separate feature audit.

Working rule for every deletion slice:
  1. enumerate direct and transitive includers/callers;
  2. distinguish production clean-route use from legacy mode-56 use;
  3. list any symbol/file changed during the preceding 24 hours;
  4. delete only the proven legacy surface;
  5. force affected Vulkan blobs, build, verify mtimes, deploy, runtime-check;
  6. checkpoint the meaningful slice without staging unrelated worktree files.

----------------------------------------------------------------------------------
6. EXECUTION LOG
----------------------------------------------------------------------------------
2026-07-21 - CORRECTED PHASE 1 COMPLETE - commit 214e0ab0b
  - Added this reviewed cleanup packet to source control.
  - Deleted only the three proven unreferenced shader files from section 2B.
  - Final reference sweep found no surviving non-document references.
  - win64-pt-dev-release build passed; no shader blob rebuild was required because
    none of the deleted files participated in the configured compile graph.

2026-07-21 - T2.1a MODE-56 ACCUMULATION PLUMBING COMPLETE
  - Removed r_pathTracingRestirPTMode56Accumulation and MaxFrames.
  - Removed the mode56 accumulation signature/frame fields and reset/update code.
  - Removed host constant transport and the combined-resolve shader accumulation
    functions. Mode-18 toy accumulation remains unchanged.
  - Forced pathtrace_restir_combined_resolve.rt.bin SPIR-V rebuild; blob mtime
    2026-07-21 11:02:40 is newer than shader edit 2026-07-21 10:56:20.
  - Full win64-pt-dev-release build passed. DXIL RT shaders remained disabled.
  - Runtime acceptance 2026-07-21: user confirmed the deployed build looks fine
    in game. This closes the view-16/recent-feature regression gate for commits
    214e0ab0b and 69658715c.
  - Next slice: T2.1b standalone mode-56 producer dispatch/table plumbing. Do not
    remove the shared primary-surface producer or clean-route initializer cases.

2026-07-21 - T2.1b STANDALONE MODE-56 PRODUCER PLUMBING COMPLETE
  - Removed dedicated indirect-initial, direct-temporal,
    direct-spatial-reservoir, and reflection shader-library/pipeline/table
    handles plus initializer cases 11-14.
  - Removed the dedicated reflection dispatch and its mode-56-only CVar.
  - Retained the generic initial/temporal/spatial prepass tables used by other
    debug modes. Mode 56 can no longer select any of the removed dedicated
    producer binaries.
  - Re-proved clean/shared initializer cases 9, 15, 17, 18, and 20 and their
    clean-route call sites after the edit.
  - Full win64-pt-dev-release build passed. No shader source changed, so no
    SPIR-V blob needed regeneration for this slice.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    18cf82adb. T2.1b runtime gate closed.
  - T2.2a removed the four mode-56 view-68/69 dump CVars, their dispatch-entry
    logging and forced-readback scheduling, and the dedicated CPU color/tuple
    classifiers, ROI scans, and terminal reports in PathTraceSmokeReadback.cpp.
  - Re-proved that the retained readback paths still cover liquid-pool probes,
    rigid-route overlap, clean temporal audit, and the generic smoke readback.
  - Full win64-pt-dev-release build passed. No shader source changed and the
    build reported all SPIR-V shaders up to date, so no blob deployment is due.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    452f2cebe. T2.2a runtime gate closed.
  - T2.2b audit found the pass-plan/context deletion proposed above was not
    viable as written: modes 26-33, 50-51, 53-55 and mode-18 hybrid still consume
    it for parameters, reservoir-page selection, and prepass scheduling.
  - T2.2b therefore seals only the retired mode-56 route: debug value 56
    normalizes to production value 0 at frame, scene-build, dispatch, and
    readback boundaries; mode 56 is removed from ReSTIR debug classification,
    pass-plan construction, and debug metadata.
  - Full win64-pt-dev-release build passed. No shader source changed and all
    SPIR-V shaders remained up to date, so this checkpoint deploys the EXE only.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    20b0cf4d6. T2.2b runtime gate closed.
  - T2.2c removed the unreachable combined and combined-resolve shader-library,
    pipeline, and shader-table handles; initializer cases 8 and 10; combined
    state selection; mode-56-only DI temporal debug prepass; Remix DI clear;
    and the duplicate legacy DLSS-RR evaluation block. The clean-route DLSS-RR
    evaluation before its early return remains intact.
  - Shared primary-surface, GI-initial, direct-temporal, and direct-spatial
    prepasses remain for mode 18 and modes 50-55; their stale PT56 marker labels
    were made route-neutral.
  - Full win64-pt-dev-release build passed. No shader source changed and all
    SPIR-V shaders remained up to date, so this checkpoint deploys the EXE only.
  - Runtime acceptance 2026-07-21: clean RTXDI DI primary view 16 had no issues
    on deployed commit e8b44a8ae. Modes 50 and 53 rendered purple, but the user
    explicitly classified those legacy diagnostics as non-blocking and placed
    the preservation priority on clean RTXDI DI plus still-useful basic modes
    around 1-18. Future cleanup does not need to preserve modes 50/53.
  - T2.2d removed all six now-unloadable mode-56 shader libraries from the build:
    combined, combined-resolve, indirect-initial producer, direct-temporal
    producer, direct-spatial-reservoir producer, and reflection producer. It also
    deleted the two include files owned only by combined-resolve.
  - Both Vulkan and dormant DXIL CMake declarations/commands/output-list entries
    were removed, but only the Vulkan build is validated or deployed. Existing
    orphan blobs are inert because no C++ loader remains; do not copy them.
  - Full win64-pt-dev-release configure/build passed. CMake graph regeneration
    rebuilt six still-active SPIR-V libraries: smoke core, primary-surface,
    PDFNEE verifier, PDFNEE RLU-current, ReGIR debug, and NEE-cache debug. Their
    blob mtimes are 2026-07-21 12:46:12 through 12:47:13 and they are the only
    shader artifacts due for deployment; no DXIL artifact is copied.
  - Runtime acceptance 2026-07-21: user reported no issues with clean RTXDI DI
    after deployed commit c29da84b9. T2.2d runtime gate closed.
  - T2.2e applies the agreed preservation boundary: ReSTIR diagnostic modes
    26-33, 50-51, and 53-55 now normalize to production mode 0 at the same frame,
    scene, dispatch, and readback boundaries as retired mode 56. Non-ReSTIR
    diagnostics and ordinary mode 18 remain selectable.
  - The optional ReSTIR direct-lighting overlay on mode 18 was severed while
    native mode-18 NEE remains. Its route override, pipeline initialization,
    pass-plan mutation, analytic-light overrides, and three dedicated CVars were
    removed. Legacy resources remain allocated/compiled until the next slice.
  - Full win64-pt-dev-release build passed. No shader source changed and all
    SPIR-V shaders remained up to date, so this checkpoint deploys the EXE only.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    41fcec790. T2.2e runtime gate closed.
  - T2.2f removes initializer cases 0-7 and the matching shader-library,
    pipeline, and shader-table handles. It also removes the unreachable legacy
    state selection plus initial, temporal, and spatial prepass dispatches that
    depended on those tables.
  - Deleted the eight retired wrapper sources (mode-18 hybrid; initial;
    temporal; temporal-shading; attribution; spatial-reservoir; spatial; and
    spatial-attribution) and removed their Vulkan and dormant DXIL CMake graph.
    The monolithic shader's now-unreachable internal branches remain for a
    separate audited strip; this slice does not modify pathtrace_smoke.rt.hlsl.
  - Full win64-pt-dev-release configure/build passed. No shader source in the
    active graph changed and the confirming build reported all SPIR-V up to
    date, so no shader artifact is due for deployment. DXIL was not compiled or
    deployed; this checkpoint deploys the EXE only.
  - Runtime acceptance 2026-07-21: user reported no problems with deployed
    commit ae2d80bf8. T2.2f runtime gate closed.
  - The T3.1 monolith pre-strip audit found one remaining live consumer of its
    general ReSTIR region: pathtrace_restir_pdf_nee_rlu_current.rt.hlsl defines
    RB_PT_ENABLE_RESTIR and includes pathtrace_smoke.rt.hlsl. Native mode 18
    also shares the adjacent pathtrace_nee/toy-path block. Do not bulk-delete
    either region; first isolate the RLU-current producer or remove only macro
    branches proven absent from both preprocessed active libraries.
  - T2.2g removes the pass-plan seam that was retained for modes 26-33, 50-51,
    and 53-55. PathTraceRestirPasses.h now contains only route normalization;
    the obsolete pass enums, feature descriptions, builders, buffer selection,
    dump path, and preview-visibility/pass-dump CVars are gone.
  - The surviving legacy debug dispatch still initializes its existing local
    ReSTIR context with an explicit resamplingMode=None descriptor. This keeps
    ordinary debug modes and native mode 18 on the same resource contract while
    removing the retired mode-dependent policy layer.
  - Full win64-pt-dev-release build passed. The build reported all SPIR-V up to
    date, so no shader artifact is due for deployment; this checkpoint deploys
    the EXE only. The two C4819 warnings are pre-existing encoding warnings in
    RenderBackend.cpp and PathTraceSmokeDispatch.cpp, not cleanup failures.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    a86f69f5a. T2.2g runtime gate closed.
  - T2.2h removes the constant-false staged direct/GI domains, sparse producer
    dimensions, legacy primary-surface prepass dispatch, mode-56 skinned-history
    request, and four dedicated scale/sparsity/prepass CVars. The legacy shader
    constants now receive the same explicit default values the dead path had
    produced: full resolution, no staging, sparsity 1, and no indirect producer.
  - The shared primary-surface producer is deliberately retained. The clean
    route still initializes case 9 and dispatches its shader table before the
    clean early return; only the unreachable second dispatch was removed.
  - The first compile exposed one stale rrGuideHistory field in the skinned
    diagnostic print. Removing that field completed the mode-56 scene-build
    severance; the subsequent full win64-pt-dev-release build passed. All
    SPIR-V remained up to date, so this checkpoint deploys the EXE only.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    c4fe6bad9. T2.2h runtime gate closed.
  - The next context/resource audit found that b28 and the legacy PT reservoir
    buffer are still part of the shared monolith/PDF-verifier binding ABI. They
    cannot be deleted before the remaining legacy shader consumers are retired.
  - T2.2i therefore completes the route prerequisite: modes 19-20 and 34-37 now
    normalize to production mode 0 alongside the previously retired ReSTIR
    modes. Native mode 18 and modes 1-17 remain selectable. Remove their C++
    policy, CVars, shader branches, and context/resource consumers only after
    this route checkpoint passes runtime validation.
  - Full win64-pt-dev-release build passed. All SPIR-V remained up to date, so
    this route-only checkpoint deploys the EXE only.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    0c8f96d70. T2.2i runtime gate closed.
  - T2.2j removes the newly unreachable mode 19-20, 26-37, 50-51, and 53-56
    selection branches from the monolithic smoke shader. Native mode 18 keeps
    EvaluateSmokeToyPathTrace and its material/integrator controls; clean DI,
    PDFNEE RLU-current, ReGIR, and NEE-cache library-kind values 19/20 are not
    debug-mode identifiers and remain untouched.
  - Matching host policy is gone: retired debug metadata, mode-20 preset/rigid
    route CVars, false ReSTIR/integrator mode tests, mode-specific texture and
    emissive setup, the mode-56 RLU request, and stale route labels/descriptions
    were removed. Draw-surface capture now consumes normalized debug values at
    its boundary, consistent with scene build and dispatch.
  - The first compile found three stale PDFNEE dump references to the deleted
    mode-56 guard; removing those diagnostic-only fields completed the host
    severance. The subsequent full win64-pt-dev-release build passed with only
    the pre-existing C4819 warning in PathTraceSmokeDispatch.cpp.
  - The edited monolith forced exactly two active Vulkan outputs to rebuild:
    pathtrace_smoke.rt.bin (1,288,320 bytes, 2026-07-21 18:02:09) and
    pathtrace_restir_pdf_nee_rlu_current.rt.bin (650,244 bytes,
    2026-07-21 18:01:51). Both blob mtimes are newer than the source edit at
    17:57:44. No DXIL shader was compiled or selected for deployment.
  - The separate active pathtrace_pdf_nee_verifier.rt.hlsl source still carries
    its own copied legacy visualization cascade. Do not treat the monolith strip
    as proof that those definitions are deletable there; audit that verifier's
    dedicated entry contract before a later removal. The monolith also retains
    ReSTIR helper definitions needed by RLU-current or gated by its standalone
    build; the next shader slice must classify those definitions by preprocessed
    consumer rather than bulk-delete the region.
  - Runtime acceptance 2026-07-21: user reported no issues in clean DI and old
    native mode 18 on deployed commit 50bdf39a6. T2.2j runtime gate closed.
  - T2.2k corrected the verifier classification above: the standalone
    pathtrace_pdf_nee_verifier route is hard-disabled by a literal `false &&`
    at its only request boundary. Its case-16 initializer is referenced only by
    that unreachable branch. The live PDFNEE implementation is the separate
    case-18 pathtrace_restir_pdf_nee_rlu_current producer; it shares the PDFNEE
    binding layout but none of the verifier library/pipeline/shader-table handles.
  - Delete the 5.9K-line copied verifier source, case-16 loader, three dead GPU
    handles, and its Vulkan plus dormant DXIL CMake commands/output entries.
    Preserve m_smokePdfNeeVerifierBindingLayout because the live case-18 RLU
    current producer and its binding sets still consume that ABI. Existing
    verifier blobs become inert orphan artifacts and are not deployed.
  - Full win64-pt-dev-release configure/build passed. All active SPIR-V and
    dormant DXIL outputs reported up to date, so no shader artifact changed or
    is due for deployment. Only the rebuilt EXE is deployed. The two C4819
    warnings remain the pre-existing encoding warnings in RenderBackend.cpp and
    PathTraceSmokeDispatch.cpp.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    0fe6147aa. T2.2k runtime gate closed.
  - T2.2l collapses the remaining host policy for that deleted standalone
    verifier. The literal-false route and its mode-56 guards, split-RAB source
    override, ReGIR source/prepass bridge, resource/binding alternatives, and
    obsolete fallback diagnostic body are removed from PathTraceSmokeDispatch.
  - The live pathtrace_restir_pdf_nee_rlu_current producer remains independently
    requested by r_pathTracingRestirPdfNeeVerifierEnable and retains its case-18
    shader table, PDFNEE binding layout, current-RLU source policies, and dump.
    Clean RTXDI DI, ReGIR debug, NEE-cache, and native mode 18 keep their own
    route/resource policy without the deleted verifier's constant-false gates.
  - Full win64-pt-dev-release build passed. All SPIR-V outputs were already up
    to date, so no shader blob is due for deployment and DXIL was not selected
    for deployment. The rebuilt EXE was copied to the prebuilt runtime; source
    and destination SHA-256 both equal
    20134E303B8867186E5447F606C422658B6693C751B4E28E04E5FA2A5B87B7CD.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    a9db7c05a. T2.2l runtime gate closed.
  - T2.2m removes the standalone verifier's final View, LightMode, and Domain
    recipe CVars. Their remaining consumers were stale early-return text, the
    discarded split-domain dump, and a superseded scene-build emissive policy;
    none fed the live RLU-current shader. docs/pathtracing.txt now describes the
    five surviving public controls as the live RLU-current producer contract.
  - The shared constant-buffer layout is deliberately unchanged because fixed
    pack offsets in the other shader libraries depend on it. The unused verifier
    metadata float4 is now an explicit reserved slot, while the adjacent live
    vector is named RestirPdfNeeRluCurrentControlInfo on both CPU and HLSL sides.
    It still carries samples, visibility, source policy, and NEE-cache-provider
    readiness at the same offsets.
  - Full win64-pt-dev-release build passed with the two pre-existing C4819
    warnings. Forced Vulkan rebuilds produced pathtrace_smoke.rt.bin (1,288,324
    bytes, 2026-07-21 18:59:07) and
    pathtrace_restir_pdf_nee_rlu_current.rt.bin (650,248 bytes,
    2026-07-21 18:58:53), both newer than the shader edit at 18:57:47. No DXIL
    shader rebuilt. The EXE and only those two SPIR-V files were deployed and
    source/destination SHA-256 pairs match.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    3f4c6c7d6. T2.2m runtime gate closed.
  - T3.1a severs the live RLU-current wrapper from the broad
    RB_PT_ENABLE_RESTIR monolith permutation. An audit-only wrapper without that
    define compiled successfully; stripped and compacted SPIR-V differed from
    the prior output only in the declaration order of integer constants 11 and
    12, with identical size, entry points, instructions, and bindings.
  - With that proof in hand, remove the monolith-only PT parameter/reservoir
    resources, PathTracer bridge and restir_pt algorithm region, its fallback
    stubs, the retired mode-18 ReSTIR override, and the direct/indirect/sparse
    producer selection that host policy now fixes off. Preserve native mode-18
    pathtrace_nee, the producer-specific RLU-current region, RAB material/light
    replay, and primary-surface history writes used by the two surviving entry
    paths. The similarly named macro around the separate primary-surface
    supplier remains live and is not part of this monolith deletion.
  - Full win64-pt-dev-release build passed. The edited shader forced exactly two
    Vulkan rebuilds: pathtrace_smoke.rt.bin (1,265,600 bytes,
    2026-07-21 19:20:36) and pathtrace_restir_pdf_nee_rlu_current.rt.bin
    (630,276 bytes, 2026-07-21 19:20:24). Both are newer than their source edits;
    DXIL remained up to date. The EXE and only those two SPIR-V files were
    deployed, with source/destination SHA-256 pairs matching.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    b53b98b9a. T3.1a runtime gate closed.
  - T3.1b follows the now-severed include graph instead of deleting by filename
    family. The 31-file candidate set has 32 internal include edges and zero
    incoming includes from any surviving HLSL/HLSLI source. No host or CMake
    source names an individual candidate. All candidates are tracked and none
    carried local modifications before deletion.
  - Delete the closed 10,960-line / 416,032-byte island: the six retired ReSTIR
    diagnostic headers, nine cleanroom restir_pt algorithm headers, eight
    Rtxdi/PT forwarding shims, two old Remix temporal/spatial reuse sources,
    the obsolete umbrella PathTraceRtxdiBridge and MIS callback, and its four
    PathTracer bridge headers. These sources only referenced members of the
    same dead island after T3.1a removed their final live root.
  - Preserve the live smoke RAB policy/material/visibility/surface/motion and
    environment suppliers, the native NEE reservoir preview, the RLU-current
    light/material replay bridge, and the separate primary-surface producer's
    surface/motion supplier path. Similar RAB and ReSTIR names are not evidence
    that those surviving contracts are dead.
  - The first post-delete build passed but exposed stale MSBuild dependency
    lists from the already-generated project, producing MSB8064 warnings for
    removed files. Regenerating with `cmake --preset win64-pt-dev` refreshed the
    recursive shader-include glob; the subsequent full
    win64-pt-dev-release build passed without missing-dependency warnings.
  - All active SPIR-V remained up to date after regeneration, so no shader blob
    is due for deployment and no DXIL artifact was selected. Only the rebuilt
    EXE was copied to the prebuilt runtime. Source and destination SHA-256 both
    equal B6411F5114D47B57ED8B72A989D14BC18D11FA7630D3F06C22B52A93195B8715.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    e69ab8f73. T3.1b runtime gate closed.
  - T3.1c removes the host resource island left after the PT shader bridge was
    severed. A full shader-tree audit found no declarations at b28, u29, u55,
    u56, or u68, while the host still allocated three full-resolution packed
    reservoir sets, uploaded a dedicated PT parameter constant buffer, and
    added all five dead slots to shared and producer-specific binding sets.
  - Remove those allocations, descriptors, state transitions, unreachable clear
    path, validity gates, resize/reset counters, and diagnostics. Delete the
    now-unreferenced PathTraceRestirPT context/parameter implementation and
    retain only primary-surface history ownership in
    PathTraceRestirPTReservoirs. Six temporal/spatial CVars whose sole consumer
    was the deleted parameter packer are removed with it.
  - Preserve the live smoke reservoirs u18-u20, primary-surface history u30-u31,
    clean DI reservoirs u69-u71, current-RLU output u69, Remix RTXDI resource
    domains, and reflection/sidecar texture u47. This slice changes no shader
    source or constant-buffer layout.
  - The first compile identified two stale checkerboard arguments in the frame
    resize implementation. Removing those completed the type severance. The
    subsequent full win64-pt-dev-release build passed; all SPIR-V remained up
    to date, so only the EXE was deployed. Source and destination SHA-256 both
    equal A81348C1C59DE531CB7D82FAD49A6601DACF29EBE6BE0310341CB3A39B2BD69D.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    0cd1fdcd1. T3.1c runtime gate closed.
  - T3.1d removes the final compile-time direct-temporal producer alternative
    from the smoke monolith. No source defines its macro and its former wrapper,
    CMake command, pipeline, and shader table were removed in earlier gates.
    The adjacent indirect-producer/sparse helper island likewise had no calls
    outside its own definitions. Remove both islands and the now-pointless
    dispatch-lambda parameters that only wrote their control fields.
  - Preserve constant-buffer offsets used by standalone ReGIR and NEE-cache
    libraries: the former RestirPTIndirectInfo slot is renamed as reserved on
    the CPU, smoke, and primary-surface-producer sides rather than deleted.
    Live primary-history direct/full mapping is intentionally retained for the
    next proof. Also remove the two PT CVars with no consumers at all:
    CombinedSpatialRadius and GiDebugView.
  - Forced Vulkan rebuilds produced smoke (1,265,608 bytes, 20:13:54),
    primary-surface producer (843,028 bytes, 20:13:35), and RLU-current
    (630,284 bytes, 20:13:40). All blob mtimes are newer than the 20:11-20:12
    shader edits; DXIL stayed current. Only those three SPIR-V files and the EXE
    were deployed, and every source/destination SHA-256 pair matches.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    e707fbb8e. T3.1d runtime gate closed.
  - T3.1e collapses the remaining direct/sparse coordinate scaffold. Host policy
    always populated full-frame dimensions, dispatch mode 0, and sparsity off;
    all seven alternate dispatch enum values had lost their producers in prior
    gates. Under that fixed contract, smoke primary-surface store/load mapping
    is identity.
  - Preserve the primary-surface store/load interface used by the shared record
    helpers and preserve the separate primary-surface producer's live tiled
    store coordinate. The former RestirPTDirectInfo and RestirPTSparsityInfo
    slots remain in place as named reserved float4s on CPU and both shaders so
    standalone ReGIR/NEE-cache constant-buffer packoffsets do not move.
  - Remove the seven-value host dispatch enum, its always-zero argument and
    writes, the fixed direct/sparsity constant initialization, and the shader
    helpers that implemented inactive direct-resolution and sparse mapping.
    Static search leaves only the three matching reserved-slot declarations.
  - The first short-lived build command continued after its caller timed out;
    an overlapping invocation reached the shader stage but then found the first
    compiler process holding PathTraceSmokeDispatch.obj. After that process
    exited, a serialized full win64-pt-dev-release build passed.
  - Forced Vulkan rebuilds produced smoke (1,251,932 bytes, 20:31:17),
    RLU-current (628,612 bytes, 20:31:03), and primary-surface producer
    (843,044 bytes, 20:30:54). All are newer than the 20:30:04-20:30:05 source
    edits; DXIL remained up to date. Only those three SPIR-V files and the EXE
    were deployed. Every source/destination SHA-256 pair matches; the EXE hash
    is BC141A2F94C36A94D98BE55989A5EE1EAB6B2C3A5E19CFB8195281B7A566EDE9.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    99e44d8b6. T3.1e runtime gate closed.
  - T3.1f audits the remaining ReSTIR diagnostic transport rather than following
    the stale T3.2 filename plan. The standalone verifier shader is already gone;
    its surviving controls belong to the live RLU-current clean-route producer
    and are explicitly preserved.
  - Four consecutive smoke constant blocks have declarations but zero shader
    reads: old DI debug flags, Remix DI reservoir geometry/pages, and GI final-
    consumer flags. Preserve their ABI offsets as reserved slots, remove their
    host population and 16 sole-purpose CVars, and keep all clean DI constants,
    RLU controls, RAB material/light inputs, and mode-18 integrator controls.
  - The small Remix RTXDI resource-gate helper is also closed: its required
    combined-mode field is never set after that producer's removal, so every
    diagnostic request and clear-source query is constant false/none. Delete the
    helper while retaining the explicit resource enable/dump controls and the
    resource owner itself for a later independent T2.3b audit.
  - Regenerate win64-pt-dev after deleting the gate source, then the full
    win64-pt-dev-release build passes. Forced Vulkan rebuilds produce smoke
    (1,251,964 bytes, 20:44:28) and RLU-current (628,644 bytes, 20:44:12), both
    newer than the 20:43:09 shader edit; DXIL remains up to date. Only those two
    SPIR-V files and the EXE are deployed, with every source/destination SHA-256
    pair matching. The EXE hash is
    7EF64475831D2E1E28F346FDE831095FF5113A62CF428C9BDC79A49F8C974F62.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    c6bb5b5f4. T3.1f runtime gate closed.
  - T2.3b now has a stronger post-T3.1 proof. PathTraceRemixRtxdiResources owns
    output-sized DI/GI reservoir allocations, but its header explicitly states
    that it binds no shader and dispatches no pass. Whole-tree search confirms
    that only its optional allocation/dump block, reset calls, and two legacy
    UAV state transitions remain; no binding-set item references either buffer.
  - The two state transitions occur after the clean-route return. Clean DI binds
    its m_smokeCleanRtxdiDi current/temporal/previous/spatial reservoirs, and
    clean GI owns its resources in PathTraceCleanRestirGi. The orphan owner is
    enabled by default, so deleting it also avoids allocating unused full-frame
    DI and GI arrays without changing either clean lane's resource contract.
  - Remove the owner, its member/reset/transition/dump plumbing, and its enable
    and dump CVars. Preserve PathTraceRemixFramePrepare, RemixLightManager, the
    live local Rtxdi host ABI, clean DI buffers, and clean GI resources.
  - Regenerate win64-pt-dev after deleting the owner source. The first compile
    exposes one transitive include: smoke dispatch still sizes live clean-DI
    reservoirs with RTXDI_PackedDIReservoir, previously obtained through the
    deleted owner header. Add the direct local <Rtxdi/DI/ReSTIRDI.h> include to
    that real consumer; the subsequent full win64-pt-dev-release build passes.
  - Every SPIR-V target remains up to date, so no shader artifact is deployed.
    Only the EXE is copied to the prebuilt runtime; source and destination hashes
    match at 342876CF2A766DA00B34C69F0F2C28D2C1116D75649C4AEA69D919FF483F2BA9.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    fbd55a227. T2.3b runtime gate closed.
  - T3.1g follows the remaining RtxdiBridge include graph. Of 27 headers, 18 are
    reachable from current shader sources. Nine form a closed 946-line island
    with no incoming edge from outside the island: the two old Debug headers,
    PathTraceRtxdiBridgeCore, DuplicationMap, EnvironmentSamplingStub,
    HitToLightIndex, the broad LightSampling compatibility wrapper, RayPayload,
    and the broad Surface compatibility wrapper.
  - Whole-repo filename hits outside this island are historical docs or comments,
    not compile entries or includes. Delete the nine tracked, unmodified headers.
    Preserve the reachable core/material/light/load/sample/target/visibility,
    ReGIR, NEE-cache, random-sampler, and unified-light bridge contracts.
  - Regenerate win64-pt-dev to remove the deleted headers from generated shader
    dependencies. The refresh recompiles five Vulkan roots despite the absence of
    include edges: NEE-cache debug (337,148 bytes, 20:59:49), primary-surface
    producer (843,044 bytes, 20:59:55), ReGIR debug (231,920 bytes, 20:59:57),
    RLU-current (628,644 bytes, 21:00:02), and smoke (1,251,964 bytes, 21:00:13).
    Every rebuilt hash is byte-identical to its prior deployed counterpart,
    independently confirming that the removed island affected no compiled code.
  - The full win64-pt-dev-release build passes and DXIL stays up to date. Copy
    exactly those five refreshed SPIR-V files and the unchanged EXE; all six
    source/destination SHA-256 pairs match.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    8c7b732ee. T3.1g runtime gate closed.
  - T3.1h extends the graph audit across all neo/shaders roots and finds nine
    remaining path-tracing headers with zero incoming shader edges. None changed
    in the last month and none has a worktree edit, so the last-24-hours feature
    set does not depend on this candidate group.
  - Delete the unintegrated adaptive GI M-cap header; four Remix bridge headers
    that explicitly identify themselves as inactive/future contracts; and four
    zero-consumer Rtxdi DI/utility forwarding shims. Remove the four shim paths
    from PATH_TRACING_SHARED_RTXDI_UTILITY_INCLUDES. Preserve the live cleanroom
    GI reservoir/temporal path, active Remix GI bridges, and all Rtxdi shims with
    real incoming edges.
  - CMake configure and the full win64-pt-dev-release build pass. The first build
    client timed out while MSBuild continued normally in the background; after
    that process exited, a second build completed with every target up to date.
    DXIL remains up to date.
  - The shared dependency-list change conservatively refreshed 15 Vulkan blobs.
    Fourteen are byte-identical to the already deployed copies and are deployed
    again with matching SHA-256 hashes. The EXE is unchanged and also matches.
  - pathtrace_clean_restir_gi_temporal.cs.bin is the sole rebuilt blob that does
    not match the known-good deployment. Its dependency graph includes an
    unrelated pre-existing worktree edit in restir_gi_reservoir.hlsli, so this
    cleanup deliberately quarantines that output instead of silently deploying
    another lane's unfinished work. Rebuilt SHA-256 is
    62D770978A36D28D0B5CB631220C5B9B59B980454E6374E1D9647D05DEC4C3FA;
    the preserved deployed SHA-256 is
    B37497B23E99827A03BBA14B281260DB23E40743D0BCC2E4CC9AAF4BEBADFBB2.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    fbaf52595. T3.1h runtime gate closed.
  - T4.3a begins the second CVar-pruning pass with ten controls whose only code
    tokens are their definition string, idCVar symbol, and extern declaration.
    Whole-repository search finds no runtime lookup by symbol or string. Remove
    ClassSummary, the explicitly legacy SceneBoundsDepthTest, the reserved RLU
    debug selector, two unwired material-classifier policy controls, three
    superseded clean-DI spatial tuning names, the reserved glass-guide selector,
    and the inactive DLSS-RR ReverseZ selector.
  - These definitions date from May through early July and none belongs to the
    preceding 24-hour feature work. Preserve the live clean-DI spatial enable
    and r_cleanDiSpatial tuning family, all glass production controls, the RLU
    domain/provider controls, current material-feature inputs, RR depth mode and
    camera planes, the retained debug-mode set, and all residency/entity-feed
    declarations involved in the separate dirty geometry worktree lane.
  - The full win64-pt-dev-release build passes. All SPIR-V and DXIL targets stay
    up to date because this slice changes no shader source or dependency. Deploy
    only the rebuilt EXE; source and destination SHA-256 match at
    ADB719BD6BCA5F2EEBC92C134D1E1E65771AD16220917283373DDDAD90D6C847.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    ebe66fed8. T4.3a runtime gate closed.
  - T4.3b follows the remaining retired-mode references. The mode-32/33 preview
    resolution cap is called only after NormalizePathTraceDebugMode maps that
    entire retired range to mode 0, so its 68-line helper and PreviewMaxPixels
    CVar cannot affect a frame. Remove both plus four CPU timing-log fields that
    have only constant legacy values at their sole producer.
  - Rename the surviving PathTraceRestirPasses.h normalization helper to
    PathTraceDebugModes.h and remove one redundant include. This helper now owns
    only stable debug-value normalization; it no longer describes or plans a
    ReSTIR pass. Preserve the separate live GPU timing pass labels and every
    production, clean-DI/GI, mode-18, and retained diagnostic route.
  - The header rename triggers CMake's configured glob check and regeneration.
    The full win64-pt-dev-release build then passes; all SPIR-V and DXIL targets
    remain up to date. Deploy only the rebuilt EXE, whose source/destination
    SHA-256 matches at
    91ED2B173316647C76226307D32CBB3FB45D73D5C16F6986D499F1ED30FA8EE8.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    78e73a355. T4.3b runtime gate closed.
  - T4.3c removes the old six-stage ReSTIR-PT GPU timestamp diagnostic. Five
    stages have no instrumentation sites after the retired producer passes were
    deleted; the sole survivor wraps the final monolith dispatch and is already
    covered by the retained CPU pass timing plus Optick/Nsight marker controls.
    Delete its query ring, poll/allocate/capture helpers, stage scope, and CVar.
  - The CPU timing record also carries a restirContext interval whose end is
    assigned directly from its start, making it identically zero. Remove that
    field and log column. The May-era diagnostic island predates the recent
    feature work and does not touch shader, DI/GI, mode-18, or marker behavior.
  - The full win64-pt-dev-release build passes and every shader target remains
    up to date. Deploy only the rebuilt EXE; source and destination SHA-256
    match at
    AADDA4E0C0C29803E7AC8ABE19DD29E5264E8ECA527497EE5596622DCBDE7D41.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    3bb3f2ffe. T4.3c runtime gate closed.
  - T3.1i begins removal of the old native smoke-reservoir owner at the shader
    boundary. The 171-line native NEE reservoir-preview include has no caller:
    its exported evaluator and every subordinate helper occur only in that file.
    The monolith u18-u20 reservoir UAV declarations, reservoir struct, and count
    helper are referenced exclusively by that dead include.
  - Delete the include and those declarations while deliberately retaining the
    host buffers/bindings for one runtime gate. Clean DI uses its independent
    RTXDI reservoir bindings beginning at u69, clean GI owns separate resources,
    and native mode 18 calls EvaluateSmokeToyPathTrace rather than the deleted
    preview evaluator. Neither shader file changed during recent feature work.
  - Delete smoke and RLU-current Vulkan blobs before building. The first build
    compiles successfully but exposes the deleted include in generated CMake
    dependencies; regenerate win64-pt-dev and repeat the full release build to
    leave incremental dependencies clean. DXIL remains up to date.
  - The dependency refresh rebuilds NEE-cache debug, primary-surface producer,
    ReGIR debug, RLU-current, and smoke. All five outputs are newer than the
    shader edit and byte-identical to their deployed predecessors, independently
    proving that the removed source reached no compiled entry point. Deploy the
    five verified SPIR-V files and unchanged EXE; all hashes match. EXE SHA-256
    remains AADDA4E0C0C29803E7AC8ABE19DD29E5264E8ECA527497EE5596622DCBDE7D41.
  - Runtime acceptance 2026-07-21: user reported no issues with deployed commit
    87c85e766. T3.1i shader-side runtime gate closed.
  - T3.1j removes the host half of the now-proven-dead native smoke-reservoir
    island: three full-frame structured buffers, u18-u20 layouts and bindings,
    barriers and clears, scene/dispatch reset bookkeeping, timing and dump
    diagnostics, and the PathTraceReservoirs implementation. Remove the now
    inert ReservoirDump and DisableReservoirWrites CVars and the unused shader
    safety bit as part of the same ownership boundary.
  - Preserve the independently owned clean-DI reservoirs at u69 and above,
    clean-GI resources, primary-surface history, mode 18, and the existing
    reservoirScene member in the general scene-transition hash. The last item
    remains because it participates in broader transition policy even though it
    no longer controls allocation or clearing of the deleted native buffers.
  - Delete pathtrace_smoke.rt.bin before building. CMake's glob refresh removes
    the deleted C++ files from the generated project; the full
    win64-pt-dev-release build passes and DXIL stays up to date. The regenerated
    smoke blob is newer than the shader edit and remains byte-identical at
    48C24BCEFD2E4DDB83861F342B5F577712270467F6CEC8A5AB8CA03C3782EEA5,
    confirming that the removed safety constant had no compiled consumer.
  - Deploy the rebuilt EXE plus the two Vulkan blobs refreshed by the dependency
    check (smoke and RLU-current). Source/destination SHA-256 pairs match. EXE
    SHA-256 is
    4EFA489A3F1463DBB6C7A6FC1BC5E39F6AB00B0ED178A0A007336B5BDE2106BC.
  - Runtime acceptance 2026-07-21: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 9e9646ae3. T3.1j runtime gate closed.
  - T4.3d is a zero-consumer residue sweep across unmodified helper surfaces.
    Whole-tree symbol counts identify seven superseded Doom analytic lookup/remap
    helpers, the abandoned stable material-slot/cache builder and its complete
    private helper closure, one unused cardboard override probe, and one obsolete
    combined rigid side-buffer capacity helper. None is referenced outside its
    own definition and none belongs to the dirty geometry worktree lane.
  - The shader half removes only helpers with no call site anywhere under the
    path-tracing shader tree: three isolated material/OpenPBR helpers and the old
    ReSTIR-PT primary-surface debug/compatibility wrapper group. Replace the two
    surviving store-wrapper calls with their existing canonical function before
    deleting the alias. Retain all clean DI/GI helpers and the material-feature
    API even where current textual counts are low.
  - NormalizePathTraceDebugMode already maps mode 56 to zero at every public
    boundary, so remove three constant-true `requestedDebugMode != 56` terms from
    scene policy without changing NEE-cache or RLU producer behavior. The slice
    removes 663 lines across nine implementation files before documentation and
    build evidence are added.
  - Delete all 15 configured Vulkan outputs that list the changed shared headers
    as dependencies. The full win64-pt-dev-release build passes and every rebuilt
    blob is newer than the latest shader edit. Twelve outputs are byte-identical
    to their pre-build/deployed copies. Smoke and RLU-current change only because
    their two live call sites now invoke the canonical primary-surface store
    directly instead of the deleted one-line alias.
  - The build also retriggers the DXIL NEE-cache primary-surface update compute
    shader through the broad RenderProgs dependency graph. It takes under one
    second, is not deployed, and should be removed from this Vulkan-only preset in
    a separate build-graph cleanup rather than mixed into this source deletion.
  - The unrelated dirty GI reservoir edit remains isolated: rebuilt GI-temporal
    stays at
    62D770978A36D28D0B5CB631220C5B9B59B980454E6374E1D9647D05DEC4C3FA,
    while deployment preserves the known-good
    B37497B23E99827A03BBA14B281260DB23E40743D0BCC2E4CC9AAF4BEBADFBB2.
    Deploy the rebuilt EXE and the other 14 SPIR-V outputs; every copied hash
    matches. EXE SHA-256 is
    318AB1E20B45E99FF8B8B59D0C2C0BDAA85FBEA31EF713AEEC3758CB725D18F8.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit e9b491469. T4.3d runtime gate closed.
  - BUILD-01 audits the unexpected DXIL compile from T4.3d. The existing build
    cache already has PATH_TRACING_BUILD_DXIL_RT_SHADERS=OFF, so bespoke PT RT
    libraries are not the source. USE_DX12 remains ON, which keeps the generic
    all-platform RenderProgs DXIL lane and NVRHI D3D12 backend in the PT build.
  - The first BUILD-01 attempt made the shared PT preset USE_DX12=OFF. Configure
    proof was clean (zero DXIL outputs in Shaders.vcxproj and
    PathTracingShaders.vcxproj, and no nvrhi_d3d12 target in the solution), but
    the full build failed at link with 103 unresolved renderer/NVRHI symbols.
    The legacy USE_DX12 switch still owns source selection for implementation
    used by Vulkan, so disabling the whole backend is not a viable cleanup-sized
    change.
  - Correct BUILD-01 to separate shader compilation from that legacy source
    selection. BUILD_DXIL_SHADERS defaults ON for existing configurations and
    gates the generic RenderProgs DXIL command. The shared win64 PT preset sets
    BUILD_DXIL_SHADERS=OFF and PATH_TRACING_BUILD_DXIL_RT_SHADERS=OFF while
    retaining USE_DX12=ON solely to preserve the current engine link graph.
    Verify from a reconfigured preset that the shader projects contain no DXIL
    outputs, then require a successful full build with no DXIL compile command.
  - BUILD-01 verification 2026-07-22: preset configure records
    BUILD_DXIL_SHADERS=OFF, PATH_TRACING_BUILD_DXIL_RT_SHADERS=OFF,
    USE_DX12=ON, and USE_VULKAN=ON. Generated Shaders.vcxproj and
    PathTracingShaders.vcxproj each contain zero DXIL output references. The
    full win64-pt-dev-release build succeeds; shader output contains only two
    "All SPIRV shaders are up to date" checks and no DXIL compile. No shader
    source or blob changed, so deployment correctly copied only RBDoom3BFG.exe.
    Source and deployed SHA-256 both equal
    DFB5CE7362FDD741EC1387C1801F1EA5C25ECF81639D362D14912E00E7FEF6CB.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit c199adc03. BUILD-01 runtime gate closed.
  - T4.3e follows the shader include graph after the earlier zero-consumer shim
    cleanup. Deleting Rtxdi/DI/SpatialResampling.hlsli and the BrdfRaySample and
    Color utility shims in fbaf52595 left their three cleanroom_common backing
    implementations with no include directive anywhere in neo/shaders. Every
    exported function/type in these files occurs only in its definition; their
    remaining CMake entries are dependency-list residue, not compilation roots.
    None of the three files changed during the recent feature window, and none
    intersects the dirty geometry/residency worktree lane. Delete the three
    orphaned implementations and their inert CMake dependency entries. Because
    no compiled shader includes them, require the normal Vulkan build but do not
    force unrelated SPIR-V outputs solely to prove an already-empty include graph.
  - T4.3e verification: the final symbol/filename sweep is empty and the full
    win64-pt-dev-release build passes with no DXIL compile. Regenerating the
    shortened dependency list conservatively rebuilds 15 SPIR-V outputs; every
    output is newer than the CMake edit. Fourteen are byte-identical to their
    deployed predecessors and are copied with the unchanged executable. Their
    source/deployment hash pairs all match. The unrelated dirty GI-reservoir edit
    again changes only pathtrace_clean_restir_gi_temporal.cs.bin, so that source
    blob is deliberately not deployed; the prebuilt tree retains the known-good
    B37497B23E99827A03BBA14B281260DB23E40743D0BCC2E4CC9AAF4BEBADFBB2.
    Source and deployed EXE SHA-256 remain
    DFB5CE7362FDD741EC1387C1801F1EA5C25ECF81639D362D14912E00E7FEF6CB.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 8a8c1ae79. T4.3e runtime gate closed.
  - T4.3f audits constant-false clean-route residue. The obsolete external
    PDFNEE mode-9 ReGIR build selector has been hardcoded false since 2026-05-28,
    but still owns shader initialization checks, a large optional binding set,
    resource barriers, a full-screen ReGIR dispatch, one GUI field, and fixed-zero
    dump fields. The current external-PDFNEE RLU producer and clean ReGIR build
    paths are independently selected and remain live. A second hardcoded-false
    GI NEE-cache live-refresh flag only decorates conditions; simplify those
    expressions to their existing effective policy. Neither constant-false lane
    was touched by the recent feature window. The first compile catches the
    retired snapshot field's console display consumer outside the NVRHI tree;
    remove that fixed-zero display column as part of the same ownership slice.
  - T4.3f verification: the post-fix whole-neo symbol sweep is empty. The full
    win64-pt-dev-release build passes; both shader targets stay up to date and no
    DXIL command runs. This is a C++-only slice, so deploy only RBDoom3BFG.exe.
    Source and deployed SHA-256 both equal
    60833F305E92B13E1557F38630E1BCD564532B69BB40A6AA3B61C1446416068E.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit d5aa8ac7e. T4.3f runtime gate closed.
  - T4.3g indexes PathTracePrimaryPass member references across the whole neo
    tree. Six old bounds-overlay camera/FOV fields are written but projection now
    consumes only the retained model-view/projection matrices. The dynamic BLAS
    descriptor is copied at resource commit but never read; only the retained
    static descriptor participates in cache reuse. Two material-feature vectors
    duplicate the freshly uploaded table then are never consumed, and five scene
    inventory counters are reset/assigned but never read. The clean GUI spatial
    reservoir flag is likewise published but has no reader. Remove these fields
    and their writes while retaining all GPU buffers, material-feature uploads,
    live counters, bounds-overlay matrices, and geometry-universe state. None of
    the candidates changed during the recent feature window.
  - T4.3g verification: the post-edit symbol sweep is empty and the full
    win64-pt-dev-release build passes. Both shader targets remain up to date and
    no DXIL command runs. Deploy only the rebuilt executable; source and deployed
    SHA-256 both equal
    840C99538D6B2065D6121836B8837C356B7075664FBDDF3400E4A2598B8C1881.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 5fe662d5e. T4.3g runtime gate closed.
  - T4.3h follows the T4.3g write-only members upstream. Six fields now travel
    only from scene build through RtSmokeSceneResourceCommitBuildDesc into
    RtSmokeSceneResourceCommitDesc and have no consumer: dynamic BLAS descriptor,
    dynamic-emissive count, textured-light count, light-candidate bytes, Doom
    analytic bytes, and invalid-remap count. Remove this redundant transport from
    both descriptors and the conversion function. Preserve the independently
    live acceleration-submit descriptor, buffer byte sizes, scene-input
    diagnostics, and debug-dump inputs.
  - T4.3h verification: scoped transport sweeps find only the intentionally
    retained buffer-create fields with similar names. The full
    win64-pt-dev-release build passes; both shader targets stay up to date and no
    DXIL command runs. Deploy only the executable. Source and deployed SHA-256
    both equal
    5DA20694A8A2B3FF1FBDC466139F90C5170B14A22CEB367A299238BFE64B29EC.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 211886445. T4.3h runtime gate closed.
  - T4.1 boundary re-audit: a direct extraction of either the clean-route body
    or the post-clean debug tail is not yet a safe mechanical move. Both consume
    dozens of handles, settings, and lambdas created by the shared prelude in
    ExecuteRayTracingSmokeTest. Moving either side now would require a large
    parameter list or a new context object, turning the cleanup into a behavioral
    refactor. Defer the file split until the route-policy inputs have been
    centralized and a narrow frame-context contract can be introduced and
    reviewed separately.
  - T4.2a centralizes four exact retained-debug policies in
    PathTraceDebugModes.h: bounds overlays, texture-probe construction, texture
    table requirement, and rigid-route participation/removal. The replacement
    truth-table comparison is empty for modes 0-57. Keep the separate rigid-route
    and routed-dynamic predicates because mode 23 intentionally participates in
    the route without removing the mirrored dynamic surface.
  - T4.3i completes the scene-input member audit. Two previous-vertex booleans
    were never assigned and therefore remained fixed false in the dump; one
    rigid transform-continuity count was assigned but never read. Remove the
    three members, their dump columns, and the lone assignment. Preserve all
    adjacent temporal counters and capability flags.
  - T4.2a/T4.3i verification: the old/new policy truth-table comparisons are
    empty for every mode from 0 through 57, the three retired scene-input symbols
    have no remaining neo reference, and git diff --check passes. The full
    win64-pt-dev-release build passes after one retry for a transient compiler
    object lock left by an intentionally short build timeout. Both SPIR-V targets
    remain up to date and no DXIL command runs. Deploy only the executable;
    source and deployed SHA-256 both equal
    0680052CBADB16E3BAEB8628BC8F757DC732C7FA0C871E12670250C2C3231570.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit ad5b535c6. T4.2a/T4.3i runtime gate closed.
  - T4.3j removes the one-shot standalone ReGIR bring-up dump. Its CVar, large
    console formatter, and repeated early-return print/reset blocks do not feed
    route selection, resource creation, binding construction, constants, or
    dispatch. Removing the formatter also orphans three public enum-to-string
    helpers and one local center-policy label helper; remove those declarations
    and definitions in the same ownership slice. Preserve the live ReGIR debug
    views, center resolver, resource state, RLU inputs, binding set, constants,
    candidate build, and dispatch. The last-day history for this dump is
    cleanup-only and does not intersect the recent rendering-feature changes.
  - T4.3j verification: whole-neo sweeps for r_pathTracingReGIRDump,
    printReGIRDump, and the four retired label helpers are empty. The full
    win64-pt-dev-release build passes; both SPIR-V targets remain up to date and
    no DXIL command runs. This is a C++-only slice, so deploy only the executable;
    source and deployed SHA-256 both equal
    B9D6144E8ADB43467DAB89632D67E8735F3BA056E2833C1FC8859112F99B8563.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit b23fd34de. T4.3j runtime gate closed.
  - T4.3k removes the one-shot primary/provider and secondary-consumer NEE-cache
    console dumps. Both are observation-only: their CVars and formatters do not
    participate in provider selection, cache invalidation, candidate build,
    clean-DI integration, debug-view dispatch, or secondary consumption. Remove
    their repeated early-return print/reset blocks while preserving those live
    paths. Three label helpers become unreferenced with the formatters and are
    removed; retain PathTraceNeeCacheSourceDomainName because the clean-DI dump
    still consumes it.
  - The dump removal also proves PathTraceNeeCacheResourceDesc's
    firstMissingContract field is write-only. Its RLU-domain strings never gate
    resource creation; requested, structuralValid, and byte counts are the live
    allocation contract. Remove the field, its assignments, and the now-unused
    RLU-input parameter while retaining every enable, mode, cell, slot, overflow,
    and nonzero-byte validation that controls allocation.
  - T4.3k verification: whole-neo sweeps for both retired dump CVars, both dump
    lambdas, the three label helpers, and the resource-desc diagnostic field are
    empty. The full win64-pt-dev-release build passes; both SPIR-V targets remain
    up to date and no DXIL command runs. This is a C++-only slice, so deploy only
    the executable; source and deployed SHA-256 both equal
    F4ADA97D8F692C3890FC8BAF9902168AB33BF086C85E25A43D5C4F91344FB0DF.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 77f2f543d. T4.3k runtime gate closed.
  - T4.3l separates the live replacement RLU-current producer from its obsolete
    verifier-named one-shot dump. Preserve r_pathTracingRestirPdfNeeVerifierEnable,
    samples, visibility, and source policy; the producer shader, reservoir pages,
    binding layout/set, NEE-cache provider readiness, constants, and dispatch all
    remain live. Remove only r_pathTracingRestirPdfNeeVerifierDump, its entry and
    pre-dispatch formatters, repeated early-return print/reset blocks, and two
    dump-only cached values. The dump's last-day history is cleanup-only; none of
    the recent rendering-feature changes depends on it.
  - T4.3l verification: whole-neo sweeps for the retired dump CVar, formatter,
    cached dump selector, and dump-only producer-enabled alias are empty. The full
    win64-pt-dev-release build passes; both SPIR-V targets remain up to date and
    no DXIL command runs. This is a C++-only slice, so deploy only the executable;
    source and deployed SHA-256 both equal
    8FCF16CDCA606710903EA25D374824051D23196570A75A35684F36D9496AF0D1.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 7c860ebd5. T4.3l runtime gate closed.
  - T4.3m removes four default-off, one-shot dispatch-tail reports: integrator
    settings, safety metadata, tiled-dispatch planning, and selected debug-light
    details. Each CVar has one get/print/reset consumer and none changed during
    the recent feature window. Preserve the live integrator settings and ray
    estimate, safety mask, tiled-dispatch settings and execution, light-selection
    policy and selected-light upload, pass timing, and all clean-DI/GI dumps.
  - T4.3m verification: whole-neo sweeps for all four retired dump CVars are
    empty, while their underlying render inputs remain referenced. The full
    win64-pt-dev-release build passes; both SPIR-V targets remain up to date and
    no DXIL command runs. This is a C++-only slice, so deploy only the executable;
    source and deployed SHA-256 both equal
    16EEA093B77D018A2FF2F45DBAD32CE1BF74F104A38C62669AC6390CC433203D.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 122aa9cb3. T4.3m runtime gate closed.
  - T4.3n removes three resource-lifecycle reports: the committed scene-input
    snapshot, portal/scene transition comparison, and retired-package event log.
    Their CVars and printers observe already-computed state only. Preserve the
    live scene transition signature, optional wait-for-idle policy, GPU-handle
    change detection, retirement-frame policy, package queue, and handle release.
  - Removing the retire report exposes stale transport in
    RtRetiredSmokeScenePackage: copied scene inputs, scene signature, current
    area, and selected-area count have no remaining reader. Remove those fields
    and writes. Collapse PushRetiredRayTracingSmokeScenePackage and
    ReleaseExpiredRetiredRayTracingSmokeScenePackages to the frame/retire inputs
    they actually consume; remove the release-only package copy and the separate
    portal-transition hash whose sole consumer was its printer. The last-day
    history for all three reports is empty, so this does not intersect recent
    rendering-feature changes.
  - T4.3n verification: whole-neo sweeps for the three retired CVars, printers,
    retire-event consumer, portal-only signature, and retired snapshot fields are
    empty. The full win64-pt-dev-release build passes; both SPIR-V targets remain
    up to date and no DXIL command runs. This is a C++-only slice, so deploy only
    the executable; source and deployed SHA-256 both equal
    72AC0DEF90DCF6574A2090A142C7D01AC6832841B300D75ADB419DC535EC0C2D.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 1beb639db. T4.3n runtime gate closed.
  - T4.3o removes three default-off scene-build reports: asynchronous CPU
    planning, per-buffer upload items, and material/dynamic-material upload diffs.
    None changed during the recent feature window. Preserve asynchronous plan
    generation and acceptance, all buffer upload items and dirty ranges,
    descriptor writes, material/dynamic-material signatures, aggregate upload and
    skipped-byte accounting, upload timing, and retained rigid/skinned reports.
  - The material report owns two diff-summary types and their compare/sample
    helper family; the buffer report owns the 49-entry item-name table, its size
    assertion, and three per-item formatting helpers. Remove that now-orphaned
    diagnostic support while retaining SumSmokeUploadBytes and
    SumSmokeSkippedUploadBytes, which still feed committed diagnostics and
    retained route reports.
  - T4.3o verification: whole-neo sweeps for the three retired CVars and all
    report-only helper symbols are empty. The full win64-pt-dev-release build
    passes; both SPIR-V targets remain up to date and no DXIL command runs. This
    is a C++-only slice, so deploy only the executable; source and deployed
    SHA-256 both equal
    5A3251B243CD4DBDFFB806DBC33A2F9AE20E5EA5FB3439906F59868DAD554A3E.
  - Runtime acceptance 2026-07-22: user reported no issues on deployed commit
    dedb2cd2e. T4.3o runtime gate closed.
  - T4.3p removes five light-domain reporting controls: the emissive bridge,
    Remix frame-prepare, Remix Light Universe, Remix light-manager, and purged
    legacy ReSTIR light-manager dumps. Each CVar gates console observation only;
    none participates in light collection, dense-domain construction, identity
    remapping, sampling ranges, GPU buffer sizing/uploads, or clean RTXDI DI.
    Symbol-level history for the five controls is empty during the recent
    feature window.
  - Removing the reports exposes diagnostic-only support beneath them. Reduce
    the frame-prepare package to the frame index and reset flags actually read by
    PathTraceRemixLightManager; remove its unused light-count package, statistics,
    begin/end counters, and structural-reset formatter state. The legacy ReSTIR
    observation-summary branch was unreachable because its CVar was cleared by
    the immediately preceding purge notice. Remove that branch, the now-unused
    observation builders and summary helpers, and both large RLU CPU payload
    replay formatters. Preserve the active Remix payload records, current and
    previous maps, RAB bridge records, buffer uploads, signatures, scene inputs,
    sampling ranges, and dispatch consumers.
  - T4.3p verification: whole-neo sweeps for all five retired CVars, the RLU
    status label, frame-prepare statistics/light inputs, legacy observation
    builders, and dump-local selectors are empty. The slice removes 1,174 lines
    without changing shader source. The full win64-pt-dev-release build passes;
    both SPIR-V targets report up to date and no DXIL command runs. No SPIR-V is
    rebuilt, so deployment copies only RBDoom3BFG.exe. Source and deployed
    SHA-256 both equal
    B517209AE0F6FA676DED81DB5499133BE4FAD8CB74871C973087190E0640C6DA.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 2c4468f6e. T4.3p runtime gate closed.
  - T4.3q follows the purged light-domain reports to the retired
    RtSmokeLightUniverse owner. No call to MergeFrameCandidates remains: the
    object is cleared every frame, its GetStats result is fixed zero/default,
    and its only readers are the purge notice, an unreachable formatter, and
    fixed-value scene-input fields. Delete PathTraceLightUniverse.cpp/.h, the
    primary-pass member and duplicate render-world reset pointer, and the fixed
    light-universe generation transport. The normal render-world reset already
    clears the active Remix frame/light managers before the removed duplicate
    block could run.
  - Remove the nine controls owned only by this retired path: the legacy dump,
    five ignored persistence/churn controls, and three write-only area-filter
    controls. Keep r_pathTracingLightAreaPortalSteps because Doom analytic
    portal selection and the scene transition contract still consume it. The
    same dump also owns the Doom uploaded-domain formatter and write-only
    universe/persistent registry statistics; remove those summaries and their
    static-list bookkeeping while preserving persistent authored-light entries,
    continuity, candidate capping, current/previous identity maps, remap flags,
    portal selection, and GPU candidate packing. Recent file history is
    cleanup-only and contains no rendering-feature edit to the retired owner.
  - T4.3q verification: whole-neo sweeps for PathTraceLightUniverse, its member
    state/types, all nine retired CVars, fixed generation transport, and the Doom
    dump/statistics helpers are empty. The slice removes 2,069 lines. The first
    build regenerates the source glob and exposes the remaining fixed-zero scene
    consumers; after removing those stale reads, the full
    win64-pt-dev-release build passes. Both SPIR-V targets report up to date and
    no DXIL command runs. No SPIR-V is rebuilt, so deploy only RBDoom3BFG.exe.
    Source and deployed SHA-256 both equal
    44BC5331FB1CD4F32D2782DB88E7948B2C285A12AE52703595CE7E6F5B127B04.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 8fa4ffa9c. T4.3q runtime gate closed.
  - T4.3r removes the purged legacy PathTraceRestirLightManager owner. None of
    BeginFrame, UpdateFromObservations, UpdateActivePayloadRecords, or EndFrame
    has a caller, so the manager can only remain empty. Scene build already uses
    a constant-true Remix selector; collapse its current/previous records,
    remaps, and payload references directly to PathTraceRemixLightManager.
    Dispatch's r_pathTracingRestirLightManagerRAB fallback therefore exposes no
    light data even when manually enabled; remove the fallback CVar, empty
    manager member, stats, remap/domain/signature implementation, and legacy
    sample-budget helpers.
  - The post-manager ABI audit finds no shader declaration at t62/t63 and no
    consumer of PathTraceRestirCurrentLightRecord or
    PathTraceRestirPreviousLightRecord. Remove those compatibility record
    builders, buffers, byte sizing, uploads, binding layout/set entries,
    lifetime transport, and the remaining header. Adjust the fixed upload
    accounting ranges for the two removed items. Preserve the live t64/t65
    remaps and t66/t67 unified payload buffers, their uploads/bindings, RLU
    enable policy, sampling ranges, and clean-DI consumers. Recent history for
    the removed manager is cleanup-only; no last-day rendering feature calls its
    retired update path.
  - T4.3r verification: whole-neo sweeps for the manager class/member, fallback
    CVar, old record/stats/observation/active-range types, t62/t63 bindings and
    buffer fields, and legacy dispatch budget helpers are empty. The shader tree
    has no t62/t63 declaration. The slice removes 1,631 lines. CMake regenerates
    the deleted source glob and the full win64-pt-dev-release build passes. Both
    SPIR-V targets report up to date and no DXIL command runs. No SPIR-V is
    rebuilt, so deploy only RBDoom3BFG.exe. Source and deployed SHA-256 both
    equal
    B13780BC43ED7A14328A04DAEDF2A1B0DCFBE42898806EDA5DAD26F9703EE8D1.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit b52485fd6. T4.3r runtime gate closed.
  - T4.3s removes five controls with no rendering consumer. The drawSurf
    residency gate and the deprecated max-distance/anti-culling controls occur
    only in their CVar definitions and declarations; no symbol or string lookup
    reads them. The reserved clean-DI denoiser and fallback-lighting controls
    feed only the one-shot dump and ImGui labels. Remove those two label fields
    and formatter entries with the controls. Preserve every active residency
    gate, clean-DI dispatch flag, shader constant, resource and history policy,
    debug view, and material-feature control. None of the five symbols changed
    during the recent feature window.
  - T4.3s verification: whole-neo sweeps for all five controls and both removed
    GUI fields are empty. The full win64-pt-dev-release build passes; both
    SPIR-V targets report up to date and no DXIL command runs. No SPIR-V is
    rebuilt, so deploy only RBDoom3BFG.exe. Source and deployed SHA-256 both
    equal
    CB5F1AD84B9DFBAE118EB0FDB4680943EDE673083455F1EFEE341A07CA764480.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 0c2f08c2a. T4.3s runtime gate closed.
  - T4.3t removes the retired scene-universe one-shot inventory dump and its
    verbose extension. The two controls only trigger console formatting after
    the scene and portal selection are already built. Remove their formatter,
    call-site flag/reset transport, dump-only source2 build report, verbose
    area/surface replay limits, selected-emissive material replay records, and
    selection-list overflow counter. Preserve the active scene-universe build,
    selected/static geometry output, portal traversal, static-area preload and
    its independent dump, persistent scene stats, and the throttled
    r_pathTracingSmokeLog summary. Neither control nor the scene-universe files
    changed during the recent feature window.
  - T4.3t verification: whole-neo sweeps for both controls, LogDump, its replay
    record type/vector, and dump call-site state are empty. The full
    win64-pt-dev-release build passes; both SPIR-V targets report up to date and
    no DXIL command runs. No SPIR-V is rebuilt, so deploy only RBDoom3BFG.exe.
    Source and deployed SHA-256 both equal
    26A8E2371648FDA07A91A6A7F3B66A6EF9558AD9CAC6048083446BB268A869AE.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 9ef54c2f0. T4.3t runtime gate closed.
  - T4.3u removes the source3 static-area-preload one-shot dump. Its control
    only enables per-area/material/cached-vs-missing observation during the
    already-selected preload walk and prints the result. Remove the control,
    parameter/reset transport, formatter, per-area record/vector, per-area
    portal counters and surface accumulation, and the dump-only baked-static
    selection summaries. Preserve selected-area discovery, portal depth,
    deduplication stamps, static surface processing, cache mutation detection,
    residency reporting, build statistics, and the legacy-walk selected-surface
    count used for reserve sizing. The control and owner files have no changes
    during the recent feature window.
  - T4.3u verification: scoped sweeps for the control, dump parameter,
    per-area record/vector, formatter label, and removed helper are empty. The
    slice removes 176 lines. The full win64-pt-dev-release build passes; both
    SPIR-V targets report up to date and no DXIL command runs. No SPIR-V is
    rebuilt, so deploy only RBDoom3BFG.exe. Source and deployed SHA-256 both
    equal
    2EB660011FBEF86B21820D6844838CF8C80E8C0F7DD6501BB54B64BCEB44DF64.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit bb4c18c35. T4.3u runtime gate closed.
  - T4.3v removes the remaining throttled scene-universe diagnostic summary and
    its transport. r_pathTracingScenePortalSteps only selected the diagnostic
    traversal depth; sceneInputs.portalPolicy.sceneUniverseSteps was write-only
    and did not participate in the scene-transition signature. Remove that CVar
    and field, RunDiagnostics/LogSummary and their one-per-world lifecycle flag,
    and the summary-only portal, material, emissive-capable, cached, and missing
    surface aggregation. Preserve r_pathTracingStaticAreaPreloadPortalSteps,
    r_pathTracingRigidResidencyPortalSteps, r_pathTracingLightAreaPortalSteps,
    the active selected-area list, and the legacy-walk selected-surface count
    used for reserve sizing. The removed owner and CVar have no recent
    rendering-feature changes.
  - T4.3v verification: scoped sweeps for the CVar, write-only scene-input
    field, summary methods/lifecycle flag, diagnostic selection fields, and
    collection flag are empty. The code slice removes 132 lines. The full
    win64-pt-dev-release build passes; both SPIR-V targets report up to date and
    no DXIL command runs. No SPIR-V is rebuilt, so deploy only RBDoom3BFG.exe.
    Source and deployed SHA-256 both equal
    527E7374B4796180BC4BAACF947F9E46D69309681FE0FC6330393FE4FEBE9693.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit fe018ebc2. T4.3v runtime gate closed.
  - T4.3w removes four one-shot material/texture bring-up reports: the paged
    texture-probe candidate dump, alpha-material dump, texture-fallback dump,
    and translucent-subtype dump. Remove their six controls, four formatters,
    trigger/reset branches, dump-only cap/fallback helper, and the resulting
    write-only material-stats pointer in the trigger package. Preserve texture
    probe selection/reset and its live latch, the material-table construction
    path, periodic probe/switch/active-window summaries, texture coverage,
    crosshair/GUI/emissive diagnostics, all material classifiers and textures,
    retained debug modes 8-18, clean DI/GI, and native mode 18. Recent history
    for the touched owners is cleanup-only and does not add a rendering-feature
    dependency on these reports.
  - T4.3w verification: whole-neo sweeps for all six controls, four formatters,
    dump-only constant/helper, and removed trigger transport are empty. The code
    slice removes 372 lines. The full win64-pt-dev-release build passes; both
    SPIR-V targets report up to date and no DXIL command runs. No SPIR-V is
    rebuilt, so deploy only RBDoom3BFG.exe. Source and deployed SHA-256 both equal
    0512E64C12A28C9ED832A51654AA8AFDBAC2097C3023D5A27287B0C4B550AAFB.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 7a03c30e3. T4.3w runtime gate closed.
  - T4.3x removes the one-shot crosshair material/stage report and GUI surface
    report. Their two controls only enter formatting after the scene and
    material table are built. Remove both report implementations, their
    trigger/reset branches, report-only classifier/compositing projection
    helpers and includes, and the seven resulting write-only trigger-package
    fields. Preserve ProcessSmokeCrosshairZeroRoughnessToggle and
    ProcessSmokeCrosshairFullMetalToggle, their request consumers and override
    mutators, center-ray surface validation, all material/classifier production
    data, GUI capture/rendering, retained debug modes, clean DI/GI, and native
    mode 18. The touched report owner has no change during the recent feature
    window; its newest pre-cleanup changes are report formatting from 2026-07-19.
  - T4.3x verification: whole-neo sweeps for both controls, both report
    functions, report-only helper types/functions, and removed trigger fields
    are empty. The two live override handlers and their complete request-to-scene
    call chains remain; their owner shrinks from 1,189 to 94 lines. The code
    slice removes 1,161 lines. The full win64-pt-dev-release build passes; both
    SPIR-V targets report up to date and no DXIL command runs. No SPIR-V is
    rebuilt, so deploy only RBDoom3BFG.exe. Source and deployed SHA-256 both equal
    19912742DF5C5C3ED87E9956D775169E28B8F8ADF6F6773510A556D27ADCF459.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 8eeb4eca5. T4.3x runtime gate closed.
  - T4.3y removes the one-shot emissive-inventory and routed-rigid-emissive
    reports. Both controls converge after live inventory construction on the
    same formatter. Remove the controls, formatter/declaration, dump-only
    per-material summary type, trigger package/function, scene-build descriptor,
    and diagnostics marker. Preserve r_pathTracingEmissiveInventoryMaxTriangles,
    all static/dynamic/rigid inventory appends and statistics, sampling-field
    finalization, temporal remaps, distribution and light-candidate builds,
    Remix light preparation, GPU sizing/upload, previous-frame history, clean
    DI/GI, and native mode 18. Recent edits in the touched owners are cleanup
    only and do not add a rendering consumer to the report path.
  - T4.3y verification: whole-neo sweeps for both controls, formatter, dump-only
    summary type, trigger package/function, and marker are empty. A scoped live
    chain sweep confirms inventory construction through remap, light preparation,
    upload, and history remains. The code slice removes 235 lines. The full
    win64-pt-dev-release build passes; both SPIR-V targets report up to date and
    no DXIL command runs. No SPIR-V is rebuilt, so deploy only RBDoom3BFG.exe.
    Source and deployed SHA-256 both equal
    AD2B308C8F04764ECA5B0A747FC206CAD83CBF8165EA9F8658B3DB6CE8DC7C25.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit 27e683da1. T4.3y runtime gate closed.
  - T4.3z removes the one-shot surface-class reason sampler. Its control only
    requests bounded reason records during an already-decided classification
    pass and formats them after scene build. Remove the CVar, reason/sample
    types and builder/accumulator, formatter, capture-API pointer transport,
    mirror initialize-only transport, scene-log fields/trigger, and call-site
    locals/arguments. Preserve the classifier and translucent subtype logic,
    stable class IDs, per-class bucket arrays/ranges, class/material/dynamic
    statistics, all captured geometry/material outputs, source comparison,
    retained debug modes, clean DI/GI, and native mode 18. Recent touched-file
    history is cleanup-only and adds no rendering dependency on the sampler.
  - T4.3z verification: whole-neo sweeps for the CVar, reason types/helpers,
    formatter, descriptor fields, sampler constant, and pointer transport are
    empty. All legacy, source3, compare, and draw-surface-mirror capture callers
    match the reduced signatures. The code slice removes 315 lines. The full
    win64-pt-dev-release build passes; both SPIR-V targets report up to date and
    no DXIL command runs. No SPIR-V is rebuilt, so deploy only RBDoom3BFG.exe.
    Source and deployed SHA-256 both equal
    05921266EFDE67BE617B58E7D5351EE6FB6083D09BDD9727D76632EADA5A5054.
  - Runtime acceptance 2026-07-22: user reported no issues in clean RTXDI DI and
    native mode 18 on deployed commit cd6735b9c. T4.3z runtime gate closed.
  - T4.3aa removes the one-shot source 0 versus source 3 comparison harness.
    Its default-off control performs a second legacy draw-surface capture after
    the live source 3 capture solely to print geometry, class, material-ID, and
    skip-count differences. Remove the CVar, trigger/reset branch, comparison
    capture and formatter, comparison-only type and counting/diff/sample
    helpers, and the otherwise unused unique-material count helper. Preserve
    r_pathTracingSceneSource, the source 3 draw-surface mirror producer,
    r_pathTracingSceneSource2RigidEntities, material hydration and signature
    helpers, active geometry/material output, retained debug modes, clean DI/GI,
    and native mode 18. The removed harness is May-era, and recent touched-file
    history is cleanup-only; no recent rendering feature consumes its output.
  - T4.3aa verification: whole-neo sweeps for the CVar, comparison capture,
    comparison type, counting/diff/sample helpers, and console label are empty.
    A scoped live-symbol sweep confirms the active source selector, source 3
    route, source 2 rigid control, and sorted-material hydration/signature
    helpers remain. The code slice removes 240 lines. The full
    win64-pt-dev-release build passes; both SPIR-V targets report up to date and
    no DXIL command runs. No SPIR-V is rebuilt, so deploy only RBDoom3BFG.exe.
    Source and deployed SHA-256 both equal
    0377A2F6C34CD09AC45F0D4CD32B919CA8B7B7CBD6AACB3DB39EA1BD14E24DF3.
