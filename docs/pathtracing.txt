RBDoom3 BFG Path Tracing Experiment
===================================

Purpose
-------

This branch is an experimental attempt to evolve the Vulkan/NVRHI renderer in
RBDoom3 BFG toward a Quake II RTX style brute-force path traced renderer. The
target hardware is currently an RTX 4090 class GPU, so the early goal is image
quality and learning rather than broad hardware support or high performance.

The current work is intentionally staged as "RT smoke" debug functionality. It
proves pieces of the path tracing pipeline in small, testable steps before
replacing the main renderer.

This file is the broad chronology/status document: what the branch is trying to
do, what has been tried, what works, what remains risky, and how it is tested.
The `pathtrace_scene_producer_*` documents are retained as detailed intervention
logs and historical build records; durable architecture, CVar, and testing
guidance should be kept here and in the module/feature notes below.
For a file-by-file map of the extracted PathTrace* modules and feature-specific
"where to touch" guidance, see:

    docs/pathtrace_modules.txt

For persistent geometry guardrails, see:

    docs/pathtrace_geometry_universe.md

For persistent material guardrails, see:

    docs/pathtrace_material_universe.md

For the current plan to refactor the monolithic PathTracePrimaryPass /
PathTraceSmokeDispatch execution layer into explicit frame resources, primary
surface, ReSTIR, debug, and future denoise/reflection contracts, see:

    docs/pathtrace_core_refactor_plan.txt
    docs/pathtrace_core_refactor_tasks/00_agent_readme.txt

For the current mode-56/ReSTIR PT quick CVar reference, including primary
prepass, sparse producer dispatch, reflection preview, Nsight/timing dumps, and
DLSS Ray Reconstruction bridge controls, see:

    docs/ReSTIR/current_restir_pt_state.txt

The early refactor sequence must include a real path-tracer core settings pass
before deeper ReSTIR work: samples per pixel, max path depth, diffuse/specular
bounce limits, first reflection mode, and accumulation reset policy belong to
the base integrator, not only to ReSTIR debug modes.

The same refactor sequence must avoid locking in early reverse-engineered
material and geometry assumptions as final contracts. The current
PathTraceDoomMaterialClassifier can remain a stopgap, but the material universe
needs room for the newer idTech4 material documentation and later RBDoom3/mod
PBR roles such as metallic/roughness, AO, height/parallax, and displacement.
Likewise, source3 portal-area residency is the validated BVH baseline, and the
geometry contract must make current/previous frame vertex or transform data a
near-term addition for motion vectors.

Current State
-------------

The renderer has a debug-only path tracing smoke pass behind r_pathTracing.
It traces primary camera rays through an NVRHI ray tracing pipeline and displays
the RT output as the 3D scene layer while leaving the normal Doom HUD, GUI, and
menus drawn on top.

Mode 18 now skips the normal 3D raster view by default after the RT output has
been produced, while still allowing later GUI/HUD/menu draw commands to rasterize
on top. This is controlled by r_pathTracingSkipRaster3D. Leaving the full 3D
raster path running underneath the path traced output roughly doubled frame cost
in testing and also kept raster-side hitches in play, making RT/PT performance
and stability work harder to interpret. Set r_pathTracingSkipRaster3D to 0 only
for side-by-side regression checks or when explicitly hunting a raster side
effect that the path traced mode still depends on.

PathTracePrimaryPass used to own nearly all of the prototype. It has now been
reduced to a small frame-level shell: feature detection, per-frame entry,
output sizing, and present/blit glue. The scene build, resource lifetime,
dispatch, readback, material processing, diagnostics, acceleration helpers,
skinning bridge, GUI admission, and emissive inventory are split across
PathTrace* modules under neo/renderer/NVRHI.

Current validated feature set:

- Primary camera rays through NVRHI ray tracing.
- Static and dynamic BLAS/TLAS with separate geometry buffers.
- Persistent static-world geometry cache.
- Per-frame dynamic geometry capture.
- RT-only CPU skinning for skinned MD5 characters.
- CPU-side skinned/deforming surface identity and current-to-previous range
  matching for the RT-only CPU-skinned bridge. The previous skinned vertex copy
  is retained for diagnostics and feeds the off-by-default previous-position
  scaffold buffer when that scaffold is enabled.
- Off-by-default PT GPU skinning scaffold. `r_pathTracingGpuSkinning 1` builds
  diagnostic skinned source/current-output/previous-position/dispatch buffers
  and current/previous joint-matrix buffers from the existing CPU-skinned
  bridge, then dispatches a PT-owned compute shader into the diagnostic
  current-output buffer. When previous joint rows are available, that compute
  pass also overwrites the previous-position buffer after the CPU bridge upload,
  leaving the CPU upload as a conservative fallback for ranges without previous
  joint data. The normal BLAS and shader path still consume the existing
  dynamic CPU-skinned output in mode 1. `r_pathTracingGpuSkinning 2` retargets
  the current-output writes to overwrite the validated skinned spans in the
  dynamic vertex buffer before dynamic BLAS build. The mode 2 path remains
  opt-in and still uses the CPU bridge for source geometry, dispatch ranges,
  and joint rows.
- With `r_pathTracingGpuSkinning 1`, skinned primary-surface records can now
  look up the retained previous CPU-skinned position for the same dynamic
  triangle and barycentric point. This only fills the primary-surface
  previous-position/object-motion fields for validated skinned hits; it does
  not change BLAS input, shading, material classification, raster GPU skinning,
  or the future GPU compute skinning plan.
- Routed rigid instances now carry current and previous object-to-world rows in
  their shader-visible route records when a consecutive previous observation is
  available. Primary-surface packing can reconstruct a previous world-space hit
  position from the current rigid triangle barycentrics and previous transform
  without duplicating rigid vertex buffers.
- Bindless material texture table using Texture.Load.
- Manual bilinear Texture.Load filtering.
- Doom diffuse YCoCg decode.
- Alpha any-hit for cutout/perforated materials.
- Albedo, normal, and specular texture discovery.
- Persistent RT smoke material universe records with stable material indexes,
  cached material facts, emissive facts, and an active material table now built
  from the universe by default.
- Debug selected-light direct lighting with RT shadow rays.
- Mode 14 normal-map lighting using Doom/RBDoom's DXT5-style normal decode.
- Mode 14 simple Blinn-style specular from Doom spec maps.
- Mode 14 unshadowed emissive/glow material preview from safe ambient/additive
  stages.
- Mode 18 toy one-bounce path tracing with selected Doom lights, texture
  sampling, force-ambient control, simple secondary rays, temporal
  accumulation, and a first extracted native NEE shader module. Primary
  selected-light direct lighting still uses the legacy full selected-light loop
  for comparison, while secondary selected-light NEE can run off, one-sample
  bounded uniform sampling, or the legacy full loop through
  `r_pathTracingSecondaryNeeMode`. Secondary/reflection Doom analytic-light NEE
  has a separate explicit gate, `r_pathTracingSecondaryAnalyticNeeMode`; mode 0
  disables it, mode 1 evaluates a bounded number of uniformly sampled analytic
  light proposals from the current uploaded candidate set, and mode 2 keeps the
  legacy full analytic-light loop as the reference. The bounded proposal count
  is controlled by `r_pathTracingSecondaryAnalyticNeeSamples`; it does not
  reduce the authored/uploaded analytic-light universe. The native NEE records
  reserve source identity, proposal PDF, target-weight, visibility, MIS, and
path-event fields for later RTXDI/ReSTIR PT extension without importing NVIDIA
sample code. They are RTXDI-shaped local records, not RTXDI-equivalent records
yet: selected point lights use a discrete proposal PDF, leave `targetPdf`
unset until a real target-function evaluator exists, and do not claim a finite
solid-angle PDF.
  Doom analytic samples now have replay-safe helper boundaries:
  `ValidateSmokeDoomAnalyticLightSampleForReplay`,
  `EvaluateSmokeDoomAnalyticLightSampleForSurface`, and
  `SmokeNeeAnalyticTargetWeight`. These helpers let future reservoir final
  shading evaluate one already-selected analytic sample without reselecting or
  looping the light buffer. The replay validation now checks both renderer
  light identity and Doom entity identity (`renderLightIndex` plus
  `entityNumber`) before accepting an analytic sample. Current analytic NEE
  radiance is explicitly marked as solid-angle-scaled local radiance, so later
  ReSTIR code must not divide it by `solidAnglePdf` again.
- Mode 19 emissive triangle inventory visualization. The CPU inventory is a
  broad material/triangle candidate list for future ReSTIR/PT work, while the
  debug view samples emissive texture luminance and skips skinned/deformed
  character dissolve overlays.
- Mode 20 single-frame reservoir direct emissive lighting from the current
  light-candidate contract. Spatial and temporal reuse are still off.
- Host-side RTXDI ReSTIR PT scaffolding. A separate packed PT reservoir buffer
  and RTXDI_PTParameters constant buffer are allocated/updated by local
  parameter packing. The old RTXDI runtime context and NVIDIA
  `Source/ReSTIRPT.cpp` host accumulator are not part of the build. The shader
  build now emits two RT smoke libraries:
  `pathtrace_smoke.rt.bin` for the core mode 18/mode 20/integrator/debug path,
  `pathtrace_smoke_mode18_restir_hybrid.rt.bin` for opt-in mode 18 primary
  direct-lighting ReSTIR integration,
  `pathtrace_smoke_restir_initial.rt.bin` for modes 26-28, and
  separate temporal-family libraries:
  `pathtrace_smoke_restir_temporal.rt.bin` for mode 31,
  `pathtrace_smoke_restir_temporal_shading.rt.bin` for mode 32, and
  `pathtrace_smoke_restir_attribution.rt.bin` for mode 33,
  `pathtrace_smoke_restir_spatial_reservoir.rt.bin` for the mode 50/51 spatial
  reservoir producer prepass,
  `pathtrace_smoke_restir_spatial.rt.bin` for mode 50, and
  `pathtrace_smoke_restir_spatial_attribution.rt.bin` for mode 51. Modes 29-30 are
  primary-surface history/reprojection diagnostics and use the core shader
  table. The core library keeps only the minimal RAB material/surface shims
  needed by shared primary-surface helpers; defining `RB_PT_ENABLE_RESTIR` in a
  ReSTIR wrapper pulls in the RTXDI PT headers and pass-specific RAB pieces.
  `PathTracePrimaryPass` creates the core shader table at smoke-path init and
  lazily creates ReSTIR shader tables only when their mode groups are selected,
  so normal mode 18 startup does not force the driver to ingest the large
  experimental RTXDI/RAB blobs. The temporal wrappers force their debug mode at
  compile time (`RB_PT_FORCE_DEBUG_MODE`), allowing DXC/driver optimization to
  discard unrelated raygen debug branches instead of carrying the whole runtime
  switch into each temporal library. The smoke
  ray tracing shader now has the Step 3 RTXDI reservoir ABI binding
  (`RTXDI_PTParameters` at b28,
  `RWStructuredBuffer<RTXDI_PackedPTReservoir>` at u29, and
  `RTXDI_PT_RESERVOIR_BUFFER` before including `Rtxdi/PT/Reservoir.hlsli`),
  and local Step 4 RAB type declarations under
  `neo/shaders/builtin/pathtracing/RtxdiBridge`. The Step 5 shader bridge can
  now build `RAB_Surface` and `RAB_Material` records from an actual
  `PathTraceSmokePayload` hit, using the same smoke material, albedo, normal,
  opacity, emissive, and specular helpers as the existing modes. The
  `RAB_GetGBufferSurface` and `RAB_GetGBufferMaterial` callbacks now read
  current/previous primary-surface buffers through the named
  PathTracePrimarySurface contract. The record is versioned and mirrored on CPU
  and shader sides at 176 bytes: header/version/valid flags, world position plus
  view depth, geometric and shading normals, view direction, diffuse albedo,
  alpha cutoff, roughness, F0/specular, emissive radiance, material/surface
  flags, instance/primitive IDs, an object/entity ID placeholder,
  height/parallax/displacement placeholder, and previous-position/object-motion
  placeholder. The object/entity ID is deliberately not finalized yet; the
  correct Doom renderer/game identity source must be chosen before object-motion
  rejection or denoiser inputs depend on it. The internal PT convention for
  motion-vector conversion is current pixel to previous pixel, in internal PT
  render-resolution pixels, using unjittered current/previous camera state; any
  external consumer that expects another basis needs an explicit conversion
  shim. The shader helper
  `TryPathTracePrimarySurfacePackedObjectMotionPixels` now centralizes that
  packed-object-motion to current-to-previous-pixel conversion for debug and
  future temporal consumers. Mode 29 validates same-pixel history, and mode 30
  validates temporary current-world-position to previous screen-space
  reprojection for camera motion. Routed rigid instances can now provide
  previous world positions for primary-surface object motion when their previous
  transform is continuous, but final denoiser-facing vector export and
  static-world disocclusion policy are still future work. The Step 6 RNG bridge
  is also present and wraps RTXDI's
  own random sampler initialization and `RTXDI_GetNextRandom` behind local
  `RAB_RandomSamplerState` callbacks so future path replay can preserve seed
  plus consumed random index. Step 7 adds conservative opaque-diffuse BRDF
  callbacks over `RAB_Surface`: cosine-hemisphere sampling, matching solid-angle
  PDF, Lambert BRDF evaluation, and BRDF*cos/PDF throughput. Unsupported
  translucent/special surfaces currently fail closed instead of pretending to
  have a complete BSDF. This is a deliberate validation simplification only:
  the RAB material/history records still preserve roughness, specular F0,
  opacity, alpha cutoff, material flags, and glass/window fallback flags so
  glossy reflection, mirror-like reflection, and glass/transmission behavior can
  be added later without redefining the whole bridge contract. Step 8 adds first light bridge callbacks over the
  existing smoke light data: current-frame emissive triangle loading/sampling,
  current-frame Doom analytic sphere loading/sampling, light target weighting,
  light direction/distance helpers, conservative visibility that only verifies
  valid surface/sample data, and neutral MIS callback placeholders. The Doom
  analytic RAB path uses the same Doom influence radius and quadratic falloff
  convention as mode 18 native analytic NEE, so ReSTIR-side proposals do not
  survive outside the authored light volume. There is still no traced RAB
  visibility, no environment light bridge, and no
  previous-frame light buffer. The RAB bridge include surface has been split
  into pass-sized capability files so future ReSTIR shader libraries can avoid
  pulling the full bridge: `PathTraceRtxdiBridgeCore.hlsli` for minimal records,
  `RAB_SurfaceCore.hlsli` versus `RAB_Brdf.hlsli`, and separate light info,
  sampling, target, visibility, duplication-map, and MIS includes. The existing
  umbrella include remains for compatibility, but new ReSTIR pass shaders should
  include only the capabilities they need. Step 9 adds a compile-validated `RAB_PathTrace`
  skeleton around the smoke TLAS path: it samples the current opaque-diffuse RAB
  BRDF, sets RTXDI continuation-ray state, traces the engine `SmokeScene`,
  converts hits back through `RAB_BuildSurfaceFromSmokePayload`, records hit
  surfaces, and records emissive hits when present. It is deliberately capped to
  a tiny bridge bounce depth and does not run NEE, environment hits, temporal
  replay, spatial reuse, or a dispatched RTXDI shader pass yet.
- Mode 21 solid drawSurf mirror bounds boxes and mode 22 wireframe drawSurf
  mirror bounds boxes. These are replacement diagnostic views for inspecting
  captured bounds/BVH/source issues, not overlays on mode 18/20.
- Mode 23 traces only the experimental rigid TLAS route instances. It is a
  validation view for local-space rigid BLAS plus TLAS transforms, not the
  normal path-traced render path.
- Mode 24 traces the old fallback scene and routed rigid instances separately,
  then color-codes their hit-distance overlap. It is the validation step before
  removing rigid triangles from the dynamic fallback.
- Mode 25 traces the normal fallback scene plus routed rigid TLAS instances
  with a simple material/normal lighting view. It is the first routed-rigid
  lighting validation path and does not change mode 18/20.
- Source 3 scene producer scaffold: existing drawSurf-seeded static cache plus
  mirrored dynamic-frame drawSurfs. It preserves the old shader-compatible
  static/dynamic buffer contract while exposing mesh identity and per-frame
  instance identity for diagnostics.
- Source3 area-selected static preload and rigid residency. Static world
  geometry can be preloaded from the current Doom area plus portal-neighbor
  areas, and routed rigid instances can stay resident from cached mesh/instance
  records instead of disappearing whenever raster visibility stops submitting
  them.
- Default source3 routed rigid BLAS/TLAS path for local-space rigid mesh
  candidates. It uploads per-mesh vertex/index buffers, builds per-mesh BLAS,
  routes instances through TLAS in modes 18/20/23/24/25, and removes
  routed-ready rigid triangles from the dynamic fallback. The old drawSurf-only
  BVH path remains available with `r_pathTracingSceneSource 0` for emergency
  comparison/testing, but it is no longer the normal PT scene stack.
- Rigid emissive-card promotion for stable object-attached signage/glow cards,
  used mainly to keep rigid prop emissives resident with their parent object.
  The gate is conservative and currently excludes `_add + 0200` default-RGBA
  variants plus the known game-logic-driven `swinglight` family.
- Doom renderer light diagnostics and a first analytic sphere-light bridge.
  Authored `idLight` entities are linked back to renderer `lightDef` handles,
  dumped with stable identity/game-state fields, filtered by the same
  current-area plus portal-neighbor selection used for PT light residency, and
  uploaded through a separate analytic light buffer when enabled. The first
  shader path treats every eligible point light as a diagnostic sphere emitter;
  it preserves evaluated RGB and game animation/on-off state, but does not yet
  classify Doom light textures into rectangle/disk/projected/area light types.
  A CPU-only Doom analytic light universe scaffold now records stable keys,
  current sampleability, previous/current remap bookkeeping, duplicate-key and
  invalid-reason counts, and conservative remap rejection for unknown,
  temporary, or otherwise unproven continuity. This scaffold is CPU-side only:
  the legacy compact candidate buffer remains the shader-visible analytic list,
  and current-frame native analytic lighting is unchanged.
  The CPU side now also maintains a persistent authored Doom-light registry
  over structural renderWorld lightDefs. That registry classifies proven
  game-linked non-temporary lights as the persistent static identity list and
  keeps dynamic/unknown/unproven lights in a separate diagnostic bucket. It is
  not yet a sampler and does not change shader lighting behavior. It also
  builds a CPU diagnostic mapping that resolves each current compact
  candidate back to a persistent universe ID, with static/dynamic proposal and
  cap-drop counts for future sampler work. The current CPU universe records are
  now emitted from that persistent registry's current-frame state, rather than
  rebuilding the universe directly from the raw lightDef scan; the compact
  candidate upload and shader-visible lighting behavior remain unchanged.
  Missing persistent static entries remain addressable as non-sampleable
  zero-radiance universe records with `MISSING_CURRENT`, so the authored
  identity domain does not shrink with current collection. The registry also
  maintains a stable static universe-ID list and reports whether current static
  proposals map into it. These static/proposal mappings are not uploaded in
  Task 01 and are not consumed by RAB/ReSTIR.
  Task 02 extends this CPU universe to the GPU as a guarded first-pass bridge:
  current-candidate identity, previous-candidate identity, previous analytic
  light records, and universe remap buffers. The compact Doom analytic
  candidate table is still the current-frame sampling table, but
  `RAB_TranslateLightIndex` now decodes previous analytic light indices with
  the previous emissive-triangle count, maps them through stable universe IDs,
  and returns invalid on failed remaps instead of reusing compact slots. The
  robot/strobe endpoint validation promoted temporal analytic NEE reuse from
  opt-in testing to the default path; the off CVar remains for diagnosis.
- GUI/SWF texture and surface diagnostics behind opt-in CVars.
- Placeholder smoke particle dithering.

RT scene residency and Doom portal areas:

- The current source3 BVH strategy deliberately separates freshness from
  residency. The final raster drawSurf stream is still the best source of
  fresh transforms, material overrides, evaluated shader registers, and
  skinned/deforming geometry. It is not a sufficient residency rule for path
  tracing, because Doom's raster frontend can stop submitting fully occluded or
  off-camera surfaces even though those surfaces must still cast shadows or emit
  light for RT.
- Static world geometry is therefore allowed to preload from Doom renderer
  area/portal membership instead of waiting for `viewDef->drawSurfs` to seed the
  static cache. Rigid routed geometry can similarly stay resident from cached
  mesh/instance records when its last known area remains in the selected area
  set.
- The selected area set is based on renderer portal areas from
  `PointInArea`, `NumPortalsInArea`, and `GetPortal`, not raw BSP leaf/internal
  cells and not the temporary `PathTraceSceneUniverse` inventory as a live scene
  source. `PathTraceSceneUniverse` is used only for static-map diagnostics and
  area-selected static-world preload.
- Portal walks seed from all valid camera-area probes instead of trusting only
  `viewDef->areaNum`: the renderer area, initial view-area origin, exact view
  origin, and short +/- view-axis offsets are inserted at depth 0 when valid.
  This keeps source3 static preload, rigid residency, Doom analytic lights, and
  light-universe area filtering stable when the camera is on a portal border or
  tiny portal-adjacent area. The failure this fixes was not a missing portal
  edge or ignored depth CVar: `viewDef->areaNum` could be valid but point at a
  sliver/connector area on a portal boundary, causing the PT walk to spend
  several hops reaching the room the player visually occupied. At one Mars
  City border this made rigid residents and Doom analytic lights in the next
  visible portal behave as if four walks had been consumed before selection
  reached them. Multi-seeding gives all plausible camera areas depth 0, so
  authored portal topology still matters but border classification no longer
  burns the traversal budget by itself.
- Early current-area-plus-one-step testing was too shallow. Mars City-style
  spaces can look like a single room to the player while spanning several Doom
  renderer portal areas; visible adjacent geometry or emissives may require
  multiple portal hops. Depth 4 is now the default source3 portal traversal
  depth.
- `PS_BLOCK_VIEW` portal edges are reported in diagnostics but currently do not
  stop source3 static preload, rigid residency, or light-area traversal. This is
  intentional while validating RT residency: Doom's raster visibility/blocking
  state is useful diagnostic input, but using it as hard authority reproduced
  raster-like BVH gaps. This policy may need refinement later once the portal
  taxonomy is better understood.
- Source3 with depth-4 portal traversal is now the normal PT default. If a later
  map proves severely game-breaking, the old behavior can be forced from a
  launch file by setting source3/residency/route CVars back to 0.

Current module/refactor state:

- PathTracePrimaryPass is intentionally small and still owns most persistent
  scene/build m_smoke* handles/counters, but output-sized PT textures,
  accumulation/readback resources, smoke reservoirs, ReSTIR PT packed
  reservoirs, primary-surface history buffers, frame settings, reset reasons,
  and readback queue bookkeeping now live behind PathTraceFrameResources.
  Static-world geometry cache ownership has moved behind
  PathTraceGeometryUniverse as the first persistent-geometry scaffold.
- PathTraceSmokeSceneBuild owns the high-level per-frame scene build sequence.
- PathTraceSmokeResources owns NVRHI smoke shader/pipeline creation,
  scene-resource creation/reset/commit, and forwards output-sized resize
  validation to PathTraceFrameResources.
- PathTracePrimarySurface defines the CPU/shader primary-surface record and
  shader helpers used by the current smoke raygen, ReSTIR PT RAB screen
  callbacks, and mode 29/30 history/reprojection validation views.
- PathTraceSceneInputs defines the CPU-side scene/material/light package
  committed after a successful smoke scene build. It records TLAS/BLAS and
  buffer handles, geometry/material/light counts, split signatures, source3
  portal-policy facts, current/previous history placeholders, skinned previous
  CPU-bridge diagnostics, and upload/timing estimates without changing GPU
  layouts or shader dispatch behavior.
- PathTraceSmokeDispatch owns constants, resource transitions, dispatch, and
  accumulation/readback use of the frame resources. It still runs the same
  smoke raygen path; this refactor did not split shader dispatches.
- Scene capture, material classification, texture discovery, texture registry,
  persistent material universe records, active material table construction,
  the static geometry universe scaffold, drawSurf mirror capture, instance
  universe diagnostics, skinning, GUI surface admission, emissive candidates,
  diagnostics, acceleration helpers, light selection, and geometry helpers are
  extracted into dedicated PathTrace* modules.
- The current split is still conservative. A future PathTraceSmokeState or
  PathTraceSmokeScene object may be useful, but that should be a deliberate
  architecture change rather than another mechanical extraction.

Refactor chronology:

1. The first phase proved the RT smoke path in one place, mostly inside
   PathTracePrimaryPass, so feature work could move quickly.
2. Geometry, skinning, GUI admission, material classification, texture registry,
   dynamic material state, emissive candidates, diagnostics, scene capture,
   light selection, smoke dispatch, readback, resources, acceleration helpers,
   CVars, and metadata registration were then extracted into PathTrace* modules.
3. The last broad extraction moved BuildRayTracingSmokeTestScene into
   PathTraceSmokeSceneBuild.cpp and left PathTracePrimaryPass as the pass shell.
4. docs/pathtrace_modules.txt was added after the extraction pass as the detailed
   file/feature map. This file remains the broader chronology and status guide.
5. The material universe then became the default producer for the active RT
   smoke material table, with optional legacy comparison/fallback validation.
   Its persistent records store stable material facts and universe indexes only;
   frame-local descriptor indexes and visible texture bindings stay in
   PathTraceDynamicMaterialState.
6. PathTraceGeometryUniverse was added as the first persistent-geometry
   ownership boundary. It currently owns the static-world cache vectors and
   stats while preserving the existing scene-capture/build behavior.
7. The next phase should be targeted architecture/performance work, especially
   persistent geometry records and GPU buffer ownership, not broad refactoring
   for line count.
8. Mode 18 was changed to behave more like a real PT integration point: the RT
   output owns the 3D scene layer, and the old 3D raster path is skipped by
   default while GUI/HUD/menu raster remains active. This removed the cost of
   rendering both paths at once and isolated a repeatable hitch to the skipped
   raster tail rather than the RT smoke build/dispatch path.
9. The generated-GUI frontEndGeo/layer-offset experiment in GuiModel.cpp was
   reverted. That experiment made Doom's painter-order GUI quads look like real
   scene geometry, which broke in-world monitor/video panels, caused black or
   overlapping screen layers, and triggered severe cinematic/GUI hitches in
   raster and PT routes. GuiModel.cpp should keep the original behavior:
   generated GUI drawSurfs have frontEndGeo = NULL.
10. FFmpeg/Bink decoder choice, upstream/master, Debug-vs-Release, and RETAIL
    were checked as possible explanations for the area-specific performance
    drop. Codec choice did not change the result. A clean latest upstream source
    bisect from v1.6.0 to babd04b3 tested clear; the final "first bad" commit
    was README-only and the direct upstream tip build was also clear. The
    decisive switch was Debug versus Release: fresh Release builds, including a
    generic non-retail Release control, fix the raster stall, while the legacy
    Debug executable reproduces the broken performance. Disabling
    r_useValidationLayers restored raster Debug to the 120 FPS cap, while PT
    remained much slower in Debug/RelWithDebInfo. The working theory is now two
    overlapping debug costs: NVRHI validation for raster, and debug
    runtime/codegen overhead in the CPU-heavy PT scene-build path. It is not
    upstream renderer regression, PT GPU cost, codec choice, or ID_RETAIL.
11. PathTraceDrawSurfCapture and PathTraceInstanceUniverse were added as the
    live-scene mirror for source3. The important split is mesh identity without
    transform versus instance identity with transform/material/entity metadata.
    PathTraceSceneUniverse remains static-map diagnostics/preload only; it is
    not the primary scene representation. Modes 21 and 22 were added as PT-native
    solid/wireframe bounds-box diagnostics after raster/debug-line and post-blit
    overlay attempts proved too fragile for useful spatial inspection.
12. PathTraceGeometryUniverse gained a rigid BLAS GPU scaffold for
    eligible source3 rigid mesh candidates. The scaffold builds local-space
    mesh input buffers, can optionally submit standalone BLAS builds, and was
    then extended into explicitly gated routed rigid TLAS integration for modes
    18/20/23/24/25.
13. Source3 static preload and rigid residency moved the BVH away from pure
    raster-visible drawSurf residency. Static-world cache population and cached
    routed rigid instances now use Doom renderer area/portal selection when the
    relevant gates are enabled. The key validation lesson is that current+1
    portal depth is too shallow for Mars City-style areas; source3 now uses
    depth 4 by default for static preload, rigid residency, and light-area
    diagnostics.
14. Source3, depth-4 static preload, rigid residency, rigid BLAS builds, rigid
    TLAS routing, dynamic rigid removal, and safe rigid emissive-card promotion
    became the default PT scene stack. The original drawSurf-only BVH is kept as
    `r_pathTracingSceneSource 0` for emergency comparison rather than as the
    default path.
15. Doom renderer lights gained a diagnostics-first identity bridge and then a
    first render-affecting analytic sphere-light path. `PathTraceDoomLights`
    maps renderer light indexes back to `idLight` game entities through
    `GetLightDefHandle`, records entity/material/color/state/area/portal-depth
    fields, and builds a separate GPU analytic-light buffer. The shader samples
    those candidates in mode 14/18/20-style lighting without adding sphere
    meshes to the BVH. `PathTraceDoomLights.cpp` owns the candidate radius
    policy through `r_pathTracingAnalyticSphereLightRadiusScale`,
    `r_pathTracingAnalyticSphereLightRadiusMin`, and
    `r_pathTracingAnalyticSphereLightRadiusMax`. The first render path was then
    changed from a hard center-point shadow ray to one-sample solid-angle sphere
    sampling, with the default intensity scale raised to 4.0 and
    `r_pathTracingAnalyticSphereLightRadiusScale` raised to 0.08 after visual
    testing. Initial in-game validation confirmed
    the candidates appear, obey RGB color, follow light animations/on-off game
    logic, and survive a level-load transition after adding game-state and
    render-world map-change guards.

Implemented debug CVars:

- r_pathTracing
  Enables the experimental RT path.

- r_pathTracingDebugWidth / r_pathTracingDebugHeight
  Set the RT debug output resolution. Tested at 320x180 and 1280x720. This is
  the PT internal render size; it is intentionally independent from the
  application/window back-buffer size. Changing one dimension first can produce
  a temporary odd aspect ratio, but aspect ratio itself is not expected to cause
  device removal. The dangerous transition is replacing the PT output,
  accumulation, readback, smoke reservoir, ReSTIR PT reservoir, or primary
  history resources while the previous frame's RT dispatch/blit/copy can still
  be in flight. `ResizeRayTracingSmokeOutput` delegates that destructive
  replacement to PathTraceFrameResources, which waits for the GPU to idle and
  runs NVRHI garbage collection before replacing existing output-sized PT
  resources. Keep that fence at the frame-resource owner; do not move it to the
  app/window resize path, because app resolution changes do not drive PT output
  size.

- r_pathTracingDebugMode
  Selects the debug visualization:
  0 = hit/miss
  1 = depth
  2 = interpolated normal
  3 = surface class
  4 = UV
  5 = geometric normal
  6 = material ID hash color
  7 = material table debug color
  8 = sampled diffuse texture probe
  9 = alpha/coverage preview
  10 = albedo
  11 = translucent overlay inspection
  12 = translucent subtype classification
  13 = simple fixed-light Lambert shading
  14 = selected point-light direct lighting with RT shadow rays
  15 = selected point-light influence debug
  16 = normal map preview
  17 = specular map preview
  18 = toy one-bounce path trace
  19 = emissive triangle inventory
  20 = single-frame reservoir direct lighting
  21 = solid drawSurf mirror bounds boxes
  22 = wireframe drawSurf mirror bounds boxes
  23 = experimental routed rigid TLAS instances
  24 = fallback-vs-rigid-route overlap validation
  25 = routed rigid lighting validation
  26 = ReSTIR PT initial reservoir diagnostics
  27 = ReSTIR PT initial reservoir shading preview
  28 = ReSTIR PT initial reservoir visibility preview
  29 = ReSTIR PT primary-surface history validation
  30 = ReSTIR PT screen-space reprojection validation
  31 = ReSTIR PT temporal reservoir validation
  32 = ReSTIR PT temporal reservoir shading preview
  33 = ReSTIR PT temporal light-source attribution
  34 = path tracer core path-depth visualizer
  35 = path tracer reflection hit/miss visualizer
  36 = path tracer reflection roughness/F0 gate visualizer
  37 = path tracer max-depth / Russian-roulette state visualizer
  38 = skinned primary object-motion vector diagnostic
  39 = routed-rigid object-motion eligibility diagnostic
  40 = routed-rigid primary object-motion vector diagnostic
  41 = combined skinned/routed-rigid object-motion vector diagnostic
  42 = packed primary object-motion flags diagnostic
  43 = packed object-motion reprojection match diagnostic
  44 = previous static snapshot shader-binding diagnostic
  45 = previous static snapshot reprojection match diagnostic
  46 = previous static snapshot motion-vector diagnostic
  47 = combined geometry motion-vector diagnostic
  48 = combined geometry reprojection-match diagnostic
  49 = combined geometry motion-source diagnostic
  50 = ReSTIR PT spatial reservoir shading preview
  51 = ReSTIR PT spatial source attribution
  52 = routed-rigid transform parity diagnostic

- r_pathTracingMode20TestPreset
  One-shot console helper that applies the common mode 20 source3 routed-rigid
  test stack, then resets itself to 0:

      1 = mode 20 + source3 + rigid route + dynamic rigid removal
      2 = preset 1 plus light-area diagnostics
      3 = preset 2 plus render-affecting light-area filter apply
      4 = preset 2 for BVH validation with light-area diagnostics but no
          render-affecting light-area filter apply

  The source3 routed stack is now already default-on. This helper remains a
  convenience/debug shortcut for switching to mode 20 and enabling extra
  diagnostics. All presets use portal depth 4, keep source3/routed rigid/static
  preload/rigid residency enabled, and ensure
  `r_pathTracingRigidRouteMaxInstances >= 256`. Presets 2, 3, and 4 also enable
  `r_pathTracingLightUniverseChurn 1`; preset 3 enables render-affecting
  light-area filter apply.

- r_pathTracingMode18TestPreset
  One-shot console helper that applies the same source3 routed-rigid/static
  preload/rigid residency stack to mode 18, then resets itself to 0:

      1 = mode 18 + source3 + rigid route + dynamic rigid removal
      4 = preset 1 plus portal depth 4 and rigid route max 256 for current
          BVH validation

  The source3 routed stack is now already default-on. This helper remains a
  convenience/debug shortcut for switching to mode 18 and ensuring depth-4
  source3/routed rigid/static preload/rigid residency state.
  Routed rigid hits still use the compact rigid route payload. Interactive
  condition-register emissive cards that need exact game-logic toggling should
  stay out of rigid emissive-card promotion until the route has a safe
  per-frame material-state channel.

- r_pathTracingClassDump
  One-shot dump of sampled surface classification reasons.

- r_pathTracingTextureProbeIndex
  Selects the material-table index to bind for debug texture probing.
  -1 means first safe texture.

- r_pathTracingTextureProbeReset
  Releases the latched texture probe and selects again.

- r_pathTracingTextureProbeDump
  One-shot dump of the currently latched texture probe and safe candidates.

- r_pathTracingTextureTableLimit / r_pathTracingTextureTableStart
  Selects a window of safe captured diffuse textures for debug mode 8. The
  active slot count currently supports up to 2048 descriptors for broader albedo
  coverage while texture sampling remains experimental.

- r_pathTracingTextureSampleMethod
  Selects the mode 8 texture fetch path:
  0 = disabled/fallback material color
  1 = SampleLevel diagnostic path
  2 = Texture.Load stable default

- r_pathTracingTextureFilter
  Selects filtering for Texture.Load sampling:
  0 = point Load
  1 = manual bilinear Load

- r_pathTracingTextureDecode
  Enables decode of Doom texture encodings in the RT texture path. Defaults to
  1. Currently this decodes diffuse YCoCg textures before albedo/lighting use.

- r_pathTracingForceTextureCodeUse
  Opt-in experiment for Doom texture filename usage codes. Defaults to 0. When
  set to 1, RT smoke material discovery treats four-digit filename codes near
  the end of image names as usage hints: 0300 = normal, 1000 = hard alpha/cutout,
  0100 = diffuse/YCoCg payload, 0200 = default RGBA, and 0000 = specular. These
  hints are used as tie-breakers before the older stage-lighting discovery
  paths, not as final material semantics. The code is generated from image usage
  and cube-file enums, so image->GetOpts().colorFormat and the material stage
  remain better truth sources when they are available.

- r_pathTracingUseNormalMaps
  Enables sampled normal maps in debug mode 14 selected point-light direct
  lighting. Defaults to 1. The normal map is decoded in the shader and applied
  through a per-hit tangent basis derived from triangle position/UV gradients.
  The decode follows the existing raster G-buffer convention: normal X is read
  from texture alpha, normal Y from green, and normal Z is reconstructed. This
  is an experimental bridge toward the later Doom material-to-PBR path;
  roughness/specular conversion is still separate future work.

- r_pathTracingNormalMapFlipGreen
  Diagnostic toggle for Doom 3 legacy normal-map convention checks. Defaults to
  0. When enabled, the RT smoke shader flips sampled normal-map green/Y before
  tangent-space reconstruction. Use this to A/B modes 26/27 after the ReSTIR PT
  initial-reservoir path showed valid reservoirs but rough normal-map shading.
  This is not a complete Doom normal fix: the current RT hit path still builds
  TBN from triangle UV gradients instead of the original idTech4 vertex
  tangent/sign basis used by legacy renderbump assets.

- r_pathTracingUseSpecularMaps
  Enables sampled specular maps in debug mode 14 selected point-light direct
  lighting and in mode 18 when `r_pathTracingToyFakePBRSpecular 1` is active.
  Defaults to 1. Mode 14 still uses a simple Blinn-style diagnostic highlight
  using Doom's specular color map. Mode 18 can opt into the rbdoom3 legacy
  specmap-to-PBR approximation described below.

- r_pathTracingUseEmissiveMaps
  Enables debug-only emissive/glow material contribution in mode 14. Defaults
  to 1. The RT smoke path looks for safe ambient/additive or glow/signage-like
  material stages, binds their texture when available, and adds the result as
  an unshadowed emissive term. This is a visibility/material-capture diagnostic,
  not light-source sampling yet. GUI/screen, particle, glass/window, animated,
  view-dependent stage textures, and skinned/deformed hits are rejected for now
  to avoid broad white false positives and game-logic-driven overlays.

- r_pathTracingToyMaxRayDistance
  Maximum secondary/direct ray length for mode 18 toy path tracing. Defaults to
  1024 Doom units. This helps reduce leaks from holes in the current
  visible-surface TLAS.

- r_pathTracingToyLightScale / r_pathTracingToyEmissiveScale
  Separate multipliers for mode 18 selected-light and emissive material
  contribution. Current useful defaults are 0.3 for lights and 4.0 for
  emissives. ReSTIR PT mode 56 also uses these as preview calibration:
  analytic sphere lights use
  `r_pathTracingAnalyticLightIntensityScale * r_pathTracingToyLightScale`, and
  emissive reservoirs use `r_pathTracingToyEmissiveScale`.

- r_pathTracingAnalyticLightIntensityScale
  Radiance calibration scale for Doom analytic sphere lights. Defaults to 4.0.
  In ReSTIR PT mode 56 this is multiplied by `r_pathTracingToyLightScale`, so
  the effective default analytic-light preview scale is 1.2.

- r_pathTracingToyLightTraceCap
  Caps the number of selected point lights that mode 18 traces for each direct
  lighting evaluation. Defaults to 8, max 32. This protects the toy PT path
  from brute-forcing too many shadow rays per pixel while ReSTIR PT work is
  still being staged. The cap is applied on the CPU by reducing the mode 18
  light-selection request count; the shader-facing LightInfo layout is
  unchanged.

- r_pathTracingToyFakePBRSpecular
  Opt-out shader experiment. Defaults to 1. When enabled, mode 18, ReSTIR PT
  bridge modes 26/27/28/29/30/31/32/33/50/51, and path-tracer visualizer modes
  34/35/36/37 also enable specular-map sampling and interpret Doom legacy
  specular maps using the same rbdoom3/Kennedith98-style fake-PBR idea as the
  raster interaction shaders: derive approximate F0 and roughness from the
  gamma-space specular texture. For ReSTIR PT this roughness and F0 are stored
  in the RAB material and primary-surface history records so temporal/spatial
  validation sees the converted Doom material state instead of treating every
  legacy surface as roughness 1.0. This is useful for a quick visual check of
  less-flat PT diffuse/specular response, but it is not a full Doom material
  conversion. It does not detect packed `_rmao` maps, does not add glossy bounce
  sampling, and does not solve alpha/blend/decal/special-stage classification.

- r_pathTracingToyAccumulation / r_pathTracingToyAccumMaxFrames
  Enables mode 18 temporal accumulation across stable camera frames and caps
  the accumulated frame count. Accumulation resets on camera/FOV/output-size
  changes and on relevant lighting, ambient, and toy-path CVar changes.

- r_pathTracingSamplesPerPixel
  Explicit mode 18 samples per pixel. Defaults to 1 and currently clamps to 1..4
  because the smoke raygen is still a monolithic validation dispatch. Values
  above 1 jitter additional primary rays per pixel and multiply secondary/direct
  ray work, so use `r_pathTracingIntegratorDump 1` before raising resolution and
  SPP together.

- r_pathTracingMaxPathDepth
  Maximum mode 18 path depth including the primary hit. Defaults to 2, which
  preserves the previous one-secondary-bounce toy behavior. Setting this to 1
  disables secondary diffuse/reflection bounces even if their lobe limits are
  nonzero.

- r_pathTracingDiffuseBounceLimit / r_pathTracingSpecularBounceLimit /
  r_pathTracingTransmissionBounceLimit
  Explicit per-lobe mode 18 bounce limits. Defaults are diffuse=1, specular=0,
  transmission=0, preserving the previous diffuse-only toy path. Transmission is
  reserved and currently fails closed; glass/liquid policy is not implemented in
  this task.

- r_pathTracingReflectionMode
  First opt-in reflection baseline for mode 18. Defaults to 0/off. Mode 1 traces
  one mirror-ish validation bounce only on low-roughness/high-F0 surfaces when
  `r_pathTracingSpecularBounceLimit > 0`; mode 2 relaxes the roughness gate for
  rougher validation. It is crude and denoiser-free by design.

Reflection/specular reservation:

- The current reflection path is only a validation bounce in mode 18. Future
  reflection work should have separate output/history names and carry roughness,
  F0/specular, geometric normal, shading normal, hit distance, reflection
  confidence, current/previous surface identity, and motion-vector or previous
  position input. Do not fold reflection history into diffuse ReSTIR history.
- Half-resolution or checkerboard reflection execution is a valid future policy,
  but it should be an explicit pass setting after pass splitting, not an
  implicit side effect of debug mode 18.

- r_pathTracingRussianRouletteDepth
  Reserved depth where Russian roulette may start. Defaults to 0, disabled. The
  current conservative shader path only reports the configured state in debug
  mode 37; it does not randomly terminate paths yet.

- r_pathTracingNextEventEstimation
  Enables the existing direct-light work from selected lights, emissive/Doom
  analytic light candidates, and the ReSTIR PT bridge path tracer. Defaults to
  1 to preserve current mode 18 behavior. Setting it to 0 leaves ambient and
  emissive terms but suppresses direct-light visibility work in the new
  integrator contract.

- r_pathTracingSecondaryNeeMode
  Selects secondary selected-light NEE behavior in mode 18 when
  `r_pathTracingNextEventEstimation` is enabled: 0 disables secondary
  selected-light NEE as a diagnostic throttle, 1 samples one uniformly selected
  point light and compensates by `selectedLightCount`, and 2 runs the legacy
  full selected-light loop. The default is 1. Doom analytic-light NEE remains
  controlled by the analytic-specific secondary gate below and is not folded
  into this selected-light sampler, but it now uses the same native
  `SmokeNeeLightSample` and `SmokeNeeResult` helper boundary inside
  `pathtrace_nee.hlsli`.

- r_pathTracingSecondaryNeeVisibility
  Diagnostic visibility gate for sampled secondary selected-light NEE. Defaults
  to 1, which traces the selected sample's shadow visibility ray. Set to 0 only
  to isolate sampled-light selection and weighting from shadow-ray visibility
  pressure; it shades selected samples as visible and is not a final lighting
  mode.

- r_pathTracingSecondaryAnalyticNeeMode
  Selects secondary/reflection Doom analytic-light NEE behavior in mode 18 when
  `r_pathTracingNextEventEstimation` and analytic-light candidates are enabled:
  0 disables secondary analytic NEE, 1 evaluates bounded uniformly sampled
  analytic-light proposals, and 2 runs the legacy full analytic-light loop. The
  default is 1 because testing with large analytic-light counts showed major
  performance gains without obvious image loss. Primary-hit analytic direct
  lighting still uses the legacy full loop for comparison.

- r_pathTracingSecondaryAnalyticNeeSamples
  Number of Doom analytic-light proposals evaluated per secondary/reflection
  NEE vertex when `r_pathTracingSecondaryAnalyticNeeMode` is 1. It clamps to
  0-8 and defaults to 4. This is a per-vertex proposal bound; the eligible
  analytic light set remains the uploaded Doom analytic-light candidate buffer
  after normal safety/upload caps.

- r_pathTracingIntegratorDump
  One-shot dump of the path tracer core settings and rough ray-budget estimate:
  output resolution, SPP, max depth, diffuse/specular/transmission limits,
  reflection mode, Russian-roulette depth, NEE state, secondary NEE mode and
  sampled visibility gate, selected/analytic light trial counts, estimated rays
  per pixel, and estimated dispatch rays. This is a safety signal for
  device-removal risk; it is intentionally approximate rather than a physical
  ray counter.

Ray dispatch safety diagnostics:

- r_pathTracingSafetyDump
  One-shot pre-dispatch safety dump. It reports output size, debug mode, SPP,
  depth/bounce/reflection/Russian-roulette/NEE settings, secondary NEE mode and
  sampled visibility gate, selected/analytic/emissive/light-candidate counts,
  material table entries, active descriptor table size, static/dynamic/
  rigid-route buffer capacities, TLAS/BLAS presence, primary-history byte
  count, smoke/ReSTIR reservoir byte counts, binding-set presence, and the
  current safety kill-mask.

- r_pathTracingDispatchTileEnable
  Diagnostic device-removal mitigation path. When set, the smoke/path tracing
  raygen uses the same TLAS, bindings, clears, and command list, but splits
  `DispatchRays` into sequential screen-space rectangles. Default is 0, so the
  normal full-screen dispatch path is unchanged.

- r_pathTracingDispatchTileWidth / r_pathTracingDispatchTileHeight
  Tiled-dispatch rectangle size in pixels. The last row/column dispatches a
  smaller partial tile when the output size is not evenly divisible. Shader-side
  output, primary-history, reservoir, reprojection, and random-seed coordinates
  use absolute full-frame pixels rather than tile-local coordinates.

- r_pathTracingDispatchTileDump
  One-shot tiled-dispatch plan dump. It reports output size, tile size, tile
  count, estimated rays per pixel, estimated rays per tile, and estimated rays
  for the full frame.

- r_pathTracingDisableAnyHitAlpha
  Diagnostic kill switch for any-hit alpha/translucent/glass/particle rejection.
  Shadow self-ignore still runs; only the alpha/translucent rejection path is
  bypassed.

- r_pathTracingDisableSelectedLightLoop
  Diagnostic kill switch for legacy selected point-light loops and selected-light
  sprite proxies.

- r_pathTracingDisableAnalyticLightLoop
  Diagnostic kill switch for Doom analytic light loops and ReSTIR/RAB analytic
  light sampling. This is independent from the upload/build CVars.

- r_pathTracingDisableEmissiveTriangleSampling
  Diagnostic kill switch for emissive triangle sampling in mode 20 and ReSTIR PT
  validation paths.

- r_pathTracingDisableDiffuseSecondaryRay
  Diagnostic kill switch for diffuse secondary/continuation rays in mode 18 and
  the RAB path-tracer bridge.

- r_pathTracingDisableReflectionRay
  Diagnostic kill switch for mode 18 reflection/specular secondary rays.

- r_pathTracingDisablePrimarySurfaceHistory
  Diagnostic kill switch for primary-surface history writes, clears, copies, and
  previous-camera history validity. Re-enabling it forces the history path to
  clear before it becomes valid again.

- r_pathTracingDisableReservoirWrites
  Diagnostic kill switch for shader-side smoke and ReSTIR PT reservoir writes.
  Re-enabling leaves the smoke reservoir clear request pending.

- r_pathTracingDisableRestirVisibilityRay
  Diagnostic kill switch for the ReSTIR PT preview visibility ray even when a
  mode or `r_pathTracingRestirPTPreviewVisibility` requests it.

- r_pathTracingRestirPTPreviewExposure
  Mode 32/50 rough ReSTIR PT lighting-preview exposure multiplier. Defaults to
  1, clamps to 0..16, and only changes the preview tonemap for
  `TargetFunction * WeightSum`. It does not affect mode 18 or reservoir
  selection.

- r_pathTracingRestirPTTemporalDepthThreshold
  RTXDI ReSTIR PT temporal/spatial neighbor relative-depth threshold for modes
  31-33 and 50-51. Defaults to 0.1. Lower values reject history more
  aggressively; higher values tolerate more depth mismatch at the cost of more
  ghosting risk.

- r_pathTracingRestirPTTemporalNormalThreshold
  RTXDI ReSTIR PT temporal/spatial neighbor normal-dot threshold for modes
  31-33 and 50-51.
  Defaults to 0.35, softened from RTXDI's stock 0.6 for Doom 3 normal-map and
  legacy tangent-basis stability. Raise it to reject more history; lower it if
  stable motion-source objects still flicker on particular sides or distances.

- r_pathTracingRestirPTTemporalReservoirReuse
  Diagnostic gate for ReSTIR PT temporal/spatial preview modes. Defaults to 1.
  Set to 0 to reject previous-frame PT reservoirs while still generating the
  current initial reservoir. Use it to isolate exact one-frame alternating
  polygon flicker from previous-reservoir reuse without changing current-frame
  candidate generation. Setting it back to 1 restores RTXDI's default temporal
  max reservoir age on the next dispatch.

- r_pathTracingRestirPTTemporalFallbackSampling
  RTXDI ReSTIR PT temporal zero-motion fallback sampling gate for modes 31-33
  and 50-51. Defaults to 1 now that motion-vector projection and the
  pre-combine bad-NEE rejection path are in place. Set to 0 only to isolate
  same-material wrong-surface history reuse against the stricter
  motion-projected path.

- r_pathTracingRestirPTTemporalAnalyticNeeReuse
  Previous-frame NEE light-reservoir reuse gate for ReSTIR PT temporal and
  spatial modes. Defaults to 1 now that shader-visible Doom analytic
  identity/remap buffers, previous analytic light records, RAB translation/load,
  and pre-combine bad-NEE rejection have been validated on the robot/strobe
  endpoint case. Set to 0 to isolate current-frame NEE sampling from previous
  light history without disabling non-NEE temporal reuse.

- r_pathTracingRestirPTTemporalAnalyticLightChangeTolerance
  Relative current/previous Doom analytic light color/intensity tolerance used
  by the ReSTIR PT RAB remap. Defaults to 0.10. When temporal analytic NEE reuse
  is enabled, matched light identity is not enough: the bridge also rejects
  previous analytic reservoirs if the light's current/previous color, intensity,
  position, or radius diverges beyond this guard. This targets
  strobe/animated-light phases without globally clearing unrelated reservoirs.

- r_pathTracingRestirPTSpatialSamples
  Mode 50/51 RTXDI_PTSpatialResampling and active RRX DI spatial-resampling
  neighbor samples per pixel. Defaults to 1 for early Vulkan/device-removal
  stability and clamps to 1..32. The temporary debug path ties disocclusion
  boost samples to this same value to avoid hidden workload jumps.

- r_pathTracingRestirPTSpatialRadius
  Mode 50/51 RTXDI_PTSpatialResampling and active RRX DI spatial-resampling
  neighbor sampling radius in pixels. Defaults to 16 and clamps to 1..128.

- r_pathTracingRestirPTCombinedSpatialRadius
  Legacy direct-spatial radius control. Do not use this CVar as evidence that
  DI/direct temporal or spatial reservoir reuse is complete.

- r_pathTracingRestirPTSpatialDiagnosticView
  Legacy mode 51 diagnostic selector. Older color names mention spatial or
  temporal comparisons, but DI/direct is raw reservoir output on this branch.
  Do not use this view as proof that direct temporal/spatial reuse is complete.

- r_pathTracingRestirPTDiDebugView
  Mode 56 DI comparison view. Defaults to `72`, the active RRX DI
  final-consumer proof path. `0` keeps legacy normal mode-56 view 0 for
  comparison only, `1` shows the raw DI reservoir estimate, `2` shows the
  debug-only DI temporal reservoir
  as a direct-light preview with temporal reservoir history, `3` shows the
  debug-only DI spatial candidate, `4` shows raw DI reservoir
  validity/M/WeightSum, `5` shows DI spatial/temporal status colors, `6` shows
  the RTXDI final-shading input as `TargetFunction * WeightSum`, `7` shows
  the RTXDI final-shading input validity/target/weight/M state, `8` shows the
  official RTXDI page-chain state in quadrants, and `9` shows the official page
  chain's `TargetFunction * WeightSum` contribution in quadrants. View `10`
  shows a temporal-readiness/rejection proxy over the official path: reset-mask
  colors first, then primary-surface projection/similarity colors, then
  blue/green when temporal output has valid history. View `11` classifies the
  official temporal page flow: red = initial invalid, blue = temporal input
  invalid, purple = temporal output invalid, cyan = output valid but current-only
  M, magenta = output kept prior input M but did not grow, and green/blue =
  output M grew beyond current/input. View `12` classifies the RTXDI-shaped
  temporal-neighbor acceptance stage: red = no motion/projection, dark blue =
  missing/out-of-range previous surface, purple = depth/normal rejection, yellow
  = material rejection, blue = previous reservoir invalid, orange = reservoir
  age cap, cyan = a previous candidate was acceptable but output stayed
  current-only, bright orange = the accepted previous sample is NEE while
  previous NEE reuse is disabled, white = invalid sampled-light data, bright green =
  accepted previous emissive light has no stable identity/remap, blue/purple/
  magenta/pink = accepted analytic identity/remap/range failure, gray =
  accepted analytic light state mismatch, and dark green/blue = output history
  grew. For views 8
  and 9, top-left is initial, top-right is temporal output, bottom-left is
  spatial output, and bottom-right is final-shading input. View `58` is the
  binary temporal keep-mask view: black = rejected and red = temporal history
  accepted. It is a debug visualization only and should not be interpreted as
  a change to reservoir finalization. View `59` is a temporary light-manager
  reservoir persistence probe: it selects stable lights through the ReSTIR
  light-manager RAB path, stores a simple full-screen diagnostic probe in the
  DI temporal reservoir pages, and shows whether the probe can retain history.
  Green means the previous probe mapped back to the same current light and
  history grew; blue means first/no previous probe; orange means previous probe
  existed but did not remap to the same current light; red means current to
  previous light-manager remap failed. View `60` is the RRX DI single-light
  heartbeat. Views `63`-`66` inspect RRX DI reservoir persistence/raw field
  behavior across same-dispatch, same-page, and previous-page cases. View `67`
  checks active RAB translation parity. View `68` is the RRX DI temporal local
  invalidation view: the left half is split into five vertical cause bands
  from left to right: projected, previous sample used, previous-to-current
  translation valid, current-to-previous translation valid, and selected-light
  stability. Green means pass/stable, red means failed, blue means no previous
  sample, purple means previous remap invalid, orange means current/previous
  selected-light mismatch, yellow means global clear pending, and dark means not
  applicable.
  The right half shows temporal output target-PDF/history/weight heat. This is
  not full Remix final-shading parity; it is a
  mode-56/preview consumer for the RRX DI temporal reservoir path. View `69`
  outputs raw RRX DI temporal light-index tuples for terminal readback:
  current light, current-to-previous translation, previous stored light, and
  previous-to-current translation, each encoded as `index + 1` with `0` meaning
  invalid or absent. Views `60`-`77` bypass mode-56 accumulation so RRX DI
  heartbeat, raw storage, temporal, final-consumer, and weight-audit proofs are
  not stabilized by the old `SmokeAccumulation` path. View `70` is the RRX DI
  temporal input evidence view. It displays fourteen vertical bands:
  producer-record expected motion source, producer-record expected motion XY,
  reset-mask safety, previous-surface validity at the RTXDI reprojected pixel,
  same-depth baseline pass, exported motion-vector Z expected-depth pass,
  normal-dot pass, RTXDI previous-sample use, temporal output validity,
  selected-light hash, exported `PathTraceMotionVectorMask` source/validity,
  exported `PathTraceMotionVectors.xy`, current producer-record source
  classification, and current producer-record packed-motion status. The final
  source-classification band only reports routed rigid when the producer record
  has both `instanceId >= 2` and `surfaceClass == RIGID_ENTITY`; cyan across a
  moving rigid object proves the primary producer is hitting the routed rigid
  TLAS copy rather than the legacy dynamic fallback. It is an input evidence
  view, not a final lighting-quality view. View `61` is the RRX DI initial
  survivor/replay attribution view and bypasses mode-56 accumulation. It shows
  eight vertical bands: selected initial reservoir light type, selected
  temporal-output light type, final/preview replay status, emissive candidate
  survival, Doom analytic candidate survival, light-info-invalid rejection
  presence, `RAB_SamplePolymorphicLight` invalid-sample rejection presence, and
  zero-target-PDF rejection presence. Green means emissive, cyan means Doom
  analytic, yellow means both types in a rejection band, purple means selected
  light-info invalid, orange means sampled-light invalid, red means zero
  target PDF, blue in the sample-invalid band means a Doom analytic sample is
  outside the authored Doom influence radius, pink in that band means a
  non-finite analytic payload reached sampling, blue in replay means
  normal/facing rejection, dark red means preview visibility rejection, and
  black/dark means invalid or absent. Use this view after the range dumps prove
  non-zero bounded emissive and Doom analytic samples; it localizes whether
  analytic candidates die at load, polymorphic sampling, target PDF, reservoir
  selection, or preview replay. View `62` is the RRX DI mode-56 view-0 branch
  audit and bypasses mode-56 accumulation. It mirrors the normal
  combined-resolve decision path, then displays eight bands: surface lighting
  gate, selected direct source, RRX replay status, unscaled RRX contribution,
  legacy spatial-direct comparison contribution, GI comparison contribution,
  raw reflected BSDF radiance, and final no-reflection view-0 preview output.
  In band 0, green
  means the surface can take opaque diffuse lighting, purple means legacy view
  0 would return only base/emissive because the surface fails that gate, and
  dark red means invalid surface. In band 1, cyan/green follows the selected
  RRX light type, yellow means only the legacy comparison branch found an old
  spatial-direct reservoir, and red/dark means no direct contribution. Use it
  only as a branch audit; it is not the active view-0 acceptance path. View
  `72` is the default RRX DI final-consumer bridge. It bypasses
  the old combined-resolve preview, consumes the RRX DI spatial-output reservoir
  selected light, samples that light through RAB, checks target PDF and
  `solidAnglePdf * reservoir.weightSum`, traces local visibility, and displays
  only direct DI contribution as
  `BRDF * radiance * NdotL * reservoir.weightSum / solidAnglePdf`. The top 24
  pixels are a status strip: green means emissive success, cyan means Doom
  analytic success, purple means light-info/resource failure, orange means
  sampled-light invalid, yellow means zero solid angle, amber means zero
  reservoir weight, red means zero target PDF, blue means facing/NdotL rejection,
  dark red means visibility rejection, pink means zero final contribution, and
  black below the strip means the selected sample did not contribute. Like the
  other RRX proof views, view `72` bypasses
  `r_pathTracingRestirPTMode56Accumulation`; use it to inspect the raw per-frame
  final-consumer result, not old accumulator convergence. View `73`
  is the temporal-output final-consumer comparator kept for checking temporal
  reservoir history while the active view-72 path consumes spatial output. Its
  left half is the per-pixel status color and the right half is contribution
  magnitude for successful samples, tinted by selected light type. View `74`
  visualizes the temporal-output final-consumer light identity: the left half
  is a stable hash color for the selected light index, mixed slightly with
  light type; the right half uses the same identity color but darkens
  successful samples by contribution magnitude and shows failure status colors
  for rejected samples. Large same-
  colored regions in overlapping-light areas indicate selection locking onto
  one source. View `75` is the RRX DI weight-chain audit. It shows eight bands:
  max target PDF by light range, source-PDF scale by range, RIS weight sum after
  `RTXDI_StreamSample`, random-reservoir inverse PDF after
  `RTXDI_FinalizeResampling`, combined pre-finalize weight sum, final initial
  inverse PDF after finalization, previous-best pressure versus current random
  candidates, and temporal current/previous/output weight heat. View `76` is
  the selected-light weight audit. It shows eight bands for the final initial
  selected light: selected source (emissive, Doom analytic, or previous-best),
  selected light-index hash, selected target PDF, selected invSourcePdf or
  effective source weight, selected RIS/effective weight, combined
  pre-finalize weight sum, final inverse selection PDF, and previous-best
  pressure versus current random candidates. View `77` is the RRX DI spatial
  proof view. It shows eight bands: temporal input valid, spatial output valid,
  RTXDI spatial sample count, spatial selected light type, spatial selected
  light identity, spatial output weight, spatial target PDF, and final-consumer
  source. The final-source band should be green for spatial; blue/cyan means a
  temporal/current-only debug route is active, and red means no valid final
  source. Active RRX spatial uses the RTXDI DI spatial helper with a
  conservative reprojection-confidence default until real Remix confidence
  textures are bound. Use these views for DI-only
  validation with GI, reflections, DLSS RR, and legacy mode-56
  fallback behavior out of the result. Views 6-11 are the
  reference-shaped DI baseline: they consume the existing RTXDI temporal/spatial
  producer output and avoid the custom DI scratch reservoir math. View 2 does
  not multiply brightness by reservoir `M`; view 3 runs a
  debug-only temporal prepass into the DI reservoir buffer, barriers it, then
  combines the current temporal DI candidate with validated current-frame
  temporal DI neighbors. The temporal debug path can carry validated previous
  DI temporal history when the current raw DI sample is missing. These views
  write only the debug output, not RR input.

- r_pathTracingRestirPTRrxFinalConsumerOutput
  Legacy/debug mode-56 view-0 replacement switch. Defaults to 0. The active RRX
  proof path is DI debug view `72`, selected by
  `r_pathTracingRestirPTDiDebugView 72` by default. Set this to 1 only when
  explicitly comparing whether view 0 can be replaced by the same RRX DI
  final-consumer bridge; do not use it as the default acceptance path. The
  normal RRX route now defaults to
  `r_pathTracingRemixLightManagerRAB 1` and internally enables the unified
  light load/sample shape needed by manager payloads, so the old unified
  load/sample CVars are legacy/debug comparison switches rather than required
  activation steps. It bypasses `r_pathTracingRestirPTMode56Accumulation` so
  normal view-0 final-consumer proof is not stabilized by the old mode-56
  accumulator.

- r_pathTracingRestirPTRrxFinalConsumerCurrentOnly
  Debug-only RRX DI final-consumer switch. Defaults to 0. Set to 1 to consume
  the current-frame RRX DI reservoir before temporal reuse. Use it with
  `r_pathTracingRestirPTRrxFinalConsumerOutput 1` or DI debug view `72` to
  distinguish a valid current producer/final-consumer path from temporal reuse
  or history state that later suppresses the contribution.

- r_pathTracingRestirPTRrxDebugBypassMotion /
  r_pathTracingRestirPTRrxDebugBypassDepth /
  r_pathTracingRestirPTRrxDebugBypassNormal /
  r_pathTracingRestirPTRrxDebugBypassSurfaceSimilarity /
  r_pathTracingRestirPTRrxDebugBypassResetMask /
  r_pathTracingRestirPTRrxDebugBypassPortal
  Debug-only RRX DI temporal validation bypasses. Defaults to 0. Set one of
  these to 1 only to isolate which temporal acceptance gate is rejecting
  previous-frame reuse. They do not change current-frame initial sampling or the
  RAB light sampling contract, and they should stay off for normal validation.
  DI debug view `68` shows the active bypass state in its top bands.

- r_pathTracingRestirPTRrxDebugFlatContribution
  Debug-only RRX DI combined-output probe. Defaults to 0. Set to 1 to replace a
  valid temporal DI contribution with a fixed color after reservoir generation.
  This proves the mode-56 output route and accumulation path can display an RRX
  DI result, but it is not a lighting-quality view and should not be used as
  evidence that sampled light contribution is correct.

- r_pathTracingRestirPTRrxTemporalPermutation
  RRX DI temporal reuse switch. Defaults to 1 because the active RRX temporal
  path feeds the Remix-style RTXDI temporal descriptor. It enables RTXDI
  temporal permutation sampling with a frame-varying uniform random number,
  matching the reference purpose of adding temporal variation to previous-frame
  reuse and making the output friendlier to denoising. Set it to 0 only for
  debug comparison when isolating selected-light lock-in or temporal jitter.

- r_pathTracingRestirPTRrxDebugPreviousTemporalAsBest
  Debug-only non-Remix RRX DI A/B switch. Defaults to 0/off. Set to 1 only to
  restore the old approximation that feeds the projected previous temporal
  reservoir as the previous-best candidate during initial reservoir
  construction. Default RRX DI has no previous-best source until a real
  RtxdiBestLights-equivalent input is bound. This does not disable RTXDI
  temporal reuse; it only isolates whether the old fake previous-best candidate
  path was poisoning initial selection before temporal reuse runs.

- r_pathTracingRestirPTView68Dump
  Mode 56 / DI view 68 or 69 terminal readback. `1` prints one dump then resets to
  `0`; `2` keeps dumping. The dump forces the existing smoke output readback and
  classifies a square neighborhood. In view 68 it buckets the visible cause
  colors, so it is meant for capturing noisy/strobed failure clusters rather
  than single pixels. In view 69 it aggregates exact raw tuples and reports top
  `cur/c2p/prev/p2c` combinations plus counts for current-to-previous invalid,
  previous-to-current invalid, selected-light changed, previous absent, and both
  directions valid. Use
  `r_pathTracingRestirPTView68DumpX` and `r_pathTracingRestirPTView68DumpY` to
  choose the screen pixel; `-1` defaults to the center of the left diagnostic
  half for X and screen center for Y. `r_pathTracingRestirPTView68DumpRadius`
  controls the neighborhood radius and defaults to `32`.

- r_pathTracingRestirPTView68DumpX / r_pathTracingRestirPTView68DumpY /
  r_pathTracingRestirPTView68DumpRadius
  Coordinate and radius controls for `r_pathTracingRestirPTView68Dump`. To
  capture a specific bad spot, keep `r_pathTracingDebugMode 56` and
  `r_pathTracingRestirPTDiDebugView 68`, place the spot inside the left half's
  cause-band view, set X/Y near it, then set
  `r_pathTracingRestirPTView68Dump 1`. The terminal output reports total ROI
  buckets plus per-band buckets for `projected`, `previousSample`,
  `previousToCurrent`, `currentToPrevious`, and `selectedStability`.

- r_pathTracingRestirPTGiDebugView
  Mode 56 GI comparison view. `0` keeps normal mode 56 output, `1` shows raw GI
  initial, `2` shows the debug-only GI temporal accumulator, `3` is reserved
  for future GI spatial reuse, and `4` shows GI initial reservoir health. This
  view writes only the debug output, not RR input.

- r_pathTracingRestirPTDirectResolutionScale
  Legacy direct-domain scale control. Future DI scaling should be rebuilt from
  the accepted GI reservoir path rather than inferred from current direct
  temporal/spatial labels.

- r_pathTracingRestirPTDirectLighting
  Defaults to 0. This is diagnostic only and must not be documented as completed
  DI temporal/spatial reservoir processing. When set to 1 in mode 18 it selects
  a diagnostic hybrid wrapper, but the direct/DI reservoir is still raw output.
  Future mode 18 direct reuse should be rebuilt from the accepted GI reservoir
  path before being described as a ReSTIR DI integration hook.

- r_pathTracingRestirPTMode18DebugView
  Mode 18 ReSTIR hybrid isolation view. Requires
  `r_pathTracingRestirPTDirectLighting 1`. Defaults to 0 for the normal hybrid
  render. `1` shows native primary direct, `2` shows ReSTIR primary direct,
  `3` shows absolute delta heat, and `4` shows luminance ratio heat. Use this
  before judging whether the hybrid is underweighted or merely similar to native
  NEE in the current view.

- r_pathTracingRestirPTMode18HeavyDirect
  Defaults to 0. When set to 1 with `r_pathTracingRestirPTDirectLighting 1`,
  suppresses native direct NEE on secondary diffuse/reflection hits so the
  primary ReSTIR direct term is easier to isolate. This is a heavy-handed test
  lever, not a desired final lighting policy.

- r_pathTracingRestirPTMode56Accumulation
  Temporary diagnostic accumulator for debug mode 56 final/subview output.
  Defaults to 0. Set to 1 only when isolating whether a mode 56 view can hold
  history once its input is otherwise stable. RRX DI proof views `60`-`77` and
  `r_pathTracingRestirPTRrxFinalConsumerOutput 1` bypass this accumulator. This
  is not a ReSTIR reservoir fix and should not be treated as final accumulation
  policy.

- r_pathTracingRestirPTMode56AccumulationMaxFrames
  Defaults to 256. Caps the temporary debug-mode-56 accumulator frame count
  when `r_pathTracingRestirPTMode56Accumulation 1` is enabled.

Glass/transmission reservation:

- `r_pathTracingTransmissionBounceLimit` is reserved and currently clamps/fails
  closed. Do not treat the existing portal/window/object-glass visibility
  approximations as final glass transport.
- Future glass work needs explicit opacity, alpha cutoff, default IOR, tint or
  transmission color, thickness approximation, nested-surface policy, and Doom
  window/liquid class flags. Ask before finalizing Doom-specific glass or liquid
  behavior.

Denoise/upscale/Ray Reconstruction reservation:

- Required future pass outputs/inputs are color, albedo, normal, roughness,
  depth, motion vectors, hit distance, diffuse/specular separation, exposure,
  and history reset reasons. Primary-surface history already reserves several
  material/motion fields, but camera-only reprojection is not enough for DLSS/RR.
- The DLSS Ray Reconstruction path now has a real render/output extent split.
  `PathTraceDLSSRRBridge_QueryOptimalRenderSize()` treats the game/display
  size as the final output size and calls `slDLSSDGetOptimalSettings()` for the
  internal render size when the selected mode upscales. For a 3840x2160 display
  with `r_pathTracingDLSSRRMode 3` (Performance), the render extent is typically
  1920x1080 while Streamline `DLSSDOptions.outputWidth/outputHeight` remain
  3840x2160. Do not set Streamline output dimensions to the internal render
  size; in Streamline they mean final target size.
- `r_pathTracingDLSSRRMode 0` is DLAA/native and does not request an upscaled
  internal size, so render and output extents stay equal. Modes 1-5 query the
  SDK optimal render size, resize PT frame resources, and reset PT/RR histories
  on the size change.
- Render-sized PT resources: primary ray dispatch, ReSTIR reservoirs,
  primary-surface history, `PathTraceRRInputColor`, clean DI boiling scratch,
  reflection preview, motion vectors, motion-vector mask, RR motion vectors,
  RR guides, RR depth, RR hit distance, RR reset/disocclusion mask, RR position,
  and the RR input EXR readback. Output-sized resources: `PathTraceSmokeOutput`,
  `PathTraceSmokeAccumulation`, final readback/presentation, and post-RR
  tonemap/display consumers.
- The RR guide scaffold is explicit. The primary-surface producer exports
  albedo (`u48`), normal+roughness (`u49`), depth (`u50`), hit distance (`u51`),
  reset/disocclusion mask (`u52`), specular albedo/F0 (`u53`), raw RR input HDR
  color (`u54`), RR motion vectors (`u78`), and RR position (`u79`). Legacy
  motion vector and validity-mask outputs remain `u39/u40`. `u54` must remain
  raw scene-referred linear HDR, not preview-tonemapped or clamped SDR.
- Streamline tagging uses the render extent for scaling input color, albedo,
  specular albedo, normal/roughness, optional position, depth, motion vectors,
  specular hit distance, and optional disocclusion mask. Only
  `kBufferTypeScalingOutputColor` uses the final output extent.
- The clean RTXDI DI path evaluates DLSS RR when the clean spatial route is
  active, `r_pathTracingCleanRtxdiDiView` is 12 or 16, `r_pathTracingDLSSRR 1`,
  and `r_pathTracingDLSSRRGuideDebugView 0`. It passes RR input color,
  accumulation/output, albedo, specular albedo, normal/roughness, position,
  finite-far depth, RR motion vectors, and hit distance to Streamline. The
  mode-56 combined primary-prepass route can also evaluate RR and copies the
  RR accumulation result back into `PathTraceSmokeOutput` on success.
- RR depth uses the finite-far idTech4 override documented in
  `docs/idtech4_dlssrr_depth.md`: `r_pathTracingDLSSRRDepthMode 2`,
  `r_pathTracingDLSSRRCameraNear 0.2`, `r_pathTracingDLSSRRCameraFar 2048`,
  `depthInverted = 0`. Mode 2 tags hyperbolic hardware depth as
  `kBufferTypeDepth`, which is the form that matched reference DLSS-RR depth
  overlays. Linear depth modes remain diagnostic.
- RR history reset must be sparse. The clean path resets for forced reset,
  missing/invalid primary-surface history, primary-surface history clear, output
  resize, backbuffer resize, or full scene-resource reset. It intentionally does
  not reset on ordinary reservoir-scene or dispatch-signature churn, because
  that caused motion boiling by wiping RR history continuously.
- `r_pathTracingMotionVectorExport` defaults to 1, so the primary-surface
  producer writes bridge motion-vector/mask outputs from static previous
  snapshots, retained skinned previous positions, and continuous rigid previous
  transforms. The reset mask is written regardless of that CVar so history
  validity can be debugged independently.
- Mode 56 primary-prepass/RR-guide export builds the skinned previous-position
  scaffold when needed even if `r_pathTracingGpuSkinning` is `0`; that CVar
  still controls the diagnostic GPU skinning compute path.
- Mode 56 now removes routed-ready rigid candidates from the legacy dynamic
  fallback when the routed rigid TLAS path is enabled, matching modes 47/49.
  This is required for clean primary-surface producer motion export: if both
  copies remain in the TLAS, the primary ray can split the same object between
  the routed rigid instance (`instanceId >= 2`, `RIGID_ENTITY`) and the old
  dynamic fallback (`instanceId == 1`). View 70 then shows patchy rigid motion
  because some pixels export the routed rigid vector and others export the
  fallback/source-mismatch result. With default
  `r_pathTracingRigidRouteRemoveDynamic 1`, mode 56 treats routed rigid as a
  replacement for route-ready dynamic fallback geometry, not an overlap layer.
  This is still a temporary workaround, not a final object/geometry ownership
  solution. The current routed-ready classification can remove visible chunks
  of level airlocks and some doors from the fallback path when those surfaces
  are classified too coarsely as route-ready rigid. If an airlock section or
  door suddenly goes invisible, remember this gate first: the long-term fix is
  a proper Doom 3 object and geometry classification system tied to the
  underlying render-world/game geometry, not more ad hoc fallback removal.
- `r_pathTracingDLSSRRGuideDebugView` inspects the current guide buffers in
  mode 56 through the standalone resolve: 0 off, 1 albedo, 2 normal, 3
  roughness, 4 depth, 5 hit distance, 6 motion-vector mask/source category, 7
  reset/disocclusion mask, 8 specular albedo/F0, 9 RR input HDR preview, and 10
  RR motion vector.
- The clean RTXDI DI path has its own in-game guide checks that do not depend on
  the DLSSRR debug overlay: `r_pathTracingCleanRtxdiDiView 19` displays the
  clean RR guide albedo full-screen, and `r_pathTracingCleanRtxdiDiView 20`
  displays the clean RR guide specular albedo/F0 full-screen. Both views run
  the same clean view-16 producer first, then display the guide texture.
- Current DLSS/RR CVar reference:
  - `r_pathTracingDLSSRRProbe` default `1`: initialize the experimental
    Streamline DLSS/RR bridge at renderer startup and print feature support.
    Set to `0` before startup only when isolating startup/platform problems.
  - `r_pathTracingDLSSRR` default `0`: evaluate DLSS Ray Reconstruction on
    supported PT/RR routes.
  - `r_pathTracingDLSSRRMode` default `0`: `0` DLAA/native, `1` Quality,
    `2` Balanced, `3` Performance, `4` Ultra Performance, `5` Ultra Quality.
  - `r_pathTracingDLSSRRInputDump` default `0`: one-shot HDR EXR dump of the
    next RR input color under `screenshots/dlssrr_input_*.exr`.
  - `r_pathTracingDLSSRRColorBuffersHDR` default `1`: sets Streamline
    `colorBuffersHDR`; keep on because the current NGX RR path rejects SDR
    tagged color buffers.
  - `r_pathTracingDLSSRRPreExposure` default `1.0`: Streamline `preExposure`.
    Keep `1.0` unless the RR input color has already been pre-exposed.
  - `r_pathTracingDLSSRRExposureScale` default `1.0`: Streamline
    `exposureScale`; diagnostic only, not the final PT exposure path.
  - `r_pathTracingDLSSRRForceReset` default `0`: force RR history reset every
    evaluated frame for isolation testing.
  - `r_pathTracingDLSSRRCameraNear` default `0.2`: finite RR depth frustum near
    plane in world units; `<= 0` uses `r_znear`.
  - `r_pathTracingDLSSRRCameraFar` default `2048`: finite RR depth frustum far
    plane in world units; `<= near` falls back to `100000`.
  - `r_pathTracingDLSSRRDepthMode` default `2`: `2` hyperbolic hardware depth
    tagged `kBufferTypeDepth`; `0` normalized linear view-Z `[0,1]`; `1` raw
    linear view-Z. Modes `0/1` are tagged `kBufferTypeLinearDepth`.
  - `r_pathTracingDLSSRRReverseZ` default `1`: legacy diagnostic CVar. The
    current evaluated RR bridge uses forward finite-far depth with
    `depthInverted = 0`, so this CVar is not consulted by the validated path.
  - `r_pathTracingDLSSRRMotionVectorScaleX` and
    `r_pathTracingDLSSRRMotionVectorScaleY` default `1.0`: diagnostic
    Streamline motion-vector scale multipliers. Use `-1` to test sign
    convention or `0` to remove that axis.
  - `r_pathTracingDLSSRRClipHistory` default `1`: sends
    `clipToPrevClip`/`prevClipToClip`; `0` sends identity matrices while keeping
    RR history active.
  - `r_pathTracingDLSSRRJitterScaleX` and
    `r_pathTracingDLSSRRJitterScaleY` default `1.0`: scales the jitter offset
    reported to Streamline only; primary rays are unchanged.
  - `r_pathTracingDLSSRRSharpness` default `0.0`: Streamline RR sharpness,
    clamped `0..1`.
  - `r_pathTracingDLSSRRDenoiserPreset` default `4`: `0` SDK default,
    `4` preset D/default transformer, `5` preset E/latest transformer.
  - `r_pathTracingDLSSRRGuideDebugView` default `0`: mode-56 RR guide debug
    view; values are listed above.
  - `r_pathTracingDLSSRRVerbose` default `0`: `1` prints one-time contract,
    resource-tag, history-reset, warning, and error messages; `2` also prints
    per-frame evaluate lines and Streamline info messages.
- Useful validation setup for 4K Performance testing:
  `r_pathTracingDLSSRR 1`, `r_pathTracingDLSSRRMode 3`,
  `r_pathTracingDLSSRRVerbose 1`, then confirm the log prints
  `render=1920x1080 output=3840x2160` or another SDK-selected internal size,
  the tag extent matches the render size, and the output extent matches the
  display size.
- Do not integrate DLSS/RR or a final denoiser against camera-only motion. The
  geometry universe must provide current/previous transforms or vertex data for
  surfaces whose history is reused.
- Tiled ray dispatch remains a future stability lever for long GPU dispatches,
  but it is intentionally not part of this reservation task.

- r_pathTracingEmissiveInventoryDump
  One-shot dump of the current emissive triangle inventory: total/captured
  static/dynamic/routed-rigid candidate counts, unique material count, area,
  approximate luminance, candidate weight, estimated radiance, per-material
  summary rows, emissive texture slot/dimensions, and a short triangle sample
  list with UV bounds.

- r_pathTracingRigidRouteEmissiveDump
  One-shot dump focused on routed rigid emissive append diagnostics. It uses
  the emissive inventory dump path and adds route instance count, seen/cache
  split, routed rigid emissive instance split, triangle considered/captured/cap
  counts, invalid/non-emissive counts, area, and weighted luminance.

- r_pathTracingEmissiveInventoryMaxTriangles
  Caps the CPU/GPU emissive triangle records captured for future light-sampling
  infrastructure. The default is 4096. This also caps the merged persistent
  static + current dynamic light-candidate list. This is intentionally an
  inventory for later ReSTIR/PT integration, not a hand-rolled area-light
  sampler.

- r_pathTracingLightUniverseDump
  One-shot dump of persistent emissive light-universe stats and the CPU-side
  Doom analytic light-universe scaffold. The emissive dump reports persistent
  static triangle count, static seen/new/updated/missing this frame,
  semi-static dynamic count, dynamic seen/promoted/updated/missing/aged this
  frame, current frame-local dynamic triangle count, merged upload count,
  area-membership diagnostics, and generation. The Doom analytic dump reports
  stable key/universe counts, current candidate count, uploaded current and
  previous identity counts, uploaded remap count, shader invalid-remap count,
  active/sampleable/zero-radiance counts, previous/current remap counts,
  duplicate keys, conservative continuity rejects, unknown entity sentinels,
  and common invalid reasons. A second Doom line reports the persistent authored
  light registry: static total/seen/new/updated/missing, static sampleable and
  zero-radiance counts, static-list count/seen/missing, dynamic/unproven/
  unknown-entity buckets, and a current proposal-to-universe mapping count.
  Static emissives are retained by stable-ish
  identity across frames. The area diagnostics report current area, total
  areas, known/unknown static and dynamic candidate area membership, and how
  many merged candidates are in the current, selected, connected, disconnected,
  or unknown area sets. Selected areas are the current camera area plus open
  portals up to `r_pathTracingScenePortalSteps`. These dumps are diagnostic
  only; Task 02 makes Doom analytic identity/remap and previous analytic light
  records shader-visible for RAB, but does not default-enable previous-frame
  analytic NEE reuse.

- r_pathTracingLightUniverseChurn
  Defaults to 0. When enabled, tracks frame-to-frame changes in the uploaded
  RT smoke emissive light set. `r_pathTracingLightUniverseDump 1` then reports
  previous/current/stayed/entered/left counts, matching weight totals, and top
  entered/left samples by weight. This is for mode 20 portal-edge stability
  diagnostics; leave it enabled while moving before taking a dump so the
  previous active set is meaningful.

- r_pathTracingLightUniversePersistDynamic
  Defaults to 1. Allows stable rigid-entity emissives to be promoted into
  semi-static light-universe persistence. Skinned/deformed, particle, and
  other non-rigid dynamic emissives remain frame-local.

- r_pathTracingLightUniverseInjectMissingDynamic
  Defaults to 0. When enabled, promoted semi-static dynamic emissives that are
  missing from the current capture are injected back into the light list. This
  remains off by default because the matching dynamic occluder/object geometry
  is not persistent yet; injecting only the light record can create inaccurate
  ghost lighting.

- r_pathTracingLightUniverseDynamicMinSeenFrames
  Defaults to 2. Number of frames a rigid-entity emissive must be seen before
  it is promoted into the semi-static light universe.

- r_pathTracingLightUniverseDynamicMaxMissingFrames
  Defaults to 90. Number of frames a promoted semi-static dynamic emissive may
  be missing from the current capture before it ages out.

- r_pathTracingLightAreaPortalSteps
  Defaults to 4. Portal traversal depth for RT smoke emissive light-area
  selection diagnostics. This is separate from the scene-universe diagnostic
  portal depth so mode 20 light selection can be tuned independently.
  `PS_BLOCK_VIEW` edges are counted for diagnostics but do not block traversal,
  matching source3 static preload and rigid residency.

- r_pathTracingLightAreaFilter
  Defaults to 0. Diagnostic-only light-area selector gate. When enabled, the
  dump reports what would be selected by area plus overflow rules, but uploaded
  mode 20 candidate buffers remain unchanged.

- r_pathTracingLightAreaFilterApply
  Defaults to 0. Experimental render-affecting gate for the light-area selector.
  When enabled, mode 20 uploads selected-area emissive candidates plus the
  highest-weight connected overflow candidates up to
  `r_pathTracingLightAreaOverflowMax`. Disconnected and unknown-area candidates
  are dropped. Keep this off unless testing area-filter impact.

- r_pathTracingLightAreaOverflowMax
  Defaults to 512. Connected-area overflow budget for light-area selection. The
  high default is intentionally greedy for Doom 3 content: keep all selected
  portal-area candidates plus most or all connected overflow candidates, and
  mainly drop disconnected/unknown-area candidates. When
  `r_pathTracingLightAreaFilterApply 1` is enabled, this budget is applied to
  the uploaded mode 20 candidate list.

- r_pathTracingRemixFramePrepareDump
  Defaults to 0. One-shot dump of the CPU-only RTX Remix-shaped
  frame-preparation scaffold.
  Reports frame ordering, output size, reset flags, light/emissive observation
  counts, and explicit zero counts for resource allocation, shader routing, and
  old smoke reservoir signature consultation.

- r_pathTracingRemixLightManagerDump
  Defaults to 0. One-shot dump of the CPU-only RTX Remix-shaped light manager
  contract.
  Reports current/previous unified light payload counts, current-to-previous
  and previous-to-current map counts, per-type ranges and sample counts, and
  structural/mapping/payload signatures. Structural signature changes are
  reserved for the stable light-domain schema/ABI; active payload/range/sample
  churn belongs to payload or mapping state.
  The RRX DI initial-sampling range contract uses these sample counts as
  bounded per-pixel proposal counts, not raw authored range sizes. Non-empty
  emissive and Doom analytic ranges each receive at least one sample when the
  requested total is non-zero; the temporary total budget remains capped at 32.
  In active Remix-manager RAB mode, this dump is the authoritative proof for
  the shader-visible `offset/count/samples` contract.

- r_pathTracingRemixLightManagerRAB
  Defaults to 1. Active shader-visible RAB light source switch for the
  Remix-shaped light manager. With the default enabled, RAB light loads,
  translation, and the RRX DI seed/temporal path use the Remix manager's
  current/previous light domains and payload buffers. `0` selects the legacy
  ReSTIR manager comparison/debug source. This is a routing/conformance gate,
  not a claim that final Remix DI parity is complete.

- r_pathTracingRemixRtxdiResourcesEnable
  Defaults to 1. Persistent allocation and active mode-56 RRX DI consumer gate
  for the RTX Remix-shaped RTXDI DI/GI reservoir resource owner. With the
  default enabled in mode 56, the DI domain is allocated, initial clear-pending
  state is cleared by the command list, and the local combined preview/final
  resolve consumes the RRX DI temporal reservoir for the direct-lighting term
  when it produces a valid reservoir. This currently uses rbdoom preview
  visibility through `TraceSmokeShadowVisibility`, not the reference RAB
  selected-light visibility callback.

- r_pathTracingRemixRtxdiResourcesDump
  Defaults to 0. One-shot dump of the RTX Remix-shaped RTXDI reservoir
  resource owner. With the default RRX resource owner enabled, the dump reports
  the active persistent owner; if the owner is disabled for comparison, the dump
  still allocates for one validation frame. It reports DI/GI
  recreate/reuse/clearPending/clearReason state, reservoir array/stride/byte
  metadata, allowed versus ignored reset flags, old smoke clear flags, light
  signatures, structural/mapping/payload signature-change flags,
  payload-only change state, active DI clear source/request state, and explicit
  zero counts for old smoke reservoir consultation, shader routes, and binding
  handoffs. `clearPending` means GPU reservoir contents still require a
  command-list clear before shader availability reports true. `clearReason`
  is limited to none, resource incompatibility, allowed reset, or a still
  pending previous RRX clear; payload-only light changes and old smoke
  reservoir signatures are diagnostic fields only and must not trigger an RRX
  DI/GI resource clear.

- r_pathTracingRestirLightManagerDump
  Defaults to 0. One-shot dump of the ReSTIR light-manager bridge. Reports
  observation counts plus active current/previous/map/payload counts when those
  domains are built. Use this to verify the manager-side contract before
  enabling the RAB routing gate. When `r_pathTracingRemixLightManagerRAB 1`
  selects the Remix-shaped manager as the active RAB source, this dump still
  prints the legacy persistent manager's staged range counts; use
  `r_pathTracingRemixLightManagerDump 1` to verify the bounded active range
  sample counts consumed by the RRX DI initial reservoir.

- r_pathTracingRestirLightManagerRAB
  Defaults to 0. Opt-in routing gate that makes the ReSTIR RAB light-load path
  consume the CPU light-manager current/previous domains and manager-owned
  payload buffers. `0` preserves the legacy RAB light path. The gate is for
  bridge validation only; it does not change RTXDI math or reservoir policy.

- r_pathTracingRestirPdfNeeVerifierEnable /
  r_pathTracingRestirPdfNeeVerifierSamples /
  r_pathTracingRestirPdfNeeVerifierVisibility /
  r_pathTracingRestirPdfNeeVerifierSourcePolicy /
  r_pathTracingRestirPdfNeeVerifierDump
  Live RLU-current PDF + NEE producer. The public CVar prefix retains its legacy
  `Verifier` name for console/config compatibility, but the standalone verifier
  shader and its View/LightMode/Domain recipes have been removed. Enabling this
  route selects from the dense current RLU domain, replays the selected light
  through the active RRX RAB bridge, and writes the clean DI current reservoir
  page at `u69` with dense RLU identity.

  Samples controls candidate proposals per pixel (1..64). Visibility selects
  forced-visible diagnostics or the active RAB NEE visibility policy.
  SourcePolicy selects dense-current uniform, typed range-stratified, or the
  NEE-cache provider/fallback source. Dump reports the live producer contract
  once. The route does not restore mode-56 planning, the discarded PDFNEE-09
  handoff, split-domain analytic identity, or old rbdoom NEE records.

- r_pathTracingCleanRtxdiDiExternalPdfNeeCurrent
  Defaults to 0. Historical clean-room consume-mode diagnostic for the old
  analytic PDFNEE prepass. It is not the replacement producer contract, not a
  default path, and not proof of the RLU current-frame estimator. Replacement
  work must use the RLU dense current domain and active RRX RAB bridge, then
  write the clean DI current reservoir page without depending on this old
  PDFNEE-09 external handoff or temporal accumulation behavior.

- r_pathTracingCleanRtxdiDiNeeCacheProvider
  Defaults to 0. Opt-in clean RTXDI DI proposal-provider consume path for the
  Remix-style NEE cache. It is a proposal source for the clean DI initial
  reservoir, not a separate final lighting path: clean DI still owns reservoir
  construction, visibility, temporal reuse, spatial reuse, and final resolve.
  Normal operation uses the dense current Remix Light Universe identity domain:

      r_pathTracingRemixLightUniverseEnable 1
      r_pathTracingNeeCacheEnable 1
      r_pathTracingCleanRtxdiDiEnable 1
      r_pathTracingCleanRtxdiDiView 12
      r_pathTracingCleanRtxdiDiLightMode 1
      r_pathTracingCleanRtxdiDiNeeCacheProvider 1
      r_pathTracingCleanRtxdiDiResolveSolidAnglePdf 1
      r_pathTracingNeeCacheCellResolution 8

  Use `r_pathTracingCleanRtxdiDiDump 1` and `r_pathTracingNeeCacheDump 1` for a
  one-shot route check. A ready clean-provider dump should report
  `cleanNeeCacheProvider requested/ready=1/1`, SRVs at `providerResultSrv=t74`,
  `cellSrv=t75`, and `candidateSrv=t77`, `resolveSolidAnglePdf=1`,
  `cellFrame=world-snapped-moving-lod`, and `cellResolution=8`.

- r_pathTracingCleanRtxdiDiResolveSolidAnglePdf
  Defaults to 1 and should stay 1 for the current clean DI NEE-cache provider.
  The provider streams a selection-domain PDF into `RTXDI_StreamSample`; the
  rbdoom RTXDI DI target evaluator still follows the local convention
  `luminance(reflected) / solidAnglePdf`, so final resolve divides by the
  selected light's `solidAnglePdf`. Turning this CVar off makes the provider
  visibly over-bright for this branch unless the target-function and final
  resolve measure are migrated together. Do not reintroduce the older
  `sourcePdf = P_select * solidAnglePdf` contract in only the provider.

- r_pathTracingNeeCacheEnable / r_pathTracingNeeCacheMode /
  r_pathTracingNeeCacheCellResolution / r_pathTracingNeeCacheFallbackProbability
  / r_pathTracingNeeCacheSourceDomain / r_pathTracingNeeCacheDebugView /
  r_pathTracingNeeCacheDump
  NEE-cache provider controls. `r_pathTracingNeeCacheEnable 1` allocates and
  updates the provider resources. `r_pathTracingNeeCacheMode` defaults to 1 for
  the Remix-style log/hash cache shell. `r_pathTracingNeeCacheCellResolution`
  defaults to 8. This is a density knob, not a cell-size knob:
  `baseCellSize = r_pathTracingNeeCacheMinRange / cellResolution`, so the
  default `minRange 256` gives 32-world-unit base cells at resolution 8 and
  16-world-unit base cells at resolution 16. Higher values make cells smaller;
  `64` gives 4-world-unit base cells and is too dense for normal validation.
  The cell mapper centers the high-resolution region on the camera, snaps that
  center to the base world-cell grid, and keeps the resulting cell coordinates
  world-grid based. This prevents a fixed-origin high-res island while avoiding
  continuous camera-relative cell sliding.

  `r_pathTracingNeeCacheFallbackProbability` defaults to 0.25. The clean
  provider evaluates the real mixture PDF for the selected dense RLU light:
  `P_select(light) = cacheProbability * p_cache(light) + fallbackProbability *
  p_fallback(light)`. It must evaluate both branches for the selected light,
  not only the branch that happened to draw it. If no usable cache distribution
  exists, fallback is forced with effective fallback probability 1. Useful A/B
  values are `1` for fallback-only baseline, `0.25` for the default
  cache/fallback mixture, and `0` for cache-heavy diagnostics only.

  `r_pathTracingNeeCacheSourceDomain` selects the fallback/current source
  domain: 0 all current RLU lights, 1 emissive range only, 2 analytic range
  only, and 3 typed lists with mixture probabilities. Debug views 2 and 3
  inspect cell identity and occupancy; views 8 and 9 inspect cache/fallback
  source and fallback reason; view 10 checks RAB replay. Source-policy-2
  PDFNEE consume remains a preserved visual/research lane, but it is not the
  production clean DI proof path and must not replace the clean DI provider.

- r_pathTracingCleanRtxdiDiSpatial / r_cleanDiSpatial / r_cleanSpatial
  Clean-room RTXDI DI spatial reuse enable gates. All three must be nonzero for
  the clean spatial reuse kernel to run. Defaults are 1. Set any one of them to
  0 for temporal-only/current-only diagnostics.

- r_cleanDiSpatialSamples
  Defaults to 1, clamped to 1..16. Controls the normal clean DI spatial
  neighbor sample count used by `pathtrace_clean_rtxdi_di_spatial.rt.hlsl`.
  Raise it toward 5 for stress-testing mixed analytic/emissive NEE-cache
  stability if temporal-only noise starts leaking through the denoiser.

- r_cleanDiSpatialDisocclusionSamples
  Defaults to 4, clamped to 1..16. Controls the clean DI short-history spatial
  sample count. The shader uses `max(r_cleanDiSpatialSamples,
  r_cleanDiSpatialDisocclusionSamples)` while the center reservoir has less
  than the configured temporal target history.

- r_cleanDiSpatialRadius
  Defaults to 30, clamped to 1..128. Screen-space radius in pixels for the clean
  DI spatial neighbor pattern.

  Long-form CVars also exist:
  `r_pathTracingCleanRtxdiDiSpatialSamples`,
  `r_pathTracingCleanRtxdiDiSpatialDisocclusionSamples`, and
  `r_pathTracingCleanRtxdiDiSpatialRadius`. The current dispatch path writes
  the shader constants from the short aliases above, so use the `r_cleanDi*`
  controls for runtime adjustment.

- r_pathTracingReservoirDump
  One-shot dump of current ReSTIR/PT reservoir ownership state: screen-sized
  record count/bytes, scene and dispatch signatures, reset/clear counters,
  emissive triangle counts, and light-candidate counts. Reservoir history
  currently resets on output size, camera/FOV, material table,
  emissive-candidate, light-candidate, or static-BLAS signature changes. The
  current reservoir is written by mode 20; previous and spatial scratch
  reservoirs are still reset/cleared ownership scaffolding.

- r_pathTracingSceneInputsDump
  One-shot dump of the committed RtPathTraceSceneInputs package after the next
  successful scene build. It reports scene source, debug mode, output size,
  geometry/material/light signatures, geometry counts and history placeholders,
  material table path and active texture count, emissive/Doom analytic light
  counts, skinned previous-bridge and GPU-skinning scaffold counts, source3
  view/current area plus portal step/area/edge facts, and CPU upload/timing
  estimates. This is a diagnostics contract only; it does not change dispatch
  routing or shader-visible buffer layouts.

- r_pathTracingPortalBruteforceFullMap
  Defaults to 0. Diagnostic override that treats every render-world area as
  selected for the PT scene, rigid residency, emissive-light, and analytic-light
  portal selectors. Set to 1 only for portal-boundary A/B tests; it is expected
  to be very slow and is not a final residency or light-selection policy.

- r_pathTracingGpuSkinning
  Off-by-default PT GPU skinning scaffold gate. `0` keeps only the existing
  CPU-skinned bridge. `1` creates diagnostic skinned source/current-output/
  previous-position/dispatch/current-joint/previous-joint buffers from the CPU
  bridge, dispatches `pathtrace_skinning.cs.hlsl` into the diagnostic output
  buffer, overwrites validated previous-position ranges from previous joint
  rows when available, and exposes counts in `r_pathTracingSceneInputsDump`.
  `2` retargets the current-output writes to overwrite validated skinned spans
  in the dynamic vertex buffer before dynamic BLAS build. This remains opt-in;
  the default path and raster GPU skinning are unchanged.

- r_pathTracingMotionVectorExport
  Off-by-default writer for the private PT RG16F motion-vector UAV. `0` leaves
  the vector texture unmodified and clears the private mask texture to zero
  before dispatch, making disabled export consumer-safe. `1` writes combined
  geometry current-to-previous pixel motion into the private vector texture for
  static, skinned, and routed-rigid hits that have valid previous geometry.
  Motion is in internal PT render-resolution pixels from the current pixel
  center to the projected previous-frame sub-pixel position. The paired private
  R32_UINT mask texture uses bit 0 for valid, bits 1-4 for source kind
  (`1` static, `2` skinned, `3` routed rigid, `4` other object motion), and
  bits 5-12 for the compact invalid reason copied from the
  `RT_PRIMARY_SURFACE_DEBUG_*` status. Zero motion is never used as an invalid
  sentinel. No current consumer reads either texture, so normal PT output is
  unchanged.

- r_pathTracingMotionVectorDisableRigid
  Defaults to 0. Debug-only quarantine for routed-rigid motion vectors. When
  enabled, routed-rigid primary hits are marked as invalid motion instead of
  exporting rigid object-motion vectors, while static and skinned motion export
  remains unchanged. Use with mode 56 / DI view 70 to test whether poisoned
  rigid vectors are degrading temporal reuse.

- r_pathTracingPortalTransitionDump
  Diagnostic logging for suspected PT scene-resource lifetime hazards. When a
  committed scene transition changes source, portal area/set counts, selected
  geometry/material/light counts, scene signatures, or core resource handles,
  this prints old/new portal facts, geometry/material/light counts, upload byte
  estimates, descriptor-table create/write flags, binding-set changes, buffer
  handle changes, and static/dynamic BLAS handle changes. It is intended for
  portal-threshold corruption/device-removal diagnosis and does not change
  rendering behavior. Values are:
  `0` off, `1` print the next portal-area transition and auto-disable,
  `2` print every portal-area transition, `3` print every broad scene/resource
  transition. Use `1` for normal in-game testing; `3` is expected to be noisy.

- r_pathTracingWaitForIdleOnPortalChange
  Diagnostic synchronization gate. When nonzero and the same PT scene
  transition signature changes, `PathTraceSmokeResources` waits for the NVRHI
  device to idle immediately before replacing the committed PT scene resources.
  This was used to confirm the portal-threshold texture corruption/device
  removal failure as an in-flight scene-resource lifetime hazard. The normal
  runtime fix is `r_pathTracingSceneRetireFrames`, not this full-device idle.
  This path is silent unless transition dumping is also enabled. Keep it
  disabled for normal testing and performance work.

- r_pathTracingSceneRetireFrames
  Frame-lagged lifetime for replaced committed PT scene packages. The default
  is `6`, clamped to `0..32`. Nonzero values keep old scene buffers,
  static/dynamic BLAS handles, the TLAS handle, binding set, descriptor table,
  and active texture handles alive after replacement so portal transitions do
  not need a normal `waitForIdle()`. Setting this to `0` restores immediate
  replacement for A/B testing. This is the confirmed fix for the high-GPU-load
  portal-transition corruption/device-removal bug: heavy ray workloads only
  made the hazard easier to hit by keeping older frames in flight longer.

- r_pathTracingSceneRetireDump
  Retirement diagnostics: `0` off, `1` print the next retire/release event and
  auto-disable, `2` print every event. Logs include current frame, retire frame,
  queue size, released count, old/new area facts, transition flags, diagnostic
  wait usage, and which old package handle groups were retained.

- r_pathTracingDebugMode 20
  Single-frame direct lighting debug view from the emissive candidate contract.
  It samples one emissive triangle candidate per pixel, writes the current
  reservoir buffer, traces a visibility ray, and shades the hit with that
  sample. This is intentionally noisy and does not perform temporal reuse,
  spatial reuse, MIS, or final light-sampler weighting yet. Static emissive
  candidates now come from a persistent light universe. Rigid-entity emissives
  can be promoted to semi-static persistence; other dynamic emissives remain
  current-frame only. The result can still have screen-space-like gaps until an
  area has been seen and promoted.

  Resolved stability note: the earlier transient texture corruption followed by
  device removal was not ultimately fixed by tiling, texture-sampling changes,
  NEE extraction, or lower light counts. The decisive diagnostic was
  `r_pathTracingWaitForIdleOnPortalChange 1`, which removed the failure by
  forcing a full sync before committed PT scene resources were replaced during
  portal/scene transitions. The production fix is frame-lagged scene-resource
  retirement with `r_pathTracingSceneRetireFrames 6`, which keeps the old scene
  package alive for later GPU work while allowing
  `r_pathTracingWaitForIdleOnPortalChange` to remain disabled. High ray
  workload, high internal resolution, and FPS below the frame cap made the bug
  easier to trigger because more work remained in flight across portal
  transitions. `r_pathTracingToyLightTraceCap` is still useful to keep the toy
  mode manageable, but it should not be treated as the root-cause fix for this
  device-removal class.

  Useful first test:

      r_pathTracingTextureTableLimit 2048
      r_pathTracingDebugMode 20
      r_pathTracingReservoirTwoSidedEmissives 1
      r_pathTracingLightUniversePersistDynamic 1
      r_pathTracingLightUniverseInjectMissingDynamic 0
      r_pathTracingLightUniverseDynamicMinSeenFrames 2
      r_pathTracingLightUniverseDynamicMaxMissingFrames 90
      r_pathTracingLightUniverseDump 1
      r_pathTracingReservoirDump 1

- r_pathTracingReservoirTwoSidedEmissives
  Defaults to 1. Treats mode 20 emissive triangle samples as two-sided for the
  reservoir direct-lighting diagnostic. This matches Doom content better for
  many emissive panels whose lighting intent is visible from both sides even
  when the triangle winding is not helpful for the debug sampler.

- r_pathTracingReservoirCandidateTrials
  Defaults to 1. Controls how many global emissive candidates mode 20 tests per
  pixel before choosing the highest local potential candidate. Higher values
  reduce off-screen/small-emissive starvation from the single-candidate
  diagnostic reservoir path, at extra shader cost. Clamp range is 1..16.

- r_pathTracingSceneSource
  Selects the current PT scene producer source:
  0 = legacy drawSurf-only producer for emergency comparison,
  1 = PathTraceSceneUniverse diagnostics only,
  2 = full static scene-universe geometry plus existing dynamic drawSurf
  fallback,
  3 = source3 portal-resident scene producer. Source3 is the default production
  scaffold for the replacement scene producer. It does not use
  PathTraceSceneUniverse records as the main render source. It routes eligible
  rigid meshes through reusable local-space BLAS/TLAS records when the route
  gates are enabled, which is the default.

- r_pathTracingSceneSourceCompare
  One-shot CPU diagnostic comparing source0 and source3 payloads on a source3
  frame. It reports surface, vertex/index, class, material, and skip-count
  deltas without changing rendering.

- r_pathTracingSceneSource2RigidEntities
  Experimental source2-only rigid entity expansion. Defaults to 0. Keep this
  out of source3; source3 should observe live drawSurfs, build mesh/instance
  identity from the drawSurf mirror, and use the gated rigid route/residency
  path for reusable local-space BLAS/TLAS testing.

- r_pathTracingInstanceUniverseDump
  One-shot dump of drawSurf mirror mesh/instance observations. Use it to inspect
  observed drawSurf count, unique mesh keys, instance records, static-world
  matches, transform history, moved-rigid samples, and skip reasons.

- r_pathTracingRigidMeshUniverseDump
  One-shot dump of diagnostics-only rigid mesh reuse eligibility. It reports
  eligible rigid candidates, rejection reasons, persistent local-source record
  counts, and how much of the current baked dynamic rigid payload could
  eventually be promoted.

- r_pathTracingRigidMeshValidate
  One-shot validation comparing eligible local-source rigid mesh records against
  the currently baked dynamic rigid payload. This is still count/material
  validation only and leaves renderPath=dynamicFallback.

- r_pathTracingRigidBlasPlanDump
  One-shot CPU-only reusable rigid BLAS/TLAS plan dump from local-source rigid
  mesh records. It creates no GPU resources and does not route rendering through
  the plan.

- r_pathTracingRigidBlasInputDump
  One-shot CPU-only NVRHI-shaped rigid BLAS input descriptor dump. It validates
  planned vertex/index byte sizes, offsets, formats, source identities, and
  invalid reason counters. It creates no GPU resources.

- r_pathTracingRigidBlasGpuScaffold
  Defaults to 1. Source3 rigid BLAS GPU scaffold gate. Set to 0 to create no
  reusable rigid BLAS GPU resources and fall back toward the legacy dynamic
  path.

- r_pathTracingRigidBlasGpuBuild
  Defaults to 1. Rigid BLAS build-submit gate for the source3 rigid BLAS
  scaffold. The BLAS records are inserted into TLAS when
  `r_pathTracingRigidTlasRoute 1` and a compatible debug/integration mode gate
  are enabled.

- r_pathTracingRigidBlasGpuDump
  One-shot dump for the rigid BLAS GPU scaffold. It reports mesh/input counts,
  buffer creation/reuse, upload bytes, BLAS handle/build counts, skips, and the
  current render path and TLAS route status.

- r_pathTracingRigidTlasPlanDump
  One-shot rigid TLAS instance plan dump. It matches current-frame and/or
  resident cached source3 rigid instances to local-source rigid mesh records and
  reports instance counts, GPU buffer/BLAS readiness, missing records,
  transform origins, material/entity metadata, and routed triangle counts. The
  dump is diagnostic, but the same records feed TLAS when route gates are
  enabled.

- r_pathTracingRigidTlasRoute
  Defaults to 1. Source3 route gate for rigid BLAS instances. It is active in
  debug modes 23, 24, and 25, mode 18 when `r_pathTracingRigidRouteMode18 1`
  is set, mode 20 when `r_pathTracingRigidRouteMode20 1` is set, ReSTIR PT
  modes 26-33/50/51/53-56, and integrator debug modes 34-37. Mode 23 traces
  routed rigid instances only. Mode 24 traces fallback-only and
  routed-rigid-only rays separately and color-codes their hit-distance
  agreement.

- r_pathTracingRigidRouteMode18
  Defaults to 1. Mode 18 integration gate for routed rigid TLAS instances. Set
  to 0 for legacy dynamic-fallback comparison.

- r_pathTracingRigidRouteMode20
  Defaults to 1. Mode 20 integration gate for routed rigid TLAS instances. This
  routes eligible rigid geometry through the rigid TLAS path; it does not change
  mode 20 reservoir/emissive sampling.

- r_pathTracingRigidRouteOverlapDump
  One-shot mode 24 readback dump. It queues a diagnostic output readback and
  prints pixel proportions for match, rigid-only, rigid-in-front,
  fallback-in-front, fallback-only, neither, and unknown color buckets.

- r_pathTracingRigidRouteRemoveDynamic
  Defaults to 1. When enabled with the rigid route gates, source3 skips
  routed-ready rigid mesh candidates from the dynamic fallback. This applies to
  the old motion-vector diagnostics and to the mode-56 primary-surface
  producer path, so routed rigid does not z-fight with a fallback copy or feed
  mixed source classifications into `PathTraceMotionVectors`/`PathTraceMotionVectorMask`.
  This is not final production geometry policy: the current route-ready test is
  coarse enough to remove chunks of level airlocks and some doors from the
  fallback path. Use that failure as the reminder that routed rigid needs a
  real object/geometry classifier connected to Doom 3's underlying geometry and
  entity data. If airlock geometry vanishes, check this CVar/gate before
  chasing shader-side visibility or motion-vector bugs.
  Set to 0 only for overlap/emergency comparison with the old dynamic fallback
  still present.

- r_pathTracingRigidRouteEmissiveCards
  Defaults to 1. When enabled, source3 may promote safe entity-attached
  translucent signage/glow drawSurfs into the rigid route instead of treating
  them as particle/transient alpha. The gate is intentionally conservative:
  GUI, particle-looking, glass, post-process, skinned/deforming, callback,
  generated, depth-hack, and dynamic-model surfaces are still rejected. This is
  for rigid prop emissive cards such as vending-machine additive panels that
  should remain resident when off-camera or occluded with their parent object.
  `_add...0200` generated default-RGBA texture variants remain banned by the
  material classifier and are not promoted by this gate. The known
  game-logic-driven `swinglight` material family is also not promoted, so it
  remains on the dynamic drawSurf path where its on/off state is observed.

- r_pathTracingRigidRouteMaxInstances
  Defaults to 256. Caps how many source3 rigid instances are routed into the
  experimental material-ID collection, rigid route buffers, and extra TLAS
  descriptors. The default was raised for depth-4 BVH validation because the
  opening Mars City tests can produce more than 150 resident route-ready rigid
  instances.

- r_pathTracingRigidResidency
  Defaults to 1. When enabled with the source3 rigid route, rigid residency is
  now frame-revalidated rather than a blind "seen once, keep forever" cache.
  The visible drawSurf mirror remains the authoritative freshness source for
  normal rigid geometry. The selected-area portal walk supplements only
  conservative emissive/signage/glow rigid surfaces, and only after the same
  render entity has previously been validated by visible drawSurfs without a
  later render-entity modification. Resident rigid records and rigid mesh
  candidates that are not revalidated in the current frame are pruned before
  route buffers/TLAS instances are built. This avoids keeping destroyed or
  despawned gameplay objects such as barrels, scan beams, and one-shot animated
  props alive in the PT TLAS while still preserving previously validated
  off-camera emissive rigid contributors. The tradeoff is that a never-seen
  occluded emissive rigid can still pop in the first time Doom submits it.

- r_pathTracingRigidResidencyPortalSteps
  Defaults to 4. Portal traversal depth for source3 rigid geometry residency.
  Depth 4 is the current standard because many visually continuous spaces are
  split into several renderer portal areas.
  `PS_BLOCK_VIEW` edges are counted for diagnostics but do not block this
  source3 RT residency traversal.

- r_pathTracingRigidResidencyDump
  One-shot dump of source3 rigid residency stats: current area, selected area
  count, visible rigid instances, emissive-only area-walk rigid instances,
  cached rigid instances, resident instances, resident seen-this-frame versus
  cache-only counts, route-ready counts, and skipped outside/unknown-area
  counts. Cache-only ordinary rigid geometry should not appear in normal mode
  18/20 integration after the live revalidation/prune pass.

  Mode 21/22 bounds visualization also appends resident rigid boxes when
  residency is enabled. Visible resident route-ready boxes are green;
  cache-only resident route-ready boxes are cyan when diagnostics expose them;
  resident boxes missing a BLAS are yellow; other resident problem boxes are
  magenta. This is intended to visually confirm whether selected-area emissive
  rigid contributors remain in the routed BVH without resurrecting destroyed
  ordinary rigid geometry.
  Resident/static overlay boxes are validated before upload and capped to 64
  boxes because mode 22 checks wireframe lines in the PT pass and very high or
  invalid line workloads can trigger GPU timeout/device removal.

- r_pathTracingStaticAreaPreload
  Defaults to 1. When enabled with source3, preloads static-world surfaces from
  the current Doom area plus selected portal-adjacent areas into the existing
  static geometry cache. This avoids waiting for raster drawSurfs to seed the
  static BLAS when Doom's area/portal frontend hides a neighboring cell. It is
  deliberately limited to static-world surfaces; rigid objects still use the
  rigid residency path, and skinned/deforming actors remain raster-frame
  dynamic work.

- r_pathTracingStaticAreaPreloadPortalSteps
  Defaults to 4. Portal traversal depth for source3 static-world preloading.
  Depth 4 is the current standard for the Mars City opening-section cell layout.
  `PS_BLOCK_VIEW` edges are counted for diagnostics but do not block this
  source3 RT preload traversal.

- r_pathTracingStaticAreaPreloadDump
  One-shot dump of source3 static-world area preload stats: current area,
  portal depth, selected areas/surfaces, built static surfaces/triangles, skip
  counts, and resulting static cache sizes.

- r_pathTracingSceneBoundsOverlay
  Bounds diagnostic category selector for the drawSurf mirror:
  0 = off for raster overlay, but modes 21/22 auto-enable eligible-rigid boxes,
  1 = eligible rigid drawSurf candidates plus resident rigid boxes,
  2 = all mirrored categories plus resident rigid boxes,
  3 = resident rigid boxes only,
  4 = static geometry cache boxes with this-frame selected/preloaded records
  first,
  5 = static cache boxes plus resident rigid boxes,
  6 = static geometry cache boxes with cache-only records first. Mode 21
  renders solid captured boxes; mode 22 renders PT-native wireframe boxes and
  is the preferred spatial inspection view. Use mode 3 when validating whether
  fully occluded cached rigid objects remain resident. Use mode 4 when
  validating the current static preload selection. Use mode 6 when validating
  whether older preloaded static level surfaces are retained after leaving that
  cell.

- r_pathTracingSceneBoundsOverlayMax
  Caps how many drawSurf mirror or residency/cache bounds boxes are collected
  per frame. Resident/static mode 21/22 boxes have an additional safety cap of
  64.

- r_pathTracingSceneBoundsDepthTest
  Legacy no-op from the first raster/debug-line bounds attempt. It is retained
  only so old test commands do not fail.

- r_pathTracingSceneBoundsOverlayGpu
  Default-off emergency gate for the old shader-composited per-pixel line
  overlay. Keep 0. That path looked useful but caused device removal/TDR. Modes
  21/22 upload the same bounds line buffer for box data without enabling the
  risky line-overlay loop.

Runtime emissive state:

- Stable material metadata means "this material can be emissive"; it is not
  enough to decide whether a particular surface is emitting this frame. Doom
  game/render logic can drive material stage condition/color registers per draw
  surface, including switchable fluorescent lights that dim as they turn off.
- PathTraceSceneCapture stamps captured triangles with the evaluated
  draw-surface emissive state from drawSurf->shaderRegisters. The emissive
  inventory and modes 18/19/20 honor that per-surface state, so switchable
  light surfaces no longer glow just because their material has an emissive
  stage in the declaration.
- Routed rigid emissive-card promotion is only safe for stable object-attached
  signage/glow cards. The known `swinglight` material family is temporarily
  excluded because it is game-logic/register driven and must remain on the
  dynamic drawSurf path until routed rigid records have a safe per-frame
  material-state channel. Do not use material name exclusions as the final
  model; the intended replacement is a detector for stage condition/color
  dependencies on shader parms, globals, time, sound, or other dynamic
  expression inputs.
- The `_add + 0200` classifier exclusion is separate from runtime material
  state. It prevents generated/default-RGBA additive variants from being treated
  as real translucent/emissive signage candidates.
- This is an important ReSTIR/PT contract: Material universe owns stable
  capabilities/facts, while the per-frame capture path owns runtime facts such
  as active emissive, dimming, and on/off state.
- See docs/pathtrace_interactive_surfaces.md for the deeper material-stage,
  shader parm, and evaluated register model behind switchable lights and
  red/green door panels.

- r_pathTracingSmokeParticleDither
  Enables a cheap smoke-card visibility hack for RT smoke particle materials.
  Defaults to 1. Particle/smoke translucent subtype hits use stable alpha
  dithering for primary rays and are ignored by debug shadow rays, which keeps
  large smoke cards from fully hiding the map while avoiding sorted blending.

- r_pathTracingSmokeParticleAlphaScale
  Opacity scale for smoke particle dithering. Defaults to 0.25. Lower values
  make smoke cards less visually dominant in RT debug modes.

- r_pathTracingSmokeParticleEdgeFade
  Enables a UV edge fade for smoke particle dithering. Defaults to 1. This
  reduces the obvious rectangular silhouette from particle cards whose source
  alpha is broad or flat.

- r_pathTracingPortalWindowStochastic
  Enables stable stochastic transparency for RT smoke portal/window surfaces.
  Defaults to 1. This is a debug placeholder for Doom 3's large level glass and
  uses the material alpha/albedo mask as a Russian-roulette opacity source.

- r_pathTracingPortalWindowAlphaScale
  Opacity scale for portal/window stochastic transparency. Defaults to 0.35.

- r_pathTracingPortalWindowMinOpacity
  Minimum primary-ray opacity for portal/window stochastic transparency.
  Defaults to 0.05.

- r_pathTracingPortalWindowShadowOpacity
  Shadow-ray opacity multiplier for portal/window stochastic transparency.
  Defaults to 0.05 so large windows mostly stop blocking debug direct shadows.

- r_pathTracingAdditiveDecalKey
  Treats additive translucent decal/signage materials as RGB-keyed overlays in
  the RT smoke path. Near-black texels are ignored by primary rays so the
  underlying surface can be hit, and additive decals do not block debug shadow
  rays.

- r_pathTracingAllowGuiTextures
  Opt-in diagnostic for GUI/SWF-like material textures. Defaults to 0. When set
  to 1, GUI/video/generated/scratch-named images may enter the RT smoke texture
  table. GUI-like render-target/UAV-backed images are allowed when their
  texture descriptor is still a usable 2D SRV, because live SWF/GUI panels can
  route through screen/dynamic stages. Timing/material logs report
  guiTextures=candidates/accepted/rejected.

- r_pathTracingAllowGuiSurfaces
  Opt-in diagnostic for GUI/SWF draw-surface geometry cards. Defaults to 0. When
  set to 1, GUI surfaces are allowed through RT smoke capture and classified as
  particle/alpha with the GUI-screen translucent subtype.

- r_pathTracingGuiDump
  One-shot dump of captured in-world GUI draw surfaces. This logs material name,
  capture status, material-table slot, vertex color/alpha range, UV range, stage
  texgen/dynamic flags, image names, and texture safety. It was used to confirm
  that generated GUI draw surfaces existed but were initially rejected because
  their synthetic GUI view entity has no normal render model.

- r_pathTracingSkipCallbackEntities
  Skips non-GUI deferred callback render entities that also use a custom shader
  in RT smoke capture. Defaults to 1. This was added after a repeatable
  hang/exit when the player collected a desk disk item: collision/pickup deleted
  the mesh while the RT smoke path was still able to touch callback-driven item
  geometry, likely the pulse/highlight shell using idItem::ModelCallback and a
  custom shell material. A broad callback skip fixed the crash but hid animated
  and skinned characters because MD5 entities also use idEntity::ModelCallback.
  The current narrower custom-shader callback guard keeps those characters
  visible while still fixing the disk pickup crash.

Normal, specular, and emissive material textures are discovered through the same
cached material metadata path as albedo. They are bound into the shared RT smoke
bindless texture table. Debug mode 16 previews sampled normal maps, debug mode
17 previews sampled specular maps, and debug mode 14 can apply normal maps to
its selected point-light direct lighting when r_pathTracingUseNormalMaps is 1,
specular maps when r_pathTracingUseSpecularMaps is 1, and an unshadowed emissive
term when r_pathTracingUseEmissiveMaps is 1.

- r_pathTracingTextureBindlessEnable
  1 samples through the bindless descriptor table. 0 samples a regular fallback
  SRV and is useful for diagnostics.

- r_pathTracingTextureForceFallback
  Binds fallback textures into active table slots instead of captured Doom
  diffuse textures. This helps separate texture-handle issues from shader
  texture-fetch issues.

- r_pathTracingTextureFallbackDump
  One-shot dump of materials that fell back to debug colors instead of sampled
  albedo textures.

- r_pathTracingAlphaDump
  One-shot dump of alpha-tested materials, discovered alpha/coverage images,
  cutoffs, texture dimensions, and descriptor slots.

- r_pathTracingTranslucentDump
  One-shot dump of translucent material subtype classification reasons.

- r_pathTracingCrosshairMaterialDump
  One-shot dump of detailed RT smoke material diagnostics for the surface hit by
  the center crosshair ray. This logs the Doom material name, coverage, sort,
  surface class/subtype, classifier flags, RT texture metadata, RT material
  slots/flags, triangle vertex colors, and all material stages with blend,
  color, alpha-test, texgen, dynamic/cinematic, image, and safety information.

- r_pathTracingLightDump
  One-shot dump of the selected debug point lights used by modes 14 and 15.

- r_pathTracingLightCount
  Maximum selected visible point lights used by modes 14 and 15. Defaults to 4,
  with a debug cap of 32.
  Set to 1 or 2 to reduce secondary shadow-ray pressure during stability
  testing, or 0 to use the fixed directional fallback path.

- r_pathTracingLightSelection
  Selected point-light ranking for modes 14 and 15. 0 keeps the original
  nearest-camera behavior. 1 ranks by estimated camera influence from evaluated
  light color/intensity and radius falloff.

- r_pathTracingLightSpriteProxies
  Enables debug-only emissive sprite proxies for selected RT smoke lights in
  mode 14. Defaults to 1. This is a placeholder for Doom 3 light shader flare
  and strobe visuals that may not have ordinary surface geometry in the BLAS.

- r_pathTracingLightSpriteRadiusScale
  World-space radius scale for selected-light sprite proxies. Defaults to 0.04.

- r_pathTracingLightSpriteIntensity
  Intensity multiplier for selected-light sprite proxies. Defaults to 2.5.

- r_pathTracingDoomLightDump
  One-shot Doom renderer light identity dump. It reports map name, renderer
  light index, linked game entity name/class, material/shader, origin, radius
  and bounds, evaluated renderer color, raw/current game color, active/hidden
  and level state, selected area, portal depth from the player area, bounds
  area counts, and crosshair/nearest metrics. The selected area is the nearest
  portal-selected area touched by the light's renderer bounds, not only the
  light origin/primary area, so portal-border lights do not disappear when the
  player moves between adjacent cells. Mode 1 prints summary plus capped
  samples; mode 2 prints verbose samples up to r_pathTracingDoomLightDumpMax.

- r_pathTracingDoomLightProbeDump
  One-shot nearest/player/crosshair light dump for finding doubled or helper
  lights near emissive surfaces. The crosshair list is only an inspection view;
  PT analytic candidate selection is area/active based and still includes
  lights behind the camera.

- r_pathTracingDoomLightDumpMax / r_pathTracingDoomLightProbeMax
  Record caps for Doom light identity and probe dumps.

- r_pathTracingAnalyticLightCandidates
  Builds, uploads, and shades analytic sphere-light candidates from active Doom
  point lights. Candidate selection uses active/on state, point-light type, and
  the current-area plus r_pathTracingLightAreaPortalSteps portal selection
  resolved over each light's renderer bounds/area membership. It does not
  require the light to be visible or in front of the camera, and it does not
  add any sphere mesh to the BVH. Defaults to 1 so the native PT and ReSTIR PT
  paths see the same Doom analytic-light universe without requiring an opt-in
  console setup.

- r_pathTracingRestirPTAnalyticLightCandidates
  ReSTIR PT debug-mode convenience gate. Defaults to 1, so modes 26-33 and
  50-51 can still build, upload, and shade Doom analytic light candidates when
  the global analytic-light CVar is off for A/B testing. The global analytic
  light path also defaults to 1. Set both CVars to 0 for emissive-only ReSTIR
  validation, CPU light-candidate profiling, or device-removal narrowing.

- r_pathTracingAnalyticLightCandidateDump
  One-shot dump of the analytic Doom light candidates that would be uploaded.
  The summary includes linked count, behind-camera count, portal steps,
  intensity scale, GPU cap, uploaded count, dropped-by-cap count, and radius
  scale/min/max.

- r_pathTracingAnalyticLightMaxGpu
  Maximum analytic Doom sphere-light candidates uploaded and sampled by the PT
  shader. Defaults to 256 so the current Mars City diagnostic cases can upload
  the full selected Doom-light set instead of letting portal-border area flips
  reorder relevant lights past the previous 96-light cap. Lower this carefully
  if direct per-hit lighting becomes too expensive; it is currently a direct
  loop, not ReSTIR.

- r_pathTracingAnalyticLightIntensityScale
  Radiance calibration multiplier for analytic Doom sphere lights. Defaults to
  4.0 after initial visual testing showed that roughly 4-5 matched Doom's
  authored illumination better than the first low diagnostic value. Use
  0/0.25/1/4 while visually isolating the contribution.

- r_pathTracingAnalyticLightReplaceSelected
  When analytic Doom lights are enabled, suppresses the legacy selected
  point-light constant array so the analytic contribution can be inspected
  alone. This does not disable emissive surfaces or reservoir candidates.

- r_pathTracingAnalyticSphereLightRadiusScale
  Analytic sphere radius as a fraction of the Doom point-light radius. Defaults
  to 0.08, clamped by the min/max radius CVars. This is only the first
  scaffold; Doom light materials such as `biground*`, `squarelight*`, and
  `triangle*` are not physically classified yet.

- r_pathTracingAnalyticSphereLightRadiusMin / r_pathTracingAnalyticSphereLightRadiusMax
  Clamp the diagnostic sphere emitter radius.

- r_pathTracingAnalyticLightDoomRadiusCutoff
  Debug-only Doom analytic RAB sphere-light switch. Defaults to 0. When 0, the
  Doom authored radius remains uploaded as explicit influence/proposal metadata
  but does not hard-reject or attenuate sampled sphere-light radiance. When 1,
  the RAB sampler restores the old hard Doom-radius cutoff/falloff mask, which
  is useful for diagnosing selected-light ownership but is not the default RRX
  analytic-light path.

- r_pathTracingSmokeLog
  Enables or disables periodic RT smoke logging.

- r_pathTracingTimingLog
  Enables slow-frame CPU timing logs for RT smoke scene build and dispatch
  submission. Scene-build timing includes material metadata-cache counters and
  whether the experimental RT smoke material table cache hit or rebuilt.
  Dispatch timing now reports named pseudo-pass CPU submission costs for the
  still-monolithic raygen path: setup, ReSTIR context update, constants upload,
  resource barriers, reservoir/primary-history/target clears, ray tracing state
  setup, RT dispatch submission, primary-history copy, and optional readback.
  These numbers are CPU-side command submission timings; use
  `r_pathTracingOptickGpuMarkers 1` only during targeted profiling when GPU
  pass duration is needed.

- r_pathTracingTimingThreshold
  Minimum RT smoke CPU time in milliseconds before timing logs are printed.

- r_pathTracingTimingLogInterval
  Minimum milliseconds between repeated RT smoke timing log lines. Defaults to
  1000. Set to 0 to print every threshold hit while doing detailed profiling.

- r_pathTracingPassTimingDump
  One-shot dump of the current RT/PT dispatch pseudo-pass timing line,
  independent of the slow-frame threshold. The line also includes the debug
  mode classification, output family, owner, output resolution, dispatch
  dimensions, SPP/max-depth settings, rough estimated rays per pixel,
  ReSTIR pass label/resampling mode, preview-visibility flag, clear requests,
  readback activity, and whether Optick or Nsight GPU markers are enabled.

- r_pathTracingOptickGpuMarkers
  Enables experimental Optick GPU markers inside the RT smoke/path tracing
  scene-build and dispatch passes. Defaults to 0. Leaving these PT-specific
  native command-object marker calls enabled caused a repeatable crash to
  desktop on level entry in the Release PT build, while the same build was
  stable when the marker calls were gated off. Keep this off unless actively
  capturing a targeted Optick GPU profile.

- r_pathTracingNsightGpuMarkers
  Enables NVRHI command-list debug markers around RT smoke/path tracing
  `DispatchRays` calls for Nsight Graphics captures. Defaults to 0. In mode 56
  the marker labels are:
  `PT56.0 PrimarySurfacePrepass DispatchRays`,
  `PT56.1 GIInitialProducer DispatchRays`,
  `PT56.2 DirectTemporalProducer DispatchRays`,
  `PT56.3 DirectSpatialReservoirProducer DispatchRays`,
  `PT56.4 ReflectionProducer DispatchRays`, and
  `PT56.5 CombinedResolve DispatchRays`. Use this instead of
  `r_pathTracingOptickGpuMarkers` when the goal is to identify which
  `vkCmdTraceRaysKHR` event owns a slowdown in Nsight.

- r_pathTracingRestirPTGpuTimingDump
  One-shot ReSTIR PT GPU timestamp capture. Set to 1 while running mode 56 to
  print one completed frame of per-dispatch GPU time once the query results are
  ready. The output reports primary-surface prepass, GI initial producer,
  legacy-named direct producer wrappers, reflection producer, and final resolve.
  The direct labels are scaffolding, not proof of completed DI temporal/spatial
  reuse. This is the fallback when Nsight does not display command-list marker
  labels.

Level-load safety:

- The Doom analytic light bridge only scans `gameLocal.entities` while
  `gameLocal.GameState()` is `GAMESTATE_ACTIVE`. Map shutdown/startup frames
  can otherwise expose stale entity pointers while the renderer is still
  drawing loading or transition frames.
- `PathTraceSmokeSceneBuild` treats render-world map name/timestamp changes as
  a scene-cache reset boundary even when Doom reuses the same render-world
  object. The reset clears PT scene buffers, BLAS handles, geometry/light
  universes, accumulation, and binding state, but deliberately preserves
  output-sized resources such as reservoir buffers and descriptor tables. A
  full scene-resource reset at this point caused a black screen with "failed to
  create RT smoke binding set" because the reservoir buffers had just been
  cleared after output resize.

PT output resize stability:

- A repeatable hard crash/device-removal during `r_pathTracingDebugWidth` /
  `r_pathTracingDebugHeight` changes was traced to PT internal output-size
  resource lifetime, not to application-window resizing and not to the transient
  aspect ratio created when width and height are changed one CVar at a time.
  The crash happened after the console printed the new PT output resolution,
  then the driver/device was removed.
- Earlier attempts to fix app back-buffer resize were misleading because the PT
  output is not tied to the native back buffer. Gating the debug blit, skipping
  PT/raster 3D for native resize frames, and invalidating the cached LDR
  framebuffer were still reasonable presentation safeguards, but they did not
  address the failing path.
- The actual resize safety policy now lives in PathTraceFrameResources and is
  called by `PathTraceSmokeResources::ResizeRayTracingSmokeOutput`: before
  replacing any existing PT output, accumulation, readback, smoke reservoir,
  ReSTIR PT reservoir, or primary-surface history handle, the code calls
  `waitForIdle()` and `runGarbageCollection()`. This prevents old output-sized
  resources from being released while the previous frame's ray dispatch, output
  blit, readback copy, or descriptor use is still pending on the GPU. The
  frame-resource diagnostics report the wait reason, reset reasons, output
  resolution, resource byte counts, descriptor/AS commits, and readback queue
  activity through `r_pathTracingReservoirDump`.
- Future PT output-resolution work should continue to treat width/height as an
  internal render-scale pair. It is acceptable for users to enter width and
  height separately; the intermediate frame may look stretched, but it must not
  replace GPU resources without an idle/deferred-destruction boundary.

- r_pathTracingSkipRaster3D
  Skips the normal 3D raster view after the RT smoke/path tracing pass has
  produced the 3D scene output. Defaults to 1. GUI/HUD/menu views still rasterize
  afterward. In testing, leaving 3D raster enabled under mode 18 roughly doubled
  frame cost and allowed raster-side hitches to appear even though the visible
  3D scene was path traced. Set this to 0 only for regression comparisons or to
  check whether a specific raster side effect is still required.

- r_pathTracingReadbackEnable
  Enables RT smoke UAV readback diagnostics. Defaults to 0 because readback
  waits for the GPU to idle and can cause large profiling hitches.

- r_pathTracingMaterialCache
  Caches the universe-derived RT smoke active material table when the material
  ID set, texture registry generation, descriptor/probe options, and relevant
  material CVars are unchanged. Defaults to 1. Set to 0 to measure the uncached
  active-table build path.

- r_pathTracingMaterialMetadataCache
  Caches stable per-material texture metadata such as diffuse/alpha image
  discovery, cutoff, and texture safety. This feeds the persistent material
  universe and active material table.

- r_pathTracingMaterialUniverseValidate
  Validates persistent RT smoke material universe records against a fresh direct
  rebuild and logs mismatches. This is for checking cached record/fact
  correctness, not the active frame table.

- r_pathTracingMaterialUniverseTable
  Builds the active RT smoke frame material table from persistent material
  universe records. Defaults to 1. The shader-facing table remains compact and
  preserves the legacy active row order so cached static triangle material-index
  buffers stay compatible.

- r_pathTracingMaterialUniverseTableValidate
  Builds both the legacy and universe-derived active material tables and compares
  them. When the universe table is active and validation finds a mismatch, the
  frame falls back to the legacy table and logs materialTablePath=legacyFallback.


Geometry and BVH Work Completed
-------------------------------

The pass now builds real ray tracing acceleration structures from visible Doom
draw surfaces:

- Captures visible draw surfaces from the current view.
- Builds BLAS/TLAS using NVRHI ray tracing APIs.
- Rebuilds dynamic-candidate geometry each frame.
- Maintains a persistent static-world geometry cache.
- Owns that cache through PathTraceGeometryUniverse, currently as a behavior-
  preserving scaffold around the existing static surface keys, vertices,
  indexes, triangle classes, and triangle material IDs.
- Rebuilds the static-world BLAS only when newly seen static surfaces are added.
- Rebuilds the TLAS every frame.
- Splits static-world and dynamic-candidate geometry into separate buffers.
- Instances static and dynamic BLASes in one TLAS.
- Closest-hit shader fetches from the correct static or dynamic buffers based
  on InstanceID.

The static-world cache is populated independently of the center-ray anchor,
using stable surface keys. This avoids obvious instability from Doom draw-surface
ordering changes.


Surface Classification
----------------------

Captured surfaces are classified into debug buckets:

- static-world
- rigid-entity
- skinned/deformed
- particle/alpha
- unknown

This is used for logging, debug visualization, and the static/dynamic BLAS split.
Entity-owned static models with static caches and no joints are treated as
rigid-entity rather than fully dynamic.

The classifier has been exercised in game. It broadly behaves as expected:

- World geometry is usually green/static.
- Doors, crates, and entity-owned rigid objects are dynamic/rigid.
- Skinned characters are correctly identified.
- Smoke cards and translucent particle-like surfaces are particle/alpha.

Some edge cases remain, such as occasional static-looking pieces classified as
rigid/dynamic. This is acceptable for now because correctness is favored over
over-aggressive static caching.

Translucent surfaces now have a second debug-only subtype classifier used by
mode 12:

- decal/grime
- object glass
- smoke/particle
- signage/glow
- portal/window
- GUI/screen
- unknown

This classifier is intentionally diagnostic. It helped identify Doom 3's heavy
use of translucent cards for dirt, grime, logos, warning labels, windows, smoke,
and light/glow surfaces. It should not be treated as final material semantics.
Some categories, especially glow/signage and object glass, need a more complete
material and lighting model before they can be handled correctly.


Skinned Geometry
----------------

Initially, skinned characters appeared in T-pose because the RT capture was
using base/static mesh data while the raster renderer was using GPU skinning.

Current behavior:

- With r_useGPUSkinning enabled, RT smoke now has an RT-only CPU skinning path
  for MD5/skinned surfaces.
- It uses current joint data and the weighted joint transform logic from the
  MD5 model path.
- Scene capture now records explicit skinned/deforming surface keys and dynamic
  ranges, matches current records to the previous frame by key, retains a
  skinned-only CPU copy of the previous RT CPU-skinned vertex output, and reports
  conservative match/invalid reason counts through `r_pathTracingSceneInputsDump`.
  This identity is still bridge-grade: it uses currently available renderer
  entity/model/surface pointer fields plus material/class and relies on scene
  reset/invalidation boundaries for safety. Do not treat it as the final
  StableInstanceID/StableGeometryID contract for long-lived temporal consumers.
- With `r_pathTracingGpuSkinning 1`, the retained previous copy is also uploaded
  as a shader-visible previous-position buffer plus dispatch records and a
  dynamic-triangle-to-dispatch-index map. The closest-hit path uses that map for
  O(1) skinned previous-position lookup instead of scanning every skinned
  dispatch record per hit, and bounds the lookup with the shader-visible map
  count rather than assuming the map always has full dynamic-triangle length.
  The compute scaffold now overwrites validated previous-position ranges from
  previous joint rows when those rows exist; the CPU-retained upload remains
  the fallback for ranges without previous joint data. The closest-hit output
  is unchanged, but primary-surface packing can mark validated skinned hits with
  `RT_PRIMARY_SURFACE_HAS_OBJECT_MOTION` and
  `RT_PRIMARY_SURFACE_HAS_PREVIOUS_POSITION` and store the previous world-space
  hit position in `previousPositionOrMotion.xyz`.
- `r_pathTracingGpuSkinning` defaults to `0`. Value `1` builds diagnostic
  source/output/previous-position/dispatch/current-joint/previous-joint buffers
  from those same records so the future GPU-skinning contract can be validated
  without changing rendering. It also dispatches `pathtrace_skinning.cs.hlsl`
  to write the diagnostic current-output buffer and the previous-position
  buffer when previous joints are available. Value `2` retargets the
  current-output writes to validated skinned spans in the dynamic vertex buffer
  before dynamic BLAS build, while still sourcing geometry/ranges/joints from
  the CPU bridge.
- This does not change the main raster r_useGPUSkinning setting.
- Characters now animate correctly in the RT debug output.

This path is not optimized. It is a correctness bridge so the RT path can trace
posed skinned geometry while the renderer continues using GPU skinning normally.


Vertex Attributes
-----------------

RT smoke geometry buffers now include:

- position
- normal
- UV

Closest-hit interpolates normals and UVs. Debug modes were added for:

- interpolated normal
- geometric normal
- UV visualization

Several issues were encountered and fixed:

- Some normals were invalid or near zero.
- Particle/alpha/translucent surfaces were especially unreliable with imported
  vertex normals.
- Fallback geometric normals are generated where needed.
- Attribute validation logs invalid normal/UV counts by class.

After hardening, normal and UV debug modes appear stable and useful.


Material Debug Work
-------------------

The RT smoke path now tracks material identity per triangle:

- Each captured surface gets a stable material ID from the idMaterial name.
- Per-triangle material IDs are stored for static and dynamic geometry.
- Debug mode 6 colors hits by material ID.
- A compact material table exists for captured materials.
- Per-triangle material table indexes are stored.
- Debug mode 7 shades from the material table debug color.

This provides the CPU/GPU plumbing needed for real material data, without yet
implementing the full Doom material system in RT.


Texture Probe Work
------------------

Debug mode 8 is an early sampled diffuse texture probe.

The current implementation:

- Discovers plausible diffuse/base-color images from captured materials.
- Avoids calling idMaterial::GetEditorImage() during RT discovery.
- Builds an NVRHI bindless Texture_SRV descriptor table.
- Reuses the descriptor table instead of recreating it every scene build.
- Writes a conservative active window of safe Doom diffuse textures.
- Stores descriptor indexes in the RT material table for captured materials.
- Fetches bindless texture data in the ray generation shader.
- Falls back to material debug colors when a material has no bound descriptor.
- Keeps texture dump logging for controlled testing.

Important history:

- Fixed Texture2D slots were tried first and caused device-removed crashes.
- A private GPU texture copy path was tried and hit Doom/NVRHI state assertions.
- Switching to bindless descriptor tables compiled and ran.
- The remaining repeatable crash was traced to texture discovery calling
  GetEditorImage(), which can load missing editor images on demand.
- Removing the editor-image fallback eliminated the repeatable crash in the
  tested map location.
- A later device-removed crash near animated SWF/GUI terminals was narrowed to
  SampleLevel from the RT shader. It reproduced even when sampling the fallback
  white texture and with bindless disabled.
- Texture.Load is stable in the same animated terminal cases so far. It is now
  the default mode 8 fetch path. SampleLevel remains available as a diagnostic.
- Texture.Load now has manual bilinear filtering behind
  r_pathTracingTextureFilter. This improves the debug texture probe without
  using sampler-state based SampleLevel in the ray tracing shader.
- Diffuse textures using Doom's YCoCg DXT5 encoding are now tagged in the RT
  material payload and decoded in the shader when r_pathTracingTextureDecode is
  enabled. Without this, TD_DIFFUSE images appear with incorrect green/orange
  colors and exaggerated block compression artifacts.

Mode 8 is still only a probe. Texture.Load does not yet implement mip selection
or full Doom material behavior.

Doom BFG generated .bimage names append a storage hint as #__%02d%02d, where
the first two digits are textureUsage_t and the second two are cubeFiles_t.
Observed 2D material texture codes are:

- #__0000 = TD_SPECULAR / CFM_DEFAULT. Specular/control texture. Its alpha is
  not material opacity.
- #__0100 = TD_DIFFUSE / CFM_YCOCG_DXT5. This is a CoCg_Y diffuse payload:
  sampled alpha stores Y/luminance for color reconstruction, not transparency.
  Never use this alpha as material alpha in the general case.
- #__0200 = TD_DEFAULT / CFM_DEFAULT. This is ordinary RGBA/default storage. If
  an alpha channel is present and the material stage/blend uses it, it should be
  treated as real alpha.
- #__0300 = TD_BUMP / CFM_NORMAL_DXT5. Normal-map payload. The raster convention
  reads normal X from alpha, normal Y from green, and reconstructs Z.
- #__1000 = TD_COVERAGE / CFM_GREEN_ALPHA. Coverage/cutout data for clipping or
  stencil-style visibility.

The safest RT material rule is: first trust image->GetOpts().colorFormat, then
the material stage lighting/blend/alpha-test state, and use filenames or paths
only as tie-breakers. In particular, CFM_YCOCG_DXT5 means alpha is occupied by
color reconstruction, CFM_DEFAULT means alpha can be real material data, and
CFM_GREEN_ALPHA/TD_COVERAGE is the dedicated coverage path.

Classic Doom 3 decal intent also survives strongly in paths and material
stages. Textures under /decals/ that are #__0100/CFM_YCOCG_DXT5 generally do
not have real alpha; they are blend decals using decoded RGB as the effect.
Lighten/additive decals tend to use black as the no-effect key, while
darken/filter decals tend to use white as the no-effect key. Their visibility
should be inferred from decoded RGB and blend state, not from the YCoCg alpha
channel. #__0200 decal/default textures with alpha are different: because they
are CFM_DEFAULT RGBA, their alpha can be real if the stage uses it.

Exception: `_add...#__0200` / `_add...0200` generated texture variants are
explicitly banned from the translucent/signage/glow classifier path for the PT
prototype. The classifier records this as `hasAddDefault0200Texture`; overlay,
emissive, alpha, subtype, and rigid emissive-card promotion consumers reject
that flag so broad `_add` name matching does not turn default-RGBA add variants
into translucent/routed emissive cards.

The active texture table was expanded to 2048 descriptors after diagnostics
showed the earlier conservative limits left many valid captured materials on
fallback debug colors. With Texture.Load as the default fetch path, larger
descriptor windows cover longer traversal tests without the earlier SWF/GUI
terminal crash pattern.


Alpha, Albedo, and Lighting Debug Modes
---------------------------------------

Alpha-test exploration now exists in debug-only form:

- Material discovery looks for dedicated alpha/coverage images on perforated
  materials when available.
- Translucent glass/signage/glow-like cards can also opt into alpha any-hit
  when the material exposes an explicit alpha-test stage, which catches cutout
  cards such as glasses lenses without treating all blended translucency as
  clipped geometry.
- Ambient blended decal/grime materials can provide their safe ambient image as
  RT albedo when no diffuse stage exists. The any-hit path also masks their
  blend-mode no-effect texels: white-keyed filter decals drop near-white texels,
  while black-keyed filter decals drop near-black texels. Retained filter decal
  texels are stochastically thinned as a temporary overlay approximation so
  door/wall surfaces can still show through in the one-hit debug path.
- Packed YCoCg perforated materials that reuse the diffuse image as their
  coverage stage can derive the RT alpha test from decoded diffuse luminance
  instead of the raw alpha channel. Reflective eyewear cutouts such as glasses
  and goggles use an inverted dark-key variant of that coverage test, plus a
  dark neutral placeholder albedo while keeping their normal/specular maps,
  since their original look depends on reflection/cube-map behavior that the
  debug path does not implement yet.
- Mode 9 visualizes alpha pass/fail using stable Texture.Load fetches.
- An any-hit shader is wired for alpha-tested materials and calls IgnoreHit()
  when sampled alpha is below the material cutoff.
- Opaque materials stay on the normal closest-hit path.

This has worked well for fan grates and perforated floors. Some Doom character
materials expose dissolve/blood-style masks; these were initially over-applied,
then narrowed so normal character albedo is usable again.

Albedo and simple lighting exploration:

- Mode 10 shades hits with sampled diffuse/albedo textures and falls back to
  material debug colors when no safe texture descriptor exists.
- Mode 11 overlays translucent/decal-like surfaces on top of the albedo view for
  inspection.
- Mode 12 colors translucent subtype classifications.
- Mode 13 applies a simple fixed-direction Lambert light using sampled albedo
  and the interpolated/geometric RT normal.
- Mode 14 extends mode 13 with debug-only shadow rays from the primary hit
  toward up to 32 selected visible Doom point lights. The default selector
  ranks lights by estimated camera influence from evaluated light color,
  r_lightScale, and radius falloff; r_pathTracingLightSelection 0 restores the
  original nearest-camera ranking. Mode 14 still uses simple radius-based debug
  attenuation and falls back to the old fixed directional light if no point
  light is available. The shadow rays preserve the existing alpha any-hit path
  so cutout blockers can be ignored when their alpha tests reject the hit.
  Mode 14 can apply sampled normal maps when r_pathTracingUseNormalMaps is 1.
  The normal decode intentionally matches the raster G-buffer shader: X comes
  from the texture alpha channel, Y from green, and Z is reconstructed. A
  previous direct .xyz normal decode made most surfaces face away from lights
  and killed the selected-light response. Mode 14 can also add a simple
  Blinn-style direct specular highlight from the sampled Doom specular map when
  r_pathTracingUseSpecularMaps is 1, plus an unshadowed emissive/glow preview
  from safe ambient/additive material stages when r_pathTracingUseEmissiveMaps
  is 1. This is diagnostic shading, not final PBR or light-source sampling.
- Mode 15 visualizes the dominant selected light at each primary hit. It uses
  the same selected-light payload and alpha-aware shadow rays as mode 14, but
  shades by light slot instead of final albedo lighting so selection and
  occlusion can be inspected directly.

Particle/smoke placeholder handling:

- Smoke/particle translucent subtype surfaces can use stable alpha dithering
  when r_pathTracingSmokeParticleDither is 1. Shadow rays ignore these smoke
  particles so broad cards do not erase direct-light shadows.
- r_pathTracingSmokeParticleAlphaScale scales the dither survival probability.
  r_pathTracingSmokeParticleEdgeFade applies a simple UV edge fade to reduce the
  visible rectangle silhouette. This remains a placeholder hack; it can still
  look noisy and card-like.
- Portal/window translucent subtype surfaces can use a similar stable
  stochastic transparency placeholder when r_pathTracingPortalWindowStochastic
  is 1. This targets large level windows only; small object glass remains on the
  existing path until a real glass material exists. Portal/window materials with
  no safe diffuse texture use their constant ambient/diffuse stage color as a
  temporary fallback tint instead of the material hash debug color, and fall
  back to a neutral blue-gray tint if the stage color is not constant. Plain
  portal/window fallback materials are kept out of the decal overlay thinning
  path and receive a small minimum stochastic opacity so indoor portal glass
  remains visible even when it has no grime/albedo mask.
- Object-glass translucent subtype materials, such as small props and vending
  machine glass doors, use the same stochastic glass placeholder at a lower
  minimum opacity. This is still a debug approximation for reflect-cube glass,
  not a physically correct glass/reflection implementation.
- Selected animated light shaders can draw a debug-only screen/ray-space sprite
  proxy in mode 14. This is meant to restore visibility for strobe/flare-like
  light visuals whose Doom material is a light shader rather than a captured
  surface. It uses the evaluated light shader color, so table/Parm-driven
  pulsing is reflected, but it is not sampled as a real path-traced emitter.

GUI/SWF panel placeholder findings:

- In-world Doom GUI panels are not ordinary textured screen meshes. The raster
  path calls the GUI/SWF system, emits many 2D painter-order draw commands into
  a synthetic GUI view entity, and then rasterizes those commands onto or around
  the panel. Generated GUI drawSurfs normally have no frontEndGeo, and that is
  important: the generated painter-order rectangles are not the physical monitor
  mesh.
- The generated GUI surfaces do not belong to a normal render model. Validation
  therefore must exempt GUI draw surfaces from the usual entity hModel check;
  otherwise r_pathTracingGuiDump reports many GUI surfaces with captured=0 and
  no material metadata.
- Directly tracing the generated GUI quads is only a proof-of-life placeholder.
  The UI content becomes visible, including animated/SWF content and individual
  text/image layers, but ray tracing interprets Doom's 2D painter-order
  rectangles as real 3D occluders. Layer offsets can make one panel look better
  while breaking another: background layers may flip to the front, large
  rectangles can hide nearby world geometry or monitor shells, and round
  screens can need additional alpha clipping. Some circular panels happened to
  look better in one offset configuration while rectangular terminals became
  worse.
- The frame-local generated-GUI frontEndGeo/layer-offset experiment has been
  removed. GuiModel.cpp is back to its original behavior, with generated GUI
  draw surfaces not exposing synthetic frontEndGeo to PT capture.
- This was not cosmetic. The bad GuiModel.cpp experiment affected the shared
  raster path too: in-world video/GUI screens could become black or show only
  leaking edges, layer panels could overlap the monitor shell, and Bink/GUI
  update work could explode into multi-hundred-millisecond hitches. Reverting
  GuiModel.cpp fixed the visible screen breakage and removed the repeatable
  hitch route. Do not reintroduce generated-GUI frontEndGeo as a normal path.
- The likely correct long-term design is to keep Doom's GUI/SWF raster path for
  panel contents: rasterize each live panel offscreen into a texture, identify
  the actual physical screen/panel surface, keep the generated painter-order GUI
  quads out of the TLAS, and have the RT material sample the composited panel
  texture on the physical surface. Interaction/raycasting should stay with the
  original GUI system. This preserves Doom's animation, text, alpha, clipping,
  and painter order while RT only sees a single physical screen material.

Known limitations:

- Normal maps can still look wrong on some props. Doom 3 legacy normal maps use
  a green/Y convention that can differ from modern defaults, now testable with
  `r_pathTracingNormalMapFlipGreen`, and original assets were baked against
  idTech4's pre-MikkTSpace tangent basis. The RT debug path still recomputes
  tangent space in shader and normalizes handedness, which can erase the engine
  vertex tangent/sign convention used by legacy renderbump assets. A proper fix
  should preserve/import the engine tangent basis or add a narrowly scoped
  legacy-vs-modern material path, rather than hard-flipping all normals globally.
- Custom-shader deferred callback render entities are skipped by default in RT
  smoke capture. This avoids a confirmed pickup/delete crash on an item disk
  without hiding normal animated/skinned entities that use model callbacks.

Material table performance:

- The material side is now mostly through the material universe for easy
  single-threaded wins: persistent records/facts, universe-derived active table,
  cached active-table builds, and stable material/universe indexes are in place.
- RT smoke caches the universe-derived active material table by default with
  r_pathTracingMaterialCache. The cached-table hit path keeps static materials
  first and rebuilds per-triangle material indexes from stable material IDs so
  cached static triangle material-index buffers remain valid across dynamic
  material changes.
- Per-material texture metadata is cached independently of the active material
  table. This avoids repeatedly scanning material stages for already discovered
  materials.
- PathTraceMaterialUniverse owns persistent per-material records, stable
  universe indexes, material facts, and emissive facts.
  PathTraceDynamicMaterialState now builds the active shader-facing table from
  those records by default while preserving the legacy compact row order needed
  by cached static material-index buffers.
- Timing logs report materialTablePath=universe/legacy/legacyFallback,
  materialUniverse record counters, and optional universeTableCompare counters.
  In testing, the universe table path reduced single-threaded material build
  cost without visible material corruption after the row-order compatibility fix.
- Diagnostic texture coverage stats are skipped unless smoke logging is
  enabled.

Geometry universe status:

- PathTraceGeometryUniverse owns the persistent static-world cache vectors and
  exposes stats/generation tracking. It also owns static surface record lookup,
  per-frame lifecycle bookkeeping, append completion, and opt-in validation for
  the existing static cache data.
- PathTraceSmokeSceneBuild still uses the same shader-facing static arrays as
  before. PathTraceSceneCapture builds static geometry through the universe
  rather than directly pushing surface keys itself, but no shader-visible buffer
  contract change has been made.
- Static records now track last/previous seen frames, seen/new/gone flags,
  dirty/history validity, and explicit current/previous vertex/index/triangle
  ranges. For unchanged static world surfaces, consecutive-frame history maps
  the previous range to the same cached range.
- Scene inputs now carry a conservative static previous-frame bridge: previous
  static geometry buffer handles/counts, current seen/new/gone/history/previous-
  range counts, and booleans for previous count match, complete previous ranges,
  previous static buffer availability, and previous material-index buffer
  availability. The dump also reports whether the previous static buffer handles
  alias the current persistent static buffers; with the dedicated previous static
  GPU snapshot path that should normally be false once previous buffers exist.
  The previous static buffers are now explicit reserved shader bindings, but no
  current shader path consumes them for shading or motion-vector output.
- The geometry universe retains a one-frame CPU snapshot of the previous static
  vertex/index/triangle-class/triangle-material arrays at `BeginFrame`. Scene
  inputs and `r_pathTracingGeometryUniverseRangeDump 1` report its counts and
  size. PathTracePrimaryPass also retains the previous static material-index
  array produced by the material table. That material-index array can be larger
  than the static triangle count because it also carries static-side material IDs
  used by full-level emissive and routed-rigid paths. This is a bridge for
  validating previous static data ownership; the previous vertex/index/triangle-
  class/triangle-material/material-index arrays are uploaded into separate
  previous static GPU snapshot buffers and bound at reserved smoke shader SRV
  slots for future consumers. Once the same previous static snapshot is already
  resident in the retained previous buffers, scene build skips the repeat upload
  and reports `prevGpuUpload=0` in `r_pathTracingSceneInputsDump`.
- The smoke path now reserves private output-sized motion-vector export
  resources in PathTraceFrameResources: RG16F vectors at raygen UAV `u39` and
  an R32_UINT validity/source mask at UAV `u40`.
  `r_pathTracingMotionVectorExport` defaults to 1 and makes raygen write combined geometry
  current-to-previous pixel motion into those private textures, using the same
  shared static/skinned/routed-rigid helper as debug mode 49. Unsupported or
  invalid pixels write a zero vector plus a zero-valid mask containing the
  invalid reason; valid stationary surfaces also may write a zero vector, but
  only with a non-zero valid/source mask. The export path preserves sub-pixel
  previous projection for vectors while still flooring to an integer previous
  pixel only for history-buffer lookups. With export disabled, dispatch setup
  clears the mask texture to zero and leaves vector contents ignored. No
  external consumer reads them yet, and normal PT shading/output is unchanged.
  The frame-resource diagnostics report their allocations as `motion` and
  `motionMask`.
- Existing diagnostics report static record lifecycle and validation through
  the static BLAS summary: seen/new/gone, history/previous-range, dirty, and
  validate=total/range/duplicate-key/history/key-vector. User testing in mode
  18 reported no crashes or visible regressions, with validate=0/0/0/0/0.
- Static lookup is backed by a key-to-record map so per-surface touches do not
  scale as O(n). Heavy validation, including duplicate-key scans, is gated by
  r_pathTracingGeometryUniverseValidate.
- `r_pathTracingGeometryUniverseRangeDump 1` prints a one-shot static range
  history dump for normal validation: records, seen/new/gone, history/previous
  range counts, dirty count, current range, previous range, and current/previous
  count-match status. The summary also reports the union dirty vertex/index/
  triangle ranges that a future partial upload path would consume, but current
  shader-facing buffer contracts remain unchanged. Reused static geometry
  buffers can now upload only those dirty vertex/index/triangle ranges when the
  dirty ranges are valid; new or resized buffers still use full uploads. The
  committed scene-input package and `r_pathTracingSceneInputsDump 1` expose the
  same static dirty surface count, dirty vertex/index/triangle ranges, and
  whether a dirty-range upload was used. It is intended to validate stable range
  mapping without waiting for a validation failure.
- Persistent geometry records are for static or future semi-static geometry with
  stable identity. One-frame effects such as muzzle flashes, particles, sparks,
  and temporary overlays should stay out of this model unless given a deliberate
  separate identity/history design.
- The geometry universe must be designed from here onward to retain one previous
  frame of geometry/history data. DLSS Ray Reconstruction and similar temporal
  reconstruction paths need accurate motion vectors; camera-only motion is not
  sufficient for dynamic objects, animated/skinned meshes, moving rigid
  instances, reflected/refracted surfaces, or disocclusion handling.
- The long-term target is geometry-derived motion: for each persistent surface
  or triangle range, the RT path should be able to access current and previous
  world positions or equivalent current/previous local vertices plus
  current/previous transforms. This lets the shader project both positions and
  produce correct screen-space/depth motion instead of guessing from object
  velocity or frame-local draw order.
- This requirement should shape persistent record identity now. Surface records
  need stable identities, previous/current range mapping, transform history,
  invalidation/disocclusion flags, and enough per-vertex or per-triangle history
  to support static, rigid, and eventually skinned geometry. Avoid designs that
  only keep the current frame's packed buffers and throw away the previous
  geometry universe.
- The next useful geometry step after the range-history, dirty-extent dump,
  partial dirty static-buffer uploads, static previous-frame scene-input bridge,
  previous static CPU snapshot, and previous static GPU snapshot is stronger
  persistent GPU buffer ownership and stronger static/dynamic separation.
- Scene-input diagnostics now expose the dynamic fallback split next to the
  aggregate dynamic vertex/index/triangle count: rigid, CPU-current skinned,
  likely-base-pose skinned, RT CPU-skinned, particle, unknown, and retained
  occluder surface/triangle counts. This is diagnostic metadata only; it does
  not split the dynamic BLAS or change shader bindings. The classified total
  excludes retained occluders because retained occluders are also counted in
  their surface class; `match=1` means the class split accounts for the dynamic
  triangle payload.
- Scene-input upload diagnostics split geometry upload bytes into static,
  previous-static, dynamic, and rigid-route buckets next to the aggregate
  geometry/material/light totals. This helps verify that previous-static
  ownership and upload skipping are doing useful work while keeping the reserved
  previous-static shader bindings populated. The static previous bridge line
  also reports
  `prevGpuSkipBytes` so a skipped previous-static upload can be quantified.

Modes 13 and 14 are not Doom lighting. They are proofs that the RT path can
combine geometry, normals, alpha any-hit, material table data, bindless albedo,
a lighting equation, and now a single direct visibility query in debug
visualizations.


Known Blockers and Issues
-------------------------

Texture system integration is the main active blocker.

Current safe approach:

- Only use already available fast-path diffuse or explicit diffuse-stage images.
- Do not use editor image fallback for RT texture discovery.
- Do not force-load missing images during RT scene capture.
- Do not copy raster-owned textures into private RT textures until resource
  state ownership is better understood.
- Use Texture.Load for RT texture debug mode 8. Treat SampleLevel as unsafe near
  animated SWF/GUI terminals until the sampler/state interaction is understood.
- Skip custom-shader deferred callback render entities by default. A repeatable
  crash on disk pickup was fixed by r_pathTracingSkipCallbackEntities 1,
  strongly suggesting callback-driven item shell geometry can become invalid
  during pickup/delete transitions.

Open issues:

- Modes 8, 10, and 13 use the experimental bindless table, not a final global
  material texture system.
- Material stages are much richer than a single diffuse texture.
- Doom materials can have generated images, videos, blend stages, alpha tests,
  deform stages, lights, and special render behavior.
- Normal/specular texture discovery exists and is folded into mode 14 direct
  lighting, and mode 18 has an opt-out `r_pathTracingToyFakePBRSpecular` path
  that reuses rbdoom3's legacy specmap-to-PBR roughness/F0 approximation for a
  basic visual test. These paths are still diagnostic. The final renderer still
  needs a deliberate Doom material facts layer and PT/PBR interpretation,
  including packed RMAO detection, blend/decal/alpha semantics, roughness/F0
  storage, and denoiser-facing material data. A roughness buffer will likely
  matter for NVIDIA ray reconstruction/denoising work later.
- Alpha any-hit exists for simple cutout/perforated materials, but blended
  translucent surfaces are not handled physically yet.
- Smoke particle dithering is a visibility hack, not correct translucency.
- GUI/SWF panels are not solved. The direct generated-GUI-surface path is an
  opt-in diagnostic only and can occlude unrelated geometry or layer panels
  incorrectly. A rasterized offscreen panel texture projected onto the physical
  panel surface is the intended future direction.
- Mode 18 has a crude one-bounce toy path tracer with accumulation, selected
  point lights, texture sampling, and emissive visibility. It is deliberately
  simple and does not include MIS, denoising, ReSTIR, or emissive area-light
  sampling yet. `r_pathTracingToyFakePBRSpecular 1` enables a basic
  specmap-derived F0/roughness direct-specular test in this mode, but the bounce
  path remains diffuse-only. Mode 14 remains the selected point-light
  direct-shadow diagnostic path.
- Mode 18 previously brute-forced every selected point light for both the
  primary direct-light pass and the one-bounce direct-light pass. At
  r_pathTracingLightCount 32 this can mean up to 64 shadow rays per pixel,
  plus primary/bounce rays, and those shadow rays still execute any-hit
  alpha/material code. That ray workload made the portal-transition
  resource-lifetime bug much easier to hit by increasing in-flight GPU work,
  but it was not the root cause of the texture corruption/device-removal class.
  `r_pathTracingSceneRetireFrames` now defaults to 6 and keeps replaced scene
  packages alive; `r_pathTracingToyLightTraceCap` defaults to 8 and remains a
  useful toy-mode workload guard before constants/shader dispatch.
- Mode 13 is a fixed-light debug shader, not real Doom light integration.
- Performance is not optimized. CPU skinning and per-frame dynamic rebuilds are
  acceptable for now but not final.


Testing Workflow Used So Far
----------------------------

Preferred Windows PT build lanes now use CMake presets from neo/CMakePresets.json:

    cmake --preset win64-pt-retail
    cmake --build --preset win64-pt-retail-release

    cmake --preset win64-pt-dev
    cmake --build --preset win64-pt-dev-release

    cmake --preset win64-pt-dev-optick
    cmake --build --preset win64-pt-dev-optick-release

Human-friendly wrappers live next to the older cmake-vs2022*.bat files:

    neo/cmake-vs2022-win64-pt-retail.bat
    neo/cmake-vs2022-win64-pt-dev.bat
    neo/cmake-vs2022-win64-pt-dev-optick.bat
    neo/cmake-build-vs2022-win64-pt-retail-release.bat
    neo/cmake-build-vs2022-win64-pt-dev-release.bat
    neo/cmake-build-vs2022-win64-pt-dev-optick-release.bat

The presets generate separate build folders such as:

    build-win64-pt-retail
    build-win64-pt-dev
    build-win64-pt-dev-optick

Build/config notes from the GUI/video and raster-stall investigations:

- Debug can be configured either as FFMPEG=ON/BINKDEC=OFF or
  FFMPEG=OFF/BINKDEC=ON. The latter uses bundled libbinkdec, which can be slow
  in unoptimized Debug builds, but switching to FFmpeg did not change the
  observed monitor/area behavior after GuiModel.cpp was restored.
- RETAIL=ON only adds ID_RETAIL. It strips most non-shipping console commands,
  hides some dev UI, changes a few defaults, and enforces more shipping-like
  behavior. It does not imply Release/RelWithDebInfo optimization in the Visual
  Studio multi-config generator.
- RETAIL=ON was an early suspect because the first fresh clean RT build was
  retail-like, but it is not the proven performance switch. A fresh generic
  Release non-retail build also ran at the 120 FPS cap.
- PATH_TRACING_DEV adds ID_PATH_TRACING_DEV while keeping the stable PT Release
  build lane. Use this for PT-specific diagnostics, commands, and cvars without
  relying on Debug builds for performance testing.
- OPTICK=ON is available through win64-pt-dev-optick for profiling. Keep Optick
  off for final performance sanity checks unless the profile capture itself is
  the point of the test. The PT build/dispatch code keeps optional GPU marker
  scopes for diagnosis, but `r_pathTracingOptickGpuMarkers` defaults to 0
  because enabling those PT-specific native command-object marker calls caused
  level-entry crashes during the diagnostics cleanup pass.
- The legacy Visual Studio Debug compile path is broken for PT validation. It
  can be used only as a compile/link sanity check, and the generated project may
  be stale until `cmake -S neo -B build` is rerun after NVRHI source additions.
  Do not copy `build\Debug\RBDoom3BFG.exe` into the prepared game folder for
  normal PT testing. Use the preset Release/PT lanes instead. Debug executables
  are about 30 MB here, while Release executables are about 14-15 MB. That
  difference is expected from /Od, /Zi, /RTC1, debug CRT, and incremental debug
  linking; it is not a source-code size clue. The old Debug executable
  reproduced the broken frame-pacing/performance collapse.
- `r_useValidationLayers` defaults to 1, enabling NVRHI validation. Turning it
  off at startup restored raster Debug performance to the 120 FPS cap. Use
  validation for targeted resource/state/binding debugging, not performance
  comparisons.
- PT is still much slower in Debug/RelWithDebInfo than Release because the
  prototype does heavy CPU staging every frame: Doom surface capture, vertex
  transforms, material metadata/table work, emissive inventory, buffer upload
  setup, and dynamic BLAS/TLAS preparation. Debug CRT allocation checks,
  disabled/reduced inlining, `/Od`, `/RTC1`, and debug-friendly codegen make
  that path non-representative. RelWithDebInfo in this generated project is
  optimized but still uses `MultiThreadedDebugDLL` and weaker inlining than
  Release.
- Clean upstream/master from v1.6.0 through babd04b3 was bisected and tested
  clear with matching generated renderprogs2. Do not assume the PT/raster stall
  comes from upstream unless clean upstream starts reproducing again.

Do not use the legacy Debug copy path for normal PT testing. It is a broken
validation lane and is kept only as historical context:

    build/Debug/RBDoom3BFG.exe

Current Release testing should copy from the relevant preset folder, for example:

    build-win64-pt-dev/Release/RBDoom3BFG.exe

into a prepared Doom 3 BFG game folder. When shaders change, also copy:

    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke.rt.bin
    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke_mode18_restir_hybrid.rt.bin
    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke_restir_initial.rt.bin
    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke_restir_temporal.rt.bin
    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke_restir_temporal_shading.rt.bin
    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke_restir_attribution.rt.bin
    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke_restir_spatial_reservoir.rt.bin
    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke_restir_spatial.rt.bin
    base/renderprogs2/dxil/builtin/pathtracing/pathtrace_smoke_restir_spatial_attribution.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke_mode18_restir_hybrid.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke_restir_initial.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke_restir_temporal.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke_restir_temporal_shading.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke_restir_attribution.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke_restir_spatial_reservoir.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke_restir_spatial.rt.bin
    base/renderprogs2/spirv/builtin/pathtracing/pathtrace_smoke_restir_spatial_attribution.rt.bin

to the matching paths in the prepared game folder. The RT shader library is
loaded from `base/renderprogs2` at runtime; copying only the exe can leave a
new binary running an old raygen shader.

Typical runtime commands:

    r_pathTracing 1
    r_pathTracingDebugWidth 1280
    r_pathTracingDebugHeight 720
    r_pathTracingDebugMode 3

For visual testing of Doom analytic lights:

    r_pathTracingDebugMode 18
    r_pathTracingAnalyticLightCandidates 1
    r_pathTracingAnalyticLightReplaceSelected 1
    r_pathTracingAnalyticLightIntensityScale 4
    r_pathTracingAnalyticSphereLightRadiusScale 0.08

Then sweep r_pathTracingAnalyticLightIntensityScale through 0, 1, 4, and 5.
For radius validation, sweep r_pathTracingAnalyticSphereLightRadiusScale through
0.02, 0.08, and 0.16 while leaving min/max at their defaults unless testing
light-size clamps specifically.
With r_pathTracingAnalyticLightReplaceSelected enabled, the old selected
point-light array is suppressed so visible changes come from the analytic Doom
light buffer. Turn the replace CVar back to 0 to compare against the legacy
selected point-light path.

For texture probe testing:

    r_pathTracingDebugMode 8
    r_pathTracingTextureTableLimit 2048
    r_pathTracingTextureSampleMethod 2
    r_pathTracingTextureFilter 1
    r_pathTracingTextureDecode 1
    r_pathTracingTextureProbeDump 1
    r_pathTracingTextureProbeIndex 0
    r_pathTracingTextureProbeReset 1

For albedo and simple lighting:

    r_pathTracingDebugMode 10
    r_pathTracingTextureTableLimit 2048
    r_pathTracingTextureSampleMethod 2
    r_pathTracingTextureFilter 1
    r_pathTracingTextureDecode 1

    r_pathTracingDebugMode 13

For alpha and translucent inspection:

    r_pathTracingDebugMode 9
    r_pathTracingAlphaDump 1

    r_pathTracingDebugMode 12
    r_pathTracingTranslucentDump 1

For crosshair material diagnostics:

    r_pathTracingCrosshairMaterialDump 1

For GUI/SWF panel diagnostics:

    r_pathTracingAllowGuiSurfaces 1
    r_pathTracingAllowGuiTextures 1
    r_pathTracingGuiDump 1

For nearest-light shadow inspection:

    r_pathTracingDebugMode 14
    r_pathTracingLightCount 4
    r_pathTracingLightSelection 1
    r_pathTracingLightDump 1
    r_pathTracingUseNormalMaps 1
    r_pathTracingUseSpecularMaps 1
    r_pathTracingUseEmissiveMaps 1

For selected-light influence inspection:

    r_pathTracingDebugMode 15
    r_pathTracingLightCount 4
    r_pathTracingLightSelection 1
    r_pathTracingLightDump 1

For clean RTXDI DI spatial reuse tuning:

    r_pathTracingCleanRtxdiDiEnable 1
    r_pathTracingCleanRtxdiDiView 16
    r_pathTracingCleanRtxdiDiTemporal 1
    r_pathTracingCleanRtxdiDiSpatial 1
    r_cleanDiSpatial 1
    r_cleanSpatial 1
    r_cleanDiSpatialSamples 1
    r_cleanDiSpatialDisocclusionSamples 4
    r_cleanDiSpatialRadius 30
    r_pathTracingCleanRtxdiDiTemporalFireflyClamp 32

Disable any one of `r_pathTracingCleanRtxdiDiSpatial`, `r_cleanDiSpatial`, or
`r_cleanSpatial` to remove the spatial reuse step. The sample/radius values
used by the shader come from the `r_cleanDi*` aliases. For temporal-only or
low-spatial-sample testing, `r_pathTracingCleanRtxdiDiTemporalFireflyClamp`
rejects older previous-frame reservoirs whose `targetPdf * W` contribution
scale is an extreme outlier against the current selected reservoir; set it to
`0` for the old behavior.

For abusive selected-light scaling tests:

    r_pathTracingDebugMode 14
    r_pathTracingLightSelection 1
    r_pathTracingLightCount 1
    r_pathTracingLightCount 4
    r_pathTracingLightCount 8
    r_pathTracingLightCount 16
    r_pathTracingLightCount 32

For smoke particle placeholder testing:

    r_pathTracingSmokeParticleDither 1
    r_pathTracingSmokeParticleEdgeFade 1
    r_pathTracingSmokeParticleAlphaScale 0.25

To disable the smoke-card placeholder hack:

    r_pathTracingSmokeParticleDither 0

For portal/window glass placeholder testing:

    r_pathTracingPortalWindowStochastic 1
    r_pathTracingPortalWindowAlphaScale 0.35
    r_pathTracingPortalWindowMinOpacity 0.05
    r_pathTracingPortalWindowShadowOpacity 0.05

For the current toy path tracing baseline:

    r_pathTracingDebugMode 18
    r_pathTracingSkipRaster3D 1
    r_forceAmbient 0.0
    r_pathTracingLightCount 4
    r_pathTracingToyLightScale 0.3
    r_pathTracingToyEmissiveScale 4.0
    r_pathTracingToyAccumulation 1
    r_pathTracingToyAccumMaxFrames 64

To opt out of the current fake-PBR specular material approximation:

    r_pathTracingDebugMode 18
    r_pathTracingToyFakePBRSpecular 0

For emissive candidate inspection:

    r_pathTracingDebugMode 19
    r_pathTracingEmissiveInventoryDump 1

For source3 drawSurf mirror equivalence diagnostics:

    r_pathTracingSceneSource 3
    r_pathTracingSceneSourceCompare 1
    r_pathTracingInstanceUniverseDump 1

For rigid mesh candidate/local-source diagnostics:

    r_pathTracingSceneSource 3
    r_pathTracingRigidMeshUniverseDump 1
    r_pathTracingRigidMeshValidate 1
    r_pathTracingRigidBlasPlanDump 1
    r_pathTracingRigidBlasInputDump 1

For rigid BLAS GPU scaffold diagnostics:

    r_pathTracingSceneSource 3
    r_pathTracingRigidBlasGpuScaffold 1
    r_pathTracingRigidBlasGpuBuild 0
    r_pathTracingRigidBlasGpuDump 1

For standalone rigid BLAS build submission diagnostics:

    r_pathTracingRigidBlasGpuBuild 1
    r_pathTracingRigidBlasGpuDump 1

This keeps renderPath=dynamicFallback only if the rigid TLAS route gate is
disabled.

For rigid TLAS instance planning without changing the render path:

    r_pathTracingSceneSource 3
    r_pathTracingRigidBlasGpuScaffold 1
    r_pathTracingRigidBlasGpuBuild 1
    r_pathTracingRigidTlasPlanDump 1

This reports what visible/resident rigid instances feed TLAS. Disable
`r_pathTracingRigidTlasRoute` or the per-mode route gate first if the goal is a
plan dump without routing.

For routed rigid TLAS transform validation:

    r_pathTracingDebugMode 23
    r_pathTracingSceneSource 3
    r_pathTracingRigidBlasGpuScaffold 1
    r_pathTracingRigidBlasGpuBuild 1
    r_pathTracingRigidTlasRoute 1
    r_pathTracingRigidTlasPlanDump 1

Mode 23 traces only routed rigid TLAS instances using a dedicated ray mask and
the packed rigid route metadata buffers. It is meant to validate
transforms/instance presence before dynamic-fallback removal.

For routed-rigid overlap validation:

    r_pathTracingDebugMode 24
    r_pathTracingSceneSource 3
    r_pathTracingRigidBlasGpuScaffold 1
    r_pathTracingRigidBlasGpuBuild 1
    r_pathTracingRigidTlasRoute 1
    r_pathTracingRigidTlasPlanDump 1
    r_pathTracingRigidRouteOverlapDump 1

To keep the dynamic fallback copy for overlap/emergency comparison, set:

    r_pathTracingRigidRouteRemoveDynamic 0

Mode 24 can trace fallback-only mask `0x01` and routed-rigid-only mask `0x02`
separately and visualize the comparison:

    green = routed rigid hit matches fallback hit distance and material/class
    yellow = matched hit distance but material id/index differs
    magenta = matched hit distance but surface class differs
    cyan = routed rigid hit, no fallback hit
    blue = routed rigid hit in front of fallback hit
    orange = fallback hit in front of routed rigid hit
    dim gray = fallback hit only
    black = neither path hit

This mode is diagnostic. It should not be used as the normal mode 18/20 render
path.

With default `r_pathTracingRigidRouteRemoveDynamic 1`, successfully removed
routed rigid surfaces should appear cyan in mode 24 because the fallback copy is
no longer present. Orange should remain possible where remaining fallback
geometry occludes the routed rigid path. Yellow or magenta pixels identify
same-depth fallback/routed coverage that disagrees on material identity or
surface class, which is a targeted duplicate/z-fight diagnostic. Set the
removal gate to 0 to restore the old overlap comparison, now with the
additional mismatch buckets.
`r_pathTracingRigidRouteOverlapDump 1` prints both full-frame proportions and a
33x33 center-region summary plus the center-pixel bucket, so aim the crosshair
at a suspect object before triggering the dump.

For routed-rigid lighting validation:

    r_pathTracingDebugMode 25
    r_pathTracingSceneSource 3
    r_pathTracingRigidBlasGpuScaffold 1
    r_pathTracingRigidBlasGpuBuild 1
    r_pathTracingRigidTlasRoute 1
    r_pathTracingRigidRouteRemoveDynamic 1
    r_pathTracingRigidTlasPlanDump 1

Mode 25 traces the combined scene with mask `0xff`, removes routed-ready rigid
surfaces from the dynamic fallback when the removal gate is enabled, and shades
with simple material debug albedo plus world-space normal lighting. Routed rigid
hits receive a slight cyan tint so they can be distinguished from fallback
geometry. This is still diagnostic and should not be treated as mode 18/20
integration.

For mode 18 routed-rigid integration:

    r_pathTracingDebugMode 18
    r_pathTracingRigidTlasPlanDump 1

This keeps the mode 18 toy path tracing shader path, appends routed rigid TLAS
instances, and removes routed-ready rigid surfaces from the dynamic fallback.
It also adds routed rigid material IDs to the frame material table before route
buffer construction so removed dynamic surfaces do not drop their materials.
This stack is default-on; the command above only requests a plan dump.

For mode 20 routed-rigid integration:

    r_pathTracingDebugMode 20
    r_pathTracingRigidTlasPlanDump 1

This keeps mode 20's single-frame reservoir direct-lighting path, appends
routed rigid TLAS instances, removes routed-ready rigid surfaces from the
dynamic fallback, and adds routed rigid material IDs to the frame material
table. It also appends CPU emissive triangle candidates for routed rigid
instances from their local mesh data transformed by the current object-to-world
matrix, so removed dynamic fallback copies do not disappear from mode 20's
light candidate list. The reservoir algorithm remains unchanged. This stack is
default-on; the command above only requests a plan dump.

For ReSTIR PT initial-sampling reservoir validation:

    r_pathTracingDebugMode 26
    r_pathTracingDebugMode 27
    r_pathTracingDebugMode 28
    r_pathTracingDebugMode 29
    r_pathTracingDebugMode 30
    r_pathTracingDebugMode 31
    r_pathTracingDebugMode 32
    r_pathTracingDebugMode 33
    r_pathTracingDebugMode 50
    r_pathTracingDebugMode 51

The host side now builds a CPU ReSTIR PT pass plan for modes 26-33 and 50-51 before the
legacy smoke raygen dispatch. The plan names the active initial, temporal,
spatial, shading, visibility, primary-surface, or source-attribution stage, chooses the
local RTXDI PT parameter resampling mode, and records the RTXDI reservoir buffer
indices that the current shader path will use. This is a pass-split scaffold,
not a production pass split: modes 26-33 and 50-51 still run through the existing debug
raygen paths. Use `r_pathTracingRestirPTPassDump 1` for a one-shot console dump
of the current mode's pass label, flags, resampling mode, and reservoir buffer
selection.

For primary object-motion vector validation:

    r_pathTracingDebugMode 38
    r_pathTracingDebugMode 39
    r_pathTracingDebugMode 40
    r_pathTracingDebugMode 41
    r_pathTracingDebugMode 42
    r_pathTracingDebugMode 43
    r_pathTracingDebugMode 44
    r_pathTracingDebugMode 45
    r_pathTracingDebugMode 46
    r_pathTracingDebugMode 47
    r_pathTracingDebugMode 48
    r_pathTracingDebugMode 49

Mode 26 is a gated RTXDI ReSTIR PT initial-sampling debug path. It traces the
normal primary ray, converts supported current hits into RAB surfaces, calls
RTXDI `GenerateInitialSamples`, and stores the returned `RTXDI_PTReservoir` into
the initial path-tracer output slice from `RTXDI_PTParameters`. The local
`RAB_PathTrace` bridge now records one conservative NEE sample from the current
RAB light list per visited path vertex before continuing the BRDF path, because
the original BRDF-only probe was too sparse to show visible reservoirs in
normal Doom scenes. The NEE picker uses the existing weighted emissive-triangle
distribution and uniform analytic-light proposals, then streams the candidates
through a small local RIS selector. It no longer keeps the highest-scoring
analytic trial while recording the raw uniform draw PDF; the RIS estimator is
folded into `radianceOverPdf` with a matching scalar effective `neePdf` for the
path-tracer metadata. Doom analytic RAB samples reject points outside
`doomRadiusAndArea.x` and apply the same quadratic influence falloff as mode 18
native analytic NEE. A single uniform pick from the full light list was too
weak for validation. This is not a
roughness/PBR problem: mode 26 is currently diffuse-only with roughness 1.0,
which is valid enough for this reservoir visibility check. Misses, unsupported
translucent/special surfaces, and invalid hits store empty reservoirs. The
visible output is a sparse heat/debug view for selected useful reservoirs; it
is not final ReSTIR PT shading.
Temporal reuse, spatial reuse, previous-frame surface history, environment
sampling, and dispatched ReSTIR PT resampling passes are still off. Modes 18
and 20 remain the stable toy PT and single-frame direct reservoir paths.

Mode 26 must use the same source3 scene membership baseline as the playable PT
diagnostic modes. It now participates in rigid TLAS routing, rigid residency,
and routed rigid emissive-card inventory; otherwise its ReSTIR PT validation can
silently test against a culled/fallback scene that differs from modes 18/20.

Temporary mode 26 diagnostic fallback colors:

- Orange: local RAB light target exists, but RTXDI initial sampling did not
  produce a useful reservoir.
- Blue: valid RAB light samples exist, but their target contribution for the
  current surface is zero.
- Red: sampled light records were invalid.
- Purple: no light candidates reached the shader.

If mode 26 is mostly blue, inspect the RAB surface hemisphere contract before
chasing light upload again. A blue-only test showed valid light samples were
visible to the shader but the per-surface target was zero. The current RAB
surface builder orients its geometry and shading normals toward the current
ray hit before BRDF evaluation so Doom/idTech winding or normal-map orientation
does not incorrectly fail the camera/view hemisphere test.

Post-fix validation from user testing: mode 26 changed from mostly blue to
mostly green with noisy white signal on emissive panels and only some remaining
blue in areas likely affected by strong normal maps. In the current temporary
debug view, that means RTXDI initial sampling is producing useful reservoirs
across normal surfaces, high target/radiance appears near emissives, and the
remaining blue is localized zero-target evaluation rather than missing scene or
light records.

Mode 27 keeps the same initial-sampling reservoir write path as mode 26 but
removes the diagnostic color key and previews initial reservoir shading using
`max(TargetFunction, 0) * max(WeightSum, 0)`, tonemapped into the smoke output.
Invalid pixels fall back to dim albedo or black misses. This is an
initial-only reservoir shading preview, not temporal/spatial ReSTIR PT. It was
added before temporal resampling because the real Step 11 prerequisites,
especially previous primary-surface data and motion vectors, are not exposed to
the smoke shader yet.

User validation: mode 27 works fairly well and runs well performance-wise, with
no serious stability or gameplay issues reported. The remaining visible issues
are rough normal handling and a few fully black objects. Treat those as localized
RAB material/normal/unsupported-surface cases, not as evidence that initial
reservoir generation or the source3 scene/light membership is failing.
Follow-up normal investigation found two likely Doom-specific causes: the green
normal-map channel convention may need inversion for legacy assets, and original
Doom 3 content was baked against idTech4's custom tangent basis rather than
MikkTSpace. `r_pathTracingNormalMapFlipGreen` was added as a diagnostic toggle
for the first issue. The second issue needs a later geometry/material payload
change so PT can use the engine's original tangent and bitangent sign instead
of reconstructing TBN from triangle UV gradients in the hit shader. Mode 27 RAB
surface setup also now constrains decoded shading normals to the oriented
geometric-normal hemisphere before BRDF evaluation, so an over-tilted or
wrong-convention normal map should not turn a reservoir-lit surface fully black.

Mode 28 is the paired initial-reservoir visibility preview. It keeps mode 27's
initial reservoir generation and shading preview, then multiplies the preview
contribution by one conservative visibility ray from the current surface to the
selected reservoir target. For NEE reservoirs it decodes the packed RTXDI light
reference, resamples the current RAB light, respects non-shadow-casting Doom
analytic lights, and ignores the selected static emissive triangle for direct
emissive visibility rays so the light does not immediately shadow itself. For
non-NEE reservoirs it falls back to the reservoir translated world position.
This mode is intentionally a validation bridge for reservoir visibility and
shadowing; it does not enable temporal reuse, spatial reuse, final RTXDI shading
passes, or full path replay.
User validation: mode 28 showed no gameplay/stability issues. The visible
shadows are expected to be 100% sharp in this mode because the preview uses one
binary visibility ray to the selected reservoir sample; soft penumbrae require
many light samples over time/spatial reuse and later final shading/denoising.

Mode 29 is the primary-surface history validation view. The RT shader now writes
an output-sized current primary-surface record for each raygen pixel and the
backend copies current to previous after dispatch so the next frame can read
stable previous records. The versioned PathTracePrimarySurface record stores
world position/depth, geometry and shading normals, view direction, material
identity, surface flags/class, diffuse albedo, opacity, alpha cutoff,
roughness/F0, emissive radiance, instance/primitive IDs, an object/entity ID
placeholder, a height/parallax/displacement placeholder, and a future
previous-position/object-motion placeholder. When
`r_pathTracingToyFakePBRSpecular 1` is enabled, modes 26/27/28/29/30/31/32/33/50
sample the Doom specular texture and store the converted fake-PBR roughness/F0
in that record; modes 34/35/36/37 use the same material policy for path-tracer
core visualizers. Mode 29 colors the current-vs-previous same-pixel comparison:
blue means current valid but no previous history yet, red means previous valid
but current invalid, yellow means material mismatch, purple means normal
mismatch, orange means roughness mismatch, and green means a conservative
material/normal/roughness match. Reprojection rejection and no-object-motion
status constants are reserved for later object-motion validation; they are not
part of the normal mode 29 success path. This is still same-pixel validation
with no motion vectors or reprojection, so camera motion is expected to create
mismatches. A previous test build produced a full-screen blue result during
camera motion because the primary-surface history clear was tied to
`m_smokeReservoirNeedsClear`, whose dispatch signature changes with camera
origin/direction/FOV. That was a history-lifetime bug, not a real reprojection
diagnostic: primary history now has its own clear flag and is only cleared on
hard history resets such as output/history resource replacement.
Post-fix user validation: stationary views return the expected green-tinted
history match, and moving-camera views no longer collapse to full-screen blue.
Remaining purple normal mismatches and orange roughness mismatches during
motion are expected same-pixel history artifacts until motion vectors and
reprojection are implemented.

Mode 30 is the temporary screen-space reprojection validation view for ReSTIR PT
history. The backend stores the camera basis/tangents that produced the
previous primary-surface history buffer. The shader projects each current
primary hit into that previous camera, samples `PrimarySurfaceHistoryPrevious`
at the resulting previous pixel, and then uses the same material/normal/
roughness color key as mode 29. Static hits still use current-world-position
camera reprojection. The shared reprojection helper now repacks the current
primary surface and consumes the stored `previousPositionOrMotion` plus
object-motion/previous-position flags. Skinned hits with validated previous
skinned positions and routed rigid hits with continuous previous transforms
therefore project the packed previous world position; if an eligible dynamic
hit lacks required previous object state, the mode reports the skinned/rigid
invalid reason instead of hiding it behind camera-only reprojection. This
should turn many static-world camera-motion mismatches back toward green while
also validating the first real object-motion reprojection path. It is still not
a final motion-vector solution: particles, translucency, disocclusions,
alternate motion, and denoiser export remain later tasks.
User validation: mode 30 is mostly green during camera motion, which confirms
that static-world current-position to previous-camera reprojection is finding
the previous primary history in normal gameplay views. The remaining purple and
orange outlines around moving objects and silhouettes look like an edge-detect
overlay; that is expected for this temporary screen-space method because moving
objects, disocclusions, and subpixel edge changes do not have vertex/object
motion yet. A later visual check noticed that one debug color in this mode had
swapped red/blue compared with an earlier expectation; the user confirmed the
mode still performs its validation role, so treat this as visualizer color drift
rather than a functional regression unless a future cleanup explicitly
normalizes the palette.

Mode 31 is the first temporal-reservoir diagnostic. It switches the RTXDI
ReSTIR PT context to temporal mode for that debug view so the packed reservoir
buffer indices ping-pong through `initialPathTracerOutputBufferIndex`,
`temporalResamplingInputBufferIndex`, and `temporalResamplingOutputBufferIndex`.
The shader still runs inside the existing raygen path: it writes the current
initial reservoir, uses the mode 30 previous-pixel reprojection as the temporal
candidate, rejects missing or mismatched previous surfaces before RTXDI
resampling, then calls `RTXDI_PTTemporalResampling` and writes the temporal
output reservoir. Because mode 30 now uses previous object positions for
validated skinned and routed rigid hits, mode 31's motion vector input uses that
object-aware previous pixel when available. The color key remains diagnostic
only: green means a valid temporal reservoir path, blue means no usable previous
surface/reservoir, yellow/purple/orange are the material/normal/roughness
rejection colors from mode 29/30, skinned/rigid missing/range colors mean object
motion was required but unavailable, and red means invalid current or temporal
output. Mode 31 does not feed final shading or enable spatial reuse.
The frame-resource diagnostics report `primaryState ... objectMotion=1` only
when scene inputs contain a concrete object-motion source: skinned previous
position dispatch ranges or routed rigid previous transforms.
User validation: mode 31 looks broadly similar to mode 30 in normal motion.
Violent camera movement can produce blue sections around screen edges until
history catches up. That is expected because those pixels reproject offscreen or
into disoccluded areas and therefore have no usable previous primary surface or
reservoir yet.

Mode 32 is the rough temporal-reservoir lighting preview. It uses the same
temporal context and conservative previous-surface gates as mode 31, but instead
of visualizing rejection state it shades the temporal output reservoir using
`TargetFunction * WeightSum` and a preview-only exposure control,
`r_pathTracingRestirPTPreviewExposure`. Invalid reservoirs now fall back to
near-black material context instead of the strong mode 31 rejection colors, so
normal camera motion does not turn the preview into a full-screen diagnostic
overlay. When the RTXDI estimate is effectively black but the reservoir carries
a packed NEE light, mode 32 reconstructs that selected light sample for preview
only; this proves candidate/source selection and is not the final estimator.
Use mode 31 for temporal rejection diagnosis and mode 33 for source
attribution. The extra conservative visibility ray used by mode 28 is behind
`r_pathTracingRestirPTPreviewVisibility`; keep it at 0 for normal mode 32
testing and enable it only for A/B visibility checks. The current initial
reservoir is generated and stored before temporal resampling, then the temporal
output reservoir is written to `temporalResamplingOutputBufferIndex` and
previewed. This is still a rough preview path: it does not feed production final
shading, does not enable spatial reuse, and still inherits mode 30/31
edge/disocclusion limits until stricter consumer-side rejection and full
object/vertex motion are in place.

Important bridge convention: NVIDIA's ReSTIR PT path context starts the primary
surface at `bounceDepth = 2`. The local bridge must therefore keep at least that
primary NEE vertex active even when the rbdoom PT integrator has zero or one
secondary diffuse bounces configured. If this cap is wrong, modes 26/27/32 show
only fallback albedo/ambient context because the initial-sampling path never
records a reservoir sample.

Mode 32 temporal resampling uses the combined geometry motion helper for its
runtime motion vector, including sub-pixel static/skinned/routed-rigid
current-to-previous pixel motion. The previous integer previous-pixel delta was
adequate for coarse validation but can make routed-rigid edges flicker during
camera movement.

Mode 32 also avoids an earlier over-strict wrapper-side rejection. RTXDI's PT
temporal pass searches a small previous-frame neighborhood around the projected
pixel; requiring the exact previous pixel to contain a matching surface and
valid reservoir before calling RTXDI caused edge flicker on static, skinned, and
routed-rigid objects. The wrapper now provides motion/depth hints and lets
RTXDI_PTTemporalResampling own previous-neighbor validation.

The remaining mode 32 object/side-specific flicker is most likely temporal
neighbor validation sensitivity rather than source coverage when mode 49 stays
stable. Mode 52 verifies routed-rigid transform/TLAS parity; if mode 52 is
stable but mode 32 or 50 shows exact one-frame alternating polygon flashes, set
`r_pathTracingRestirPTTemporalReservoirReuse 0`. If the flicker disappears, the
culprit is previous-reservoir ping-pong/reuse, not current-frame routed
geometry or current initial reservoir generation. Restore it to 1 for normal
ReSTIR testing. To isolate the old branch-local mitigation, set
`r_pathTracingRestirPTTemporalFallbackSampling 0`; the normal default is now 1.
That off state prevents RTXDI's zero-motion fallback sample from importing a
weakly validated same-material previous reservoir. If fallback sampling has no
effect, the next critical validation detail is the temporal expected-depth hint:
mode 32/50 now
derives that depth from the projected previous world position's ray distance
instead of reading the previous surface at the projected pixel. It must use ray
distance, not previous-camera forward-Z, because the smoke primary surface
stores `linearDepth` as ray `hitT`; using forward-Z creates a centered circular
region where temporal validation appears to work and rejects toward the edges.
Reading the previous surface first can let a wrong same-material triangle
validate itself by supplying its own depth. Modes 31-33 and 50-51 expose the RTXDI temporal/spatial depth/normal thresholds through
`r_pathTracingRestirPTTemporalDepthThreshold` and
`r_pathTracingRestirPTTemporalNormalThreshold`; use
`r_pathTracingRestirPTPassDump 1` to print the active values.

Stationary-camera flicker from animated or flickering lights is a separate
reservoir-validity problem, not a motion-vector problem. The ReSTIR PT packed
reservoir history clears when the smoke reservoir scene signature changes, and
that signature includes Doom analytic light topology/identity fields. It does
not hash analytic light color/intensity, because that would globally wipe
history for unrelated rooms whenever any flickering light animates. Instead,
mode 32/50 debug previews re-evaluate NEE reservoirs against current light data
before display, so stale bright-phase target energy is not trusted for visible
preview contribution. A later production path still needs per-light
radiance-version validation/remapping rather than this preview-side mitigation.
The RAB light loader treats `previousFrame=true` as an inverse-shift request:
RTXDI PT carries the current reservoir light index, so the bridge first remaps
that current packed analytic index through the Doom analytic universe to the
previous packed analytic index, then reads the previous analytic light table.
Failed remaps return an empty light rather than falling through to compact-index
reuse.
The temporal wrapper also pre-scans the reprojected 3x3 previous-reservoir
neighborhood. If a previous NEE reservoir cannot translate through the current
Doom analytic remap, including the light-state compatibility guard, the wrapper
sets temporal max age to zero before RTXDI combines the neighbor. This prevents
zero-target failed NEE replays from still influencing temporal reservoir
bookkeeping.

Validated robot/strobe fix, 2026-05-14: the bug was not fixed by stable light
identity/remap alone, nor by current/previous light color-state checks inside
`RAB_LoadLightInfo` alone. The visible failure was stale or invalid previous NEE
reservoir history surviving far enough into RTXDI temporal combine/bias
correction to affect the selected reservoir or its normalization. The final
effective mitigation was the pre-combine 3x3 previous-reservoir scan described
above. With that scan active, the user-confirmed symptom was fixed: stationary
lights no longer turn off or flicker until camera/player movement, and the
robot/strobe animation no longer inexplicably "repairs" nearby lights only while
active. Treat this as the first validated path for manual analytic NEE temporal
reuse testing.

Previous-frame NEE light reservoirs are enabled by default through
`r_pathTracingRestirPTTemporalAnalyticNeeReuse 1`. The CVar name is historical.
Task 02 now uploads shader-visible Doom analytic current/previous identity
buffers, previous analytic light records, and the universe remap needed by RAB
inverse-shift and previous-frame load callbacks. The robot-train/strobe sequence
proved that the current-frame light path was sound and that stale previous NEE
reservoirs were the failure source: lights in the same portal could work while
the strobe animation was active, then break and flicker as soon as that source
stopped in a small offscreen hole. The validated fix is to keep NEE reuse
enabled while rejecting previous NEE reservoirs that cannot translate through the
current Doom analytic remap or fail the light-state compatibility guard. Set
`r_pathTracingRestirPTTemporalAnalyticNeeReuse 0` only to isolate current-frame
NEE sampling from previous light history.

Mode 32 is also currently very expensive. User validation reports it works at
roughly 1280x720 but going much beyond that can device-remove the GPU. Treat
this as a TDR/workload-pressure limit of the current debug implementation, not
as evidence that the ReSTIR PT data path is unusable. The debug path runs a
full-screen raygen that builds the current initial PT reservoir and then invokes
RTXDI temporal resampling/replay work per pixel; full-resolution presentation is
expected to rely on later denoising/upscaling rather than brute-force full-res
mode 32/33/50/51. `r_pathTracingRestirPTPreviewMaxPixels` exists as an experimental
safety cap for the temporal preview family, but the practical validated guidance
is still to keep mode 32/33/50/51 at or below 720p for now.

Mode 33 is the temporal light-source attribution diagnostic. It uses the same
temporal reservoir generation path as mode 32, but colors the selected temporal
reservoir by the source that supplied the NEE sample: cyan/blue means a Doom
analytic sphere light, orange means an emissive triangle, green means a non-NEE
or indirect reservoir sample, and red means an invalid/unknown packed light.
The normal mode 30/31 rejection colors still apply before a valid temporal
reservoir exists. Modes 26 through 33 and modes 50-51 use
`r_pathTracingRestirPTAnalyticLightCandidates 1` as a debug convenience path,
while `r_pathTracingAnalyticLightCandidates` itself now also defaults to 1 so
native PT and ReSTIR PT share the Doom analytic-light universe by default. Set
`r_pathTracingAnalyticLightCandidates 0` and
`r_pathTracingRestirPTAnalyticLightCandidates 0` to test emissive-only ReSTIR
behavior. The same ReSTIR mode range also uses the routed rigid scene path and
routed rigid emissive-card inventory, so temporal and spatial diagnostics see
the same source3 membership baseline as the earlier initial-reservoir views.
ReSTIR PT analytic NEE proposals now sample the full uploaded Doom analytic
candidate domain uniformly. Do not reintroduce a compact-list "local first 32"
proposal bias: the compact list is sorted by portal depth/distance for upload
stability, so that shortcut causes hard doorway discontinuities where lights
suddenly flood mode 33 after a tiny camera-area change. A future local proposal
path needs a real surface/light spatial structure or portal-visible light set,
not compact array order.

Mode 50 is a rough DI/direct reservoir preview. Older shader names and wrapper
defines still say temporal/spatial, but DI/direct should currently be treated as
raw initial-reservoir output. It is not a completed temporal/spatial ReSTIR DI
path.

Modes 50-51 may still run legacy-named producer and consumer dispatches. Those
labels are diagnostic scaffolding, not evidence of finished DI reservoir reuse.
Older notes described this around RTXDI_PTSpatialResampling, current-frame
neighbor surfaces, and a one-dispatch initial->temporal->spatial debug path.
Treat that as historical scaffolding only; DI/direct remains raw reservoir
output until explicitly rebuilt from the accepted GI path.

The older staged direct-lighting/domain notes are provisional and should not be
read as a completed DI implementation. The current accepted direction is to
finish the GI reservoir path first, then duplicate/adapt that finished path for
DI/direct rather than treating the existing direct labels as done.

Mode 18's opt-in direct-lighting hook is diagnostic only. Do not document it as
consuming a completed DI reservoir path; DI/direct remains raw reservoir output
until a future slice explicitly ports the accepted GI reservoir flow.

Mode 51 is a direct/DI source-attribution diagnostic over the current raw
reservoir output. Do not use it as evidence that direct temporal/spatial
resampling is finished. Set
`r_pathTracingRestirPTSpatialDiagnosticView 1` while staying in mode 51 to
replace source colors with spatial acceptance/source-change colors: blue means
no spatial neighbor contribution was detected, green means spatial neighbor
contribution was detected but the selected packed source stayed the same, and
cyan/orange/magenta/yellow identify spatial output that changed source type. Set
`r_pathTracingRestirPTSpatialDiagnosticView 2` to keep the temporal-vs-spatial
source comparison but mark changed Doom analytic/emissive NEE sources yellow
when the selected spatial source cannot reconstruct a usable local rough-preview
contribution at the current surface. That view is intended to catch source
identity that looks plausible in attribution but does not agree with mode 50
spatial shading near hard depth/object edges.

Modes 53-55 are the first indirect/GI reservoir diagnostics. They reuse the
initial RTXDI PT reservoir buffer and initial ReSTIR shader library, and now run
as staged initial-GI passes: a producer dispatch writes completed indirect
reservoirs, then the visible debug mode consumes those reservoirs. The RAB path
tracer skips primary-surface NEE in these modes so the first visible candidate
domain is secondary/indirect: secondary NEE, emissive hits, or non-NEE path
radiance. Mode 53 shows indirect reservoir metadata/heat, mode 54 shades the
rough indirect reservoir estimate, and mode 55 colors source/path attribution.
These modes are debug-only and initial-reservoir-only; do not treat them as
production GI, temporal GI reuse, spatial GI reuse, or mode 18 GI integration.

Modes 53-55 also force one secondary bridge bounce and raise the RTXDI initial
path-length budget to 4 inside the debug path. RTXDI starts the primary surface
at bounceDepth 2 and records secondary-surface NEE as pathLength 4; keeping the
default initial max of 3 makes indirect analytic NEE disappear while emissive
secondary hits can still appear. The override is scoped to the GI debug modes.

Mode 56 now defaults to DI debug view `72`, the active RRX DI final-consumer
proof path. It consumes the RRX DI temporal reservoir through the Remix-manager
RAB light records and does not mix the old spatial-direct, GI preview,
reflection preview, or combined-preview fallback into the result. Set
`r_pathTracingRestirPTDiDebugView 0` only for legacy mode-56 view-0 comparison.
The 01C GI temporal candidate exists only in
`r_pathTracingRestirPTGiDebugView 2`; it does not feed normal mode 56 or RR input
yet. The mode-56 primary-prepass route now forces motion-vector export because
the DI/GI temporal debug reservoirs use
the primary-surface producer's motion-vector mask, xy motion, and reset mask for
previous-pixel lookup. Same-pixel DI history fallback is restricted to valid
near-zero exported motion, plus a debug-scoped ordinary-rigid fallback when the
only reset reason is missing object-motion data and the previous same-pixel
surface still matches. This follows the RTXDI PT motion-vector direction, but
the local `u39` texture still stores xy motion only; the reference also provides
a depth delta for temporal depth validation.

`r_pathTracingRestirPTRaySparsity` is a legacy direct-domain sparse update
control over raw DI/direct diagnostics. Do not use it as evidence that DI has
completed temporal/spatial reservoir reuse.

`r_pathTracingRestirPTGiRaySparsity` is the default-off sparse update control
for staged GI initial reservoirs. Values 2-8 update roughly one out of N
full-resolution GI reservoir pixels in modes 53-56, skip inactive producer
pixels before ray tracing, and make the visible GI modes read the nearest active
GI representative while keeping their current full-resolution surface context.

User validation reported that mode 56 works visually, and sparsity visibly
changes the image, but sparsity does not meaningfully improve performance. Treat
the next performance pass as GPU shader/location profiling: in bad locations
below 60 FPS, Nsight shows `vkCmdTraceRays` dominating frame time while RT core
use is only roughly 5-10%. This points away from raw traversal ray count and
toward shader work inside raygen: environment/sky paths, material/light
sampling, memory/cache behavior, or branch divergence. User validation also
found dark base-game areas with apparently open roofs that raster lighting keeps
hidden from skybox light; mode 56 GI can over-brighten those areas until the
Doom sky/environment visibility or light-mask contract is mapped.

Mode 56 can emit legacy-labeled `vkCmdTraceRays` events such as GI initial
producer, direct temporal producer, direct spatial producer, and final combined
consumer. The direct labels are scaffolding, not proof of completed DI reuse.
Do not prioritize DI temporal/spatial tuning until DI has been rebuilt from the
accepted GI reservoir path.

Mode 38 is the skinned primary object-motion vector diagnostic. It uses the
same primary hit data as mode 30, but only visualizes skinned surfaces with
validated retained previous positions. Valid skinned hits encode
current-to-previous pixel displacement as signed direction in red/green around
0.5 and magnitude in blue, capped at 32 internal PT pixels. Static surfaces,
routed rigid surfaces, and dynamic surfaces without required previous skinned
state use the existing no-object-motion or skinned invalid colors. Rigid object
motion is intentionally not visualized here because routed-rigid coverage can be
view-dependent and needs a separate validation pass. This mode does not export a
final motion-vector buffer. Use `r_pathTracingGpuSkinning 1` when validating
skinned previous-position data without changing BLAS input. Mode 2 is the
opt-in path that lets the PT-owned compute output feed skinned dynamic BLAS
spans for visual validation.

Mode 39 is the routed-rigid object-motion eligibility diagnostic. It does not
draw vectors. It isolates rigid-class primary hits and reports whether the
surface has a routed rigid instance with a continuous previous transform:
green means the rigid object-motion source is valid for the current hit,
cyan means the hit is rigid-class but still using the non-routed fallback path,
and the existing rigid invalid colors show missing previous transform or
range/index mismatch. Mode 39 removes routed-ready rigid candidates from the
dynamic fallback path so green/cyan mixing points at true coverage gaps instead
of deliberate overlap between two copies of the same routed-ready surface.
Static, skinned, and other non-rigid surfaces use the no-object-motion color.
This mode is diagnostic-only and does not change mode 18 shading or write a
production motion-vector buffer.

Mode 40 is the routed-rigid primary object-motion vector diagnostic. It uses
the same current-pixel to previous-pixel convention as mode 38, but only for
routed rigid hits with a continuous previous transform. Valid routed rigid hits
encode current-to-previous pixel displacement as signed red/green direction
around 0.5 plus blue magnitude, capped at 32 internal PT pixels. Missing
previous transforms, range/index mismatches, and non-routed rigid fallback hits
use the same invalid/no-object colors as mode 39. Like mode 39, it removes
routed-ready rigid candidates from the dynamic fallback so the vector view is
not contaminated by duplicate fallback coverage. This mode is diagnostic-only
and does not change mode 18 shading or write a production motion-vector buffer.

Mode 41 is the combined skinned/routed-rigid object-motion vector diagnostic.
It uses the shared packed-object-motion pixel helper to draw vectors from
`previousPositionOrMotion` when the record carries object-motion and
previous-position flags. Valid dynamic hits encode current-to-previous pixel
displacement with the same red/green direction plus blue magnitude convention
as modes 38 and 40. Static surfaces use the no-object-motion color; skinned and
routed rigid invalid states keep their existing missing/range/out-of-range
colors. Mode 41 also removes routed-ready rigid candidates from the dynamic
fallback so the combined view represents the routed object-motion source rather
than overlapping fallback geometry. The skinned half needs
`r_pathTracingGpuSkinning 1` for the diagnostic previous-position scaffold;
with the CVar at 0, skinned surfaces should show the skinned missing-previous
color. It is diagnostic-only and does not change mode 18 shading or write a
production motion-vector buffer.

Mode 42 is the packed primary object-motion flags diagnostic. Instead of
drawing vector direction, it repacks the current primary surface and verifies
the actual `PathTracePrimarySurfaceRecord` flags and
`previousPositionOrMotion.w` selector. Valid packed skinned object motion is
green, valid packed routed-rigid object motion is teal, static/no-object
surfaces use the no-object-motion color, and missing/range/out-of-range states
reuse the normal primary-surface invalid colors. Use `r_pathTracingGpuSkinning
1` when checking skinned records; with it disabled, skinned surfaces correctly
report missing previous-position scaffold data. This mode is diagnostic-only
and does not change mode 18 shading or write a production motion-vector buffer.

Mode 43 is the packed object-motion reprojection match diagnostic. It repacks
the current primary surface, requires the same object-motion and
previous-position flags as mode 42, uses the shared packed-object-motion pixel
helper to project `previousPositionOrMotion.xyz` through the previous camera,
loads the previous primary-surface record at that pixel, and applies the normal
primary-surface material/normal/roughness match colors. Static/no-object
surfaces stay in the no-object-motion color, missing skinned/routed-rigid state
keeps the existing invalid colors, and green means the packed object-motion
source lands on a conservatively matching previous primary hit. This mode is
diagnostic-only and does not change mode 18 shading or write a production
motion-vector buffer.

Mode 44 is the previous static snapshot shader-binding diagnostic. It traces
normal primary hits, isolates static-world hits, and reads the reserved
previous-static vertex/index/triangle-class/triangle-material/material-index
buffers using the current persistent static primitive index. Green means the
previous static snapshot is shader-visible, range-safe, materially consistent,
and reconstructs a previous position matching the current static hit. Non-static
surfaces use the no-object-motion color, missing previous static buffers use
the missing-previous color, range failures reuse the rigid range color, material
or class changes use the material-mismatch color, and position mismatches use
the normal-mismatch color. It is diagnostic-only and does not feed motion
vectors or final shading.

Mode 45 is the previous static snapshot reprojection match diagnostic. It uses
the same previous-static lookup as mode 44, projects the reconstructed previous
static position through the previous camera, loads the previous primary-surface
history record at that pixel, and applies the normal material/normal/roughness
match colors. Green means the previous static snapshot can drive a conservative
previous-hit match through the same temporal comparison used by the ReSTIR/PT
primary-surface diagnostics. Non-static hits and static lookup failures reuse
the mode 44 colors. It is diagnostic-only and does not feed production motion
vectors or final shading.

Mode 46 is the previous static snapshot motion-vector diagnostic. It uses the
same previous-static lookup as mode 44, projects the reconstructed previous
static position through the previous camera, and visualizes current-to-previous
pixel displacement with the same signed red/green plus blue magnitude convention
as modes 38, 40, and 41. Non-static hits and static lookup failures reuse the
mode 44 colors. It is diagnostic-only and does not feed production motion
vectors or final shading.

Mode 47 is the combined geometry motion-vector diagnostic. Static-world hits
use the previous-static snapshot path from mode 46, while skinned and
routed-rigid hits use the packed object-motion path from mode 41. Other dynamic
or invalid hits keep the existing object-motion rejection colors. Like mode 41,
it removes routed-ready rigid candidates from the dynamic fallback path so the
rigid portion visualizes the routed source rather than overlapping fallback
geometry. It is diagnostic-only and does not feed production motion vectors or
final shading.

Mode 48 is the combined geometry reprojection-match diagnostic. Static-world
hits use the previous-static reprojection path from mode 45, while skinned and
routed-rigid hits use the packed object-motion reprojection path from mode 43.
Other dynamic or invalid hits keep the existing object-motion rejection colors.
Like mode 47, it removes routed-ready rigid candidates from the dynamic fallback
path so the rigid portion validates the routed source rather than overlapping
fallback geometry. It is diagnostic-only and does not feed production motion
vectors or final shading. The shared current/previous surface-similarity check
uses geometric normals for temporal identity rather than normal-mapped shading
normals; Doom 3 normal maps remain active for lighting, but they no longer
cause history rejection by themselves.

Mode 49 is the combined geometry motion-source diagnostic. It uses the shared
combined geometry motion helper that future production export code should call,
then colors valid static, skinned, and routed-rigid motion sources distinctly.
Invalid or unsupported hits keep the same rejection colors used by modes 47 and
48. Like modes 47 and 48, it removes routed-ready rigid candidates from the
dynamic fallback path. It is diagnostic-only and does not feed production motion
vectors or final shading.

Mode 52 is the routed-rigid transform parity diagnostic. It targets suspected
rotated-rigid reprojection failures by comparing three positions for each routed
rigid triangle hit: the actual DXR world hit position, the TLAS
`ObjectToWorld3x4()` reconstruction, and the CPU-uploaded
`PathTraceRigidRouteInstance.currentObjectToWorld` reconstruction. It removes
routed-ready rigid candidates from the dynamic fallback path so the hit comes
from the routed TLAS instance. Color code:

    green = route and TLAS reconstructions both match the actual hit
    yellow = small routed reconstruction error
    orange = medium routed reconstruction error
    red = large routed reconstruction error
    purple = routed metadata transform disagrees with TLAS transform
    blue = non-routed/unsupported hit

Use it on the exact polygons that flicker in modes 32/48. Purple on arbitrary
rotations means the routed metadata matrix packing does not match the TLAS
transform. Red/orange without purple means the routed local mesh/index data
does not reconstruct the same hit point as the BLAS/TLAS path.

Clear optimization path for mode 32/33/50/51 / future final shading:
- move ReSTIR PT stages out of the monolithic debug raygen into explicit
  pass-sized dispatches so initial sampling, temporal resampling, visibility,
  and final shading can be profiled and throttled independently;
- keep the visibility/shadow ray optional until a final shading pass has a
  denoiser/upscaler target;
- add half-resolution, checkerboard, or fixed internal-resolution execution for
  temporal PT, then upscale to the game output;
- reduce per-pixel replay cost by tightening initial sample count, bounce depth,
  and hybrid-shift/reconnection settings once correctness is established; do not
  reduce analytic proposals by biasing toward the first compact light slots
  unless those slots come from a real spatially local light proposal structure;
- replace the temporary camera-only reprojection with vertex/object motion
  vectors so fewer pixels churn through rejection/fallback paths;
- profile GPU pass timing before raising resolution, because driver device
  removal here is consistent with timeout pressure from long RT dispatches.

Rejected temporal-resolve shortcut: a follow-up, uncommitted mode 33 prototype tried to
reuse `SmokeAccumulation` as previous resolved color, read it at the reprojected
previous pixel, write current output to `SmokeOutput`, and copy output back to
accumulation after dispatch. User validation reported that it mostly picked up
emissives with little regular lighting, showed orange outlines, and
device-removed after roughly 30 seconds. That prototype was removed from the
active mode list; active mode 33 is now the light-source attribution diagnostic
described above, not a temporal resolve. Do not reintroduce the resolved-color
shortcut as the next denoiser step; the next temporal resolve should use an
explicit history resource/pass design with clear ownership, lower internal
resolution/checkerboard execution, and GPU timing around each stage.

Mode 20 reservoir scope is intentionally narrower than mode 18 lighting. Mode
20 samples `SmokeEmissiveTriangles` / `SmokeLightCandidates`; regular Doom
scene lights selected for mode 18 do not contribute to the reservoir yet.
Therefore some light seen in mode 18 can be absent in mode 20. Routed rigid
emissive candidates move with their routed object transform, but remaining
edge-of-screen flicker can still occur when an emissive candidate source depends
on current visible drawSurfs. Persistent/off-screen emissive light records and
area/portal light lists are later light-universe work, not part of this rigid
TLAS route step.

The first Stage F/G scaffold records area membership diagnostics for emissive
light-universe candidates. `r_pathTracingLightUniverseDump 1` reports area
membership for frame candidates and merged/persistent candidates using
`PointInArea` at the candidate center with a small normal-offset fallback. This
validates whether emissive records can be assigned areas before any area/portal
candidate filtering is enabled. It also reports the selected-area portal walk
using `r_pathTracingScenePortalSteps`, including selected area count, walked
portal edges, blocked portal edges, merged selected candidates, and connected
but unselected candidates. The dump also includes `portalSweep areas` and
`portalSweep merged` for portal depths 0, 1, 2, 3, and 4 so a single run shows
how aggressively portal depth would affect the light list. The
`portalDepthBins` histogram reports the minimum portal depth that reaches each
merged candidate, plus `>4`, disconnected, and unknown-area buckets.
When `r_pathTracingLightAreaFilter 1` is set, the dump also reports
diagnostic-only `areaFilter` counts: selected candidates, connected overflow,
disconnected, unknown, would-upload, would-drop, and top connected-overflow
samples by candidate weight. It also reports pre/post filter candidate area and
weight, dropped area/weight, dropped weight split by overflow/disconnected/
unknown, and top dropped samples by weight with their drop reason. This still
does not affect rendering.
`r_pathTracingLightAreaFilterApply 1` is a separate render-affecting gate that
applies the reported selection to the uploaded mode 20 emissive candidate list.
Current validation is limited to the opening section because this PT branch is
still missing enough game-logic parity to make broader campaign testing useful.
Do not infer a final portal-depth heuristic from this section alone; current
evidence only supports greedy connected-candidate retention as the safest first
apply behavior.

Routed rigid closest-hit notes:

- Routed rigid BLAS records store vertices/normals in local object space.
- The closest-hit path for `InstanceID >= 2` transforms geometric and
  interpolated normals through the TLAS instance object-to-world transform
  before writing the payload.
- It also builds a world-space tangent/bitangent basis from local UV gradients.
  This is required before testing routed rigid records in lighting modes that
  expect world-space payload normals.

For PT-native bounds/BVH inspection:

    r_pathTracingSceneSource 3
    r_pathTracingDebugMode 22
    r_pathTracingSceneBoundsOverlayGpu 0
    r_pathTracingSceneBoundsOverlayMax 16

For all mirrored bounds categories:

    r_pathTracingSceneBoundsOverlay 2

For quieter traversal testing:

    r_pathTracingSmokeLog 0

For hitch diagnostics:

    r_pathTracingTimingLog 1
    r_pathTracingTimingThreshold 40
    r_pathTracingReadbackEnable 0

For a one-shot current-frame pass timing and debug-output classification:

    r_pathTracingPassTimingDump 1

The pass timing line is submit-side CPU timing. Use it to separate setup,
barrier, clear, dispatch submission, history-copy, and readback pressure before
assuming a present or "Wait for Frame" stall names the root cause. For GPU pass
duration, capture a targeted run with `r_pathTracingOptickGpuMarkers 1` in a
stable Release/PT dev build.

CPU-hotspot triage checklist:

- Check scene build timing before blaming the raygen: capture/classification,
  material discovery, emissive/light candidate generation, buffer creation,
  buffer upload, BLAS/TLAS submit, and descriptor/binding rebuilds can all
  dominate the frame before the RT dispatch is submitted.
- Check whether skinned/deformed geometry, material table validation, texture
  lookup, portal walks, readback, or debug overlays ran on the frame being
  profiled.
- Check whether a resource resize/replacement forced idle/garbage collection or
  invalidated descriptors/history.

Device-removal triage checklist:

- Record build lane first. Release/PT dev Release is the only meaningful lane
  for performance conclusions; Debug/validation runs are correctness traces.
- Record the last timing line, debug mode, output resolution, estimated rays per
  pixel, ReSTIR pass label, visibility/reflection/spatial/temporal toggles,
  readback activity, and whether output-sized resources resized that frame.
- Capture `r_pathTracingSafetyDump 1` before changing workload assumptions. If
  the device removal happens at modest ray counts, treat invalid indexing,
  resource lifetime, stale descriptors, barriers, or pathological shader control
  flow as the first suspects.
- If the failure correlates with sustained single-dispatch GPU time rather than
  invalid resource access, enable `r_pathTracingDispatchTileEnable 1` and capture
  `r_pathTracingDispatchTileDump 1`. Start with 512x512 tiles, then reduce to
  256x256 or 128x128 if needed. Tiling is diagnostic dispatch shaping only; it
  does not change scene selection, TLAS/BLAS inputs, or shading quality.
- Recommended bisect sequence for mode 18/20/26-33/50 device removal:
  disable readback and Optick GPU markers, dump pass timing and safety metadata,
  then toggle in this order until the failure disappears:
  `r_pathTracingDisableAnyHitAlpha 1`,
  `r_pathTracingDisableReflectionRay 1`,
  `r_pathTracingDisableDiffuseSecondaryRay 1`,
  `r_pathTracingDisableSelectedLightLoop 1`,
  `r_pathTracingDisableAnalyticLightLoop 1`,
  `r_pathTracingDisableEmissiveTriangleSampling 1`,
  `r_pathTracingDisableRestirVisibilityRay 1`,
  `r_pathTracingDisableReservoirWrites 1`,
  `r_pathTracingDisablePrimarySurfaceHistory 1`.
  Once a switch changes stability, re-enable earlier switches one at a time to
  separate a bad index/resource path from pure workload fan-out.
- Do not stop at "Wait for Frame", present, or average FPS until CPU staging,
  upload/readback pressure, resource lifetime, and named pass timing have been
  checked.

If a hitch appears only with r_pathTracingSkipRaster3D 0, treat it as a raster
tail issue unless another RT/PT diagnostic contradicts that. Skipping 3D raster
removed the GUI/cinematic hitch during extended testing after GuiModel.cpp was
restored to original behavior, while also producing a large FPS gain.

For material universe table validation:

    r_pathTracingMaterialUniverseTable 1
    r_pathTracingMaterialUniverseTableValidate 1
    r_pathTracingTimingLog 1
    r_pathTracingTimingThreshold 0
    r_pathTracingSmokeLog 0

Clean validation reports materialTablePath=universe and
universeTableCompare=1/0 in the slow scene-build timing line. If validation
finds a mismatch while the universe table is active, the frame falls back to
the legacy table and logs materialTablePath=legacyFallback.


Useful Commits
--------------

Recent milestone commits on this branch:

- 3c583342 Add drawSurf mirror bounds diagnostics
- e55ba1ab Add path trace geometry universe scaffold
- 9a78a170 Cache universe material table work
- 90719330 Default to material universe table
- d8e6ca22 Add universe-derived material table path
- 7004b522 Add stable material universe indexes
- 68237ab6 Upload emissive light candidate buffer
- 5c94ac5c Add emissive light candidate summaries
- d37f1238 Add material universe facts
- 4b179101 Use material universe for emissive inventory
- f79870ee Extract RT smoke material universe module
- 4d1e5cda Add RT smoke material universe validation
- a4dca266 Add path trace module comments
- 34be0103 Document path trace module map
- 59f00215 Extract path trace scene resource commit packaging
- 7b9a4f4c Move path trace smoke scene build
- 355ac29e Extract path trace scene build diagnostic logs
- d2d06b57 Extract path trace diagnostic triggers
- c5194175 Extract path trace scene summary diagnostics
- c66ce58b Extract path trace slow scene diagnostics
- 010724b6 Extract path trace material metadata registration
- 2ba361d3 Extract path trace CVars
- c4165bdd Extract path trace scene buffer creation
- 9f4df79e Extract path trace acceleration buffer uploads
- 20db5d49 Extract path trace acceleration submit helpers
- 6c8868d0 Extract path trace scene resource handoff
- 406f71f5 Move path trace smoke resource lifecycle
- 1341e2ca Add toy path tracing accumulation
- 805eb9f5 Tune toy path tracing lighting scales
- 2b28217d Add toy path tracing debug mode
- c23a6d6e Improve RT smoke decal emissive semantics
- 9648cf11 Add RT smoke Lambert debug mode
- 4cb0419d Add RT smoke particle dithering
- 8eb40def Add RT smoke normal specular lighting
- e545feaa Add RT smoke GUI texture diagnostics
- 75229a99 Add RT smoke normal and specular texture previews
- fc802458 Add RT smoke translucent subtype debug mode
- 9de51c53 Add RT smoke albedo texture diagnostics
- bab07b5b Add RT smoke alpha any-hit traversal
- 3fab923f Add RT smoke alpha coverage texture preview
- ab85af6b Harden RT smoke texture table lifetime
- 352c6f75 Stabilize RT smoke texture probe sampling
- 5005d0a9 Add bindless RT smoke texture probe
- 847fc6f9 Add RT smoke material table lookup
- f9343e4a Add RT smoke material ID debug mode
- 862d8833 Add RT smoke vertex attributes
- 70954940 Add RT smoke CPU skinning path
- 47fef9da Split RT smoke static and dynamic buffers
- cf1a7a11 Stabilize RT smoke static world capture
- f7b7a895 Clean up RT smoke capture correctness
- 5b601109 Accumulate RT smoke static world cache
- 23e78735 Cache RT smoke static BLAS
- 282d6eb0 Split RT smoke BLAS buckets


Recommended Next Steps
----------------------

Near term:

1. Grow PathTraceGeometryUniverse into a real persistent geometry system.
   - Static surface key lookup and append policy now live in the universe.
   - Expand the persistent surface records so they can own or reference static
     ranges, material IDs, BLAS/build signatures, and dirty state.
   - Reserve a previous-frame/history model early: current and previous
     transforms, stable range mapping, and previous vertex/triangle data where
     transforms alone are insufficient.
   - Treat geometry-derived motion vectors as a core requirement for future
     DLSS Ray Reconstruction support, not as a late denoiser integration detail.
   - Keep the shader-facing buffers and material-index compatibility unchanged
     until a deliberate resource contract change is made.
   - Prefer persistent GPU buffers and dirty-range uploads over more
     per-frame CPU micro-optimizations when the behavior is clear.

2. Keep material-universe work focused on validation and missing facts rather
   than another broad rewrite.
   - The universe-derived active table is the default and is cached.
   - Use r_pathTracingMaterialUniverseTableValidate when changing material table
     behavior; a clean frame reports universeTableCompare=1/0.
   - Add new persistent material facts only when they remove repeated per-frame
     work or feed future PT/ReSTIR data.

3. Prepare emissive inventory data for NVIDIA ReSTIR PT integration without
   duplicating the sampler itself.
   - Keep mode 19 as a broad CPU material/triangle inventory plus texel-tight
     debug view.
   - Add metadata that future reservoir sampling will need: area, luminance
     proxy, and transform stability. Emissive texture slot/dimensions and
     triangle UV bounds are already recorded per candidate. Candidate records
     also carry centroid UV, estimated radiance/luminance, area*luminance
     weight, normalized placeholder inventory PDF, area PDF, material universe
     index, material id, and a 64-bit stable-ish triangle identity hash; the
     shader has a helper for centroid emissive texture evaluation.
     The current CPU estimated radiance/luminance is only the material emissive
     color proxy, so many Doom emissive stages report white `(1,1,1)`. The
     stored candidate weight is `area * estimatedLuminance` and is useful as a
     placeholder, but final ReSTIR/PT light preparation should replace or
     augment it in a GPU pass that samples `emissiveSlot` at `centroidUV` or
     over `uvBounds`.
   - Current dumps show sane material variety in clustered emissive scenes:
     static/dynamic split, real texture slots/sizes, and skinned/game-logic
     dissolve overlays excluded.
   - Continue excluding game-logic/skinned dissolve emissive overlays from
     light candidates.
   - Emissive proposal sampling now has a dedicated shader boundary in
     `pathtrace_emissive_sampling.hlsli`. The public selector name is still
     `SelectSmokeWeightedEmissiveTriangle`, but it can use a CPU-built CDF
     distribution table (`PathTraceEmissiveDistribution`, SRV t46) when
     `r_pathTracingEmissiveDistribution 1` and falls back to the legacy linear
     scan otherwise. The CDF table only chooses an index; it does not shrink the
     authored emissive triangle universe or replace the full triangle records
     used for shading, attribution, visibility ignore, and RAB reconstruction.
   - Reservoir current/previous/spatial buffers now exist as owned resources.
     They have a reset signature covering camera/output, material table,
     emissive candidate records, light-candidate records, and static BLAS
     identity, and are cleared when that signature changes. They are not yet
     bound into a sampling shader.
   - A persistent light universe now keeps static emissive candidates across
     frames by stable-ish identity, tracks stable rigid-entity emissives as
     semi-static dynamic records, and merges current-frame dynamic emissives
     with the persistent static list. Missing dynamic records are not injected
     by default because matching dynamic occluder geometry is not persistent
     yet. Mode 20 samples that merged list, but spatial and temporal reuse are
     still intentionally off.
   - Q2RTX's Ray Reconstruction reference fork gives the right direction for
     off-camera emissives: static emissive BSP surfaces become light polygon
     records, are assigned to BSP clusters, and are sampled through per-cluster
     light lists instead of through the current screen-visible emissive set.
     Dynamic/model lights are injected into those cluster lists each frame, and
     temporal ReSTIR history uses previous-to-current light identity remapping
     so missing dynamic lights invalidate history. For Doom 3 BFG, this argues
     for future area/portal/cluster-style light membership and identity
     remapping, not blindly retaining stale dynamic emissive candidates.

4. Start a deliberate Doom material to RT/PBR conversion.
   - Base color from albedo.
   - Shading normal from normal map.
   - Specular/F0 from Doom spec map.
   - Roughness approximation for denoising/ray reconstruction.

5. Keep missing, unsafe, or dynamically generated materials on fallback debug
   colors instead of forcing image loads.

6. Investigate why SampleLevel in RT removes the device near animated SWF/GUI
   surfaces while Texture.Load survives.

7. Start separating decal/blend/translucent behavior from opaque albedo. The
   current subtype classifier is useful for inspection but not a renderer.

8. Keep a visible-portal-chain augmentation in reserve for rare portal layouts
   that defeat a fixed off-camera depth. The current default should remain
   multi-seed depth 4 for source3 static preload, rigid residency, Doom
   analytic lights, and light-universe area filtering because off-camera
   geometry/lights deeper than that are unlikely to matter often enough to
   justify a global depth increase. If playtesting finds a visible doorway or
   corridor built from a silly chain of 5-10 tiny overlapping portal cells, add
   a separate CVar-gated exception instead of raising every residency walk:
   recursively clip Doom portal windings from the multi-seed camera areas,
   union the areas reached through genuinely screen-visible portal openings
   with the depth-4 set, and cap that exception separately by visible depth and
   max added areas. A first version can use frustum + recursive portal-winding
   clipping only; a later version can add cheap CPU trace probes to the portal
   center/corners to reject portal boundaries hidden behind solid map geometry.
   This is an on-screen correction path, not a replacement for off-camera PT
   residency.

Medium term:

1. Keep focused changes small and feature-specific now that the broad extraction
   pass is complete. The current module split is good enough for targeted work;
   do not keep splitting purely for line count.

2. Consider a dedicated PathTraceSmokeState / PathTraceSmokeScene ownership
   design only when there is a concrete reason to change state ownership. The
   current PathTracePrimaryPass m_smoke* ownership is still intentional.

3. Replace the current static-cache-vector compatibility layer with explicit
   persistent geometry records once PathTraceGeometryUniverse owns lookup,
   append, invalidation, and range metadata. This is stateful and should be
   tested as a behavior change, not as a cosmetic extraction.

4. Evolve mode 18 into a real PT integration point:
   - keep the current low-bounce/low-sample baseline as a debugging reference
   - feed ReSTIR/PT-ready scene, material, and emissive candidate buffers
   - replace the placeholder direct/emissive logic with reservoir-based light
     sampling when NVIDIA's implementation is brought in

5. Preserve the current PT-owns-3D / raster-owns-GUI split. Do not re-enable the
   full 3D raster path underneath mode 18 as a default fallback; it doubles the
   renderer workload and can make raster hitches look like path tracing hitches.
   If a missing side effect is found, bring back that narrow side effect rather
   than the whole 3D raster frame.

6. Add explicit material fields needed by path tracing:
   - base color
   - alpha/cutout
   - normal map
   - roughness/specular/metalness approximation
   - emissive/light material support

7. Replace the smoke particle dithering placeholder with a better translucent
   strategy if it becomes important:
   - stochastic transparency with accumulation
   - sorted or approximate compositing
   - ray-marched/volumetric approximation for major smoke effects

Longer term:

1. Continue replacing the raster 3D scene with a full path traced scene output.
   Mode 18 now skips the normal 3D raster tail by default, but the RT scene is
   still incomplete and should grow by adding missing PT data, not by relying on
   hidden full-raster work underneath.

2. Preserve raster GUI/HUD/menu composition on top.

3. Investigate denoising and reconstruction technologies after a visually
   meaningful brute-force baseline exists.

4. Study Quake II RTX and NVIDIA RTXDI/RTXPT/Donut as external references for
   architecture ideas, especially material handling, bindless texture tables,
   light sampling, accumulation, and denoiser integration. The Doom 3 renderer
   differs enough that code should be adapted carefully rather than copied
   wholesale.


Local References
----------------

The user keeps NVIDIA RTXDI/ReSTIR reference source and manuals here for
read-only study during integration planning:

    E:\prog\references\RTXDI-main

The user also keeps a Q2RTX Ray Reconstruction reference fork here:

    E:\prog\references\Q2RTX-rayreconstruction-master

Use it for architecture study around DLSS Ray Reconstruction, motion-vector
outputs, previous-frame geometry/transform history, and temporal reconstruction
contracts. The relevant design lesson for this branch is that the geometry
universe should preserve previous-frame geometry state so RT shaders can compute
motion from current vs previous hit positions. Do not rely on camera-only motion
or a frame-local geometry rebuild if Ray Reconstruction is a goal.

For emissive light sampling, the relevant design lesson is that Q2RTX does not
depend on emissive surfaces being on screen. Static emissive BSP surfaces are
promoted into light polygon records and assigned to cluster/PVS light lists.
ReSTIR samples the list for the shaded point's cluster. Dynamic/model emissive
lights are transformed and injected into those same lists per frame, with
previous-to-current light id remapping for temporal reuse and invalidation when
a dynamic light disappears.

Keep reference checkouts outside the RBDoom3 BFG repo. Use them as reference
material for reservoir/light-sampling architecture, motion-vector architecture,
and manuals; do not revive the removed in-tree RTXPT/Donut attempt unless doing
a deliberate clean integration.


Notes for Future Contributors
-----------------------------

This work is debug-only and intentionally conservative. If something looks
overly defensive, it is probably because a previous experiment crashed the device
or produced silently wrong shader data.

Avoid these traps unless you are intentionally revisiting them:

- Do not call GetEditorImage() from RT texture discovery during frame capture.
  It can load missing images and caused repeatable device-removed crashes.

- Do not assume a BLAS built from one vertex/index buffer can be reused with
  different shader-side attribute buffers. BLAS geometry and closest-hit fetches
  must refer to matching data.

- Do not merge static and dynamic triangle metadata casually. InstanceID and
  per-instance buffer selection are important.

- Be cautious with Doom draw-surface order. It is not stable enough to use as a
  persistent identity.

- Be cautious with large single surfaces and transient draw surfaces. The capture
  code has validation for null data, stale CPU geometry, invalid indexes,
  limits, and zero-area triangles for a reason.

The current code is not final architecture. It is a working ladder: each rung is
there because it made the next ray tracing feature testable in game.
