D0b Blob X first landing
========================

Date: 2026-08-18
Status: accepted and shipping default (2026-08-18). threeVertexInitial
        and threeVertexSplit default 1. exec upt pins the same. Monolith
        A/B remains exec uptb2 (split 0). 2-vtx A/B is exec uptw0.

Fixture (handover G3, informal overlay fps, 2026-08-18)
  Human: wall ~120 fps, fixture on-screen ~110 fps. Start of the
  investigation was ~120 to ~85. Residual is still there and small.
  Not a formal G-P3 Nsight wall-to-fixture stamp; overlay fps has a
  20-37% cross-launch floor (handover R2). Direction is the old
  ~30% fixture hit shrinking to ~10 fps. Do not treat as compact-on
  permission (G-P4 still unrun).

What
  threeVertexInitial=1 + threeVertexSplit=1 selects 2-vtx produce
  (..._light64_x3_produce.bin) then 64x1 x3 consume
  (..._light64_x3_consume.bin). Fat 3-vtx monolith is exec uptb2
  (split=0).

  Produce decodes the live x2 hit, runs PrepareSecondContinuation
  (q + minT) on that surface, and queues only if the x3 ray is
  admitted. Token is eight uint4s (explicit 128 bytes): prepared
  ray + path + x2 position. Consume unpacks that ray and traces;
  it does not Prepare or re-decode x2. Intermediate/final writes
  unchanged.

  Compact is ignored on this path for the first landing (use
  emissiveCompact 0).

  Visual no-op after the 80-byte ID token (redeployed 2026-08-18
  04:05): consume still rebuilt the first continuation and
  re-decoded. ID re-decode can fail closed even when produce's
  live decode succeeded. Prepare now runs on produce with the
  live surface. uptx.cfg pins minT=0.0 / q=0.8 to match uptb2.
  Compare exec uptx vs exec uptb2 in a second-bounce-only corner,
  not by toggling the 3-vtx cvar.

Enable
  exec uptx
  Confirm: "x3 consume compiler=slang" and split-indirect is the
  produce blob, not the 38k three_vertex.bin.
  Leave X with exec uptb2 (now pins threeVertexSplit 0). upt / uptw /
  uptw0 also pin split 0. The cvar is not archived; a restart also
  returns to 0. Do not exec uptb2 after uptx without that pin — you
  stay on the X PSO and the 3rd bounce never returns.

Gate
  Same easy camera, T/S off: (produce + consume) must beat 5.6 ms
  and 60% NI of the monolith. Scaling target ~1.7x vs 2-vtx 1.5 ms,
  not RTXDI milliseconds. Combined D0 = produce + barriers + consume.

Same-section A/B 2026-08-18 (files, this level)
  Full table: docs/ReSTIR/d0b_gi_level_ab_nsight.txt
  Combined X GI 2.01M samples vs monolith 4.18M (2.08x).
  Human earlier ~3.5M vs 2M; the exports are 4.18M vs 2.01M. That is duration,
  not skipped work — same 3rd bounce, two fetched bodies finish
  sooner. Cross-scene earlier was 2.05M vs 2.59M and understated
  it. Combined NI% 64.8 vs 75.4 is the stall mix, not the 2x.
  Produce is still ~70% NI. Product stays X. Do not read this as
  ~20% NI or as permission to resume Track B / unroll / noinline.

RTXDI same-look 2026-08-18 (human)
  Busier RTXDI scene: 3-vtx indirect ~1.0M samples vs combined
  uptx ~2.0M. D0a stall mix looks like RTXDI DI, but ~2x that
  dispatch's samples. Indirect is not that: RTXDI PT is LS-bound
  (scene-batch 4.2% NI / 70% LS); X produce is 70% NI. Half the
  samples and those samples are traces, not fetch. Closing 2M->1M
  on X is produce fetch/contiguity, not skipping bounces. D0a 2x
  with matching metrics is extra healthy work / footprint, not
  the produce cliff.

Two-word token restore 2026-08-18
  Root cause (Voss / review section 6): Upt04X3UnpackPrepared
  zero-inited Upt04InitialTraceWork and never restored
  partialJacobian (stayed 0 -> Upt03StructurallyValid reject) or
  candidateFlags (Global|Replayable|NeedsShift). Token stayed 128
  bytes; packed the live produce values into unused words:
    token.c.w = asuint(prepared.work.partialJacobian)
    token.f.w = prepared.work.candidateFlags
    work.partialJacobian = asfloat(token.c.w)
    work.candidateFlags  = token.f.w
  Did not hardcode 1.0f or flag literals. No stride / host buffer
  / MIS / reservoir-format change.

  Deployed (Vulkan only)
    E:/prog/rbdoom-3-BFG-prebuilt/RBDoom3BFG.exe
      16485888 bytes  mtime 2026-08-18 04:27:52
      sha256 25788097E7E0485C844F80FFE411746DDD3C508AAD3D3EC3D6618B279E25B8A2
    .../slang_upt04/upt04_initial_split_indirect_rayquery_compact32_light64_x3_produce.bin
      412668 bytes  mtime 2026-08-18 12:44:24
      sha256 0C152B1551D845F5971F3A60C106B7C7A5E432ADEC2EBEA924254E1C19B067DB
    .../slang_upt04/upt04_initial_split_indirect_rayquery_compact32_light64_x3_consume.bin
      323984 bytes  mtime 2026-08-18 12:44:24
      sha256 3D55DAA766B67086B1B22349D5ABAE26A3F7377C7AA34710168742752D49CF35
  Emissive consume left in place. Compact default still 0.

  Human A/B after restart: exec uptx vs exec uptb2 vs exec uptw0
  in a second-bounce-only corner. Confirm recordBytes=128,
  produce+consume blobs (not three_vertex.bin), threeVertexSplit=1,
  q=0.80 minT=0. Leave X with exec uptb2.