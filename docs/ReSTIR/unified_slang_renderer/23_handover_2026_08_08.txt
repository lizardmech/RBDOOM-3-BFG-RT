Unified Slang ReSTIR PT Handover - 2026-08-08
================================================

Read this first
---------------

This is the current handover for the experimental UPT renderer on branch
restir-development. It summarizes the accepted direct-reuse route, the fixes
that must not be lost, the current unresolved emissive problem, and the next
implementation boundary. The numbered documents remain the detailed contract;
this file is the entry point for continuing the work without reconstructing the
entire development conversation.

Repository state at handover
----------------------------

    branch: restir-development
    checkpoint: d5929c127eb3  Promote tested UPT reuse defaults

The worktree is intentionally dirty and contains substantial user/renderer
work after that checkpoint. Do not reset, checkout over, or broadly reformat
it. In particular, 22_upt09_spatial_rescue_slice.txt and alternates/ are
untracked at this handover, while several earlier numbered documents and the
UPT host/shader sources are modified. Inspect the scoped diff before staging.

Renderer purpose and present boundary
-------------------------------------

UPT is a Vulkan-only Slang renderer built beside the legacy clean DI/GI lanes.
It is not a refactor of those lanes. Its long-term target is the ReSTIR PT
Enhanced path-tree design: direct light is a short selected path and indirect
light is a longer selected path in one reservoir stream. Materials remain
separate providers rather than being embedded in ReSTIR passes.

The earlier emissive-acquisition and indirect geometry/material/alpha replay
failures are fixed. The indirect path is now functionally accepted. Current
work is performance-only under UPT-10. The
latest exact finite-predicate/candidate-state trial made family 1 and family 2
slightly faster in isolation but did not materially improve family 0. Treat
the monolithic fusion penalty as unresolved and continue with production
specialization before considering a physical direct/indirect split.

The native112 exact shared-reservoir split has since recovered approximately
8 ms versus monolithic family 0, including both dispatches and their barrier.
Treat split D0a/D0b as the current accepted performance direction. The next
open measurement is D0b's scalar native-light view, introduced after Nsight
identified its eager 112-byte StructuredBuffer record load as a high LSU
global-memory dependency. It changes only the shader's view of the existing
bytes; runtime image and timing acceptance are still pending.

Accepted runtime shape
----------------------

The accepted direct test composition is:

    P0  UPT lean primary producer
    D0  monolithic RayQuery direct initial sampling
    T0  optional temporal direct shift and winner visibility
    U0  optional sample-ID publication
    U1  optional 17x17 duplication count
    S0  optional direct spatial reuse
    R0  trace-free RGBA16F resolve

Important defaults inside that opt-in route are:

    r_pathTracingUnifiedPtLeanPrimary             1
    r_pathTracingUnifiedPtCompactReceiver         2
    r_pathTracingUnifiedPtDirectProposalParity    1
    r_pathTracingUnifiedPtTemporalSearch          2
    r_pathTracingUnifiedPtTemporalMaxHistoryM     32
    r_pathTracingUnifiedPtTemporalMaxAge          32
    r_pathTracingUnifiedPtTemporalPreviousBest    0
    r_pathTracingUnifiedPtTemporalPairwise        0
    r_pathTracingUnifiedPtDuplication             1
    r_pathTracingUnifiedPtSpatialProofMode        0
    r_pathTracingUnifiedPtDirectTargetPdfParity   1
    r_pathTracingUnifiedPtAnalyticPortalDomain    1
    r_pathTracingUnifiedPtBackend                 0
    r_pathTracingUnifiedPtFamily                  1

The renderer, presentation, temporal pass, and spatial pass themselves remain
default off. The promoted values above are the accepted sub-policy defaults
when their owning route/pass is enabled; they do not make UPT the normal game
renderer.

Known-good direct-reuse test setup
----------------------------------

Apply controls while UPT is disabled, then enable it last:

    r_pathTracingUnifiedPtEnable 0
    r_pathTracingUnifiedPtBackend 0
    r_pathTracingUnifiedPtFamily 1
    r_pathTracingUnifiedPtProofStage 9
    r_pathTracingUnifiedPtShaderProof 6
    r_pathTracingUnifiedPtResolve 1
    r_pathTracingUnifiedPtResolveView 0
    r_pathTracingUnifiedPtLeanPrimary 1
    r_pathTracingUnifiedPtCompactReceiver 2
    r_pathTracingUnifiedPtDirectProposalParity 1
    r_pathTracingUnifiedPtTemporal 1
    r_pathTracingUnifiedPtTemporalSearch 2
    r_pathTracingUnifiedPtTemporalMaxHistoryM 32
    r_pathTracingUnifiedPtTemporalMaxAge 32
    r_pathTracingUnifiedPtTemporalPreviousBest 0
    r_pathTracingUnifiedPtTemporalPairwise 0
    r_pathTracingUnifiedPtDuplication 1
    r_pathTracingUnifiedPtSpatial 1
    r_pathTracingUnifiedPtSpatialProofMode 0
    r_pathTracingUnifiedPtDirectTargetPdfParity 1
    r_pathTracingUnifiedPtAnalyticPortalDomain 1
    r_pathTracingUnifiedPtDiagnostics 0
    r_pathTracingUnifiedPtFixedSampleIndex -1
    r_pathTracingUnifiedPtEnable 1

Safe CVar defaults use proofStage=1, shaderProof=1, resolve=0, and enable=0.
Full-frame shaded testing therefore requires the explicit 9/6/1 setup above.

Debug and profiling controls
----------------------------

Resolve views currently useful for diagnosis:

    0  final estimator
    5  temporal state/rejection
    6  emissive-triangle cast contribution only
    7  Doom analytic contribution only
    8  emissive cast plus primary self-emission
    9  primary self-emission only

r_pathTracingUnifiedPtDiagnostics is a one-shot diagnostic specialization. A
0 -> 1 transition compiles/runs it, reads the fixed counter/signature tuple,
then returns to production. It is not a persistent overlay.

Set r_pathTracingNsightGpuMarkers 1 for the P0/D0/T0/U0/U1/S0/R0 markers.
Temporal is named "UPT.T0 Temporal Direct Shift..."; duplication adds U0/U1.

Accepted low-level and ABI fixes - do not regress
-------------------------------------------------

1. Slang RayQuery Vulkan bindings

   Initial device removals were descriptor-layout errors, not a RayQuery or
   Slang hardware failure. The Vulkan/NVRHI binding-offset contract, TLAS slot,
   push constants, and reflected descriptor order must remain exact. Later
   duplicate t5 and UPT-08 SRV ordering crashes were also binding-layout bugs.
   Treat reflection and the live binding set as one ABI.

2. Two fully written 64-byte pages, no per-frame clear

   D0/T0/S0 fully cover their destination. Canonical empty/count-only encoding
   makes stale bytes self-invalid. Pages are cleared only when allocated; reset
   and camera/scene invalidation advance host metadata. Do not add a 225 MiB
   per-frame clear. Future sparse sidecars require stamps or compact active
   storage rather than relying on page metadata.

3. Frame serial and page-role advancement

   History metadata includes a host frame serial. History is usable only when
   its serial is exactly current-1. A skipped T0/S0 frame must advance or repair
   page ownership consistently; otherwise an N-2 reservoir page is compared
   against N-1 primary surfaces and portal crossings look like a persistent
   flush. This fixed the portal temporal desynchronization. Preserve the source
   comment documenting this invariant.

4. Stable direct-light identity

   Reservoir word2 is the current dense manager index+1. Word3 is a fingerprint
   of (type, identityA, identityB, materialOrLightId). sourceIndex is a
   frame-local source-array address and must never be treated as stable light
   identity. T0 maps through the manager previous-to-current table, then checks
   the fingerprint. This fixed animated-light ID churn/strobing.

5. Doom-scale visibility rays

   Primary-direct visibility uses both geometric- and shading-normal horizon
   tests, a 0.75-unit signed geometric-normal offset, 0.25 units along the ray,
   and a 0.5-unit endpoint margin. RayQuery uses non-opaque candidate control
   and ignores the selected emissive instance/primitive tuple. Reverting to an
   unoffset origin, 0.02 endpoint margin, FORCE_OPAQUE, or no self-ignore causes
   self-intersections on real Doom maps.

6. Temporal visibility ownership

   R0 is intentionally trace-free. Therefore T0 performs at most one
   winner-only visibility RayQuery for a reused winner whose current-frame
   visibility is not already proven. On failure it restores the untouched D0
   reservoir; rejected history mass is not retained. Do not remove this ray on
   the assumption that RTXDI's resampling helper is trace-free: its surrounding
   pipeline re-establishes visibility elsewhere.

7. Temporal surface search discipline

   Search mode 2 dithers reprojection and probes up to nine radius-4 surface
   positions. The first compatible surface wins, and only then is its reservoir
   loaded. Reservoir content must never steer the surface search. The rejected
   fixed/favorable-neighbor variants produced watery noise, trails, and hidden
   spatial reuse inside T0.

8. Compact receiver and authored smoothing

   CompactReceiver=2 uses a 32-byte hot D0 receiver plus a separate 32-byte cold
   history sidecar for T0/S0. The lean primary route also preserves authored
   vertex normals/smoothing. Do not replace authored smoothing with a fixed
   angle or recomputed face normal.

9. Direct target measure and proposal parity

   DirectTargetPdfParity=1 stores the RTXDI-shaped target
   luminance(BRDF*Li*cos)/solidAnglePdf, with technique MIS applied once in the
   scalar RIS weight. DirectProposalParity=1 uses one CDF-selected emissive
   trial plus up to 32 range-stratified analytic trials. Changing only one pass
   to another target convention invalidates D0/T0/S0 reuse math.

10. Duplication control and bounded age

   U0/U1 publish/count a stable sample ID over a 17x17 neighborhood and reduce
   the next-frame temporal M cap in correlated regions. MaxHistoryM=32 and
   MaxAge=32 passed the accepted mode-0 test. Age 63 remains a stress/no-expiry
   setting and can still amplify isolated regions. Spatial mode 3 is a useful
   bounded 1/M diagnostic, not the production default.

11. Non-recursive empty-center rescue

   An empty-center rescue winner is consumable for the current frame but is
   stamped so it cannot donate recursively through later temporal/spatial
   frames. Neighbor offsets are unique and edge-clamped pixel duplicates are
   rejected. Do not restore with-replacement recursive donation.

12. Monolithic D0 remains the accepted performance shape

   The combined direct/indirect and continuation split experiments serialized
   work and duplicated traversal. They were slower than the approximately
   0.9 ms monolithic D0 in the accepted direct scene. Do not reopen a split
   solely because its individual marker is smaller; compare the complete sum
   and RT-core work.

Performance observations worth preserving
------------------------------------------

The small test scene reached the 120 FPS cap at 2560x1440 with textured models.
CompactReceiver=2 saved roughly 0.8 ms in the user's capture. In the tested
larger scene DirectProposalParity=1 was unexpectedly about 2 ms faster than the
fallback; S0 itself was about 2.3 ms faster while moving about 0.4 ms elsewhere.
The compact route also reduced render-thread pressure and improved FPS in the
CPU-bound parts of the game. Treat in-game FPS as secondary at high frame rates;
use Nsight marker sums for small differences.

The early performance investigation also found discrete slow/fast GPU states
whose delay moved between substantial dispatches. It was not tied to one shader
and Nsight's clock lock did not explain the within-capture change. That issue is
recorded separately in docs/optimize and is not an excuse to discard measured
UPT payload/bandwidth wins.

Emissive state: what is fixed
-----------------------------

The following are distinct contracts and have been repaired:

    primary visible self-emission is a local R0 term and never enters reuse;
    back-facing Doom panels follow the explicit two-sided emissive policy;
    D0/T0/S0 replay exact triangle UV/material emission rather than a flat
        manager radiance estimate;
    conditional door/signage stages use the lean nearest-signage tuple;
    static-bucket replay uses the TLAS instance/primitive identity rather than
        confusing it with the canonical surface record;
    stale/dynamic material-state ownership that created ghost emitters was
        corrected in the current worktree.

Bucket routing explained an emission-color/white-replay defect, but disabling
bucket routes did not fix emitters disappearing. Do not conflate those issues.

Current blocker: UPT-only emissive acquisition loss
---------------------------------------------------

Some emitters disappear reproducibly in particular player portal positions,
often inside or one portal away. Running through other portals can make them
return; one standing position can reliably turn them off. Legacy clean DI is
not affected. Analytic portal-domain, static-bucket route, and other tested
route controls did not change the disappearance. The problem is not the global
emissive cap: observed inventories are well below the 4096-triangle cap.

The important comparison is:

    shared upstream data
        Both lanes consume the same emissive triangle inventory, the light
        manager's current payload/ranges, and the same host-built power/area CDF.

    legacy D0
        Before ordinary RIS, it reprojects the prior best reservoir, translates
        the light through stable previous-to-current identity, replays it at the
        current receiver, and streams it once as a current proposal. The default
        r_pathTracingCleanRtxdiDiBestLights=1 enables this D0 reseed.

    UPT D0
        It currently relies on fresh CDF selection. Its previous-best experiment
        lives in T0, defaults off, and was visually broken (dark/overexposed).

This makes the leading next fix a legacy-parity D0 previous-best reseed with
stable-ID translation and duplicate exclusion. Do not simply enable
r_pathTracingUnifiedPtTemporalPreviousBest; that is a different stage and its
estimator was not accepted.

If that does not close the portal-position loss, instrument the stages in this
order, using one selected emitter identity across both positions:

    1. CDF interval selected and dense manager index resolved;
    2. dense index maps to the expected emissive replay tuple;
    3. tuple/triangle/UV/material replay succeeds;
    4. exact endpoint emission is finite and colored;
    5. target and source PDF are positive;
    6. visibility ray reaches the receiver without self-hit;
    7. reservoir selection/finalization retains the candidate.

Do not add candidate-count guesses, portal-local caps, or another emissive
universe until that boundary records where the same light is lost.

Implemented D0 acquisition checkpoint, 2026-08-08
-------------------------------------------------

The reported uniform-mixture experiment led to a verified pre-proposal defect.
UPT used the emissive CDF structured buffer's physical GetDimensions result as
the searchable entry count. The allocation is intentionally capacity-reused,
so a smaller portal-frame publication left a stale high-water tail visible to
UPT's binary search. The scene already publishes the exact current count and
valid bit, which legacy clean DI consumes through its current-frame constants.

The implemented repair uses that exact current count/validity and treats the
physical dimension only as an OOB guard. Invalid current weighted publication
uses the existing typed uniform fallback. It does not add a PDF floor, enlarge
the portal domain, shrink/reallocate the buffer, or add random candidates.

D0 now also has an emissive-only legacy-shaped previous-best reseed behind
r_pathTracingUnifiedPtD0PreviousBest=1. It validates prior receiver history,
stable remap, current identity, exact stored barycentric replay, current
material/target, duplicate exclusion, and winner visibility. Dynamic-history
emissives remain rejected. Analytic previous-best samples remain excluded, so
fresh analytic behavior is unchanged. The merge preserves UPT's target-scaled
mean-mass convention and uses no ray beyond the existing selected D0 visibility
query. T0 was not used as compensation.

Diagnostics now distinguish:

    previous lookup attempted/succeeded;
    remap, replay, current-target, and visibility failure;
    duplicate rejection and successful previous admission;
    fresh analytic versus fresh emissive selection;
    unavailable weighted CDF plus current count/capacity/valid/zero-PDF skips.

Verification completed:

    cmake --build --preset win64-pt-dev-release passed;
    Vulkan reflection and bounded trace-site checks passed;
    compact32, direct/unified specialization, repeatability, UPT-07 temporal,
        UPT-08 duplication, and UPT-09 spatial verification passed;
    no DX12 shader build was run;
    no per-frame clear or third reservoir page was added.

Runtime gate accepted, 2026-08-08: user testing at the previously failing portal
borders reports the same behavior as legacy clean DI. The transient
border-crossing hitch remains, but the large-scale/persistent emissive failures
do not. This accepts D0 acquisition without claiming that the upstream
one-frame portal publication hitch was removed.

Unified indirect first build checkpoint, 2026-08-08
---------------------------------------------------

The separately authorized first no-reuse unified indirect slice is now
implemented and Vulkan-build verified. It is not yet runtime image, energy, or
performance accepted.

The implemented contract is:

    one opaque primary continuation;
    endpoint emissive evaluation plus one secondary NEE proposal;
    one shared direct/indirect initial reservoir and trace-free resolve;
    exact current-CDF reverse emissive density in a widened 20-byte lookup;
    logical hash capacity with physical stride/OOB guarding;
    generalized multi-sample n_j p_j NEE density for MIS;
    complete PSS estimators at the outer stream boundary, avoiding a second
        inverse-primary-BSDF factor;
    additive path-tree mass with confidence c = 1;
    accepted Doom offsets, finite visibility range, masks, and emissive
        self-ignore;
    no temporal/spatial indirect reuse, extra page, per-frame clear, or extra
        dispatch.

The control ABI is version 8 and 208 bytes. Diagnostics now own 107 counters;
the reservoir, D0, and C0 probes begin at words 107, 123, and 139. They
distinguish continuation,
endpoint emission, reverse lookup/PDF, secondary selection/material/PDF-ray,
visibility/self-hit, and final indirect-selection stages.

The full win64-pt-dev-release build passed Vulkan reflection and all UPT-04
backend, geometry, material, compact32, proposal-parity, specialization and
repeatability checks, followed by UPT-07/08/09 verification. No DX12 shader was
compiled. Runtime proof must keep D0 previous-best, temporal, and spatial off;
direct-only family 1 is the baseline, unified family 0 is the target, and
indirect-only family 2 plus one-shot diagnostics is the localization view.

First indirect runtime finding and correction
---------------------------------------------

The first deployed unified image produced basic indirect lighting, but runtime
testing reported the same disappearing and phantom emissive behavior that D0
had just closed. The source audit found a concrete universe mismatch before
secondary proposal replay: secondary NEE uniformly sampled the entire manager
emissive range, while accepted direct D0 sampled the exact logical-count current
power CDF. The full range can contain portal-ineligible identities which the
current weighted publication intentionally omits.

Secondary NEE now uses one draw from the accepted D0 typed mixture and exact
current emissive CDF. It remains one candidate and adds no ray or work. The
shared selector is binding-minimal; indirect-only adds only current CDF binding
25, exposes exactly 25 descriptors, retains two bounded trace sites, and pulls
in no history/previous-page resource. The focused UPT-04 artifact build and the
full win64-pt-dev-release preset passed, and only the 23 rebuilt Vulkan blobs
were deployed. Runtime visual retest remains required before accepting the
indirect estimator or changing any reuse policy.

gi.txt diagnostic finding and bounded inner-RIS correction
----------------------------------------------------------

The first CDF-corrected runtime image showed no visible improvement. The
indirect-only gi.txt capture then localized the dominant failure: 3,530,606
continuations hit geometry, but secondary NEE attempted only 4,846 visibility
rays and only 390 passed; 3,678,098 of 3,686,400 receiver pixels remained empty.
Endpoint emission supplied 7,913 positives. Reverse lookup succeeded with a
positive PDF for 6,002 of those and failed for 1,911, so remap/replay was not
the mechanism capable of explaining the full-frame disappearance.

The cause was the one-draw interpretation of the D0 typed trial schedule. With
32 analytic trials and one configured emissive trial, only roughly one in 33
secondary vertices attempted an emissive identity. Secondary NEE now streams
the complete existing D0 schedule through an inner RIS, converts its
weightSum/candidateCount UCW into one complete PSS estimator, and traces
visibility only for the selected winner. Invalid/zero trials remain in the
fixed denominator and cannot be selected.

The candidate loop remains capped at the existing 48-proposal D0 per-vertex
ceiling and adds no ray, descriptor, ABI field, resource, clear, or dispatch.
Focused and full Vulkan builds passed and 23 rebuilt UPT-04 blobs were deployed.
The additional candidate-evaluation cost and visual/energy result require
runtime acceptance before this correction is committed or reuse work resumes.

gi2.txt finding and secondary-hit normal correction
----------------------------------------------------

The bounded-inner-RIS retest produced no visible improvement, but gi2.txt
proves that the new proposal loop ran. Visibility attempts increased from
4,846 to 106,334 and passed samples increased from 390 to 8,189. Selection
rejection fell to zero. The dominant failure was instead 3,217,063 PDF/ray
rejections after 3,479,829 continuation geometry hits; 3,661,420 of 3,685,241
valid receivers still produced empty canonical reservoirs.

The next direct legacy comparison found that UPT04 decoded each committed
triangle's frontFace value but never used it to orient the hit surface. Unlike
the accepted legacy adapter, a back-side hit therefore entered OpenPBR and the
visibility builder with raw mesh-winding geometric and shading normals. This
can zero the secondary target or fail the light-direction horizon test before
visibility traversal, matching the dominant PDF/ray rejection stage.

Upt04BuildGeometryHitSurface now flips the geometric normal for a back-face
commit, keeps the interpolated shading normal in its hemisphere, and applies
the legacy minimum 0.02 geometric-normal dot constraint. The change is confined
to traced secondary-hit receiver construction and does not alter emissive
replay or direct D0. It adds no work or storage. Focused and full
Vulkan builds passed. The final shared-module build produced and deployed 33
UPT-04, four UPT-07, and two UPT-09 SPIR-V blobs. The executable did not rebuild
and was not recopied. Runtime proof must show a material fall from the 3,217,063
PDF/ray rejection count and a corresponding rise in visibility
attempts/admissions before this follow-up is accepted or committed.

Mirror finding and secondary diffuse correction
-----------------------------------------------

The normal-orientation build remained visually broken. Turning surfaces into
mirrors exposed false-color/placeholder-looking materials throughout
continuation-ray visibility. The two decisive screenshots are
rbdoom-3-bfg-20260808-235808-001.png and
rbdoom-3-bfg-20260809-000031-003.png under the prebuilt base/screenshots
directory.

The static/dynamic/rigid/skinned hit material-index routes match the accepted
legacy sources. The proven mismatch was material interpretation after lookup.
UPT multiplied sampled diffuse textures by debugAlbedo, even though the
material universe commonly fills debugAlbedo with a material-ID hash color.
It also omitted flagged Doom YCoCg decoding and multiplied opaque surfaces by
vertex color. Legacy uses debugAlbedo only as a missing-texture or forced-debug
fallback, decodes YCoCg under r_pathTracingTextureDecode, and reserves vertex
color modulation/fallback for GUI-screen translucent surfaces.

Upt04EvaluateSecondaryDirectProbe now follows those legacy rules. Invalid
material or texture indexes still reject; no fallback is admitted merely to
increase density. The existing texture-decode cvar is transported in a new
material-policy bit, with no push-constant growth. There is no additional ray,
descriptor, reservoir field, buffer, clear, dispatch, or reuse behavior.

The diagnostic-only entry adds a secondary material census distinguishing
valid/invalid material index, forced-debug fallback, missing-texture fallback,
ordinary textured input, YCoCg textured input, and descriptor OOB. Counter
words occupied 0 through 93 in that material-census build. The later
continuation-geometry diagnostic below supersedes that layout. Production
shaders contain none of these atomics.

The focused UPT-04 artifact target and full win64-pt-dev-release preset passed.
The rebuilt 16,292,864-byte executable, exactly 23 affected production Vulkan
UPT blobs, and the subsequently rebuilt diagnostic blob were deployed and
verified. First retest the same mirror overrides: reflected surfaces should
lose the hash tint/raw-YCoCg palette and show their authored diffuse textures.
Then repeat indirect-only family 2 and unified family 0 with
D0 previous-best, temporal, spatial, and duplication disabled. Do not resume
reuse work or commit this follow-up until both material and emissive behavior
are visually accepted.

Mirror geometry follow-up
-------------------------

Runtime confirms the secondary diffuse correction fixed reflected textures.
The mirror experiment separately shows missing level-brush chunks, doors, and
rigid props, with corresponding ghost illumination. Do not merge that evidence
back into the now-closed material bug.

UPT traverses the shared live TLAS with the established 0xff instance mask.
The original audit split TLAS absence from static/static-bucket/dynamic/rigid/
skinned decode failure, but gi8 later proved that split omitted closest-hit
selection: a valid decoded committed hit can still be the wrong, non-nearest
surface.

The diagnostic-only entry now owns 107 counters and adds per-route continuation
hit/decode counts. Probe bases are reservoir=107, D0=123, and C0=139. The new
20-word crosshair C0 record prints continuation validity and ray data plus the
hardware hit identity, classified route, decoded flag, material, and surface
class. No production shader or work ceiling changed.

gi4.txt through gi7.txt close the decode branch: all 13,975,945 committed hits
across the four final captures decoded successfully, every route-specific
failure count is zero, and all four C0 records are valid decoded hits. Each
capture also reports portalSteps=1 reflectionHalo=0. Those facts reject decoder
corruption but do not prove that a committed background hit is nearest.

The first attempted fix widened static-bucket and rigid portal residency. gi8
rejects it: holes remained, duplicated reflected emissives appeared, and GI
temporarily lost all geometry. Its route census was 0 static / 3,277,568
static-bucket, proving that the patch switched the complete static traversal
provider instead of merely adding missing neighbors. The host change is fully
rolled back; do not restore it.

The legacy comparison then found the UPT-only error. Legacy Clean GI resolves
surface continuations without RAY_FLAG_ACCEPT_FIRST_HIT_AND_END_SEARCH. UPT
used that flag on continuation rays, so BVH candidate order could select a
non-nearest wall, door, prop, brush, or emissive triangle. Bucket BLAS changed
candidate order and amplified the bug, which explains gi8. UPT RayQuery and
ray-generation continuations now omit accept-first; visibility rays retain it.
No scene membership, ray/candidate ceiling, reservoir, buffer, clear, dispatch,
or reuse rule changed. The focused artifact gate and full
win64-pt-dev-release preset pass. Exactly 31 rebuilt Vulkan UPT blobs are
deployed with zero hash mismatches. The rebuilt 16,294,400-byte executable is
deployed with matching SHA-256
74A6FBA7ECC384516ACEC7D40EE986D932DB1A1B63A95AF826C21B6DFCEDFAE2.
Selected production SPIR-V disassembly reports continuation RayQuery flags=1
and visibility flags=6, so the closest-hit/accept-first separation is present
in the deployed blob.
Mirror runtime acceptance is required before a commit.

Mirror acceptance and remaining alpha-cutout correction, 2026-08-09
--------------------------------------------------------------------

Runtime accepted the closest-hit correction: the reflected door/prop/world
holes, hovering duplicate emissives, and transient all-black GI failure are
effectively fixed. The remaining obvious defect was alpha-tested geometry in
front of several light panels being treated as a fully opaque indirect hit or
occluder.

The selected Vulkan RayQuery path now exposes candidates for the continuation
and the indirect secondary-NEE visibility ray. It resolves the existing UPT
route/triangle identity, interpolates the stored UV, samples current alpha or
diffuse coverage using the legacy alpha-test key/decode rules, and skips only a
proven texel below alphaCutoff. Missing route/material/texture state commits as
solid geometry, preserving the fail-closed no-hole rule. Primary direct
visibility and every reservoir/reuse rule are unchanged.

Material48 did not grow. Its two unused packed color W lanes now carry
alphaTextureIndex and alphaCutoff; SPIR-V reflection still reports a 48-byte
array stride. The focused UPT artifact target and full
win64-pt-dev-release preset pass. Exactly 26 rebuilt Vulkan UPT blobs are
deployed with zero hash mismatches; the engine executable was unchanged and
still matches the deployed SHA-256
74A6FBA7ECC384516ACEC7D40EE986D932DB1A1B63A95AF826C21B6DFCEDFAE2.
No DX12 shader was compiled. Production disassembly proves continuation
flags=2, candidate texture sampling plus conditional commit, and indirect
visibility flags=6. Runtime accepted the alpha-cutout correction; the user
reports all previously identified basic indirect-lighting defects fixed. This
is the checkpoint boundary before any multi-bounce or indirect-reuse work.

Latest external evidence
------------------------

The latest console captures are under:

    C:/Users/lizard/Saved Games/id Software/RBDOOM 3 BFG/base/

Useful names include case1.txt through case6.txt, capture1.txt through
capture8.txt, emissive.txt, emissive2.txt, and emissive_inventory.txt.
capture7/capture8 established that bucket routing affected color but not the
portal-dependent disappearance. These are external evidence, not repo inputs.

Next implementation sequence
----------------------------

1. D0 emissive acquisition is complete and runtime-accepted. Preserve the
   exact current CDF count contract, previous-best rejection rules, one-ray
   ceiling, and stage diagnostics as regression coverage.

2. The separately authorized first indirect continuation and secondary NEE
   path-tree slice is implemented and build verified. Preserve one shared
   selected path; do not create separate DI and GI reservoirs or tag-specific
   Lambert kernels. The following required corrections are now present:

       keep bare physical NEE density distinct from proposal-parity source PDF;
       use the multi-sample typed-mixture density for NEE MIS;
       make reverse emissive-hit density match forward power-CDF and triangle-
           region sampling;
       publish/use logical reverse-hash capacity with physical OOB guarding;
       retain fail-closed inexact-lookup handling;
       use accepted direct ray offsets and emissive self-ignore;
       expand diagnostics without occupying reservoir-probe words 69..84.

   Candidate-count-one UCW/parity behavior, previous-best composition, and the
   need for a new lobe-sequence ABI remain focused runtime/test questions rather
   than guessed design changes. First prove the no-history estimator with
   previous-best off. Require candidate-count-one output bit identity to plain
   NEE and converged mean-energy agreement among NEE-only, endpoint-only, and
   combined modes. Re-enable previous-best only after those tests pass.

3. Validate the unified image before adding more reuse policy. Compare direct-
   only versus unified D0/R0 with T0/S0 disabled first, then admit temporal and
   spatial shift providers for indirect paths explicitly. A partial provider
   must reject an unsupported history event without changing current M/age.

4. Only after the one-continuation unified path is correct, extend toward the
   user-validated three-bounce/Russian-roulette target in UPT-10A. No current
   ABI choice should prevent it, but no extra queue/buffer/dispatch is admitted
   without measured justification.

Build and deployment
--------------------

From neo/:

    cmake --build --preset win64-pt-dev-release

Deploy:

    build-win64-pt-dev/Release/RBDoom3BFG.exe
        -> E:/prog/rbdoom-3-BFG-prebuilt/RBDoom3BFG.exe

Copy only rebuilt Vulkan SPIR-V under:

    base/renderprogs2/spirv/

Do not compile or deploy DX12 shaders. Slang UPT targets are declared in
neo/shaders/CMakeLists.txt and emit under their slang_uptXX directories.

Gotcha: editing an HLSLI or RT HLSL include may not retrigger DXC. Delete the
affected .rt.bin before the build and verify the rebuilt blob's timestamp is
newer than the source edit. Always verify deployed EXE/blob size and mtime when
a runtime A/B shows no change.

Documentation ownership
-----------------------

    02  exact GPU resource/work ledger
    03  reservoir validity, page, duplication, and estimator invariants
    12/13  D0 admission and corrected ReSTIR PT Enhanced path-tree authority
    20  two-page history/no-clear/serial contract
    21  T0 implementation, visibility, stable identity, and temporal controls
    22  S0/UPT-08 experiments and accepted direct-reuse sub-policy
    23  this current handover and continuation sequence

When a runtime result changes an accepted default or invalidates a hypothesis,
update the owning numbered document in the same change. Do not let this handover
become a second authority that silently diverges from those contracts.

Temporal-indirect implementation checkpoint, 2026-08-09
-------------------------------------------------------

Work has started on the next authorized task. The false replayable label was
corrected first: all one-continuation and secondary inner-RIS random dimensions
now regenerate from persisted replayKey/replayIndex without a source-pixel
dependency. The 64-byte reservoir ABI, compact32 path, proposal parity,
duplication identity, and D0 work/ray bounds are unchanged.

UPT-07 now has separate direct and opt-in unified Vulkan variants. The latter
is selected only by r_pathTracingUnifiedPtTemporalIndirect 1 and implements
stable-light validation, continuation retrace, routed closest-hit/material
decode, exact endpoint replay, complete secondary inner-RIS reconstruction,
alpha-clipped winner visibility, reciprocal target evaluation, and stage-
specific resolve-view-5 rejection reasons. It adds no page, queue, sidecar,
clear, or dispatch. Static closure and CPU contract harnesses pass. Runtime
visual/energy/performance validation is the current gate; keep spatial reuse
disabled until that gate is accepted.

First runtime integration correction, 2026-08-09: the unified temporal layout
initially appended SRV register 23 before optional indirect SRV register 22,
while the binding set supplied ascending 22 then 23. NVRHI rejected binding-set
items 20/21 before dispatch. The layout now inserts optional register 22 before
register 23 for the unified variant; the direct variant retains register 23 in
the same item position it had before indirect replay was added. The full Vulkan
release build passes after this host-only correction. Runtime image validation
remains pending.

Runtime then reported the unified path mostly working, with an Nsight baseline
of D0 34.51 ms and T0 43.32 ms. The first performance correction defers BASIC
current-at-history global replay until the mass selection says the current
sample wins; mature-history winners no longer pay a discarded second complete
continuation/inner-RIS/visibility replay. Pairwise MIS remains eager. The ABI,
selection random, merge math, and maximum ray ceiling are unchanged. Rebuilt
UPT-07 Vulkan blobs are deployed; same-scene timing validation is the current
gate before any spatial implementation.

D0 family capture result, 2026-08-09
------------------------------------

The seven supplied Nsight reports were read as separate report windows, not as
one active tab. With temporal/spatial off, exact D0 durations were:

    parity=1  family 0/1/2        29.86 / 5.12 / 11.77 ms
    parity=0  family 0/1/2        29.96 / 14.38 / 14.38 ms
    parity=0  family 0 trials=1   22.39 ms

The corrected parity-0 batch is almost additive: its 14.38 ms direct-only plus
14.38 ms indirect-only total is 28.76 ms versus 29.96 ms unified. The large
unified penalty is therefore parity-1-specific. Its specialized direct shader
falls to 5.12 ms, but unified remains 29.86 ms; the expected direct saving is
lost when parity-direct and indirect transport share the shader. The final
trials=1 capture is also not a valid work-count comparison: the parity=0
fallback shader always executes its fixed eight-trial schedule and ignores the
configured emissive-trial count. Its 22.39 ms result is capture/scene/GPU-state
variance, not evidence that trials=1 saved 7.57 ms.

The valid parity=1 split is decisive: the 29.86 ms unified megakernel costs
12.97 ms more than direct-only plus indirect-only (16.89 ms). This is the new
measured D0 critical path. Candidate count alone cannot explain it, and broader
light filtering or reuse changes are not justified. Before adding a new queue
or shader boundary, remeasure the existing exact r_pathTracingUnifiedPtSplitInitial
A/B in this same view and add D0a + barrier + D0b. The earlier split rejection
was measured against an approximately 0.9 ms direct-scene monolithic D0; the
current parity-1 12.97 ms family-interaction excess changes that break-even
condition. Parity 0 is the control and currently has only 1.20 ms unified
overhead.

The subsequent Nsight shader-source CSV export confirms the parity-1
monolithic symptom: 60.67 percent No Instructions, 12.44 percent Long
Scoreboard, and 12.09 percent Wait across 3,770,861 samples. The user's
specialized direct/indirect/unified comparison is approximately 0/40/60
percent No Instructions. The two supplied CSV files are byte-identical and
describe the same monolithic shader, not D0a versus D0b. Treat this as evidence
for instruction-fetch/code-locality and latency-hiding failure in the combined
shader; it is not evidence for an estimator requirement to fuse dispatches.

Monolithic-first optimization authority, 2026-08-09
---------------------------------------------------

The current performance direction is now 24_upt10_monolithic_first_optimization_plan.txt.
Do not infer from the current family-0 penalty that a split is required. First
remove repeated receiver/material preparation, candidate live state, and
production-reachable policy/debug work while retaining the monolithic unified
reservoir path. The strongest first target is the secondary NEE loop, which
currently reloads the same material and diffuse/specular textures for every
light candidate instead of preparing the secondary receiver once.

ReSTIR PT Enhanced's principal applicable structural saving is one unified
DI/GI reservoir and one temporal/spatial history chain rather than parallel DI
and PT pass families. Russian roulette is deferred because UPT currently has
only one continuation. A physical D0/T0/S0 split remains legal, but only as an
execution detail over the same reservoir and only after the cleaned monolithic
path is reprofiled. The existing SplitInitial A/B is the first escalation gate;
do not create separate DI/GI products or a new queue pre-emptively.

UPT-10A now implements the first prepared receiver closure. Primary material
state is prepared once before direct RIS, and secondary Doom material/texture
decode is prepared once per continuation hit rather than once per secondary
light candidate. All estimator, ray, ABI, binding, and dispatch contracts are
unchanged. Focused UPT-04/UPT-07 builds, the full Vulkan release preset, static
shader gates, and the UPT-04/UPT-07 CPU harnesses pass. The primary production
D0 blob fell by 5,792 bytes to 640,616 bytes. Runtime image and timing validation
is the next gate before UPT-10B.

UPT-10A runtime and UPT-10B candidate-state result, 2026-08-09
---------------------------------------------------------------

The user reported no change at all in Nsight from the deployed UPT-10A build.
Prepared material ownership remains correct, but it is not the dominant GPU
stall source. No image regression was reported, so the next bounded 10B slice
was implemented.

D0 now constructs one immutable light schedule per receiver and passes it into
the fresh direct and secondary NEE candidate loops. A candidate carries only
the visibility endpoint rather than a full trace request; the request is built
once for the selected winner. Event and stable identity fields are derived
after selection. Candidate counts, random dimensions, proposal/target PDFs,
fixed-M normalization, duplicate exclusion, visibility eligibility, ray
ceilings, reservoir ABI, descriptors, and dispatches are unchanged.

A selected-coordinate replay version was rejected because it grew the main
blob to 703,144 bytes. The retained version reduces the main 10A blob from
640,616 to 638,056 bytes and direct-only by 3,272 bytes, while indirect-only
grows 784 bytes. The full Vulkan build and static/CPU gates pass. Do not call
this a performance win until the deployed family 0/1/2 parity-1 markers are
recaptured; if timings remain unchanged, proceed to UPT-10C specialization
cleanup rather than adding more candidate math changes.

UPT comparison-pressure trial, 2026-08-09
-----------------------------------------

The four newest GPU Trace files are family 0/1/2 followed by family 0 with
reuse cvars enabled. The last report embeds D0, unified T0, and U0 markers but
no S0. This is not a capture failure: current host policy only enables UPT-09
spatial for DirectOnly family 1, so family 0 cannot execute spatial yet.

Static family 0/1/2 SPIR-V contained 545/244/308 OpIsNan and 542/243/305
OpIsInf instructions. The family-0 footprint is additive and comes largely
from Upt04FinitePositive expanding inside the direct and indirect candidate
loops. It now uses an exact binary32 bit-range predicate instead of floating
isfinite plus greater-than. Invalid/zero samples remain rejected.

The main D0 blob falls 15,456 bytes to 622,600 and loses 1,272 SPIR-V
operations. Direct-only falls 8,560 bytes/705 ops; indirect-only falls 6,880
bytes/566 ops. The 72 removed unified predicate expansions equal direct 40
plus indirect 32. T0 unified base falls 22,720 bytes to 900,364; S0 direct base
falls 15,104 bytes to 423,892. Full Vulkan/static/CPU gates pass. Treat these
as static wins only until fixed-scene timing and image parity are reported.

UPT-11 light-tile A/B handover, 2026-08-09
------------------------------------------

The exact-PDF light-tile experiment in document 25 is built and deployed. It
is deliberately narrower than RTXDI's complete presampling system: it moves
only light-identity proposal into coherent per-frame tiles while D0 continues
to load and validate the authoritative current light record and replay the
selected triangle coordinates. Separate emissive-CDF and analytic-ordinal
domains preserve the accepted proposal mixture and portal/replay behavior.

The route is opt-in through `r_pathTracingUnifiedPtLightTiles` and is admitted
only by the family-0 split/native/parity-1 production configuration. Offline
proof is complete: Vulkan build passes, five UPT contract tests pass, binding
30 is present in both split shaders, and the ray ceiling remains 0+1+2 across
presample/D0a/D0b. Runtime image parity and complete-scope Nsight timing are
still required before accepting or checkpointing UPT-11.

UPT-11 rejection and UPT-12 handover, 2026-08-09
-------------------------------------------------

The user reported no measurable Nsight change from light tiles, so UPT-11 is
rejected and `r_pathTracingUnifiedPtLightTiles` remains zero. Advanced Nsight
CSV exports then isolated a direct-only 112-byte whole-light load at 129,523
dependency samples and 7,206 cycles while indirect already used native uint4
loads. UPT-12 mechanically enables that existing view for split D0a and the
compact32 direct-only profiling artifact.

Static proof now reports zero whole light-record loads, 18 uint4 load sites,
and one ray query in both direct artifacts. The full Vulkan build and five UPT
CPU contract tests pass. Runtime image parity and D0a/complete-D0 timing are the
remaining gate. If the measured change is negligible, proceed to indirect D0b
register/footprint and bounded replay-compaction work described in document 26.

UPT-12 rejection and UPT-13 handover, 2026-08-09
-------------------------------------------------

The user observed no change from UPT-12. Keep the exact native direct-light view
as neutral cleanup, but reject it as a performance solution and stop pursuing
current-light load width. D0b remains the dominant pass and the advanced source
capture places 91.19 percent of its samples above 64 live registers.

UPT-13 stages the production split-RayQuery indirect preparation so the
continuation endpoint is streamed before secondary NEE winner construction.
It no longer returns endpoint and pending NEE as two full work records in one
aggregate. The compatibility wrapper preserves raygen and diagnostics. Static
output retains two RayQuery sites and zero whole light loads; native D0b falls
876 bytes to 355,304 while operation count rises by 60 to 21,765. Treat this as
a runtime register/latency A/B only.

The ReSTIR PT Enhanced section 6.2.2 replay compaction is explicitly not the D0
mechanism: it compacts sparse temporal/spatial pixel-neighbor replay pairs. D0b
has no such pair list, so no queue is authorized by that section. See document
27 for the fixed-sample and complete-marker acceptance gate.

The full Vulkan release preset and all five PathTraceUnified contract tests
pass. The executable and 26 rebuilt Vulkan UPT blobs are deployed with zero
hash mismatches. No DX12 shader was compiled or deployed. Runtime D0b register
and complete-marker evidence remains the acceptance gate; no checkpoint commit
has been made for this unmeasured experiment.

UPT-13 rejection and UPT-14 handover, 2026-08-09
-------------------------------------------------

Runtime rejected UPT-13: registers decreased, but the pass slowed and No
Instructions rose from about 38 to 48 percent. Its staged endpoint/secondary
refactor is rolled back completely.

The next audit found a larger algorithmic mismatch. The paper uses 32 / i^2 NEE
trials by bounce index, but UPT used the complete primary schedule at the first
secondary hit. UPT-14 leaves primary NEE byte-for-byte unchanged and reduces
the parity-1 default secondary schedule from 32 analytic + 1 emissive to 8 + 1.
D0 selection/denominator/source PDF, endpoint reverse MIS PDF, and T0 random
replay all use the same decayed domain counts. The content-generation hash now
includes secondary bounce index 2.

Static native D0b is 356,568 bytes with 21,726 operations and two RayQuery
sites. Direct native artifacts are byte-identical to the prior build. T0 unified
base falls 8,336 bytes to 892,028. The full Vulkan build and five UPT tests pass.
Runtime fixed-sample image and complete D0 timing remain the acceptance gate;
the executable and exactly 30 changed Vulkan D0/T0 artifacts are deployed with
zero hash mismatches. No DX12 shader was compiled or deployed. No checkpoint
commit has been made.

UPT-14 runtime and UPT-15 temporal handover, 2026-08-09
--------------------------------------------------------

Runtime retained UPT-14 as an approximately 1 ms elapsed-time improvement.
No Instructions and SM utilization percentages worsened, but those ratios grew
after arithmetic was removed while the same ray-latency phases remained. Do not
roll back a measured elapsed-time win based on those percentages alone.

A different-level A/B then isolated unified indirect temporal at 10x to 20x
the cost of direct temporal plus spatial. The concrete cause was the secondary-
NEE branch in upt07_temporal_indirect.slang: every shifted path re-ran all nine
secondary candidates, including a dependent emissive-CDF binary search per
emissive proposal. This is section 6.2.3's forced NEE reconnection target.

UPT-15 changes the secondary D0 winner from folded-UCW representation to the
algebraically equivalent raw contribution/target plus effective proposal PDF.
The streamed inner-RIS mass and final resolved radiance are unchanged, while
the physical target required by temporal shift is retained. UPT content ABI 9
invalidates older folded history.

T0 now remaps and fingerprints the stored light, reconstructs its exact current
typed PDF, random-replays only the continuation, directly replays stored
emissive barycentrics or analytic coordinates, evaluates the current target,
and traces selected visibility. It has no candidate loop, CDF entry access, or
fallback admission. The 30-binding layout and five static RayQuery sites remain
unchanged; the CDF binding is retained only by structural length/stride proof.

The final unified base T0 is 815,444 bytes versus 892,028 before UPT-15, with
51,440 operations, five RayQuery sites, two structural CDF length queries, and
zero CDF entry access chains. Native split D0b is 356,432 bytes. The exact full
Vulkan release build and all five PathTraceUnified tests pass. The matched
16,303,616-byte executable and exactly 30 hash-different Vulkan D0/T0 blobs are
deployed with zero mismatches; no DX12 shader was compiled or deployed.

Runtime temporal image/energy and complete-marker timing are now the next gate.
Spatial indirect remains blocked. See document 29 for the estimator proof and
rejection checklist.

UPT-15 runtime acceptance, 2026-08-09
-------------------------------------

Forced NEE reconnection reduced the temporal scope from about 34 ms to 20 ms
on a Doom 3 level and from about 20 ms to 7.85 ms on a small level. Accept the
change: it removes a proven level-complexity multiplier. The small-level result
is still roughly seven times the direct-temporal-plus-spatial cost.

The next concrete audit found selected-light visibility still executes inside
every secondary-NEE replay, before history selection, and also during the
reciprocal current-at-history call whose only consumer is a scalar target.
Direct T0 already performs winner-only visibility. UPT-16 should return a
transient indirect visibility request from history-at-current replay, omit the
ray from target-only replay, and trace it only if shifted history wins. Do not
reconstruct the continuation a second time and do not add a reservoir field or
sidecar.

UPT-16 implementation handover, 2026-08-09
-------------------------------------------

The audited winner-only visibility change is implemented. Secondary-NEE replay
now returns an in-register request and no longer traces it eagerly. Reciprocal
current-at-history replay discards that request after target evaluation. After
merge, only a shifted indirect history winner lacking proof uses
Upt07TraceIndirect, retaining alpha clipping and emissive self-ignore. Failure
or occlusion publishes the original current D0 reservoir. Endpoint continuation
proof and the established direct winner query are unchanged.

Static unified T0 RayQuery sites fall from five to four. The base Vulkan blob
falls from 815,444 to 793,796 bytes, with the same 21,648-byte reduction in all
four unified temporal variants. The full Vulkan release build and all five
PathTraceUnified tests pass. The executable is byte-identical to the deployed
UPT-15 build. Exactly the four unified temporal Vulkan blobs are deployed with
matching source/destination hashes; no DX12 artifact was compiled or copied.

Runtime acceptance remains open. Compare against the accepted 20 ms Doom 3
level and 7.85 ms small-level UPT-15 baselines, and verify fixed-camera energy,
alpha-clipped occluders, moving doors, emissive/analytic lights, portal motion,
and absence of leaked unshadowed history. Do not begin spatial indirect until
UPT-16 is accepted or corrected. See document 30.

UPT-16 runtime and hybrid-shift audit, 2026-08-09
-------------------------------------------------

UPT-16 is retained after measuring approximately another 1.5 ms temporal win.
Within one level, moving outside also roughly halved GI temporal cost; denser
nearby lighting correlated with higher cost. Since UPT-15 removed temporal
candidate/CDF loops, the leading explanation is secondary-NEE event occupancy:
more such selected history events execute more unconditional continuation
closest-hit replays.

The paper's true hybrid-shift observation is not implemented yet. Current T0
replays and retraces to x2 for every eligible indirect sample, then reconnects
only the stored light. However, the claim that the reservoir already contains
x2 is false for secondary NEE: its four identity/coordinate fields contain the
selected light, while reconnectionVertexLength is only a tag and
partialJacobian remains 1. The existing 32-byte continuation-hit buffer is a
single current-frame, split-D0 buffer from a rejected performance A/B and has no
stable previous-geometry identity contract.

Do not simply delete Upt07ReplayContinuation or ping-pong that full sidecar.
First prove the indoor/outdoor event-occupancy hypothesis with diagnostic-only
route counts, then design a compact stable secondary-vertex representation and
an exact rough/footprint reconnection plus Jacobian evaluator. Preserve random
replay as the specular/ineligible fallback and UPT-16 final winner visibility.
See document 31.

UPT-17 static hybrid reconnection implementation, 2026-08-10
-------------------------------------------------------------

The bounded static route is implemented without a sidecar or page growth.
Secondary-NEE reservoirs event-union dormant words 13..15 into static x2
instance/front-face, primitive, and UNORM16 barycentrics; ABI 10 invalidates
older pages. Dynamic, rigid, and skinned paths retain exact random replay.

The dedicated reconnect temporal blobs apply roughness 0.2 and footprint
constant 0.02 percent, visibility-test y1-to-x2, evaluate source/target OpenPBR
directional PDFs and the hybrid-shift Jacobian, scale BASIC history mass by the
Jacobian, then retain UPT-15 stored-light replay and UPT-16 winner-only final
visibility. Pairwise remains on replay. The off blobs retain the UPT-16
30-binding/four-query code shape; reconnect blobs use 31 bindings and six
static branch-dependent query sites. A 16-counter diagnostic readback reports
locator, footprint, reconnect visibility, fallback, evaluation/Jacobian, and
history-selection stages.

The exact Vulkan release build and shader verifiers pass. Runtime A/B remains
open: compare r_pathTracingUnifiedPtTemporalEarlyReconnect 0/1 with pairwise
off, first indoors and outdoors on the same level, then enable
r_pathTracingUnifiedPtTemporalReconnectDiagnostics briefly to explain coverage.
Do not begin spatial indirect until image, energy, and timing evidence accepts
or rejects this slice. See document 32.

UPT-17 rejection and UPT-18 state-variance handover, 2026-08-10
----------------------------------------------------------------

Static early reconnection measured about 6 ms slower and remains default off.
Do not promote that branch from the current result.

Baseline indirect temporal, also with reconnection off, varied by up to 7 ms
in the same view after analytic-intensity changes and movement. The audit found
a live state-dependent workload: indirect history performs one replay, and a
BASIC current selection can require a second reciprocal replay. Analytic
intensity does not invalidate the UPT generation, but it can cause a one-frame
analytic payload-remap rejection and reshape retained event mix. Motion can
reshape it further. Camera/cvar-only Nsight comparisons are insufficient.

Use r_pathTracingUnifiedPtTemporalRouteDiagnostics 1 briefly to collect the
aggregate route line, then disable it before timing. Compare slow, fast, and a
temporal off-for-one-frame/on reset state as specified in document 33. The
diagnostic uses separate Vulkan blobs; production temporal remains untouched.
Do not begin spatial-indirect work until this variance is explained.

UPT-19 hardware-opacity correction handover, 2026-08-10
--------------------------------------------------------

The unexplained temporal variance led to a lower shared traversal defect.
Production UPT and clean RTXDI rays forced non-opaque, while the smoke and
static-bucket BLAS never supplied GeometryFlags::Opaque. Every triangle was
therefore exposed to programmable candidate handling, including material and
alpha work, and scene overlap could multiply SM round trips.

The correction partitions the existing index order into fixed 256-triangle
geometries and marks a chunk opaque only if all resolved materials are proven
hardware-safe. Uncertain, alpha-tested, glass, decal, liquid and fallback
materials remain programmable. Shaders recover the unchanged source identity
as GeometryIndex * 256 + PrimitiveIndex; no reorder, remap buffer or full clear
is introduced. UPT production rays and clean RTXDI visibility stop forcing
non-opaque, relying on the existing distance-minus-0.5 endpoint shortening.

Use r_pathTracingHardwareOpaqueGeometry 0/1 for the rollback A/B and
r_pathTracingSmokeLog 1 briefly to prove opaque/programmed chunk populations.
The next gate is runtime alpha/geometry identity plus matched Nsight captures,
not further UPT reservoir optimization. See document 34.

UPT-19 build and deployment closure, 2026-08-10
------------------------------------------------

The focused CPU planning harness passes the 257-triangle 256+1 chunk boundary.
The prescribed win64-pt-dev-release build completes with all UPT Vulkan
verifiers passing and a 16,312,832-byte executable. DXIL shader generation was
disabled in the preset cache.

The exact build also exposed an already-dirty clean-GI producer shim that
accepted only the main payload while parsing a shadow-payload TraceRay. After
deleting its stale Vulkan blob, a minimal shadow-payload overload rebuilt it at
363,016 bytes and the full build passed. Deployment contains the executable and
the 105 SPIR-V artifacts actually rebuilt in this session, with matching
source/destination sizes and SHA-256 hashes; no DX12 artifact was copied.

Runtime validation remains the acceptance gate. Compare
r_pathTracingHardwareOpaqueGeometry 0/1 in identical states, prove nonzero
opaque/programmed chunk counts with r_pathTracingSmokeLog 1, then inspect alpha
clips, glass, decals, doors, mirrors, rigid/skinned geometry and emissive
self-visibility before recording matched UPT and clean-RTXDI Nsight captures.

UPT-19 downstream correction before runtime testing, 2026-08-10
----------------------------------------------------------------

Do not test the earlier UPT-19 deployment: two downstream contracts were
corrected first. Static and static-bucket BLAS reuse now includes a separate
per-chunk hardware-opacity signature, so a registry-only alpha/coverage change
cannot leave stale GeometryFlags::Opaque on an otherwise unchanged mesh.
This preserves the geometry-only cache rule for material edits that do not
alter opacity classification.

UPT alpha traversal now resolves the material index and loads its flags before
indices, vertices, the full material record or textures. Ordinary opaque
candidates therefore stop at the narrow classification load; only proven
alpha candidates run the full fail-closed coverage test. The canonical
GeometryIndex * 256 + PrimitiveIndex normalization remains unchanged.

The focused CPU planning harness passes, including the new opacity-token case.
The exact Vulkan release build passes all UPT verifiers and links a
16,313,856-byte executable. The follow-up rebuilt 21 Vulkan SPIR-V artifacts;
DXIL generation remained disabled. Deploy this follow-up before the first
runtime correctness/performance A/B.

UPT-19 CPU regression correction, 2026-08-10
---------------------------------------------

The first downstream build must not be used for timing. opaqueissue.txt proved
169--189 ms CPU scene builds with only about 20 ms of GPU activity. The new
opacity signature was copying the large material metadata record once per
triangle, multiplied by repeated static-bucket readiness checks.

Hardware-opacity eligibility is now precomputed once per derived registry
record and consumed as a one-bit lookup by signature walks. The conservative
classification and invalidation behavior are unchanged. The exact Vulkan
release build passes with all SPIR-V already current; only the rebuilt
16,314,368-byte executable needs deployment for this correction. Recheck CPU
scene-build time before collecting GPU comparisons.

UPT-18 fresh-endpoint visibility correction, 2026-08-10
--------------------------------------------------------

temptests.txt proved that maximum history age 1 reduced inferred indirect ray
work by about 44 percent, while exact single-tap temporal search did not change
the age-32 workload. It also exposed visibilityFallback rising from about 424k
to 877k as BASIC selected more current samples.

The cause was a narrow D0/T0 contract mismatch. A D0 BSDF continuation-hit
endpoint is visible by construction, but Upt04PrepareIndirectHitTrials did not
persist VisibilityKnown|VisibilityPassed. T0 therefore treated fresh endpoint
winners as missing-proof indirect shifts, failed closed, and discarded the
merged reservoir state. D0 now records the proof before packing; history replay
and secondary-NEE visibility rules are unchanged. Re-run the age-1 reset with
route diagnostics briefly enabled, then time the normal temporal blob with the
diagnostic off.

The exact Vulkan release build and the new static proof verifier pass. The 26
rebuilt UPT-04 SPIR-V blobs were deployed with matching hashes. The executable
remained the prior verified 16,314,368-byte build with SHA-256
FAAB471E658CC8614366503365A3F4D8D722A14EC0B0B11487C2A8630ECACA2D and was
redeployed as requested. No DX12 shader was compiled or deployed.

UPT-19 rejected rigid-instance opacity trial, 2026-08-10
--------------------------------------------------------

wall.txt and orbs.txt show that the small-map twofold timing swing is not a
ray-count or temporal-history-age effect. D0 continuation rays remain about
3.51 million, D0 visibility rays remain about 7.1 million, T0 history replay
remains about 455--457k and reciprocal replay remains about 149--154k. The orb
view instead changes rigid continuation hits from 14,882 to 463,694 and raises
secondary material/PDF/occlusion failures by 30--197 times.

The shared canonical rigid BLAS provider is material-independent and its one
geometry is non-opaque, so a conservative trial supplied ForceOpaque or
ForceNonOpaque from the effective rigid instance material. The build and
deployment passed, but orbs2.txt showed no performance change. The instance-
opacity trial is therefore rejected and rolled back rather than retained as
unused traversal risk.

This negative result narrows the remaining mechanism. The map reports about
2.22 million dynamic indices and the orb diagnostic now records 507,133 rigid
continuation hits. With almost unchanged D0/T0 ray counts, the view-dependent
cost is rigid BLAS traversal or committed-hit decode/shading, not temporal age,
history population, light selection or programmable opacity. Use the existing
split-continuation C0 marker to compare pure exact continuation traversal in
the wall and orb views before another implementation change. See documents 33
and 34.

The opacity code was removed, the prescribed Vulkan release rebuild passed,
and no SPIR-V shader changed. Only RBDoom3BFG.exe was redeployed; source and
destination match at 16,314,368 bytes and SHA-256
70E0C76921B11A13E70C0E88597B5657D6DF9D0E577C4BF84D0AFEB722D4E6F6.

UPT-20 admission: temporal replay compaction, 2026-08-10
--------------------------------------------------------

The continuation split was admitted and captured in the PBR-orb map. The wall
capture measures frame/C0/D0/T0 at 14.92/0.28/6.64/4.79 ms; the orb capture
measures 18.46/0.80/6.92/8.23 ms. C0 proves geometry-sensitive traversal, but
T0 owns 3.44 ms of the 4.24 ms summed marker delta and D0 shade is nearly flat.
The existing T0 shader can trace history replay, reciprocal current-at-history
replay and final winner visibility in one full-screen divergent kernel. This
is now the measured paper-shaped compaction target. Document 35 defines the
bounded implementation; opacity classification, light selection, temporal age
and broader portal filtering are closed as explanations for this swing.

UPT-21 build-ready diagnostic: static Lambert material isolation, 2026-08-10
-----------------------------------------------------------------------------

To test whether OpenPBR execution or transitive shader footprint owns the
remaining view-sensitive cost, UPT now has a separate constant-gray Lambert
specialization selected by r_pathTracingUnifiedPtLambertDiagnostic. It replaces
primary and secondary receiver BSDF work without runtime-branching around a
still-linked OpenPBR closure. Real emitter textures/colors, alpha clipping,
geometry/light identities, reservoir math, proposal schedule and ray ceilings
remain unchanged. The content-generation hash includes the mode.

The full Vulkan release build passes all D0/T0/S0 Lambert artifact and closure
checks and links RBDoom3BFG.exe. Runtime pipeline logs explicitly report
shading=lambert-diagnostic or shading=openpbr. Document 36 owns the supported
gate, matched A/B procedure and interpretation. No runtime performance claim
has been made yet and no DX12 shader was compiled.

UPT-21 runtime result: Lambert is not the heavy-area bottleneck
----------------------------------------------------------------

The static Lambert specialization makes temporal about 3 ms faster in the
PBR-orb view and produces no measurable temporal change in the heavy Doom-level
area. OpenPBR is therefore a real local cost in the material-stress scene but
is rejected as the dominant cause of the production heavy-area T0 slowdown.
Because Lambert also changes continuation directions, the orb delta is not a
pure instruction-cost measurement. Continue with indirect replay/traversal
scheduling, light-count scaling and dependent memory traffic; do not pursue a
broader OpenPBR simplification as the next optimization.

UPT-22 build-ready diagnostic: temporal bottleneck ladder, 2026-08-10
---------------------------------------------------------------------

Five static Lambert/compact64/duplication T0 probes now execute cumulative work
and then republish the untouched D0 reservoir. A per-pixel digest at binding 31
keeps the selected calculations live without feeding partial temporal results
into the next frame. Their compiled sizes grow from 71,684 to 712,112 bytes and
their static RayQuery sites grow 0, 1, 2, 2, 4, proving that the intended stages
survived optimization.

Use r_pathTracingUnifiedPtTemporalBottleneckProbe 1..5 at one fixed heavy-area
camera. Adjacent T0 marker deltas price receiver/history work, first indirect
replay, reciprocal/cross evaluation, merge and final visibility respectively.
Mode 0 remains production. Document 37 contains the exact gate, capture matrix
and decision rule. The Vulkan release build passes; runtime results remain the
acceptance gate and no DX12 shader was compiled.

UPT-22 first-run correction
---------------------------

probe.txt proves that none of the requested probe modes engaged: D0 reported
geometry=legacy112 lights=legacy112 and T0 remained bottleneckProbe=0. Compact
lights are subordinate to r_pathTracingUnifiedPtCompactGeometry, which was
missing from the abbreviated test instructions. Set CompactGeometry 1 before
timing and require D0 geometry=compact48 lights=compact64 plus T0 lightStride=64
and bottleneckProbe=N. The host now logs requested/effective probe mode and all
six gate bits whenever the request or effective mode changes.

UPT-22 valid ladder run and timestamp instrumentation
------------------------------------------------------

probe1.txt proves that all five requested probe stages engaged: every request
matched its effective mode, all six gate bits were one, T0 reported the expected
stage-specific blob and D0 reported compact48 geometry plus compact64 lights.
The condump contains no steady-state GPU pass timing, however; `createUs` is CPU
pipeline creation latency and cannot rank the stages.

The host therefore owns an opt-in dispatch-only Vulkan timestamp batch through
r_pathTracingUnifiedPtTemporalGpuTiming. It ignores 16 valid-history warm-up
dispatches, gathers 64 samples, and reports median/mean/minimum/p90/maximum for
the effective stage. Changing the probe stage automatically rearms the batch.
This removes Nsight capture overhead and manual overlay transcription from the
decisive T1..T5 measurement while leaving the renderer untouched when disabled.

UPT-22 measured result and reciprocal sub-ladder
------------------------------------------------

The 2560x1440 timestamp batch measured P/T1/T2/T3/T4/T5 medians of
13.501/1.293/2.261/8.749/9.092/11.546 ms. Reciprocal/cross evaluation therefore
adds 6.488 ms, while merge adds only 0.343 ms. That is the decisive hotspot;
receiver/history, first replay and merge are not the next optimization lane.

Static probe modes 6--9 now split the hotspot after bookkeeping/BASIC
selection, current-receiver target, direct cross-receiver target, and global
reciprocal traversal plus full hit decode. Existing mode 3 supplies the final
post-hit endpoint/light/material/PDF boundary. SPIR-V inspection proves modes
6/7/8 retain one RayQuery, mode 9 introduces exactly the second, and mode 3
keeps two while adding the post-hit closure. The Vulkan build passed. Only new
probe6--9 blobs and the executable were deployed with matching hashes; no DX12
shader was compiled.

UPT-22 reciprocal T9 request/query/decode split
-----------------------------------------------

Modes 10 and 11 refine the measured 2.535 ms T9 traversal/decode interval.
Mode 10 retains reciprocal identity resolution, continuation replay and exact
trace-request construction but does not submit the second query. Mode 11 adds
the second RayQuery and every committed-hit fact but stops before geometry
surface decoding. Existing mode 9 supplies the full-decode boundary and mode 3
the post-hit target boundary. Static SPIR-V inspection proves RayQuery counts
of one/two/two/two for modes 10/11/9/3 and monotonically growing load counts of
604/664/721/883. The Vulkan build passed; only new probe10/11 blobs and the
executable require deployment, with no DX12 shader compilation.

UPT-23 frozen scene isolation diagnostics
-----------------------------------------

The Vulkan UPT path now has two escalating isolation levels behind
r_pathTracingUnifiedPtFrozenScene. Mode 1 captures the next complete production
scene package and switches to mode 2, which reuses that committed package while
skipping all later scene/universe uploads and BLAS/TLAS construction. Mode 3
captures and reuses a static-only monolithic TLAS and selects dedicated D0/T0
shader variants with direct static-triangle decoding, analytic lights only,
identity current/history light mapping, and no dynamic, rigid, skinned,
emissive-lookup, or previous-to-current-remap access.

OpenPBR and Lambert mode-3 variants were built. Static SPIR-V inspection proves
that the five excluded resource families do not survive in the four principal
D0/T0 variants; the variants retain three and four RayQueryProceedKHR sites,
respectively, so this diagnostic does not obtain a misleading result by silently
lowering the existing ray/work ceiling. Runtime logging distinguishes capture
from replay and reports uploads=0, blas=0, tlas=0, and universeUpdates=0 for a
valid replay. The exact test matrix and interpretation gates are recorded in
38_upt23_frozen_scene_isolation.txt. Runtime timing remains to be collected on
the user's representative wall/orb and heavy-level views.

UPT-23 first field result and factorial split, 2026-08-10
---------------------------------------------------------

The first field run established a live T0 median of 18.872 ms at 2560x1440.
Frozen production replay recovered roughly 4 ms, while the bundled static-TLAS,
flat-geometry, analytic-only mode reduced temporal cost to roughly 6 ms. Scene
publication/acceleration interference is therefore real, but the larger
remaining reduction belongs to the previously combined geometry/light bundle.

Modes 4 and 5 now provide the missing factorial corners. Mode 4 keeps the full
production geometry/TLAS and statically removes emissive lookup plus D0/T0
previous/current remapping. Mode 5 uses the static-only TLAS/direct geometry
decode while retaining the full emissive distribution, lookup and remap
closures. SPIR-V inspection proves that each mode retains the opposite resource
family and that D0/T0 still contain three/four RayQueryProceedKHR sites. The
Vulkan release build passed. Exact mode 2/4/5/3 timing batches remain open.

UPT-24 build-ready resolver narrowing, 2026-08-10
--------------------------------------------------

Extended field observation identifies visible rigid geometry as the dominant
content correlate: heavy Doom views contain many rigid props, mode 3 removes
the rigid-static PBR spheres with the same large reduction, and the live empty-
wall view matches mode 3 despite retaining the production TLAS. Modes 1/2 also
recover roughly 4 ms, so scene publication/acceleration and per-hit resolve are
additive rather than mutually exclusive. Lambert did not fix the heavy area.

The FullSample comparison was corrected: its indexed geometry chain is still
four dependent levels and its compute resolver reads an instance transform.
Its useful differences are one common route, attribute masking and pre-skinned
vertices, not a zero-cost transform.

UPT-24 implements the first contract-preserving slice. Rigid and skinned route
tables retain their CPU byte layouts and bindings but are word-addressed by the
Vulkan shaders. Current rigid hits fetch 80 rather than 144 bytes, rigid
material lookup fetches 16 bytes, and current skinned hits fetch 48 rather than
80 bytes. Skinned range classification now derives its exact producer-owned
base from 2 + rigidInstanceCount and performs no header load. T0 alpha traversal
checks material identity/flags before full triangle decode, matching D0's
accepted no-hole shape.

The Vulkan release build passes all artifact, binding, geometry and trace-count
closures and links the executable. Production T0 SPIR-V retains bindings 17/20,
contains no whole rigid/skinned record loads and uses v4uint loads. Runtime
image and timing acceptance remains open; document 39 owns the exact A/B and
the next common-route/attribute-mask escalation.

UPT-24 runtime rejection and UPT-23 replay-state audit, 2026-08-10
-----------------------------------------------------------------

UPT-24's narrowed rigid/skinned route loads produced no performance difference.
They remain a valid ABI-preserving cleanup, but route-record load width is not
the dominant slowdown. A stronger field result supersedes the earlier direct
rigid-decoder inference: a second Doom view measured about 18 FPS live, 44 FPS
in frozen modes 1/2 and 56 FPS in mode 3, and mode 0 remained faster after the
modes were traversed backward. This history dependence points to persistent
scene/BVH/acceleration planning or GPU structure state rather than work paid
independently by each hit.

The frozen host state machine had also collapsed every successful mode >=3
capture to captured mode 3. Consequently modes 4/5 never matched their replay
requests and rebuilt each frame; previous factorial timings for those modes are
invalid. The captured identity now preserves modes 4 and 5. Releasing any
frozen mode to zero arms eight `frozen release audit` lines covering full scene
time through the end of the function, pre-BVH scene time, component timings,
geometry/TLAS counts and async rigid-route, rigid-TLAS, BVH and acceleration
cache/queue state. The prescribed Vulkan build passes and all SPIR-V is up to
date. The executable deployment remains pending while the running game holds
the destination file open.

UPT-25 build-ready emissive temporal PDF correction, 2026-08-10
----------------------------------------------------------------

The decisive field A/B is emissive population rather than rigid decode:
r_pathTracingToyEmissiveScale 0 reduced the representative T0 dispatch from
about 20 ms to 7.43 ms and also accelerated the apparently non-emissive PBR
wall view. Scale zero leaves the light universe resident but prevents positive
emissive candidates from occupying D0, proving that stored emissive replay is
amplifying the temporal cost.

Source audit found a redundant serialized lookup. Secondary-emissive T0 first
remapped the stored dense index, validated its range/type and stable fingerprint,
then performed up to 16 hash probes on instance/primitive merely to recover the
same light's CDF probability. The authoritative 112-byte light already carries
that exact value in sourcePdf. The compact 64-byte pack now preserves sourcePdf
in a word used by analytic influence radius only for analytic records, and T0
uses it after the existing identity checks. Endpoint reverse-NEE retains the
lookup because arbitrary geometry hits still require geometry-to-light mapping.

The now-dead temporal emissive-distribution SRV was removed from shader and
host bindings; D0 still owns it for weighted candidate generation. The Vulkan
release build passes with unified T0 reduced from 30 to 29 resources and the
four-RayQuery ceiling unchanged. The executable and exactly 58 rebuilt Vulkan
SPIR-V files were deployed with matching hashes; no DX12 shader was compiled.
Document 40 owns the scale-4 image/performance acceptance.

UPT-28 diagnostic configuration parity, 2026-08-11
---------------------------------------------------

The accepted fastest test configuration is split D0 with compact64 lights and
native geometry; compact geometry is slower. The earlier temporal probes could
not represent it because compact lights were subordinate to compact geometry,
mixed split storage was rejected, and probe shaders forced Lambert.

UPT-28 adds native-geometry/compact-light split-direct and split-indirect Slang
variants, keeps compact materials on the existing compact-geometry route, and
recompiles probe stages 1--12 with production OpenPBR. The probe gate now admits
native geometry and split initial. The required runtime proof is D0
geometry=legacy112 lights=compact64 split=direct+indirect, T0 shading=openpbr,
and bottleneckProbe=N. No estimator, reservoir, visibility or ray-ceiling
contract changed.

The first UPT-28 runtime capture found one further diagnostic-only correctness
error: the probe-12 counter publisher overwrote the already-written merged T0
reservoir with D0, suppressing temporal feedback. Probe 12 is now observer-only
and preserves production history recursion; stages 1--11 remain destructive
ladder cuts by design. Previous probe-12 camera comparisons do not prove that
production executed work was stable and must be repeated with the corrected
artifact. Counter readback now waits for 16 valid-history frames so the captured
work is steady-state rather than the first post-pipeline-switch history frame.

UPT-29 temporal ray-site attribution, 2026-08-11
-------------------------------------------------

The corrected production-feedback probe pair measured 13.486 ms slow versus
12.845 ms fast. The slow pose executes about 20 percent more RayQuery Proceed
iterations and 13 percent more geometry/material work, while light-record loads
are lower. With alpha-clipped surfaces hidden, the remaining programmable work
is glass/translucency or mixed non-opaque BLAS content. The pair is only a
0.641 ms delta, so it does not yet reproduce the larger camera discontinuity.

Probe 12 now attributes ray, Proceed, geometry and material counts separately
to history replay, reciprocal replay, final indirect visibility and final
direct visibility. Four packed site words extend the per-pixel ABI to 13 dwords;
the CPU derives 8x8 infected-group counts and Proceed-active lane thresholds.
The path remains observer-only, uses no atomics or full-buffer clear and leaves
the estimator and production temporal feedback unchanged. Document 44 owns the
capture and interpretation contract. The full Vulkan release build and UPT-07
artifact verifier pass. The rebuilt executable and five content-changed Vulkan
SPIR-V blobs were deployed with matching SHA-256 hashes; no DX12 shader was
compiled. Runtime site distributions remain the next acceptance evidence.

UPT-30 genuine DI/GI reuse unification decision, 2026-08-11
------------------------------------------------------------

UPT-29 confirmed that view-dependent temporal work is sparse indirect history
replay, but the current reuse architecture is transitional. D0 already streams
short direct and longer global paths into one reservoir. T0 still chooses
separate direct/global mapping and merge behavior, while S0 rejects every
global sample and the host admits S0 only for the direct-only family.

Further optimization of that event-specific T0 is stopped. Document 45 defines
the replacement: one shift-result contract carrying source/target targets,
Jacobian, mapping validity and deferred winner visibility; one target-scaled-
UCW GRIS core; common temporal reciprocal shifts; and combined spatial reuse
followed by Enhanced reciprocal pairing. Temporal and spatial remain ordered
passes, not one dispatch. The existing artifacts remain parity oracles.

The first bounded slice is resource-free contract/oracle work only. Production
D0/T0/S0 selection must not change until direct/global label invariance,
Jacobian-once and byte-level deterministic merge vectors pass on CPU and Slang.

The first half of T30.1 is now built. slang_upt10 supplies the pure mapping/
merge contract, a deterministic CPU oracle and a compile/reflection verifier.
The CPU vectors pass, the SPIR-V exposes only two SRVs plus one UAV, and its
disassembly contains no ray-query or ray-tracing capability. The full Vulkan
release build passes. Runtime selection is unchanged. CPU/GPU byte comparison
is still required before this contract is admitted into T0.

T30.1 is now complete. The Vulkan harness ran the eight-pair corpus twice on
the RTX 4090; all 128 output bytes matched the CPU oracle with hash
0x302f95d704cf624f. The pure contract remains ray-free.

T30.2 now has an opt-in runtime parity specialization selected by
r_pathTracingUnifiedPtSharedReuseAdapter. It normalizes both temporal shift
directions into Upt10ShiftResult while retaining the old direct and global
mapping mechanics and the old merge algebra. The accepted compact64-light plus
duplication layout remains 30 bindings and four RayQuery sites. The cvar is
default off and reports requested/effective state in the temporal creation line.

The full Vulkan release build passes. The deployed executable SHA-256 is
BF0DD5197E36F7E0A8D448C93ACDE38B45EC86F4FBA7604CF927AAC32AA6C8FE;
the sole new deployed Vulkan blob is the shared-adapter T0 specialization with
SHA-256 37317C7018946C26E95AC3BECB676247115F1DF36B431A49A791E240C7ECF1B3.
No DX12 shader was compiled. Runtime image/timing parity is the remaining T30.2
gate before the common GRIS merge replaces event-specific temporal algebra.

The executable hash above includes the corrected activation reporter. It emits
the shared-reuse requested/effective pair and all ten individual gate bits when
state changes, even if temporal never reaches pipeline creation.

T30.2 passed its runtime parity check once split initial made compact64 lights
effective. No image regression was found; the troublesome test view also became
faster, but that improvement is not yet attributed and is not used as estimator
evidence.

T30.3 is deployed as another default-off Vulkan specialization selected by
r_pathTracingUnifiedPtCommonGrisMerge. It requires both the accepted shared
adapter and r_pathTracingUnifiedPtTemporalPairwise 1. The common core maps the
current and history proposals through explicit source UCW, destination target,
pairwise MIS and exactly one forward Jacobian, with count-only valid-zero
confidence handled separately from selectable mass. The accepted adapter and
old production shaders remain available for rollback.

Switching into or out of the common merge invalidates the previous reservoir
and duplication metadata only. This prevents cross-convention history feedback;
the first frame after a toggle uses fresh D0 and performs no buffer clear.

Offline gates pass: the CPU/Vulkan oracle remains byte-identical with hash
0x302f95d704cf624f; the new artifact preserves 30 bindings, four RayQuery sites
and no TraceRay; and the full Vulkan release build passes. The deployed EXE is
3DB79C5B65710B52E5EE4772C5C87F27740A987211B2B4DBF7A68A66138926BF and the
only newly deployed blob is the common-GRIS T0 specialization with SHA-256
169C01540C1183B2C30A31E0EE89530035218DE77C4DB31AF8C948F27CA8C60B.

The next gate is runtime A/B with shared adapter 1, temporal pairwise 1 and
common GRIS merge 0/1. Check fixed-pose brightness/noise, temporal stability,
portal crossings, nearby emissives, mirrors, alpha blockers and moving rigid or
skinned geometry. Stop on ghosting, color corruption or accumulating energy.
Do not begin S30.1 until this common temporal estimator is accepted.

T30.3 passed that runtime gate after approximately twenty minutes of play. No
image, ghosting, color or energy issue was observed, and temporal performance
was less erratic overall. Proceed with S30.1 selected-center combined spatial
admission while retaining the accepted T30.3 artifact as its temporal oracle.

S30.1 is now built and deployed behind
r_pathTracingUnifiedPtSharedSpatial. With the accepted temporal cvars and
r_pathTracingUnifiedPtSpatial 1, it evaluates exactly one selected-center /
selected-neighbor pair through the common direct/global shift and GRIS core.
There is no empty-center rescue or multi-neighbor expansion. Same-frame global
replay performs no previous-light remap, final secondary-NEE visibility remains
winner-only, and equal nonzero replay keys are rejected as duplicate paths.

The full Vulkan build and structural verifier pass: 26 bindings, 96-byte push
constants, four RayQuery sites, no TraceRay. The deployed EXE SHA-256 is
8F804CF61362C0F003065D55853C4A6514196634CF75665C3764630FC90B6F46; the only
new spatial blob is
6D96707AD0D4795C21EB2C8C173B1D29521F43D8BCE5B59C6525C506444B9B2C.
Runtime acceptance of shared spatial 0/1 is the next gate. Do not begin S30.2
or increase the neighbor count until it passes.

The first S30.1 runtime artifact passed visual correctness but failed
performance: emissive-enabled scenes lost approximately 50 to 80 percent of
total frame rate. The cause was redundant reciprocal ownership, not a return of
the emissive identity search. Each pixel paid both global shifts independently.

A corrected artifact is deployed. One lane now owns a disjoint two-pixel pair,
computes the two reciprocal shifts once and publishes both reservoirs. This
reduces the maximum dynamic ray work over two output pixels from six to four
without changing GRIS mass, Jacobian, duplicate rejection or winner-only
visibility, and adds no dispatch, buffer or clear. Pair orientation/origin
cycles through four adjacent-pixel phases. The runtime marker is
UPT.S0 Shared Disjoint Reciprocal Pair GRIS; the pipeline line reports
pairedPixels=2 mappingRaysMaxPerPair=2.

The rebuilt Vulkan EXE SHA-256 is
51F8F8FFD7F37A1FC6C6C9DF78CD4E4D967D09950EBA6B34CEFFB1949C9413B3. The only
replaced SPIR-V blob is upt09_spatial_shared_selected_pair_light64.bin with
SHA-256 8F16F2FEB5B93943858F6CC47572667A376AB7B2BFB9E53D134F25F03CDEC95A.
Repeat the same emissive-heavy A/B before expanding spatial reuse.

The disjoint pair recovered some performance, but its adjacent-only reuse was
blotchier. With emissive scale zero, spatial performance was completely fine;
the remaining cliff therefore follows selected secondary-emissive replay.

That route was evaluating four coarse emissive texture probes to rebuild a
receiver-invariant region PDF and then evaluating the exact endpoint texture.
D0 now persists its already-computed selected-region PDF scale only after
reservoir selection, in the otherwise structural proposalPdf word. This does
not change D0 selection or weight. Temporal preserves it and keeps a transition
fallback; same-frame shared spatial fails closed on an untagged record and
compiles out all four coarse probes. Exact endpoint texture/material replay is
unchanged.

The full Vulkan build passes. The three deployed blobs are split indirect D0
3470E6F2690D1D89E4E4D7D7713F698D6F5004E636715CEFC60E89D2FC8B888D, common
temporal GRIS 9C0F3E5CA5A3125A75689BC6480C79A82D81393497B876910D95576063167529,
and paired shared spatial
324E8B0A5AD1E904A087DC7587C6DC7071872ECAEDD26C1E0847A422E47394FC. Re-test
the same emissive-heavy pose before changing the adjacent-pair quality schedule.

That persisted-scale build was subsequently removed as a benchmark authority
before any route optimization. Compiled T0 and shared S0 read replay geometry
class buffers at bindings 9 and 13, while the host omitted them. Resolve also
treated the static-reconnection locator in reservoir words 13..15 as floating-
point path metadata, rejecting instance-zero locators and misreading others.
Finally, the positive persisted-scale sentinel overlapped valid proposal PDFs.

The correction makes T0/S0 share one complete bindings-6..21 geometry replay
contract, decodes the active reservoir union arm in resolve, and stores the
regional emissive replay scale in the negative finite word12 domain. Actual
PDFs remain positive, so the representation cannot collide. UPT packed-content
ABI 11 rejects old history via page metadata and performs no full-buffer clear.
Build gates now check shader/host binding 9/13 parity, the resolve union and
signed payload contract, plus four-phase pairing involution/borders. Runtime
image parity and a fresh fixed-pose emissive timing matrix are required before
using the S30.1 measurements or changing any emissive route.

The full Vulkan release build passes and no DX12 shader was compiled. Deployed
SHA-256 values are EXE
C36F79A12746ED8D2B94662F461827D35D4D725D1708968CEB95A285D7C6CED8,
split indirect D0
7E92A60184A9B137C8B3CFED485622A2EDF9BDA6926AC095ADE09BE5ACEFF30A,
compact32 resolve
00C5D3AB8151386554D83BD65EB82960020240530E55DBAAEB84822EC235C981,
common temporal
694EC92A032FD8AB79FA027C2D34B46D4CAF7AA4AC8A495986A0C72B83C66A08,
and paired shared spatial
9F41740D5F6C7F518A4310A8A6B58EA820038146B381EFDBF511C1A8FCA8CC28.

The corrected baseline did not recover performance and slightly regressed the
observed indirect RayQuery time. Treat the binding, packed-union and signed
payload fixes as correctness prerequisites, not as the performance cause.

UPT-31 now has a separate default-off direct source-target specialization:

    r_pathTracingUnifiedPtSpatialStoredSourceTarget 0
        accepted shared S0 artifact; freshly replays the direct emitter at its
        source receiver solely to recover sourceTarget;

    r_pathTracingUnifiedPtSpatialStoredSourceTarget 1
        trusts the canonical selected.target already written in that source
        receiver domain; all target shifting, visibility and GRIS math remain
        unchanged.

The writer audit covers D0, previous-best injection, both T0 winner branches,
both shared-S0 winner branches and the read-only duplication pass. The new
artifact retains 26 bindings, six static RayQuery sites, no TraceRay and the
same four-rays-per-pair dynamic ceiling. It shrinks from 878884 to 749492
bytes and reduces static explicit texture-sample sites from 26 to 22.

Use r_pathTracingUnifiedPtSpatialGpuTiming 1 for the next comparison. It warms
16 valid S0 dispatches, records 64 dispatch-only samples and reports robust
statistics with the active variant and pairing phase. Set
r_pathTracingUnifiedPtFixedSampleIndex to 0 for the first matched pair, toggle
stored source target 0 then 1, and repeat. The timing mode change rearms the
batch automatically; toggle timing off/on to repeat an unchanged mode. Test
fixed phases 1..3 only after phase zero is reproducible. The quantized-sample
coordinate caveat requires adversarial checks at alpha-masked emissives,
stretched UVs and texture edges before acceptance.

The full Vulkan release build passes; no DX12 shader was compiled. The new EXE
SHA-256 is
309C2DB011EBEB92293D7CC392C3363C5AB446445DE07C3E75D43A3AD4BEC39A and the
new stored-target S0 blob SHA-256 is
4EB27ECAB61B7F939827FBD79AC493CCB9929EC75263147B9C1BEA5A454F21A5. The
accepted shared S0 blob was not rebuilt. Do not begin the two-pass paired
prepass or pairing-texture work until this source-target A/B is measured.

The first UPT-31 runtime batch is positive but not sufficient for acceptance.
At fixed sample index/phase zero, the nearest repeated baseline was 11.435 ms
median / 11.596 ms mean and stored source target was 10.786 ms / 10.901 ms.
That is a 5.7 percent median and 6.0 percent mean S0 saving. It proves the
source-side direct emitter replay was redundant, but leaves almost all of the
pass cost intact. Visual parity was not reported with this timing log.

More importantly, the accepted artifact measured 20.267-20.812 ms median with
live sample indices but 11.435-12.330 ms at fixed sample index zero. The fixed
cvar changes all UPT sample selection/history population as well as pairing
phase, so do not attribute this gap to phase orientation. It makes event-route
population the next diagnostic target. Obtain one more stored-target batch and
one return-to-baseline batch, then add per-workgroup route/rejection counters
for direct event 2, global event 2 and endpoint event 4 without a per-frame
clear or globally contended per-pixel atomics.

spatiallog2.txt completes the return baseline. The immediately preceding
stored-target phase-zero batch was 7.447 ms median / 7.962 ms mean; returning
to source replay produced 8.706 ms / 8.745 ms. This confirms a conservative
14.5 percent median and 9.0 percent mean improvement. Combined with the first
9.569 -> 6.963 ms transition and the tightly grouped 6.963/7.218/6.981 ms
stored-target medians, the direction is decisive even though the magnitude
varies with reservoir population.

The implementation is performance-accepted but not yet visually accepted.
Before changing its default, inspect alpha-masked and stretched-UV emitters,
mirrors, portals and moving rigid/skinned geometry for target mismatch, color
corruption or energy changes. Once visual parity is confirmed, promote stored
source target and proceed to per-workgroup S0 event-route counters; about 7-9
ms remains even in fixed-index phase zero.

Temporal motion test interface, 2026-08-11
-------------------------------------------

A reported post-correction motion artifact was not immediately reproducible.
Manual setup had become too large for reliable console paste and could retain
incompatible diagnostic state. Use the checked-in one-command presets instead:

    exec uptm0    production temporal baseline
    exec uptm1    exact single-tap reprojection
    exec uptm2    pre-common-GRIS merge
    exec uptm3    direct temporal history only
    exec uptm4    no-reuse D0/R0 reference

All five select the accepted fast D0 layout (split initial, compact64 lights,
native geometry), disable spatial, clear diagnostic specializations and reset
history through a one-frame temporal-off transition. Compare the same moving
rigid/skinned object or camera-disocclusion edge. If uptm1 alone fixes the
artifact, audit multi-probe receiver acceptance. If uptm2 alone fixes it,
audit common-GRIS history selection. If uptm3 fixes it, audit global replay;
uptm4 is the non-reuse oracle.

baseline.txt verifies that uptm0 reached the intended gates: shared adapter
1/1, common GRIS 1/1, split direct+indirect D0, compact64 lights, indirect T0,
nine history taps and spatial off. The user reported stable performance and
good image quality, with no repeat of the suspected motion artifact. Treat the
production temporal baseline as accepted and wait for a reproducible motion
case before changing reprojection or merge policy.

UPT-32 full-occupancy shared spatial, 2026-08-11
-------------------------------------------------

Feature completion resumed at S30.3. The prior leader artifact left half of
every warp inactive. A new default-off workgroup specialization gives every
lane one source-to-partner shift, exchanges mapped records through one 8x8
group-shared barrier, and gives every lane one publication. A nonzero hashed
XOR mask is a self-inverse 64-lane pair map and replaces the four adjacent-only
phases. It adds no full-resolution buffer, page, clear or dispatch.

The original S30.1 and UPT-31 artifacts remain available. Offline gates pass:
26 bindings, triangle classes at 9/13, 96-byte push constants, one barrier,
three static RayQuery sites, no TraceRay and the same four dynamic queries per
pair. The moving-boundary SPIR-V is 477508 bytes with SHA-256
F99D5582B81A1958572D62A8530A0D89A6C4B49258C03794F1F16E85BF514C71;
the rebuilt executable SHA-256 is
40014535871FE098691F20FD442535B4D7860F8924444286D72ADC38D4CCC780.

Use `exec uptf0` for the stored-target leader oracle and `exec uptf1` for the
new workgroup path. Require requested/effective=1/1 and workgroupPairing=1.
Runtime must reject seams, black output, motion ghosts, color corruption,
missing geometry or energy growth before widening the pair domain, adding
multiple neighbors or re-admitting empty-center rescue. Document 47 owns the
exact contract and remaining feature sequence.

The first uptf1 runtime test reported a decent performance boost with broadly
good image quality, validating full-lane ownership. Faint 8x8 squares remained
occasionally visible. UPT-32 now translates the complete workgroup lattice by
a frame-varying toroidal offset for extents divisible by eight. This keeps a
one-to-one output mapping and exact reciprocal pairs while moving tile borders
across frames; non-multiple extents retain the edge-safe unshifted schedule.
Wrap-crossing pairs reject symmetrically rather than connecting opposite
screen edges. Re-test uptf1 before adding more neighbors or rescue.

The corrected uptf1 runtime test passed. The performance improvement remained,
the moving boundary removed the faint 8x8 squares, and no DLSS-RR masking was
required. Treat UPT-32 as runtime accepted. Its blob remains byte-identical at
477508 bytes, SHA-256
F99D5582B81A1958572D62A8530A0D89A6C4B49258C03794F1F16E85BF514C71.

UPT-33 bounded empty-center rescue, 2026-08-11
------------------------------------------------

S30.4 now has a separate default-off artifact and `exec uptf2` preset. Only a
valid current receiver carrying the explicit needs-rescue bit may borrow its
reciprocal partner. The empty/count-only center is streamed as a valid-zero
technique, so its confidence enters the common pairwise denominator but it can
never be selected. Invalid, stale, incompatible, duplicate, zero-target or
invisible donors still fail closed to the input reservoir.

A successful rescue clears Replayable and NeedsShift on global paths and sets
the reserved replay-key rescue bit. Existing direct and indirect T0/S0
eligibility therefore rejects the borrowed output without any temporal shader
change. It is useful for this frame's resolve only and cannot recursively
poison reuse. UPT-32 remains the exact `exec uptf1` oracle.

Offline verification passes with 26 bindings, triangle classes at 9/13,
96-byte push constants, one barrier, five static RayQuery sites, zero TraceRay
sites and the unchanged dynamic four-query ceiling per selected pair. UPT-33
is 519800 bytes, SHA-256
0BBB146E7421C9EA18C5BEE36EB9FCE3A7D9F437D5C5F9A04FD71D62F1E9B956.
The executable is 16347648 bytes, SHA-256
06D1ED92C0C6FBF53DA8C0FDB25956EBA809854D26552BFD269501848DDF4300.

uptf2.txt confirmed exact activation: shared adapter, common GRIS, shared
spatial and workgroup pairing all reached requested/effective=1/1, and S0
reported blobBytes=519800, workgroupPairing=1, emptyRescue=1. There were no
binding, validation or shader errors. The user reported that performance and
visuals seemed fine. Treat UPT-33 as runtime accepted and preserve both uptf1
and uptf2 as rollback oracles before multi-neighbor work.

UPT-34 simultaneous multi-neighbor spatial, 2026-08-11
-------------------------------------------------------

The paper audit confirmed that three reciprocal neighbors are simultaneous
GRIS inputs, not three sequential two-proposal merges. Document 49 freezes the
exact estimator: each neighbor uses denominator k M_i p_i + M_c p_c; the
canonical MIS is the average of its three pair terms, with unit contribution
for a missing pair. Canonical confidence is added once, each admitted neighbor
confidence once, and UPT-10 converts every mapped target-scaled weight back to
UCW before applying target and forward Jacobian exactly once.

The new default-off UPT-34 specialization uses three distinct nonzero XOR
involutions inside the accepted moving 8x8 lattice. Every lane publishes all
three mappings to disjoint group-shared slots, one barrier completes the
pre-pass, and every lane then performs one canonical-plus-three-neighbor WRS
and at most one winner visibility query. Duplicate replay identities reject
before confidence or mass admission. Empty-center rescue remains exactly the
first randomized partner and retains UPT-33 non-donation stamping.

There is no new persistent page, buffer, descriptor, clear, dispatch or ABI.
Offline verification passes with 26 bindings, triangle classes at 9/13,
96-byte push constants, three distinct reciprocal maps, one barrier, five
static RayQuery sites and no TraceRay. The new blob is 579244 bytes, SHA-256
EB7D15C7660C8818CB123EAFE85D8CB678481CBB90A16580F194A767B02C446C.
The accepted UPT-32/33 hashes remain byte-identical. The executable is
16348672 bytes, SHA-256
53ECCBED2A4A5D252118B5DF7667EFEC6DE4A11F6F04CB417B2498864A157BFA.

Run `exec uptf3` and require multi-neighbor requested/effective=1/1 plus the
pipeline fields multiNeighbor=1, neighbors=3 and emptyRescue=1. Compare after
pipeline warm-up against `exec uptf2`. Runtime acceptance is pending; reject
brightness drift, energy growth, ghosts, color corruption, black output,
missing geometry, tile structure or an unjustified spatial performance cliff.

UPT-34 runtime result, 2026-08-11
---------------------------------

The feature rendered correctly and slightly improved image quality, but the
user measured an approximately 40 percent frame-rate loss against UPT-33. This
fails the production performance gate. The measured shape matches the explicit
work expansion: UPT-33 maps one reciprocal neighbor per lane, while UPT-34
maps three before the barrier. Reciprocal ownership eliminates duplicate
reverse shifts; it cannot eliminate the two additional global mappings.

Do not promote or checkpoint UPT-34 as the accepted renderer. Keep it default
off as a research artifact and use `exec uptf2` as the production-shaped
configuration. A future revisit needs a genuinely different selective or
amortized schedule and must retain the simultaneous k-technique MIS contract;
do not replace it with sequential two-proposal merges.

UPT-35 bounded three-vertex D0, 2026-08-11
-------------------------------------------

The next production feature lane is UPT-10A, implemented first as UPT-35 D0
only. Local NVIDIA FullSample source resolves the terminology: its accepted
maxBounceDepth=3, roulette-enabled preset traces x1->x2, performs NEE at x2,
then survives with fixed q=0.8 before x2->x3 and can select the x3 emissive
endpoint. It does not run an x3 NEE schedule. In rbdoom this is exactly two
continuation rays after the primary receiver.

The new `uptb2` specialization extends only split-initial, native-geometry,
compact32-receiver, compact64-light OpenPBR D0. It adds one prepared x2 BSDF
sample, one roulette draw at path vertex 1, at most one x2->x3 closest-hit
query, and one `eventKind=4,pathLength=2` endpoint proposal in the existing
64-byte reservoir. No descriptor, page, payload, clear or persistent ABI was
added. T0/S0 currently rejects that endpoint shape, so the host gate requires
temporal and spatial off rather than replaying it incorrectly.

On survival, throughput contains 1/q, cumulative path PDF contains q, the
stored target multiplies the compensated contribution target by q, and the
proposal PDF is q. Therefore target/proposalPdf retains the correct survived
streaming mass while the selected target follows the reference convention and
does not double-compensate roulette. Termination emits no candidate and no ray.

Offline proof passes: 25 bindings, 208-byte push constants, 8x8 workgroup,
three static RayQuery sites, zero TraceRay sites, and the CPU q=1/q=0.8 oracle.
The accepted split-indirect D0 artifact recompiles byte-identically at SHA-256
7E92A60184A9B137C8B3CFED485622A2EDF9BDA6926AC095ADE09BE5ACEFF30A.
The new blob is 528756 bytes, SHA-256
14A5A511631AF00386B3A799334B0FC441669952DA089A1320CDE26234ACC487.
The deployed executable is 16350208 bytes, SHA-256
54EAB781319B28A7C5A9C51C03859C2A70E73FF0713DAE300B54A617D27EEEB9.

Run `exec uptb2` and require requested/effective=1/1 plus the split-indirect
pipeline fields threeVertex=1, continuationRaysMax=2 and rouletteQ=0.80.
Validate D0 image, energy and cost before extending temporal/spatial replay.
`exec uptf2` remains the accepted production-shaped rollback preset.

UPT-35 runtime result, 2026-08-11
---------------------------------

`exec uptb2` activated the specialization and the user reported that it worked
without issues. D0 two-continuation transport, q=0.8 roulette and the x3
endpoint proposal are runtime accepted. Preserve the separate artifact and
unchanged `uptf2` rollback. The next bounded task is temporal replay/shift for
`eventKind=4,pathLength=2`; do not enable temporal or spatial for that event
until the two-continuation reconstruction and ray ceiling are explicit.

UPT-36 exact three-vertex T0, 2026-08-11
-----------------------------------------

UPT-36 extends only the accepted common-GRIS compact64-light duplication T0
configuration. An `eventKind=4,pathLength=2,reconnectionVertexLength=1`
history/current endpoint is eligible only when both persisted roulette fields
are finite, lie in (0,1], and agree. T0 replays x1->x2, the path-vertex-1
stored-q survival decision and
x2->x3, then evaluates the x3 emission and the same x2 BSDF/NEE balance term
used by D0. Any structural, roulette, traversal, geometry, material, target or
PDF disagreement rejects the sample.

No light-universe search or remap participates in this endpoint route. The
separate artifact preserves 30 bindings, the 64-byte reservoir and all page
contracts. Its six static RayQuery sites are the previous four-site temporal
ceiling plus at most one second continuation in each direction of the
pairwise mapping. Spatial remains disabled. Run `exec uptb3` and require
threeVertexReplay=1 plus indirectReplayRaysMax=6 before visual testing.

Offline verification passes at 30 bindings, triangle classes 9/13, six static
RayQuery sites and zero TraceRay. The new blob is 1086456 bytes, SHA-256
A51227CCB09094300501FAA3AABDAD015975E09FE230B455124F90F560A7BEB0. The
64-byte codec/math oracle proves D0/T0 equality and the Release host build
passes. The accepted common-GRIS rollback blob remains byte-identical at
SHA-256 92C2954BAD341B92BEFF5C730AD7B3C41C85862EC84507E8BBFD2B8EB6313C44.

UPT-37 termination controls, 2026-08-12
----------------------------------------

The three-vertex lane now reads runtime q and minimum-throughput values from
the existing reserved initial-control words. D0 samples the x2 BSDF, multiplies
uncompensated accumulated throughput, kills paths whose squared RGB L2 length
falls below the squared threshold, then performs roulette and emits x2->x3.
The cutoff is biased, uncompensated, creates no candidate/ray, and is initial
sampling only.

T0 never reapplies the cutoff and replays q from the selected sample's matching
proposalPdf/accumulatedRouletteProbability fields. Both cvar bit patterns enter
the content-generation fingerprint, so changes invalidate history without a
full clear. `exec uptb3` is the q=0.8/threshold=0 baseline; `exec uptb4` is the
aggressive q=0.25/threshold=0.10 measurement preset. Spatial remains disabled.

Offline verification passes. D0 is 529636 bytes, SHA-256
39E47D38107AA7320176D055213AA6EB87D3995A99D7DA68C9BEA122C59E3301;
T0 is 1087424 bytes, SHA-256
520E988F8203F68CF64B217F5811EFEADA84197C7E91F53263075F51E5B99ACE.
The codec/math harness and focused Release host build pass. Runtime acceptance
subsequently passed. Both the q=0.8/threshold=0 baseline and aggressive
q=0.25/threshold=0.10 configuration worked without a reported replay,
geometry, emissive, motion or energy failure. UPT-36/37 may be checkpointed as
an optional completed D0/T0 feature.

Do not promote the aggressive preset or the extra bounce as Doom 3 defaults.
The added transport depth is expensive relative to its benefit in this game;
retain it for future renderer ports and preserve `uptf2` as the
production-shaped one-continuation lane. Three-vertex spatial reuse remains a
separate future task.

UPT-38 exact three-vertex S0, 2026-08-12
-----------------------------------------

UPT-38 completes reuse for the optional path-length-two endpoint without a
new spatial estimator. A separate Vulkan artifact compiles the accepted UPT-33
one-neighbor reciprocal-pair/empty-rescue route with UPT-36 exact stored-q
replay. The rejected UPT-34 three-neighbor route is excluded by the host gate.

S0 reuses `Upt07ReplayIndirectSample`: it reconstructs x1->x2, validates the
stored-q survival branch, traces x2->x3, and evaluates endpoint emission,
target, path PDF and emissive-hit MIS in the target receiver domain. It never
reapplies the initial-only biased minimum-throughput cutoff. Invalid or
incompatible samples fail closed.

No descriptor, buffer, page, clear, dispatch or persistent ABI was added. The
new artifact verifies at 26 bindings, 96-byte push constants, triangle-class
bindings 9/13, one workgroup barrier, six static RayQuery sites and zero
TraceRay sites. Each lane maps one partner with at most two continuation
queries; the conceptual reciprocal pair therefore has at most four mapping
queries plus the existing per-winner visibility bound.

The blob is 655100 bytes, SHA-256
2D3AC59F233826901C5AE89B3F496CFB15F302D0FE0D57F4C8916E8F24AD0BBD. The
Release executable is 16352256 bytes, SHA-256
5FD36907F904A642D6863249CDECEED49EECC4B2154929593A78019EBA188AD2. The
UPT-02 D0/T0/S0 equality oracle passes at a 64-byte reservoir and 22 random
slots. No DX12 shader was compiled.

Run `exec uptb5` and require threeVertexReplay=1 in both temporal and spatial
pipeline logs, with spatial neighbors=1, continuationRaysMaxPerMapping=2 and
mappingRaysMaxPerPair=4. Compare visually
and for cost against `exec uptb3` after warm-up. Runtime acceptance is pending;
do not checkpoint or enable the path by default before that comparison.

UPT-38 runtime result, 2026-08-12
---------------------------------

The deployed `uptb5` specialization worked correctly and is runtime accepted.
Spatial reuse can propagate occasional high-energy temporal fireflies, but the
user judged the current image cleaner than comparable shipping renderers and
the effect cannot be assessed meaningfully before the intended denoiser/Ray
Reconstruction pipeline is tested.

Do not compensate in S0 with a clamp, target cap, broader rejection or invalid
sample admission. The high-energy sample originates in temporal reuse and S0
preserves it. Reopen the issue only if denoiser testing exposes a specific T0
estimator, replay, visibility or material defect. Preserve `uptf2` as the
production-shaped rollback and keep the extra bounce optional.

UPT-39 DLSS Ray Reconstruction bridge, 2026-08-12
--------------------------------------------------

The next feature audit found that the production clear-window PSR cannot be
connected to the accepted UPT route by merely changing dispatch order. It
rewrites the 176-byte clean primary record after clean-DI setup, whereas the
accepted UPT route exits earlier and consumes compact32 plus a history sidecar.
Do not regress UPT to the wide receiver for glass. A later glass task must add
a compact PSR publication adapter.

UPT-39 therefore takes the existing RR bridge first. The shared primary
producer already has exact material, depth, position, motion and reset guide
writes; enable those only when UPT beauty resolve and DLSS RR are both active.
UPT keeps its unchanged trace-free RGBA16F resolve as Streamline input, the
existing output-sized accumulation texture receives reconstruction, and UPT
presents that texture directly on success. RR-off and evaluation failure retain
the ordinary UPT resolve. No new ray, shader, guide texture, full-frame clear or
copy is admitted. See 54_upt39_dlss_rr_bridge.txt.

The host build and deployment are complete. The deployed executable is
16354816 bytes, SHA-256
E808793F4C8418E5B00D298E94273E2FE02BBF71C6CB783A27071295646D121F. No SPIR-V
was deployed; the production compact32 resolve remains byte-identical to the
prebuilt copy at SHA-256
00C5D3AB8151386554D83BD65EB82960020240530E55DBAAEB84822EC235C981. Runtime
acceptance is pending with `exec uptrr`.

UPT-39 runtime result and UPT-40 jitter reprojection, 2026-08-12
---------------------------------------------------------------

The UPT-39 bridge is functionally accepted: reconstruction executes and the
image is otherwise correct. Temporal reuse interfered with the reconstructed
result and exposed a concrete producer/consumer mismatch. RR primary rays use
`pixel + 0.5 - jitter`, but D0 previous-best and T0 projected into the prior
camera and floored the unjittered coordinate. They could therefore address an
adjacent history pixel before surface/replay/visibility evaluation.

UPT-40 persists the exact jitter with the published camera state and adds it
back only when converting a projected world point to the previous storage
pixel. D0 grows from the accepted 208-byte prefix to 216 bytes; T0 grows from
the accepted 160-byte prefix to 168 bytes. No target, PDF, Jacobian, weight,
visibility, reservoir, ray, dispatch, clear or resource changes.

The CPU temporal oracle passes and proves both the sign and separation from
random search dither. Targeted UPT-04 and UPT-07 Vulkan builds and reflection
verifiers pass. Runtime validation remains with `exec uptrr`; see
55_upt40_rr_jitter_temporal_reprojection.txt.

The deployed executable is 16355328 bytes, SHA-256
10F85EF790F505A7B07B61D993B94844A9E83340196490F19E9E0615502B667A.
Only the accepted split compact32/light64 D0 direct and indirect artifacts and
the unified light64/duplication/common-GRIS T0 artifact were deployed; their
hashes are recorded in the numbered task. No DX12 shader was compiled.

The full preset build also demonstrated a separate build-graph problem: a
low-level include change fans out through many unselectable storage/backend
option products. UPT-41 will audit host filename reachability and prune those
products while explicitly retaining accepted presets, diagnostics, parity and
feature-level multi-bounce/RR variants. See
56_upt41_shader_variant_reachability_pruning.txt.

UPT-41 default shader-graph pruning, 2026-08-12
------------------------------------------------

The normal RBDoom3BFG dependency graph no longer builds the complete UPT
low-level specialization matrix. RBDoom3BFG now reaches dedicated production
targets containing 7 D0, 6 T0 and 4 S0 SPIR-V inputs. The retained set covers
the accepted split-initial compact-light route, compact32/proposal-parity
rollback, Lambert diagnostic, common-GRIS temporal reuse, UPT-33 spatial
rescue, and optional three-vertex/RR transport.

The full 73 + 61 + 10 declaration matrix remains available through
PathTraceSlangUpt04RuntimeArtifacts,
PathTraceSlangUpt07TemporalOwnership and PathTraceSlangUpt09Spatial. Build
those targets explicitly before using a historical compact-geometry,
compact-material, light-tile, split-continuation, frozen, route/reconnect,
bottleneck-probe, raygen or rejected multi-neighbor diagnostic after shared
shader changes.

UPT-05's hidden dependency on PathTraceSlangUpt04ModuleGraph was also changed
to the lean D0 production target. That edge was the reason the first full
build still rebuilt the entire D0 matrix despite the direct RBDoom dependency
being pruned.

The required Vulkan Release build passes. The generated production projects
contain exactly 17 unique UPT-04/07/09 .slang.spv inputs, and an up-to-date
full build completes in 4.7 seconds while listing only those production
artifacts. No shader source, estimator, ABI, ray ceiling, cvar or filename
changed, so no dedicated runtime image/performance retest is required for
UPT-41 itself. The Release executable plus exactly the 17 retained Vulkan
binaries were deployed to keep every production fallback on the current
shared-source ABI; no experimental or DX12 shader was deployed. See
56_upt41_shader_variant_reachability_pruning.txt for the exact manifest and
explicit full-matrix command.

UPT-42 Gaussian reuse-texture spatial pairing, 2026-08-12
---------------------------------------------------------

The accepted UPT-33 production schedule was still confined to a moving 8x8
XOR permutation. UPT-42 replaces that schedule with the exact Section 3.2
construction: independent 254/230/210 self-inverting maps, sigma=16, 128
alternating toroidal 2x2 shuffle rounds, signed packed deltas, and per-frame
transpose/mirror/offset conjugation. The host validates every reverse link at
construction and uploads the immutable 969,096-byte buffer once with no clear.

Production remains one-neighbor because the three-neighbor UPT-34 estimator
was rejected for cost. It consumes the 254 map. The other maps and pair lists
are generated for future explicit work but do not silently re-enable UPT-34.

To avoid both a full-resolution mapped-work page and half-idle pixel warps,
the shader dispatches one 64x1 lane per canonical pair and repeated texture
tile. Each lane computes both mappings once and publishes both endpoints.
UPT-33 compatibility, duplicate exclusion, GRIS weights, winner visibility,
empty-center rescue, reservoir ABI and ray ceilings are unchanged. UPT-38 has
an equivalent reuse-texture artifact.

The focused reflection verifier and the required
`cmake --build --preset win64-pt-dev-release` build pass. The deployed files
are:

    RBDoom3BFG.exe
        bytes=16361984
        sha256=57E9E0B59F88EAEC73BB161DED2D81DED4B25DD306090E7D86B5A14268BBFB09
    upt09_spatial_shared_reuse_texture_pair_rescue_stored_target_light64.bin
        bytes=1092124
        sha256=F3E3B6524549B9D1DA16A30D2826D261FA2E525B729ED72CD8EC630887454F55
    upt09_spatial_shared_reuse_texture_pair_rescue_stored_target_light64_three_vertex.bin
        bytes=1483452
        sha256=B4651F5084A99FCC5E1A33E68E21B30D592AE721F719FEC3EC87C7E87DE5CEF6

No DX12 shader was compiled or deployed. Runtime acceptance is pending. Run
`exec uptf2` and require the one-time `UPT-42 reuse textures` line to report
sizes 254/230/210, rounds 128/128/128 and `involution=verified`; the spatial
pipeline line must report `groups=64x1-pairs reuseTexturePairing=1
reuseTextureSize=254 multiNeighbor=0 neighbors=1`. Check the prior 8x8 square
and softness locations, image edges, motion with DLSS RR, spatial GPU time
after warm-up, emissive identity/color, and empty-center disocclusions. See
57_upt42_gaussian_reuse_texture_pairing.txt.

UPT-42 runtime rejection and temporal follow-up, 2026-08-12
-----------------------------------------------------------

Runtime quality was excellent, confirming that the old 8x8 reuse domain was
responsible for its structured softness. The pair-list implementation is not
production viable: S0 alone measured about 11 ms. Under temporal stress the
large spatial reach also exposed glitches around area boundaries. Do not add
portal filtering or spatial clamps around that symptom; the larger S0 domain
is revealing unstable T0 samples.

`exec uptf2` is restored to the accepted UPT-33 workgroup path. UPT-42 remains
available as `exec uptf4`, controlled by
`r_pathTracingUnifiedPtSpatialReuseTexture`; it defaults to zero. The normal
Vulkan graph retains both accepted UPT-33/38 artifacts and their UPT-42
diagnostic counterparts.

The NVIDIA FullSample audit gives a concrete next order. Its ReSTIR PT temporal
configuration defaults permutation sampling off, but enables the boiling
filter at strength 0.2. After temporal resampling it computes the group average
of nonzero `luminance(TargetFunction) * WeightSum` and empties reservoirs above
the strength-derived multiple (41x at 0.2). UPT's persisted `weightSum` is
already target-scaled UCW, so it is the corresponding scalar. Test a bounded
post-T0 boiling filter first. Do not multiply by `selected.target` again, and
do not start with temporal permutation sampling: NVIDIA leaves it off for PT
and warns that permutation increases noise on thin/high-detail surfaces.

The rollback build passes and the executable was deployed:

    bytes=16362496
    sha256=312D8987510A09DDB35E879A6FE8064FD237DD79C2F270FA80CB4CF153A84A49

The accepted UPT-33 and optional UPT-38 source/deployed SPIR-V hashes already
matched exactly, so no shader copy was needed. No DX12 shader was compiled.

UPT-42 screen-ordered correction, 2026-08-12
---------------------------------------------

Review of the rejected 11 ms UPT-42 pass confirmed that its canonical-pair
launch was structurally wasteful. At 1920x1080 the padded repeated-tile grid
launched 2,258,060 lanes for 1,036,800 useful pairs. Rejected lanes performed
reuse-buffer reads and transform work before exiting, while valid warps loaded
unrelated screen records on both endpoints. The leader also rediscovered its
partner inside the reciprocal evaluator.

UPT-42 now uses the ordinary 8x8 full-screen dispatch. Every pixel performs
one transformed delta lookup. An out-of-screen partner publishes unchanged
fallback; otherwise the higher linear endpoint exits and the lower endpoint
publishes both reciprocal mappings using the already-resolved partner. Pair
anchor lists, padded tile enumeration, the 64x1 specialization and duplicate
leader delta lookup are removed. The immutable upload shrank from 969,096 to
646,064 bytes and still has no per-frame rebuild or clear.

The proposed seam explanation was separately disproved. A reproduction of the
exact map builder and shader transpose/mirror/offset transform checked
65,132,602 in-screen relationships over 32 frame transforms with zero
involution failures. The build verifier now requires SPIR-V LocalSize 8 8 1,
screen-leader ownership and absence of the old pair-list symbols.

The required Vulkan build passes. UPT-42 remains opt-in and UPT-33 remains the
production default. Runtime acceptance must require
`groups=8x8-screen-leaders reuseTexturePairing=1`, then compare warmed S0 GPU
time and stressed motion against the original 11 ms pair-list result. Do not
begin boiling-filter work until this corrected temporal/spatial stage proves
usable. No DX12 shader was compiled.

The corrected executable and only the two rebuilt Vulkan UPT-42 artifacts were
deployed:

    RBDoom3BFG.exe
        bytes=16361984
        sha256=F42DCA588F459DAF35A45467C596486049E1AC441CD2946B2487E0FF2F75123C
    upt09_spatial_shared_reuse_texture_pair_rescue_stored_target_light64.bin
        bytes=1072916
        sha256=EE574B7438DBD46BB8A1665B8615C33C1A362F1988D4FDB1D8F9EED238B3AE2E
    upt09_spatial_shared_reuse_texture_pair_rescue_stored_target_light64_three_vertex.bin
        bytes=1464244
        sha256=18956A32B3775239E34D3CA4C93B9A447DA9215FC28A70AD8D65BA50BDBAE6F4

UPT-43 bounded endpoint reuse pivot, 2026-08-12
------------------------------------------------

The corrected screen-ordered UPT-42 fixed the stressed-motion image artifacts
but improved performance by only one or two frames. At 1920x1080 the current
UPT temporal plus spatial cost remains about 220 percent above the ReSTIR PT
Enhanced reference result. At the normal resolution, emissive scale zero can
increase total frame rate by about 50 percent. Pairing and filtering work is
therefore paused until the mapping itself is bounded.

The local RTXDI FullSample PT audit identifies the missing base architecture.
Its 64-byte reservoir stores the reconnection endpoint position, normal,
outgoing radiance, partial Jacobian, incident-direction PDF, path/reconnection
length and replay seed. It invokes random replay only when RcVertexLength > 2.
Ordinary x2 reconnection is trace-free, and a BSDF-hit emissive reuses cached
endpoint radiance without resolving its triangle, material, UV or texture.
NEE reuses one stored light identity/sample coordinate pair through a single
dense frame remap rather than sampling or searching the light universe.

Document 58 now owns the pivot. The implementation order is: persist the
already-computed direct-emissive four-region PDF scale; add exact cached
endpoints; make x2 reconnection the trace-free common path; compact only later
random-replay prefixes; split paired spatial into an Enhanced-style shift
prepass and resampling pass; then restore the remaining Enhanced quality
layers. Boiling filtering is not the next task because it cannot repair an
unbounded reuse mapping.

The first slice is deliberately estimator-preserving. D0 already computes the
receiver-invariant direct-emissive region PDF scale; secondary emissive NEE
already stores the same value in the negative proposalPdf union. UPT-43.1
extends that established payload to direct emissives and removes four emitter
texture probes from every direct T0/S0 mapping. It must survive D0, T0 and S0
publication, reject malformed payloads, bump the history ABI, and retain the
selected endpoint texture evaluation until the exact endpoint-cache slice.

UPT-43.1 implementation and deployment, 2026-08-12
---------------------------------------------------

UPT-43.1 is implemented. Fresh and previous-best direct D0 proposals preserve
their exact four-region emissive PDF scale. D0 finalization, T0 direct shifts
and S0 direct shifts publish that scale through the collision-free negative
word12 emissive union while retaining the physical proposal PDF in pathPdf.
T0/S0 direct emissive replay now decodes the stored scale and performs only the
selected endpoint texture evaluation; the four region probes are absent.
Malformed emissive payloads fail closed. Analytic-light storage is unchanged.

UPT04_ABI_VERSION is 12, so incompatible history is rejected by page
generation without a full-buffer clear. The Vulkan release build passed and
reported all SPIR-V up to date after compilation. RBDoom3BFG.exe and exactly
the 21 Vulkan UPT blobs rebuilt since the build start were deployed. No DX12
shader was compiled or deployed. Runtime visual and timing acceptance is still
required before checkpointing or starting S43.2.

UPT-43.2 implementation and deployment, 2026-08-12
---------------------------------------------------

UPT-43.2 exact emissive geometry is built and deployed for runtime validation.
ABI 13 binds a 64-byte current-frame sidecar at set 0 binding 32. Static,
dynamic, rigid and world records are CPU-published; skinned records are updated
by the existing post-skinning GPU publisher and invalidated when unavailable.
Shared selected-emissive replay no longer calls the general geometry resolver.
The Vulkan build and descriptor/ray-ceiling verifiers pass. One executable,
one skinned publication compute blob and exactly 18 UPT D0/T0/S0 Vulkan blobs
were deployed. No DX12 shader was compiled or deployed.

Test with the existing `exec uptf2.cfg` baseline; no S43.2 cvar is required.
Check moving/animated emissives, portal crossings, textured small emitters,
temporal motion and spatial reuse. The checkpoint remains deferred until
runtime acceptance.

UPT-43.2 runtime acceptance, 2026-08-12
---------------------------------------

Runtime validation reported no visual issues. With `uptf2.cfg` at 2560x1440,
temporal dispatch timing was 11.100 ms median / 11.102 ms mean over 64 samples
after 16 warm-up frames. This is 3.579 ms (24.4 percent) below the preceding
14.679 ms S43.1 median. Shared spatial remained effectively flat at 1.072 ms
median versus 1.063 ms previously. S43.2 is accepted; checkpoint it before
starting S43.3 trace-free x2 reconnection.

S43.3 audit decision, 2026-08-12
---------------------------------

The old `TemporalEarlyReconnect` experiment is not the intended cached-x2
path. Its reservoir stored only a static triangle locator, so every mapping
ran the general geometry decoder, evaluated x2 source and target materials,
and performed receiver-to-x2 visibility before selection. It was also compiled
as a specialization mutually exclusive with the now-accepted shared adapter.
That combination explains its roughly 6 ms regression and must not be revived.

The accepted implementation plan is now fixed in document 58. The 64-byte
secondary-NEE rcLength=2 arm will store x2 position, packed normal, and cached
suffix radiance directly. Temporal use requires CPU-built light remap plus a
stable current payload; spatial use stays in the current universe. Ordinary
cached-x2 mapping performs no continuation trace and no x2 geometry/material
decode. Exact replay remains the fallback for dynamic x2, unstable lights and
all unsupported paths. Two transient winner-only visibility requests prove
receiver-to-x2 and x2-to-light without adding a persistent buffer or any
pre-selection traversal.

UPT-43.3 implementation and deployment, 2026-08-12
---------------------------------------------------

UPT-43.3 cached x2 reconnection is built and deployed for runtime validation.
ABI 14 stores stable static x2 position, packed normal and suffix radiance in
the existing 64-byte reservoir. T0/S0 now shift these samples by evaluating
only the current primary receiver; continuation traversal and x2
geometry/material reconstruction are absent from this common route. Exact
replay remains the fail-closed fallback for unsupported endpoints.

Both visibility segments remain required, but only receiver-to-x2 is carried
through mapping. The x2-to-light request is reconstructed and traced only for
the selected winner, avoiding a second request in every transient/groupshared
proposal record. The Vulkan build and exact RayQuery/TraceRay verifiers pass.
RBDoom3BFG.exe and exactly 21 rebuilt UPT04/05/07/09 Vulkan blobs were
deployed; no DX12 shader was compiled or deployed.

Test with `exec uptf2.cfg` and keep the obsolete
`r_pathTracingUnifiedPtTemporalEarlyReconnect` experiment at 0. First check
motion, portal boundaries, emissive color/position, static props and mirrors.
Then capture the same 16-warm-up/64-sample temporal and spatial timings used
for S43.2. Acceptance requires temporal to improve below 11.100 ms without a
material spatial regression above the accepted 1.072 ms median.

UPT-43.3 first runtime result, 2026-08-12
-----------------------------------------

Image correctness passed, but temporal performance regressed to 11.787 ms
median at 2560x1440 from S43.2's accepted 11.100 ms (+0.687 ms, +6.2%).
Spatial remained effectively flat at 1.096 ms versus 1.072 ms. Do not
checkpoint or promote S43.3 yet.

The production-shaped probe-12 specialization now reports cached-x2 attempted,
admitted, replay-fallback and selected-winner counts in addition to its
existing per-pixel work ledger. This diagnostic compiles away from production.
Run it at the same fixed view before modifying visibility policy or the
estimator again.

UPT-43.3 corrected performance result, 2026-08-12
-------------------------------------------------

The route ledger showed 99.2 percent cached-x2 admission and 260374 selected
cached winners. The regression was the redundant x2-to-light trace performed
for every such winner. Cached suffix radiance begins at x2 and already owns
that proven suffix; only the shifted receiver-to-x2 prefix needs current
visibility. Removing the suffix trace restored the original static RayQuery
ceilings.

With the complete `uptf2.cfg` production gates effective at 2560x1440, the
corrected temporal median is 9.994 ms, down 1.106 ms / 10.0 percent from the
accepted S43.2 11.100 ms. Spatial is 1.072 ms, exactly matching S43.2. The
faulty two-ray S43.3 temporal result was 11.787 ms. Performance acceptance now
passes; checkpoint only after visual/motion validation of this corrected
build.

UPT-43.3 final acceptance, 2026-08-12
-------------------------------------

Corrected-build visual and motion validation passed without issue. S43.3 is
accepted at 9.994 ms temporal / 1.072 ms spatial median at 2560x1440, a 10.0
percent temporal improvement over S43.2 with no spatial regression. Proceed to
bounded replay-prefix compaction only after the scoped S43.3 checkpoint.

UPT-43.4 implementation and deployment, 2026-08-12
---------------------------------------------------

Temporal replay compaction is built and deployed behind the default-off
`r_pathTracingUnifiedPtTemporalReplayCompaction` gate. T0a keeps direct and
cached-x2 shifts in the coalesced 8x8 screen pass and appends only at the exact
continuation-replay boundary. T0b reloads authoritative state and runs the
unmodified exact path over a uint pixel queue via 64x1 compute-indirect.

The queue is never cleared. The CPU resets only 20 bytes of dispatch/meta state
per frame; shader reads are bounded by min(publishedCount, capacity), and
overflow leaves fresh D0 intact. The timestamp scope includes T0a, barriers
and T0b. Static SPIR-V inspection reports two RayQuery sites for the classifier
and four for the compact consumer versus four in the monolithic oracle; the
three-vertex pair reports two/six.

Enable optional aggregate proof with
`r_pathTracingUnifiedPtTemporalReplayCompactionDiagnostics 1`. Runtime visual,
motion, count and timing validation are still required before checkpoint or
default promotion.

UPT-43.4 first runtime performance result, 2026-08-12
-----------------------------------------------------

The complete accepted gate reported 1 in all eleven positions. At 2560x1440,
16 warm-ups and 64 dispatch-only samples, compaction reduced temporal median
time from 10.348 ms to 3.043 ms (-7.305 ms / -70.6 percent). The queue contained
127816 of 3686400 pixels (3.47 percent), dispatched exactly 1998 expected 64x1
groups and reported zero overflow / PASS.

The performance and bounded-work proof pass. Do not checkpoint or promote the
default yet: visual and motion parity still need explicit confirmation for this
exact compacted build.

UPT-43.4 final acceptance, 2026-08-12
-------------------------------------

Visual and motion validation passed without an observed issue on the same
compacted build. UPT-43.4 is accepted and ready for a scoped checkpoint:
3.043 ms temporal median, 70.6 percent below its 10.348 ms non-compacted A/B,
3.47 percent queued replay density, exact group count and zero overflow.

UPT-43.5 spatial split ready for runtime A/B, 2026-08-12
--------------------------------------------------------

The default-off Gaussian spatial experiment now separates mapping from merge:
`r_pathTracingUnifiedPtSpatialShiftPrepass 1` runs a coalesced full-screen
8x8 S0a mapping prepass, writes one fully overwritten 16-byte scalar record per
pixel, inserts a UAV barrier and runs the reciprocal S0b merge/publication
pass. Neighbor candidate reconstruction and visibility occur only if that
proposal wins; canonical winners remain scalar-only. The persistent mapping
buffer is never cleared.

The gate additionally requires `r_pathTracingUnifiedPtSpatialReuseTexture 1`
and the accepted shared/stored-target/workgroup/empty-rescue chain. It rejects
multi-neighbor and three-vertex configurations. Static Vulkan verification
reports shift/resample RayQuery sites 1/6 versus 9 for the rejected monolithic
Gaussian kernel. The complete release build passes. Deploy and compare the
same view with shift prepass 0 then 1 while reuse texture stays 1; acceptance
requires visual parity and a material reduction from the prior roughly 11 ms
Gaussian spatial result. A zero setting retains the accepted UPT-33 path.

UPT-43.5 first runtime performance result, 2026-08-12
-----------------------------------------------------

At 2560x1440 the effective split reduced the Gaussian spatial median from
8.316 ms to 3.370 ms (-4.946 ms / -59.5 percent) and mean from 8.337 ms to
3.383 ms. The 3686400-entry mapping buffer is 58982400 bytes and is overwritten
without a clear. Performance acceptance passes. Visual and motion parity are
still required before checkpoint; do not confuse this with default promotion,
because accepted adjacent UPT-33 spatial remains around 1.07 ms.

UPT-43.5 final acceptance, 2026-08-12
-------------------------------------

Visual and motion testing found no issue in the exact timed build, including
portal/area transitions, disocclusion and bright emissive boundaries. S43.5 is
accepted and ready for checkpoint at 3.370 ms versus 8.316 ms Gaussian spatial
median (-59.5 percent). Keep the experiment default-off; this validates the
paper-shaped split, but does not yet justify replacing the approximately
1.07 ms adjacent UPT-33 production default.

UPT-43.6 temporal boiling filter ready for runtime A/B, 2026-08-12
------------------------------------------------------------------

The NVIDIA FullSample-shaped post-temporal boiling filter is implemented,
Vulkan-built and deployed behind
`r_pathTracingUnifiedPtTemporalBoilingFilter 1`. It requires effective S43.4
replay compaction and runs after T0b so queued pixels are finalized before the
8x8 group comparison. The pass is ray-free and owns only the current reservoir
page at binding 0.

UPT word11 already contains target-scaled UCW; the shader correctly consumes
it directly. Strength 0.2 produces the reference 41x nonzero-group-average
threshold. Rejected records are completely zeroed, including M and history
age. The temporal timer includes the filter cost.

Run the accepted `uptf2.cfg` baseline, explicitly set
`r_pathTracingUnifiedPtTemporalReplayCompaction 1`, measure filter 0, then set
filter 1 and strength 0.2 and measure again. `uptf2.cfg` does not itself enable
the required S43.4 compaction gate. Check motion, disocclusion, portal/emissive
boundaries, 8x8 dark holes and temporal firefly persistence. Do not enable by
default until image and incremental-cost acceptance are recorded. See
`60_upt43_temporal_boiling_filter.txt`.

UPT-43.6 final acceptance, 2026-08-13
-------------------------------------

The effective gate reported 1/1 with replay compaction active, strength 0.2,
multiplier 41 and the intended 6068-byte shader. Visual validation passed. At
2560x1440, 16 warm-ups and 64 dispatch-only samples, enabling the filter moved
temporal median time from 3.266 to 3.339 ms: +0.073 ms / +2.2 percent. S43.6
passes correctness and performance acceptance and is ready for a scoped
checkpoint. It remains default-off because its rejection is deliberately
biased; default promotion belongs to a later preset decision.

UPT-43.7 temporal permutation ready for build/runtime A/B, 2026-08-13
---------------------------------------------------------------------

An exact NVIDIA PT first-history-tap permutation is implemented behind
`r_pathTracingUnifiedPtTemporalPermutationSampling 1`, default 0 like RTXDI
PT. It uses JenkinsHash(frameIndex) and the shifted 4x4 XOR-by-3 map. Only
probe 0 changes; the eight fallback probes, signed bounds, surface validation,
reservoir selection, replay compaction and visibility remain unchanged.

The toggle occupies free temporal control bit 21 and introduces no shader
matrix, ABI change, buffer, clear, dispatch or ray. The focused verifier keeps
the S43.4 classifier at exactly two RayQuery sites and zero TraceRay operations.
The full Vulkan release build passed; the executable and ten rebuilt temporal
SPIR-V blobs were deployed with exact source/deployed SHA-256 matches. No DX12
shader was built.

Runtime timing rejected promotion. At 2560x1440 over 64 dispatch-only samples,
permutation 0 measured 3.570 ms median and permutation 1 measured 3.794 ms:
+0.224 ms or +6.3 percent. requested/effective was 1/1 and the zero-extra-ray
diagnostic was present. Retain default 0 and do not add the toggle to production
configs. The implementation remains available only as a bounded diagnostic A/B
specified in `61_upt43_temporal_permutation_sampling.txt`.

UPT-43.8 sparse disocclusion boost plan, 2026-08-13
----------------------------------------------------

The next implementation is not a revival of rejected UPT-34. RTXDI PT boosts
spatial samples only for rcLength-two reservoirs whose effective M is below
eight, and explicitly avoids longer replay paths. UPT can test that exact
condition from its packed reservoir without using age as a proxy.

S43.8 keeps UPT-33 as the ordinary full-screen pass, appends only triggered
pixels, and runs a bounded 64x1 simultaneous-GRIS compact side lane over them.
It reuses the accepted S43.4 queue/meta/indirect buffers after temporal, resets
20 bytes, reads donors from the immutable temporal page and overwrites only
queued pixels in the completed spatial output page. No new full-screen buffer
or clear is permitted. Full controls, gates, estimator rules and acceptance
criteria are in `62_upt43_sparse_disocclusion_boost.txt`.

UPT-43.8 ready for runtime A/B, 2026-08-13
-------------------------------------------

The default-off sparse disocclusion lane is implemented and deployed. It
classifies only non-rescue x2 samples with effective M below eight during the
accepted UPT-33 pass, then uses a 64x1 indirect compact dispatch over the
reused S43.4 queue. Ordinary pixels retain UPT-33 output and queued failures
are fail-closed without overwriting it.

The focused verifier and full `win64-pt-dev-release` build pass. Static proof
includes exact classification, canonical and donor duplicate exclusion,
winner-only visibility, immutable input/output separation, bounded queue
dispatch, 5/4 RayQuery-site ceilings and no TraceRay/native16. The executable
and exactly eight changed UPT-09 Vulkan blobs were deployed; no DX12 shader
was built. Runtime acceptance commands and diagnostics are in document 62.

UPT-43.8 runtime rejection, 2026-08-13
--------------------------------------

The implementation behaved exactly as bounded: it queued 199470/3686400
pixels (5.41 percent), dispatched 3117/3117 groups, overflowed zero entries
and reported PASS. Spatial time at 2560x1440 increased from 1.063 ms to 3.735
ms (+2.672 ms), while visual testing found no observable effect on temporal
ghosting. Retain the code only as a default-off reference diagnostic. The
ghosting investigation should next target temporal history validation,
shift/replay acceptance and the final temporal consumer, not additional
spatial sample density.

UPT-43.9 temporal receiver validation correction, 2026-08-13
-------------------------------------------------------------

The temporal gate audit confirmed that UPT admitted normals down to dot=0.35,
looser than the RTXDI PT/GI default of 0.6, while omitting even the surface-
class boundary already enforced by UPT spatial reuse. The bounded correction
now requires equal compact-sidecar surface classes and dot(normal)>=0.6.

The proposed direct instance comparison was not implemented: production
compact32 history does not transport `instancePrimitiveObject`, so that field
reconstructs as zero and cannot supply the claimed free identity check. The
relative ten-percent depth gate matches the reference default; no uncalibrated
absolute Doom-unit cap was added. `uptf2.cfg` already enables the nine-probe
temporal search. Focused harness/build proof and runtime motion acceptance are
recorded in document 63. The focused harness and full Vulkan release build
pass; the executable and exactly ten rebuilt UPT-07 Vulkan blobs were deployed
with matching hashes. Runtime motion acceptance remains pending.

Runtime testing found a small visible improvement with no reported regression.
Accept and retain the surface-class/0.6 gate, then continue feature completion.
Defer stable-instance transport and any absolute depth cap unless final-feature
testing leaves a repeatable material ghosting case.

UPT-44 production preset ready for runtime validation, 2026-08-13
-----------------------------------------------------------------

`exec upt` now selects the accepted compact D0, common-GRIS unified temporal,
S43.4 replay compaction, S43.6 boiling filter and UPT-33 spatial base in one
self-contained configuration. Production DLSS RR validation additionally
requires S43.7 temporal permutation and the Gaussian reuse texture with S43.5
split spatial execution; these are enabled despite their isolated timing cost
because the cheaper topology does not handle motion adequately. The preset
explicitly disables multi-neighbor, sparse-disocclusion and early-reconnect
experiments, optional three-vertex transport, and every timing/probe control so
stale console state cannot contaminate production testing.

This task changes configuration and documentation only; it adds no shader,
resource, dispatch, ray, clear, estimator or ABI. Runtime activation and visual
acceptance remain pending. Exact expected requested/effective and compiler-line
proof is in document 64.

UPT-45 S45.1 compact clear-window publication, 2026-08-13
----------------------------------------------------------

The clear-window PSR ownership audit is recorded in document 65. The existing
clean transmission producer already owns bounded reflection/transmission
tracing, wide primary replacement, glass sidecars and RR guide policy, but it
did not update UPT's separate compact32 receiver/history streams. Dispatching
it unchanged would therefore give clean DI and RR the resolved behind-glass
surface while UPT D0 shaded the stale front pane.

S45.1 adds no buffer, clear, copy, ray or estimator work. The existing Vulkan
layout exposes the already allocated compact resources to the transmission
producer through clean-layout-local u97/u98 aliases; P0 and UPT D0 retain the
canonical u89/u90 ABI. A successful PSR publish packs the exact accepted
surface into the same compact32 receiver and packs its sidecar from the exact
wide record written to u30. Compact writes are guarded by reserved producer
status 3; the clean-DI route remains status 2 and pays no added traffic.

The compact packer now takes camera position explicitly, removing its hidden
dependency on the primary producer's `CameraOriginAndTMax` symbol. Both the
shared primary producer and glass producer use the same implementation. The
full Vulkan `win64-pt-dev-release` build passes. S45.2/S45.3 remain: extract a
shared material-feature pass context, dispatch the producer before UPT D0,
and dispatch only glass composition after R0 and before optional RR. Do not
duplicate the large clean binding table inside the UPT branch.

UPT-45 Vulkan descriptor collision correction, 2026-08-13
----------------------------------------------------------

The first runtime launch after S45.2/S45.3 failed while creating the clean
material-feature layout: clean glass inputs already occupy t89/t90, while the
new compact publication aliases were initially declared as u89/u90. Vulkan's
NVRHI layout uses zero SRV/UAV offsets, so both resource classes mapped to
descriptor bindings 89/90. The producer aliases, clean layout and both clean
binding sets now consistently use unused u97/u98 while referencing the exact
same compact buffers. This is an adapter-slot correction only; the canonical
UPT P0/D0 u89/u90 contract and buffer ownership are unchanged.

UPT-45 unreachable pipeline gate correction, 2026-08-13
---------------------------------------------------------

Runtime then reported every printed prerequisite as one except the final
pipeline bit, with no material-feature shader-load attempt. The hidden cause
was an impossible dependency on the four clean-DI reservoir pages: the UPT
route checked them before pipeline creation, while their allocator lives later
in the mutually exclusive clean-DI route. The UPT glass entry points do not
read those buffers. Their shared-layout u69-u72 descriptors now use the
persistent status UAV as a non-accessed fallback, with reservoirCount zero so
any accidental access fails closed. No clean-DI reservoir pages are allocated
for UPT glass. The diagnostic now prints fallbackUav and true pipeline-ready
states separately instead of labeling the aggregate effective state as the
pipeline result.

UPT-45 S45.2/S45.3 opt-in pass graph ready for runtime validation, 2026-08-13
-----------------------------------------------------------------------------

The default-off `r_pathTracingUnifiedPtGlassPsr` route now dispatches the
existing clean transmission producer between shared P0 and UPT D0, then
dispatches only the existing glass composer after R0 and before optional
DLSS-RR. The producer and composer use a UPT-owned material-feature pipeline
state compiled against the proven clean layout, preventing cross-layout
pipeline reuse. No UPT reservoir, candidate, replay or resolve estimator math
changed.

The S45.1 audit contained one corrected assumption: compact producer mode 3
does not also write the 176-byte primary record. Effective UPT-45 therefore
selects explicit mode 4, which retains compact32 plus sidecar publication and
adds a wide companion record solely for the proven glass producer. UPT
D0/T0/S0 remain compact32 consumers. The already allocated wide companion is
copied to N-1 only while UPT-45 is effective so sticky glass-lane validation
has coherent history.

The late composer reads R0 directly at canonical t89 and writes the existing
presentation/RR outputs. Under producer status 3 it forwards non-glass R0
pixels, avoiding a full-screen R0 copy. Producer sidecars are initialized for
every pixel by the existing raygen, so no full-frame clear was added.
The effective gate accepts D0-only mode with current compact32/current sidecar;
compact N-1 buffers are required only when temporal or spatial reuse is live.

The full Vulkan `win64-pt-dev-release` build passes and rebuilt exactly the
glass composer, transmission producer and two primary-producer SPIR-V blobs.
Runtime acceptance remains required before any default promotion. Start with
`exec upt; r_pathTracingUnifiedPtGlassPsr 1`; expected host proof is
`glass PSR requested/effective=1/1` and Nsight markers `UPT.PSR ClearWindow`
before D0 plus `UPT.GlassCompose` after R0. Validate no-glass opacity, clear
panes, mirrors, motion/history, RR guides and timing; gate 0 must preserve the
accepted UPT baseline.
UPT-45 authority correction: native Slang PSR, 2026-08-13
---------------------------------------------------------

Runtime continued to report the final UPT glass pipeline gate as false after
the descriptor-collision and unreachable-reservoir corrections. The attempted
clean-DI adapter is rejected rather than widened again. `65` now requires the
accepted older clear-glass behavior to be rebuilt in a UPT-owned Slang file
against compact32 plus its history sidecar. Clean-DI reservoirs, ReGIR, the
176-byte companion receiver, global feature registry and HLSL producer layout
are explicitly forbidden dependencies.

The RTXDI Full Sample Nsight ordering was also resolved. Its final `Glass`
marker is a separate transparent forward/composition pass after ReSTIR PT,
denoising and opaque composition. Its actual PSR guide data is produced inside
the earlier PT initial/reuse path. UPT-45 therefore retains a native receiver
PSR before D0 and a distinct native appearance composition after R0.

Because prior UPT profiling repeatedly found occupancy collapse and serialized
loads caused by fat records and long live ranges, UPT-45 now has an explicit
GPU-footprint gate. The new producer must use narrow field loads, defer identity
and output reconstruction until after traversal, report reflected dependencies,
SPIR-V size and RayQuery sites, and be accepted with Nsight registers/thread,
occupancy, No Instructions and Long Scoreboard evidence. No further work should
attempt to make the old adapter's final pipeline gate pass.

S45.1 then audited the native compact contract in `66`. Compact32 plus its
32-byte sidecar contains every post-replacement D0/T0/S0 fact; no wide companion
is required. Stable material IDs are sparse 32-bit FNV hashes, so the front is
classified from sidecar flags with no identity translation; the committed back
hit gets its dense index directly from its route. The general UPT decoder is
also forbidden in the native producer: its three 112-byte vertices and
356/124-byte logical triangle/surface values would recreate the register
failure. The first native appearance payload is limited to one overwritten
RGBA16F token (8 bytes/pixel, no clear, no history).

UPT-45 native producer/composer checkpoint, 2026-08-13
-------------------------------------------------------

S45.2/S45.3 are now implemented as UPT-owned Vulkan Slang compute passes. The
pass order is `P0 -> native PSR producer -> D0/T0/S0/R0 -> native Glass compose
last -> optional RR`, matching the important distinction in the RTXDI Full
Sample capture: its final Glass marker is composition, while the replacement
surface must exist before reuse and shading.

The producer overwrites only compact32 plus the accepted sidecar and writes one
RGBA16F composition token. It performs one bounded RayQuery only for eligible
clear-window pixels, uses narrow word-level geometry/material reads and imports
neither the general UPT geometry decoder nor clean-DI state. The composer binds
only R0, token and the existing frame output. There is no full-buffer clear,
wide receiver, clean-DI reservoir, ReGIR binding or second private output.

The full requested Vulkan build passes. Runtime remains default-off under
`r_pathTracingUnifiedPtGlassPsr 1`; expected proof is requested/effective 1/1,
`UPT.PSR NativeClearWindow` before D0 and `UPT.Glass NativeCompose Last` after
R0. Validate clear panes, no-glass parity, motion/history and RR before Nsight
register/occupancy acceptance. Artifact hashes and the remaining profile gate
are recorded in `66`.
